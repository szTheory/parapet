---
phase: 26-audit-propagation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/operator.ex
  - lib/parapet/evidence/retrospective.ex
  - test/parapet/operator_test.exs
  - test/parapet/operator/preview_lifecycle_test.exs
  - test/parapet/evidence/retrospective_test.exs
autonomous: true
requirements: [AUD-01, AUD-02, AUD-03]

must_haves:
  truths:
    - "Querying TimelineEntry after a successful Confirm returns a row with type \"recovery_confirmed\" carrying actor, capability, target_refs, and an outcome map"
    - "Querying ToolAudit after the same Confirm returns a row with output set, action_name + target_refs in input, success: true, and the operator actor"
    - "A capability whose execute/2 returns {:error, reason} (or raises, normalized to {:capability_raised, msg}) produces a TimelineEntry with type \"recovery_failed\" and a ToolAudit with success: false, while confirm_runbook_step/4 still returns {:error, reason}"
    - "Short-circuit (preview_expired, target_refs_drift, incident_resolved) and conflict outcomes write NO TimelineEntry and NO ToolAudit"
    - "The retrospective generator renders recovery_confirmed and recovery_failed entries with human-readable copy inline in the chronology, not as raw inspect(payload) output"
  artifacts:
    - path: "lib/parapet/operator.ex"
      provides: "Enriched recovery_confirmed timeline+audit write in the {:ok} arm and new recovery_failed write in the {:error, reason} arm of confirm_runbook_step/4"
      contains: "recovery_failed"
    - path: "lib/parapet/evidence/retrospective.ex"
      provides: "Two format_payload/1 clauses for recovery_confirmed and recovery_failed payloads, ordered before the inspect/1 catch-all"
      contains: "Recovery failed"
    - path: "test/parapet/operator_test.exs"
      provides: "AUD-01/AUD-02 enriched-write assertions on the success Confirm test"
    - path: "test/parapet/operator/preview_lifecycle_test.exs"
      provides: "AUD-03 failure-write assertions (CR-02 error return, CR-03 raised) + short-circuit negative assertions"
    - path: "test/parapet/evidence/retrospective_test.exs"
      provides: "recovery_confirmed + recovery_failed rendering assertions"
  key_links:
    - from: "lib/parapet/operator.ex confirm_runbook_step/4 {:ok} arm"
      to: "Parapet.Evidence.run_operator_command/1"
      via: "enriched timeline_attrs + audit_attrs maps"
      pattern: "run_operator_command"
    - from: "lib/parapet/operator.ex confirm_runbook_step/4 {:error, reason} arm"
      to: "Parapet.Evidence.run_operator_command/1"
      via: "best-effort failure write BEFORE ClaimService.mark_failed"
      pattern: "recovery_failed"
    - from: "lib/parapet/evidence/retrospective.ex format_payload/1"
      to: "recovery_confirmed / recovery_failed timeline payloads"
      via: "pattern-matched clauses before the inspect/1 fallback"
      pattern: "outcome.*status"
---

<objective>
Make every operator Confirm leave a complete, durable evidence trail. Enrich the existing `recovery_confirmed` TimelineEntry + ToolAudit writes in the `{:ok}` success arm of `Parapet.Operator.confirm_runbook_step/4` with the full AUD-01/AUD-02 field set (operator identity, action name, args, outcome), add a NEW `recovery_failed` write in the `{:error, reason}` post-won arm (AUD-03), and add two `format_payload/1` rendering clauses to the retrospective generator so recovery actions surface inline in the canonical chronology.

This is the audit half of the Phase 25 Confirm path — it lands as ONE coherent PR: enriched success write + new failure write + retrospective display clauses + test assertions together.

Purpose: Close AUD-01, AUD-02, AUD-03 and ROADMAP success criterion #4. The durable spine must record what was done, by whom, and with what outcome — for both successful and failed recovery actions — regardless of which surface triggered it.

Output: Edits to `lib/parapet/operator.ex` and `lib/parapet/evidence/retrospective.ex`; extended assertions in three existing test files. NO new files, NO migration, NO `mix.exs`/`mix.lock` churn, NO `Ecto.Enum` conversion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/26-audit-propagation/26-CONTEXT.md
@.planning/phases/26-audit-propagation/26-RESEARCH.md
@.planning/phases/26-audit-propagation/26-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from live codebase — no exploration required. -->

`Parapet.Operator.confirm_runbook_step/4` — central edit target (lib/parapet/operator.ex:691-787).
In-scope bindings inside the post-won arms:
- `payload` — the `%ActionPayload{}` function arg (operator identity at `payload.actor`; also `payload.reason`, `payload.correlation_id`, `payload.idempotency_key`, `payload.action_type`).
- `capability_id` — atom, bound at line 704 (action name = `to_string(capability_id)`).
- `preview_entry` — the `%TimelineEntry{}` from `find_recent_preview/3`; `preview_entry.target_refs` is the args (may be nil for legacy previews → guard with `|| []`).
- `step_id_atom` — atom, bound at line 699.
- `{:ok, exec_result}` arm at lines 743-761 (existing thin write site).
- `{:error, reason}` arm at lines 763-768 (AUD-03 write site; `reason` includes the rescued `{:capability_raised, msg}` from lines 738-740).

`build_audit/2` (private, lib/parapet/operator.ex:990-1002) — current shape:
```
defp build_audit(tool_name, %ActionPayload{} = payload) do
  %{
    tool_name: tool_name,
    success: true,
    input: %{
      "actor" => payload.actor,
      "reason" => payload.reason,
      "correlation_id" => payload.correlation_id,
      "idempotency_key" => payload.idempotency_key,
      "action_type" => Atom.to_string(payload.action_type)
    }
  }
end
```
Missing today: `output` field; `action_name`/`target_refs` in `input`. Sets `success: true` only.

`Parapet.Evidence.run_operator_command/1` (lib/parapet/evidence.ex:126) — atomic Ecto.Multi seam.
Keyword opts: `incident_changeset:`, `timeline_attrs:`, `audit_attrs:`.
Builds: `update(:incident)` → `insert(:timeline_entry)` → `insert(:tool_audit)` → `run(:broadcast_audit)` (fires `[:parapet, :audit, :created]`).
Returns `{:ok, %{incident:, timeline_entry:, tool_audit:, broadcast_audit:}}` or `{:error, step_name, reason, changes_so_far}`.
In the SUCCESS arm its return IS the function's success return (tests assert `{:ok, result}` then `result.timeline_entry` / `result.tool_audit`).

`Parapet.Spine.ToolAudit` (lib/parapet/spine/tool_audit.ex:18-28) — `output: :map` is cast by `changeset/2` but unset today; `success: :boolean` required; `duration_ms: :integer` optional.

`Parapet.Spine.TimelineEntry` (lib/parapet/spine/timeline_entry.ex:33) — `field(:type, :string)`; `validate_typed_payload/1` (:49-59) only special-cases `"triage_snapshot"`, everything else passes through.

`Parapet.Evidence.Retrospective.format_payload/1` (lib/parapet/evidence/retrospective.ex:140-148) — clause chain ending in `defp format_payload(payload), do: inspect(payload)`. `format_type/1` (:136) already humanizes type strings ("recovery_confirmed" → "Recovery confirmed").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich the success and failure audit writes in confirm_runbook_step/4</name>
  <files>lib/parapet/operator.ex</files>
  <read_first>
    - lib/parapet/operator.ex (the function being modified — read 691-787 for both arms, and 990-1002 for build_audit/2)
    - lib/parapet/evidence.ex (run_operator_command/1 at 126-162 — confirm the keyword opts and return shape)
    - lib/parapet/spine/tool_audit.ex (confirm output/success/duration_ms field availability + changeset cast)
    - lib/parapet/automation/executor.ex (actor convention "system:automation:executor" — AUD-02 cross-surface precedent)
    - lib/parapet/escalation/worker.ex (inspect/1 normalization precedent ~line 100 and the error arm)
    - .planning/phases/26-audit-propagation/26-PATTERNS.md (Pattern Assignments for operator.ex success + failure arms)
  </read_first>
  <behavior>
    - Success arm ({:ok, exec_result}): TimelineEntry payload gains "actor" (= payload.actor), "capability" (= to_string(capability_id)), "target_refs" (= preview_entry.target_refs || []), and an "outcome" map %{"status" => "succeeded", "result" => inspect(exec_result)}; "step_id" is retained.
    - Success arm: ToolAudit gains output = %{"status" => "succeeded", "result" => inspect(exec_result)}, and input gains "action_name" (= to_string(capability_id)) + "target_refs" (= preview_entry.target_refs || []); success stays true; existing input keys (actor/reason/correlation_id/idempotency_key/action_type) are preserved.
    - Failure arm ({:error, reason}, post-won): a new write produces a TimelineEntry type "recovery_failed" with payload "actor"/"capability"/"target_refs"/"outcome" where outcome = %{"status" => "failed", "reason" => inspect(reason)}, AND a ToolAudit with success: false, output = %{"status" => "failed", "reason" => inspect(reason)}, and the same enriched input shape.
    - Failure arm: the write happens BEFORE ClaimService.mark_failed(claim, reason); its return is discarded; the arm STILL calls mark_failed and STILL returns {:error, reason} unchanged.
    - The rescued {:capability_raised, msg} reason flows through the same {:error, reason} arm and is normalized by inspect(reason) into the "recovery_failed" payload — no structured failure_class.
    - Short-circuit arms (:preview_expired, :target_refs_drift, :incident_resolved) and the {:conflicted, _} arm continue to write nothing.
  </behavior>
  <action>
    In `lib/parapet/operator.ex`, edit `confirm_runbook_step/4` (the post-won `case exec_outcome do` block, lines ~743-768).

    SUCCESS arm ({:ok, exec_result}, lines ~746-761):
    - Replace the `timeline_attrs` map's payload so it carries keys "step_id" (keep `to_string(step_id_atom)`), "actor" => `payload.actor`, "capability" => `to_string(capability_id)`, "target_refs" => `preview_entry.target_refs || []`, and "outcome" => `%{"status" => "succeeded", "result" => inspect(exec_result)}`. Keep `type: "recovery_confirmed"` as a STRING (D-01/D-02 — do NOT use the atom :recovery_confirmed; workbench_contract.ex:130 and three tests assert the string).
    - Enrich `audit_attrs` from `build_audit("operator_confirm_recovery", payload)` by piping: `|> Map.put(:output, %{"status" => "succeeded", "result" => inspect(exec_result)})` then `|> Map.update!(:input, fn base -> Map.merge(base, %{"action_name" => to_string(capability_id), "target_refs" => preview_entry.target_refs || []}) end)`. (Inline Option B from RESEARCH Pattern 2 — do not add a new build_audit arity unless you also apply it consistently to the failure arm.)
    - Leave the `Evidence.run_operator_command(...)` call and its return as the arm's last expression UNCHANGED (its `{:ok, %{...}}` return is the function's success return).

    FAILURE arm ({:error, reason}, lines ~763-767):
    - Build `failure_timeline_attrs` = `%{type: "recovery_failed", payload: %{"step_id" => to_string(step_id_atom), "actor" => payload.actor, "capability" => to_string(capability_id), "target_refs" => preview_entry.target_refs || [], "outcome" => %{"status" => "failed", "reason" => inspect(reason)}}}`.
    - Build `failure_audit_attrs` directly (do NOT reuse build_audit/2 then flip success — Pitfall 4): `%{tool_name: "operator_confirm_recovery", success: false, input: %{"actor" => payload.actor, "reason" => payload.reason, "correlation_id" => payload.correlation_id, "idempotency_key" => payload.idempotency_key, "action_type" => Atom.to_string(payload.action_type), "action_name" => to_string(capability_id), "target_refs" => preview_entry.target_refs || []}, output: %{"status" => "failed", "reason" => inspect(reason)}}`.
    - Call `Evidence.run_operator_command(incident_changeset: Ecto.Changeset.change(incident, %{}), timeline_attrs: failure_timeline_attrs, audit_attrs: failure_audit_attrs)` and DISCARD its result with `_ =` (Pitfall 2 — a failed audit write must NOT change the {:error, reason} return).
    - This write MUST come BEFORE `ClaimService.mark_failed(claim, reason)` (D-06 / Pitfall 6 canonical order: write_audit → mark_failed → return).
    - Keep `ClaimService.mark_failed(claim, reason)` and the final `{:error, reason}` return exactly as they are.

    Do NOT rebind the `payload` variable (Pitfall 1 — keep map vars named `timeline_attrs`/`audit_attrs`/`failure_*`). Do NOT touch the short-circuit cond arms (709-718), the `{:short_circuited, ...}` / `{:conflicted, ...}` claim arms (770-777), or the `confirm_runbook_step/4` return contract (D-11).
  </action>
  <verify>
    <automated>mix test test/parapet/operator_test.exs test/parapet/operator/preview_lifecycle_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `result.timeline_entry.type == "recovery_confirmed"` (string, unchanged) on the success Confirm.
    - `result.timeline_entry.payload["actor"] == payload.actor`, `payload["capability"] == "retry_async_item"`, `is_list(payload["target_refs"])`, `payload["outcome"]["status"] == "succeeded"`.
    - `result.tool_audit.success == true`, `result.tool_audit.input["action_name"] == "retry_async_item"`, `is_list(result.tool_audit.input["target_refs"])`, `result.tool_audit.input["actor"] == payload.actor`, `is_map(result.tool_audit.output)`, `result.tool_audit.output["status"] == "succeeded"`.
    - CR-02 still returns `{:error, :provider_unavailable}` and CR-03 still returns `{:error, {:capability_raised, message}}` with `message =~ "boom from host"` (return contract preserved — D-11).
    - `grep -v '^#' lib/parapet/operator.ex | grep -c 'recovery_failed'` returns >= 1 (the new failure-arm type literal is present).
    - No `mix.exs` / `mix.lock` diff; no new migration file under `priv/repo/migrations/`.
    - `mix compile --warnings-as-errors` passes (no unreachable/unused warnings — Pitfall 1/5).
  </acceptance_criteria>
  <done>
    The success arm writes the enriched recovery_confirmed TimelineEntry + ToolAudit; the post-won failure arm writes a recovery_failed TimelineEntry + ToolAudit (success: false) before mark_failed and still returns {:error, reason}; short-circuit/conflict arms unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add recovery_confirmed and recovery_failed render clauses to the retrospective</name>
  <files>lib/parapet/evidence/retrospective.ex</files>
  <read_first>
    - lib/parapet/evidence/retrospective.ex (read 110-149 — format_entry/1, format_type/1, and the full format_payload/1 clause chain)
    - .planning/phases/26-audit-propagation/26-PATTERNS.md (retrospective.ex Pattern Assignment — clause copy + ordering)
    - .planning/phases/26-audit-propagation/26-CONTEXT.md (D-07 — cosmetic-only; NO whitelist/filtering logic)
  </read_first>
  <action>
    In `lib/parapet/evidence/retrospective.ex`, insert TWO new `defp format_payload/1` clauses IMMEDIATELY BEFORE the generic catch-all `defp format_payload(payload), do: inspect(payload)` at line ~146 (Pitfall 5 — clauses after the catch-all are unreachable dead code).

    Clause 1 (success): match `%{"capability" => cap, "actor" => actor, "outcome" => %{"status" => "succeeded"}} = p`, bind `target_refs = Map.get(p, "target_refs", [])`, return a human-readable line such as `"#{cap} confirmed by #{actor} on #{inspect(target_refs)}"`.

    Clause 2 (failure): match `%{"capability" => cap, "actor" => actor, "outcome" => %{"status" => "failed", "reason" => reason}}`, return a line beginning with the distinct phrase "Recovery failed:" — e.g. `"Recovery failed: #{cap} by #{actor} — #{reason}"`.

    Exact copy is your discretion (D-07) but the failure clause MUST contain the literal substring "Recovery failed:" and the success clause MUST NOT, so the two render distinctly. Do NOT add any `where`/whitelist filtering to `generate_markdown/1` (D-07 — the query has no type whitelist and recovery entries already flow in for free; format_type/1 already humanizes the type label).
  </action>
  <verify>
    <automated>mix test test/parapet/evidence/retrospective_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - For a recovery_confirmed entry payload `%{"capability" => "retry_async_item", "actor" => "ops@example.com", "target_refs" => ["job-1"], "outcome" => %{"status" => "succeeded"}}`, `Retrospective.generate_markdown/2` output contains `"retry_async_item confirmed by ops@example.com"` (or equivalent copy naming cap + actor) and does NOT contain `%{` for that entry.
    - For a recovery_failed entry payload with `"outcome" => %{"status" => "failed", "reason" => ":provider_unavailable"}`, output contains the literal `"Recovery failed:"`.
    - `grep -v '^#' lib/parapet/evidence/retrospective.ex | grep -c 'Recovery failed:'` returns >= 1.
    - The two new clauses appear before the `inspect(payload)` catch-all (verify by line order: the catch-all is the LAST format_payload clause).
    - `mix compile --warnings-as-errors` passes (no unreachable-clause warning).
  </acceptance_criteria>
  <done>
    Recovery entries render with human-readable, success-vs-failure-distinct copy inline in the chronology; the inspect/1 fallback no longer fires for recovery payloads; no filtering logic added.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests for AUD-01/02 enrichment, AUD-03 failure writes, and negative cases</name>
  <files>test/parapet/operator_test.exs, test/parapet/operator/preview_lifecycle_test.exs, test/parapet/evidence/retrospective_test.exs</files>
  <read_first>
    - test/parapet/operator_test.exs (read the success Confirm test ~line 560-600 — the `{:ok, result}` + `%TimelineEntry{type: "recovery_confirmed"}` assertion at :593-594)
    - test/parapet/operator/preview_lifecycle_test.exs (read 90-203 for the DummyRepo transaction/1 harness + setup; 300-368 for the short-circuit-resolved, CR-02, and CR-03 tests)
    - test/parapet/evidence/retrospective_test.exs (read the existing "generates markdown retrospective" test — the generate_markdown(incident, entries) call pattern)
    - .planning/phases/26-audit-propagation/26-RESEARCH.md (Test Observable Signals + Validation Architecture — exact assertion shapes)
    - .planning/phases/26-audit-propagation/26-PATTERNS.md (the three test-file Pattern Assignments)
  </read_first>
  <action>
    Extend the existing test modules ONLY — create NO new test files (VALIDATION.md Wave 0 = none).

    A) `test/parapet/operator_test.exs` — after the existing success-Confirm assertion (`%TimelineEntry{type: "recovery_confirmed"} = result.timeline_entry`, :594), add AUD-01 assertions on `result.timeline_entry.payload` (`["actor"] == payload.actor`, `["capability"] == "retry_async_item"`, `is_list(["target_refs"])`, `["outcome"]["status"] == "succeeded"`) and AUD-02 assertions on `result.tool_audit` (`success == true`, `input["action_name"] == "retry_async_item"`, `is_list(input["target_refs"])`, `input["actor"] == payload.actor`, `is_map(output)`, `output["status"] == "succeeded"`). Use the capability id/target_refs that the test's registered capability actually returns — read the test setup to get the exact strings rather than assuming.

    B) `test/parapet/operator/preview_lifecycle_test.exs` — extend CR-02 (:314, execute → {:error, :provider_unavailable}) and CR-03 (:341, execute raises). Both must keep their existing return-shape asserts. Add AUD-03 verification that a `recovery_failed` write occurred. Because the failure-arm `run_operator_command/1` result is discarded (best-effort, Pitfall 2), the {:error, reason} return alone does NOT expose the write — capture it via the existing DummyRepo harness: extend `DummyRepo.transaction(multi)` (lines 99-127) to record applied `:timeline_entry` / `:tool_audit` inserts into a process-dict key (e.g. `Process.put(:captured_writes, ...)`), then assert in CR-02/CR-03 that a captured timeline insert has `type == "recovery_failed"` and a captured tool_audit has `success == false` with `output["status"] == "failed"`. Keep the change minimal and additive so the existing success/short-circuit tests still pass. For CR-03 confirm the captured recovery_failed payload's `outcome["reason"]` contains the inspected `{:capability_raised, ...}` string.
    Also add a NEGATIVE assertion to a short-circuit test (e.g. the :preview_expired test at :184 or the :incident_resolved test at :300): after asserting the `{:short_circuited, _}` return, assert that NO `recovery_confirmed` and NO `recovery_failed` insert was captured (the captured-writes process key is empty / has no recovery entry). Add the same negative check pattern for the conflict path if a conflict test exists in this file; if not, leave conflict coverage to operator_test.exs.

    C) `test/parapet/evidence/retrospective_test.exs` — mirror the existing markdown test: build an entry list containing a `recovery_confirmed` entry and a `recovery_failed` entry (payloads matching the Task 2 clause shapes, with distinct inserted_at timestamps so they render in chronological order), call `generate_markdown/2`, and assert `markdown =~ "retry_async_item confirmed by"` (success copy), `markdown =~ "Recovery failed:"` (failure copy), and `refute markdown =~ "%{"` (no raw inspect fallback). Confirm both recovery lines appear inline within the single chronological entry list (SC-4), not in a separate section.

    Do NOT change any existing string-type assertion to an atom; do NOT delete the existing regression-guard asserts.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_test.exs test/parapet/operator/preview_lifecycle_test.exs test/parapet/evidence/retrospective_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The success-Confirm test in operator_test.exs asserts all six AUD-01/AUD-02 enriched fields listed in action (A) and passes.
    - CR-02 asserts `{:error, :provider_unavailable}` return AND a captured `recovery_failed` timeline insert + `success: false` tool_audit; CR-03 asserts `{:error, {:capability_raised, message}}` return AND a captured `recovery_failed` write whose outcome reason reflects the raised message.
    - At least one short-circuit test asserts NO recovery_confirmed/recovery_failed write was captured (AUD-03 negative).
    - retrospective_test.exs asserts both `"... confirmed by ..."` and `"Recovery failed:"` appear and `refute markdown =~ "%{"`.
    - No new files under `test/` (the three modified test files are the only test diffs): `git status --porcelain test/ | grep -v '^.M ' | grep -c '^??'` returns 0 for test files.
    - Full targeted run is green: `mix test test/parapet/operator_test.exs test/parapet/operator/preview_lifecycle_test.exs test/parapet/evidence/retrospective_test.exs`.
  </acceptance_criteria>
  <done>
    All three test files extended (no new files); AUD-01/02 enrichment, AUD-03 success+raised failure writes, short-circuit negatives, and retrospective rendering are all covered and green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host capability → operator audit payload | The adopter-supplied `execute/2` closure returns `exec_result` (success) or `reason` (error), both untrusted values that get serialized into the audit `output`/payload maps. |
| operator-supplied input → audit identity | `payload.actor` (operator URN) crosses into the durable TimelineEntry/ToolAudit rows; already `validate_required` + `validate_not_blank` upstream in `ActionPayload.changeset/2`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | `exec_result` / `reason` injected into audit `output`/payload maps (control chars, crafted terms) | mitigate | Normalize via `inspect/1` (Elixir term notation, not raw user JSON) before storing; values land in `output: :map` / `payload: :map` JSONB fields which escape at the DB layer. No string interpolation into SQL. |
| T-26-02 | Information disclosure | Stack trace leaking into `recovery_failed` payload via `inspect(reason)` | mitigate | The rescue at `operator.ex:738-740` already normalizes exceptions to `{:capability_raised, Exception.message(e)}` — a single message string, NOT a full stacktrace. `inspect/1` of that tuple exposes only the host-provided message. |
| T-26-03 | Repudiation | Operator denies having confirmed a recovery action | mitigate | Every successful AND failed Confirm now writes `payload.actor` (operator URN) into both the TimelineEntry payload and the ToolAudit `input` — a durable, queryable who-did-what record consistent across operator and automation surfaces (AUD-02). |
| T-26-04 | Tampering (audit suppression) | Failure-path audit write silently dropped if DB write fails | accept | The failure-arm `run_operator_command/1` result is intentionally discarded (best-effort) so the original `{:error, reason}` return contract is preserved (D-06/D-11). A lost failure-audit row on DB outage is accepted: the capability error still surfaces to the operator, and the success path propagates DB errors normally. Low value/low likelihood; no PII beyond the operator URN already in scope. |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a — no package installs | Phase 26 adds NO dependencies and NO `mix.lock` churn (verified: RESEARCH.md "Package Legitimacy Audit — Not applicable"). No supply-chain checkpoint required. |

No HIGH-severity unmitigated threats. ASVS L1 relevant category is V5 Input Validation, already satisfied by `validate_required`/`validate_not_blank` on `payload.actor` and `inspect/1` term-serialization on opaque results.
</threat_model>

<verification>
- `mix test test/parapet/operator_test.exs test/parapet/operator/preview_lifecycle_test.exs test/parapet/evidence/retrospective_test.exs` — all green.
- `mix test` — full suite green (phase gate).
- `mix compile --warnings-as-errors` — no warnings (catches Pitfall 1 unused-`payload` and Pitfall 5 unreachable-clause regressions).
- `git diff --name-only` shows ONLY `lib/parapet/operator.ex`, `lib/parapet/evidence/retrospective.ex`, and the three test files — NO `mix.exs`/`mix.lock`, NO new `priv/repo/migrations/` file, NO new test file.
- Manual confirmation that `TimelineEntry.type` remains `field(:type, :string)` (no Ecto.Enum) and `workbench_contract.ex:130` string matcher is untouched.
</verification>

<success_criteria>
- AUD-01: A successful Confirm writes a TimelineEntry (`type: "recovery_confirmed"`) capturing operator identity (`actor`), action name (`capability`), args (`target_refs`), outcome map, and (auto) timestamps.
- AUD-02: The same Confirm writes a ToolAudit row with `output` set, `action_name` + `target_refs` in `input`, the operator `actor`, and `success: true` — consistent with the automation path's cross-surface identity convention.
- AUD-03: A capability `execute/2` returning `{:error, reason}` (or raising → `{:capability_raised, msg}`) produces a `recovery_failed` TimelineEntry + a `ToolAudit` with `success: false`, while `confirm_runbook_step/4` still returns `{:error, reason}`; short-circuit and conflict outcomes write nothing.
- Criterion #4: The retrospective generator surfaces recovery_confirmed/recovery_failed entries inline in the canonical chronology with human-readable copy (no raw `inspect` map dump), via two new `format_payload/1` clauses — no filtering logic added.
- Constraints honored: no migration, no `Ecto.Enum`, no `mix.exs`/`mix.lock` change, no new test files, no telemetry emit-site work, no change to the `confirm_runbook_step/4` return contract or dedup mechanism.
</success_criteria>

<output>
Create `.planning/phases/26-audit-propagation/26-01-SUMMARY.md` when done.
</output>
