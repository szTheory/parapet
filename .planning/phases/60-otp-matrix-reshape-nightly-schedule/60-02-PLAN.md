---
phase: 60-otp-matrix-reshape-nightly-schedule
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - .github/workflows/release-please.yml
  - README.md
  - CONTRIBUTING.md
  - .planning/PROJECT.md
  - .planning/milestones/v1.7-MILESTONE-AUDIT.md
  - .planning/MILESTONES.md
autonomous: true
requirements: [MATRIX-02, MATRIX-03]
must_haves:
  truths:
    - "release-please.yml publish-hex job builds on Elixir 1.20.2 / OTP 28.x, matching the toolchain CI now tests (D-06a)"
    - "README's factual 'CI validates...' sentence names the new set (no longer 'Elixir 1.19 across OTP 26, 27, and 28') (D-06b)"
    - "CONTRIBUTING has a 4th 'Local vs CI deltas' bullet explaining PR runs one trimmed cell and release_gate on main is the real multi-OTP gate (D-05b)"
    - "The v1.7 D-11 CI-coverage tech-debt flag is retired via a single canonical explanation in PROJECT.md Key Decisions plus one-line dated pointers elsewhere (D-04a/b/c, MATRIX-02)"
    - "The D-11 retirement is dated 2026-07-02 and append-only — the frozen v1.7 audit body is not rewritten (D-04a/c)"
  artifacts:
    - ".github/workflows/release-please.yml (publish-hex toolchain bumped)"
    - "README.md (factual CI sentence corrected)"
    - "CONTRIBUTING.md (4th Local-vs-CI-deltas bullet)"
    - ".planning/PROJECT.md (canonical D-11 resolution row)"
    - ".planning/milestones/v1.7-MILESTONE-AUDIT.md (register-row dated pointer)"
    - ".planning/MILESTONES.md (dated pointer)"
  key_links:
    - "PROJECT.md Key Decisions row -> single canonical D-11 explanation; all other spots point to it (anti-drift)"
---

<objective>
Land the adjacent toolchain + doc edits that accompany the `ci.yml` reshape: bump the `release-please.yml` publish-hex toolchain to match CI, fix the factually-stale README "CI validates..." sentence, add a CONTRIBUTING bullet explaining the PR-trimmed / main-full split, and formally retire the v1.7 D-11 CI-coverage tech-debt flag via a single canonical PROJECT.md explanation plus dated one-line pointers.

Purpose: Keep the published Hex artifact on a tested toolchain, keep support-surface claims honest, and close the D-11 tech-debt flag single-sourced and append-only so no location drifts.

Output: Updated `release-please.yml`, `README.md`, `CONTRIBUTING.md`, `PROJECT.md`, `v1.7-MILESTONE-AUDIT.md`, `MILESTONES.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-otp-matrix-reshape-nightly-schedule/60-CONTEXT.md
@.github/workflows/release-please.yml
@README.md
@CONTRIBUTING.md
@.planning/milestones/v1.7-MILESTONE-AUDIT.md
@.planning/MILESTONES.md
</context>

<artifacts_produced>
This plan creates the following new symbols/records:
- A bumped `publish-hex` toolchain pin in `release-please.yml` (Elixir 1.20.2 / OTP 28.x; setup-beam SHA unchanged).
- A corrected factual CI sentence in `README.md`.
- A 4th "Local vs CI deltas" bullet in `CONTRIBUTING.md`.
- A canonical D-11 resolution row in `PROJECT.md` Key Decisions plus an annotation of the existing `⚠️ Revisit` v1.7-debt row.
- Dated one-line D-11 pointers in `v1.7-MILESTONE-AUDIT.md` (register row) and `MILESTONES.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bump release-please publish-hex toolchain, fix README CI sentence, add CONTRIBUTING delta bullet</name>
  <files>.github/workflows/release-please.yml, README.md, CONTRIBUTING.md</files>
  <read_first>
    - .github/workflows/release-please.yml (publish-hex job setup-beam pin ~lines 58-62 — VERIFY live line numbers)
    - README.md (support matrix ~line 22-25 and the factual "CI validates..." sentence ~line 27 — VERIFY)
    - CONTRIBUTING.md ("Local vs CI deltas" section ~lines 15-23 — VERIFY)
    - .planning/phases/60-otp-matrix-reshape-nightly-schedule/60-CONTEXT.md (D-06a, D-06b, D-05b)
  </read_first>
  <action>
    In `release-please.yml` (D-06a): in the `publish-hex` job's setup-beam step, change `elixir-version: '1.19.0'` to `'1.20.2'` and `otp-version: '27.2'` to `'28.x'` so the published Hex artifact is built on a toolchain CI actually tests. Leave the setup-beam SHA pin (`erlef/setup-beam@fc68ffb9...`) unchanged.

    In `README.md` (D-06b): replace the factual sentence "CI validates on Elixir 1.19 across OTP 26, 27, and 28." with a sentence naming the new set — CI validates on Elixir 1.20.2 across OTP 27, 28, and 29 (on main + nightly; PRs run a single trimmed cell). LEAVE the adopter-facing *support* matrix table (Elixir 1.19+, OTP 26–28) untouched — that is a support claim, not a CI-pin fact (D-06b).

    In `CONTRIBUTING.md` (D-05b): add a 4th bullet to the existing "Local vs CI deltas" list (after bullet (c)), labeled (d), stating: PRs run a single trimmed cell (OTP 28 x `parapet`); the full matrix runs on merge to `main` + nightly; a green PR is not full-matrix proof — `release_gate` on `main` is the real multi-OTP gate. LEAVE the `Elixir 1.19+` floor claim (~line 77) untouched.
  </action>
  <verify>
    <automated>grep -Fq "1.20.2" .github/workflows/release-please.yml && ! grep -Fq "Elixir 1.19 across OTP 26, 27, and 28" README.md && grep -Eiq 'release_gate.*(main|multi-OTP)|trimmed cell' CONTRIBUTING.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `release-please.yml` publish-hex setup-beam step reads `elixir-version: '1.20.2'` and `otp-version: '28.x'`; `grep -c "1\.19\.0" .github/workflows/release-please.yml` == 0 and `grep -c "'27\.2'" .github/workflows/release-please.yml` == 0.
    - The setup-beam SHA `fc68ffb9` still appears in `release-please.yml` (pin unchanged).
    - `grep -c "Elixir 1.19 across OTP 26, 27, and 28" README.md` == 0; README now contains a CI sentence naming OTP 27, 28, 29 on Elixir 1.20.2.
    - README still contains the support-matrix row `| Elixir    | 1.19+     |` (support claim untouched).
    - `CONTRIBUTING.md` "Local vs CI deltas" list contains a (d) bullet mentioning the trimmed PR cell and `release_gate` on `main` as the real multi-OTP gate; the existing (a)/(b)/(c) bullets are intact.
  </acceptance_criteria>
  <done>The published Hex toolchain matches CI (1.20.2/28.x, SHA unchanged); README's factual CI sentence is corrected without touching the support matrix; CONTRIBUTING explains the PR-trimmed / main-full gate.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the D-11 CI-coverage flag — canonical PROJECT.md row + dated one-line pointers</name>
  <files>.planning/PROJECT.md, .planning/milestones/v1.7-MILESTONE-AUDIT.md, .planning/MILESTONES.md</files>
  <read_first>
    - .planning/PROJECT.md (Key Decisions table ~line 264+, and the `⚠️ Revisit` v1.7-debt row ~line 339 — VERIFY)
    - .planning/milestones/v1.7-MILESTONE-AUDIT.md (register row ~line 165; DO NOT touch frozen frontmatter ~line 22 or Seam-7 prose ~line 132)
    - .planning/MILESTONES.md (D-11 pointer ~line 31 — VERIFY)
    - .planning/phases/60-otp-matrix-reshape-nightly-schedule/60-CONTEXT.md (D-04a canonical wording, D-04b/c/e)
  </read_first>
  <action>
    D-11 retirement is dated, append-only, and single-sourced (D-04). PROJECT.md Key Decisions is the ONE canonical explanation; every other spot is a one-line dated pointer, never a re-derived rationale.

    In `.planning/PROJECT.md` Key Decisions table (`| Decision | Rationale | Outcome |`) (D-04b): (1) add ONE new row whose Rationale carries the canonical D-04a resolution wording — "RESOLVED 2026-07-02 (Phase 60 / MATRIX-02): retired by design, not by fix. The full matrix now runs on main + nightly only, so the original solo-maintainer CI-budget reason for the prune is gone. The remaining parapet x3-OTP / public x1-OTP asymmetry is intentional — prefix resolution is compile-time and OTP-independent, so one OTP on the public leg is complete signal." — and set Outcome to `✓ Good` (Decision cell e.g. "Retire v1.7 D-11 CI-coverage prune (v1.8)"). (2) Update the existing `⚠️ Revisit` v1.7-debt row (~line 339, "Complete v1.7 tracking internal debt...") to resolve/annotate its D-11 portion — change its Outcome from `⚠️ Revisit` to `✓ Good` (or annotate that the OTP-coverage-prune item is resolved by Phase 60), pointing to the new canonical row. Leave the unrelated v1.6 `⚠️ Revisit` row (~line 332) untouched.

    In `.planning/milestones/v1.7-MILESTONE-AUDIT.md` (D-04c): edit ONLY the register row (~line 165, `| 3 | CI coverage | Public leg OTP 26/27 untested (accepted D-11 prune) | Low | No |`) to append a dated one-line pointer such as ` — RESOLVED 2026-07-02 (Phase 60); see PROJECT.md Key Decisions`. DO NOT touch the frozen frontmatter (~line 22) or the Seam-7 prose (~line 132) — those are frozen audit body.

    In `.planning/MILESTONES.md` (D-04c): edit the D-11 pointer (~line 31) to append the same dated one-line pointer (` — RESOLVED 2026-07-02 (Phase 60); see PROJECT.md Key Decisions`).

    DO NOT touch `.planning/STATE.md` line 114 (D-04e) — its "D-11" is an unrelated `@concurrency_hold_ms` decision; a grep-and-replace would corrupt an unrelated record. Make each pointer a targeted edit, not a global find-replace.
  </action>
  <verify>
    <automated>grep -Fq "RESOLVED 2026-07-02 (Phase 60 / MATRIX-02)" .planning/PROJECT.md && grep -Fq "RESOLVED 2026-07-02 (Phase 60)" .planning/milestones/v1.7-MILESTONE-AUDIT.md && grep -Fq "RESOLVED 2026-07-02 (Phase 60)" .planning/MILESTONES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains exactly one canonical D-11 resolution carrying the D-04a wording including the literal `RESOLVED 2026-07-02 (Phase 60 / MATRIX-02): retired by design, not by fix.` with Outcome `✓ Good`.
    - The existing v1.7-debt `⚠️ Revisit` row's D-11/OTP-coverage-prune portion is resolved (Outcome no longer `⚠️ Revisit` for that item, or annotated as resolved by Phase 60).
    - The v1.6 `⚠️ Revisit` row (TOKEN-04 / Phase-48) is unchanged.
    - `v1.7-MILESTONE-AUDIT.md` register row 3 has the appended dated pointer `RESOLVED 2026-07-02 (Phase 60)` referencing PROJECT.md Key Decisions; the file's frozen frontmatter (~line 22) and Seam-7 prose (~line 132) are byte-for-byte unchanged.
    - `MILESTONES.md` D-11 row (~line 31) has the appended dated pointer.
    - `.planning/STATE.md` line 114 is unchanged (the unrelated `@concurrency_hold_ms` D-11).
    - The canonical rationale text appears in PROJECT.md ONLY — other spots are one-line pointers, not re-derived rationale (anti-drift).
  </acceptance_criteria>
  <done>D-11 is retired single-sourced: PROJECT.md carries the one dated canonical explanation and the old Revisit flag is resolved; the v1.7 audit register and MILESTONES carry one-line dated pointers; the frozen audit body and the unrelated STATE.md D-11 are untouched.</done>
</task>

</tasks>

<verification>
Overall phase-2 checks (source-assertion based):
- `grep -c "1\.19\.0" .github/workflows/release-please.yml` == 0; `grep -c "1.20.2" .github/workflows/release-please.yml` >= 1; setup-beam SHA `fc68ffb9` present.
- `grep -c "Elixir 1.19 across OTP 26, 27, and 28" README.md` == 0.
- CONTRIBUTING "Local vs CI deltas" has a (d) bullet.
- `grep -c "RESOLVED 2026-07-02 (Phase 60 / MATRIX-02)" .planning/PROJECT.md` == 1 (single-sourced canonical).
- `v1.7-MILESTONE-AUDIT.md` and `MILESTONES.md` each carry a dated D-11 pointer.
- `.planning/STATE.md` line 114 unchanged (unrelated D-11).
</verification>

<success_criteria>
- MATRIX-02: D-11 CI-coverage prune retired — single canonical PROJECT.md explanation, dated pointers elsewhere, frozen audit body preserved.
- MATRIX-03 (doc surface): published Hex toolchain and factual README/CONTRIBUTING claims match the reshaped CI (1.20.2 / OTP {27,28,29}, PR-trimmed vs main-full split explained).
- Support-surface honesty preserved: adopter-facing support matrix (Elixir 1.19+, OTP 26–28) and the CONTRIBUTING 1.19+ floor left intact.
</success_criteria>

<output>
Create `.planning/phases/60-otp-matrix-reshape-nightly-schedule/60-02-SUMMARY.md` when done.
</output>
