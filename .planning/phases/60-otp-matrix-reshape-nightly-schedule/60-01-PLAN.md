---
phase: 60-otp-matrix-reshape-nightly-schedule
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [MATRIX-01, MATRIX-03, MATRIX-04, MATRIX-02]
must_haves:
  truths:
    - "A pull_request CI run resolves to exactly 1 test cell: OTP 28.x, Elixir 1.20.2, schema_prefix parapet (D-02a, SC-1, MATRIX-01)"
    - "A push-to-main and a scheduled run resolve to exactly 4 test cells: OTP {27,28,29}x parapet + OTP 28x public, all Elixir 1.20.2 (D-02a, SC-2, MATRIX-02)"
    - "Every matrix-config include object carries all three keys elixir, otp, schema_prefix (no blank interpolation false-green) (D-02d)"
    - "The matrix-config JSON is emitted single-line to $GITHUB_OUTPUT (multi-line corrupts the output) (D-02a)"
    - "The test _build cache key retains ${{ matrix.schema_prefix }} and every cell retains mix compile --force (D-02c invariant, carried from Phase 59)"
    - "OTP 26 and Elixir 1.19 no longer appear in any test/demo/lint-once matrix or setup-beam pin; toolchain is Elixir 1.20.2 x OTP {27,28,29} (SC-2, MATRIX-02)"
    - "The demo job is skipped on pull_request, runs on push+schedule, is a plain job (no strategy/matrix) on Elixir 1.20.2 / OTP 28.x (D-03b, SC-3, MATRIX-03)"
    - "A nightly schedule trigger cron '17 3 * * *' exists and drives the full matrix + demo (D-03a, SC-4, MATRIX-04)"
    - "release_gate keeps if: always(); blocks on skipped/failure/cancelled for lint-once and test; passes demo only on success|skipped while blocking cancelled demo (D-03c truth table, MATRIX-04)"
    - "concurrency.group includes github.event_name; cancel-in-progress stays PR-only (D-03d)"
    - "Job names lint-once and release_gate are verbatim unchanged; lint-once has no if: (D-03e)"
  artifacts:
    - ".github/workflows/ci.yml (reshaped: matrix-config job, schedule trigger, plain demo, hardened release_gate)"
  key_links:
    - "matrix-config.outputs.test-matrix -> test.strategy.matrix via fromJson (test needs: [matrix-config])"
    - "test _build cache key -> ${{ matrix.schema_prefix }} (dual-prefix isolation)"
    - "release_gate.needs -> [lint-once, matrix-config, test, demo]; result checks -> merge block/pass"
---

<objective>
Reshape `.github/workflows/ci.yml` so pull requests run one trimmed test cell while push-to-`main` and a nightly cron run the full multi-OTP x dual-prefix matrix, retire EOL OTP 26 / Elixir 1.19 (bump to Elixir 1.20.2 x OTP {27,28,29}), trim `demo` to a single OTP-28 cell skipped on PRs, harden `release_gate` against false-greens, and fix the concurrency group.

Purpose: Give contributors fast PR feedback without sacrificing full-matrix coverage on merge/nightly, on a currently-supported toolchain, while preserving the v1.7 dual-prefix false-green guard and the 0-review-repo `release_gate` backstop.

Output: A rewritten `ci.yml` with a `matrix-config` resolver job, a `schedule` trigger, a plain `demo` job, a truth-table-hardened `release_gate`, and event-scoped concurrency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-otp-matrix-reshape-nightly-schedule/60-CONTEXT.md
@.github/workflows/ci.yml
@.planning/research/v1.8/02-OTP-MATRIX-STRATEGY.md
</context>

<artifacts_produced>
This plan creates the following new symbols in `ci.yml`:
- A `schedule:` trigger with `cron: '17 3 * * *'` (03:17 UTC).
- A `matrix-config` job with a self-documenting `name:` and a `test-matrix` output (single-line `{"include":[...]}` JSON, branched on `github.event_name`).
- `test.needs: [matrix-config]` consuming `fromJson(needs.matrix-config.outputs.test-matrix)`.
- A plain (no-matrix) `demo` job with `if: github.event_name != 'pull_request'`.
- A hardened `release_gate` with `needs: [lint-once, matrix-config, test, demo]` and a per-result truth-table check.
- An asymmetry-explainer comment anchored on the `matrix-config` job (D-04d wording; the D-11 pointer clause lands here as part of MATRIX-02).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add schedule trigger, event-scoped concurrency, and the matrix-config resolver job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the file being reshaped — VERIFY current line numbers against live file; CONTEXT numbers are as-of-authoring hints)
    - .planning/phases/60-otp-matrix-reshape-nightly-schedule/60-CONTEXT.md (D-02a/b/d/e, D-03a/d, D-04d)
    - .planning/research/v1.8/02-OTP-MATRIX-STRATEGY.md (recommended resolver YAML — but its drafted JSON is multi-line; emit single-line per D-02a)
  </read_first>
  <action>
    In the `on:` block add a `schedule:` trigger with a single entry `cron: '17 3 * * *'` (03:17 UTC, D-03a — deliberately off-the-hour, house `:17` convention; the ROADMAP SC-4 shows an illustrative `'0 3 * * *'` but 17 3 is the intentional refinement — do NOT treat this as a deviation). Keep existing `push.branches: [main]` and `pull_request:` triggers.

    Update the `concurrency.group` (D-03d) to insert `github.event_name` between the workflow and the ref key so nightly and a same-time main push get separate lanes: group becomes `${{ github.workflow }}-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}`. Leave `cancel-in-progress: ${{ github.event_name == 'pull_request' }}` unchanged (stays PR-only).

    Add a new `matrix-config` job (D-02a, D-02e) as the first job. Give it a self-documenting `name:` such as `matrix-config (PR=trimmed, main+nightly=full)`. It runs on ubuntu-latest, needs no external action (a bare `run:` step, so no SHA pin required, D-code-context). It declares an output `test-matrix` bound to a step output. The step branches on `github.event_name`: on `pull_request` emit the trimmed 1-cell set; on any other event (push, schedule) emit the full 4-cell set. Emit SINGLE-LINE JSON of shape `{"include":[...]}` to `$GITHUB_OUTPUT` (multi-line corrupts `$GITHUB_OUTPUT` — D-02a). Bash phrasing of the if/else heredoc is Claude's discretion provided the shape holds.

    Every include object MUST carry all three keys `elixir`, `otp`, `schema_prefix` (D-02d) — a missing key silently interpolates to empty string downstream (blank `PARAPET_SCHEMA_PREFIX` = wrong-schema false-green). Set `elixir: '1.20.2'` (exact three-part string, never floating `1.20`, D-01a) in EVERY entry; whether Elixir is per-include or job-env is Claude's discretion (D-02b/discretion) as long as no `${{ matrix.* }}` interpolation ever resolves empty.
    - PR set (1 cell): otp `28.x`, schema_prefix `parapet`, elixir `1.20.2`.
    - push/schedule set (4 cells): otp `27.x`/parapet, otp `28.x`/parapet, otp `29.x`/parapet, otp `28.x`/public — all elixir `1.20.2`.

    Immediately above or inside the `matrix-config` job add the asymmetry-explainer comment (D-04d verbatim intent): note the matrix is intentionally asymmetric (parapet leg x {27,28,29}, public leg x 28 only), that it must NOT be "balanced" because prefix resolution is compile-time and OTP-independent, and that this retires the v1.7 D-11 coverage-prune (pointing to PROJECT.md Key Decisions). This comment is the `ci.yml` D-11 pointer and part of MATRIX-02. Add a one-line note at/near the OTP-29 cell (D-05a) that OTP 29 requires Elixir >= 1.20 (the cell proves forward-compat under 1.20, not that a ~> 1.19 adopter can run OTP 29).

    Do NOT add `version-type: strict` to setup-beam anywhere (D-01d — strict errors against `.x` OTP ranges).
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "cron: '17 3 \* \* \*'" .github/workflows/ci.yml && grep -q 'matrix-config' .github/workflows/ci.yml && grep -Fq 'github.workflow }}-${{ github.event_name }}' .github/workflows/ci.yml && ! grep -q 'version-type: strict' .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cron: '17 3 \* \* \*'" .github/workflows/ci.yml` == 1 (nightly schedule present; `17 3` accepted, not the illustrative `0 3`).
    - `ci.yml` `concurrency.group` contains the literal `${{ github.workflow }}-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}`.
    - `ci.yml` contains a job `matrix-config` with a `test-matrix` output.
    - The PR branch of the resolver emits exactly one include object with `otp` `28.x`, `schema_prefix` `parapet`, `elixir` `1.20.2`.
    - The push/schedule branch emits exactly four include objects: `27.x`/parapet, `28.x`/parapet, `29.x`/parapet, `28.x`/public — every one with `elixir` `1.20.2`.
    - Every emitted include object contains all three keys `elixir`, `otp`, `schema_prefix` (no object missing a key).
    - The resolver writes single-line JSON to `$GITHUB_OUTPUT` (the `{"include":...}` payload is on one physical line; not a multi-line heredoc body).
    - `grep -c 'version-type: strict' .github/workflows/ci.yml` == 0.
    - `ci.yml` contains the D-04d asymmetry-explainer comment mentioning the public leg is OTP 28 only and pointing to PROJECT.md Key Decisions / D-11 retirement.
    - `ci.yml` is valid YAML (`python3 -c "import yaml; yaml.safe_load(...)"` exits 0).
  </acceptance_criteria>
  <done>The workflow triggers on push/pull_request/schedule('17 3'), concurrency is event-scoped, and a `matrix-config` job emits single-line 3-key-complete JSON — 1 cell on PR, 4 cells on push/schedule — with the D-11 asymmetry comment in place.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the test job to the resolver, retire OTP 26 / Elixir 1.19, and convert demo to a plain PR-skipped job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (test job lines ~58-111, demo job ~113-164, lint-once cache keys ~24-43 — VERIFY live line numbers)
    - .planning/phases/60-otp-matrix-reshape-nightly-schedule/60-CONTEXT.md (D-01a/b, D-02a/c/d, D-03b, D-05a, mechanical checklist in <code_context>)
  </read_first>
  <action>
    Rewire the `test` job (D-02a): add `needs: [matrix-config]`, remove the entire static `matrix:`/`include:` base block (the `elixir/otp/schema_prefix` axes AND the public include — no residual base axis may remain, R3/D-02), and set `matrix: ${{ fromJson(needs.matrix-config.outputs.test-matrix) }}`. Keep `fail-fast: false`. Keep `env.PARAPET_SCHEMA_PREFIX: ${{ matrix.schema_prefix }}`. Keep setup-beam consuming `${{ matrix.elixir }}` / `${{ matrix.otp }}` (unchanged — values now flow from the resolver, so no literal edit here).

    INVARIANT (D-02c, non-negotiable, carried from Phase 59): the `test` `_build` cache key and its restore-keys MUST retain `${{ matrix.schema_prefix }}`, and the `mix compile --force` step MUST remain. The resolver only chooses which `{otp, schema_prefix}` cells exist — never how they compile. Do NOT remove or reorder these.

    Retire EOL toolchain from `lint-once` (D-01a, mechanical checklist): swap every literal `1.19.0` token to `1.20.2` in the lint-once setup-beam pin (~line 24) and its three cache keys + restore-keys (deps ~30/31, _build ~36/37, PLT ~42/43 — note the PLT key has otp-then-elixir order, so change the `1.19.0` token there, not `28.x`). The lint-once `otp-version: '28.x'` stays. lint-once keeps NO `if:` (runs on all three triggers, D-03e).

    Convert `demo` to a plain job (D-03b, D-05a): add `if: github.event_name != 'pull_request'` (correctly includes push AND schedule, excludes PR). Remove the `strategy:`/`matrix:` block entirely. Set literal `elixir-version: '1.20.2'` and `otp-version: '28.x'` in the demo setup-beam step. Its cache keys interpolate `${{ matrix.elixir }}`/`${{ matrix.otp }}` today — since there is no more matrix, replace those interpolations with the literals `1.20.2`/`28.x` so the keys resolve (no blank interpolation). Keep `demo.needs: [lint-once, test]` and every functional step (deps, compile, ecto.create/migrate, seed, smoke test) unchanged. Keep `demo` in `release_gate.needs` (handled in Task 3).

    Confirm no `1.19.0`, `1.19`, `26.x`, or OTP `26` token survives in any test/demo/lint-once matrix, setup-beam pin, or cache key (the test/demo interpolated keys flow from matrix/literals and need no `1.19` literal).
  </action>
  <verify>
    <automated>! grep -Eq "1\.19\.0|'26\.x'|otp: *\[.*26" .github/workflows/ci.yml && grep -Fq 'fromJson(needs.matrix-config.outputs.test-matrix)' .github/workflows/ci.yml && grep -Fq 'matrix.schema_prefix' .github/workflows/ci.yml && grep -Fq 'mix compile --force' .github/workflows/ci.yml && grep -Fq "if: github.event_name != 'pull_request'" .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test` job has `needs: [matrix-config]` and `matrix: ${{ fromJson(needs.matrix-config.outputs.test-matrix) }}`; `fail-fast: false` retained; no static `elixir:`/`otp:`/`schema_prefix:` base axis or public `include:` remains in the test job.
    - The `test` `_build` cache key line contains `${{ matrix.schema_prefix }}` AND the restore-keys line contains `${{ matrix.schema_prefix }}` (D-02c preserved).
    - `grep -c 'mix compile --force' .github/workflows/ci.yml` >= 1 (the test cell force-recompile step survives).
    - `grep -c '1\.19\.0' .github/workflows/ci.yml` == 0 and `grep -c "'26\.x'" .github/workflows/ci.yml` == 0 (EOL toolchain gone).
    - `lint-once` setup-beam pin reads `elixir-version: '1.20.2'` with `otp-version: '28.x'`; all three lint-once cache key + restore-key pairs contain `1.20.2` (not `1.19.0`).
    - `demo` job contains `if: github.event_name != 'pull_request'`, has no `strategy:`/`matrix:` block, and its setup-beam step uses literal `elixir-version: '1.20.2'` / `otp-version: '28.x'`; demo cache keys contain no unresolved `${{ matrix.* }}`.
    - `lint-once` job has no `if:` key.
    - `ci.yml` is valid YAML.
  </acceptance_criteria>
  <done>The `test` job draws its cells from the resolver while preserving the dual-prefix cache key + force-compile; lint-once is bumped to 1.20.2; demo is a plain OTP-28 job skipped on PRs; and no OTP-26/Elixir-1.19 token survives.</done>
</task>

<task type="auto">
  <name>Task 3: Harden release_gate against the false-green truth table</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (release_gate job ~166-193 — VERIFY live line numbers)
    - .planning/phases/60-otp-matrix-reshape-nightly-schedule/60-CONTEXT.md (D-03c truth table, D-03e, <specifics> demo case form)
  </read_first>
  <action>
    Harden the existing `release_gate` aggregator (D-03c) — extend it, do not rebuild. Keep the job name `release_gate` verbatim (branch-protection required-check name — renaming disables the backstop, D-03e) and keep `if: always()` (a skipped required check reads green in branch protection — catastrophic for this 0-review repo).

    Update `release_gate.needs` to `[lint-once, matrix-config, test, demo]` (add `matrix-config` so a resolver failure yields a clear gate message; keep the others).

    Implement the D-03c result truth table in the aggregate step (exact bash phrasing is Claude's discretion):
    - `lint-once` and `test`: STRICT `= success` allowlist — BLOCK (fail the gate) on skipped, failure, OR cancelled. Any result other than `success` blocks.
    - `demo`: pass on `success` OR `skipped` (skipped is the legitimate PR case), but BLOCK on failure OR cancelled. Use a form such as `case "$DEMO_RESULT" in success|skipped) : ;; *) FAILED=1 ;; esac` so a `cancelled` demo still blocks (a naive `!= failure` or `!= success` check would leak a cancelled run — closed).
    - Optionally check `matrix-config` result (strict success) for a clear failure message.
    On any block, echo which job + its result and exit 1; otherwise echo success and exit 0.
  </action>
  <verify>
    <automated>grep -Fq 'if: always()' .github/workflows/ci.yml && grep -Fq 'release_gate' .github/workflows/ci.yml && grep -Eq 'needs:.*matrix-config' .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'release_gate' .github/workflows/ci.yml` >= 1 with the job id spelled exactly `release_gate` (unchanged).
    - `release_gate` retains `if: always()`.
    - `release_gate.needs` lists `lint-once`, `matrix-config`, `test`, and `demo`.
    - The aggregate step treats any non-`success` result of `lint-once` or `test` as a block (strict success allowlist — skipped/failure/cancelled all fail the gate).
    - The aggregate step passes `demo` on `success` or `skipped` but blocks on `cancelled` (a `success|skipped`-only allowlist, e.g. a `case` matching `success|skipped`, not a `!= failure` check).
    - `ci.yml` is valid YAML.
  </acceptance_criteria>
  <done>`release_gate` keeps its verbatim name + `if: always()`, gains `matrix-config` in needs, and blocks the merge on any skipped/failed/cancelled `lint-once`/`test` and on any failed/cancelled `demo` while letting a PR-skipped `demo` pass.</done>
</task>

</tasks>

<verification>
Overall phase-1 checks (source-assertion based — no CI runner at plan/verify time):
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (valid YAML).
- `grep -c "cron: '17 3 \* \* \*'" .github/workflows/ci.yml` == 1.
- `grep -c '1\.19\.0' .github/workflows/ci.yml` == 0 and `grep -c "'26\.x'" .github/workflows/ci.yml` == 0.
- `grep -c 'matrix.schema_prefix' .github/workflows/ci.yml` >= 1 (dual-prefix cache key preserved).
- `grep -c 'mix compile --force' .github/workflows/ci.yml` >= 1.
- `grep -c 'fromJson(needs.matrix-config.outputs.test-matrix)' .github/workflows/ci.yml` == 1.
- `grep -c "if: github.event_name != 'pull_request'" .github/workflows/ci.yml` >= 1 (demo PR-skip).
- `grep -c 'if: always()' .github/workflows/ci.yml` >= 1 (release_gate).
- If `actionlint` is available, `actionlint .github/workflows/ci.yml` reports no errors.
</verification>

<success_criteria>
- SC-1 (MATRIX-01): resolver emits 1 trimmed cell (OTP 28 / Elixir 1.20.2 / parapet) on pull_request.
- SC-2 (MATRIX-02/03): resolver emits the 4-cell full matrix on push/schedule; OTP 26 + Elixir 1.19 retired; toolchain is Elixir 1.20.2 x OTP {27,28,29}.
- SC-3 (MATRIX-03): demo is a single OTP-28 plain job skipped on PRs.
- SC-4 (MATRIX-04): nightly cron '17 3 * * *' drives full matrix + demo; release_gate hardened; concurrency event-scoped.
- All three false-green footguns closed: (a) release_gate blocks skipped/cancelled correctly; (b) every include has all 3 keys; (c) _build key keeps schema_prefix + mix compile --force retained.
</success_criteria>

<output>
Create `.planning/phases/60-otp-matrix-reshape-nightly-schedule/60-01-SUMMARY.md` when done.
</output>