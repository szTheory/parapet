---
phase: 10-tighten-archive-retention-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/evidence/archiver.ex
  - lib/mix/tasks/parapet.archive.ex
  - test/parapet/evidence/archiver_test.exs
  - test/mix/tasks/parapet.archive_test.exs
  - test/parapet/evidence/archive_worker_test.exs
autonomous: true
requirements:
  - SCALE-01.b
  - AC-02
must_haves:
  truths:
    - "Running `mix parapet.archive --days 90` archives only resolved incidents older than the retention window."
    - "Old `investigating` incidents remain active work and are not archived or deleted."
    - "The CLI and optional Oban worker still delegate to the same bounded archive path without changing their public entry surface."
    - "Archive deletion still uses the existing FK-safe repo delete path after JSONL export."
  artifacts:
    - path: "lib/parapet/evidence/archiver.ex"
      provides: "Resolved-only archive eligibility query with unchanged cutoff and delete flow"
      min_lines: 60
    - path: "test/parapet/evidence/archiver_test.exs"
      provides: "Regression coverage proving resolved incidents archive while investigating incidents remain active"
      min_lines: 170
    - path: "test/mix/tasks/parapet.archive_test.exs"
      provides: "CLI regression coverage for the corrected retention contract"
      min_lines: 100
    - path: "test/parapet/evidence/archive_worker_test.exs"
      provides: "Worker regression coverage proving the scheduled path shares the corrected retention contract"
      min_lines: 110
  key_links:
    - from: "lib/mix/tasks/parapet.archive.ex"
      to: "lib/parapet/evidence/archiver.ex"
      via: "Parapet.Evidence.Archiver.archive/3 delegation"
      pattern: 'Parapet\.Evidence\.Archiver\.archive\(repo, path, days\)'
    - from: "lib/parapet/evidence/archive_worker.ex"
      to: "lib/parapet/evidence/archiver.ex"
      via: "ArchiveWorker.perform/1 delegation"
      pattern: 'Parapet\.Evidence\.Archiver\.archive\(repo, path, days\)'
    - from: "lib/parapet/operator.ex"
      to: "lib/parapet/evidence/archiver.ex"
      via: "shared incident state semantics; active queue states must remain excluded from archival"
      pattern: 'investigating|resolved'
---

<objective>
Repair the archive-retention contract in runtime code and regression tests without widening the public `mix parapet.archive` surface.

Purpose: Close the executable mismatch identified by the milestone audit so active `investigating` work is never pruned.
Output: A resolved-only archive predicate, unchanged CLI/worker entrypoints, and targeted tests that prove active work stays in the queue.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-tighten-archive-retention-semantics/10-RESEARCH.md
@.planning/phases/10-tighten-archive-retention-semantics/10-PATTERNS.md
@.planning/phases/10-tighten-archive-retention-semantics/10-VALIDATION.md
@.planning/v0.9-phases/2/VERIFICATION.md
@.planning/v0.9-MILESTONE-AUDIT.md
@lib/parapet/evidence/archiver.ex
@lib/mix/tasks/parapet.archive.ex
@lib/parapet/evidence/archive_worker.ex
@lib/parapet/operator.ex
@docs/operator-ui.md
@test/parapet/evidence/archiver_test.exs
@test/mix/tasks/parapet.archive_test.exs
@test/parapet/evidence/archive_worker_test.exs

<interfaces>
From `lib/parapet/evidence/archiver.ex`:
```elixir
@spec archive(module(), Path.t(), pos_integer()) :: {:ok, :ok}
def archive(repo, path, retention_days) when is_integer(retention_days) and retention_days > 0 do
  ...
end

defp archive_query(queryable, cutoff) do
  from(
    incident in queryable,
    where: incident.state != "open",
    where: incident.inserted_at < ^cutoff
  )
end
```

From `lib/mix/tasks/parapet.archive.ex`:
```elixir
{opts, _, _} = OptionParser.parse(args, switches: [days: :integer, path: :string])

repo = Application.fetch_env!(:parapet, :repo)
days = Keyword.get(opts, :days, @default_days)
path = Keyword.get(opts, :path, @default_path)

_result = Parapet.Evidence.Archiver.archive(repo, path, days)
Mix.shell().info(Jason.encode!(%{status: "ok", result: "ok"}))
```

From `lib/parapet/evidence/archive_worker.ex`:
```elixir
def perform(%Oban.Job{args: args}) do
  repo = Application.fetch_env!(:parapet, :repo)
  days = Map.get(args, "days", @default_days)
  path = Map.get(args, "path", @default_path)

  Parapet.Evidence.Archiver.archive(repo, path, days)
end
```

From `lib/parapet/operator.ex`:
```elixir
@active_queue_states ["open", "investigating"]
...
Incident
|> where([i], i.state in ^@active_queue_states)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrow archive eligibility to resolved incidents only</name>
  <read_first>
    lib/parapet/evidence/archiver.ex
    lib/mix/tasks/parapet.archive.ex
    lib/parapet/operator.ex
    docs/operator-ui.md
    test/parapet/evidence/archiver_test.exs
    .planning/phases/10-tighten-archive-retention-semantics/10-RESEARCH.md
  </read_first>
  <files>lib/parapet/evidence/archiver.ex, lib/mix/tasks/parapet.archive.ex, test/parapet/evidence/archiver_test.exs</files>
  <behavior>
    - Test 1: `Parapet.Evidence.Archiver.archive/3` archives only incidents where `state == "resolved"` and `inserted_at` is older than the cutoff, per the Phase 10 contract.
    - Test 2: old `investigating` incidents remain in the repo snapshot and do not appear in the JSONL export.
    - Test 3: the delete path still runs after JSONL append and continues to preload nested evidence before encoding.
    - Test 4: `mix parapet.archive` keeps the same `--days`, `--path`, and JSON success output while its docs stop claiming broader `non-open` semantics.
  </behavior>
  <action>Modify only the ownership surface for the destructive retention rule: `lib/parapet/evidence/archiver.ex`, `lib/mix/tasks/parapet.archive.ex`, and `test/parapet/evidence/archiver_test.exs`. Follow the concrete analogs in `10-PATTERNS.md`: reuse the existing `lib/parapet/evidence/archiver.ex` service shape, the thin delegation pattern in `lib/mix/tasks/parapet.archive.ex`, and the fake-repo regression structure in `test/parapet/evidence/archiver_test.exs`. Change `archive_query/2` from a negative active-state filter to an explicit resolved-only allowlist per `SCALE-01.b` and the Phase 10 research recommendation; keep the retention cutoff based on `inserted_at`, keep chunked streaming, keep JSONL export, and keep the repo delete flow unchanged. Update the mix task module documentation to say resolved incidents older than the retention window are archived, but do not add new flags, change default output, or alter task delegation. Rewrite the archiver regression fixture so an old `investigating` incident is a negative proof case: it must remain in the repo snapshot and stay out of the archive file even when older than the cutoff. Do not change `lib/parapet/operator.ex` or `docs/operator-ui.md`; they are semantic references, not owned edit targets for this task.</action>
  <acceptance_criteria>
    - `rg -n 'state == \"resolved\"' lib/parapet/evidence/archiver.ex` finds the resolved-only archive predicate.
    - `rg -n 'inserted_at < \\^cutoff' lib/parapet/evidence/archiver.ex` still finds the existing cutoff field.
    - `rg -n 'Old investigating incident|remain active|not appear' test/parapet/evidence/archiver_test.exs` shows a named regression case for preserved active work.
    - `rg -n 'resolved incidents older than the retention window' lib/mix/tasks/parapet.archive.ex` matches the corrected module documentation.
    - `rg -n 'status: \"ok\"|result: \"ok\"' lib/mix/tasks/parapet.archive.ex` still shows the unchanged CLI success payload.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/parapet/evidence/archiver_test.exs -x</automated>
  </verify>
  <done>The only incidents eligible for archival are resolved incidents older than the cutoff, the CLI surface remains backward-compatible, and the archiver test suite proves `investigating` stays active.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the CLI and worker paths inherit the corrected retention rule</name>
  <read_first>
    lib/parapet/evidence/archive_worker.ex
    lib/mix/tasks/parapet.archive.ex
    test/mix/tasks/parapet.archive_test.exs
    test/parapet/evidence/archive_worker_test.exs
    test/parapet/evidence/archiver_test.exs
    .planning/phases/10-tighten-archive-retention-semantics/10-VALIDATION.md
  </read_first>
  <files>test/mix/tasks/parapet.archive_test.exs, test/parapet/evidence/archive_worker_test.exs</files>
  <behavior>
    - Test 1: `Mix.Tasks.Parapet.Archive.run/1` still archives old resolved incidents and leaves old `investigating` incidents untouched.
    - Test 2: `Parapet.Evidence.ArchiveWorker.perform/1` shares the same retention contract through the common archiver and leaves old `investigating` incidents untouched.
    - Test 3: both tests still prove the archive path runs inside a repo transaction and keeps the bounded `max_rows` stream option.
  </behavior>
  <action>Limit edits to `test/mix/tasks/parapet.archive_test.exs` and `test/parapet/evidence/archive_worker_test.exs`. Follow the exact test analogs in `10-PATTERNS.md`: preserve the Mix shell/env harness and fake-repo snapshot assertions in `test/mix/tasks/parapet.archive_test.exs`, and preserve the conditional-compile + job execution structure in `test/parapet/evidence/archive_worker_test.exs`. Add old `investigating` fixtures to both fake-repo scenarios, update their stream filters so the tests reflect the resolved-only query shape, and assert that only resolved IDs are archived while the `investigating` row remains in repo state after each entry surface runs. Preserve the current public CLI invocation and worker job contract; do not edit `lib/parapet/evidence/archive_worker.ex` unless the tests expose a real delegation break. Keep verification tight: these tests should continue to prove transaction count, `max_rows` usage, and archive file creation in addition to the new state-semantics assertions.</action>
  <acceptance_criteria>
    - `rg -n 'investigating' test/mix/tasks/parapet.archive_test.exs` shows an explicit CLI regression fixture and assertion for preserved active work.
    - `rg -n 'investigating' test/parapet/evidence/archive_worker_test.exs` shows an explicit worker regression fixture and assertion for preserved active work.
    - `rg -n 'stream_opts|transactions|archived_ids' test/mix/tasks/parapet.archive_test.exs test/parapet/evidence/archive_worker_test.exs` still shows bounded-stream and transaction assertions.
    - `mix test test/mix/tasks/parapet.archive_test.exs test/parapet/evidence/archive_worker_test.exs -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/parapet.archive_test.exs test/parapet/evidence/archive_worker_test.exs -x</automated>
  </verify>
  <done>The public CLI task and optional worker both prove that old resolved incidents archive successfully while old `investigating` incidents remain active and untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/job args -> archive runtime | untrusted retention input selects a destructive archive path |
| archiver -> primary database | archived incident IDs are hard-deleted after export |
| operator active-state contract -> archive predicate | a drift here can silently delete active operator work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | `lib/parapet/evidence/archiver.ex` | mitigate | Treat active-work deletion as a high-severity blocker: replace `state != "open"` with an explicit resolved-only predicate and add negative regression cases for `investigating`. |
| T-10-02 | I | `mix parapet.archive` / archive JSONL | accept | Archive output intentionally includes resolved incident evidence; Phase 10 does not widen the data emitted or change the storage format. |
| T-10-03 | R | CLI and worker entry surfaces | mitigate | Preserve the existing `--days`, `--path`, and JSON success output, and prove both entrypoints still delegate through the shared archiver with targeted tests. |
| T-10-04 | D | repo streaming/delete path | mitigate | Keep the existing transaction-bound `Repo.stream/2`, bounded `max_rows`, preload shape, and delete path intact while tightening only the state predicate. |
</threat_model>

<verification>
Run `mix test test/parapet/evidence/archiver_test.exs test/mix/tasks/parapet.archive_test.exs test/parapet/evidence/archive_worker_test.exs -x` after both tasks. Confirm the targeted suite proves resolved-only archival, preserved `investigating` rows, bounded `max_rows`, and successful JSONL file creation.
</verification>

<success_criteria>
The runtime archive path is repaired without a CLI contract change: resolved incidents older than the retention window archive correctly, `investigating` incidents remain active work, and all three targeted archive tests are green.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tighten-archive-retention-semantics/10-01-SUMMARY.md`
</output>
