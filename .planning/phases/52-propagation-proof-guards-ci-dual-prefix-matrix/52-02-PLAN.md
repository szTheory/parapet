---
phase: 52-propagation-proof-guards-ci-dual-prefix-matrix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/parapet/schema_prefix_guard_test.exs
autonomous: true
requirements: [PROP-02]
must_haves:
  truths:
    - "A static fitness-function guard scans lib/parapet/**/*.ex and fails the build with a structured, teaching message if runtime prefix:/string-table writes/search_path/raw parapet_ SQL appear."
    - "The guard is green from day one — zero offenders in the current lib/parapet/ tree."
    - "The scan excludes lib/mix/tasks/ where generators legitimately emit create table(:parapet_*) and references(:parapet_*, on_delete: :delete_all)."
    - "The failure message explains the read/write precedence split-brain (why) and the fix, with a per-offender file/line/pattern/code/fix block."
  artifacts:
    - test/parapet/schema_prefix_guard_test.exs
  key_links:
    - "Path.wildcard(\"lib/parapet/**/*.ex\") does NOT match lib/mix/tasks/ — this single scoping is the false-positive remover (D-02)."
    - "Guard matches forbidden SHAPE not forbidden WORD (D-03 fingerprints a/b/c/d) so insert_all(ActionClaim,...) and on_delete: :delete_all never match."
  prohibitions:
    - "No new hex dependency (no Credo custom check, no Sourceror/AST) — pure ExUnit regex over source files."
    - "No bare prefix: match that would fire on schema_prefix:/module_prefix/_prefix: lines."
---

<objective>
Implement the PROP-02 negative-proof guard: a line-filtered regex ExUnit fitness function that makes
runtime prefix: options, string-literal-table writes, search_path switching, and raw parapet_ SQL
impossible to reintroduce in lib/parapet/. Green from day one (zero current offenders) — it is the
runtime-side complement to Plan 01's WR-03 delegation.

Purpose: Seal the negative direction. Even after Plan 01 single-sources the prefix, a future edit could
thread a runtime prefix: back in; this guard fails the build with a teaching message when it does.

Output: test/parapet/schema_prefix_guard_test.exs (async: true, no DB), modeled on the two existing
Parapet fitness guards.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-CONTEXT.md
@.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-RESEARCH.md
@.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New file: `test/parapet/schema_prefix_guard_test.exs` — PROP-02 static guard.
No new production symbols. No new dependencies.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the PROP-02 static guard fitness function (D-01..D-05)</name>
  <files>test/parapet/schema_prefix_guard_test.exs</files>
  <read_first>
    - test/parapet/operator_ui_compile_out_test.exs (lines 49-64 — Path.wildcard + File.read! + refute loop shape, the structural analog)
    - test/parapet/operator_ui_palette_gate_test.exs (lines 54-64 — Regex.scan + structured per-offender failure message analog)
    - .planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-PATTERNS.md (section "test/parapet/schema_prefix_guard_test.exs", lines 27-67, and "ExUnit Fitness Function Guard Structure", lines 544-573)
    - .planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-RESEARCH.md (Pattern 1 lines 197-251; "Verified: Zero guard offenders" lines 435-449; "lib/mix/tasks/ exclusion is essential" lines 463-465)
  </read_first>
  <action>
    Create test/parapet/schema_prefix_guard_test.exs with `use ExUnit.Case, async: true` (no DB).
    Bind the scan set at module-attribute level: the file list via `Path.wildcard("lib/parapet/**/*.ex")`
    (this glob does NOT match lib/mix/tasks/ — the D-02 exclusion is achieved by the glob alone; do NOT add
    lib/mix/tasks/ to the scan).
    Define the four forbidden regex fingerprints (D-03), Claude's discretion on exact literals but they MUST
    encode these shapes:
      (a) string/sigil table literal immediately after an insert_all/update_all/delete_all paren — i.e. the
          verb, optional whitespace, an open paren, optional whitespace, then a double-quote or sigil char.
          This must NOT match `insert_all(ActionClaim, ...)` (module ref) nor `on_delete: :delete_all`.
      (b) a bare repo `prefix:` option with negative lookbehinds excluding `schema_prefix:`, `module_prefix`,
          and `_prefix:`.
      (c) `search_path`.
      (d) `parapet_` appearing inside a `fragment(` call (raw SQL naming a prefixed table).
    Before matching each line, strip trailing comments and SKIP any line containing `schema_prefix:` or
    `_prefix:` (these are legitimate). Collect offenders as {file, line, pattern, code} tuples via per-line
    iteration (so the message can cite a line number).
    Assert `offenders == []` with a multi-line teaching failure message (D-05): explain the read/write
    precedence split-brain (writes prefer the repo prefix: option, reads prefer from/@schema_prefix, so a
    runtime prefix: lands reads and writes in different Postgres schemas), then a per-offender
    `file:line / pattern / code / fix:` block via a format helper, then a one-line fix directive.
    The guard MUST pass on the first run (D-04 — RESEARCH verified zero offenders); if it fails on creation,
    that signals a real offender to surface, not a regex to loosen.
  </action>
  <verify>
    <automated>mix test test/parapet/schema_prefix_guard_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/parapet/schema_prefix_guard_test.exs` exits 0 on the first run (green from day one).
    - `grep -c "Path.wildcard(\"lib/parapet/\*\*/\*.ex\")" test/parapet/schema_prefix_guard_test.exs` returns at least 1 (correct scope, excludes lib/mix/tasks/).
    - `grep -c "search_path" test/parapet/schema_prefix_guard_test.exs` returns at least 1 (fingerprint c present).
    - Temporarily adding a line `prefix: "public"` inside a repo call in a scratch lib/parapet file makes the test fail (verify shape (b) bites) — revert the scratch edit after confirming.
    - Failure message references the read/write split-brain and a per-offender fix block.
  </acceptance_criteria>
  <done>schema_prefix_guard_test.exs exists, scans lib/parapet/**/*.ex (excluding lib/mix/tasks/), encodes the four D-03 fingerprints with comment-stripping + schema_prefix: skip, passes green with zero offenders, and emits a teaching per-offender message on violation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer edit → repo call | A future code edit could thread a runtime `prefix:` option into a `Repo.*` call (untrusted-from-the-contract's-view), redirecting writes to a different schema than reads. The guard is the boundary check at CI time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-03 | Tampering | runtime `prefix:` reintroduced into any `lib/parapet/` repo call | high | mitigate | The PROP-02 static guard fails the build via fingerprint (b) the moment a bare `prefix:` repo option appears in `lib/parapet/**/*.ex`; blocks the read/write schema-hopping split-brain at CI time. Verified by the scratch-edit reject check in Task 1 acceptance. |
| T-52-04 | Tampering | raw `search_path` / string-literal-table write / raw `parapet_` SQL | medium | mitigate | Fingerprints (a)/(c)/(d) flag string-literal-table writes, `search_path` switching, and raw `parapet_` SQL inside `fragment(`. Green-from-day-one baseline (zero offenders) makes any future introduction a hard build failure. |
| T-52-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `mix test test/parapet/schema_prefix_guard_test.exs` exits 0 (green from day one).
- Manual negative check: a scratch `prefix: "public"` repo option triggers a failure with a teaching message, then reverted.
- Guard runs free on every CI matrix cell (it is in the mix test lane — no separate wiring).
</verification>

<success_criteria>
The static guard scans lib/parapet/**/*.ex (excluding lib/mix/tasks/), is green with zero current
offenders, and fails the build with a structured read/write split-brain teaching message if any of the
four D-03 forbidden shapes are reintroduced.
</success_criteria>

<output>
Create `.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-02-SUMMARY.md` when done
</output>
