---
phase: 52-propagation-proof-guards-ci-dual-prefix-matrix
plan: 03
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - lib/parapet/mcp/server.ex
  - test/parapet/spine/prefix_propagation_test.exs
autonomous: true
requirements: [PROP-01, PROP-03]
must_haves:
  truths:
    - "mcp/server.ex's get_incident_timeline join is extracted into a @doc false pure query builder (timeline_for_correlation_query/1) that execute_tool/2 calls — the identical Ecto.Query still flows to repo.all (zero call-site semantic change)."
    - "to_sql(:all, ...) proves the qualified \"parapet\".\"parapet_…\" identifier appears on BOTH the FROM and the JOIN for the two spine↔spine joins (mcp/server.ex builder + circuit_breaker.ex execution_count_query/3)."
    - "Ecto.get_meta(struct, :prefix) == @prefix proves the compiled prefix is materialized on the claim_service insert_all and the evidence.ex Ecto.Multi write paths after a sandbox round-trip."
    - "Tests are leg-aware: the six-schema __schema__(:prefix) equality runs unconditionally; the negative 'no bare parapet_incidents' assertion gates behind if @prefix."
    - "Assertions use the stable qualified-identifier token, never whole-SQL equality."
  artifacts:
    - lib/parapet/mcp/server.ex
    - test/parapet/spine/prefix_propagation_test.exs
  key_links:
    - "@prefix Parapet.Spine.Schema.__prefix__() bound at module top (baked at compile time, matches what @schema_prefix saw)."
    - "The test imports the REAL extracted builder Parapet.MCP.Server.timeline_for_correlation_query/1 — never rebuilds the query inline (RESEARCH Pitfall 2)."
    - "to_sql does NOT support :insert_all — those paths use Ecto.get_meta on returned structs (D-08)."
  prohibitions:
    - "No public API / telemetry / call-site-semantics change in mcp/server.ex (mix verify.public_api stays green)."
    - "No to_sql(:insert_all, ...) call (raises FunctionClauseError — RESEARCH Pitfall 3)."
    - "No whole-SQL-string equality or alias/placeholder-text assertion (breaks on Ecto patch releases)."
---

<objective>
Land the one justified production edit (extract mcp/server.ex's inlined timeline join into a @doc false
pure builder) and the PROP-01/PROP-03 positive-proof test suite that rides the REAL call sites — proving
the compiled prefix propagates across selects, the two spine↔spine joins, insert_all, and Ecto.Multi
with zero call-site semantic change.

Purpose: Positive proof complements Plan 02's negative guard. The extraction pins the real site so the
test can't drift green while production regresses (RESEARCH Pitfall 2). Depends on Plan 01 because the
test binds @prefix from the single-sourced Schema.__prefix__().

Output: timeline_for_correlation_query/1 in mcp/server.ex; test/parapet/spine/prefix_propagation_test.exs
under ConcurrencyCase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-CONTEXT.md
@.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-RESEARCH.md
@.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-PATTERNS.md
@.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbol: `Parapet.MCP.Server.timeline_for_correlation_query/1` (`@doc false`) — pure Ecto.Query builder
extracted from the inlined `get_incident_timeline` join; called by `execute_tool/2`.
New file: `test/parapet/spine/prefix_propagation_test.exs` — PROP-01 + PROP-03 behavioral proof.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract timeline_for_correlation_query/1 from mcp/server.ex (D-07)</name>
  <files>lib/parapet/mcp/server.ex</files>
  <read_first>
    - lib/parapet/mcp/server.ex (lines 33-44 — drift-corrected: the get_incident_timeline execute_tool clause with the inlined from/join to extract)
    - lib/parapet/automation/circuit_breaker.ex (lines 40-41 call site + 44-61 execution_count_query/3 — the @doc false pure-builder template)
    - .planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-PATTERNS.md (section "lib/parapet/mcp/server.ex — extract inlined join", lines 267-330; "@doc false Pure Query Builder", lines 575-591)
  </read_first>
  <behavior>
    - Parapet.MCP.Server.timeline_for_correlation_query("key-123") returns an %Ecto.Query{} joining
      TimelineEntry to Incident on incident_id == id, filtered by correlation_key
    - execute_tool("get_incident_timeline", %{"correlation_key" => key}) returns {:ok, list} with identical
      results to before the extraction (the same Ecto.Query flows into repo.all)
  </behavior>
  <action>
    Extract the inlined `from(t in TimelineEntry, join: i in Incident, on: t.incident_id == i.id, where:
    i.correlation_key == ^correlation_key)` (mcp/server.ex:33-44) into a new `@doc false def
    timeline_for_correlation_query(correlation_key)` that returns the query, mirroring
    circuit_breaker.ex's execution_count_query/3 shape. Change the execute_tool/2 clause to call
    `repo.all(timeline_for_correlation_query(correlation_key))`. The query must be byte-for-byte
    equivalent — same bindings, same where — so the Ecto.Query reaching repo.all is unchanged. ZERO
    public API / telemetry / call-site-semantics change (D-07): timeline_for_correlation_query/1 is
    @doc false and execute_tool/2's external contract is identical.
  </action>
  <verify>
    <automated>mix test test/parapet/mcp/ && mix verify.public_api</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def timeline_for_correlation_query" lib/parapet/mcp/server.ex` shows the extracted @doc false builder.
    - `grep -n "timeline_for_correlation_query(correlation_key)" lib/parapet/mcp/server.ex` shows execute_tool/2 delegates to it.
    - `mix verify.public_api` exits 0 (no public API drift).
    - existing mcp tests (if any under test/parapet/mcp/) and the full suite stay green.
  </acceptance_criteria>
  <done>The inlined timeline join is a @doc false pure builder called by execute_tool/2; identical query flows to repo.all; public API unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create prefix_propagation_test.exs — to_sql join proof + get_meta insert_all/Multi proof (PROP-01/PROP-03, D-06/D-08/D-09/D-10)</name>
  <files>test/parapet/spine/prefix_propagation_test.exs</files>
  <read_first>
    - test/support/concurrency_case.ex (lines 1-36 — ConcurrencyCase harness: Sandbox.checkout + ConcurrencyBootstrap.reset! in setup)
    - lib/parapet/automation/circuit_breaker.ex (lines 44-61 — execution_count_query/3, the second spine↔spine join to prove via to_sql)
    - lib/parapet/automation/claim_service.ex (line 111 — repo.insert_all(ActionClaim, ...) path to prove via get_meta)
    - lib/parapet/evidence.ex (lines 82-90 first Ecto.Multi — drift-corrected start 82; lines 157-187 second Ecto.Multi — create_incident path to prove via get_meta)
    - .planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-PATTERNS.md (section "prefix_propagation_test.exs", lines 71-147; Patterns 2 & 3 in RESEARCH lines 254-299)
    - .planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-RESEARCH.md (External API Facts lines 499-549 — to_sql type support, get_meta keys, Multi prefix inheritance)
  </read_first>
  <behavior>
    - to_sql(:all, ConcurrencyRepo, Server.timeline_for_correlation_query("k")) — under parapet leg, SQL
      contains "parapet"."parapet_timeline_entries" (FROM) AND "parapet"."parapet_incidents" (JOIN); under
      public leg, SQL does NOT contain "parapet"."parapet_
    - to_sql(:all, ...) on circuit_breaker.ex execution_count_query/3 — qualified prefix on both FROM and JOIN
      under parapet leg
    - After a sandbox round-trip on claim_service insert_all(ActionClaim, ...): Ecto.get_meta(claim, :prefix) == @prefix
    - After Evidence.create_incident(...) (Ecto.Multi): Ecto.get_meta(incident, :prefix) == @prefix
    - All six spine schemas: __schema__(:prefix) == @prefix (unconditional, both legs)
    - Negative: under if @prefix, the prefixed-leg SQL must not contain a bare unqualified parapet_incidents
  </behavior>
  <action>
    Create test/parapet/spine/prefix_propagation_test.exs with `use Parapet.TestSupport.ConcurrencyCase`,
    alias ConcurrencyRepo and the spine schemas. Bind `@prefix Parapet.Spine.Schema.__prefix__()` at module
    top (D-09 — baked at compile time, leg-aware).
    to_sql proofs (D-08, two spine↔spine joins): call the REAL extracted builder
    Parapet.MCP.Server.timeline_for_correlation_query("key") and circuit_breaker.ex's execution_count_query/3,
    pass each to `Ecto.Adapters.SQL.to_sql(:all, ConcurrencyRepo, query)`, and assert the qualified
    `"#{@prefix}"."parapet_…"` token appears on BOTH the FROM table and the JOIN table. Do NOT rebuild the
    query inline (RESEARCH Pitfall 2). Assert on the stable qualified-identifier token only, never whole-SQL
    equality (D-10).
    get_meta proofs (D-08, insert_all + Multi — to_sql cannot reach :insert_all, RESEARCH Pitfall 3): drive
    the real claim_service insert_all path and the evidence.ex Ecto.Multi (create_incident) under the
    ConcurrencyCase sandbox, then assert `Ecto.get_meta(struct, :prefix) == @prefix` on the returned struct(s).
    Leg-awareness (D-09): the six-schema `__schema__(:prefix) == @prefix` equality assertion runs
    unconditionally (passes on both legs). Gate the negative "no bare parapet_incidents" / "no
    \"parapet\".\"parapet_\" under public" assertions behind `if @prefix` / `else` as in the PATTERNS shape.
  </action>
  <verify>
    <automated>mix test test/parapet/spine/prefix_propagation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/parapet/spine/prefix_propagation_test.exs` exits 0 on the parapet leg.
    - `grep -c "timeline_for_correlation_query" test/parapet/spine/prefix_propagation_test.exs` returns at least 1 (rides the real builder, not an inline rebuild).
    - `grep -c "to_sql(:insert_all" test/parapet/spine/prefix_propagation_test.exs` returns 0 (insert_all uses get_meta).
    - `grep -c "Ecto.get_meta" test/parapet/spine/prefix_propagation_test.exs` returns at least 2 (insert_all + Multi paths).
    - test asserts the qualified token `"parapet"."parapet_timeline_entries"` and `"parapet"."parapet_incidents"` (FROM + JOIN), not whole-SQL equality.
    - Local public-leg check: `PARAPET_SCHEMA_PREFIX="" mix compile --force && PARAPET_SCHEMA_PREFIX="" mix test test/parapet/spine/prefix_propagation_test.exs` exits 0 (leg-aware branches hold).
  </acceptance_criteria>
  <done>prefix_propagation_test.exs proves the compiled prefix on both spine↔spine joins (to_sql FROM+JOIN), the insert_all and Multi paths (get_meta), and six-schema equality; leg-aware negative assertion gated behind if @prefix; passes on both legs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled query → Postgres | The Ecto.Query / insert_all / Multi paths emit SQL against Postgres; the proof that the compiled `@schema_prefix` (not a runtime prefix) materializes on every path is what keeps writes and reads in the same schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-05 | Tampering | spine↔spine joins + insert_all/Multi write paths | high | mitigate | Positive proof: to_sql asserts the qualified `"parapet"."parapet_…"` on FROM and JOIN; Ecto.get_meta asserts the materialized prefix on insert_all/Multi structs. A regression that drops the prefix on any path fails the suite. Riding the REAL extracted builder (not an inline rebuild) prevents a false-green. Verified by Task 2 on both legs. |
| T-52-06 | Spoofing | test rebuilds query in-test instead of pinning the real site | medium | mitigate | D-07 extraction + importing `Parapet.MCP.Server.timeline_for_correlation_query/1` ensures the test exercises the production call site; the grep acceptance check forbids an inline rebuild. |
| T-52-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `mix test test/parapet/spine/prefix_propagation_test.exs` exits 0 on the parapet leg.
- `PARAPET_SCHEMA_PREFIX="" mix compile --force && PARAPET_SCHEMA_PREFIX="" mix test test/parapet/spine/prefix_propagation_test.exs` exits 0 on the public leg.
- `mix verify.public_api` exits 0 (mcp/server.ex extraction is internal).
- Full `mix test` suite green on the parapet leg.
</verification>

<success_criteria>
The compiled prefix is proven on every read/write path (two joins via to_sql FROM+JOIN, insert_all and
Multi via get_meta, six-schema __schema__(:prefix) equality) by tests that ride the real call sites and
assert stable qualified-identifier tokens, passing on both the parapet and public legs.
</success_criteria>

<output>
Create `.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-03-SUMMARY.md` when done
</output>
