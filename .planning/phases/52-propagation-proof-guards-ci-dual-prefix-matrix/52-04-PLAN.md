---
phase: 52-propagation-proof-guards-ci-dual-prefix-matrix
plan: 04
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - .github/workflows/ci.yml
  - test/parapet/spine/compiled_prefix_leg_test.exs
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "The CI test: job matrix gains a schema_prefix axis: parapet × OTP 26/27/28 plus public × OTP 28 only (net +1 cell, 3→4) via matrix.include."
    - "Each cell injects env PARAPET_SCHEMA_PREFIX: ${{ matrix.schema_prefix }} and runs mix compile --force before mix test."
    - "The _build cache key AND restore-keys are namespaced by ${{ matrix.schema_prefix }}; the deps cache stays shared (prefix-independent); fail-fast: false is set."
    - "An in-suite leg guard asserts Parapet.Spine.Schema.__prefix__() equals normalize(PARAPET_SCHEMA_PREFIX) for the leg — the tertiary tripwire against the _build false-green."
    - "--no-validate-compile-env is NOT passed (the free compile_env boot-check stays active)."
  artifacts:
    - .github/workflows/ci.yml
    - test/parapet/spine/compiled_prefix_leg_test.exs
  key_links:
    - "The leg guard calls Parapet.Spine.Schema.normalize/1 (single-sourced in Plan 01) so the test and production share one normalization rule."
    - "Cache-key namespace (primary) + mix compile --force (secondary) + in-suite leg guard (tertiary) + compile_env boot-check (free) are the four false-green defenses."
    - "Only _build cache namespaced — deps cache key unchanged (RESEARCH: dependency artifacts are prefix-independent)."
  prohibitions:
    - "Do NOT namespace the deps cache by schema_prefix (would waste cache; deps are prefix-independent)."
    - "Do NOT pass --no-validate-compile-env in any CI step."
    - "Do NOT copy Oban's non-namespaced _build cache reflex (causes the exact false-green; Parapet's prefix is compile_env not runtime)."
---

<objective>
Land the TEST-03 honest both-legs proof: add a pruned schema_prefix CI matrix axis (parapet × all OTP +
public × primary OTP), force a recompile per leg, namespace the _build cache by prefix so the public leg
cannot silently reuse the parapet build, and add an in-suite leg-guard tripwire. This is the end-to-end
oracle that exercises both legs of Plan 01's normalize/1 across the full suite.

Purpose: Without the cache-key namespace + --force, the public leg restores the parapet-compiled _build
and lies green (the Oban trap). Four defense layers make the proof honest.

Output: .github/workflows/ci.yml matrix + cache + compile changes; test/parapet/spine/compiled_prefix_leg_test.exs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-CONTEXT.md
@.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-RESEARCH.md
@.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-PATTERNS.md
@.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New file: `test/parapet/spine/compiled_prefix_leg_test.exs` — TEST-03 in-suite false-green tripwire (D-14).
CI change: `schema_prefix` matrix axis on the `test:` job; prefix-namespaced `_build` cache key;
`mix compile --force` step; `fail-fast: false`.
No new production symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the in-suite leg guard compiled_prefix_leg_test.exs (D-14)</name>
  <files>test/parapet/spine/compiled_prefix_leg_test.exs</files>
  <read_first>
    - test/parapet/spine/schema_test.exs (lines 43-55 — resolver legacy-nil describe: the use ExUnit.Case async:true, single-assertion-on-__prefix__/0, no-DB analog)
    - .planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-PATTERNS.md (section "compiled_prefix_leg_test.exs", lines 150-185)
    - .planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-RESEARCH.md (D-14 lines; Pitfall 1 "_build Cache False-Green" lines 573-581)
  </read_first>
  <behavior>
    - When PARAPET_SCHEMA_PREFIX=parapet (or unset → defaults "parapet"): Schema.__prefix__() == "parapet"
    - When PARAPET_SCHEMA_PREFIX="" or "public": Schema.__prefix__() == nil
    - The test compares the compiled __prefix__() against Schema.normalize(System.get_env("PARAPET_SCHEMA_PREFIX", "parapet"))
      so it passes on whichever leg compiled the artifact, and FAILS if _build was restored from the other leg
  </behavior>
  <action>
    Create test/parapet/spine/compiled_prefix_leg_test.exs with `use ExUnit.Case, async: true` (no DB).
    Read the env at module-attribute level: `@env_prefix System.get_env("PARAPET_SCHEMA_PREFIX", "parapet")`
    and `@compiled_prefix Parapet.Spine.Schema.__prefix__()`. In the test, compute
    `expected = Parapet.Spine.Schema.normalize(@env_prefix)` (reusing the Plan 01 single-sourced normalizer —
    do NOT re-implement the normalization rule) and assert `@compiled_prefix == expected` with a failure
    message explaining the _build cache was likely restored from a different leg and pointing at the
    cache-key namespace + mix compile --force remedy (D-14, RESEARCH Pitfall 1). Claude's discretion (per
    CONTEXT D-09/D-14 note): may co-locate with the six-schema equality assertion or keep standalone — keep
    standalone here since the six-schema equality already lives in schema_test.exs (Plan 01).
  </action>
  <verify>
    <automated>mix test test/parapet/spine/compiled_prefix_leg_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/parapet/spine/compiled_prefix_leg_test.exs` exits 0 on the default (parapet) leg.
    - `grep -c "Parapet.Spine.Schema.normalize" test/parapet/spine/compiled_prefix_leg_test.exs` returns at least 1 (reuses the single-sourced normalizer).
    - Public-leg check: `PARAPET_SCHEMA_PREFIX="" mix compile --force && PARAPET_SCHEMA_PREFIX="" mix test test/parapet/spine/compiled_prefix_leg_test.exs` exits 0.
    - The failure message references the _build cache restore / mix compile --force remedy.
  </acceptance_criteria>
  <done>compiled_prefix_leg_test.exs asserts __prefix__() == normalize(env) using the single-sourced normalizer, passes on both legs, and teaches the false-green remedy on failure.</done>
</task>

<task type="auto">
  <name>Task 2: Add schema_prefix matrix axis, prefix-namespaced _build cache, --force compile to ci.yml (D-11/D-12/D-13)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (lines 63-68 test: job matrix — elixir/otp only; lines 92-97 deps cache — leave unchanged; lines 98-103 _build cache to namespace; lines 106-107 Test step)
    - .planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-PATTERNS.md (section ".github/workflows/ci.yml", lines 440-507 — exact before/after YAML)
    - .planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-RESEARCH.md (Pattern 5 lines 340-378; "Verified: CI cache key has no schema_prefix today" lines 477-485; "Verified: Primary OTP" lines 491-493 → 28.x)
  </read_first>
  <action>
    Edit the test: job strategy (ci.yml:63-68): add `fail-fast: false`, add `schema_prefix: ['parapet']` to
    the matrix, and a `matrix.include:` entry adding the single public cell `{elixir: '1.19.0', otp: '28.x',
    schema_prefix: 'public'}` (28.x is the verified primary OTP — D-11, net +1 cell 3→4). Add
    `PARAPET_SCHEMA_PREFIX: ${{ matrix.schema_prefix }}` to the job-level `env:` block alongside the existing
    `MIX_ENV: test` (D-12).
    Namespace the _build cache (ci.yml:98-103, D-13): insert `${{ matrix.schema_prefix }}-` into BOTH the
    `key:` and `restore-keys:` between the otp segment and the mix.lock-hash segment. Leave the deps cache
    (ci.yml:92-97) UNCHANGED — deps are prefix-independent (RESEARCH; do NOT namespace it).
    Add a `mix compile --force` step BEFORE the Test step (D-12) so the new env value is baked into _build.
    Do NOT pass `--no-validate-compile-env` anywhere (D-14 — keep the free boot-check). The lint and demo
    jobs stay single-leg (unchanged).
  </action>
  <verify>
    <automated>ruby -ryaml -e "YAML.load_file('.github/workflows/ci.yml')" || python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml parses as valid YAML (the verify command exits 0).
    - `grep -c "schema_prefix: \['parapet'\]" .github/workflows/ci.yml` returns at least 1 and an include entry sets `schema_prefix: 'public'` with `otp: '28.x'`.
    - `grep -c "fail-fast: false" .github/workflows/ci.yml` returns at least 1.
    - The _build cache key contains `${{ matrix.schema_prefix }}` in BOTH key and restore-keys; the deps cache key does NOT (`grep -n "mix-" .github/workflows/ci.yml` shows the deps key unchanged).
    - `grep -c "PARAPET_SCHEMA_PREFIX: \${{ matrix.schema_prefix }}" .github/workflows/ci.yml` returns at least 1.
    - `grep -c "mix compile --force" .github/workflows/ci.yml` returns at least 1; `grep -c "no-validate-compile-env" .github/workflows/ci.yml` returns 0.
    - Local both-legs sanity: `PARAPET_SCHEMA_PREFIX=parapet mix compile --force && mix test` then `PARAPET_SCHEMA_PREFIX="" mix compile --force && PARAPET_SCHEMA_PREFIX="" mix test` both exit 0.
  </acceptance_criteria>
  <done>The test: job runs 4 cells (parapet×26/27/28 + public×28), injects PARAPET_SCHEMA_PREFIX, force-recompiles, namespaces only the _build cache by prefix, sets fail-fast: false, and never passes --no-validate-compile-env.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI cache restore → compiled artifact | The GitHub Actions `_build` cache can restore an artifact compiled under a different `schema_prefix` leg; without a prefix-namespaced key the public leg silently reuses the parapet build and the both-legs proof is a false-green (security-testing bypass). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-07 | Tampering (test bypass) | `_build` cache false-green on the public leg | high | mitigate | Four defense layers (D-13/D-12/D-14): prefix-namespaced `_build` cache key (primary), `mix compile --force` (secondary), in-suite `compiled_prefix_leg_test.exs` tripwire (tertiary), and the never-disabled `compile_env` boot-check (free). The in-suite guard fails the suite if the compiled prefix ≠ the leg's env, so a false-green cannot pass silently. Verified by Task 1 both-legs check + Task 2 cache-key grep. |
| T-52-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `test/parapet/spine/compiled_prefix_leg_test.exs` passes on both legs locally.
- ci.yml parses as valid YAML; matrix produces 4 cells; only _build cache namespaced by prefix; fail-fast: false; --force present; no --no-validate-compile-env.
- Push-time oracle: both `test (… parapet)` cells and the `test (… public)` cell pass in CI (manual — requires the GitHub Actions runner; see Manual-Only verification in 52-VALIDATION.md).
</verification>

<success_criteria>
CI reruns the full suite under both the parapet and public legs with a prefix-namespaced _build cache and
--force recompile, an in-suite tripwire catches any _build false-green, and the compile_env boot-check
stays enabled — the honest proof of "green under parapet AND under nil."
</success_criteria>

<output>
Create `.planning/phases/52-propagation-proof-guards-ci-dual-prefix-matrix/52-04-SUMMARY.md` when done
</output>
