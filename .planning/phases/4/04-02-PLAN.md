---
phase: 04
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/parapet/integrations/scoria.ex
  - test/parapet/integrations/scoria_test.exs
autonomous: true
requirements:
  - AI-HITL-01
  - AI-HITL-02
must_haves:
  truths:
    - "System emits metrics when workflow staleness telemetry occurs."
    - "System captures ActionItem natively on workflow staleness."
    - "System can idempotently check workflow status on resume events."
  artifacts:
    - path: "lib/parapet/integrations/scoria.ex"
      provides: "Telemetry handlers for staleness"
      contains: "handle_event([:scoria, :workflow, :stale]"
  key_links:
    - from: "lib/parapet/integrations/scoria.ex"
      to: "lib/parapet/evidence.ex"
      via: "create_action_item"
      pattern: "Evidence.create_action_item"
---

<objective>
Implement Scoria dual-track telemetry integration.

Purpose: Track 1 emits low-cardinality Prom metrics for alerting. Track 2 syncs durable pointers (ActionItem) into Ecto for Operator UI rendering.
Output: Telemetry handler capturing `[:scoria, :workflow, :stale]` and `[:scoria, :workflow, :resumed]`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/4/RESEARCH.md
@lib/parapet/integrations/scoria.ex
</context>

<interfaces>
From lib/parapet/evidence.ex:
```elixir
def create_action_item(attrs \\ %{})
def resolve_action_item(id)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Handle Staleness Telemetry</name>
  <files>lib/parapet/integrations/scoria.ex, test/parapet/integrations/scoria_test.exs</files>
  <behavior>
    - Test 1: Staleness event emits `[:parapet, :scoria, :metrics, :stale]` and calls `Evidence.create_action_item`.
    - Test 2: Expiration event emits `[:parapet, :scoria, :metrics, :expired]`.
  </behavior>
  <action>Update `setup/0` to attach to `[:scoria, :workflow, :stale]` and `[:scoria, :workflow, :expired]`. Add `process_event` for these events. Follow the 'Telemetry Attachment pattern' and 'Core Telemetry processing pattern' as mapped in PATTERNS.md for `lib/parapet/integrations/scoria.ex`. Emit low-cardinality metrics (Track 1), explicitly including the metric `scoria_workflow_stale_total`, and synchronously create an `ActionItem` (Track 2) via `Evidence.create_action_item` with `integration: "scoria"`, `external_id: metadata.workflow_id`, and `title`. Handle exceptions gracefully as existing patterns do.</action>
  <verify>
    <automated>mix test test/parapet/integrations/scoria_test.exs</automated>
  </verify>
  <done>Telemetry correctly split between metrics and durable action items.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handle Resumed Telemetry</name>
  <files>lib/parapet/integrations/scoria.ex, test/parapet/integrations/scoria_test.exs</files>
  <behavior>
    - Test 1: Resume event triggers status check before closing.
  </behavior>
  <action>Attach to `[:scoria, :workflow, :resumed]`. Implement `process_event` for it. Because of dropped telemetry risk, do NOT close the ActionItem directly here without checking. Implement the actual status check against Scoria in `Parapet.Integrations.Scoria.check_status/1`. Query the real external Scoria workflow state (e.g., using `Scoria.Workflow.get_state(workflow_id)`) and conditionally delegate to `Evidence.resolve_action_item` ONLY if the state indicates it is no longer paused. Call `check_status` from the resume telemetry handler, ensuring it doesn't blindly update the database.</action>
  <verify>
    <automated>mix test test/parapet/integrations/scoria_test.exs</automated>
  </verify>
  <done>Resume event acts as cache invalidation hint and does real status check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telemetry | Internal messages are assumed trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Denial of Service | Telemetry handlers | mitigate | Rescue exceptions in `handle_event/4` to prevent crashing the emitter. |
</threat_model>
