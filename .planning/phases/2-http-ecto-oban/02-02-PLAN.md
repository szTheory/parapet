---
phase: 02-http-ecto-oban
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/parapet/metrics/ecto.ex
  - lib/parapet/metrics/oban.ex
  - test/parapet/metrics/ecto_test.exs
  - test/parapet/metrics/oban_test.exs
autonomous: true
requirements: [ECTO-01, ECTO-02, ECTO-03, OBAN-01, OBAN-02, OBAN-03, OBAN-04, ERR-02]
must_haves:
  truths:
    - Ecto queue and query times appear as separate histograms.
    - Oban telemetry is emitted conditionally and app does not break if Oban is not loaded.
    - Oban metrics track job states in a way that allows rate() based alerting.
    - Startup metrics registration catches duplicate ArgumentError and logs rather than crashing.
  artifacts:
    - path: lib/parapet/metrics/ecto.ex
      provides: Database pool and query latency metrics
    - path: lib/parapet/metrics/oban.ex
      provides: Oban queue health metrics conditionally
  key_links:
    - from: lib/parapet/metrics/oban.ex
      to: Oban telemetry
      via: Code.ensure_loaded?(Oban)
---

<objective>
Implement the Ecto and Oban metrics collection surfaces for Parapet, enabling the telemetry pipeline to monitor database and background worker health.

Purpose: Provide out-of-the-box visibility into database queries and background jobs without adding hard dependencies.
Output: Core metrics modules for Ecto and Oban and corresponding test suites.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/2-http-ecto-oban/RESEARCH.md
@.planning/phases/2-http-ecto-oban/2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Scaffold Ecto/Oban Test Files</name>
  <files>test/parapet/metrics/ecto_test.exs, test/parapet/metrics/oban_test.exs</files>
  <action>Create the ExUnit test scaffolds for the Ecto and Oban metrics components to support TDD-style verification for subsequent tasks.</action>
  <verify>
    <automated>mix test test/parapet/metrics/ecto_test.exs test/parapet/metrics/oban_test.exs</automated>
  </verify>
  <done>Both test files exist and run (even if 0 tests or failing).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Build Ecto Metrics Registration</name>
  <files>lib/parapet/metrics/ecto.ex, test/parapet/metrics/ecto_test.exs</files>
  <behavior>
    - Test 1: Handle event from [:my_app, :repo, :query] converting native to ms.
    - Test 2: Set source label to metadata.source or "_raw".
    - Test 3: Defines separate distributions for query_time_ms and queue_time_ms, wrapped in try/rescue ArgumentError.
  </behavior>
  <action>Create `Parapet.Metrics.Ecto` that attaches to `[:my_app, :repo, :query]` events. Use `Parapet.attach/1` to ensure handlers are insulated from crashes. It must convert native time measurements to milliseconds and bucket Ecto sources (using "_raw" if absent). Register corresponding Telemetry.Metrics wrapped in `try/rescue` and validate via `Parapet.Internal.LabelPolicy.assert_safe!/1`. Base the implementation on the closest analog file: `lib/parapet/metrics/http.ex`.</action>
  <verify>
    <automated>mix test test/parapet/metrics/ecto_test.exs</automated>
  </verify>
  <done>Ecto metrics are registered safely and handle default values (e.g., "_raw") correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Oban Metrics conditionally</name>
  <files>lib/parapet/metrics/oban.ex, test/parapet/metrics/oban_test.exs</files>
  <behavior>
    - Test 1: Conditionally defines module if Code.ensure_loaded?(Oban).
    - Test 2: Attaches to job stop and exception events using Parapet.attach/1.
    - Test 3: Registers job distributions and counters wrapped in try/rescue ArgumentError with explicitly aligned state labels for rate() evaluation.
  </behavior>
  <action>Create `Parapet.Metrics.Oban`. Wrap the entire module logic inside `if Code.ensure_loaded?(Oban) do ... end`. Attach telemetry handlers for `[:oban, :job, :stop]` and `[:oban, :job, :exception]` using `Parapet.attach/1`, extracting `meta.state`. Define metrics securely inside `try/rescue` and use `Parapet.Internal.LabelPolicy.assert_safe!/1`. Crucially, to satisfy requirement OBAN-02, ensure that `parapet_oban_jobs_total` is registered as a counter metric explicitly grouping by the `[:worker, :queue, :state]` labels, providing the necessary signature for Phase 3's `rate()` calculations over a minimum window. Base the implementation on the closest analog file: `lib/parapet/metrics/http.ex`.</action>
  <verify>
    <automated>mix test test/parapet/metrics/oban_test.exs</automated>
  </verify>
  <done>Module does not crash host if Oban is omitted, and accurately processes Oban job lifecycles with counter signatures properly aligned for `rate()` math.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External dependencies | Oban presence must be gracefully handled to prevent internal node crashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | Parapet.Metrics.Ecto | mitigate | Wrap `Telemetry.Metrics` registration in `try/rescue e in [ArgumentError]` to avoid initialization node crashes. Apply `LabelPolicy.assert_safe!/1`. |
| T-02-04 | Denial of Service | Parapet.Metrics.Oban | mitigate | Wrap `Telemetry.Metrics` registration in `try/rescue e in [ArgumentError]` to avoid initialization node crashes. Conditional compilation ensures absent deps won't halt boot. |
</threat_model>

<verification>
mix test test/parapet/metrics/ecto_test.exs test/parapet/metrics/oban_test.exs
mix format --check-formatted
mix credo --strict
</verification>

<success_criteria>
All ExUnit tests for Ecto and Oban modules are passing.
Ecto times correctly transformed and separated into distinct buckets.
Oban compilation correctly handled via `ensure_loaded?`.
Application restarts or duplicate config do not trigger a boot crash (`ArgumentError` mitigated).
Oban job count metrics accurately expose state for `rate()` aggregations.
</success_criteria>

<output>
After completion, create `.planning/phases/2-http-ecto-oban/02-02-SUMMARY.md`
</output>