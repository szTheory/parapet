---
phase: 56-contract-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/parapet/metrics/ecto_test.exs
  - test/telemetry_contract_test.exs
autonomous: true
requirements: [SAFE-02]
must_haves:
  truths:
    - "A real spine query under the default `parapet` prefix emits Ecto telemetry whose `:source` is the bare table name `parapet_incidents` with no `parapet.` schema qualifier (D-08/D-09)."
    - "The telemetry contract test stays green and still pins exactly 35 families — no `[:parapet, :schema, ...]` family is added (D-07)."
  artifacts:
    - "test/parapet/metrics/ecto_test.exs — new behavioral test asserting bare `:source` from a live ConcurrencyRepo spine query."
  prohibitions:
    - "MUST NOT add any new telemetry event/family (would break the length-35 assertion in telemetry_contract_test.exs)."
    - "MUST NOT assert a schema-qualified `:source` value like `parapet.parapet_incidents` — the bare name is the whole point."
    - "MUST NOT modify lib/parapet/metrics/ecto.ex or any production code — this plan is test-only."
  key_links:
    - "Ecto derives `:source` from `schema.__schema__(:source)` (bare table name), structurally independent of `@schema_prefix` (the Postgres schema)."
---

<objective>
Add the one genuinely-new piece of code this phase: a behavioral ExUnit assertion proving that Ecto query telemetry `:source` remains the bare table name `parapet_incidents` under the default `parapet` schema prefix, and re-assert the frozen telemetry contract (35 families, no `schema` event).

Purpose: SAFE-02 claims the prefix change does not leak into telemetry. The `:source` value is currently unproven — the existing fixture only pins `:source` as a metadata *key*, not its *value* (D-08). A future refactor could leak `parapet.parapet_incidents` into the Prometheus `:source` label and silently double series cardinality. This behavioral test guards that invariant (D-09).

Output: A new test in `test/parapet/metrics/ecto_test.exs` and an optional documenting note in `test/telemetry_contract_test.exs`; no production code changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-contract-release-hardening/56-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- One new ExUnit test in `test/parapet/metrics/ecto_test.exs` (test name at executor discretion per D-09, e.g. "Test 4: real spine query emits bare :source == parapet_incidents (no schema qualifier)").
- Optionally, one static documenting comment/note in `test/telemetry_contract_test.exs` near the `[:parapet, :ecto, :query] => [:source]` fixture (line ~168).
No new production symbols, no new telemetry families, no new exports.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add behavioral bare-name :source assertion driven by a live spine query</name>
  <files>test/parapet/metrics/ecto_test.exs</files>
  <read_first>
    - test/parapet/metrics/ecto_test.exs (existing Tests 1-3; the module uses `use ExUnit.Case, async: true` and synthetic sources only — "users", "accounts", "_raw")
    - test/parapet/spine/prefix_propagation_test.exs (the established pattern: `use Parapet.TestSupport.ConcurrencyCase, async: false`, `import Ecto.Query`, `alias Parapet.Spine.Incident`, `alias Parapet.TestSupport.ConcurrencyRepo`, and driving real Incident queries through ConcurrencyRepo)
    - test/support/concurrency_case.ex (Sandbox.checkout + ConcurrencyBootstrap.reset! per test; sets Application.put_env(:parapet, :repo, ConcurrencyRepo))
    - test/support/concurrency_repo.ex (real Postgres repo; default Ecto telemetry prefix is [:parapet, :test_support, :concurrency_repo], so its query event is [:parapet, :test_support, :concurrency_repo, :query])
    - lib/parapet/metrics/ecto.ex (line 79: `source = Map.get(metadata, :source, "_raw")` — Ecto supplies `:source` from `schema.__schema__(:source)`, the bare table name)
    - lib/parapet/spine/incident.ex (line 37: `schema "parapet_incidents"` — the bare source; `@schema_prefix` is applied separately by the macro)
    - test/test_helper.exs (ConcurrencyRepo is always started + bootstrapped; DB is unconditionally available; no exclude tags gate DB tests)
  </read_first>
  <action>
    Add a new behavioral test to `test/parapet/metrics/ecto_test.exs` that proves Ecto's `:source` is the bare, unprefixed table name when a real spine query runs under the default `parapet` prefix.

    Because the existing module is `use ExUnit.Case, async: true` (no DB sandbox), do NOT convert the whole module. Instead add a SEPARATE describe block or a clearly-scoped test that establishes its own DB access using the ConcurrencyCase pattern. Two acceptable implementation shapes (executor picks one):

    (A) Preferred — same file, DB-scoped test using the sandbox directly: inside the test, check out the ConcurrencyRepo sandbox and reset the bootstrap the same way `Parapet.TestSupport.ConcurrencyCase` does (`Ecto.Adapters.SQL.Sandbox.checkout(Parapet.TestSupport.ConcurrencyRepo)`, then `Parapet.TestSupport.ConcurrencyBootstrap.reset!()`), attach a telemetry handler to the RAW repo query event `[:parapet, :test_support, :concurrency_repo, :query]` that forwards `metadata` to `self()`, run a real query `Parapet.TestSupport.ConcurrencyRepo.all(from i in Parapet.Spine.Incident, select: i.id)`, receive the metadata, and assert `metadata.source == "parapet_incidents"`. Detach the handler on exit.

    (B) Alternative — a new file `test/parapet/metrics/ecto_source_test.exs` that does `use Parapet.TestSupport.ConcurrencyCase, async: false` (getting sandbox checkout + reset for free) and performs the same telemetry-attach + real-query + assert flow. If the executor chooses this shape, update this plan's files_modified is NOT required — but the assertion content is identical.

    The assertion MUST be twofold and exact:
    - Positive: the captured `:source` equals the bare string `parapet_incidents` (assert equality against that exact literal).
    - Negative: the captured `:source` does NOT contain a `parapet.` schema qualifier (assert it does not start with, and does not equal, a schema-qualified form). Phrase the negative by concept — check that the value has no `.` schema-qualifier segment — rather than embedding the qualified literal as a matched string, per comment-text discipline.

    Confirm at the top of the test (in a comment) WHY this holds: Ecto sets `:source` from `schema.__schema__(:source)` = the value passed to `schema "parapet_incidents"`, which is prefix-free; `@schema_prefix` only sets the Postgres schema for FROM/JOIN qualification, never the `:source` metadata. Do NOT restate the mechanism as prose that duplicates docs/upgrade-1.x.md.

    Use the raw repo query event `[:parapet, :test_support, :concurrency_repo, :query]` (Ecto's own event, which carries `:source` from `__schema__(:source)`), NOT the re-emitted `[:parapet, :ecto, :query]` — the raw event is the authoritative source of the `:source` value under test. Give the telemetry handler a unique id and detach it in an on_exit/after block.

    Do NOT modify `lib/parapet/metrics/ecto.ex` or any production file.
  </action>
  <behavior>
    - Test: a real `Parapet.Spine.Incident` query executed through `ConcurrencyRepo` under the default (`parapet`) prefix emits Ecto query telemetry whose `metadata.source == "parapet_incidents"`.
    - Test (negative): the emitted `:source` carries no `parapet.` schema qualifier (no dotted schema segment).
  </behavior>
  <verify>
    <automated>mix test test/parapet/metrics/ecto_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - New test passes: a live spine query's Ecto telemetry `:source` equals the bare `parapet_incidents`.
    - The negative assertion (no `parapet.` schema qualifier) passes.
    - No production file was modified (`git diff --name-only lib/` shows nothing from this plan).
    - The full `mix test test/parapet/metrics/ecto_test.exs` file is green (existing Tests 1-3 still pass).
  </acceptance_criteria>
  <done>The bare-name `:source` invariant is proven by a behavioral test driving a real spine query; no production code changed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-assert the frozen 35-family telemetry contract (optional documenting note)</name>
  <files>test/telemetry_contract_test.exs</files>
  <read_first>
    - test/telemetry_contract_test.exs (lines 168 fixture `[:parapet, :ecto, :query] => [:source]`; lines 212-216 the exact-35-families length assertion)
  </read_first>
  <action>
    Run the existing telemetry contract test unchanged to confirm it stays green and still pins exactly 35 families with none being a `[:parapet, :schema, ...]` family (D-07 re-assert half — no new event this phase).

    OPTIONALLY (executor discretion per D-09), add a single one-line documenting comment adjacent to the `[:parapet, :ecto, :query] => [:source]` fixture (line ~168) pointing to the new behavioral bare-name assertion in the metrics ecto test — e.g. noting that the *value* of `:source` (bare table name, prefix-free) is proven behaviorally in the metrics ecto test, while this fixture pins the metadata *key*. Keep it to a comment only; do NOT add, rename, or remove any family, and do NOT change the length assertion. If adding the comment risks any churn, skip it — the behavioral test in Task 1 is the required proof; this note is decorative.

    Do NOT add a `schema` family, do NOT change the count 35, do NOT alter any assertion logic.
  </action>
  <verify>
    <automated>mix test test/telemetry_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/telemetry_contract_test.exs` is green.
    - The "all documented event families total 35" assertion still passes (count unchanged).
    - `grep -n 'parapet, :schema' test/telemetry_contract_test.exs` returns no telemetry-family match (no schema family introduced).
    - If a documenting comment was added, it is a comment only and did not change any fixture entry or the length assertion.
  </acceptance_criteria>
  <done>Telemetry contract confirmed frozen at 35 families with no schema event; optional cross-reference note added or intentionally skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ecto query metadata → Prometheus `:source` label | The `:source` value becomes a metric label; if it were to carry the schema qualifier, series cardinality could change unexpectedly. This plan is test-only and asserts the invariant; it introduces no new input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Information Disclosure / Denial of Service | Prometheus `:source` label cardinality | medium | mitigate | The new behavioral assertion pins `:source` to the bare table name; a future refactor leaking `parapet.parapet_incidents` (which would double series cardinality) fails the test. No high-severity injection/authz threat exists — this is a contract-regression guard, not a runtime input path. |
| T-56-02 | Tampering | Telemetry contract (35 families) | low | mitigate | The existing length-35 assertion auto-fails on any added/removed family; re-asserted green this plan. No new event introduced. |
</threat_model>

<verification>
- `mix test test/parapet/metrics/ecto_test.exs` green (new behavioral test included).
- `mix test test/telemetry_contract_test.exs` green (35 families, no schema event).
- No production code modified by this plan.
</verification>

<success_criteria>
SAFE-02 fully proven: telemetry contract frozen at 35 families with no schema event, AND the Ecto `:source` bare-name invariant proven behaviorally by a live spine query.
</success_criteria>

<output>
Create `.planning/phases/56-contract-release-hardening/56-01-SUMMARY.md` when done.
</output>
