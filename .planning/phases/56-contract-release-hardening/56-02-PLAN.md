---
phase: 56-contract-release-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/56-contract-release-hardening/56-SAFE-01-EVIDENCE.md
autonomous: true
requirements: [SAFE-01]
must_haves:
  truths:
    - "`mix verify.public_api` (no `--write`) exits 0 — the frozen public API manifest has zero drift despite the v1.7 schema-prefix change (D-05/D-06)."
    - "The only prefix-related public export, `Parapet.Evidence.schema_prefix/0`, is a pre-existing Stable helper already in the frozen manifest — no export was added by phases 53-55."
  artifacts:
    - "56-SAFE-01-EVIDENCE.md — captured proof (command + zero-drift exit) feeding the D-10 done-criterion record in Plan 04."
  prohibitions:
    - "MUST NOT run `mix verify.public_api --write` — a drift means an accidental contract expansion to be walled off (`Parapet.Internal.*` / `@moduledoc false`), NOT blessed into the manifest (D-06)."
    - "MUST NOT add any new public export or production/task code (D-05)."
    - "MUST NOT add a bespoke milestone-gate mix task or a `mix ci` alias (D-10 / deferred to v1.8)."
  key_links:
    - "The compile-time `@schema_prefix`/`@prefix` attribute is not an export; it is structurally impossible for the prefix to appear in the exported surface — this plan proves the invariant the architecture already guarantees."
---

<objective>
Prove SAFE-01: run the already-existing, CI-gated `mix verify.public_api` and demonstrate zero `--write` drift, capturing the result as evidence for the milestone done-criterion (D-10, recorded in Plan 04). No new production or task code is written.

Purpose: The v1.7 schema-prefix change is a compile-time module attribute, not an export. SAFE-01 requires proving the frozen public API surface did not expand across phases 53-55 (D-05). If the manifest drifts, that is a signal of accidental contract leakage to be walled off, NOT blessed with `--write` (D-06).

Output: A short evidence file recording the exact command and its zero-drift result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-contract-release-hardening/56-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan produces exactly one evidence artifact:
- `.planning/phases/56-contract-release-hardening/56-SAFE-01-EVIDENCE.md` — records the `mix verify.public_api` invocation, its exit status (0 = zero drift), and the note that `Parapet.Evidence.schema_prefix/0` is the sole prefix-related export and is pre-existing/Stable in the frozen manifest.
No production symbols, no manifest changes, no new exports.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run verify.public_api and prove zero drift</name>
  <files>.planning/phases/56-contract-release-hardening/56-SAFE-01-EVIDENCE.md</files>
  <read_first>
    - lib/mix/tasks/verify.public_api.ex (line 25 `write_manifest? = "--write" in args`; line 97 `verify_stable_manifest!`; lines 104-111 drift → `System.halt(1)`; line 128 module filter excludes `Parapet.Internal.*`)
    - priv/parapet/public_api_stable.json (frozen manifest; line 46 `schema_prefix/0` under Parapet.Evidence)
    - .github/workflows/ci.yml (line ~54-55: `Verify Public API` step runs `mix verify.public_api` — the enforcement backstop, D-10)
    - lib/parapet/evidence.ex (the `schema_prefix/0` export, `@doc since: "1.0.3"`, pre-existing Stable helper — NOT added this milestone)
    - lib/parapet/spine/schema.ex (lines 81-102: `@raw_prefix`/`@prefix`/`__prefix__` — compile-time attribute + `@doc false` internal reader, not part of the exported surface)
  </read_first>
  <action>
    Run `mix verify.public_api` WITHOUT the `--write` flag. Confirm it exits 0 (zero drift). The task halts with exit 1 and prints "Stable public API manifest drifted" on any drift.

    If it exits 0: write `.planning/phases/56-contract-release-hardening/56-SAFE-01-EVIDENCE.md` capturing:
    - The exact command run (`mix verify.public_api`) and its exit status 0.
    - The confirmation that `git status --short priv/parapet/public_api_stable.json` shows the manifest UNCHANGED (no drift means the task did not, and must not, alter it).
    - A one-line note: the sole prefix-related public export is `Parapet.Evidence.schema_prefix/0` (pre-existing Stable, `@doc since: "1.0.3"`, already at line 46 of the frozen manifest); the compile-time `@schema_prefix`/`@prefix` attribute is not an export; phases 53-55 added zero exports (D-05).

    If it exits NON-ZERO (drift detected): STOP. Do NOT run `--write`. This signals an accidental contract expansion leaked from phases 53-55. Record the drifting symbol(s) from the task output in the evidence file and surface a blocker recommending the leaked export be walled off (`Parapet.Internal.*` namespace or `@moduledoc false`), per D-06. Do not attempt to bless the drift.

    Do NOT write any production or task code. Do NOT add a `mix ci` alias or a milestone-gate task (deferred to v1.8 / CI-01, D-10).
  </action>
  <verify>
    <automated>mix verify.public_api && test -z "$(git status --short priv/parapet/public_api_stable.json)" && echo ZERO_DRIFT</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.public_api` (no `--write`) exits 0.
    - `git status --short priv/parapet/public_api_stable.json` is empty (manifest unchanged — the run performed no `--write`).
    - `56-SAFE-01-EVIDENCE.md` exists and records the command, exit 0, and the `schema_prefix/0`-is-pre-existing note.
    - `grep -c 'schema_prefix/0' priv/parapet/public_api_stable.json` returns at least 1 (export present and frozen).
    - No file under `lib/` or `priv/` was modified by this plan.
  </acceptance_criteria>
  <done>SAFE-01 proven: public API manifest has zero drift; evidence captured; no code or manifest changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library public API surface → adopter code | Adopters depend on the frozen exported surface; an accidental new export would silently expand the contract. This plan verifies the boundary is unchanged; it adds no new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-03 | Tampering / Elevation of Privilege (contract creep) | Public API manifest (`public_api_stable.json`) | medium | mitigate | `mix verify.public_api` fails CI on any drift; this plan re-asserts zero drift and forbids `--write` blessing. A leaked export would be walled off to `Parapet.Internal.*`, not accepted. No new inputs or runtime paths — the compile-time prefix attribute cannot appear in the exported surface. |
</threat_model>

<verification>
- `mix verify.public_api` exits 0.
- `priv/parapet/public_api_stable.json` unchanged (no `--write`).
- Evidence file records the zero-drift proof.
</verification>

<success_criteria>
SAFE-01 satisfied: the frozen public API surface is provably unchanged by the v1.7 schema-prefix work, captured as evidence for the milestone done-criterion.
</success_criteria>

<output>
Create `.planning/phases/56-contract-release-hardening/56-02-SUMMARY.md` when done.
</output>
