---
phase: 56-contract-release-hardening
plan: 04
type: execute
wave: 2
depends_on: ["56-01", "56-02", "56-03"]
files_modified:
  - .planning/phases/56-contract-release-hardening/56-VERIFICATION.md
  - .planning/phases/56-contract-release-hardening/56-UAT.md
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-04]
must_haves:
  truths:
    - "The three frozen-contract checks are recorded as milestone done-criteria in `56-VERIFICATION.md` and `56-UAT.md`, each traceable to its CI enforcement backstop (D-10)."
    - "Each recorded criterion is command-checkable and green: `mix verify.public_api` zero-drift (SAFE-01); telemetry contract 35 families + no schema event + behavioral bare-name `:source` (SAFE-02); two-part honest CHANGELOG feat entry (SAFE-04)."
  artifacts:
    - "56-VERIFICATION.md — goal-backward verification record for the three checks."
    - "56-UAT.md — done-criteria acceptance record."
  prohibitions:
    - "MUST NOT introduce a bespoke aggregate 'milestone gate' mix task or a `mix ci` alias — the enforcement backstop is the already-existing CI (ci.yml Verify Public API step + the ExUnit suite), per D-10."
    - "MUST NOT re-run `--write` or edit the manifest, production code, or telemetry families."
  key_links:
    - "Enforcement backstop is existing CI: `.github/workflows/ci.yml` runs `mix verify.public_api` (SAFE-01) and the ExUnit suite covers the telemetry contract + new ecto assertion (SAFE-02). No new gate is added."
---

<objective>
Close the phase (and the v1.7 milestone) by recording the three frozen-contract checks as milestone done-criteria in `56-VERIFICATION.md` and `56-UAT.md`, each tied to its already-existing CI enforcement backstop (D-10). This is the "asserted as a milestone done-criterion" deliverable — a documentation record, not a new gate.

Purpose: SAFE-01/02/04 each require the check be pinned as a milestone done-criterion. Per D-10 the enforcement is the existing CI (ci.yml `Verify Public API` step for SAFE-01; the ExUnit suite for the SAFE-02 telemetry contract + new ecto assertion), NOT a new bespoke runner or `mix ci` alias (deferred to v1.8 / CI-01).

Output: `56-VERIFICATION.md` and `56-UAT.md` recording the three green checks and their backstops.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-contract-release-hardening/56-CONTEXT.md
@.planning/phases/56-contract-release-hardening/56-01-SUMMARY.md
@.planning/phases/56-contract-release-hardening/56-02-SUMMARY.md
@.planning/phases/56-contract-release-hardening/56-03-SUMMARY.md
@.planning/phases/56-contract-release-hardening/56-SAFE-01-EVIDENCE.md
</context>

<artifacts_this_phase_produces>
This plan produces exactly two records:
- `.planning/phases/56-contract-release-hardening/56-VERIFICATION.md`
- `.planning/phases/56-contract-release-hardening/56-UAT.md`
Each records the three done-criteria (SAFE-01/02/04) with the exact command that proves each and the CI backstop that enforces it. No code, no gate task.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Record the three done-criteria in 56-VERIFICATION.md</name>
  <files>.planning/phases/56-contract-release-hardening/56-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/55-demo-app-upgrade-docs/55-VERIFICATION.md (prior-phase artifact shape: frontmatter with phase/verified/status/score; "Goal Achievement" observable-truths table; "Required Artifacts" table — follow this shape, D-10 discretion clause)
    - .planning/phases/56-contract-release-hardening/56-01-SUMMARY.md, 56-02-SUMMARY.md, 56-03-SUMMARY.md (the executed results to cite)
    - .planning/phases/56-contract-release-hardening/56-SAFE-01-EVIDENCE.md (the zero-drift proof)
    - .github/workflows/ci.yml (the `Verify Public API` step + the `test` job — the CI backstops to cite)
  </read_first>
  <action>
    Write `.planning/phases/56-contract-release-hardening/56-VERIFICATION.md` following the Phase-55 shape (frontmatter + observable-truths table + artifacts table). Record the phase goal ("The milestone closes with the public API and telemetry contracts provably frozen and the change framed honestly as additive for existing installs") and these three done-criteria as observable truths, each with the exact proving command and the CI enforcement backstop:

    1. SAFE-01 — `mix verify.public_api` exits 0 with zero `--write` drift; manifest unchanged. Backstop: ci.yml `Verify Public API` step. Evidence: 56-SAFE-01-EVIDENCE.md.
    2. SAFE-02 — telemetry contract green at exactly 35 families with no `[:parapet, :schema, ...]` event (`mix test test/telemetry_contract_test.exs`), AND the new behavioral assertion proving Ecto `:source == "parapet_incidents"` (bare, no schema qualifier) from a live spine query (`mix test test/parapet/metrics/ecto_test.exs`). Backstop: the ExUnit suite in the ci.yml `test` job.
    3. SAFE-04 — the two-part honest `feat(schema)` CHANGELOG entry (headline reassurance + existing-adopter action-required + docs/upgrade-1.x.md link; no false unqualified banner). Backstop: release-please renders it into the v1.7 release note.

    State explicitly that the enforcement is the ALREADY-EXISTING CI, and that NO bespoke milestone-gate mix task and NO `mix ci` alias were added (deferred to v1.8 / CI-01, D-10). Set status to `passed` and score `3/3` only if all three underlying commands are green (re-run them if needed to confirm).
  </action>
  <verify>
    <automated>test -f .planning/phases/56-contract-release-hardening/56-VERIFICATION.md && grep -q 'SAFE-01' .planning/phases/56-contract-release-hardening/56-VERIFICATION.md && grep -q 'SAFE-02' .planning/phases/56-contract-release-hardening/56-VERIFICATION.md && grep -q 'SAFE-04' .planning/phases/56-contract-release-hardening/56-VERIFICATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `56-VERIFICATION.md` exists with frontmatter (phase, verified, status, score) matching the Phase-55 shape.
    - All three requirement IDs (SAFE-01, SAFE-02, SAFE-04) appear, each with its exact proving command and CI backstop.
    - The file states no new gate/`mix ci` alias was added (D-10).
    - Status is `passed` and score `3/3` (only if the three commands are green).
  </acceptance_criteria>
  <done>The three frozen-contract checks are recorded as milestone done-criteria in the verification report, tied to existing CI.</done>
</task>

<task type="auto">
  <name>Task 2: Record the done-criteria acceptance in 56-UAT.md</name>
  <files>.planning/phases/56-contract-release-hardening/56-UAT.md</files>
  <read_first>
    - .planning/phases/55-demo-app-upgrade-docs/55-UAT.md (prior-phase UAT artifact shape to follow — D-10 discretion clause)
    - .planning/phases/56-contract-release-hardening/56-VERIFICATION.md (the verification record written in Task 1 — keep the two consistent)
  </read_first>
  <action>
    Write `.planning/phases/56-contract-release-hardening/56-UAT.md` following the Phase-55 UAT shape, recording the same three done-criteria (SAFE-01/02/04) as acceptance items, each phrased as a user-facing acceptance statement with its automated proof command. Note that all three are automated (0 human verification required — the repo pattern is "Automate UAT into CI"): `mix verify.public_api`, `mix test test/telemetry_contract_test.exs`, `mix test test/parapet/metrics/ecto_test.exs`, and the CHANGELOG grep checks from Plan 03. Reference the CI backstop and reiterate no bespoke gate/`mix ci` alias was added (D-10).

    Keep 56-UAT.md consistent with 56-VERIFICATION.md (same three criteria, same green status). Do NOT add any new gate, task, or CI job.
  </action>
  <verify>
    <automated>test -f .planning/phases/56-contract-release-hardening/56-UAT.md && grep -q 'SAFE-01' .planning/phases/56-contract-release-hardening/56-UAT.md && grep -q 'SAFE-02' .planning/phases/56-contract-release-hardening/56-UAT.md && grep -q 'SAFE-04' .planning/phases/56-contract-release-hardening/56-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `56-UAT.md` exists following the Phase-55 UAT shape.
    - All three requirement IDs appear as acceptance items with automated proof commands (0 human verification).
    - The file is consistent with 56-VERIFICATION.md (same three criteria, same green status).
    - No new gate/`mix ci` alias recorded (D-10).
  </acceptance_criteria>
  <done>The done-criteria are recorded in the UAT artifact, fully automated, closing the D-10 requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Milestone done-criteria records → future maintainers | These artifacts assert the milestone is safe to ship; if they claimed green without the CI backstop, a regression could slip through. This plan is documentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-05 | Repudiation (unverified done-criteria) | 56-VERIFICATION.md / 56-UAT.md | low | mitigate | Each recorded criterion cites a concrete, re-runnable command AND the existing CI step that enforces it; status is set green only when the commands pass. No runtime surface. |
</threat_model>

<verification>
- `56-VERIFICATION.md` and `56-UAT.md` exist, both recording SAFE-01/02/04 with proving commands and CI backstops.
- Both are consistent (same three criteria, same green status).
- No bespoke gate or `mix ci` alias introduced.
</verification>

<success_criteria>
D-10 satisfied: the three frozen-contract checks are asserted as milestone done-criteria in the phase verification/UAT records, enforced by the already-existing CI — closing the v1.7 milestone.
</success_criteria>

<output>
Create `.planning/phases/56-contract-release-hardening/56-04-SUMMARY.md` when done.
</output>
