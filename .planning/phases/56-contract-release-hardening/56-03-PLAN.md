---
phase: 56-contract-release-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [SAFE-04]
must_haves:
  truths:
    - "CHANGELOG.md Unreleased section carries a `feat(schema)` entry using the two-part banner: a headline reassurance true for everyone (no automatic data migration) PLUS a distinct 'action required' line for existing adopters pointing to `config :parapet, schema_prefix: nil` + recompile, linking `docs/upgrade-1.x.md` (D-02)."
    - "The entry tells the SAME story as `docs/upgrade-1.x.md` (no restated mechanics; links out for Track A/B detail) and the release-please-generated GitHub release note inherits both parts (D-03)."
  artifacts:
    - "CHANGELOG.md — new `feat(schema)` two-part entry under `## Unreleased` / `### Features`."
  prohibitions:
    - "MUST NOT write the unqualified string 'No action required for existing installs' verbatim — it is factually false for existing adopters (D-01/D-02); the ROADMAP Success Criteria #3 literal is OVERRIDDEN by CONTEXT D-01/D-02."
    - "MUST NOT restate Track A/B mechanics in the CHANGELOG — link to docs/upgrade-1.x.md (single-source, D-03)."
    - "MUST NOT contradict the Phase-54 D-18 / Phase-55 D-07 'action required' framing."
  key_links:
    - "release-please (`release-type: elixir`) generates the v1.7 semver-minor GitHub release note body from this `feat` entry — the two-part text must survive rendering (D-03)."
    - "Config key spelling: `config :parapet, schema_prefix: nil` (matches config/config.exs:31 `config :parapet, schema_prefix: ...`; the key is `schema_prefix`, never bare `:prefix`)."
---

<objective>
Ship SAFE-04: a `feat(schema)` CHANGELOG entry that frames the v1.7 schema-prefix change honestly as additive (semver-minor) using the LOCKED two-part banner (D-02), consistent with `docs/upgrade-1.x.md` (D-03), and verify the release-note callout inherits both parts.

Purpose: SAFE-04's original literal "No action required for existing installs" banner is factually false as an unqualified claim — a do-nothing existing adopter who recompiles hits `relation "parapet.parapet_incidents" does not exist` because their pre-1.7 tables live in `public` (D-01). CONTEXT D-02 overrides the ROADMAP literal with a two-part banner that satisfies SAFE-04's spirit (reassuring, additive, semver-minor) AND the Phase-54/55 "action required" letter.

Output: The `feat(schema)` entry in `CHANGELOG.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-contract-release-hardening/56-CONTEXT.md
@.planning/phases/55-demo-app-upgrade-docs/55-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan produces exactly one edit:
- A new `### Features` block under `## Unreleased` in `CHANGELOG.md` containing one `feat(schema)` two-part entry.
No production symbols, no new docs (links to the already-shipped `docs/upgrade-1.x.md`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the two-part feat(schema) CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (lines 14-18: `## Unreleased` currently has a `### Changed` block for the archive change; house format = Keep a Changelog + Conventional Commits + release-please `release-type: elixir`; historical entries use `### Features` with `* **scope:** ...` bullets)
    - .planning/phases/56-contract-release-hardening/56-CONTEXT.md (D-02 reference wording — the two-part banner to use verbatim as the starting point; D-03 single-story rule; D-04 `feat`/`schema` scope)
    - docs/upgrade-1.x.md (lines 1, 9, 15 — H1 title, TL;DR "your data never moves automatically", H2 "## Action Required for Existing Adopters"; the CHANGELOG must stay consistent and link here, never restate Track A/B mechanics)
    - config/config.exs (line 31 `config :parapet, schema_prefix: schema_prefix` — confirms the exact key spelling `schema_prefix` for the "action required" line)
    - .planning/phases/55-demo-app-upgrade-docs/55-CONTEXT.md (D-07 locked "action required" framing the entry must not contradict)
  </read_first>
  <action>
    Add a new `### Features` block under `## Unreleased` in `CHANGELOG.md` (place it before or after the existing `### Changed` block per Keep-a-Changelog ordering; `### Added`/`### Features` conventionally precedes `### Changed`). Add exactly one `feat(schema)` bullet using the D-02 two-part banner as the verbatim starting point, adjusting only to match repo spelling.

    The entry MUST contain, in order:
    1. A `* **schema:**` bullet stating spine tables now live in a dedicated `parapet` Postgres schema by default, marked semver-minor / additive, and noting the public API & telemetry are unchanged.
    2. A headline reassurance TRUE FOR EVERYONE: that no data is migrated automatically — evidence tables never move unless the adopter chooses.
    3. A DISTINCT "action required" sub-line for existing adopters: to keep tables in `public`, add `config :parapet, schema_prefix: nil` and recompile, with a link to `docs/upgrade-1.x.md` for the full Track A / Track B guide.

    Use the exact config key `schema_prefix` (verified against config/config.exs:31 — never bare `:prefix`). Link to the upgrade guide using a relative markdown link `docs/upgrade-1.x.md` (matching how docs/migration-v1.md and docs/deployment.md reference it).

    DO NOT write the unqualified string "No action required for existing installs" verbatim anywhere (D-01/D-02 — it is factually false for existing adopters and would contradict Phase-54 D-18 / Phase-55 D-07).
    DO NOT restate Track A/B mechanics, GRANTs, recompile order, or rollback steps in the CHANGELOG — link out to docs/upgrade-1.x.md (single-source, D-03).
    Keep the commit type `feat`, scope `schema` (D-04) so release-please computes a semver-minor bump.
  </action>
  <verify>
    <automated>grep -q 'feat' CHANGELOG.md && grep -q 'schema_prefix: nil' CHANGELOG.md && grep -q 'docs/upgrade-1.x.md' CHANGELOG.md && ! grep -q 'No action required for existing installs' CHANGELOG.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` has a `### Features` block under `## Unreleased` with a `feat`/`**schema:**` two-part entry.
    - The entry contains the exact config directive `schema_prefix: nil` and a relative link `docs/upgrade-1.x.md`.
    - The entry contains a headline reassurance that no data is migrated automatically AND a distinct existing-adopter action-required line.
    - The verbatim string "No action required for existing installs" does NOT appear anywhere in CHANGELOG.md (`grep -c 'No action required for existing installs' CHANGELOG.md` returns 0).
    - No Track A/B mechanics (GRANTs, recompile order, rollback) are restated — the entry links out instead.
    - `mix format --check-formatted` is unaffected (CHANGELOG is not Elixir; but the repo build stays green: `mix compile --warnings-as-errors` succeeds).
  </acceptance_criteria>
  <done>The two-part `feat(schema)` CHANGELOG entry is present, honest, single-sourced to docs/upgrade-1.x.md, and free of the false unqualified banner.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the release-note callout inherits both parts</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the entry added in Task 1)
    - .planning/phases/56-contract-release-hardening/56-CONTEXT.md (D-03: release-please generates the GitHub release body from the `feat` entry; both parts of the banner must survive rendering)
  </read_first>
  <action>
    Confirm the two-part banner is structured so release-please (`release-type: elixir`) will carry BOTH parts into the generated GitHub release note. release-please lifts the Conventional-Commit `feat` subject line and body into the release notes; verify the entry's reassurance line and the action-required line are both part of the `feat(schema)` entry body (not detached prose elsewhere) so they render together.

    Since release-please runs in CI (not locally), verification here is a static structural check: confirm both the reassurance sentence and the action-required sentence live inside the same `### Features` `feat`/`**schema:**` entry block, immediately adjacent, so the rendered release note inherits both. Record a one-line note in the plan SUMMARY that the live release-note rendering is enforced by the existing release-please CI workflow (no new job added — D-10). Do NOT add a release-please config change or a new workflow.
  </action>
  <verify>
    <automated>awk '/^### Features/{f=1} f&&/schema_prefix: nil/{a=1} f&&/never move|not migrated|migrated automatically|does not move/{r=1} END{exit (a&&r)?0:1}' CHANGELOG.md && echo BOTH_PARTS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - Both banner parts (everyone-reassurance AND existing-adopter action-required) appear within the same `### Features` entry block.
    - No new release-please config or CI workflow was added (D-10).
  </acceptance_criteria>
  <done>The release-note callout is structurally guaranteed to inherit both parts of the two-part banner via the existing release-please pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release notes / CHANGELOG → adopters | Adopters make upgrade decisions from this copy; a false "no action required" banner would cause do-nothing upgraders to hit a broken first spine query. This plan is a doc edit with no runtime code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-04 | Repudiation / Information Disclosure (misleading release copy) | CHANGELOG / release note | medium | mitigate | The two-part banner (D-02) states the honest "action required" for existing adopters and links to docs/upgrade-1.x.md; the automated grep forbids the false unqualified banner. No injection/authz surface — this is a documentation-accuracy control, not a runtime input path. |
</threat_model>

<verification>
- CHANGELOG carries the `feat(schema)` two-part entry with `schema_prefix: nil` and a `docs/upgrade-1.x.md` link.
- The false unqualified banner string is absent.
- Both banner parts sit within the same entry so release-please renders them together.
</verification>

<success_criteria>
SAFE-04 satisfied: an honest, additive, semver-minor `feat(schema)` CHANGELOG entry consistent with docs/upgrade-1.x.md and the Phase-54/55 "action required" framing, with a matching release-note callout.
</success_criteria>

<output>
Create `.planning/phases/56-contract-release-hardening/56-03-SUMMARY.md` when done.
</output>