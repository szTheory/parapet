---
phase: 27-prebuilt-playbooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/parapet.gen.runbooks/deploy_tied_incident.ex.eex
  - priv/templates/parapet.gen.runbooks/cardinality_blowout.ex.eex
  - priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex
  - lib/mix/tasks/parapet.gen.runbooks.ex
  - test/mix/tasks/parapet.gen.runbooks_test.exs
  - lib/parapet/runbook.ex
autonomous: true
requirements: [PB-01, PB-02, PB-03, PB-04, PB-05, PB-06]

must_haves:
  truths:
    - "Running `mix parapet.gen.runbooks` generates all six JTBD-MAP runbook modules (retry_storm, suppression_drift, stalled_executor, dead_letter, deploy_tied_incident, cardinality_blowout) into the host's lib/<app>/parapet/runbooks/ directory."
    - "The two guidance-only templates (retry_storm, suppression_drift) carry NO `capability:` key on any step — adopters cannot accidentally wire a capability into them."
    - "The suppression_drift template's warning text explains WHY automated clearing is unsafe (mass-trigger escalations / re-suppress incorrectly), not just on-call readiness."
    - "The two new capability templates declare `requires_preview: true`, a `target_kind:` atom, and a `warning:` on their mitigate step, structurally proving the Preview→Confirm flow."
    - "Each new capability template references its capability by atom only (`:revert_feature_flag`, `:disable_metric_label`) — never a host module — so it compiles with zero optional-dep coupling."
    - "`mix test test/mix/tasks/parapet.gen.runbooks_test.exs` passes with assertions for all six in-scope templates."
  artifacts:
    - path: "priv/templates/parapet.gen.runbooks/deploy_tied_incident.ex.eex"
      provides: "Deploy-Tied Incident capability runbook template (PB-05)"
      contains: "capability: :revert_feature_flag"
    - path: "priv/templates/parapet.gen.runbooks/cardinality_blowout.ex.eex"
      provides: "Cardinality Blowout capability runbook template (PB-06)"
      contains: "capability: :disable_metric_label"
    - path: "priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex"
      provides: "Hardened guidance-only template (PB-02)"
      contains: "step(:clear_stale_suppressions,"
    - path: "lib/mix/tasks/parapet.gen.runbooks.ex"
      provides: "Generator wiring for the two new templates"
      contains: "deploy_tied_incident.ex.eex"
    - path: "test/mix/tasks/parapet.gen.runbooks_test.exs"
      provides: "Content assertions for the two new templates"
      contains: "Test.Parapet.Runbooks.DeployTiedIncident"
  key_links:
    - from: "lib/mix/tasks/parapet.gen.runbooks.ex"
      to: "priv/templates/parapet.gen.runbooks/deploy_tied_incident.ex.eex"
      via: "Igniter.copy_template with on_exists: :skip"
      pattern: "copy_template.*deploy_tied_incident"
    - from: "lib/mix/tasks/parapet.gen.runbooks.ex"
      to: "priv/templates/parapet.gen.runbooks/cardinality_blowout.ex.eex"
      via: "Igniter.copy_template with on_exists: :skip"
      pattern: "copy_template.*cardinality_blowout"
    - from: "test/mix/tasks/parapet.gen.runbooks_test.exs"
      to: "generated deploy_tied_incident.ex / cardinality_blowout.ex"
      via: "Rewrite.source! content assertion"
      pattern: "capability: :revert_feature_flag"
---

<objective>
Ship the six JTBD-MAP prebuilt playbooks as host-owned `Parapet.Runbook` DSL templates under `priv/templates/parapet.gen.runbooks/`, completing PB-01..PB-06. Four templates already exist from v0.10 and are reused unchanged (retry_storm, suppression_drift, stalled_executor, dead_letter). This plan authors the two net-new capability-backed templates (Deploy-Tied Incident via `:revert_feature_flag`, Cardinality Blowout via `:disable_metric_label`), hardens the suppression_drift guidance-only warning to state WHY automated clearing is unsafe, wires the two new templates into the existing `mix parapet.gen.runbooks` generator, and extends the generator test.

Purpose: An adopter who runs the existing `mix parapet.gen.runbooks` task gets the full six-playbook catalog covering the JTBD-MAP failure modes — two guidance-only by design, four capability-backed exercising the claim-protected Preview→Confirm path landed in Phases 23–26.

Output: Two new EEx templates, one hardened EEx template, two appended generator `copy_template` calls, two extended test assertion blocks, and (discretionary) an accuracy fix to the `runbook.ex` `:capability` doc list.

This is ONE coherent PR. Template authoring + suppression_drift hardening + generator wiring + test extension must land atomically so the generator test is never red against a missing template (per CONTEXT.md specifics and 27-VALIDATION.md Wave 0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase-specific authoritative context — READ THESE
@.planning/phases/27-prebuilt-playbooks/27-CONTEXT.md
@.planning/phases/27-prebuilt-playbooks/27-RESEARCH.md
@.planning/phases/27-prebuilt-playbooks/27-PATTERNS.md
@.planning/phases/27-prebuilt-playbooks/27-VALIDATION.md

<interfaces>
<!-- Key contracts extracted from the codebase. Use these directly — no exploration needed. -->

The `Parapet.Runbook` `step/2` macro (lib/parapet/runbook.ex:45) accepts these opts verbatim:
  :label (string), :description (string), :type (:manual | :mitigation), :kind (:guidance | :capability),
  :capability (atom, nil if unset), :target_kind (atom or string, nil if unset),
  :requires_preview (bool, default false), :preview_only (bool, default false),
  :auto_execute (bool, default false), :guidance (string), :warning (string).
The macro stores `:capability` verbatim — there is NO allowlist validation in runbook.ex.

`Parapet.Capabilities.@valid_capabilities` (lib/parapet/capabilities.ex:14-20) — all five atoms already present:
  :retry_async_item, :requeue_dead_letter, :request_manual_provider_check,
  :revert_feature_flag, :disable_metric_label
The allowlist guard is at `register_recovery/2` (capabilities.ex:29), enforced only at host registration time, never at template compile time.

Generator `assigns` and `lib_dir` (lib/mix/tasks/parapet.gen.runbooks.ex:24-30) are already computed; new copy_template calls reuse them unchanged:
  assigns = [app_name:, base_name:, module_prefix:]
  lib_dir = "lib/#{app_name}/parapet/runbooks"

EEx module header (every existing template):
  defmodule <%= inspect(@module_prefix) %>.<Suffix> do / use Parapet.Runbook
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the two new capability templates + harden suppression_drift warning</name>
  <files>priv/templates/parapet.gen.runbooks/deploy_tied_incident.ex.eex, priv/templates/parapet.gen.runbooks/cardinality_blowout.ex.eex, priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex, lib/parapet/runbook.ex</files>
  <read_first>
    - priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex (PRIMARY ANALOG — copy its exact 3-step investigate→mitigate→verify shape; this is the source of truth for the two new templates)
    - priv/templates/parapet.gen.runbooks/dead_letter.ex.eex (secondary capability-template analog — warning placement variant)
    - priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex (the file being MODIFIED — current step 2 `:clear_stale_suppressions` warning at line 24 is the text to harden)
    - priv/templates/parapet.gen.runbooks/retry_storm.ex.eex (framing reference for the guidance-only "no safe automated mitigation" rationale; its line 14 warning is the target tone for the suppression_drift hardening)
    - lib/parapet/runbook.ex:25-60 (the documented `step/2` opts; line 36 `:capability` doc lists only 3 of 5 atoms — discretionary accuracy fix)
    - lib/parapet/capabilities.ex:14-20 (confirm `:revert_feature_flag` and `:disable_metric_label` are both in @valid_capabilities)
  </read_first>
  <action>
Create `priv/templates/parapet.gen.runbooks/deploy_tied_incident.ex.eex` by mirroring `stalled_executor.ex.eex` exactly. Use module header `defmodule <%= inspect(@module_prefix) %>.DeployTiedIncident do` / `use Parapet.Runbook`. Set a `title(...)` and `description(...)` for the deploy-tied-incident / feature-flag-revert context. Three steps in the investigate→mitigate→verify shape:
  - Step 1 investigate (e.g., id `:investigate_deployment`): `type: :manual, kind: :guidance, preview_only: true`, with `guidance:` describing how to correlate the incident with the recent deploy / feature-flag change. A `warning:` on this step is optional.
  - Step 2 mitigate (e.g., id `:revert_flag`): `type: :mitigation, kind: :capability, capability: :revert_feature_flag, target_kind: :feature_flag, requires_preview: true`, with a `warning:` describing the blast radius of reverting a flag. Include the canonical wiring pointer to Rulestead as the `:revert_feature_flag` host capability target (per D-08) inside this step's `guidance:` and/or `warning:` text. Do NOT name a host module — pointer text only.
  - Step 3 verify (e.g., id `:verify_recovery`): `type: :manual, kind: :guidance, preview_only: true`, `guidance:` describing the post-revert health check. No `capability:`, no `warning:` (matches the analog verify step).

Create `priv/templates/parapet.gen.runbooks/cardinality_blowout.ex.eex` the same way. Module header `defmodule <%= inspect(@module_prefix) %>.CardinalityBlowout do`. Three steps:
  - Step 1 investigate (e.g., id `:investigate_cardinality`): guidance for detecting which metric label is blowing out cardinality.
  - Step 2 mitigate (e.g., id `:disable_label`): `type: :mitigation, kind: :capability, capability: :disable_metric_label, target_kind: :metric_label, requires_preview: true`, with a `warning:`. Include the wiring pointer to the cardinality analyzer surface (`mix parapet.doctor cardinality` / `Parapet.Metrics.Validator`) as the `:disable_metric_label` reference (per D-08) in the step's `guidance:` and/or `warning:` text. Pointer text only, no host module reference.
  - Step 3 verify (e.g., id `:verify_recovery`): guidance for confirming cardinality reduction in APM/dashboards.

Modify `priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex`: harden the `warning:` on step 2 `:clear_stale_suppressions` (currently line 24) so it explains WHY automated clearing is architecturally unsafe — not just on-call readiness. The hardened text must convey: (a) automated bulk clearing can mass-trigger escalations simultaneously across all previously-suppressed incidents, and (b) automated logic cannot tell which suppressions should remain active, risking incorrect re-suppression — therefore the safe path is operator review of each window. Extend the existing line-24 warning inline. Do NOT add a fourth step, do NOT change step ids, do NOT add a `capability:` key anywhere (it must remain guidance-only per D-03). Leave retry_storm.ex.eex untouched (its line-14 warning already satisfies PB-01).

Discretionary accuracy fix (low-effort, same conceptual surface): in `lib/parapet/runbook.ex` line 36, the `:capability` `@doc` lists only `:retry_async_item, :requeue_dead_letter, :request_manual_provider_check`. Update it to include `:revert_feature_flag` and `:disable_metric_label` so the doc matches the real allowlist in capabilities.ex. This is doc text only — no behavior change.

> **D-13 tension — accepted (decided at plan-phase 2026-05-28).** CONTEXT.md D-13 says `runbook.ex` stays "untouched / no DSL changes." This edit is accepted as an in-scope nicety: D-13's intent is *no DSL/behavior changes* (success criterion #4 is satisfied by reusing the DSL unchanged), and a `@doc` string is neither. The `@doc` list went stale when Phase 24 widened `@valid_capabilities` to 5 atoms; the two missing atoms are exactly the ones this phase's new templates introduce, so fixing adopter-facing accuracy here is coherent and zero-risk (guarded by `mix compile`). If strict literal D-13 compliance is ever required, cut this one paragraph + remove `lib/parapet/runbook.ex` from `files_modified` — PB-01..PB-06 delivery is unaffected.
  </action>
  <verify>
    <automated>grep -c "capability: :revert_feature_flag" priv/templates/parapet.gen.runbooks/deploy_tied_incident.ex.eex && grep -c "capability: :disable_metric_label" priv/templates/parapet.gen.runbooks/cardinality_blowout.ex.eex && test "$(grep -v '^#' priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex | grep -c 'capability:')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `priv/templates/parapet.gen.runbooks/deploy_tied_incident.ex.eex` exists and contains the literal lines `defmodule <%= inspect(@module_prefix) %>.DeployTiedIncident do`, `use Parapet.Runbook`, `capability: :revert_feature_flag`, `target_kind: :feature_flag`, `requires_preview: true`, and at least one `warning:`.
    - `priv/templates/parapet.gen.runbooks/cardinality_blowout.ex.eex` exists and contains `defmodule <%= inspect(@module_prefix) %>.CardinalityBlowout do`, `use Parapet.Runbook`, `capability: :disable_metric_label`, `target_kind: :metric_label`, `requires_preview: true`, and at least one `warning:`.
    - Each new template has exactly 3 `step(` declarations (investigate / mitigate / verify); only the mitigate step has a `capability:` key.
    - Deploy-Tied Incident references Rulestead as the `:revert_feature_flag` wiring target in guidance/warning text; Cardinality Blowout references `mix parapet.doctor cardinality` or `Parapet.Metrics.Validator` as the `:disable_metric_label` reference. Neither names a host module (no `Code.ensure_loaded?`, no module alias).
    - `suppression_drift.ex.eex` step 2 `:clear_stale_suppressions` warning now states the mass-escalation AND incorrect-re-suppression rationale; the file still contains zero `capability:` keys (excluding any comment lines) and still has exactly 3 steps.
    - `lib/parapet/runbook.ex` line ~36 `:capability` doc lists all five atoms including `:revert_feature_flag` and `:disable_metric_label`.
    - `mix compile` succeeds (no template or DSL doc syntax errors).
  </acceptance_criteria>
  <done>Both new capability templates exist mirroring the stalled_executor 3-step shape with the correct capability atoms, target_kinds, requires_preview, warnings, and wiring pointers; suppression_drift's step-2 warning explains the guidance-only architectural rationale while remaining capability-free; runbook.ex doc lists all five atoms.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the two new templates into the generator and extend the generator test</name>
  <files>lib/mix/tasks/parapet.gen.runbooks.ex, test/mix/tasks/parapet.gen.runbooks_test.exs</files>
  <read_first>
    - lib/mix/tasks/parapet.gen.runbooks.ex (the file being MODIFIED — the `Igniter.copy_template` pipe chain runs lines 33-109; the `partial_backlog_drain` call ends at line 109, immediately followed by `Igniter.add_notice` at line 110. `assigns` and `lib_dir` are bound at lines 24-30.)
    - test/mix/tasks/parapet.gen.runbooks_test.exs (the file being MODIFIED — single `test` block; file-presence `Enum.any?` asserts run lines 15-42; per-template content-assertion blocks run lines 44-99; the `partial_backlog_drain` content block ends at line 99 just before the closing `end` at line 100)
    - priv/templates/parapet.gen.runbooks/deploy_tied_incident.ex.eex (created in Task 1 — the template this task wires + asserts)
    - priv/templates/parapet.gen.runbooks/cardinality_blowout.ex.eex (created in Task 1 — the template this task wires + asserts)
  </read_first>
  <action>
In `lib/mix/tasks/parapet.gen.runbooks.ex`, insert two new `Igniter.copy_template(...)` calls into the pipe chain BETWEEN the existing `partial_backlog_drain` call (ends line 109) and the `Igniter.add_notice` call (line 110). Mirror the exact shape of the existing calls: first arg `Path.join([:code.priv_dir(:parapet), "templates", "parapet.gen.runbooks", "deploy_tied_incident.ex.eex"])`, second arg `Path.join([lib_dir, "deploy_tied_incident.ex"])`, third arg `assigns`, fourth `on_exists: :skip`. Then an identical call for `cardinality_blowout.ex.eex` → `Path.join([lib_dir, "cardinality_blowout.ex"])`. Reuse the existing `assigns` and `lib_dir` bindings unchanged — no other edits to this file.

In `test/mix/tasks/parapet.gen.runbooks_test.exs`, extend the SINGLE existing `test "creates fixed runbook files..."` block (do NOT add a second test block — Pitfall 3):
  1. After the `partial_backlog_drain` file-presence assert (line 39-42), append two `Enum.any?(files, &String.contains?(&1, "lib/test/parapet/runbooks/deploy_tied_incident.ex"))` / `...cardinality_blowout.ex` assertions mirroring the existing block.
  2. After the `partial_backlog_drain` content-assertion block (ends line 99, before the closing `end`), append two content-assertion blocks mirroring the `stalled_executor` block (lines 44-51): for deploy_tied_incident, assert source `=~ "defmodule Test.Parapet.Runbooks.DeployTiedIncident do"`, `=~ "capability: :revert_feature_flag"`, `=~ "warning:"`; for cardinality_blowout, assert `=~ "defmodule Test.Parapet.Runbooks.CardinalityBlowout do"`, `=~ "capability: :disable_metric_label"`, `=~ "warning:"`. Use the `Rewrite.source!(igniter.rewrite, "lib/test/parapet/runbooks/<file>.ex") |> Rewrite.Source.get(:content)` pattern. Do NOT compile the generated modules — content assertions only, matching the existing test.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/parapet.gen.runbooks_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mix/tasks/parapet.gen.runbooks.ex` contains `Igniter.copy_template` calls referencing both `deploy_tied_incident.ex.eex`→`deploy_tied_incident.ex` and `cardinality_blowout.ex.eex`→`cardinality_blowout.ex`, both with `on_exists: :skip`, positioned before `Igniter.add_notice`.
    - `grep -c "deploy_tied_incident" lib/mix/tasks/parapet.gen.runbooks.ex` ≥ 2 and `grep -c "cardinality_blowout" lib/mix/tasks/parapet.gen.runbooks.ex` ≥ 2.
    - `test/mix/tasks/parapet.gen.runbooks_test.exs` contains assertions `=~ "defmodule Test.Parapet.Runbooks.DeployTiedIncident do"`, `=~ "capability: :revert_feature_flag"`, `=~ "defmodule Test.Parapet.Runbooks.CardinalityBlowout do"`, and `=~ "capability: :disable_metric_label"`, all inside the single existing `test` block (the file still has exactly one `test ` declaration).
    - `mix test test/mix/tasks/parapet.gen.runbooks_test.exs` exits 0.
    - Full suite `mix test` exits 0 (per-wave gate; no regressions in other runbook/generator tests).
  </acceptance_criteria>
  <done>The generator copies all six in-scope templates (plus the two pre-existing out-of-scope ones) into the host runbooks dir; the generator test asserts presence, module name, capability atom, and warning for both new templates and passes green; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter host app ← generated template | The generator emits host-owned `.ex` files into the adopter's `lib/<app>/parapet/runbooks/`. The template is static text with EEx interpolation of the (compile-time, library-controlled) `@module_prefix` only — no adopter-supplied or network input crosses into it. |
| runbook step → operator Confirm path | A capability step names a capability *atom*. Actual execution is gated by the operator Preview→Confirm path + `ClaimService` + `Parapet.Capabilities` allowlist (built and tested in Phases 23–26, untouched here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Elevation of Privilege | A guidance-only template (retry_storm / suppression_drift) accidentally gaining a `capability:` key, exposing an automated mitigation path the design forbids | mitigate | Structural guarantee per D-03: guidance-only templates carry NO `capability:` key on any step. Task 1 verify (`grep -c 'capability:' suppression_drift.ex.eex == 0` after comment-filtering) and acceptance criteria enforce zero capability keys. This is success criterion #2. |
| T-27-02 | Tampering | Generated template overwriting an adopter's hand-customized runbook on re-run | accept (already mitigated upstream) | All `copy_template` calls use `on_exists: :skip` (existing pattern reused verbatim in Task 2) — re-running the generator never clobbers adopter edits. No new risk introduced. |
| T-27-03 | Elevation of Privilege | A capability template naming an atom outside the `@valid_capabilities` allowlist | accept | `:revert_feature_flag` and `:disable_metric_label` are both already in the allowlist (capabilities.ex:14-20, confirmed). Even if a typo'd atom shipped, the `register_recovery/2` guard rejects unknown ids at host registration time (capabilities.ex:29) — the template layer is non-load-bearing for this control. |
| T-27-SC | Tampering | npm/pip/cargo/hex supply-chain (new dependency installs) | n/a | No package installs in this phase (D-12: no `mix.exs` changes, no new deps; RESEARCH Package Legitimacy Audit = SKIPPED). No install task exists; no legitimacy checkpoint required. |

**Analysis:** The realistic surface for pure template/generator/test work is narrow. There is no user-input parsing, no network, no auth, no datastore change, and no new dependency. The single design-critical control is the structural guidance-only guarantee (T-27-01), which is enforced by an automated grep gate in Task 1 and by the content-assertion test. No block-worthy (high) threat applies; the `<threat_model>` is recorded as mandated.
</threat_model>

<verification>
- `mix compile` succeeds — both new EEx templates and the runbook.ex doc edit are syntactically valid.
- `mix test test/mix/tasks/parapet.gen.runbooks_test.exs` exits 0 — all eight generated files (six in-scope + two pre-existing out-of-scope) assert correctly, including the two new templates' module name + capability atom + warning.
- `mix test` (full suite) exits 0 — no regressions.
- `grep -c "capability:" priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex` (comment-filtered) == 0 — guidance-only structural guarantee holds (success criterion #2).
- Manual content review: suppression_drift step-2 warning states the mass-escalation + incorrect-re-suppression rationale; both new templates carry the Rulestead / cardinality-analyzer wiring pointers.
</verification>

<success_criteria>
Maps to the four ROADMAP Phase 27 success criteria:
1. An adopter running `mix parapet.gen.runbooks` gets all six host-owned runbook modules; each compiles cleanly because capability steps name an atom only, never a host module (PB-01..PB-06, criterion #1). [Per D-06, the existing whole-catalog generator is the v1.1 contract — no per-template CLI.]
2. The two guidance-only templates (retry_storm, suppression_drift) carry explicit `warning:` blocks documenting why every obvious automated mitigation worsens the failure, with NO `capability:` key — adopters cannot accidentally wire a capability in (PB-01, PB-02, criterion #2).
3. The four capability-backed templates (stalled_executor `:retry_async_item`, dead_letter `:requeue_dead_letter`, deploy_tied_incident `:revert_feature_flag`, cardinality_blowout `:disable_metric_label`) declare `requires_preview: true` + `target_kind:` + `warning:`, structurally proving the Preview→Confirm flow against the documented preview-output shape (PB-03..PB-06, criterion #3). [Runnable demo is Phase 28 per D-10.]
4. All six templates are ordinary `use Parapet.Runbook` modules mappable to an SLO/alert name via the existing DSL with no modification (criterion #4, satisfied by reusing the unchanged DSL per D-13).
</success_criteria>

<output>
Create `.planning/phases/27-prebuilt-playbooks/27-01-SUMMARY.md` when done.
</output>
