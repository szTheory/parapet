---
phase: 46-navigation-shell-data-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/parapet/operator_ui_contrast_test.exs
autonomous: true
requirements: [NAV-01, NAV-02, NAV-05, DATA-02, DATA-03, DATA-06, A11Y-03]
must_haves:
  truths:
    - "operator_ui_contrast_test.exs has a new @detail_template_paths constant covering operator_detail_live.ex.eex + its demo mirror, plus a test asserting the detail landmark contract (NAV-05)."
    - "The @live_template_paths test is extended with NAV-02 (refute bg-teal-50/text-teal-950), NAV-05 (Skip to main content + id=\"parapet-main\"), DATA-06 (aria-live=\"polite\"), A11Y-03 (aria-disabled), and DATA-02 (refute overflow-y-auto) assertions."
    - "The @component_paths test is extended with NAV-01 (border-bottom: 2px solid var(--parapet-accent)), DATA-03 (No timeline entries yet, po-timeline-list, li:last-child), and DATA-06 (animate-pulse) assertions."
    - "The new and extended assertions FAIL (red) when run against the current un-modified templates — proving the markup contract is not yet satisfied."
  artifacts:
    - "Extended test/parapet/operator_ui_contrast_test.exs with @detail_template_paths constant + landmark test + extended @live_template_paths/@component_paths assertions."
  key_links:
    - "These assertions are the red→green gate for plans 46-02 and 46-03; every markup change in those plans flips one of these assertions from failing to passing."
    - "Assertions reuse the existing File.read! + assert content =~ / refute content =~ loop pattern (no new test files, no new helpers)."
---

<objective>
Lay down the Nyquist red-tests for Phase 46 BEFORE any markup lands. Extend the existing `test/parapet/operator_ui_contrast_test.exs` harness with a new `@detail_template_paths` constant + landmark test, and add the NAV/DATA/A11Y string assertions to the existing `@live_template_paths` and `@component_paths` tests. These assertions must fail against today's templates — that failure is the contract that plans 46-02/46-03 satisfy.

Purpose: Per 46-VALIDATION.md "Wave 0 Requirements", every Phase 46 requirement gets an automated assertion (or a routed gallery check) wired into the existing harness. Establishing them as red tests first guarantees the markup plans cannot silently no-op. No new test files — additive only.
Output: One modified test file whose new assertions are red now and turn green as 46-02/46-03 land their byte-mirrored markup.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-navigation-shell-data-display/46-RESEARCH.md
@.planning/phases/46-navigation-shell-data-display/46-PATTERNS.md
@.planning/phases/46-navigation-shell-data-display/46-VALIDATION.md
@test/parapet/operator_ui_contrast_test.exs
</context>

<interface>
Existing harness anchors (verified from 46-PATTERNS.md "## @detail_template_paths" section):
- `@component_paths` constant + `"operator components use semantic tokens..."` test (contrast test ~lines 4 / 92–172) — loop shape: `for path <- @component_paths do / content = File.read!(path) / assert content =~ ... / refute content =~ ...`.
- `@live_template_paths` constant (~line 175) + `"operator live templates use semantic tokens..."` test (~line 180) — already includes BOTH operator_live template + demo mirror paths.
- New `@detail_template_paths` to add after `@live_template_paths`:
  `["priv/templates/parapet.gen.ui/operator_detail_live.ex.eex", "examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex"]`.
The exact assertion blocks to add are enumerated in 46-PATTERNS.md "Extensions to existing @live_template_paths test" / "Extensions to existing @component_paths test" / "New test to add" and in 46-VALIDATION.md "## Wave 0 Requirements".
</interface>

<tasks>

<task type="auto">
  <name>Task 1: Add @detail_template_paths constant + detail-landmark test</name>
  <files>test/parapet/operator_ui_contrast_test.exs</files>
  <read_first>
    operator_ui_contrast_test.exs lines 1–10 (module attrs) and the @live_template_paths constant + its test (~lines 175–190) — copy the constant declaration and the `for path <- @*_paths do / content = File.read!(path)` loop shape exactly.
    46-PATTERNS.md "@detail_template_paths constant + landmark test" — the exact constant value and test body.
  </read_first>
  <action>
    Add a module attribute `@detail_template_paths` immediately after the existing `@live_template_paths` constant, listing exactly the two detail paths: `priv/templates/parapet.gen.ui/operator_detail_live.ex.eex` and `examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex`. Add a new test `"operator detail templates have correct landmarks"` that loops `for path <- @detail_template_paths do`, reads the file with `File.read!(path)`, and asserts the three NAV-05 detail contracts: `aria-label="Incident actions"`, `id="parapet-main"`, and `"Skip to main content"`. Use the `~S|...|` sigil for the attribute-bearing strings so embedded quotes are literal (per 46-PATTERNS.md). Do NOT modify any existing assertion; this task is purely additive. These assertions will be RED until plan 46-03 lands the detail-template markup — that is expected.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_contrast_test.exs:1 2>&1 | tail -5; mix test test/parapet/operator_ui_contrast_test.exs -k "operator detail templates have correct landmarks" 2>&1 | tail -8</automated>
  </verify>
  <done>The new @detail_template_paths constant exists; the "operator detail templates have correct landmarks" test exists and FAILS (red) against the current un-modified detail templates (asserting it is wired correctly and will gate 46-03). No existing test was altered.</done>
</task>

<task type="auto">
  <name>Task 2: Extend @live_template_paths and @component_paths tests with NAV/DATA/A11Y assertions</name>
  <files>test/parapet/operator_ui_contrast_test.exs</files>
  <read_first>
    operator_ui_contrast_test.exs @live_template_paths test body (~lines 180–188) and @component_paths test body (~lines 92–172) — locate the inside of each `for path <- ... do` loop where new `assert`/`refute` lines append.
    46-PATTERNS.md "Extensions to existing @live_template_paths test" and "Extensions to existing @component_paths test" — the exact assertion lines.
    46-VALIDATION.md "## Per-Requirement Verification Map" — confirms which req each assertion covers.
  </read_first>
  <action>
    In the existing `@live_template_paths` test loop, append: `refute content =~ "bg-teal-50"` and `refute content =~ "text-teal-950"` (NAV-02); `assert content =~ "Skip to main content"` and `assert content =~ ~S|id="parapet-main"|` (NAV-05); `assert content =~ ~S|aria-live="polite"|` (DATA-06); `assert content =~ "aria-disabled"` (A11Y-03); `refute content =~ "overflow-y-auto"` (DATA-02). In the existing `@component_paths` test loop, append: `assert content =~ "border-bottom: 2px solid var(--parapet-accent)"` (NAV-01); `assert content =~ "No timeline entries yet"`, `assert content =~ "po-timeline-list"`, `assert content =~ "li:last-child"` (DATA-03); `assert content =~ "animate-pulse"` (DATA-06); and `refute content =~ "overflow-y-auto"` (DATA-02). Use the `~S|...|` sigil for any assertion string containing double quotes. Append only — do not remove or reorder any existing assertion. These new lines are RED until 46-02/46-03 land.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_contrast_test.exs 2>&1 | tail -8</automated>
  </verify>
  <done>The @live_template_paths and @component_paths tests carry the new NAV-01/NAV-02/NAV-05/DATA-02/DATA-03/DATA-06/A11Y-03 string assertions; running the contrast test now reports FAILURES on exactly these new assertions (red), confirming they gate the markup plans. No pre-existing assertion regressed (existing passing assertions still pass).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → template files | Test reads template files as strings via File.read!; no execution of template content, no user/incident data, no network or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01 | Tampering | New ExUnit string assertions | low | mitigate | Assertions are static literal strings compared with File.read! content; no interpolation of external/user data; additive-only edits to an existing test file. |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (46-RESEARCH.md Package Legitimacy Audit: none). |

Test-only change. No data flow, auth, user-input, or markup surface touched. No `high`-severity threats.
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_contrast_test.exs` runs and reports the new assertions as RED (expected — markup not yet landed).
- The new `@detail_template_paths` constant + "operator detail templates have correct landmarks" test exist and execute.
- No previously-passing assertion regressed.
</verification>

<success_criteria>
- `@detail_template_paths` constant + landmark test added (NAV-05 detail coverage).
- `@live_template_paths` extended with NAV-02/NAV-05/DATA-02/DATA-06/A11Y-03 assertions.
- `@component_paths` extended with NAV-01/DATA-03/DATA-06/DATA-02 assertions.
- New assertions are red against current templates; existing assertions still green.
- No new test files introduced.
</success_criteria>

<output>
Create `.planning/phases/46-navigation-shell-data-display/46-01-SUMMARY.md` when done. List the @detail_template_paths constant added, the new landmark test, every assertion appended to @live_template_paths and @component_paths (with its requirement ID), paste the mix test tail showing the new assertions are red, and confirm no existing assertion regressed.
</output>
