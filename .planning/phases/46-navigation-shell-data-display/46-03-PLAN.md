---
phase: 46-navigation-shell-data-display
plan: 03
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - priv/templates/parapet.gen.ui/operator_live.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex
  - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex
autonomous: true
requirements: [NAV-02, NAV-03, NAV-04, NAV-05, DATA-04, DATA-06, A11Y-03, DATA-02]
must_haves:
  truths:
    - "operator_live.ex.eex and operator_detail_live.ex.eex each carry a skip-to-content <a href=\"#parapet-main\"> as the first child of the .parapet-ui div (sr-only focus:not-sr-only ... po-focus), and every <main> element (three branches in operator_live, one in detail) gains id=\"parapet-main\" tabindex=\"-1\" (NAV-05)."
    - "operator_detail_live.ex.eex <aside> gains aria-label=\"Incident actions\" (NAV-05)."
    - "The queue-refresh notification in operator_live.ex.eex replaces bg-teal-50/text-teal-950 with bg-[color:var(--parapet-accent-soft)] ring-1 ring-[color:var(--parapet-border)] container + style=\"color: var(--parapet-text);\" text (NAV-02)."
    - "The incident_list call site in operator_live.ex.eex is wrapped in an aria-live=\"polite\" + aria-busy region that shows an animate-pulse skeleton gated on !connected?(assigns) and the real <.incident_list/> otherwise (DATA-06)."
    - "pagination_link_class(true) gains focus:outline-none focus:ring-2 focus:ring-offset-2 po-focus; every disabled pagination <.link> gains aria-disabled and tabindex=\"-1\" via inline attributes (not the class string) (A11Y-03)."
    - "The cockpit no-selection empty state in operator_live.ex.eex gains a decorative aria-hidden SVG icon before its heading, with no pointer affordance (DATA-04)."
    - "Every edit to each template is applied byte-identically to its demo mirror in this same plan; the theme-switcher JS/aria-pressed and the IA labels (Respond/Actions/History) are unchanged (NAV-03, NAV-04); no overflow-y-auto is added (DATA-02)."
  artifacts:
    - "operator_live.ex.eex + mirror with: skip-link, three <main id=\"parapet-main\">, queue-refresh token colors, aria-live skeleton wrapper, pagination po-focus + aria-disabled/tabindex, cockpit empty-state icon."
    - "operator_detail_live.ex.eex + mirror with: skip-link, <main id=\"parapet-main\">, <aside aria-label=\"Incident actions\">."
  key_links:
    - "Skip-link, pagination, and queue-refresh reuse the locked po-focus group and --parapet-* token vocabulary from Phase 44/45; the skeleton reuses the existing prefers-reduced-motion zeroing (no new reduced-motion logic)."
    - "The skip-link MUST be inside the .parapet-ui wrapper so po-focus + token colors resolve (46-RESEARCH Pitfall 2); all three operator_live <main> branches MUST receive the id (Pitfall 3)."
---

<objective>
Make the two LiveView templates (operator_live.ex.eex and operator_detail_live.ex.eex, plus their byte-mirrors) satisfy the NAV-02 queue-refresh color, NAV-05 skip-link/landmark structure, DATA-04 cockpit empty-state icon, DATA-06 skeleton, and A11Y-03 pagination focus + disabled-link semantics — turning the matching 46-01 red assertions green. Verify NAV-03 (theme switcher) and NAV-04 (IA labels) are untouched.

Purpose: This is the shell/behavior half of Phase 46. These two templates are disjoint from operator_components (plan 46-02), so this plan runs in the same wave but owns its own files. The top operational risk is byte-mirror drift across four files — process each template+mirror as an atomic pair.
Output: Re-skinned operator_live + operator_detail_live templates + mirrors; the NAV-02/NAV-05/DATA-06/A11Y-03 live + detail assertions from 46-01 pass; the quick contrast/demo-contract command is green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-navigation-shell-data-display/46-RESEARCH.md
@.planning/phases/46-navigation-shell-data-display/46-PATTERNS.md
@.planning/phases/46-navigation-shell-data-display/46-UI-SPEC.md
@priv/templates/parapet.gen.ui/operator_live.ex.eex
@priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
</context>

<interface>
Verified anchors (apply each edit identically to the matching demo mirror under examples/demo_app/lib/demo_app_web/live/parapet/):

operator_live.ex.eex:
- `.parapet-ui` div ~line 121 (`<div class="parapet-ui antialiased flex min-h-screen flex-col bg-stone-100 text-stone-900">`) — INSERT the skip-link as the FIRST child, before `<.operator_nav>` (46-RESEARCH Pattern 2 / Pitfall 2: must be INSIDE the div).
- THREE `<main class="flex-1 bg-stone-50 px-4 py-6 md:px-8">` elements (~lines 125 :actions, 139 :history, 196 :response) — ALL three become `<main id="parapet-main" tabindex="-1" class="...">` (46-RESEARCH Pitfall 3: search `<main class=` to find all three).
- Queue-refresh notification ~line 206 (`<div class="mt-4 rounded-xl bg-teal-50 px-4 py-3 shadow-sm ring-1 ring-stone-300">` + `<p class="text-sm font-medium text-teal-950">`) → container `bg-[color:var(--parapet-accent-soft)] ring-1 ring-[color:var(--parapet-border)]` (keep mt-4 rounded-xl px-4 py-3 shadow-sm), text `<p class="text-sm font-medium" style="color: var(--parapet-text);">`. The inner button is already tokenized — do NOT change it (46-PATTERNS "Queue-refresh notification").
- `<.incident_list ... />` call site ~lines 235–241 — WRAP in `<div aria-live="polite" aria-busy={if !connected?(assigns), do: "true", else: "false"}>` with `<%= if !connected?(assigns) do %>` skeleton (three `h-16 rounded-lg bg-[color:var(--parapet-panel-muted)]` blocks inside `animate-pulse space-y-3` aria-hidden) `<% else %>` the existing `<.incident_list .../>` `<% end %>` (46-PATTERNS "Skeleton aria-live wrapper"; RESEARCH Open Q1 RESOLVED → use !connected?(assigns), no new assign).
- Cockpit no-selection empty state ~line 281 — ADD the cursor-select aria-hidden SVG before its heading (46-PATTERNS "Decorative SVG icon additions"). No cursor-pointer/hover.
- `pagination_link_class(true)` ~line 468 — append `focus:outline-none focus:ring-2 focus:ring-offset-2 po-focus` to the returned class string; leave pagination_link_class(false) styling as-is.
- The four pagination `<.link>` call sites (Previous/Next, Newer/Older; ~lines 172–189, 243–261) — add `aria-disabled={unless <has_page?>, do: "true"}` and `tabindex={unless <has_page?>, do: "-1"}` inline attributes (A11Y-03; 46-RESEARCH Pattern 6 / Pitfall 6 — never use the `disabled` HTML attr on `<.link>`).

operator_detail_live.ex.eex:
- `.parapet-ui` div ~line 206 — INSERT the skip-link as first child.
- `<main class="flex-1 bg-stone-50 px-4 py-6 md:px-8">` ~line 218 — `<main id="parapet-main" tabindex="-1" class="...">`.
- `<aside class="min-w-0 space-y-6">` ~line 231 — `<aside class="min-w-0 space-y-6" aria-label="Incident actions">`.

DO NOT touch: theme-switcher JS / aria-pressed (NAV-03 locked), the Respond/Actions/History nav labels (NAV-04, preserve verbatim), or add any overflow-y-auto (DATA-02). The exact skip-link class string + SVG path data are in 46-PATTERNS.md and 46-UI-SPEC §NAV-05 / §DATA-04.
NOTE: line numbers are approximate — search for `<main class=`, `bg-teal-50`, `pagination_link_class`, `<.incident_list`, `<aside class="min-w-0` to locate anchors.
</interface>

<tasks>

<task type="auto">
  <name>Task 1: operator_detail_live.ex.eex skip-link + main landmark + aside aria-label (template + mirror)</name>
  <files>priv/templates/parapet.gen.ui/operator_detail_live.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex</files>
  <read_first>
    operator_detail_live.ex.eex lines 203–237 (.parapet-ui div, the single <main>, the <aside>, and the existing <section aria-label="Incident timeline"> analog).
    46-PATTERNS.md "Skip-to-content link + id=\"parapet-main\" markup" and "<aside aria-label=\"Incident actions\">" — exact skip-link markup + aside edit.
  </read_first>
  <action>
    Insert the skip-to-content `<a href="#parapet-main" class="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-50 flex min-h-[40px] items-center rounded-lg px-4 py-2 text-sm font-semibold bg-[color:var(--parapet-panel)] text-[color:var(--parapet-accent)] focus:outline-none focus:ring-2 focus:ring-offset-2 po-focus">Skip to main content</a>` as the FIRST child INSIDE the `.parapet-ui` div (before `<.operator_nav>`). Add `id="parapet-main" tabindex="-1"` to the single `<main>` element (keep its existing class). Add `aria-label="Incident actions"` to the `<aside class="min-w-0 space-y-6">` element. Apply EVERY edit identically to the demo mirror operator_detail_live.ex in this same task.
  </action>
  <verify>
    <automated>grep -c "Skip to main content" priv/templates/parapet.gen.ui/operator_detail_live.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex; grep -c 'aria-label="Incident actions"' priv/templates/parapet.gen.ui/operator_detail_live.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex; grep -c 'id="parapet-main"' priv/templates/parapet.gen.ui/operator_detail_live.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex</automated>
  </verify>
  <done>Both detail files contain the skip-link (count 1), aria-label="Incident actions" (count 1), and id="parapet-main" (count 1); the skip-link is inside the .parapet-ui div; the 46-01 "operator detail templates have correct landmarks" test now passes; template and mirror are byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: operator_live.ex.eex skip-link + three main landmarks + queue-refresh color + cockpit empty-state icon (template + mirror)</name>
  <files>priv/templates/parapet.gen.ui/operator_live.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex</files>
  <read_first>
    operator_live.ex.eex lines 118–296 (.parapet-ui div, the three page-mode branches with their <main> elements, the queue-refresh notification ~206, the cockpit no-selection empty state ~281).
    46-PATTERNS.md "Skip-to-content link", "Queue-refresh notification tokenized colors", "Decorative SVG icon additions" (cockpit cursor-select icon).
    46-RESEARCH.md Pitfall 3 (all three <main> branches) and Pitfall 2 (skip-link inside the div).
  </read_first>
  <action>
    Insert the same skip-to-content `<a href="#parapet-main" ...>Skip to main content</a>` (exact class string from Task 1) as the FIRST child INSIDE the `.parapet-ui` div, before `<.operator_nav>`. Add `id="parapet-main" tabindex="-1"` to ALL THREE `<main class="flex-1 bg-stone-50 px-4 py-6 md:px-8">` elements (`:actions`, `:history`, `:response` branches — search `<main class=` to find all three; missing any one is a defect). Replace the queue-refresh notification container `bg-teal-50 ... ring-stone-300` with `bg-[color:var(--parapet-accent-soft)] ring-1 ring-[color:var(--parapet-border)]` (keep mt-4 rounded-xl px-4 py-3 shadow-sm) and the text `<p class="text-sm font-medium text-teal-950">` with `<p class="text-sm font-medium" style="color: var(--parapet-text);">`; do NOT change the inner queue-refresh button (already tokenized). Add the cursor-select decorative SVG (`aria-hidden="true"`, `class="mx-auto mb-3 h-8 w-8"`, `style="color: var(--parapet-text-muted);"`, `stroke="currentColor"`) before the cockpit no-selection heading; no cursor-pointer/hover added. Do NOT touch the theme switcher JS/aria-pressed or the nav labels. Apply EVERY edit identically to the demo mirror operator_live.ex in this same task.
  </action>
  <verify>
    <automated>grep -c 'id="parapet-main"' priv/templates/parapet.gen.ui/operator_live.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex; grep -c "Skip to main content" priv/templates/parapet.gen.ui/operator_live.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex; grep -E "bg-teal-50|text-teal-950" priv/templates/parapet.gen.ui/operator_live.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex || echo "NO-TEAL-CLEAN"</automated>
  </verify>
  <done>Both operator_live files: id="parapet-main" appears exactly 3 times (one per page-mode branch), the skip-link appears once, the grep prints NO-TEAL-CLEAN (queue-refresh tokenized), and the cockpit empty state carries an aria-hidden SVG; theme switcher + nav labels unchanged; template and mirror byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: operator_live.ex.eex aria-live skeleton wrapper + pagination po-focus/aria-disabled (template + mirror)</name>
  <files>priv/templates/parapet.gen.ui/operator_live.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex</files>
  <read_first>
    operator_live.ex.eex the `<.incident_list ... />` call site (~235–241), `pagination_link_class/1` (~468–472), and the four pagination `<.link>` call sites (~172–189, 243–261).
    46-PATTERNS.md "Skeleton aria-live wrapper + animate-pulse" and "pagination_link_class/1 + po-focus + disabled aria-disabled/tabindex" — the exact wrapper + attribute markup.
    46-RESEARCH.md Open Question 1 (RESOLVED: use !connected?(assigns)) and Pitfall 6 (aria-disabled, not disabled, on <.link>).
  </read_first>
  <action>
    Wrap the `<.incident_list ... />` call in `<div aria-live="polite" aria-busy={if !connected?(assigns), do: "true", else: "false"}>` containing `<%= if !connected?(assigns) do %>` a skeleton `<div class="animate-pulse space-y-3" aria-hidden="true">` with three `<div class="h-16 rounded-lg bg-[color:var(--parapet-panel-muted)]"></div>` blocks `<% else %>` the existing `<.incident_list .../>` (unchanged props) `<% end %>`. Use `!connected?(assigns)` as the gate — add NO new socket assign (RESOLVED Open Q1). The `animate-pulse` is already zeroed by the existing prefers-reduced-motion block — add no new reduced-motion logic. Append `focus:outline-none focus:ring-2 focus:ring-offset-2 po-focus` to the `pagination_link_class(true)` return string (keep its existing utilities); leave `pagination_link_class(false)` styling unchanged. On each of the four pagination `<.link>` call sites, add inline attributes `aria-disabled={unless <has_page?>, do: "true"}` and `tabindex={unless <has_page?>, do: "-1"}` using the matching predicate (`@queue_page.has_previous_page?` / `has_next_page?`); do NOT use the HTML `disabled` attribute on `<.link>`. Do NOT add overflow-y-auto anywhere. Apply EVERY edit identically to the demo mirror operator_live.ex in this same task.
  </action>
  <verify>
    <automated>grep -c 'aria-live="polite"' priv/templates/parapet.gen.ui/operator_live.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex; grep -c "po-focus" priv/templates/parapet.gen.ui/operator_live.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex; grep -c "aria-disabled" priv/templates/parapet.gen.ui/operator_live.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex; grep -E "overflow-y-auto" priv/templates/parapet.gen.ui/operator_live.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex || echo "NO-OVERFLOW-CLEAN"; mix test test/parapet/operator_ui_contrast_test.exs test/parapet/operator_ui_demo_contract_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>Both operator_live files carry the aria-live="polite" skeleton wrapper (animate-pulse, !connected? gate), pagination_link_class(true) includes po-focus, the four pagination links carry aria-disabled + tabindex, the grep prints NO-OVERFLOW-CLEAN, and the quick contrast/demo-contract command is green (the 46-01 NAV-02/NAV-05/DATA-06/A11Y-03 live assertions now pass); template and mirror byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → host app | LiveView-template edits add static markup (skip-link, landmarks, skeleton, aria attributes) and a class-string append. No user/incident data is interpolated into new class/style/aria values; the only dynamic expressions (`aria-disabled={...}`, `aria-busy={...}`) are boolean-derived from existing pagination/connection predicates and HEEx auto-escapes them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-04 | Tampering | aria-* / aria-live / skip-link markup additions | low | mitigate | aria-current/aria-disabled/aria-busy are boolean-derived from existing predicates; aria-label and id are static strings; HEEx auto-escapes all interpolation; skip-link href="#parapet-main" is a same-page fragment with no URL-injection surface. |
| T-46-05 | Elevation of Privilege | skip-link target manipulation | low | accept | href and target id are static literals (`#parapet-main` / `id="parapet-main"`) — no user-controlled fragment, no injection path. |
| T-46-06 | Information Disclosure | Demo mirror divergence across four files | low | mitigate | Byte-mirror duplication enforced per template+mirror pair as an acceptance criterion; demo-contract test reads both paths. |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (46-RESEARCH Package Legitimacy Audit: none). |

Markup-only change. No data flow, auth, or user-input handling change. No `high`-severity threats.
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_contrast_test.exs test/parapet/operator_ui_demo_contract_test.exs` green (the 46-01 NAV-02/NAV-05/DATA-06/A11Y-03 live + detail assertions pass).
- grep confirms id="parapet-main" appears 3× in operator_live (per branch) and 1× in detail; skip-link present in both templates + both mirrors; NO-TEAL-CLEAN and NO-OVERFLOW-CLEAN; aria-live + po-focus + aria-disabled present.
- Theme switcher JS/aria-pressed and Respond/Actions/History labels unchanged (NAV-03/NAV-04).
</verification>

<success_criteria>
- Skip-link + id="parapet-main" landmarks present in both LiveView templates + mirrors (NAV-05); detail <aside aria-label> added.
- Queue-refresh notification tokenized — no bg-teal-50/text-teal-950 (NAV-02).
- aria-live skeleton wrapper on the incident list, !connected? gated, reduced-motion-safe (DATA-06).
- Pagination enabled links carry po-focus; disabled links carry aria-disabled + tabindex="-1" (A11Y-03).
- Cockpit no-selection empty state gains a decorative icon, no pointer affordance (DATA-04).
- No overflow-y-auto added (DATA-02); theme switcher + IA labels unchanged (NAV-03/NAV-04).
- Every edit byte-identical across template and demo mirror; quick command green.
</success_criteria>

<output>
Create `.planning/phases/46-navigation-shell-data-display/46-03-SUMMARY.md` when done. List each markup change per template, confirm id="parapet-main" appears 3× in operator_live and 1× in detail, confirm all four files (two templates + two mirrors) byte-identical for every edit, paste the NO-TEAL-CLEAN / NO-OVERFLOW-CLEAN + green quick-test tails, and confirm the theme switcher and nav labels were not touched.
</output>
