---
phase: 46-navigation-shell-data-display
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex
autonomous: true
requirements: [NAV-01, DATA-01, DATA-03, DATA-04, DATA-05]
must_haves:
  truths:
    - "The .po-nav-active CSS rule in operator_theme_bootstrap/1 gains border-bottom: 2px solid var(--parapet-accent) — a non-color shape indicator for the active nav item, working in both themes via the existing --parapet-accent cascade (no dark override added) (NAV-01)."
    - "incident_timeline/1 has an empty-state branch (icon + \"No timeline entries yet\" + explanatory copy) wrapping the entry <ul>, with timeline_entries hoisted above the if Enum.empty? check; the <ul> carries class po-timeline-list (DATA-03)."
    - "A CSS rule .parapet-ui .po-timeline-list > li:last-child > div > span[aria-hidden=\"true\"] { display: none } suppresses the connector spine on the final timeline entry (DATA-03)."
    - "incident_list/1 and action_center/1 empty branches each gain a decorative aria-hidden=\"true\" inline SVG icon before their heading, with no cursor-pointer/hover affordance added (DATA-04)."
    - "Every edit to operator_components.ex.eex is applied byte-identically to its demo mirror examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex in this same plan (GUARD-03 precursor)."
    - "No overflow-y-auto is introduced; incident_row truncation (min-w-0 flex-1 + truncate) and color-blind-safe chip text remain unchanged (DATA-01, DATA-02, DATA-05)."
  artifacts:
    - "operator_components.ex.eex + mirror with: .po-nav-active border-bottom rule, .po-timeline-list spine-suppression rule, timeline empty-state branch, and two empty-state icons."
  key_links:
    - "All new CSS rules use the .parapet-ui .po-* selector scoping and the locked --parapet-* variables — no new hex, no variable-value change (additive on Phase 44/45)."
    - "The empty-state icons reuse the inline style=\"color: var(--parapet-text-muted);\" + stroke=\"currentColor\" pattern; the nav-active border reuses the existing accent cascade so dark mode resolves to #7FB4C6 automatically."
---

<objective>
Make the operator_components.ex.eex template (and its byte-mirror) satisfy the NAV-01 active-nav shape indicator, the DATA-03 timeline empty-state + last-entry spine suppression, and the DATA-04 empty-state icons for the incident-queue and action-items empty branches — turning the matching 46-01 red assertions green. Confirm DATA-01/DATA-02/DATA-05 surfaces are already correct and remain untouched.

Purpose: This is the component-template half of Phase 46. The shared CSS for the whole operator UI lives in `operator_theme_bootstrap/1` inside this file, so the nav-active border rule and the timeline spine-suppression rule both land here. All changes are additive on the Phase 44/45 token system — no variable values change.
Output: A re-skinned operator_components.ex.eex + its demo mirror; the NAV-01 and DATA-03 component assertions from 46-01 pass; the quick contrast/demo-contract command is green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-navigation-shell-data-display/46-RESEARCH.md
@.planning/phases/46-navigation-shell-data-display/46-PATTERNS.md
@.planning/phases/46-navigation-shell-data-display/46-UI-SPEC.md
@priv/templates/parapet.gen.ui/operator_components.ex.eex
</context>

<interface>
Verified anchors in operator_components.ex.eex (apply every edit identically to the demo mirror operator_components.ex):
- `.po-nav-active` CSS rule at ~line 330 inside operator_theme_bootstrap/1 <style> — currently `background: var(--po-nav-active-bg); color: var(--po-nav-active-fg);`. ADD `border-bottom: 2px solid var(--parapet-accent);` to the SAME rule. Do NOT add a dark-block override — `--parapet-accent` already resolves to #7FB4C6 in both dark blocks (46-RESEARCH Pitfall 1).
- `incident_timeline/1` at ~line 915: `<div class="flow-root"><ul role="list" class="-mb-8">`; the `timeline_entries = @detail.timeline_entries || Enum.map(...)` assignment currently sits at ~line 919 INSIDE the loop scope — it MUST be hoisted above the new `if Enum.empty?(timeline_entries)` conditional (46-RESEARCH Pitfall 4). The `<ul>` becomes `<ul role="list" class="po-timeline-list -mb-8">`.
- `incident_list/1` empty branch at ~line 744: `<div class="px-4 py-6 text-center">` with text-only — ADD the decorative SVG before the `<p class="text-sm font-semibold...">` heading; container may go `px-4 py-8 text-center` per 46-RESEARCH Pattern 5.
- `action_center/1` empty branch at ~line 696: `<div class="rounded-xl border border-dashed border-stone-300 bg-white/70 p-6 text-center shadow-sm">` — ADD the decorative checkmark/inbox SVG before its heading.
- Existing spine `<span class="absolute top-4 left-4 -ml-px h-full w-0.5 bg-stone-200" aria-hidden="true">` at ~line 925 is suppressed VIA CSS only (the new .po-timeline-list last-child rule) — do NOT add per-item conditional markup.
The exact SVG path data + empty-state copy are in 46-PATTERNS.md "Decorative SVG icon additions" and 46-UI-SPEC §DATA-04 / §DATA-03. Empty-state copy is FIXED — use it verbatim; add no new copy beyond the timeline empty-state heading/body already specified.
NOTE: line numbers are approximate — search for `.po-nav-active`, `incident_timeline`, `Enum.empty?(@incidents)`, `Enum.empty?(@items)` to locate anchors; do not trust fixed offsets.
</interface>

<tasks>

<task type="auto">
  <name>Task 1: Add .po-nav-active border + .po-timeline-list spine-suppression CSS rules (template + mirror)</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</files>
  <read_first>
    operator_components.ex.eex lines 321–333 (the .po-nav-item / .po-nav-item:hover / .po-nav-active rule block) and the surrounding operator_theme_bootstrap/1 <style> region, plus the .po-queue-row-selected rule (after which the new spine rule is appended).
    46-PATTERNS.md ".po-nav-active CSS rule border-bottom addition" and "incident_timeline/1 empty-state branch + spine-suppression CSS" — the exact rule text.
    46-RESEARCH.md Pitfall 1 (no dark override for the border).
  </read_first>
  <action>
    In operator_theme_bootstrap/1's `<style>` block, edit the existing `.parapet-ui .po-nav-active` rule to add a third declaration `border-bottom: 2px solid var(--parapet-accent);` (keep the existing background + color declarations). Do NOT add any `html[data-parapet-theme="dark"]` override for this border — the accent variable cascade handles dark mode. Separately, append a new rule after the existing `.po-queue-row-selected` rule: `.parapet-ui .po-timeline-list > li:last-child > div > span[aria-hidden="true"] { display: none; }` (DATA-03 spine suppression). Every new CSS rule MUST keep the `.parapet-ui .po-*` selector scoping. Do NOT touch any Phase 45 rule (.po-button-*, .po-chip-*, .po-focus, .po-link, .po-timeline-badge-*, .po-queue-row-selected) or any `--parapet-*`/`--po-*` variable value. Apply BOTH rule edits identically to the demo mirror operator_components.ex in this same task.
  </action>
  <verify>
    <automated>grep -c "border-bottom: 2px solid var(--parapet-accent)" priv/templates/parapet.gen.ui/operator_components.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex; grep -c "po-timeline-list > li:last-child" priv/templates/parapet.gen.ui/operator_components.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</automated>
  </verify>
  <done>Both files contain the .po-nav-active border-bottom declaration and the .po-timeline-list last-child spine-suppression rule (grep count 1 each, both files); no dark-block override was added for the border; no Phase 45 rule or CSS variable value changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add timeline empty-state branch + po-timeline-list class + empty-state icons (template + mirror)</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</files>
  <read_first>
    operator_components.ex.eex incident_timeline/1 (~lines 915–958, full function), incident_list/1 empty branch (~lines 744–751), action_center/1 empty branch (~lines 696–701).
    46-PATTERNS.md "incident_timeline/1 empty-state branch" (hoist + conditional + class) and "Decorative SVG icon additions" (exact SVG path data for incident_list inbox icon and action_center checkmark icon, plus the timeline clock icon).
    46-UI-SPEC §DATA-03 (timeline empty-state copy) and §DATA-04 (empty-state container rules).
  </read_first>
  <action>
    In incident_timeline/1: HOIST the `timeline_entries = @detail.timeline_entries || Enum.map(...)` assignment to the first line inside the flow-root div, BEFORE any conditional (46-RESEARCH Pitfall 4 — EEx is top-to-bottom). Wrap the entry list in `<%= if Enum.empty?(timeline_entries) do %> ... <% else %> ... <% end %>`: the empty branch renders the designed empty state (clock SVG `aria-hidden="true"`, heading `No timeline entries yet`, body `Evidence and operator actions will appear here as they are recorded.`) using `style="color: var(--parapet-text);"` for the heading and `var(--parapet-text-muted)` for body/icon; the else branch renders the existing loop unchanged EXCEPT the `<ul>` now reads `<ul role="list" class="po-timeline-list -mb-8">`. Do NOT add per-item spine conditionals — the CSS rule from Task 1 handles spine suppression. In incident_list/1 empty branch: insert the inbox SVG (`aria-hidden="true"`, `class="mx-auto mb-3 h-8 w-8"`, `style="color: var(--parapet-text-muted);"`, `stroke="currentColor"`) before the `No active incidents` heading; the existing copy is unchanged. In action_center/1 empty branch: insert the checkmark/inbox SVG (same icon conventions) before the `No pending action items` heading. Do NOT add `cursor-pointer` or `hover:bg-*` to any empty-state container. Use the empty-state copy VERBATIM as specified — add no new copy. Apply EVERY edit identically to the demo mirror operator_components.ex in this same task.
  </action>
  <verify>
    <automated>grep -c "No timeline entries yet" priv/templates/parapet.gen.ui/operator_components.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex; grep -c 'class="po-timeline-list' priv/templates/parapet.gen.ui/operator_components.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex; mix test test/parapet/operator_ui_contrast_test.exs test/parapet/operator_ui_demo_contract_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>Both files contain the timeline empty-state heading and the po-timeline-list class (grep count 1 each); incident_list and action_center empty branches each carry an aria-hidden SVG icon with no pointer affordance; timeline_entries is hoisted above the empty check; the quick contrast/demo-contract test command is green (the 46-01 NAV-01 + DATA-03 component assertions now pass); template and mirror are byte-identical for every edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → host app | Component-template edits add static CSS rules + static markup (SVG icons, empty-state copy). No user/incident data is interpolated into class, style, or new attribute values; HEEx auto-escapes surrounding interpolations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-02 | Tampering | New CSS rules + static SVG/empty-state markup in operator_components | low | mitigate | All additions are static literal CSS and markup; aria-hidden SVGs carry no data; no new unescaped EEx interpolation introduced. |
| T-46-03 | Information Disclosure | Demo mirror divergence (template vs examples/) | low | mitigate | Byte-mirror duplication enforced as an acceptance criterion; the demo-contract test (GUARD-03 precursor) reads both paths. |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (46-RESEARCH Package Legitimacy Audit: none). |

Markup/CSS-only change. No data flow, auth, or user-input handling change. No `high`-severity threats.
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_contrast_test.exs test/parapet/operator_ui_demo_contract_test.exs` green (the 46-01 NAV-01 + DATA-03 component assertions pass).
- grep confirms the .po-nav-active border, the .po-timeline-list spine rule, the timeline empty-state copy, and the po-timeline-list class are present in BOTH the template and its demo mirror.
- No off-palette utility or variable value introduced; no Phase 45 rule touched.
</verification>

<success_criteria>
- .po-nav-active gains the border-bottom shape indicator (NAV-01); dark mode resolves via the accent cascade with no dark override.
- Timeline has a designed empty state + po-timeline-list class; the last-entry spine is CSS-suppressed (DATA-03).
- incident_list and action_center empty branches gain decorative icons with no pointer affordance (DATA-04).
- DATA-01/DATA-02/DATA-05 surfaces verified unchanged (truncation, no overflow-y-auto, chip-text status).
- Every edit is byte-identical in template and demo mirror.
- Quick contrast/demo-contract command green.
</success_criteria>

<output>
Create `.planning/phases/46-navigation-shell-data-display/46-02-SUMMARY.md` when done. List each CSS rule and markup change, confirm both operator_components files are byte-identical for every edit, note that timeline_entries was hoisted, paste the green quick-test tail, and confirm no Phase 45 rule or CSS variable value was changed.
</output>
