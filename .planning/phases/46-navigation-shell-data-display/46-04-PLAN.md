---
phase: 46-navigation-shell-data-display
plan: 04
type: execute
wave: 3
depends_on: ["46-02", "46-03"]
files_modified: []
autonomous: false
requirements: [NAV-02, NAV-05, DATA-03, DATA-04, DATA-06, A11Y-04]
must_haves:
  truths:
    - "The full ExUnit suite (mix test --exclude unboxed) is green — all Phase 46 contrast/demo-contract assertions from 46-01 pass and no prior test regressed."
    - "The gallery (make -C examples/demo_app gallery / gallery-shot) renders the nav active states, the empty timeline, the empty queue, the cockpit no-selection, and the empty action-items states under both light and dark themes at 390px with no horizontal overflow (NAV-02)."
    - "A keyboard walkthrough confirms the skip-to-content link is the first tab stop, becomes visible on focus, jumps to #parapet-main, and that tab order is logical with no keyboard trap (NAV-05, A11Y-04)."
    - "The timeline degrades correctly at zero/few/many entries — the empty state shows, and the connector spine is suppressed on the final entry (DATA-03); the skeleton replaces content with no layout jump and is static under reduced-motion (DATA-06)."
  artifacts:
    - "A human-approved gallery walkthrough record captured in 46-04-SUMMARY.md (no source files modified — this is the phase exit gate)."
  key_links:
    - "This plan is the rendered-behavior confirmation for the inherently-visual requirements that 46-01's string assertions cannot fully cover (NAV-02 390px, A11Y-04 tab order, DATA-03/DATA-06 rendered degradation), exactly as Phase 45's 45-04 closed with a gallery checkpoint."
---

<objective>
Close Phase 46 with the full-suite gate plus a human gallery walkthrough that confirms the rendered behaviors the automated string assertions cannot fully cover: 390px no-overflow shell, keyboard tab order + skip-link focus appearance, timeline degradation (zero/few/many + spine suppression), designed empty states, and skeleton no-layout-jump under reduced motion.

Purpose: Per 46-VALIDATION.md "## Manual / Gallery-Only Verifications", NAV-02 (390px), A11Y-04 (tab order), NAV-05 (skip-link focus), DATA-03 (spine), and DATA-06 (skeleton) require browser rendering. The 46-01 assertions lock the markup contract; this plan confirms the rendered result and serves as the phase exit gate. No source files change here.
Output: Full suite green + a human-approved gallery walkthrough recorded in the summary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-navigation-shell-data-display/46-UI-SPEC.md
@.planning/phases/46-navigation-shell-data-display/46-VALIDATION.md
</context>

<interface>
Gallery commands (verified in examples/demo_app/Makefile): `make -C examples/demo_app gallery` (interactive preview at /parapet/_gallery), `make -C examples/demo_app gallery-shot` (screenshot capture, desktop + mobile). Full-suite command: `mix test --exclude unboxed`. Quick command: `mix test test/parapet/operator_ui_contrast_test.exs test/parapet/operator_ui_demo_contract_test.exs`.
The gallery surfaces the empty/overflow/long-string/disabled states (GALLERY-01, Phase 44). 390px mobile capture is the NAV-02 / FLOW-05 mobile lens.
</interface>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite gate + gallery screenshot capture</name>
  <files></files>
  <read_first>
    46-VALIDATION.md "## Manual / Gallery-Only Verifications" — the five rendered behaviors and their verification approach.
    examples/demo_app/Makefile gallery / gallery-shot targets.
  </read_first>
  <action>
    Run the full ExUnit suite `mix test --exclude unboxed` and confirm green — all Phase 46 assertions added in 46-01 must now pass (NAV-01, NAV-02, NAV-05, DATA-03, DATA-06, A11Y-03, and the detail-landmark test) with no regression in any prior test. Then run `make -C examples/demo_app gallery-shot` to capture the desktop + mobile (390px) screenshots of the operator UI and gallery states. Confirm the capture completes and produces the light + dark, desktop + mobile shots that the human checkpoint will inspect. Do NOT modify any source file in this task — if the suite is red, STOP and report which assertion failed (the fix belongs in 46-02 or 46-03, not here).
  </action>
  <verify>
    <automated>mix test --exclude unboxed 2>&1 | tail -4</automated>
  </verify>
  <done>`mix test --exclude unboxed` is green (every 46-01 Phase 46 assertion passes, no prior regression); the gallery screenshot capture completed and produced the light/dark × desktop/mobile shots for the human walkthrough. No source files modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human gallery walkthrough — 390px, keyboard, timeline, empty states, skeleton</name>
  <files>(none — verification-only checkpoint)</files>
  <action>Drive the gallery walkthrough below; this is a blocking human-verify checkpoint, so the operative contract is in the &lt;what-built&gt;/&lt;how-to-verify&gt;/&lt;resume-signal&gt; elements. Do not modify source files; route any defect back to 46-02 or 46-03.</action>
  <what-built>The Phase 46 shell + data-display re-skin: an unambiguous active-nav indicator (accent underline) with aria-current, a skip-to-content link + parapet-main landmark on every page, a tokenized queue-refresh notice, a designed empty timeline + spine-suppressed final entry, decorative empty states (queue / cockpit no-selection / action-items), pagination focus rings + semantically-disabled disabled links, and a reduced-motion-safe loading skeleton — all byte-mirrored into the demo app and rendered in the gallery under both themes.</what-built>
  <how-to-verify>
    1. Start the gallery: `make -C examples/demo_app gallery` and open `/parapet/_gallery`.
    2. Resize to 390px (or use the gallery-shot mobile capture): confirm the header / nav / theme switcher stack with NO horizontal scrollbar and no squished controls (NAV-02). Confirm incident rows truncate long titles rather than overflow (DATA-01).
    3. Toggle Light / Dark / System: confirm the active nav tab shows the accent bottom-border indicator (not color alone) in every mode, and that the theme switcher still persists via localStorage (NAV-01, NAV-03).
    4. Press Tab from the very top of the page: confirm the "Skip to main content" link is the FIRST focus stop, becomes visible on focus with the po-focus ring, and that activating it moves focus into the main content (#parapet-main). Continue tabbing: confirm a logical order (nav → theme switcher → queue rows → pagination → refresh) with a visible focus ring on every control and NO keyboard trap (NAV-05, A11Y-03, A11Y-04).
    5. View the timeline with zero, a few (1–3), and many (20+) entries: confirm the empty state shows icon + "No timeline entries yet" + copy, and that on the few/many cases the connector spine does NOT render below the final entry (DATA-03).
    6. View the empty queue, the cockpit no-selection panel, and the empty action-items: confirm each shows a decorative icon + heading + copy and has NO pointer cursor / hover affordance (DATA-04).
    7. Reload the incident queue (or observe the SSR→mount cycle): confirm the skeleton placeholder matches row height so there is NO layout jump when content arrives, and that under prefers-reduced-motion the skeleton is static (no pulse) (DATA-06).
    8. Confirm status/severity chips read by their text label (not color alone) and the selected queue row shows its left accent bar (DATA-05).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any 390px-overflow / tab-order / skip-link / timeline / empty-state / skeleton issue to fix (fixes route back to 46-02 or 46-03).</resume-signal>
  <verify>Human confirms every &lt;how-to-verify&gt; step renders correctly in the gallery under both themes at 390px.</verify>
  <done>Human types "approved" after confirming NAV-02 390px no-overflow, NAV-05 skip-link first-tab + focus, A11Y-04 logical tab order with no trap, DATA-03 timeline degradation + spine suppression, DATA-04 designed empty states without pointer affordance, and DATA-06 reduced-motion-safe skeleton with no layout jump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → gallery/test runner | This plan only runs the test suite and the gallery preview; no source files, no user/incident data, no network or auth surface is modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-07 | Tampering | Gallery screenshot capture / test run | low | accept | Read-only verification step; no template, data, or config mutation; gallery is demo-only and never shipped into generated host UI (GALLERY-01 boundary). |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (46-RESEARCH Package Legitimacy Audit: none). |

Verification-only plan. No data flow, auth, or markup change. No `high`-severity threats.
</threat_model>

<verification>
- `mix test --exclude unboxed` green (phase exit gate; all 46-01 Phase 46 assertions pass).
- Gallery walkthrough at 390px + keyboard confirms NAV-02, NAV-05, A11Y-04, DATA-03, DATA-04, DATA-06 rendered behaviors.
- Human approval recorded.
</verification>

<success_criteria>
- Full ExUnit suite green with no regression.
- Gallery confirms 390px no-overflow, accent active-nav indicator in both themes, skip-link first-tab + focus visibility, logical tab order with no trap, timeline degradation + spine suppression, designed empty states without pointer affordance, and reduced-motion-safe skeleton with no layout jump.
- Human gallery walkthrough approved.
</success_criteria>

<output>
Create `.planning/phases/46-navigation-shell-data-display/46-04-SUMMARY.md` when done. Paste the green full-suite tail, confirm the gallery-shot capture, record the human walkthrough result per checklist item (NAV-02 390px, NAV-05 skip-link, A11Y-04 tab order, DATA-03 timeline degradation, DATA-04 empty states, DATA-06 skeleton), and note the human approval signal.
</output>
