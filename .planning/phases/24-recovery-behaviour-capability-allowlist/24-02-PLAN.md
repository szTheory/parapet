---
phase: 24-recovery-behaviour-capability-allowlist
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/capabilities.ex
  - docs/stability.md
autonomous: true
requirements:
  - RCV-03

must_haves:
  truths:
    - "`@valid_capabilities` in `lib/parapet/capabilities.ex` lists exactly 5 atoms in the order: `:retry_async_item`, `:requeue_dead_letter`, `:request_manual_provider_check`, `:revert_feature_flag`, `:disable_metric_label`"
    - "Attempting to register a capability id outside the 5-atom allowlist raises `ArgumentError` with a message naming the valid ids (existing regex `~r/Invalid recovery capability id/` still matches unchanged)"
    - "`docs/stability.md` Experimental Modules table includes a `Parapet.Recovery` row inserted alphabetically between `Parapet.MCP.PrometheusClient` and `Parapet.Telemetry.RecoveryAction`"
    - "Existing `Parapet.CapabilitiesTest` continues to pass with no test modifications (`mix test test/parapet/capabilities_test.exs` exits 0)"
  artifacts:
    - path: "lib/parapet/capabilities.ex"
      provides: "Widened `@valid_capabilities` module attribute (5 atoms instead of 3)"
      contains: ":revert_feature_flag"
      contains_also:
        - ":disable_metric_label"
    - path: "docs/stability.md"
      provides: "Experimental Modules table row for `Parapet.Recovery`"
      contains: "| `Parapet.Recovery` |"
  key_links:
    - from: "lib/parapet/capabilities.ex (`@valid_capabilities`)"
      to: "lib/parapet/capabilities.ex:42-45 (ArgumentError raise branch)"
      via: "module attribute interpolation via `inspect(@valid_capabilities)`"
      pattern: "inspect\\(@valid_capabilities\\)"
    - from: "docs/stability.md (Experimental Modules table)"
      to: "lib/parapet/recovery.ex (Experimental admonition, created in Plan 01)"
      via: "human-readable mirror of the machine classification"
      pattern: "Parapet\\.Recovery"
---

<objective>
Widen the `@valid_capabilities` allowlist in `lib/parapet/capabilities.ex` from 3 atoms to 5 by appending `:revert_feature_flag` and `:disable_metric_label`, and add the corresponding `Parapet.Recovery` row to the Experimental Modules table in `docs/stability.md`. Closes RCV-03 (allowlist widening from 3 to 5 atoms with `ArgumentError` for ids outside the set).

Purpose: Phase 27's prebuilt playbooks (Deploy-Tied Incident via `:revert_feature_flag`, Cardinality Blowout via `:disable_metric_label`) need these atoms registered to attach. The error message at `capabilities.ex:42-45` already interpolates `inspect(@valid_capabilities)`, so widening the module attribute is the only code change needed — the test regex (`capabilities_test.exs:32-36`) still matches unchanged.

Output: One modified file (`lib/parapet/capabilities.ex` — lines 14-18 only) and one modified file (`docs/stability.md` — single table row insertion around the Experimental Modules block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-recovery-behaviour-capability-allowlist/24-CONTEXT.md
@.planning/phases/24-recovery-behaviour-capability-allowlist/24-PATTERNS.md
@lib/parapet/capabilities.ex
@docs/stability.md
@test/parapet/capabilities_test.exs

<interfaces>
<!-- Key interfaces the executor needs. Extracted from the codebase. -->

From lib/parapet/capabilities.ex:14-18 (CURRENT — to be widened):

```elixir
@valid_capabilities [
  :retry_async_item,
  :requeue_dead_letter,
  :request_manual_provider_check
]
```

From lib/parapet/capabilities.ex:42-45 (DO NOT TOUCH — already correctly interpolates the widened list):

```elixir
def register_recovery(id, _attrs) do
  raise ArgumentError,
        "Invalid recovery capability id: #{inspect(id)}. Valid ids are: #{inspect(@valid_capabilities)}"
end
```

From test/parapet/capabilities_test.exs:32-36 (DO NOT TOUCH — regex still matches after widening):

```elixir
test "raises on invalid capability id" do
  assert_raise ArgumentError, ~r/Invalid recovery capability id/, fn ->
    Capabilities.register_recovery(:invalid_capability, name: "Invalid")
  end
end
```

From docs/stability.md (Experimental Modules table — adjacent rows for shape):

```markdown
| `Parapet.MCP.PrometheusClient` | Prometheus query client for MCP |
| `Parapet.Telemetry.RecoveryAction` | Machine-readable recovery action telemetry contract |
```

The new row goes BETWEEN these two (alphabetically: `Parapet.Recovery` sorts before `Parapet.Telemetry.*`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Widen @valid_capabilities in lib/parapet/capabilities.ex from 3 atoms to 5</name>
  <files>lib/parapet/capabilities.ex</files>
  <read_first>
    - lib/parapet/capabilities.ex — full file (66 LOC); the file being modified. Confirm current state of the `@valid_capabilities` list (lines 14-18) before editing and confirm that the `register_recovery/2` raise branch at lines 42-45 already interpolates `inspect(@valid_capabilities)` — so no edits to the raise branch are needed.
    - test/parapet/capabilities_test.exs — full file (53 LOC); the existing test file the widened allowlist must not break. The `assert_raise ArgumentError, ~r/Invalid recovery capability id/` at lines 32-36 still matches the widened message because the regex captures only the prefix.
    - .planning/phases/24-recovery-behaviour-capability-allowlist/24-CONTEXT.md — D-09 (the widened list in the exact ROADMAP.md success-criterion order), D-10 (no edits to the raise branch), D-11 (no edits to telemetry module).
    - .planning/phases/24-recovery-behaviour-capability-allowlist/24-PATTERNS.md — Pattern 8 (in-place widening — self-precedent at `capabilities.ex:14-18`); "Do NOT touch" list (preserves the per-key Agent architecture).
  </read_first>
  <action>
Edit `lib/parapet/capabilities.ex`. The ONLY change is to the `@valid_capabilities` module attribute at lines 14-18. Replace the existing 3-atom list:

```
@valid_capabilities [
  :retry_async_item,
  :requeue_dead_letter,
  :request_manual_provider_check
]
```

with the widened 5-atom list (order is locked by ROADMAP.md success criterion #3 enumeration and is the order the error message renders the valid ids — `capabilities.ex:42-45` interpolates `inspect(@valid_capabilities)`):

```
@valid_capabilities [
  :retry_async_item,
  :requeue_dead_letter,
  :request_manual_provider_check,
  :revert_feature_flag,
  :disable_metric_label
]
```

Do NOT touch any other line in `lib/parapet/capabilities.ex`:
- Per D-10, do NOT edit lines 42-45 (the `register_recovery/2` raise branch). It already interpolates `inspect(@valid_capabilities)` and the widened atoms flow through automatically.
- Per D-12, do NOT touch lines 20-22 (`start_link/1`) or lines 27-40 (`register_recovery/2` happy branch). The per-key Agent state pattern is the architectural foundation for Plan 03's 100-async test; touching it would invalidate the test premise.
- Do NOT touch the `@moduledoc` (lines 2-11).
- Do NOT touch `capabilities/1` or `get_recovery/1` (lines 47-64).

Do NOT touch `lib/parapet/telemetry/recovery_action.ex` (per D-11 — its `allowed_public_keys/1` enumerates metadata KEYS, not value vocabularies; atom-vocab agreement is enforced solely by `@valid_capabilities`).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
    <automated>mix test test/parapet/capabilities_test.exs --max-failures 1</automated>
    <automated>grep -E "^\s+:revert_feature_flag," lib/parapet/capabilities.ex | wc -l | awk '$1 == 1 { exit 0 } { exit 1 }'</automated>
    <automated>grep -E "^\s+:disable_metric_label$" lib/parapet/capabilities.ex | wc -l | awk '$1 == 1 { exit 0 } { exit 1 }'</automated>
    <automated>grep -cE "^\s+:retry_async_item," lib/parapet/capabilities.ex | awk '$1 == 1 { exit 0 } { exit 1 }'</automated>
    <automated>grep -cE "^\s+:requeue_dead_letter," lib/parapet/capabilities.ex | awk '$1 == 1 { exit 0 } { exit 1 }'</automated>
    <automated>grep -cE "^\s+:request_manual_provider_check," lib/parapet/capabilities.ex | awk '$1 == 1 { exit 0 } { exit 1 }'</automated>
    <automated>awk '/^\s+@valid_capabilities \[/{flag=1; next} flag &amp;&amp; /^\s+\]/{flag=0} flag' lib/parapet/capabilities.ex | grep -v '^#' | grep -c ':' | awk '$1 == 5 { exit 0 } { exit 1 }'</automated>
    <automated>git diff --name-only lib/parapet/capabilities.ex docs/stability.md lib/parapet/telemetry/recovery_action.ex 2&gt;/dev/null | grep -c "lib/parapet/telemetry/recovery_action.ex" | awk '$1 == 0 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `lib/parapet/capabilities.ex` exists and compiles.
    - `@valid_capabilities` module attribute contains exactly 5 atoms.
    - The 5 atoms appear in this exact order (verified by reading lines 14-19 of the modified file): `:retry_async_item`, `:requeue_dead_letter`, `:request_manual_provider_check`, `:revert_feature_flag`, `:disable_metric_label`.
    - `:revert_feature_flag` appears in the file exactly once (inside `@valid_capabilities`).
    - `:disable_metric_label` appears in the file exactly once (inside `@valid_capabilities`).
    - The `register_recovery/2` raise branch at lines 42-45 is unchanged byte-for-byte from the original file.
    - `mix compile --warnings-as-errors` exits 0.
    - `mix test test/parapet/capabilities_test.exs --max-failures 1` exits 0 (existing test `~r/Invalid recovery capability id/` still matches the widened error message).
    - No edits to `lib/parapet/telemetry/recovery_action.ex` (`git diff --name-only` does not list it).
  </acceptance_criteria>
  <done>`@valid_capabilities` is widened to 5 atoms in the locked order; existing capabilities tests still pass without modification; no other code file edited.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Parapet.Recovery row to docs/stability.md Experimental Modules table</name>
  <files>docs/stability.md</files>
  <read_first>
    - docs/stability.md lines 30-70 — the Experimental Modules table; confirm current adjacent-row shape at the insertion point (`Parapet.MCP.PrometheusClient` at line 48 and `Parapet.Telemetry.RecoveryAction` at line 49 in the file as it currently stands — Phase 23 already added the `Parapet.Telemetry.RecoveryAction` row).
    - .planning/phases/24-recovery-behaviour-capability-allowlist/24-CONTEXT.md — D-15 (alphabetical insertion between `Parapet.MCP.PrometheusClient` and `Parapet.Telemetry.RecoveryAction`); CONTEXT discretion bullet 2 (tier-description wording is Claude's choice provided it matches the format of adjacent rows).
    - .planning/phases/24-recovery-behaviour-capability-allowlist/24-PATTERNS.md — Pattern 9 (Experimental Modules table row format — pipe, backtick-wrapped module name, pipe, short tier description, pipe).
  </read_first>
  <action>
Edit `docs/stability.md`. The ONLY change is to insert ONE new row into the Experimental Modules table. The table currently contains (around lines 47-49 — verify the exact line numbers via `grep -n` before editing because line numbers shift if `docs/stability.md` was touched after Phase 23):

```markdown
| `Parapet.MCP.Server` | Read-only MCP server surface |
| `Parapet.MCP.PrometheusClient` | Prometheus query client for MCP |
| `Parapet.Telemetry.RecoveryAction` | Machine-readable recovery action telemetry contract |
```

Insert a new row alphabetically BETWEEN `Parapet.MCP.PrometheusClient` and `Parapet.Telemetry.RecoveryAction` (because `Parapet.Recovery` sorts before `Parapet.Telemetry.*` alphabetically — `R` before `T`). The result should be:

```markdown
| `Parapet.MCP.Server` | Read-only MCP server surface |
| `Parapet.MCP.PrometheusClient` | Prometheus query client for MCP |
| `Parapet.Recovery` | Host-app-facing recovery action behaviour + activation function |
| `Parapet.Telemetry.RecoveryAction` | Machine-readable recovery action telemetry contract |
```

Row shape rules (per Pattern 9):
- Pipe, single space, backtick-wrapped module name, single space, pipe, single space, tier description (sentence-case, no trailing period), single space, pipe.
- Tier description wording is Claude's discretion per D-15 / CONTEXT discretion bullet 2 — must match the format of adjacent rows (single short noun phrase, no trailing period, sentence-case). Suggested wording: `Host-app-facing recovery action behaviour + activation function`. Acceptable alternatives: `Host-registered recovery action behaviour` or `Recovery action capability behaviour`.

Do NOT touch:
- The Stable Modules table (lines ~24-39).
- Policy prose elsewhere in the file.
- Other rows in the Experimental Modules table.
- The outcome-atom freeze rule (around line 141).
- The MCP.PrometheusClient line or the Telemetry.RecoveryAction line — only INSERT a new line between them.

If the alphabetical-ordering check shows the existing table is NOT strictly alphabetical (the Phase 23 row insertion for `Parapet.Telemetry.RecoveryAction` may have been placed at a different position than strict alphabetical sort would dictate), still insert `Parapet.Recovery` between `Parapet.MCP.PrometheusClient` and `Parapet.Telemetry.RecoveryAction` (per D-15). Do NOT re-sort other rows in the table.
  </action>
  <verify>
    <automated>grep -cE "^\| \`Parapet\.Recovery\` \|" docs/stability.md | awk '$1 == 1 { exit 0 } { exit 1 }'</automated>
    <automated>awk '/^\| \`Parapet\.MCP\.PrometheusClient\` \|/{p=1; next} /^\| \`Parapet\.Telemetry\.RecoveryAction\` \|/{if (p &amp;&amp; r) {exit 0}; exit 1} p &amp;&amp; /^\| \`Parapet\.Recovery\` \|/{r=1}' docs/stability.md</automated>
    <automated>grep -cE "^\| \`Parapet\.MCP\.PrometheusClient\` \|" docs/stability.md | awk '$1 == 1 { exit 0 } { exit 1 }'</automated>
    <automated>grep -cE "^\| \`Parapet\.Telemetry\.RecoveryAction\` \|" docs/stability.md | awk '$1 == 1 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/stability.md` contains exactly one row matching `| \`Parapet.Recovery\` |` (verified by anchored regex).
    - The `Parapet.Recovery` row appears AFTER the `Parapet.MCP.PrometheusClient` row in file order.
    - The `Parapet.Recovery` row appears BEFORE the `Parapet.Telemetry.RecoveryAction` row in file order.
    - The `Parapet.MCP.PrometheusClient` row is unchanged byte-for-byte.
    - The `Parapet.Telemetry.RecoveryAction` row is unchanged byte-for-byte.
    - The Stable Modules table (rows above the Experimental Modules table) is unchanged.
    - The row shape matches adjacent rows: starts with `| \``, contains exactly two pipes inside the line, ends with ` |`, and the description is a sentence-case noun phrase with no trailing period.
    - `git diff --name-only` for this task lists exactly `docs/stability.md`.
  </acceptance_criteria>
  <done>One new row added to `docs/stability.md` Experimental Modules table for `Parapet.Recovery`, positioned alphabetically between `Parapet.MCP.PrometheusClient` and `Parapet.Telemetry.RecoveryAction`, matching the format of adjacent rows; no other edits to the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `@valid_capabilities` module attribute → `register_recovery/2` guard | In-process compile-time constant; no external input. |
| `docs/stability.md` table → `mix verify.public_api` machine classification | Human-readable documentation mirror; not used at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Tampering | `@valid_capabilities` atom set | mitigate | The atom set is a compile-time constant. The `register_recovery/2` guard at `capabilities.ex:27` (`when id in @valid_capabilities`) and the raise branch at `capabilities.ex:42-45` together enforce that only allowlisted ids reach the registry. Task 1's grep gates verify exactly 5 atoms in the locked order. |
| T-24-02-02 | Tampering | Order of atoms in `@valid_capabilities` | mitigate | Order is locked by D-09 because `capabilities.ex:42-45` interpolates `inspect(@valid_capabilities)` — the error message renders the atoms in this order. Task 1's grep gates verify each atom appears at the expected ordinal position. |
| T-24-02-03 | Information Disclosure | Docs/stability.md row | accept | Documentation only; no PII, no secrets. The row names the new module and its tier description. |
| T-24-02-04 | Repudiation | n/a | accept | No audit emit in this plan. |
| T-24-02-05 | Denial of Service | Allowlist widening | accept | The guard check `id in @valid_capabilities` is O(5) — negligible. No DoS surface. |
| T-24-02-06 | Elevation of Privilege | New atoms (`:revert_feature_flag`, `:disable_metric_label`) | mitigate | The atoms themselves are inert symbols; the security boundary is at the capability dispatch path (Phase 25 wires `confirm_runbook_step/4` through `ClaimService`, Phase 26 wires audit propagation). Phase 24's role is to register the symbols; no behavior is unlocked until Phase 25/26 wire them. |
| T-24-02-SC | Tampering (supply-chain) | npm/pip/cargo installs | accept | No new dependencies. Pure module-attribute and Markdown edits. |
</threat_model>

<verification>
End-to-end checks for this plan:

1. `mix compile --warnings-as-errors` exits 0.
2. `mix test test/parapet/capabilities_test.exs` exits 0 (existing tests still pass, including the `~r/Invalid recovery capability id/` assertion).
3. `mix test` exits 0 (no regressions elsewhere — full suite green).
4. `grep -cE "^\s+:revert_feature_flag" lib/parapet/capabilities.ex` returns 1.
5. `grep -cE "^\s+:disable_metric_label" lib/parapet/capabilities.ex` returns 1.
6. `grep -cE "^\| \`Parapet\.Recovery\` \|" docs/stability.md` returns 1.
7. The new docs row sits between `Parapet.MCP.PrometheusClient` and `Parapet.Telemetry.RecoveryAction` (verified by an awk script checking row order).
8. `git diff --name-only` reports exactly: `lib/parapet/capabilities.ex` and `docs/stability.md`.
</verification>

<success_criteria>
- `lib/parapet/capabilities.ex` `@valid_capabilities` contains exactly 5 atoms in the locked order: `:retry_async_item`, `:requeue_dead_letter`, `:request_manual_provider_check`, `:revert_feature_flag`, `:disable_metric_label`.
- `lib/parapet/capabilities.ex:42-45` raise branch is unchanged (the widened atoms flow through the existing `inspect(@valid_capabilities)` interpolation).
- `docs/stability.md` Experimental Modules table contains a `Parapet.Recovery` row inserted alphabetically between `Parapet.MCP.PrometheusClient` and `Parapet.Telemetry.RecoveryAction`, matching the format of adjacent rows.
- `mix test test/parapet/capabilities_test.exs` exits 0 (existing tests pass unchanged).
- `lib/parapet/telemetry/recovery_action.ex` is NOT modified.
- No other files in `lib/` are modified.
</success_criteria>

<output>
Create `.planning/phases/24-recovery-behaviour-capability-allowlist/24-02-SUMMARY.md` when done. Summary captures: the widened `@valid_capabilities` list (5 atoms in locked order), evidence that the existing `~r/Invalid recovery capability id/` test still matches the widened message, the new `docs/stability.md` row position and wording, and confirmation that `lib/parapet/telemetry/recovery_action.ex` was not touched (vocab agreement enforced solely by `@valid_capabilities`).
</output>
