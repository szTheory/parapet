---
phase: 33-documentation-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - MAINTAINING.md
  - CONTRIBUTING.md
  - examples/demo_app/README.md
  - examples/demo_app/Makefile
  - examples/demo_app/docker-compose.yml
autonomous: true
requirements:
  - MAT-05
  - MAT-06
  - MAT-08
must_haves:
  truths:
    - "Maintainers have a single root-level release procedure checklist that complements, rather than duplicates, release policy docs."
    - "Contributors can choose Conventional Commit prefixes that map cleanly to stable-line maintenance and deliberate feature work."
    - "The demo app has a documented, reproducible Docker Compose startup and smoke path with an explicit demo-only warning."
  artifacts:
    - path: "MAINTAINING.md"
      provides: "Maintainer-only release procedure truth"
      contains: "release_gate"
    - path: "CONTRIBUTING.md"
      provides: "Release Please-aligned commit taxonomy"
      contains: "BREAKING CHANGE"
    - path: "examples/demo_app/README.md"
      provides: "Compose startup and smoke-check instructions"
      contains: "docker compose up --build"
    - path: "examples/demo_app/Makefile"
      provides: "Demo compose convenience commands"
      contains: "up"
    - path: "examples/demo_app/docker-compose.yml"
      provides: "Demo app compose baseline"
      contains: "services:"
  key_links:
    - from: "MAINTAINING.md"
      to: "docs/release-policy.md"
      via: "linked maintainer/adopter authority split"
      pattern: "docs/release-policy.md"
    - from: "MAINTAINING.md"
      to: "docs/branch-protection.md"
      via: "branch protection reference"
      pattern: "docs/branch-protection.md"
    - from: "examples/demo_app/README.md"
      to: "examples/demo_app/docker-compose.yml"
      via: "documented compose startup path"
      pattern: "docker compose up --build"
    - from: "examples/demo_app/Makefile"
      to: "examples/demo_app/docker-compose.yml"
      via: "compose convenience wrapper"
      pattern: "docker compose|docker-compose"
---

<objective>
Add the remaining maintainer and contributor maturity docs, then validate the existing demo Compose path without creating a competing runtime setup.

Purpose: Complete MAT-05, MAT-06, and MAT-08 while keeping authority boundaries clean and preserving the existing demo app container flow.
Output: `MAINTAINING.md`, an expanded `CONTRIBUTING.md`, and a documented/validated Compose startup path for `examples/demo_app`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-documentation-polish/33-CONTEXT.md
@.planning/phases/33-documentation-polish/33-RESEARCH.md
@.planning/phases/33-documentation-polish/33-PATTERNS.md
@.planning/phases/33-documentation-polish/33-VALIDATION.md
@.planning/phases/32-ci-supply-chain-hardening/32-02-SUMMARY.md
@CONTRIBUTING.md
@docs/release-policy.md
@docs/branch-protection.md
@.github/PULL_REQUEST_TEMPLATE.md
@examples/demo_app/README.md
@examples/demo_app/docker-compose.yml
@examples/demo_app/Dockerfile
@examples/demo_app/Dockerfile.dev
@examples/demo_app/Makefile
@examples/demo_app/entrypoint.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create maintainer-only release procedure truth</name>
  <files>MAINTAINING.md</files>
  <read_first>
MAINTAINING.md (new file target)
docs/release-policy.md
docs/branch-protection.md
README.md
.planning/phases/33-documentation-polish/33-CONTEXT.md
.planning/phases/33-documentation-polish/33-RESEARCH.md
.planning/phases/33-documentation-polish/33-PATTERNS.md
.planning/phases/32-ci-supply-chain-hardening/32-02-SUMMARY.md
  </read_first>
  <acceptance_criteria>
- `MAINTAINING.md` exists at the repo root.
- The file explicitly references `docs/release-policy.md` and `docs/branch-protection.md` instead of duplicating their adopter-facing explanations.
- The file contains concrete maintainer steps for routine Release Please cuts, staged cuts via `Release-As:`, blocked cuts using `do-not-merge`, and post-publish verification.
- The file contains the exact identifier `release_gate`.
  </acceptance_criteria>
  <action>Create root `MAINTAINING.md` per D-05 as maintainer-only procedure truth. Keep it complementary to `docs/release-policy.md`: use the root file for checklist/operator actions, not for re-explaining release philosophy. Include concrete sections for routine release train checks (`release_gate`, Release Please PR state, branch protection expectations), staged version overrides using the `Release-As:` footer, how to pause a cut with the `do-not-merge` label, what to inspect if `.release-please-manifest.json` or CI blocks a cut, and how to verify the published Hex release after merge. Link out to `docs/release-policy.md` for policy and `docs/branch-protection.md` for repository settings.</action>
  <verify>
    <automated>test -f MAINTAINING.md && rg -q 'release_gate' MAINTAINING.md && rg -q 'Release-As:' MAINTAINING.md && rg -q 'do-not-merge' MAINTAINING.md && rg -q 'docs/branch-protection.md' MAINTAINING.md</automated>
  </verify>
  <done>`MAINTAINING.md` gives maintainers an exact release checklist without overlapping authority with adopter-facing release docs.</done>
</task>

<task type="auto">
  <name>Task 2: Expand CONTRIBUTING with Release Please commit taxonomy</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
CONTRIBUTING.md
.github/PULL_REQUEST_TEMPLATE.md
docs/release-policy.md
MAINTAINING.md
.planning/phases/33-documentation-polish/33-CONTEXT.md
.planning/phases/33-documentation-polish/33-RESEARCH.md
.planning/phases/33-documentation-polish/33-PATTERNS.md
  </read_first>
  <acceptance_criteria>
- `CONTRIBUTING.md` still owns the contributor workflow; no second contribution doc is introduced.
- The Conventional Commit section maps `feat:`, `fix:`, `docs:`, `refactor:`, `test:`, and `chore:` to concrete Parapet work classes.
- The file explains when `feat:` is appropriate on the stable line and reserves breaking markers (`!` or `BREAKING CHANGE:`) for explicit major-version work.
- The file references Release Please and aligns terminology with the PR template's `Stable-line maintenance` vs `Feature work` split.
  </acceptance_criteria>
  <action>Update `CONTRIBUTING.md` in place per D-06. Preserve the existing proof commands and PR flow, but replace the current short commit-prefix paragraph with a clearer taxonomy section that maps each allowed prefix to Parapet's actual work classes from `.github/PULL_REQUEST_TEMPLATE.md`. Make the guidance concrete: `feat:` is for deliberate additive behavior, `fix:` for bug fixes, `docs:` for documentation-only work, `refactor:` for behavior-preserving code movement, `test:` for proof-only changes, and `chore:` for maintenance/CI/package truth. Add a note that `!` and `BREAKING CHANGE:` imply intentional major-version planning and are not routine stable-line maintenance. Mention Release Please explicitly so contributors understand why prefix choice affects release notes and versioning.</action>
  <verify>
    <automated>rg -q 'Release Please|ReleasePlease' CONTRIBUTING.md && rg -q 'feat:' CONTRIBUTING.md && rg -q 'fix:' CONTRIBUTING.md && rg -q 'docs:' CONTRIBUTING.md && rg -q 'BREAKING CHANGE' CONTRIBUTING.md && rg -q 'Stable-line maintenance|stable-line maintenance' CONTRIBUTING.md</automated>
  </verify>
  <done>`CONTRIBUTING.md` gives contributors a release-aware commit taxonomy that matches the stable-line work classes already used in the PR template.</done>
</task>

<task type="auto">
  <name>Task 3: Document and validate the existing demo Compose startup path</name>
  <files>examples/demo_app/README.md, examples/demo_app/Makefile, examples/demo_app/docker-compose.yml</files>
  <read_first>
examples/demo_app/README.md
examples/demo_app/Makefile
examples/demo_app/docker-compose.yml
examples/demo_app/Dockerfile
examples/demo_app/Dockerfile.dev
examples/demo_app/entrypoint.sh
.github/workflows/ci.yml
.planning/phases/33-documentation-polish/33-CONTEXT.md
.planning/phases/33-documentation-polish/33-RESEARCH.md
.planning/phases/33-documentation-polish/33-PATTERNS.md
  </read_first>
  <acceptance_criteria>
- `examples/demo_app/README.md` documents `docker compose up --build`, `curl -f http://localhost:${WEB_PORT:-4000}/parapet`, and `docker compose down -v`.
- The README documents `WEB_PORT` and `DB_PORT` overrides and keeps the explicit demo-only auth warning.
- No new root-level Compose file is introduced; the baseline remains `examples/demo_app/docker-compose.yml`.
- If validation finds a concrete command-path defect, the fix is limited to `examples/demo_app/Makefile` and/or `examples/demo_app/docker-compose.yml` while preserving the existing `db` + `web` service shape.
  </acceptance_criteria>
  <action>Per D-07 and D-08, keep `examples/demo_app/docker-compose.yml` as the baseline MAT-08 artifact and validate the existing flow before editing it. Update `examples/demo_app/README.md` so the demo app has a reproducible container path with the exact commands `docker compose up --build`, `curl -f http://localhost:${WEB_PORT:-4000}/parapet`, and `docker compose down -v`, plus notes about `WEB_PORT` and `DB_PORT`. Preserve the existing warning that `/parapet` is intentionally unauthenticated only for the demo. Run `docker compose config` when Docker is available; if the only failure is that the convenience wrapper still assumes legacy `docker-compose`, update `examples/demo_app/Makefile` to prefer `docker compose` while allowing a `docker-compose` fallback. Only edit `examples/demo_app/docker-compose.yml` if validation proves a real defect in the existing two-service `db` + `web` setup.</action>
  <verify>
    <automated>cd examples/demo_app && if command -v docker >/dev/null 2>&1 && docker compose version >/dev/null 2>&1; then docker compose config >/dev/null; else rg -q 'docker compose up --build' README.md && rg -q 'curl -f http://localhost:\\$\\{WEB_PORT:-4000\\}/parapet' README.md && rg -q 'docker compose down -v' README.md && rg -q 'WEB_PORT' docker-compose.yml && rg -q 'DB_PORT' docker-compose.yml; fi</automated>
  </verify>
  <done>The demo app documents a reproducible Compose startup and smoke-check path, and any compatibility fix stays within the existing demo app Compose surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer docs -> release operations | Incorrect procedure text can cause unsafe or inconsistent release handling. |
| demo docs -> local Docker execution | Readers may treat the demo Compose path or open `/parapet` route as production guidance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Tampering | `MAINTAINING.md` vs `docs/release-policy.md` authority split | mitigate | Keep `MAINTAINING.md` checklist-oriented and link back to `docs/release-policy.md` and `docs/branch-protection.md` for canonical policy/settings. |
| T-33-04 | Elevation of Privilege | `examples/demo_app/README.md` demo routing guidance | mitigate | Preserve the explicit demo-only auth warning and avoid presenting the open `/parapet` route as production-safe. |
| T-33-05 | Denial of Service | `examples/demo_app/Makefile` compose command compatibility | mitigate | Prefer `docker compose` with a legacy fallback only if validation proves it necessary; validate with `docker compose config` when Docker is available. |
| T-33-SC | Tampering | package/dependency surface | mitigate | Do not add dependencies, do not touch `mix.lock`, and limit any demo changes to existing docs/Compose files. |
</threat_model>

<verification>
- `mix format --check-formatted`
- `test -f MAINTAINING.md`
- `rg -q 'BREAKING CHANGE' CONTRIBUTING.md`
- `cd examples/demo_app && if command -v docker >/dev/null 2>&1 && docker compose version >/dev/null 2>&1; then docker compose config >/dev/null; else rg -q 'docker compose up --build' README.md; fi`
</verification>

<success_criteria>
- `MAINTAINING.md` exists at the repo root and links to the existing release policy and branch-protection docs.
- `CONTRIBUTING.md` contains a Release Please-aware commit taxonomy aligned with the PR template work classes.
- The demo app README documents a reproducible Compose startup and smoke verification path, with Docker-unavailable fallback verification still available from source assertions.
</success_criteria>

<output>
Create `.planning/phases/33-documentation-polish/33-02-SUMMARY.md` when done
</output>
