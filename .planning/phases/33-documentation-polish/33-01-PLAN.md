---
phase: 33-documentation-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/migration-v1.md
  - docs/deployment.md
  - docs/assets/parapet-logo.svg
  - docs/assets/favicon.svg
  - mix.exs
autonomous: true
requirements:
  - DX-03
  - DX-04
  - MAT-07
must_haves:
  truths:
    - "Adopters can follow a published v0.x -> v1.0 migration guide with concrete upgrade checks."
    - "Adopters can follow a published deployment guide covering the host-owned Parapet surfaces they must wire and validate."
    - "HexDocs navigation and branding expose the new guides with a Parapet logo and favicon."
  artifacts:
    - path: "docs/migration-v1.md"
      provides: "Practical v0.x -> v1.0 migration guidance"
      contains: "mix parapet.doctor --ci"
    - path: "docs/deployment.md"
      provides: "Host-owned deployment guide"
      contains: "Parapet.Plug.DeployMarker"
    - path: "docs/assets/parapet-logo.svg"
      provides: "HexDocs logo asset"
      contains: "viewBox"
    - path: "docs/assets/favicon.svg"
      provides: "HexDocs favicon asset"
      contains: "viewBox"
    - path: "mix.exs"
      provides: "ExDoc extras, guide grouping, and branding configuration"
      contains: "docs/assets/parapet-logo.svg"
  key_links:
    - from: "mix.exs"
      to: "docs/migration-v1.md"
      via: "docs().extras and groups_for_extras"
      pattern: "docs/migration-v1.md"
    - from: "mix.exs"
      to: "docs/deployment.md"
      via: "docs().extras and groups_for_extras"
      pattern: "docs/deployment.md"
    - from: "mix.exs"
      to: "docs/assets/parapet-logo.svg"
      via: "docs().logo"
      pattern: "logo:"
    - from: "mix.exs"
      to: "docs/assets/favicon.svg"
      via: "docs().favicon"
      pattern: "favicon:"
---

<objective>
Publish the two remaining adopter-facing maturity guides and wire them into HexDocs with package-safe branding assets.

Purpose: Complete DX-03, DX-04, and MAT-07 without widening the package surface or changing runtime behavior.
Output: `docs/migration-v1.md`, `docs/deployment.md`, `docs/assets/parapet-logo.svg`, `docs/assets/favicon.svg`, and `mix.exs` docs configuration updates.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-documentation-polish/33-CONTEXT.md
@.planning/phases/33-documentation-polish/33-RESEARCH.md
@.planning/phases/33-documentation-polish/33-PATTERNS.md
@.planning/phases/33-documentation-polish/33-VALIDATION.md
@.planning/phases/29-stability-adopter-onboarding/29-04-SUMMARY.md
@mix.exs
@docs/getting-started.md
@docs/HISTORY.md
@docs/stability.md
@docs/recovery-actions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the practical v1 migration guide</name>
  <files>docs/migration-v1.md</files>
  <read_first>
docs/migration-v1.md (new file target)
docs/getting-started.md
docs/HISTORY.md
docs/stability.md
docs/recovery-actions.md
.planning/phases/33-documentation-polish/33-CONTEXT.md
.planning/phases/33-documentation-polish/33-RESEARCH.md
.planning/phases/33-documentation-polish/33-PATTERNS.md
  </read_first>
  <acceptance_criteria>
- `docs/migration-v1.md` exists under `docs/`.
- The guide explicitly points older release history to `docs/HISTORY.md` instead of retelling the full changelog.
- The guide names the `Parapet.SLO.define/2` deprecation and the `Parapet.SLO.Provider` replacement.
- The guide includes a safe-upgrade validation checklist that contains `mix compile --warnings-as-errors` and `mix parapet.doctor --ci`.
  </acceptance_criteria>
  <action>Create `docs/migration-v1.md` per D-01, D-02, and D-03 as a practical adopter guide for moving from pre-1.0 releases to the stable 1.x line. Use the concise, step-numbered style from `docs/getting-started.md`. Include concrete sections covering: who should use the guide; what 1.0 freezes (`docs/stability.md` public API and telemetry contract); install/config deltas from the `~> 0.10` examples to the current `~> 1.0` posture; migrating off deprecated `Parapet.SLO.define/2` toward `Parapet.SLO.Provider`; recovery/operator surfaces that are stable vs experimental; and a final upgrade checklist that ends with `mix compile --warnings-as-errors`, `mix test`, and `mix parapet.doctor --ci`. Link back to `docs/HISTORY.md` for pre-v1 historical detail instead of duplicating it.</action>
  <verify>
    <automated>test -f docs/migration-v1.md && rg -q 'docs/HISTORY.md|HISTORY.md' docs/migration-v1.md && rg -q 'Parapet\\.SLO\\.Provider' docs/migration-v1.md && rg -q 'mix parapet\\.doctor --ci' docs/migration-v1.md</automated>
  </verify>
  <done>`docs/migration-v1.md` gives adopters an actionable v0.x -> v1.0 path with explicit upgrade checks and no changelog duplication.</done>
</task>

<task type="auto">
  <name>Task 2: Write the host-owned deployment guide</name>
  <files>docs/deployment.md</files>
  <read_first>
docs/deployment.md (new file target)
docs/getting-started.md
docs/troubleshooting.md
docs/operator-ui.md
docs/release-policy.md
examples/demo_app/README.md
.planning/phases/33-documentation-polish/33-CONTEXT.md
.planning/phases/33-documentation-polish/33-RESEARCH.md
.planning/phases/33-documentation-polish/33-PATTERNS.md
  </read_first>
  <acceptance_criteria>
- `docs/deployment.md` exists under `docs/`.
- The guide explicitly covers metrics endpoint exposure, Prometheus rule loading, deploy markers, durable-evidence migrations, optional dependency compile-out notes, and `mix parapet.doctor --ci`.
- The guide warns that auth and endpoint exposure remain host-app responsibilities.
- The guide contains the exact identifiers `Parapet.Plug.DeployMarker`, `mix parapet.gen.prometheus`, and `mix ecto.migrate`.
  </acceptance_criteria>
  <action>Create `docs/deployment.md` per D-01, D-02, and D-04 as the adopter-facing deployment guide. Keep it focused on the Phoenix host surfaces Parapet actually touches: exposing and securing `/metrics`, loading the generated Prometheus rule files from `mix parapet.gen.prometheus`, recording deploy markers with `Parapet.Plug.DeployMarker` or `Parapet.Deploy.mark/1`, running `mix ecto.migrate` for Parapet's durable evidence tables, handling optional `Oban` and sibling-library compile-out behavior, and validating the deployed app with `mix parapet.doctor --ci`. Make the auth boundary explicit: Parapet does not own route authentication, admin scope, or network policy for metrics and operator routes.</action>
  <verify>
    <automated>test -f docs/deployment.md && rg -q 'Parapet\\.Plug\\.DeployMarker' docs/deployment.md && rg -q 'mix parapet\\.gen\\.prometheus' docs/deployment.md && rg -q 'mix ecto\\.migrate' docs/deployment.md && rg -q 'mix parapet\\.doctor --ci' docs/deployment.md</automated>
  </verify>
  <done>`docs/deployment.md` describes the concrete deployment surfaces, caveats, and validation steps Parapet adopters must own in production.</done>
</task>

<task type="auto">
  <name>Task 3: Wire guides into ExDoc and add package-safe branding assets</name>
  <files>mix.exs, docs/assets/parapet-logo.svg, docs/assets/favicon.svg</files>
  <read_first>
mix.exs
docs/assets/parapet-logo.svg (new file target)
docs/assets/favicon.svg (new file target)
docs/getting-started.md
.planning/phases/33-documentation-polish/33-CONTEXT.md
.planning/phases/33-documentation-polish/33-RESEARCH.md
.planning/phases/33-documentation-polish/33-PATTERNS.md
.planning/phases/29-stability-adopter-onboarding/29-04-SUMMARY.md
  </read_first>
  <acceptance_criteria>
- `docs/assets/parapet-logo.svg` and `docs/assets/favicon.svg` exist and each include `width`, `height`, and `viewBox`.
- `mix.exs` `docs()` includes exact `extras` entries for `docs/migration-v1.md` and `docs/deployment.md`.
- `mix.exs` `groups_for_extras` `Guides` includes both new guide paths.
- `mix.exs` `docs()` includes `logo: "docs/assets/parapet-logo.svg"` and `favicon: "docs/assets/favicon.svg"`.
- `package.files` remains docs-local; no new root asset directory is introduced.
  </acceptance_criteria>
  <action>Per D-01 and D-09, create static SVG assets at `docs/assets/parapet-logo.svg` and `docs/assets/favicon.svg` with hand-authored markup only; include `width`, `height`, and `viewBox` so ExDoc renders them predictably. Update `mix.exs` `docs()` in one pass: add `"docs/migration-v1.md"` and `"docs/deployment.md"` to `extras`, add both paths to the `Guides` group in `groups_for_extras`, and set `logo: "docs/assets/parapet-logo.svg"` plus `favicon: "docs/assets/favicon.svg"`. Leave `package.files` unchanged because the existing `docs` whitelist already covers the new guides and assets.</action>
  <verify>
    <automated>test -f docs/assets/parapet-logo.svg && test -f docs/assets/favicon.svg && rg -q 'width=' docs/assets/parapet-logo.svg && rg -q 'viewBox=' docs/assets/parapet-logo.svg && rg -q 'width=' docs/assets/favicon.svg && rg -q 'viewBox=' docs/assets/favicon.svg && rg -q '"docs/migration-v1.md"' mix.exs && rg -q '"docs/deployment.md"' mix.exs && rg -q 'logo: \"docs/assets/parapet-logo.svg\"' mix.exs && rg -q 'favicon: \"docs/assets/favicon.svg\"' mix.exs && MIX_ENV=dev mix docs --warnings-as-errors</automated>
  </verify>
  <done>HexDocs publishes the new guides in the `Guides` group and renders them with docs-local Parapet branding assets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> adopter deployment changes | Readers may treat guide steps as production-safe operational truth. |
| mix.exs docs config -> HexDocs output | Misconfigured extras or assets can silently hide guides or break branding in published docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | `docs/deployment.md` | mitigate | State explicitly that auth, metrics exposure, and operator route security remain host-app responsibilities; do not publish unauthenticated production route guidance. |
| T-33-02 | Tampering | `mix.exs` docs() wiring | mitigate | Add exact `extras`, `Guides`, `logo`, and `favicon` entries and prove them with `MIX_ENV=dev mix docs --warnings-as-errors`. |
| T-33-SC | Tampering | package/dependency surface | mitigate | Do not add dependencies or touch `mix.lock`; keep branding assets under `docs/assets/` so the existing package whitelist covers them. |
</threat_model>

<verification>
- `mix format --check-formatted`
- `MIX_ENV=dev mix docs --warnings-as-errors`
- `rg -q '"docs/migration-v1.md"' mix.exs`
- `rg -q '"docs/deployment.md"' mix.exs`
</verification>

<success_criteria>
- `docs/migration-v1.md` and `docs/deployment.md` are published through ExDoc `Guides`.
- HexDocs uses `docs/assets/parapet-logo.svg` and `docs/assets/favicon.svg` without changing the package whitelist.
- The new guides document the exact upgrade/deployment checks named in D-03 and D-04.
</success_criteria>

<output>
Create `.planning/phases/33-documentation-polish/33-01-SUMMARY.md` when done
</output>
