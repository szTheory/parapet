---
phase: 45-primitive-components
plan: 03
type: execute
wave: 3
depends_on: ["45-02"]
files_modified:
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex
autonomous: true
requirements: [COMP-01, COMP-05, COMP-07, COMP-08, MOTION-02]
must_haves:
  truths:
    - "preview_panel header/close/info no longer use indigo utilities — header bg is var(--parapet-accent), info block is .po-guidance, close hover is opacity-only (COMP-08)."
    - "suspect_changes_card icon + scope badges and the runbook guidance block use .po-chip po-chip-info / .po-guidance, not raw purple/violet/blue utilities (COMP-07, COMP-08)."
    - "The three standalone primary-tier navigation buttons (Return to response, Copy retrospective, Back to history) emit control_class(:primary) — removing inline bg-stone-950 + duration-100 + focus:ring-teal-300 (COMP-01, MOTION-02)."
    - "Stat/metric cards carry no cursor-pointer / hover affordance (COMP-05)."
    - "Template and demo mirror remain byte-identical after every inline-markup edit."
  artifacts:
    - "Re-skinned inline markup in operator_components.ex.eex preview_panel, suspect_changes_card, runbook_card, action_center, retrospective_card, action_rail (+ byte-identical demo mirror)."
  key_links:
    - "Inline markup consumes the .po-guidance / .po-button-primary classes defined in Plan 02."
    - "control_class(:primary) (added in Plan 02) is the replacement for the three inline bg-stone-950 buttons."
    - "operator_ui_contrast_test.exs second-test refutes for bg-indigo-*, bg-purple-100, bg-violet-100, bg-blue-50, transition-all, cursor-pointer go fully green after this plan."
---

<objective>
Remediate the remaining off-palette inline markup in `operator_components.ex.eex` (and its byte-identical demo mirror) — catalogued items 4-12 in 45-RESEARCH.md. Replace the `preview_panel` indigo utilities, the `suspect_changes_card` purple/violet badges, the `runbook_card` blue-50 guidance block, and migrate the three standalone `bg-stone-950 ... duration-100` navigation buttons to `control_class(:primary)`. Verify stat/metric cards carry no spurious pointer affordance.

Purpose: Plan 02 supplied the semantic vocabulary (`.po-guidance`, `.po-button-primary`, etc.); this plan applies it to the inline component markup that Plan 02 did not touch, closing the last operator_components off-palette gaps so the contrast-test refutes turn fully green for the primary file.
Output: Re-skinned inline markup in template + mirror; `mix test test/parapet/operator_ui_contrast_test.exs` fully green for the operator_components corpus.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-primitive-components/45-RESEARCH.md
@.planning/phases/45-primitive-components/45-PATTERNS.md
@.planning/phases/45-primitive-components/45-UI-SPEC.md
@priv/templates/parapet.gen.ui/operator_components.ex.eex
@examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex
</context>

<interface>
Verified inline-markup anchors in operator_components.ex.eex (apply identically to demo mirror):
- Line 609 (action_center "Return to response" .link): `bg-stone-950 ... duration-100 ... hover:bg-stone-800`.
- Line 899 (suspect_changes_card icon badge span): `bg-purple-100 text-purple-800`.
- Line 915 (suspect_changes_card scope badge span): `bg-violet-100 text-violet-800 ring-1 ring-violet-200/50`.
- Line 954 (retrospective_card "Copy retrospective" button): `bg-stone-950 ... duration-100 ... focus:ring-2 focus:ring-teal-300`.
- Line 1001 (runbook_card guidance block div): `bg-blue-50 border border-blue-100 rounded text-xs text-violet-800 italic`.
- Line 1059 (preview_panel header div): `bg-indigo-500 px-4 py-2 ...` plus ring-indigo-500.
- Line 1062 (preview_panel close button): `hover:text-indigo-100`.
- Line 1097 (preview_panel info <p>): `bg-indigo-50 px-3 py-2 text-xs text-indigo-900 ring-1 ring-indigo-100`.
- Line 1132 (action_rail "Back to history" <a>): `bg-stone-950 ... duration-100 ... hover:bg-stone-800`.
control_class(:primary) was added in Plan 02 and returns control_base() <> " po-button-primary".
NOTE: lines 569 (bg-teal-50 ring-teal-200), 727/889 (bg-violet-50/50 ring-violet-900/5), 1015 (bg-purple-50/text-purple-700/border-purple-100) use utilities ALREADY intercepted by the CSS rules at lines 257-274 — they are NOT COMP-08 violations and are NOT in scope. Do not edit them.
</interface>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin preview_panel, suspect_changes_card, and runbook_card inline markup (template + mirror)</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</files>
  <read_first>
    operator_components.ex.eex lines 889-920 (suspect_changes_card badges), 995-1005 (runbook_card guidance), 1055-1100 (preview_panel header/close/info).
    45-PATTERNS.md "Inline markup fixes in operator_components.ex.eex" — exact class replacements.
    45-RESEARCH.md "preview_panel Color Fix" code example and off-palette inventory items 7-12.
  </read_first>
  <action>
    preview_panel header (line ~1059): remove `bg-indigo-500` and `ring-indigo-500`; set the header background via `style="background: var(--parapet-accent);"` and the ring via `ring-[color:var(--parapet-border)]` (keep `ring-1` and existing layout utilities). preview_panel close button (line ~1062): replace `hover:text-indigo-100` with `hover:opacity-80`. preview_panel info <p> (line ~1097): replace `bg-indigo-50 ... text-indigo-900 ring-indigo-100` with `po-guidance` (keep `px-3 py-2 text-xs ring-1` and the rounded utility). suspect_changes_card icon badge (line ~899): replace `bg-purple-100 text-purple-800` with `po-chip po-chip-info` (keep sizing/layout utilities). suspect_changes_card scope badge (line ~915): replace `bg-violet-100 text-violet-800 ring-1 ring-violet-200/50` with `po-chip po-chip-info`. runbook_card guidance block (line ~1001): replace `bg-blue-50 border border-blue-100 ... text-violet-800` with `po-guidance` (keep `rounded text-xs italic` and spacing). Apply EVERY edit IDENTICALLY to the demo mirror in this same task. Do not introduce any new off-palette hex or raw color utility; use only the semantic classes/var() expressions named here.
  </action>
  <verify>
    <automated>grep -cE 'bg-indigo-500|bg-indigo-50 |ring-indigo|hover:text-indigo|bg-purple-100|bg-violet-100|bg-blue-50' priv/templates/parapet.gen.ui/operator_components.ex.eex; diff <(grep -oE '\.po-[a-z-]+|po-guidance|po-chip-info' priv/templates/parapet.gen.ui/operator_components.ex.eex | sort -u) <(grep -oE '\.po-[a-z-]+|po-guidance|po-chip-info' examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex | sort -u)</automated>
  </verify>
  <done>The targeted indigo/purple/violet/blue-50 utilities return 0 from grep in the template; the byte-mirror `.po-*` diff is empty; preview_panel/suspect_changes/runbook now use po-guidance / po-chip po-chip-info / var(--parapet-accent).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the three standalone primary-tier buttons to control_class(:primary) and confirm stat-card affordance (template + mirror)</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</files>
  <read_first>
    operator_components.ex.eex lines 605-612 (action_center "Return to response"), 950-958 (retrospective_card "Copy retrospective"), 1128-1135 (action_rail "Back to history").
    45-PATTERNS.md "Three standalone navigation buttons" + 45-RESEARCH.md "retrospective_card Copy Button Fix".
    45-UI-SPEC.md Stat/Metric Cards (COMP-05) — confirm no cursor-pointer / hover on stat containers.
  </read_first>
  <action>
    For each of the three buttons (line ~609 action_center .link, line ~954 retrospective_card button, line ~1132 action_rail <a>): replace the inline `bg-stone-950 ... transition-transform duration-100 ease-out active:scale-[0.96] ... hover:bg-stone-800` (and, on line 954, `focus:outline-none focus:ring-2 focus:ring-teal-300`) class string with a `class={[..layout-only utilities.., control_class(:primary)]}` list per 45-PATTERNS.md — preserving only genuinely-layout utilities (e.g. `flex-shrink-0` / `shrink-0` / `mt-4` / positioning) and letting control_class(:primary) supply the button styling, motion token, focus ring, and press affordance. Do NOT leave `duration-100`, `bg-stone-950`, or `focus:ring-teal-300` in any of the three. Then confirm (COMP-05) the operator_overview stat cards and response_cockpit metric boxes carry no `cursor-pointer` and no `hover:bg-*` on non-interactive containers; if any spurious pointer/hover affordance exists on a stat container, remove it (add `cursor-default` only if a pointer affordance is currently present). Apply EVERY edit IDENTICALLY to the demo mirror in this same task.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_contrast_test.exs && grep -c 'cursor-pointer' priv/templates/parapet.gen.ui/operator_components.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</automated>
  </verify>
  <done>`mix test test/parapet/operator_ui_contrast_test.exs` is fully GREEN (all Plan-01 refutes/asserts for the operator_components corpus pass); the three buttons emit control_class(:primary); `grep -c 'cursor-pointer'` returns 0 in both files; demo mirror is byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → host app | Inline markup edits change only static class/style strings; no user/incident data is interpolated into class or style attributes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-04 | Tampering | preview_panel `style="background: var(--parapet-accent)"` inline style | low | mitigate | The style value is a static CSS custom-property reference, not interpolated user data; HEEx renders it as a literal attribute. No injection surface. |
| T-45-05 | Information Disclosure | Demo mirror divergence | low | mitigate | Byte-mirror duplication enforced as an acceptance criterion (diff gate in verify). |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (45-RESEARCH.md audit: N/A). |

Presentation/markup-only changes. No data flow, auth, or user-input handling change. No `high`-severity threats.
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_contrast_test.exs` fully green (operator_components corpus has zero catalogued off-palette utilities, duration token present, po-focus present, no cursor-pointer).
- Byte-mirror `.po-*` diffs empty.
- Targeted indigo/purple/violet/blue-50 grep returns 0 in the template.
</verification>

<success_criteria>
- preview_panel, suspect_changes_card, runbook_card use tokenized semantic classes / var() expressions only.
- The three standalone primary-tier buttons emit control_class(:primary) with no leftover bg-stone-950/duration-100/focus:ring-teal-300.
- Stat/metric cards carry no spurious pointer/hover affordance.
- Template and demo mirror are byte-identical.
- operator_components contrast test is fully green.
</success_criteria>

<output>
Create `.planning/phases/45-primitive-components/45-03-SUMMARY.md` when done. List each inline markup block re-skinned, confirm the three buttons now use control_class(:primary), confirm stat-card affordance, confirm the byte-mirror diff is empty, and confirm `mix test test/parapet/operator_ui_contrast_test.exs` is fully green.
</output>
