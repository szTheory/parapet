---
phase: 45-primitive-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/parapet/operator_ui_contrast_test.exs
autonomous: true
requirements: [COMP-01, COMP-02, COMP-04, A11Y-02, COMP-05, COMP-06, COMP-07, COMP-08, FORM-02, MOTION-02]
must_haves:
  truths:
    - "Contrast test asserts AA (4.5:1) for primary/destructive/success button bg+fg pairs in BOTH light and dark themes (COMP-01, A11Y-02)."
    - "The second test refute-gates every off-palette utility catalogued for operator_components (bg-indigo-600, bg-emerald-600, bg-purple-100, bg-violet-100, bg-blue-50, bg-teal-700, hover:ring-teal-700, border-amber-, #042f2e) (COMP-08, COMP-04)."
    - "The second test asserts a disabled-affordance marker is present (disabled:opacity) so COMP-02 is gated red→green."
    - "The second test refutes transition-all and asserts duration-[--motion-fast] is present (MOTION-02)."
    - "The second test asserts po-focus is present and refutes cursor-pointer (COMP-06, COMP-05)."
    - "After this plan the targeted test suite is RED (refutes/asserts fail because violations still exist and disabled affordance is not yet wired in operator_components) — proving the gate is wired before remediation lands."
  artifacts:
    - "test/parapet/operator_ui_contrast_test.exs (extended @themes map + new assert_contrast calls + new refute/assert block)"
  key_links:
    - "@themes button pairs feed the for-loop assert_contrast calls."
    - "@component_paths (operator_components.ex template + demo mirror) is the corpus the refute/assert block scans."
---

<objective>
Wire the Phase 45 verification gate into the existing Phase 44 ExUnit harness BEFORE any remediation lands, so the re-skin verifies red→green. Extend `@themes` with the three new button-variant bg/fg pairs (primary, destructive, success) for both light and dark, add `assert_contrast/5` calls for each, and add a refute/assert block that gates off-palette utilities, motion-token usage, focus-ring presence, and pointer-cursor absence.

Purpose: Nyquist Dimension 8 — every behavior-adding remediation task in later plans (02/03/04) needs an automated verify that fails before the change and passes after. This plan creates that scaffold. No new test files; all assertions are additive to `test/parapet/operator_ui_contrast_test.exs`.
Output: An extended `operator_ui_contrast_test.exs` whose new assertions are RED until the operator_components remediation (Plans 02/03) lands.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-primitive-components/45-VALIDATION.md
@.planning/phases/45-primitive-components/45-PATTERNS.md
@test/parapet/operator_ui_contrast_test.exs
</context>

<interface>
Existing `@themes` light map ends with `warning_button_bg: "#B45309", warning_button_fg: "#FFFFFF"` (line 32).
Existing `@themes` dark map ends with `warning_button_bg: "#D97706", warning_button_fg: "#101820"` (line 56).
Existing for-loop (`for {theme, tokens} <- @themes do`, line 61) uses `assert_contrast(theme, :token_name, fg, bg, minimum)`.
Existing second test (`for path <- @component_paths do`, line 84) uses `content = File.read!(path)` then `assert content =~ "..."` / `refute content =~ "..."`; last assertion before its `end` is `assert content =~ "font-display: swap"` (line 119).
`@component_paths` = demo mirror `operator_components.ex` + template `operator_components.ex.eex` only (NOT the secondary live templates).
</interface>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend @themes with new button pairs and add contrast assertions</name>
  <files>test/parapet/operator_ui_contrast_test.exs</files>
  <read_first>
    test/parapet/operator_ui_contrast_test.exs lines 9-81 — the @themes light/dark maps and the for {theme, tokens} assertion loop.
    .planning/phases/45-primitive-components/45-PATTERNS.md "@themes extension" and "new assert_contrast calls" sections (exact values + insertion points).
  </read_first>
  <behavior>
    - Light theme asserts AA 4.5:1: primary_button (#FFFFFF on #101820), destructive_button (#FFFFFF on #B13A32), success_button (#FFFFFF on #567236).
    - Dark theme asserts AA 4.5:1: primary_button (#2E3A42 on #F8F4EC), destructive_button (#FFFFFF on #B13A32), success_button (#EFF6E8 on #3F5E28).
    - These three pairs are the new --po-button-* variants COMP-01/A11Y-02 require; the contrast helper proves each pairing meets AA in both themes.
  </behavior>
  <action>
    Add to the @themes LIGHT map (after the existing `warning_button_fg: "#FFFFFF"` entry) the keys per 45-PATTERNS.md "@themes extension": primary_button_bg/fg (#101820 / #FFFFFF), destructive_button_bg/fg (#B13A32 / #FFFFFF), success_button_bg/fg (#567236 / #FFFFFF). Add to the @themes DARK map (after `warning_button_fg: "#101820"`): primary_button_bg/fg (#F8F4EC / #2E3A42), destructive_button_bg/fg (#B13A32 / #FFFFFF), success_button_bg/fg (#3F5E28 / #EFF6E8). Inside the `for {theme, tokens} <- @themes do` loop, add three `assert_contrast(theme, :primary_button, tokens.primary_button_fg, tokens.primary_button_bg, 4.5)` style calls for primary_button, destructive_button, success_button per 45-PATTERNS.md "new assert_contrast calls". These dark success values (#EFF6E8 on #3F5E28) are the inverted-token pairing locked by 45-UI-SPEC.md Button Variants table; do not substitute lighter values.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_contrast_test.exs:61</automated>
  </verify>
  <done>The three new button pairs exist in both @themes maps and the for-loop runs assert_contrast for primary_button, destructive_button, success_button; the first test (line 61) passes (the new contrast pairs are AA-valid token values).</done>
</task>

<task type="auto">
  <name>Task 2: Add off-palette / disabled / motion / focus / cursor gate to the second test</name>
  <files>test/parapet/operator_ui_contrast_test.exs</files>
  <read_first>
    test/parapet/operator_ui_contrast_test.exs lines 84-121 — the `for path <- @component_paths do` block and its File.read!/assert/refute idiom.
    .planning/phases/45-primitive-components/45-PATTERNS.md "new refute/assert for COMP-06/07/08 + MOTION-02" section (exact assertion list).
  </read_first>
  <action>
    Inside the existing `for path <- @component_paths do` block, after the `assert content =~ "font-display: swap"` line and before the block's closing `end`, append the refute/assert set from 45-PATTERNS.md: refute each off-palette token (the indigo/emerald/purple/violet/blue-50/teal-700 utilities, `hover:ring-teal-700`, and the literal dark-pressed hex catalogued in 45-RESEARCH.md item 13); refute `transition-all` and assert `duration-[--motion-fast]` (MOTION-02); assert `po-focus` (COMP-06); refute the raw badge color-utility pairs and `bg-indigo-700` / `bg-violet-700` / `bg-slate-700` (COMP-07); refute `cursor-pointer` (COMP-05). THEN add the two new gates this revision requires: (COMP-02) `assert content =~ "disabled:opacity"` — proving control_base() wires a visual+semantic disabled affordance that every button variant inherits; and (COMP-04) `refute content =~ "border-amber-"` — catching the non-intercepted raw amber border utilities in the escalation-chain markup (lines 742/754/1007). Source the off-palette strings from 45-PATTERNS.md so they match the markup the remediation plans will produce. Do NOT add any of these forbidden literals anywhere except inside `refute content =~ "..."` / `assert content =~ "..."` calls (the `disabled:opacity` and `duration-[--motion-fast]` strings are inside `assert` calls, which is allowed).
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_contrast_test.exs 2>&1 | grep -qE 'failure|test.*failed' && echo "RED-as-expected" || echo "UNEXPECTED-GREEN"</automated>
  </verify>
  <done>The second test now refute/assert-gates off-palette utilities, motion token, po-focus, and cursor-pointer. Running the full file is RED (refutes fail because operator_components.ex.eex still holds the violations) — confirming the gate is live before remediation. The verify prints RED-as-expected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits a test file only. No runtime boundary, no user input, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-01 | Tampering | Test assertions encode forbidden literals | low | accept | Forbidden hex/utility strings appear only inside `refute content =~` guards in a test file; they are never written into generated templates and cannot reach a host app. |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (45-RESEARCH.md Package Legitimacy Audit: N/A). No [ASSUMED]/[SUS] packages. |

Phase 45 changes only presentation/markup/CSS of generated operator-UI templates and a test file. No data flow, auth, or user-input handling changes. Phoenix HEEx auto-escapes all interpolations; class/style strings are static. No `high`-severity threats.
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_contrast_test.exs:61` passes (new button contrast pairs are AA-valid).
- `mix test test/parapet/operator_ui_contrast_test.exs` is RED on the second test (refutes fail) — expected and required; remediation Plans 02/03 turn it green.
</verification>

<success_criteria>
- @themes carries primary/destructive/success button bg+fg pairs in both light and dark maps.
- The for-loop asserts AA contrast for all three new button variants in both themes.
- The second test refute/assert-gates off-palette utilities, transition-all/duration token, po-focus, and cursor-pointer.
- The harness is wired red so later remediation verifies green.
</success_criteria>

<output>
Create `.planning/phases/45-primitive-components/45-01-SUMMARY.md` when done. Record: which @themes keys were added, the new assert_contrast calls, the refute/assert set added to the second test, and explicit confirmation that the second test is RED (expected) pending Plans 02/03.
</output>
