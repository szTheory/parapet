---
phase: 45-primitive-components
plan: 04
type: execute
wave: 4
depends_on: ["45-03"]
files_modified:
  - priv/templates/parapet.gen.ui/operator_live.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex
  - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex
  - brandbook/notes/operator-audit-matrix.md
autonomous: false
requirements: [COMP-01, COMP-03, COMP-06, COMP-08, MOTION-02, FORM-01, FORM-02, COMP-02]
must_haves:
  truths:
    - "operator_live.ex.eex queue-refresh button uses tokenized accent/po-focus/duration-[--motion-fast] classes, not bg-teal-700/focus:ring-teal-300/duration-100 (COMP-08, MOTION-02, COMP-06)."
    - "operator_live.ex.eex pagination link hover uses var(--parapet-accent)/var(--parapet-border) tokens, not hover:ring-teal-700/hover:text-teal-700 (COMP-08)."
    - "operator_detail_live.ex.eex back-link uses .po-link, not text-teal-800/hover:text-teal-950 (COMP-03, COMP-08)."
    - "Every secondary-template edit is mirrored byte-identically to its demo counterpart (GUARD-03 precursor)."
    - "No off-palette/raw-Tailwind color hex remains in ANY of the three operator templates or their mirrors; the full ExUnit suite is green (COMP-08, A11Y-02)."
  artifacts:
    - "Re-skinned operator_live.ex.eex + mirror (queue-refresh button, pagination link hover)."
    - "Re-skinned operator_detail_live.ex.eex + mirror (back-link)."
    - "Updated brandbook/notes/operator-audit-matrix.md primitive-component cells marked done/verified."
  key_links:
    - "Secondary templates reuse the same --parapet-accent / --po-focus / .po-link vocabulary established Phase 44 + Plan 02."
    - "The phase-wide off-palette grep gate (this plan) is the Phase-50 GUARD-04 dry-run — it must return zero across all three templates."
---

<objective>
Clear the three deferred Phase-44 stubs in the secondary operator templates (`operator_live.ex.eex` queue-refresh button + pagination link hover; `operator_detail_live.ex.eex` back-link), each mirrored byte-identically, then run the phase-wide off-palette gate and full suite to prove zero off-palette color hex remains across all three templates. Finish by marking the primitive-component cells in the audit matrix done/verified.

Purpose: `@component_paths` in the contrast test only scans `operator_components` — the secondary templates carry their own off-palette utilities that Plans 02/03 did not cover. This plan closes them and runs the Phase-50 GUARD-04-style grep as a dry-run so Phase 50's gate finds nothing. It is the phase's exit gate (COMP-08 is gate-enforced).
Output: Tokenized secondary templates + mirrors; zero off-palette hits phase-wide; full suite green; audit matrix updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-primitive-components/45-RESEARCH.md
@.planning/phases/45-primitive-components/45-PATTERNS.md
@.planning/phases/45-primitive-components/45-UI-SPEC.md
@priv/templates/parapet.gen.ui/operator_live.ex.eex
@priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
@brandbook/notes/operator-audit-matrix.md
</context>

<interface>
Verified secondary-template anchors (apply identically to demo mirrors):
- operator_live.ex.eex line 211 (queue-refresh button): `bg-teal-700 ... transition-transform duration-100 ease-out active:scale-[0.96] hover:bg-teal-800 focus:outline-none focus:ring-2 focus:ring-teal-300`.
- operator_live.ex.eex line 469 (pagination link hover, in a class-returning helper): `ring-1 ring-stone-300 bg-white text-stone-900 hover:ring-teal-700 hover:text-teal-700`.
- operator_detail_live.ex.eex line 211 (back-link .link): `text-teal-800 underline decoration-stone-300 underline-offset-4 hover:text-teal-950`.
NOTE: operator_live.ex.eex line 206-207 (`bg-teal-50` panel + `text-teal-950` text) are INTERCEPTED by the operator_components CSS rules at lines 257-261 (bg-teal-50 → accent-soft, text-teal-950 → accent-text) — they are NOT off-palette violations and are out of scope. operator_live.ex.eex does NOT import control_class/2, so use inline token expressions (not control_class) per 45-RESEARCH.md Open Question 3.
Phase-wide off-palette gate pattern (45-RESEARCH.md "Don't Hand-Roll"): grep the three templates for raw signal-color utilities + non-token hex; the only admitted non-token hex is #7FB4C6 (operator dark-link exception, D-08).
</interface>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin operator_live.ex.eex + operator_detail_live.ex.eex secondary stubs (templates + mirrors)</name>
  <files>priv/templates/parapet.gen.ui/operator_live.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex, priv/templates/parapet.gen.ui/operator_detail_live.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex</files>
  <read_first>
    operator_live.ex.eex lines 205-215 (queue-refresh button) and 465-472 (pagination link helper).
    operator_detail_live.ex.eex lines 207-215 (back-link).
    45-PATTERNS.md "Secondary template stubs" sections — exact inline token replacements.
  </read_first>
  <action>
    operator_live.ex.eex queue-refresh button (line ~211): replace `bg-teal-700 hover:bg-teal-800 focus:ring-teal-300 ... duration-100` with the inline token expression from 45-PATTERNS.md: `bg-[color:var(--parapet-accent)] text-white hover:bg-[color:var(--parapet-accent-strong)] focus:outline-none focus:ring-2 focus:ring-offset-2 po-focus duration-[--motion-fast] transition-colors ease-out` (keep min-h/layout utilities; drop transition-transform-with-duration-100 in favor of the listed transition; do not use transition-all). operator_live.ex.eex pagination link helper (line ~469): replace `hover:ring-teal-700 hover:text-teal-700` with `hover:text-[color:var(--parapet-accent)] hover:ring-[color:var(--parapet-border)]` (keep `ring-1 ring-stone-300 bg-white text-stone-900`). operator_detail_live.ex.eex back-link (line ~211): replace `text-teal-800 hover:text-teal-950` with `po-link` (keep `inline-flex min-h-[40px] items-center rounded-lg text-sm font-semibold underline decoration-stone-300 underline-offset-4`; .po-link supplies color + hover). Apply EVERY edit IDENTICALLY to each demo mirror in this same task.
  </action>
  <verify>
    <automated>grep -cE 'bg-teal-700|hover:bg-teal-800|focus:ring-teal-300|hover:ring-teal-700|hover:text-teal-700|hover:text-teal-950|text-teal-800|duration-100|transition-all' priv/templates/parapet.gen.ui/operator_live.ex.eex priv/templates/parapet.gen.ui/operator_detail_live.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex</automated>
  </verify>
  <done>The off-palette/duration grep returns 0 for all four secondary files; queue-refresh button + pagination hover use accent/border tokens + po-focus; back-link uses po-link; every edit is mirrored byte-identically.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-wide off-palette gate dry-run + full suite + audit matrix update</name>
  <files>brandbook/notes/operator-audit-matrix.md</files>
  <read_first>
    brandbook/notes/operator-audit-matrix.md — the component × state ledger; locate the primitive-component rows (buttons, links, badges, chips, status pills, stat cards, icons, dividers, focus, theme switcher) and their todo/done/verified status column.
    45-RESEARCH.md "Off-Palette Hex Remediation: Complete Inventory" — the full 20-item list this gate must find zero of.
  </read_first>
  <action>
    Run the Phase-50 GUARD-04-style off-palette grep across all three templates and their mirrors (raw signal-color Tailwind utilities — indigo/violet/purple/emerald/teal/slate signal uses and non-token color hex), allowing ONLY the #7FB4C6 operator dark-link exception (D-08) and intercepted neutral-stone utilities. Confirm zero hits beyond the allowed exception. Run the full suite `mix test --exclude unboxed` and confirm green. Then update brandbook/notes/operator-audit-matrix.md: mark the primitive-component cells (buttons/links/badges/chips/status pills/stat cards/icons/dividers/focus rings/theme-switcher) from `todo` to `done` (automated-verified) or `verified` where a manual gallery check applies. Do NOT mark Phase 46+ component rows. Do NOT add the off-palette literals to the matrix prose except as already-present row labels.
  </action>
  <verify>
    <automated>mix test --exclude unboxed 2>&1 | tail -3; grep -rnoE 'bg-indigo-[0-9]|bg-emerald-[0-9]|bg-purple-100|bg-violet-(100|700)|bg-blue-50|bg-teal-700|bg-slate-700|bg-indigo-700|#042f2e|focus:ring-(teal|indigo|emerald|amber)-300' priv/templates/parapet.gen.ui/ examples/demo_app/lib/demo_app_web/live/parapet/ | grep -v '#7FB4C6' || echo "OFF-PALETTE-CLEAN"</automated>
  </verify>
  <done>`mix test --exclude unboxed` is green; the off-palette grep prints OFF-PALETTE-CLEAN (zero hits beyond the #7FB4C6 exception) across all three templates and mirrors; the primitive-component audit-matrix cells are marked done/verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full operator-UI primitive re-skin: tokenized button variants, links, badges/chips/status pills, stat cards, icons/dividers, focus rings, and theme-switcher controls across both themes — with disabled controls visually + semantically disabled and fast (~120ms) motion.</what-built>
  <how-to-verify>
    1. Start the demo app and open `/parapet/_gallery` (the demo-only component gallery).
    2. Toggle Light / Dark / System via the theme switcher; confirm every primitive renders on-brand in all three modes with no off-brand teal/indigo/emerald/purple residue.
    3. Tab through buttons, links, and theme-option buttons; confirm a visible focus-visible ring on every control (watch-blue on light, limestone on dark).
    4. Confirm disabled buttons read as disabled (reduced opacity, not-allowed cursor) and are non-interactive; confirm no enabled-looking-but-dead or disabled-looking-but-live controls (COMP-02).
    5. Hover/press each button variant; confirm the press scale feels fast (~120ms) with no transition-all thrash (MOTION-02).
    6. Confirm stat/metric cards show no pointer cursor or hover affordance (COMP-05).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any off-brand / a11y / motion issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → host app | Secondary-template edits change only static class strings; no user/incident data is interpolated into class or style attributes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-06 | Tampering | Inline token class strings in secondary templates | low | mitigate | All replacements are static literal class strings; no user/incident data interpolated. HEEx auto-escapes surrounding interpolation. |
| T-45-07 | Information Disclosure | Demo mirror divergence | low | mitigate | Byte-mirror duplication enforced as an acceptance criterion (grep gate in verify). |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (45-RESEARCH.md audit: N/A). |

Presentation/markup-only changes. No data flow, auth, or user-input handling change. No `high`-severity threats.
</threat_model>

<verification>
- `mix test --exclude unboxed` green (phase exit gate).
- Off-palette grep across all three templates + mirrors prints OFF-PALETTE-CLEAN (only #7FB4C6 admitted).
- Human gallery walkthrough approves the re-skin in all three theme modes (COMP-02, COMP-05, COMP-06, MOTION-02 confirmatory checks).
</verification>

<success_criteria>
- Secondary templates + mirrors carry zero off-palette signal-color utilities; queue-refresh/pagination/back-link are tokenized.
- Phase-wide off-palette gate returns zero hits beyond the #7FB4C6 exception.
- Full suite green.
- Primitive-component audit-matrix cells marked done/verified.
- Human gallery walkthrough approved.
</success_criteria>

<output>
Create `.planning/phases/45-primitive-components/45-04-SUMMARY.md` when done. List each secondary-template stub re-skinned, confirm all four mirrors byte-identical, paste the OFF-PALETTE-CLEAN gate result, confirm full-suite green, list the audit-matrix cells updated, and record the human gallery approval.
</output>