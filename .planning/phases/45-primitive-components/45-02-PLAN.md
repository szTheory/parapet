---
phase: 45-primitive-components
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex
autonomous: true
requirements: [COMP-01, COMP-02, COMP-04, COMP-06, COMP-07, COMP-08, MOTION-02, A11Y-02]
must_haves:
  truths:
    - "Button variants primary/recovery/destructive/success/warning resolve to tokenized .po-button-* classes with rest+hover states correct in BOTH themes (COMP-01)."
    - "control_base() uses duration-[--motion-fast], focus:ring-offset-2, and po-focus — so every variant gets the motion token and the per-surface focus ring automatically (COMP-06, MOTION-02)."
    - "timeline actor badges (operator/copilot/external) and the selected queue row use new semantic .po-* classes, not raw indigo/violet/slate/teal utilities (COMP-07, COMP-08)."
    - "The dark theme-switcher selected-state color is var(--po-nav-active-fg), not the raw #042f2e hex (COMP-08, FORM-01-precursor)."
    - "Template and demo mirror are byte-identical for every new CSS rule, new --po-button-* variable, and every edited Elixir function body (GUARD-03 precursor)."
  artifacts:
    - "New CSS rules in operator_theme_bootstrap/1: .po-button-primary, .po-button-recovery, .po-button-destructive, .po-button-success, .po-guidance, .po-timeline-badge-operator, .po-timeline-badge-copilot, .po-timeline-badge-external, .po-queue-row-selected (+ :hover variants)."
    - "New CSS variables: --po-button-primary-bg/fg/hover, --po-button-recovery-bg/fg/hover, --po-button-destructive-bg/fg/hover, --po-button-success-bg/fg/hover."
    - "Updated Elixir functions: control_base/0, control_class(:primary|:recovery|:success|:destructive|:warning, _), timeline_entry_badge_class/1, queue_row_class/2."
  key_links:
    - "control_class/2 variants reference .po-button-* classes whose rules read --po-button-* vars defined in the same <style> block."
    - "po-focus in control_base() reads --po-focus / --po-focus-offset (already defined Phase 44) for the per-surface ring."
    - "operator_ui_contrast_test.exs (Plan 01) refutes the removed utilities and asserts duration-[--motion-fast] + po-focus — this plan turns those green for operator_components."
---

<objective>
Remediate the CSS foundation and the class-returning Elixir functions in `operator_components.ex.eex` (and its byte-identical demo mirror) to the locked v1.5 token system. Add the new `.po-button-*` / `.po-guidance` / `.po-timeline-badge-*` / `.po-queue-row-selected` CSS rules and their `--po-button-*` variables; rewrite `control_base/0`, `control_class/2`, `timeline_entry_badge_class/1`, and `queue_row_class/2` to emit semantic classes; and fix the raw `#042f2e` hex in the dark theme-switcher CSS rule.

Purpose: This is the values-applied core of the re-skin — every off-palette utility that lives in a CSS rule or a class-returning function (catalogued items 1-3, 13-17 in 45-RESEARCH.md) is replaced here. The new `.po-*` rules are the vocabulary the inline-markup remediation (Plan 03) consumes.
Output: A re-skinned CSS block + function set in both template and mirror; the contrast-test contrast pairs and the function-level refutes (timeline badges, queue row, #042f2e, duration token, po-focus) go green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-primitive-components/45-RESEARCH.md
@.planning/phases/45-primitive-components/45-PATTERNS.md
@.planning/phases/45-primitive-components/45-UI-SPEC.md
@priv/templates/parapet.gen.ui/operator_components.ex.eex
@examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex
</context>

<interface>
Existing CSS anchors in operator_components.ex.eex `operator_theme_bootstrap/1`:
- Light `.parapet-ui { }` block declares `--po-button-warning-*` ending near line 94.
- `.po-button-warning` / `.po-button-warning:hover` rules are at lines 356-363; new rules insert after them (before `.parapet-theme-option` at line 365).
- Three theme blocks exist: light (~8-60), explicit dark (~62-115), media-query dark (~196-234). Variables whose VALUE differs in dark must be added to BOTH dark blocks.
- Line 378: `html[data-parapet-theme="dark"] .parapet-theme-option[aria-pressed="true"] { color: #042f2e; }`.
Existing Elixir anchors (verified line numbers):
- `control_class(:recovery, width)` line 1262 (`bg-indigo-600 ...`).
- `control_class(:success, width)` line 1281 (`bg-emerald-600 ...`).
- `control_class(:warning, width)` carries trailing `focus:ring-amber-300`.
- `control_base/0` line 1284 (`... transition-transform duration-100 ease-out active:scale-[0.96] focus:outline-none focus:ring-2`).
- `timeline_entry_badge_class/1` lines 1475-1477 (`bg-indigo-700` / `bg-violet-700` / `bg-slate-700`; the `:system` clause already returns `"po-button-warning"`).
- `queue_row_class/2` selected branch line 1334 (`border-l-teal-700 bg-teal-50/80 hover:bg-teal-50`).
Demo mirror `operator_components.ex` is byte-equivalent and holds the same anchors.
</interface>

<tasks>

<task type="auto">
  <name>Task 1: Add new CSS variables + semantic class rules + fix #042f2e (template + mirror)</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</files>
  <read_first>
    operator_components.ex.eex lines 8-115 and 196-234 — the three theme blocks; lines 356-365 — the .po-button-warning rules and where new rules insert; line 378 — the #042f2e rule.
    45-PATTERNS.md "New CSS class rules in operator_theme_bootstrap/1" — the exact variable list, dark-block overrides, the full new-rules CSS block, and the #042f2e replacement.
    45-RESEARCH.md "Code Examples" — Adding New CSS Variable Group + Adding New Semantic CSS Class Rules.
  </read_first>
  <action>
    In the LIGHT `.parapet-ui { }` block, after the existing `--po-button-warning-hover` declaration, append the new variables exactly as listed in 45-PATTERNS.md: `--po-button-primary-bg/fg/hover` (var(--parapet-text) / var(--parapet-panel) / var(--parapet-text-muted)), `--po-button-recovery-bg/fg/hover` (var(--parapet-accent) / #FFFFFF / var(--parapet-accent-strong)), `--po-button-destructive-bg/fg/hover` (#B13A32 / #FFFFFF / #8C2E27), `--po-button-success-bg/fg/hover` (#567236 / #FFFFFF / #3F5E28). In BOTH dark blocks add ONLY the success override (`--po-button-success-bg: #3F5E28; --po-button-success-fg: #EFF6E8; --po-button-success-hover: #567236;`) per 45-PATTERNS.md — primary/recovery derive from already-overridden parapet vars and destructive is theme-invariant, so they need no dark override. After the `.po-button-warning:hover` rule (before `.parapet-theme-option`), insert the new rules from 45-PATTERNS.md verbatim: `.po-button-primary` (+hover), `.po-button-recovery` (+hover), `.po-button-destructive` (+hover), `.po-button-success` (+hover), `.po-guidance`, `.po-timeline-badge-operator`, `.po-timeline-badge-copilot`, `.po-timeline-badge-external`, `.po-queue-row-selected` (+hover) — every selector prefixed with `.parapet-ui`. Replace line 378 `color: #042f2e;` with `color: var(--po-nav-active-fg);`. Apply EVERY one of these edits IDENTICALLY to the demo mirror examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex in this same task. Do not change any existing CSS variable VALUE or any `.po-chip*` rule.
  </action>
  <verify>
    <automated>diff <(grep -oE '\.po-[a-z-]+' priv/templates/parapet.gen.ui/operator_components.ex.eex | sort -u) <(grep -oE '\.po-[a-z-]+' examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex | sort -u) && grep -c 'po-button-success-bg' priv/templates/parapet.gen.ui/operator_components.ex.eex</automated>
  </verify>
  <done>Template and mirror declare an identical set of `.po-*` class names (diff empty); `--po-button-success-bg` appears ≥3 times in the template (light + both dark blocks); `grep -c '#042f2e' priv/templates/parapet.gen.ui/operator_components.ex.eex` returns 0 in both files; no existing variable value or `.po-chip*` rule changed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite control_base, control_class, timeline badge, and queue-row functions (template + mirror)</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</files>
  <read_first>
    operator_components.ex.eex lines 1256-1290 (control_class/2 + control_base/0), 1474-1478 (timeline_entry_badge_class/1), 1332-1338 (queue_row_class/2).
    45-PATTERNS.md "control_base() function replacement", "control_class/2 function replacements", "timeline_entry_badge_class/1 function replacement", "queue_row_class/2 function replacement" — exact before/after bodies.
    45-UI-SPEC.md "Button Variants" table — the variant→token mapping and the disabled contract.
  </read_first>
  <action>
    Rewrite `control_base/0` to `"flex min-h-[40px] items-center justify-center rounded-lg px-4 py-2 text-sm font-medium transition-transform duration-[--motion-fast] ease-out active:scale-[0.96] focus:outline-none focus:ring-2 focus:ring-offset-2 po-focus"` (swap duration-100→duration-[--motion-fast]; add focus:ring-offset-2 and po-focus). Rewrite `control_class(:recovery, width)` to append `" po-button-recovery"` (drop bg-indigo-600/hover/focus utilities). Rewrite `control_class(:success, width)` to append `" po-button-success"` (drop bg-emerald-600/hover/focus). Add a new `control_class(:primary, width)` clause appending `" po-button-primary"` and a new `control_class(:destructive, width)` clause appending `" po-button-destructive"` (place per 45-PATTERNS.md insertion notes). Clean up `control_class(:warning, width)` to drop the trailing `focus:ring-amber-300` (po-focus now lives in control_base()). Rewrite the three `timeline_entry_badge_class/1` clauses (:operator/:copilot/:external) to return `"po-timeline-badge-operator"` / `"po-timeline-badge-copilot"` / `"po-timeline-badge-external"` (leave the :system and catch-all clauses unchanged). Rewrite the selected branch of `queue_row_class/2` to return `"po-queue-row-selected"` (leave the unselected branch — `border-l-transparent bg-stone-50/40 hover:bg-stone-100` — unchanged; it is intercepted neutral stone, not a COMP-08 violation). Apply EVERY edit IDENTICALLY to the demo mirror in this same task.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_contrast_test.exs 2>&1 | tail -5; grep -c 'transition-all' priv/templates/parapet.gen.ui/operator_components.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</automated>
  </verify>
  <done>control_base() emits duration-[--motion-fast] + focus:ring-offset-2 + po-focus; :primary/:recovery/:destructive/:success/:warning variants emit their `.po-button-*` classes; timeline badge + queue-row functions emit `.po-*` classes; `grep -c 'transition-all'` returns 0 for both files; the contrast test's function-level refutes (bg-indigo-700/bg-violet-700/bg-slate-700, duration token, po-focus) pass — remaining failures are only the inline-markup utilities Plan 03 fixes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → host app | The .eex template generates host-owned code. This plan edits only static class/style strings and CSS rules; no interpolation of user/incident data is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-02 | Tampering | New `.po-*` CSS rules + class-returning functions | low | mitigate | All new class/style strings are static literals; no user/incident data is interpolated into class or style attributes. Functions return constant strings. HEEx auto-escapes any surrounding interpolation. |
| T-45-03 | Information Disclosure | Demo mirror divergence | low | mitigate | Byte-mirror duplication enforced as an acceptance criterion (diff gate in verify); prevents the demo surface drifting from generated output. |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (45-RESEARCH.md audit: N/A). |

This plan changes only presentation/markup/CSS of generated operator-UI templates. No data flow, auth, or user-input handling change. No `high`-severity threats.
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_contrast_test.exs` — the new button contrast pairs pass; function-level refutes (timeline/queue/#042f2e/duration/po-focus) pass. The only remaining red assertions are the inline-markup utilities Plan 03 removes (preview_panel indigo, suspect_changes purple/violet, runbook blue-50, standalone duration-100 buttons).
- Byte-mirror diff (Task 1 verify) is empty.
- `grep -c '#042f2e' priv/templates/parapet.gen.ui/operator_components.ex.eex examples/.../operator_components.ex` returns 0.
</verification>

<success_criteria>
- All nine new `.po-*` CSS rules + their variables exist in template and mirror, byte-identical.
- control_base() carries the motion token + offset + po-focus; the five button variants emit semantic classes.
- timeline badges and the selected queue row emit semantic classes.
- The dark theme-switcher selected color is tokenized (no #042f2e).
- No existing CSS variable value or `.po-chip*` rule was modified.
</success_criteria>

<output>
Create `.planning/phases/45-primitive-components/45-02-SUMMARY.md` when done. List every new CSS class + variable added, every function rewritten, confirm the byte-mirror diff is empty, and note which contrast-test assertions are now green vs. still red (the inline-markup ones for Plan 03).
</output>
