---
phase: 32-ci-supply-chain-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release-please.yml
autonomous: true
requirements:
  - MAT-01
  - MAT-02
must_haves:
  truths:
    - CI jobs execute against multiple OTP versions automatically
    - Cache hits are isolated per Elixir/OTP version to avoid thrashing
    - Actions are securely pinned to exact commit SHAs
  artifacts:
    - path: .github/workflows/ci.yml
      provides: Matrix definitions and pinned actions
      contains: strategy.matrix.otp
    - path: .github/workflows/release-please.yml
      provides: Pinned actions
      contains: 5c625bfb5d1ff62eadeeb3772007f7f66fdcf071
  key_links:
    - from: .github/workflows/ci.yml
      to: actions/cache
      via: matrix-aware cache key
      pattern: \$\{\{ matrix.otp \}\}
---

<objective>
Harden the CI and release pipelines by introducing a comprehensive Elixir/OTP matrix and mitigating supply-chain risks through strict SHA-pinning of all GitHub Actions.

Purpose: Ensure robust backwards compatibility across supported environments while preventing vulnerabilities related to mutable action tags.
Output: Hardened `.github/workflows/ci.yml` and `.github/workflows/release-please.yml`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/ci.yml
@.github/workflows/release-please.yml
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions | Execution context for CI pulling third-party code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | GitHub Actions | mitigate | Pin all action usages to explicit 40-character SHAs, preventing tag hijacking. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: CI Pipeline Matrix and SHA Pinning</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Per D-01, update `lint`, `test`, and `demo` jobs to use a strategy matrix:
    ```yaml
    strategy:
      matrix:
        elixir: ['1.19.0']
        otp: ['26.x', '27.x', '28.x']
    ```
    Update `erlef/setup-beam` `with:` inputs to use `${{ matrix.elixir }}` and `${{ matrix.otp }}`.
    Update `actions/cache` `key:` and `restore-keys:` inputs to dynamically incorporate the matrix variables (e.g., `-mix-${{ matrix.elixir }}-${{ matrix.otp }}-`) to avoid cache thrashing.
    
    Per D-02, replace all floating version tags (`@v4`, `@v1`) with full commit SHAs:
    - `actions/checkout@v4` -> `@34e114876b0b11c390a56381ad16ebd13914f8d5`
    - `erlef/setup-beam@v1` -> `@fc68ffb90438ef2936bbb3251622353b3dcb2f93`
    - `actions/cache@v4` -> `@0057852bfaa89a56745cba8c7296529d2fc39830`

    Keep `release_gate` unchanged as it aggregates the matrix jobs.
  </action>
  <verify>
    <automated>grep -q 'matrix.otp' .github/workflows/ci.yml && grep -q '34e114876b0b11c390a56381ad16ebd13914f8d5' .github/workflows/ci.yml</automated>
  </verify>
  <done>CI jobs use the defined matrix, cache keys are matrix-aware, and all actions are SHA-pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Release Pipeline SHA Pinning</name>
  <files>.github/workflows/release-please.yml</files>
  <action>
    Per D-02, replace all floating version tags (`@v4`, `@v1`) with full commit SHAs:
    - `actions/checkout@v4` -> `@34e114876b0b11c390a56381ad16ebd13914f8d5`
    - `erlef/setup-beam@v1` -> `@fc68ffb90438ef2936bbb3251622353b3dcb2f93`
    - `googleapis/release-please-action@v4` -> `@5c625bfb5d1ff62eadeeb3772007f7f66fdcf071`
  </action>
  <verify>
    <automated>grep -q '5c625bfb5d1ff62eadeeb3772007f7f66fdcf071' .github/workflows/release-please.yml</automated>
  </verify>
  <done>All actions in release-please.yml are SHA-pinned.</done>
</task>

</tasks>

<verification>
Ensure both YAML files parse correctly and contain no floating action tags.
</verification>

<success_criteria>
- CI workflow defines a matrix for OTP versions 26, 27, and 28.
- Cache thrashing is prevented via dynamic cache keys.
- All external GitHub Actions use explicit commit SHAs.
</success_criteria>

<output>
Create `.planning/phases/32-ci-supply-chain-hardening/32-01-SUMMARY.md` when done
</output>
