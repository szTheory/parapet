---
phase: 32-ci-supply-chain-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/dependabot.yml
  - docs/branch-protection.md
autonomous: true
requirements:
  - MAT-03
  - MAT-04
must_haves:
  truths:
    - Dependabot is configured to monitor mix and github-actions
    - Branch protection enforcement is explicitly documented
  artifacts:
    - path: .github/dependabot.yml
      provides: Dependency update configuration
      contains: package-ecosystem
    - path: docs/branch-protection.md
      provides: Instructions to enforce branch protection
      contains: gh api
  key_links:
    - from: .github/dependabot.yml
      to: mix
      via: package-ecosystem
      pattern: mix
---

<objective>
Configure automated dependency updates via Dependabot and document the manual/CLI steps required to enforce branch protection rules.

Purpose: Prevent supply-chain vulnerabilities by keeping packages up-to-date, and ensure branch protection is properly configured so unauthorized code cannot bypass CI.
Output: `.github/dependabot.yml` and `docs/branch-protection.md` created.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository Configuration | Settings and workflows governing code lifecycle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02 | Elevation of Privilege | Branch Protection | mitigate | Document exact steps to require `release_gate` CI status checks for main. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Configure Dependabot</name>
  <files>.github/dependabot.yml</files>
  <action>
    Per D-03, create a Dependabot configuration file.
    Include entries for `package-ecosystem: mix` with `directory: "/"` and `package-ecosystem: github-actions` with `directory: "/"`.
    Set the `schedule.interval` to `weekly` to avoid notification noise.
  </action>
  <verify>
    <automated>grep -q 'package-ecosystem: mix' .github/dependabot.yml && grep -q 'package-ecosystem: github-actions' .github/dependabot.yml</automated>
  </verify>
  <done>Dependabot configuration is present for Mix and GitHub Actions.</done>
</task>

<task type="auto">
  <name>Task 2: Document Branch Protection Rules</name>
  <files>docs/branch-protection.md</files>
  <action>
    Per D-04, create `docs/branch-protection.md` documenting the required branch protection rules for the `main` branch.
    Specifically document that the `release_gate` status check must be enforced.
    Provide the exact `gh api` command or the step-by-step GitHub UI instructions to apply these protections.
  </action>
  <verify>
    <automated>grep -q 'release_gate' docs/branch-protection.md</automated>
  </verify>
  <done>Documentation explicitly enforcing the release_gate job exists.</done>
</task>

</tasks>

<verification>
Ensure Dependabot schema is valid and the documentation clearly outlines enforcement.
</verification>

<success_criteria>
- `.github/dependabot.yml` is created and correctly configured.
- `docs/branch-protection.md` clearly outlines how to enforce protection via the CLI or UI.
</success_criteria>

<output>
Create `.planning/phases/32-ci-supply-chain-hardening/32-02-SUMMARY.md` when done
</output>
