---
phase: 37-archive-durability
plan: 03
type: execute
wave: 3
depends_on: ["37-01", "37-02"]
files_modified:
  - lib/parapet/evidence/archiver.ex
  - docs/stability.md
  - CHANGELOG.md
  - test/parapet/evidence/archiver_test.exs
  - test/mix/tasks/parapet.archive_test.exs
  - test/parapet/evidence/archive_worker_test.exs
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04]
requirements_addressed: [ARCH-01, ARCH-02, ARCH-03, ARCH-04]

must_haves:
  truths:
    - "D-03: archive is operational evidence export/prune, not host backup/restore"
    - "D-04: Phase 37 does not add a DB archive manifest/job table"
    - "D-06: external provider rows, telemetry samples, Prometheus data, Grafana state, and object-store contents remain out of bundle scope"
    - "D-11: logs/telemetry are optional supplements, not the truth source"
    - "D-14: docs state retention means resolved incidents created before cutoff, not resolved before cutoff"
    - "D-16: no new runtime dependencies or install contents are added"
  artifacts:
    - path: "lib/parapet/evidence/archiver.ex"
      provides: "ExDoc-facing archive contract and retention limitation"
      contains: "created before the cutoff"
    - path: "CHANGELOG.md"
      provides: "Experimental API change notice"
      contains: "Archiver"
    - path: "docs/stability.md"
      provides: "stability table remains accurate after nested struct additions"
      contains: "Parapet.Evidence.Archiver"
  key_links:
    - from: "Experimental API policy"
      to: "CHANGELOG.md"
      via: "single release note for changed return shape"
      pattern: "archive"
    - from: "retention semantics"
      to: "Archiver moduledoc"
      via: "created-before-cutoff limitation"
      pattern: "inserted_at"
---

<objective>
Close the Phase 37 compatibility and proof loop after the core archiver and caller surfaces are implemented: document the Experimental API change, keep stability references accurate, state the retention limitation in ExDoc-facing copy, and run the focused and broad verification commands.

Purpose: The phase changes an Experimental public module return shape and hardens an operator trust boundary. Even though Phase 39 owns adoption docs, this phase must leave the runtime contract, semver note, and verification evidence coherent.

Output: Archive contract docs/changelog are aligned with the implemented result shape, no install/dependency/schema scope has expanded, and the phase has final verification commands attached to the execution summary.
</objective>

<context>
@.planning/phases/37-archive-durability/37-CONTEXT.md
@.planning/phases/37-archive-durability/37-RESEARCH.md
@.planning/phases/37-archive-durability/37-PATTERNS.md
@.planning/phases/37-archive-durability/37-VALIDATION.md

<interfaces>
- `docs/stability.md` classifies `Parapet.Evidence.Archiver` and `Parapet.Evidence.ArchiveWorker` as Experimental.
- Experimental modules may change in a minor release with a single CHANGELOG entry.
- `mix.exs` runtime dependencies must remain unchanged for Phase 37.
- Phase 39 owns broad adoption docs; this plan only handles the runtime contract and release-note minimum.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ExDoc-facing archive contract copy and changelog note</name>
  <files>lib/parapet/evidence/archiver.ex, docs/stability.md, CHANGELOG.md</files>
  <read_first>
    - lib/parapet/evidence/archiver.ex
    - docs/stability.md
    - CHANGELOG.md
    - .planning/phases/37-archive-durability/37-CONTEXT.md
  </read_first>
  <action>
Update `Parapet.Evidence.Archiver` moduledoc or `archive/3` docs to describe the new return shape: `{:ok, %Parapet.Evidence.Archiver.Summary{}}` and `{:error, %Parapet.Evidence.Archiver.Failure{}}`. State that the archive is an operational Parapet-owned evidence export/prune path, not a host backup/restore system. State the Phase 37 retention limitation exactly: current retention means resolved incidents created before the cutoff (`inserted_at < cutoff`), not incidents resolved before the cutoff, because the schema has no `resolved_at`.

Update `CHANGELOG.md` with an unreleased or next-release note documenting that the Experimental `Parapet.Evidence.Archiver.archive/3` return shape changed from `{:ok, :ok}` to structured summary/failure tuples and that `mix parapet.archive` now surfaces failure rather than static success. If nested public modules `Summary` and `Failure` were added, ensure `docs/stability.md` remains accurate; either keep them covered under the Experimental `Parapet.Evidence.Archiver` row or add explicit Experimental rows if the docs gate requires it.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/parapet/evidence/archiver.ex` mentions `Summary` and `Failure`
    - `lib/parapet/evidence/archiver.ex` states `inserted_at < cutoff` or equivalent created-before-cutoff retention limitation
    - `CHANGELOG.md` mentions the Experimental archive return-shape change
    - `docs/stability.md` still includes `Parapet.Evidence.Archiver` and `Parapet.Evidence.ArchiveWorker` as Experimental surfaces
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>Maintainers and ExDoc readers can see the new Experimental archive result contract and retention limitation.</done>
</task>

<task type="auto">
  <name>Task 2: Run final focused and repository-level verification for archive durability</name>
  <files>test/parapet/evidence/archiver_test.exs, test/mix/tasks/parapet.archive_test.exs, test/parapet/evidence/archive_worker_test.exs</files>
  <read_first>
    - test/parapet/evidence/archiver_test.exs
    - test/mix/tasks/parapet.archive_test.exs
    - test/parapet/evidence/archive_worker_test.exs
    - .planning/phases/37-archive-durability/37-VALIDATION.md
  </read_first>
  <action>
Run the archive-focused test files together, then run repository-level formatting, compile, and test commands. Fix any failures caused by the Phase 37 changes. Do not broaden the phase into docs/adoption content beyond Task 1. Do not add runtime dependencies, install generator changes, schema migrations, object-store config, or host backup ownership.
  </action>
  <verify>
    <automated>mix test test/parapet/evidence/archiver_test.exs test/mix/tasks/parapet.archive_test.exs test/parapet/evidence/archive_worker_test.exs && mix format --check-formatted && mix compile --warnings-as-errors && mix test</automated>
  </verify>
  <acceptance_criteria>
    - Focused archive/CLI/worker tests exit 0 together
    - `mix format --check-formatted` exits 0
    - `mix compile --warnings-as-errors` exits 0
    - `mix test` exits 0, or any unrelated pre-existing failure is recorded in the summary with evidence
    - `mix.exs` dependency list is unchanged by Phase 37
    - No migration file or install template is added for archive manifest/job state
  </acceptance_criteria>
  <done>Phase 37 runtime, caller, and compatibility changes have focused and repository-level verification evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Experimental API change -> adopter expectations | Maintainers need a visible release note for changed return shape |
| Runtime archive scope -> host backup ownership | Parapet must not imply it replaces host database/object-store backups |
| Test evidence -> phase completion claim | Final verification must prove the trust boundary, not just compile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-07 | Repudiation | undocumented Experimental return-shape change | mitigate | Add CHANGELOG note and ExDoc-facing contract text. |
| T-37-08 | Elevation of scope | archive represented as backup system | mitigate | Explicitly document export/prune boundary and avoid dependency/schema/install expansion. |
| T-37-09 | Tampering | verification gap after broad changes | mitigate | Run focused archive tests plus format, compile, and full test suite. |
</threat_model>

<verification>
- `mix test test/parapet/evidence/archiver_test.exs test/mix/tasks/parapet.archive_test.exs test/parapet/evidence/archive_worker_test.exs`
- `mix format --check-formatted`
- `mix compile --warnings-as-errors`
- `mix test`
</verification>

<success_criteria>
- Experimental archive contract is documented and changelogged.
- Retention limitation is explicit.
- No new runtime dependency, install content, schema table, or backup ownership is introduced.
- Focused and full verification commands are run or their failures are recorded with evidence.
</success_criteria>

<output>
Create `.planning/phases/37-archive-durability/37-03-SUMMARY.md` when done.
</output>

