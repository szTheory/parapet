---
phase: 21-runnable-demo-app
plan: 04
type: execute
wave: 4
depends_on: ["21-03"]
files_modified:
  - .github/workflows/ci.yml
  - docs/getting-started.md
autonomous: false
requirements: [DEMO-03, DEMO-04]

must_haves:
  truths:
    - "ci.yml has a demo job that runs the smoke test against an explicit postgres service"
    - "ci.yml has a release_gate job with needs test+demo and no continue-on-error anywhere"
    - "mix hex.build --dry-run confirms examples/ is absent from the published package"
    - "getting-started.md Next steps links the runnable demo app"
    - "release_gate is configured as a required status check in GitHub branch protection"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "demo + release_gate CI jobs"
      contains: "release_gate"
    - path: "docs/getting-started.md"
      provides: "Next steps link to the demo app"
      contains: "examples/demo_app"
  key_links:
    - from: ".github/workflows/ci.yml release_gate"
      to: "test + demo jobs"
      via: "needs test+demo"
      pattern: "needs:.*demo"
    - from: ".github/workflows/ci.yml demo"
      to: "examples/demo_app smoke test"
      via: "mix test --only smoke"
      pattern: "mix test --only smoke"
---

<objective>
Wire the demo into CI as a REQUIRED gate and finish the DEMO-04 packaging/docs work: add a
`demo` job (with an explicit postgres service) that creates/migrates/seeds the demo DB and
runs the smoke test, add a `release_gate` fan-in job (needs test + demo, no
`continue-on-error`), verify `examples/` is excluded from the Hex package, add the
"Next steps" link in getting-started.md, and capture the manual GitHub branch-protection
step as a checkpoint.

Purpose: DEMO-03 requires the smoke test to run as a required CI gate that goes red if the
Operator UI stops returning 200. DEMO-04 requires the demo to be absent from the published
Hex package and linked from the getting-started guide. The branch-protection requirement is
a GitHub Settings action that cannot be done in the workflow file — it needs a human.

Output: Updated ci.yml with two new jobs; updated getting-started.md; verified Hex exclusion;
branch protection confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-runnable-demo-app/21-CONTEXT.md
@.planning/phases/21-runnable-demo-app/21-RESEARCH.md
@.planning/phases/21-runnable-demo-app/21-PATTERNS.md
@.planning/phases/21-runnable-demo-app/21-03-SUMMARY.md

<interfaces>
<!-- Verified against the repo. -->

Existing .github/workflows/ci.yml (verified) has ONE job, `test:`, on ubuntu-latest with
Elixir 1.19.0 / OTP 27.2, deps + _build cache, then format/credo/dialyzer/test/verify steps.
The `test` job relies on the runner's pre-installed Postgres (no `services:` block). The new
`demo` job runs in a clean context and therefore NEEDS its own `services: postgres:` block.

New `demo` job (RESEARCH.md lines 906-950 / PATTERNS.md lines 590-637), appended after test:
  - runs-on ubuntu-latest, needs [test], env MIX_ENV: test
  - services.postgres: image postgres:16-alpine, env POSTGRES_USER/PASSWORD/DB =
    postgres/postgres/demo_app_test, ports ['5432:5432'], health-cmd pg_isready (interval 10s,
    timeout 5s, retries 5)
  - steps: checkout@v4; erlef/setup-beam@v1 (elixir 1.19.0 / otp 27.2); cache demo deps
    (path examples/demo_app/deps, key hashFiles('examples/demo_app/mix.lock')); cache demo
    _build (path examples/demo_app/_build); cd examples/demo_app && mix deps.get;
    cd examples/demo_app && mix ecto.create && mix ecto.migrate;
    cd examples/demo_app && mix run priv/repo/seeds.exs;
    cd examples/demo_app && mix test --only smoke
  Use `examples/demo_app/mix.lock` (NOT `**/mix.lock`) in cache keys to avoid colliding with
  the root deps cache.

New `release_gate` job (RESEARCH.md lines 957-962 / Pitfall 5 lines 830-840), appended after
demo:
  release_gate:
    needs: [test, demo]
    runs-on: ubuntu-latest
    steps:
      - run: echo "All required CI checks passed"
  Pitfall 5: a job with only `needs:` is invalid — it MUST have `runs-on:` and at least one
  step. NEVER set `continue-on-error` on `demo` or `release_gate` (D-07 / DEMO-03).

Hex exclusion (DEMO-04 / D-10): root mix.exs `files:` whitelist (verified, line 42) is
`~w(lib priv .formatter.exs mix.exs README* CHANGELOG* CONTRIBUTING* SECURITY*
CODE_OF_CONDUCT* LICENSE* docs)`. `examples/` is ABSENT → already excluded. No mix.exs change
needed; verify via `mix hex.build --dry-run` from the parapet root and confirm no path under
`examples/` is listed.

getting-started.md "Next steps" (verified, lines 94-98) currently ends with the Sigra
integration bullet. Append the demo bullet (D-11 exact text, PATTERNS.md lines 669-672):
  - [Runnable Demo App](https://github.com/szTheory/parapet/tree/main/examples/demo_app) — explore a live, seeded Parapet setup end-to-end: incidents, timeline entries, runbook steps, and the Operator UI populated and ready to browse

Branch protection (RESEARCH.md lines 965-971 / PATTERNS.md line 762): `release_gate` must be
added as a REQUIRED status check in GitHub branch protection settings for `main`. This is not
expressible in the workflow file — it is a GitHub Settings (or `gh api`) action. Try the
`gh api` route first; if it fails (permissions/auth), fall back to a human checkpoint.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the demo + release_gate CI jobs, verify Hex exclusion, add getting-started link</name>
  <files>.github/workflows/ci.yml, docs/getting-started.md</files>
  <read_first>
    - .github/workflows/ci.yml (the existing single test job — append the two new jobs)
    - docs/getting-started.md lines 94-98 (the Next steps section to append to)
    - mix.exs line 42 (the files: whitelist — confirm examples/ absent; no change)
    - examples/demo_app/test/demo_app/operator_smoke_test.exs (confirms `mix test --only smoke` is the command — produced in Plan 03)
    - .planning/phases/21-runnable-demo-app/21-RESEARCH.md (Exact CI Changes lines 899-971; Pitfall 5 lines 830-840; Exact getting-started change lines 975-981)
    - .planning/phases/21-runnable-demo-app/21-PATTERNS.md (ci.yml + getting-started self-analog sections lines 545-672)
  </read_first>
  <action>
Edit `.github/workflows/ci.yml`. Keep the existing `test:` job unchanged. Append a `demo:`
job and a `release_gate:` job exactly per the interfaces above.

demo: runs-on ubuntu-latest, needs [test], env MIX_ENV test, a services.postgres block
(image postgres:16-alpine; env POSTGRES_USER postgres, POSTGRES_PASSWORD postgres,
POSTGRES_DB demo_app_test; ports ['5432:5432']; options with --health-cmd pg_isready
--health-interval 10s --health-timeout 5s --health-retries 5). Steps in order: checkout@v4;
erlef/setup-beam@v1 with elixir 1.19.0 / otp 27.2; cache@v4 for examples/demo_app/deps keyed
on hashFiles('examples/demo_app/mix.lock'); cache@v4 for examples/demo_app/_build; a step
running `cd examples/demo_app && mix deps.get`; a step running
`cd examples/demo_app && mix ecto.create && mix ecto.migrate`; a step running
`cd examples/demo_app && mix run priv/repo/seeds.exs`; a step running
`cd examples/demo_app && mix test --only smoke`.

release_gate: needs [test, demo], runs-on ubuntu-latest, one step running
`echo "All required CI checks passed"` (Pitfall 5: a job needs runs-on + at least one step).

NEVER add `continue-on-error` to either job (D-07 / DEMO-03). Do NOT add a services block to
the existing test job. After editing, validate the YAML parses with
`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`.

Verify DEMO-04 Hex exclusion: run `mix hex.build --dry-run` from the parapet ROOT and confirm
NO file path beginning with `examples/` appears in the included-files output. Do NOT modify
the files: whitelist — `examples/` is already excluded by omission (D-10). Record the dry-run
result in the SUMMARY. If `mix hex.build --dry-run` needs network/auth and fails in the
sandbox, note it and defer that confirmation to the human checkpoint in Task 2.

Edit `docs/getting-started.md`: append the demo bullet to the END of the "Next steps" list
(after the Sigra bullet at line 98), using the EXACT D-11 text from the interfaces block.
  </action>
  <verify>
    <automated>grep -q 'release_gate' .github/workflows/ci.yml && grep -q 'mix test --only smoke' .github/workflows/ci.yml && grep -q 'postgres:16-alpine' .github/workflows/ci.yml && grep -Eq 'needs:.*demo' .github/workflows/ci.yml && ! grep -q 'continue-on-error' .github/workflows/ci.yml && grep -q 'examples/demo_app' docs/getting-started.md && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo CI_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a `demo:` job with `needs: [test]` and `mix test --only smoke`
    - `.github/workflows/ci.yml` contains `image: postgres:16-alpine` under the demo job services block
    - `.github/workflows/ci.yml` contains a `release_gate:` job whose `needs` lists both `test` and `demo`
    - `.github/workflows/ci.yml` contains NO `continue-on-error` anywhere
    - `.github/workflows/ci.yml` cache keys use `examples/demo_app/mix.lock` (not `**/mix.lock`)
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
    - `docs/getting-started.md` "Next steps" contains the `[Runnable Demo App](...examples/demo_app)` bullet
    - The SUMMARY records the `mix hex.build --dry-run` result showing no `examples/` path (or notes deferral to the checkpoint)
  </acceptance_criteria>
  <done>ci.yml has demo + release_gate jobs (postgres service, smoke test, fan-in gate, no continue-on-error, YAML valid); getting-started.md links the demo; Hex dry-run confirms examples/ is excluded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Configure release_gate as a required status check + confirm live demo</name>
  <files>(GitHub branch protection settings for main — not a tracked repo file)</files>
  <action>
Before pausing, Claude attempts the branch-protection wiring via `gh api` (how-to-verify step
1). If `gh` is authenticated and the PUT succeeds, confirm `release_gate` is listed in the
required checks and note it in the SUMMARY. If `gh` lacks permissions or the workflow has not
yet run on a PR (so the check name is not yet selectable), pause for the human to configure it
in GitHub Settings → Branches. This task also has the human confirm the Hex dry-run shows no
`examples/` path and visually verify the live, seeded Operator UI at /parapet.
  </action>
  <what-built>
    Claude added the `demo` and `release_gate` CI jobs to .github/workflows/ci.yml, linked
    the demo app from docs/getting-started.md "Next steps", and ran `mix hex.build --dry-run`
    to confirm `examples/` is excluded from the Hex package. The full demo app
    (examples/demo_app/) compiles, migrates, seeds, and its smoke test passes locally.
  </what-built>
  <how-to-verify>
1. REQUIRED STATUS CHECK (DEMO-03 — cannot be automated via the workflow file): In GitHub,
   go to Settings → Branches → branch protection rule for `main` → "Require status checks to
   pass before merging", and add `release_gate` to the required checks list. (The check name
   appears after the workflow has run at least once on a PR.) Alternatively run:
   `gh api -X PUT repos/szTheory/parapet/branches/main/protection/required_status_checks -f 'checks[][context]=release_gate'`
   then confirm via:
   `gh api repos/szTheory/parapet/branches/main/protection/required_status_checks --jq '.checks'`
   and verify `release_gate` is listed.
2. HEX EXCLUSION (DEMO-04): From the parapet root run `mix hex.build --dry-run` and confirm
   no path under `examples/` appears in the "Files" list.
3. LIVE DEMO (DEMO-01 / DEMO-02 — manual run): `cd examples/demo_app && mix setup && mix phx.server`,
   then open http://localhost:4000/parapet. Confirm the Operator UI loads and shows seeded
   incidents in open/investigating/resolved states, timeline entries, a tool audit, and a
   runbook step with a `warning:` callout. (Styling requires the Tailwind build; run
   `mix assets.build` if the page renders unstyled.)
  </how-to-verify>
  <verify>
    <human-check>release_gate is a required status check on main; mix hex.build --dry-run shows no examples/ path; live /parapet shows seeded incidents across all three states with a runbook warning</human-check>
  </verify>
  <resume-signal>
Type "approved" once `release_gate` is a required status check on `main`, `mix hex.build
--dry-run` shows no `examples/` paths, and the live /parapet page shows the seeded incidents.
Otherwise describe what is missing.
  </resume-signal>
  <done>release_gate is enforced as a required status check on main; Hex package confirmed free of examples/; the live seeded Operator UI is confirmed at /parapet by the human.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → demo Postgres | demo job connects to its own throwaway postgres service |
| Hex publish surface | files included in the published package |
| GitHub branch protection | the gate that prevents merging broken Operator UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Repudiation/Bypass | CI gate bypass | mitigate | `release_gate` uses `needs: [test, demo]` with NO `continue-on-error`, so it fails if either parent fails; making it a REQUIRED status check in branch protection (Task 2) prevents merging a red Operator UI (DEMO-03). The grep gate forbids any `continue-on-error` token in ci.yml. |
| T-21-11 | Information Disclosure | accidental Hex publish of demo | mitigate | The `files:` whitelist omits `examples/`; Task 1 verifies via `mix hex.build --dry-run` that no `examples/` path is packaged (DEMO-04). |
| T-21-12 | Tampering | CI postgres credentials | accept | Throwaway service-container creds (postgres/postgres) scoped to the ephemeral CI job; no production data. |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (CI config + docs only). No [ASSUMED]/[SUS] packages → no blocking human checkpoint beyond the branch-protection step. |
</threat_model>

<verification>
- ci.yml parses as valid YAML and contains demo + release_gate jobs with no continue-on-error.
- `mix hex.build --dry-run` from root shows no `examples/` path (DEMO-04).
- getting-started.md links the demo (DEMO-04).
- `release_gate` is a required status check on `main` (DEMO-03 — confirmed at the checkpoint).
- Live /parapet shows seeded incidents across all three states with a runbook warning (DEMO-01/02 — confirmed at the checkpoint).
</verification>

<success_criteria>
- The `demo` smoke test runs in CI behind an explicit postgres service and feeds `release_gate`.
- `release_gate` is a required branch-protection check (no continue-on-error anywhere).
- The demo is verified absent from the Hex package and linked from getting-started.md.
- The live, seeded Operator UI is confirmed at /parapet by the human.
</success_criteria>

<output>
Create `.planning/phases/21-runnable-demo-app/21-04-SUMMARY.md` when done.
</output>
