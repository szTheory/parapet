---
phase: 57-test-suite-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/parapet/docs_phase_33_test.exs
  - test/parapet/telemetry/recovery_action_test.exs
  - test/parapet/metrics/exemplar_telemetry_test.exs
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "TEST-01/D-01: docs_phase_33_test.exs no longer asserts the stale 'make up-auto' string; that test passes"
    - "TEST-02/D-03: recovery_action_test.exs no longer flakes under async — the atom_count delta block is gone"
    - "TEST-02/D-04: the CR-01 String.to_existing_atom(poison)-raises guard at line 130 and its sibling at line 173 are RETAINED byte-for-byte"
    - "TEST-03/D-05: the three Process.sleep(10) waits in exemplar_telemetry_test.exs are removed; the following assertions pass unchanged"
    - "All three edited test files compile and their tests pass under a bare default-env `mix test`"
  artifacts:
    - test/parapet/docs_phase_33_test.exs
    - test/parapet/telemetry/recovery_action_test.exs
    - test/parapet/metrics/exemplar_telemetry_test.exs
  key_links:
    - "recovery_action_test.exs line 130 guard MUST survive — deleting it would silently drop the CR-01 atom-table-exhaustion regression proof"
---

<objective>
Fix the two pre-existing red tests directly (not by quarantine) and delete the three
spurious telemetry sleeps, closing TEST-01, TEST-02, and TEST-03. These are three
independent, small-surface edits to three separate test files: two are pure deletions
of dead/flaky assertions, one is deletion of dead-time sleeps. No new symbols, no logic.

Purpose: A bare `mix test` cannot be honestly green while `DocsPhase33Test` asserts a
string that does not exist and `RecoveryActionTest` intermittently flakes on an
async-sensitive atom-count delta. This plan removes both reds and the three dead-time
`Process.sleep(10)` waits, so the "CI is the enforcement backstop" claim rests on a real
green suite.

Output: Three edited test files, all passing under default-env `mix test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-test-suite-baseline/57-CONTEXT.md
@.planning/phases/57-test-suite-baseline/57-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces NO new symbols — it is deletion-only:
- Removes `test/parapet/docs_phase_33_test.exs` line 102 (stale `"make up-auto"` assertion)
- Removes `test/parapet/telemetry/recovery_action_test.exs` lines 132–153 (atom_count delta block)
- Removes three `Process.sleep(10)` lines from `test/parapet/metrics/exemplar_telemetry_test.exs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete stale doc-drift assertion and the atom-count flake block (TEST-01, TEST-02)</name>
  <files>test/parapet/docs_phase_33_test.exs, test/parapet/telemetry/recovery_action_test.exs</files>
  <read_first>
    - test/parapet/docs_phase_33_test.exs lines 100–105 (the `make up` / `make up-auto` / `make urls` assertion cluster)
    - test/parapet/telemetry/recovery_action_test.exs lines 125–155 (the retained guard at 130, the delete block at 132–153, the closing `end` at 154)
    - 57-CONTEXT.md D-01, D-02 (TEST-01) and D-03, D-04 (TEST-02)
    - 57-PATTERNS.md "docs_phase_33_test.exs" and "recovery_action_test.exs" sections
  </read_first>
  <action>
    Edit `test/parapet/docs_phase_33_test.exs`: delete exactly the single line `assert readme =~ "make up-auto"` (currently line 102). Per D-01/D-02: do NOT rewrite it to `"make up"` — line 101 already asserts `readme =~ "make up"`, so a rewrite would produce a redundant duplicate. Do NOT touch line 101 (`"make up"`), line 103 (`"make urls"`), line 104 (`curl -f ...`), or line 105 (`"make down"`). This is the only stale-string red; do not broaden the edit to any other `make up-*` assertion — the others reference targets that still exist.

    Edit `test/parapet/telemetry/recovery_action_test.exs`: delete exactly the atom-count delta block spanning lines 132–153. That block begins with the comment `# Warm-up: run one rejection pass ...` (line 132) and ends with the closing string `"the closed-vocabulary lookup."` (line 153) — it comprises the warm-up comment (132–134), `warmup_poison = ...` (135), the blank line (136), the warm-up `assert_raise` (137–139), the blank line (140), `poison_2 = ...` (141), `before_count = :erlang.system_info(:atom_count)` (142), the blank line (143), the second `assert_raise` (144–146), the blank line (147), `after_count = :erlang.system_info(:atom_count)` (148), the blank line (149), and the `assert after_count == before_count, ...` guard with its multi-line message (150–153). After deletion, the test body ends with the retained guard at line 130 followed by the function's closing `end` (currently line 154).

    RETAIN per D-04, byte-for-byte, do NOT touch: line 130 `assert_raise ArgumentError, fn -> String.to_existing_atom(poison) end` (the CR-01 atom-table-exhaustion regression guard) and its independent sibling at line 173 inside the second test. These are load-bearing security regression guards, not concurrency-sensitive — removing either would silently drop the proof that poison strings are never interned as atoms.

    Remove the now-orphaned blank line 131 between the retained guard (130) and the deleted block if it leaves a double blank; leave a single blank line before the function's closing `end` to match the file's existing style.
  </action>
  <verify>
    <automated>grep -rn 'make up-auto' test/ ; test $(grep -rc 'make up-auto' test/ | grep -v ':0$' | wc -l | tr -d ' ') -eq 0 && grep -c 'String.to_existing_atom(poison)' test/parapet/telemetry/recovery_action_test.exs | grep -qx 2 && grep -c ':erlang.system_info(:atom_count)' test/parapet/telemetry/recovery_action_test.exs | grep -qx 0 && mix test test/parapet/docs_phase_33_test.exs test/parapet/telemetry/recovery_action_test.exs --color 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - D-01: `grep -rn 'make up-auto' test/` returns zero hits (the only repo-wide occurrence was that assertion line).
    - D-04: `grep -c 'String.to_existing_atom(poison)' test/parapet/telemetry/recovery_action_test.exs` equals exactly 2 (line 130 + line 173 both survive).
    - D-03: `grep -c ':erlang.system_info(:atom_count)' test/parapet/telemetry/recovery_action_test.exs` equals 0 (both reads deleted).
    - `mix test test/parapet/docs_phase_33_test.exs test/parapet/telemetry/recovery_action_test.exs` exits 0 with zero failures.
  </acceptance_criteria>
  <done>Both red tests pass; the stale assertion and the atom-count delta block are gone; the two CR-01 guards remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Delete three spurious telemetry sleeps (TEST-03)</name>
  <files>test/parapet/metrics/exemplar_telemetry_test.exs</files>
  <read_first>
    - test/parapet/metrics/exemplar_telemetry_test.exs lines 18–62 (the three `:telemetry.execute/3` + `Process.sleep(10)` + `assert` clusters at ~23, ~41, ~59)
    - 57-CONTEXT.md D-05
    - 57-PATTERNS.md "exemplar_telemetry_test.exs — delete 3 spurious sleeps" section
  </read_first>
  <action>
    In `test/parapet/metrics/exemplar_telemetry_test.exs`, delete all three `Process.sleep(10)` call sites (currently at lines 23, 41, 59) together with each one's misleading preceding comment `# Allow async telemetry handlers to run`. Per D-05: `:telemetry.execute/3` dispatches synchronously, so `ExemplarStore` is already populated when `execute` returns — the sleeps are dead time and the comment is factually wrong. No barrier or helper replaces them. Leave every following `assert ExemplarStore.get_trace(...)` line unchanged; each still passes because the store was written synchronously. After each deletion the `:telemetry.execute(...)` call is immediately followed (after at most one blank line) by its `assert`.

    Do NOT add an `INTENTIONAL HOLD:` comment here — these are removals, not holds. After this task there must be zero `Process.sleep` calls remaining in this file.
  </action>
  <verify>
    <automated>grep -c 'Process.sleep' test/parapet/metrics/exemplar_telemetry_test.exs | grep -qx 0 && mix test test/parapet/metrics/exemplar_telemetry_test.exs --color 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - D-05: `grep -c 'Process.sleep' test/parapet/metrics/exemplar_telemetry_test.exs` equals 0.
    - The misleading `# Allow async telemetry handlers to run` comment appears 0 times in the file.
    - `mix test test/parapet/metrics/exemplar_telemetry_test.exs` exits 0 with zero failures (all `get_trace` assertions still pass — dispatch is synchronous).
  </acceptance_criteria>
  <done>All three spurious sleeps and their misleading comments are removed; the file's telemetry assertions still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a test-suite-only change. No `lib/` production code, public API, telemetry
contract, network surface, or untrusted-input handling is touched (v1.8 is DX/pipeline
only, per STATE.md non-negotiable invariants). No new production attack surface is
introduced by this plan.

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits only `test/` files; it crosses no trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-01-01 | Tampering | recovery_action_test.exs CR-01 guards (lines 130, 173) | high | mitigate | The atom-count delta deletion must NOT weaken the CR-01 atom-table-exhaustion regression guard. Task 1's acceptance gate `grep -c 'String.to_existing_atom(poison)' == 2` proves both guards survive verbatim; the verify command fails the task if either is dropped. |
| T-57-01-02 | Repudiation | docs_phase_33_test.exs doc-drift coverage | low | accept | Deleting the stale `make up-auto` assertion removes coverage of a target that does not exist; the surviving `make up` / `make urls` / `make down` assertions still guard real doc drift. Accepted: no real regression is masked (D-02 confirms it is the only stale red). |
| T-57-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan — deletion-only edits to existing test files. No RESEARCH.md package audit required. |
</threat_model>

<verification>
- `grep -rn 'make up-auto' test/` returns zero hits.
- `grep -c 'String.to_existing_atom(poison)' test/parapet/telemetry/recovery_action_test.exs` == 2.
- `grep -c ':erlang.system_info(:atom_count)' test/parapet/telemetry/recovery_action_test.exs` == 0.
- `grep -c 'Process.sleep' test/parapet/metrics/exemplar_telemetry_test.exs` == 0.
- `mix test test/parapet/docs_phase_33_test.exs test/parapet/telemetry/recovery_action_test.exs test/parapet/metrics/exemplar_telemetry_test.exs` exits 0.
</verification>

<success_criteria>
TEST-01, TEST-02, TEST-03 satisfied: the two pre-existing red tests pass directly, the
three spurious telemetry sleeps are removed, and both CR-01 regression guards are retained.
</success_criteria>

<output>
Create `.planning/phases/57-test-suite-baseline/57-01-SUMMARY.md` when done
</output>
