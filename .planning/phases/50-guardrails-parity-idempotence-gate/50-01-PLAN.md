---
phase: 50-guardrails-parity-idempotence-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex
  - brandbook/notes/operator-audit-matrix.md
  - test/support/operator_ui_paths.ex
  - test/parapet/operator_ui_parity_test.exs
  - test/parapet/operator_ui_palette_gate_test.exs
  - test/parapet/operator_ui_motion_test.exs
autonomous: true
requirements: [GUARD-03, GUARD-04, GUARD-05]
must_haves:
  truths:
    - "A fresh `mix parapet.gen.ui` produces operator_components.ex byte-equal (under Code.format_string!) to the committed demo mirror, INCLUDING the D-07/D-08 WCAG-1.4.1 rationale comments (D-03)."
    - "The GUARD-03 parity test fails loudly (named pair + first divergent line + remediation string) if any of the three template↔mirror pairs diverge (D-04/D-05)."
    - "The GUARD-04 hex gate fails if any 6-digit hex literal in the three operator templates is outside the live-parsed tokens.css allowlist ∪ 5 documented exceptions, and fails closed if the tokens.css parse yields < 31 hexes (D-06/D-07/D-09)."
    - "The GUARD-05 motion test asserts the brand easing curve (whitespace/leading-zero tolerant) and that BOTH --motion-fast: 0ms and --motion-base: 0ms are zeroed under prefers-reduced-motion (D-10/D-11)."
  artifacts:
    - "test/parapet/operator_ui_parity_test.exs"
    - "test/parapet/operator_ui_palette_gate_test.exs"
    - "test/parapet/operator_ui_motion_test.exs"
    - "test/support/operator_ui_paths.ex"
    - "brandbook/notes/operator-audit-matrix.md (GUARD-04 exception rationale for #1A5066/#556B77/#8C2E27)"
  key_links:
    - "The D-03 template fix (operator_components.ex.eex) is what makes the GUARD-03 parity test pass for the RIGHT reason — without AST-stripping comments."
    - "GUARD-04 reads its exception set from the audit-matrix declaration; the matrix must document #1A5066/#556B77/#8C2E27 before the gate ships."
---

<objective>
Build the forward-only test apparatus for GUARD-03 (template↔demo byte-parity), GUARD-04 (off-palette-hex gate), and GUARD-05 (motion / reduced-motion), extract the shared `OperatorUIPaths` path helper, and fix the one in-scope source defect (D-03) that GUARD-03 exists to catch.

Purpose: Lock the operator UI built in phases 44–49 against silent regression. The parity test must catch the live generator↔demo comment drift (D-03) for the right reason (template reproduces mirror), not paper over it.

Output: Three new `async: true` test files under `test/parapet/`, one shared support module, a corrected generator template + mirror, and a documented exception rationale in the audit matrix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/50-guardrails-parity-idempotence-gate/50-CONTEXT.md
@.planning/phases/50-guardrails-parity-idempotence-gate/50-PATTERNS.md

# Igniter.Test invocation idiom (GUARD-03, D-01) — read for test_project/igniter/Rewrite.Source.get pattern
@test/mix/tasks/parapet.gen.ui_test.exs
# Existing grep-gate apparatus + path attributes + motion assertions to migrate (GUARD-04/05)
@test/parapet/operator_ui_contrast_test.exs
# Shared-module shape for test/support
@test/support/concurrency_repo.ex
# The palette source of truth (GUARD-04, D-06) — 31 #rrggbb token literals
@brandbook/tokens/tokens.css
# GUARD-04 exception home (D-07) — the file to extend
@brandbook/notes/operator-audit-matrix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the D-03 generator-template comment defect + extract OperatorUIPaths helper</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex, test/support/operator_ui_paths.ex</files>
  <read_first>
    - priv/templates/parapet.gen.ui/operator_components.ex.eex lines ~1428 and ~1442 (the defective `<%#- D-07 … %>` / `<%#- D-08 … %>` build-time comments that render to empty string)
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex lines ~1428 and ~1442 (the mirror — what must be reproduced: `<%# D-07 … %>` / `<%# D-08 … %>`)
    - test/support/concurrency_repo.ex (plain `Parapet.TestSupport.*` module shape, no `use`)
    - test/parapet/operator_ui_contrast_test.exs lines ~4-7 and ~237-245 (the @component_paths / @live_template_paths / @detail_template_paths attribute strings to promote into shared functions)
  </read_first>
  <action>
    Per D-03, fix the generator template `operator_components.ex.eex` so a fresh generation reproduces the mirror's two WCAG-1.4.1 rationale comments. At the two D-07/D-08 comment sites (~:1428, ~:1442), replace the EEx build-time comment opener `<%#-` with the EEx-escape opener `<%%#` so the rendered output emits a runtime comment `<%# D-07 … %>` / `<%# D-08 … %>` byte-identical to the mirror. Do NOT change the comment text. Do NOT touch the surrounding `<%%=` interpolations. Run `mix parapet.gen.ui` mentally against the mirror: the generated comment must read `<%# D-07: risk chip …` exactly. Planner note per D-03: if compiling the template surfaces a deprecation warning that `<%#` is deprecated in favor of `<%!-- … --%>`, instead modernize BOTH the template comment site and the mirror line to `<%!-- D-07 … --%>` / `<%!-- D-08 … --%>` so they remain byte-equal under formatting — never resolve the drift by stripping comments. Confirm no `--warnings-as-errors` compile warning is introduced for the chosen syntax.

    Then extract the shared path helper per D-13: create `test/support/operator_ui_paths.ex` defining `Parapet.TestSupport.OperatorUIPaths` (plain module, `@moduledoc false`, no `use`) with three zero-arity functions `component_paths/0`, `live_template_paths/0`, `detail_template_paths/0`. Each returns the two-element string list (demo mirror path + template path) for that pair, copied verbatim from the @component_paths / @live_template_paths / @detail_template_paths attributes in operator_ui_contrast_test.exs. `test/support` is already on `elixirc_paths(:test)`. Retrofitting the existing contrast/integration files to consume this helper is planner-discretion (D-13) — leave them unless trivially safe.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && grep -c '<%#- D-0' priv/templates/parapet.gen.ui/operator_components.ex.eex | grep -qx 0 && mix compile --warnings-as-errors 2>&1 | grep -qi 'warning' && echo FAIL || echo OK</automated>
  </verify>
  <done>
    The template no longer contains build-time `<%#-` comments at the D-07/D-08 sites; `OperatorUIPaths` compiles and exposes the three path functions; no new compile warning.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GUARD-03 template↔demo byte-parity test</name>
  <files>test/parapet/operator_ui_parity_test.exs</files>
  <read_first>
    - test/mix/tasks/parapet.gen.ui_test.exs lines ~1-30 and ~296-300 (Igniter.Test idiom: `test_project(app_name: :demo_app)` → `Ui.igniter()` → `Rewrite.source!(igniter.rewrite, path) |> Rewrite.Source.get(:content)`, and the `generated_source/2` private helper)
    - .planning/phases/50-guardrails-parity-idempotence-gate/50-PATTERNS.md lines ~61-89 (core parity pattern, three pairs, failure-DX requirement)
    - test/support/operator_ui_paths.ex (the helper from Task 1)
  </read_first>
  <behavior>
    - Generating operator_components.ex and normalizing through Code.format_string!/1 equals the formatted mirror (passes ONLY after the Task 1 D-03 fix).
    - Generating operator_live.ex equals its formatted mirror.
    - Generating operator_detail_live.ex equals its formatted mirror.
    - A divergent pair produces a flunk naming the pair, the first divergent line number, and the remediation string.
    - A template that fails to parse produces an explicit named parity-failure flunk (not a raw stack trace).
  </behavior>
  <action>
    Per D-01/D-02/D-04, create `Parapet.OperatorUIParityTest` (`use ExUnit.Case, async: true`, `import Igniter.Test`, `alias Mix.Tasks.Parapet.Gen.Ui`). Invoke the REAL generator once: `igniter = test_project(app_name: :demo_app) |> Ui.igniter()`. This drives the real `Igniter.copy_template` pipeline with real `web_module: DemoAppWeb` and `repo_module: DemoApp.Repo` assigns — the transform interpolates BOTH @web_module AND @repo_module (@repo_module differs 4× in operator_live, 6× in operator_detail_live; @app_name is passed but not interpolated into these three templates). Add a private `generated_source(igniter, path)` helper that reads content via `Rewrite.source!(igniter.rewrite, path) |> Rewrite.Source.get(:content)`.

    For all THREE pairs (operator_components, operator_live, operator_detail_live), assert parity per D-02: inside a `try`, set `normalized_generated = Code.format_string!(generated) |> IO.iodata_to_binary()` and `normalized_mirror = Code.format_string!(File.read!(mirror_path)) |> IO.iodata_to_binary()`, then `assert normalized_generated == normalized_mirror` with the D-05 message. Normalize BOTH sides through `Code.format_string!/1` (do not assume the committed mirror is canonically formatted across formatter versions). REJECT AST-compare (`Code.string_to_quoted`) — it discards comments and would mask the D-03 bug. The generated path is the in-rewrite path `lib/demo_app_web/live/parapet/<file>.ex`; the mirror path comes from `OperatorUIPaths` (the `examples/demo_app/...` entry of each pair). In `rescue` for parse/SyntaxError, `flunk` with the named pair and the offending message (surface the EEx line).

    D-05 failure-message DX: the assert message must name the failing pair, show the first divergent line and its line number (compute by zipping the two normalized strings split on newline and reporting the first index that differs), and end with the literal remediation guidance to regenerate `mix parapet.gen.ui` into examples/demo_app then `mix format`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && mix test test/parapet/operator_ui_parity_test.exs --max-failures 1</automated>
  </verify>
  <done>
    All three pairs pass under Code.format_string! normalization; the test is green ONLY because of the Task 1 D-03 fix (reverting the fix would make operator_components fail with a named, line-numbered, remediation-bearing message).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GUARD-04 palette gate + GUARD-05 motion tests + audit-matrix exception rationale</name>
  <files>test/parapet/operator_ui_palette_gate_test.exs, test/parapet/operator_ui_motion_test.exs, brandbook/notes/operator-audit-matrix.md</files>
  <read_first>
    - .planning/phases/50-guardrails-parity-idempotence-gate/50-PATTERNS.md lines ~93-213 (palette-gate allowlist parse, exception set, scan/assert; motion easing regex + dual-zero assertions)
    - brandbook/tokens/tokens.css (31 #rrggbb token literals — the live allowlist source)
    - brandbook/notes/operator-audit-matrix.md lines ~132-160 (the GUARD-04 Off-Palette Exception section that documents #7FB4C6/#A8D0DE; #1A5066/#556B77/#8C2E27 are NOT yet documented)
    - test/parapet/operator_ui_contrast_test.exs lines ~118-125, ~232-233 (existing motion assertions)
    - test/support/operator_ui_paths.ex (path helper from Task 1)
  </read_first>
  <behavior>
    - GUARD-04: parsing tokens.css yields ≥ 31 hexes; with < 31 the test fails closed with an explicit message.
    - GUARD-04: every 6-digit hex in the three operator templates is a member of allowlist ∪ the 5 documented exceptions; an injected off-palette hex (e.g. #123456) fails with a message pointing to tokens.css / the exception list.
    - GUARD-05: the brand easing curve matches the whitespace/leading-zero-tolerant regex in both template (`cubic-bezier(.2, 0, 0, 1)`) and tokens.css (`cubic-bezier(0.2, 0, 0, 1)`) form.
    - GUARD-05: BOTH `--motion-fast: 0ms` and `--motion-base: 0ms` appear inside the prefers-reduced-motion block.
  </behavior>
  <action>
    First, per D-07, extend the GUARD-04 Off-Palette Exception section of `brandbook/notes/operator-audit-matrix.md`: add a one-line rationale plus a contrast note for each of `#1A5066` (light link-hover / accent-strong — darkened Watch Blue), `#556B77` (dark chip-neutral-border), and `#8C2E27` (destructive-hover — darkened Incident Red), in the same style as the existing `#7FB4C6`/`#A8D0DE` entries. This is the single documented home the gate's exception set is sourced from; it must land before the gate ships.

    GUARD-04 (D-06/D-08/D-09): create `Parapet.OperatorUIPaletteGateTest` (`use ExUnit.Case, async: true`). Parse the allowlist live from tokens.css: `Regex.scan(~r/#[0-9a-fA-F]{6}\b/, File.read!("brandbook/tokens/tokens.css"), capture: :first) |> List.flatten() |> Enum.map(&String.upcase/1) |> MapSet.new()`. Guard fail-closed: `assert MapSet.size(allowlist) >= 31` with a message reporting the actual count. Declare the 5 exceptions per D-07 as a named module attribute `@palette_exceptions` (uppercased) — `#7FB4C6`, `#A8D0DE`, `#1A5066`, `#556B77`, `#8C2E27` — each with an inline rationale comment referencing the audit-matrix home (do NOT use bare undocumented literals). For each path in `OperatorUIPaths.component_paths() ++ live_template_paths() ++ detail_template_paths()`, read content, scan 6-digit hexes only (`~r/#[0-9a-fA-F]{6}/`, upcased), and assert each is a member of `MapSet.union(allowlist, @palette_exceptions)` with a message instructing to add to tokens.css or the GUARD-04 exception list. This AUGMENTS (does not replace) the existing `refute content =~ "bg-indigo-600"` class-denylist checks in operator_ui_contrast_test.exs (D-08) — leave those in place.

    GUARD-05 (D-10/D-11): create `Parapet.OperatorUIMotionTest` (`use ExUnit.Case, async: true`). Over `OperatorUIPaths.component_paths()`, read content and assert: the brand easing curve via the tolerant regex `~r/cubic-bezier\(\s*0?\.2\s*,\s*0\s*,\s*0\s*,\s*1\s*\)/` (NOT a literal string — template and tokens.css disagree on the leading zero); AND that BOTH `--motion-fast: 0ms` and `--motion-base: 0ms` are present (both exist at operator_components.ex.eex ~:468-469, locking them against regression). Migrating the pre-existing motion assertions out of operator_ui_contrast_test.exs into this file is planner-discretion (D-12) — only do it as a clean string move if zero-risk, otherwise leave them.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && mix test test/parapet/operator_ui_palette_gate_test.exs test/parapet/operator_ui_motion_test.exs</automated>
  </verify>
  <done>
    Both tests pass; tokens.css parse yields ≥ 31; the audit matrix documents all five exception hexes; the palette gate would reject an injected off-palette hex and the motion test locks both zeroed tokens + the tolerant easing regex.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → tokens.css parse | The palette allowlist is derived at test time from a file parse; a broken/empty parse must not silently admit any hex. |

This plan is additive regression guardrails (new test files) plus exactly one source edit (the D-03 generator-template comment fix). It introduces no new runtime attack surface, no new endpoints, no new user-input paths, and no new dependencies.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01 | Tampering | tokens.css allowlist parse (operator_ui_palette_gate_test) | medium | mitigate | Fail-closed `assert MapSet.size(allowlist) >= 31` (D-06) so a broken/empty parse fails the gate explicitly rather than admitting an empty allowlist. |
| T-50-02 | Tampering | GUARD-03 parity normalization | medium | mitigate | Normalize via `Code.format_string!` (preserves comments) and explicitly REJECT AST-compare; a `try/rescue` reports an unparseable render as a named failure instead of a passing silence (D-02). |
| T-50-03 | Information disclosure | palette exception backdoor | low | mitigate | Exception hexes are sourced only from the documented audit-matrix section (D-07); no bare inline undocumented literals are permitted in the gate. |
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_parity_test.exs test/parapet/operator_ui_palette_gate_test.exs test/parapet/operator_ui_motion_test.exs` all green.
- `mix compile --warnings-as-errors` clean (D-03 syntax introduces no warning).
- `mix format --check-formatted` clean for the new test files and support module.
- Reverting the D-03 template fix makes the parity test fail with a named, line-numbered, remediation-bearing message (proves the test catches drift for the right reason).
</verification>

<success_criteria>
- GUARD-03: all three template↔mirror pairs byte-equal under Code.format_string! normalization, green only after the D-03 fix.
- GUARD-04: fail-closed hex gate (≥31 token allowlist ∪ 5 documented exceptions) over the three templates; audit matrix documents all five exceptions.
- GUARD-05: tolerant easing regex + both motion tokens zeroed under prefers-reduced-motion.
- Shared `Parapet.TestSupport.OperatorUIPaths` with the three path functions exists and is consumed by the new tests.
</success_criteria>

<output>
Create `.planning/phases/50-guardrails-parity-idempotence-gate/50-01-SUMMARY.md` when done.
</output>
