---
phase: 50-guardrails-parity-idempotence-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/demo_app/scripts/capture_operator_ui_screenshots.sh
  - examples/demo_app/scripts/operator-ui-baseline.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [GUARD-06]
must_haves:
  truths:
    - "`capture_operator_ui_screenshots.sh --manifest` prints the 15-row Markdown baseline table (name|route|viewport|theme|scenario + total-count line) and exits WITHOUT invoking Chrome, requiring a DB, or starting the server (D-14)."
    - "The committed `operator-ui-baseline.md` contains the manifest table plus a `## Re-run & compare` procedure in the same file (D-15/D-16); no PNGs and no hashes are committed."
    - "A CI step runs `--manifest` and diffs it against the committed baseline, failing on drift (D-17)."
    - "The capture path and the manifest path both iterate ONE shared capture list — the screenshots and the manifest can no longer drift apart (D-14)."
  artifacts:
    - "examples/demo_app/scripts/capture_operator_ui_screenshots.sh (--manifest mode + shared capture list)"
    - "examples/demo_app/scripts/operator-ui-baseline.md"
    - ".github/workflows/ci.yml (manifest-drift step in the no-DB quality job)"
  key_links:
    - "The shared bash array is the single source of truth: capture mode and --manifest mode both read it, structurally eliminating the Phase-49 stale-count drift risk."
    - "The CI diff step must run in the Postgres-free `quality` job so --manifest needs no infra."
---

<objective>
Make the operator-UI screenshot baseline a script-generated, CI-enforced manifest (GUARD-06) while keeping the repo raster-free. Refactor the 15 duplicated `capture` calls into one shared list, add a `--manifest` mode that emits the baseline table with no Chrome/DB/server, commit the baseline Markdown (manifest + re-run/compare procedure in one file), and wire a cheap CI drift check.

Purpose: Eliminate the stale-count drift the Phase-49 D-09 note worried about by making the script the single source of truth, and turn "manifest matches the script" into an enforced gate without a headless-Chrome-on-seeded-Postgres CI pipeline.

Output: A refactored capture script with `--manifest`, a committed `operator-ui-baseline.md`, and a CI step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/50-guardrails-parity-idempotence-gate/50-CONTEXT.md
@.planning/phases/50-guardrails-parity-idempotence-gate/50-PATTERNS.md

# The capture list to refactor (lines ~47-90) + the $DETAIL_ID resolution block (lines ~35-42)
@examples/demo_app/scripts/capture_operator_ui_screenshots.sh
# CI structure — the Postgres-free `quality` job (steps ~49-56) is where the drift check belongs
@.github/workflows/ci.yml
# House style for the baseline doc's verification block
@.planning/milestones/v1.5-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor capture list to a shared array + add --manifest mode</name>
  <files>examples/demo_app/scripts/capture_operator_ui_screenshots.sh</files>
  <read_first>
    - examples/demo_app/scripts/capture_operator_ui_screenshots.sh lines ~35-42 (the $DETAIL_ID resolution that queries the DB — this runs only in capture mode, NOT manifest mode)
    - examples/demo_app/scripts/capture_operator_ui_screenshots.sh lines ~47-90 (the `capture()` function and the 15 `capture …` call sites to consolidate)
    - .planning/phases/50-guardrails-parity-idempotence-gate/50-PATTERNS.md lines ~284-344 (refactor target + --manifest dispatch pattern)
  </read_first>
  <action>
    Per D-14, refactor the 15 duplicated `capture` call sites (~:69-88) into one shared bash array `CAPTURES` where each element encodes `name|viewport|route|theme` (use the same field order the capture function consumes). Keep the existing `capture()` function (~:47-67) unchanged. Replace the call block with a single dispatch loop that splits each entry on `|` (`IFS='|' read -r name size path theme`) and calls `capture "$name" "$size" "$path" "$theme"`. Preserve all 15 entries exactly (6 desktop + 5 mobile + 4 gallery) including viewports and themes.

    Handle the detail route's `$DETAIL_ID` carefully: in capture mode the route still resolves to `/parapet/incidents/$DETAIL_ID` (the DB query at ~:35-42 runs as today). In the manifest array, the detail entries must use a stable DB-free placeholder route — `/parapet/incidents/:detail_id` — so `--manifest` needs no DB. Achieve this by storing `:detail_id` literally in the array and, in capture mode only, substituting the resolved `$DETAIL_ID` (e.g. `path="${path/:detail_id/$DETAIL_ID}"` inside the dispatch loop). This keeps capture and manifest driven by the SAME list while keeping manifest DB-free.

    Add `--manifest` mode per D-14/D-15/D-17: parse the flag at the top of the script (`MANIFEST_MODE=false; [[ "${1:-}" == "--manifest" ]] && MANIFEST_MODE=true`). When set, the script must exit BEFORE any Chrome/server/DB resolution (move or guard the `$DETAIL_ID` block so it does not run under `--manifest`). In manifest mode, print the exact Markdown table that the committed baseline holds: a `| name | route | viewport | theme | scenario |` header + separator, one row per array entry (render viewport `1440,1100` as `1440×1100`, scenario column = `stress` for all rows per the script's `PARAPET_DEMO_SCENARIO=stress` default), a blank line, then a `Total: 15 captures` line, then `exit 0`. The manifest output must be byte-stable across runs (no timestamps, no resolved IDs).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && bash examples/demo_app/scripts/capture_operator_ui_screenshots.sh --manifest | grep -c '^| operator-\|^| gallery-' | grep -qx 15 && echo OK || echo FAIL</automated>
  </verify>
  <done>
    `--manifest` emits exactly 15 data rows + a `Total: 15 captures` line, runs with no Chrome/DB/server, and exits 0; capture mode still resolves `$DETAIL_ID` and drives off the same array.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the baseline manifest + re-run/compare procedure</name>
  <files>examples/demo_app/scripts/operator-ui-baseline.md</files>
  <read_first>
    - .planning/phases/50-guardrails-parity-idempotence-gate/50-PATTERNS.md lines ~348-377 (baseline.md structure: table + `## Re-run & compare` + `## CI manifest-drift check`)
    - The exact `--manifest` output from Task 1 (the committed file must be byte-equal to it for the diff check to pass)
  </read_first>
  <action>
    Per D-15/D-16, create `examples/demo_app/scripts/operator-ui-baseline.md`. The manifest-table BLOCK in this file — from the `| name |` header row through the `Total: 15 captures` sentinel line — MUST be the exact byte-for-byte stdout of `capture_operator_ui_screenshots.sh --manifest`: no Markdown bolding, no reformatting, no reflow. Generate it by running the script with `--manifest`, capturing stdout, and pasting that block verbatim; then wrap PROSE around it (above and below the block). In particular, the count sentinel must be the bare `Total: 15 captures` line the script emits — do NOT bold it (`**Total: …**` would break the `^Total: ` anchor that Task 2's verify and Task 3's CI step use to extract the table region via `sed -n '/^| name /,/^Total: /p'`, producing a spurious diff mismatch). The file contains, in one file: (1) a short header line above the block noting it is generated by `capture_operator_ui_screenshots.sh --manifest` (header prose is fine; just keep it OUTSIDE the verbatim block); (2) the verbatim `--manifest` block — the `name | route | viewport | theme | scenario` table, 15 rows, detail rows showing `/parapet/incidents/:detail_id`, ending with the bare `Total: 15 captures` line; (3) a `## Re-run & compare` section per D-16 — env setup + `PARAPET_DEMO_SCENARIO=stress` seed → start demo app → run the capture script into a tmp out-dir (PNGs land in tmp, NEVER committed) → compare each PNG against the manifest rows (present, correct viewport/theme, eyeball layout) → when/how to update the baseline (re-run `--manifest`, commit the regenerated `.md`, never commit PNGs); (4) a `## CI manifest-drift check` section showing the exact `--manifest | diff` command CI runs. No PNGs, no content hashes (D-15) — the repo stays raster-free.

    Critical: the verbatim block (header row through `Total: 15 captures`) must be byte-identical to `bash …/capture_operator_ui_screenshots.sh --manifest` stdout so the `sed`-extracted region matches under `diff` in both Task 2's verify and Task 3's CI step on a clean tree.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && diff <(bash examples/demo_app/scripts/capture_operator_ui_screenshots.sh --manifest) <(sed -n '/^| name /,/^Total: /p' examples/demo_app/scripts/operator-ui-baseline.md) && echo OK || echo FAIL</automated>
  </verify>
  <done>
    The committed baseline's table block is byte-equal to live `--manifest` output; the file also contains the `## Re-run & compare` and `## CI manifest-drift check` sections; no rasters/hashes committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire the CI manifest-drift check into the no-DB quality job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines ~49-58 (the Postgres-free `quality` job's last steps, ending at "Verify Public API"; the `test` job below it has Postgres services — the drift step must NOT go there)
  </read_first>
  <action>
    Per D-17, add one cheap step to the Postgres-free `quality` job (after the "Verify Public API" step at ~:55-56, before the `test:` job begins) named e.g. "Operator UI manifest drift". The step runs the script in manifest mode and diffs against the committed baseline table, failing on any drift. Use a command equivalent to: run `bash examples/demo_app/scripts/capture_operator_ui_screenshots.sh --manifest`, extract the committed table block from `operator-ui-baseline.md` (the lines from the `| name ` header through the `Total: ` line), and `diff` them — non-zero diff fails the step. Match the exact extraction the Task 2 verify uses so local and CI behavior are identical. Do NOT add Postgres, Chrome, or a server to this job — `--manifest` needs none. Keep the YAML style (action SHA pins, step naming) consistent with the surrounding steps.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && grep -q 'manifest' .github/workflows/ci.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('OK')"</automated>
  </verify>
  <done>
    `ci.yml` is valid YAML, contains a manifest-drift step in the Postgres-free `quality` job, and the step uses `--manifest` + `diff` (no new services added).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → manifest drift check | A committed manifest that silently diverges from the script would let screenshot coverage rot; the diff gate is the enforcement point. |

This plan adds a script mode, a committed Markdown manifest, and one CI step. The `--manifest` mode runs without Chrome, DB, network, or server. No new runtime attack surface, endpoints, user-input paths, or dependencies are introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-04 | Tampering | manifest ↔ script drift | medium | mitigate | The CI `--manifest \| diff` step (D-17) fails the build on any divergence; capture and manifest both iterate one shared array (D-14) so they cannot drift independently. |
| T-50-05 | Denial of service | CI infra footprint | low | accept | `--manifest` deliberately needs no Chrome/DB/server; the drift step runs in the Postgres-free quality job, adding negligible CI cost. Accepted by design (repo-lean). |
</threat_model>

<verification>
- `bash examples/demo_app/scripts/capture_operator_ui_screenshots.sh --manifest` emits 15 rows + total line, no Chrome/DB/server, exit 0.
- The committed `operator-ui-baseline.md` table is byte-equal to live `--manifest` output.
- `ci.yml` is valid YAML with a manifest-drift step in the Postgres-free quality job.
- `shellcheck` (if available) reports no new errors on the refactored script.
</verification>

<success_criteria>
- GUARD-06: script is the single source of truth (capture + manifest iterate one array); committed baseline + re-run/compare procedure in one file; CI drift check enforced without new infra; repo stays raster-free.
</success_criteria>

<output>
Create `.planning/phases/50-guardrails-parity-idempotence-gate/50-02-SUMMARY.md` when done.
</output>
