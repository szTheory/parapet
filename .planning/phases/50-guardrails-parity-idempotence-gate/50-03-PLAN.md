---
phase: 50-guardrails-parity-idempotence-gate
plan: 03
type: execute
wave: 2
depends_on: ["50-01", "50-02"]
files_modified:
  - .planning/milestones/v1.6-MILESTONE-AUDIT.md
autonomous: true
requirements: [GUARD-07]
must_haves:
  truths:
    - "`v1.6-MILESTONE-AUDIT.md` exists in v1.4/v1.5 house style (YAML frontmatter scores/gaps/nyquist → Summary → per-requirement evidence table → Phase Verification → Non-Regression Proofs → Idempotence Proof → Font Package-Size Delta → Verification Commands w/ pasted output → Result) (D-18)."
    - "Every requirement row resolves to a re-runnable command or a file:line — no evidence cell is a bare 'see phase N' (D-19)."
    - "Three non-regression proofs are bound to existing evidence: public-API (`mix verify.public_api` exit 0), host-ownership (compile-out/integration tests + on_exists: :skip sites + matrix grep-proofs), telemetry (`mix test test/telemetry_contract_test.exs`) (D-20)."
    - "Idempotence proof cites `mix parapet.gen.ui --dry-run` zero-change + the three on_exists: :skip sites + the audit-matrix ledger semantics (D-22)."
    - "Font package-size delta reports absolute woff2 ≈52.2 KB (machine-checked under 150 KB) AND a `mix hex.build` Package size tarball delta (pre-v1.6 vs post) (D-23)."
  artifacts:
    - ".planning/milestones/v1.6-MILESTONE-AUDIT.md"
  key_links:
    - "GUARD-03/04/05/06 rows must cite the gates landed in plans 01 and 02 (parity/palette/motion tests, the manifest + drift check) — this is why this plan depends on both."
    - "The audit is coherent-by-reference with operator-audit-matrix.md (the matrix stays the single idempotence ledger), not a duplication of it (D-19)."
---

<objective>
Write `.planning/milestones/v1.6-MILESTONE-AUDIT.md` (GUARD-07): a thin evidence-binding document over the existing gates and the ones landed in plans 01–02. It records per-requirement evidence, proves no public-API/telemetry/host-ownership regression, the font package-size delta, and that the milestone is forward-only/idempotent.

Purpose: Close the v1.6 Operator UI Brand & Design-System Audit milestone with a re-runnable evidence trail — every claim resolves to a command, a manifest diff, or a file:line.

Output: One audit document in v1.4/v1.5 house style, with pasted command output.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/50-guardrails-parity-idempotence-gate/50-CONTEXT.md
@.planning/phases/50-guardrails-parity-idempotence-gate/50-PATTERNS.md

# House style + frontmatter + Verification Commands block to mirror
@.planning/milestones/v1.5-MILESTONE-AUDIT.md
# Idempotence ledger + host-ownership grep-proofs to cite by reference (D-19/D-20/D-22)
@brandbook/notes/operator-audit-matrix.md
# Non-regression evidence sources (D-20)
@test/telemetry_contract_test.exs
# Font delta source (D-23) — the 5 woff2 files + 150 KB ceiling
@test/parapet/operator_ui_fonts_test.exs
# on_exists: :skip (43/54/65) + --dry-run (74) call sites for the idempotence proof (D-22)
@lib/mix/tasks/parapet.gen.ui.ex
# The plan-01/02 SUMMARYs landed before this plan — cite the gates they produced
@.planning/phases/50-guardrails-parity-idempotence-gate/50-01-SUMMARY.md
@.planning/phases/50-guardrails-parity-idempotence-gate/50-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather and paste real verification evidence</name>
  <files>.planning/milestones/v1.6-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v1.5-MILESTONE-AUDIT.md lines ~105-148 (Verification Commands block style: fenced bash with `# Expected:` comments + pasted actual output, then Result)
    - test/parapet/operator_ui_fonts_test.exs lines ~5-37 (the 5 woff2 filenames + 153_600-byte ceiling — source the absolute font total)
  </read_first>
  <action>
    Run each verification command and capture its real output for pasting into the audit (D-18 principle: every claim resolves to a re-runnable artifact). Run, from the repo root:
    - `mix verify.public_api` (expect exit 0 — public-API drift gate, D-20).
    - `mix test test/telemetry_contract_test.exs` (expect green — frozen telemetry-family contract, D-20).
    - `mix test test/parapet/operator_ui_compile_out_test.exs test/parapet/operator_ui_integration_test.exs` (host-ownership, D-20).
    - `mix test test/parapet/operator_ui_parity_test.exs test/parapet/operator_ui_palette_gate_test.exs test/parapet/operator_ui_motion_test.exs test/parapet/operator_ui_fonts_test.exs` (the new GUARD-03/04/05 gates + the font ceiling, D-23).
    - `mix parapet.gen.ui --dry-run` against an already-installed host (expect zero file changes — idempotence, D-22). If a clean target host is unavailable, cite the `--dry-run` honoring at parapet.gen.ui.ex:74 + Igniter `super/1` and the three `on_exists: :skip` sites at :43/:54/:65 as the proof, and note the command form.
    - Font absolute total: compute the sum of the 5 woff2 file sizes (the `priv/static/parapet/fonts/*.woff2` set listed in operator_ui_fonts_test.exs) — expect ≈52.2 KB, under the 153_600-byte ceiling.
    - `mix hex.build` and capture the `Package size` line for the post-v1.6 tarball delta (D-23). For the pre-v1.6 number, derive it from the last pre-font release tarball if available; otherwise report the post-v1.6 `Package size` and state the woff2 delta (≈52.2 KB added) as the honest reproducible pair per D-23.

    Stash these captured outputs (in the SUMMARY or a scratch note) for Task 2 to paste verbatim into the Verification Commands block. Do not fabricate output — paste real results.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && mix verify.public_api && mix test test/telemetry_contract_test.exs test/parapet/operator_ui_parity_test.exs test/parapet/operator_ui_palette_gate_test.exs test/parapet/operator_ui_motion_test.exs test/parapet/operator_ui_fonts_test.exs</automated>
  </verify>
  <done>
    All cited verification commands run and their real outputs are captured (public-API exit 0, telemetry contract green, the three new gates + font ceiling green, dry-run zero-change or its file:line proof, font absolute total + hex.build Package size).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write v1.6-MILESTONE-AUDIT.md in v1.4/v1.5 house style</name>
  <files>.planning/milestones/v1.6-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v1.5-MILESTONE-AUDIT.md (full — frontmatter, section order, Verification Commands block, Result)
    - .planning/REQUIREMENTS.md (the TOKEN/FONT/COMP/NAV/DATA/GROUP/FLOW/COPY/A11Y/MOTION/GALLERY/FIXTURE/GUARD ids for the coverage table)
    - brandbook/notes/operator-audit-matrix.md (the component×state ledger to link by reference — do NOT duplicate it)
    - the captured command outputs from Task 1
  </read_first>
  <action>
    Create `.planning/milestones/v1.6-MILESTONE-AUDIT.md` per D-18 in the v1.4/v1.5 house style. Sections in order:

    1. YAML frontmatter: `milestone: v1.6`, `milestone_name: Operator UI Brand & Design-System Audit`, `audited:` (ISO timestamp), `status: passed`, `scores:` (requirements N/N, phases 7/7 for 44–50, integration, flows), `gaps:` (empty lists if clean), `tech_debt: []` (record the deferred stable-telemetry manifest as a backlog note, not as a gap — D-21), `nyquist:` (`compliant_phases: [44,45,46,47,48,49,50]`, `overall: compliant`).
    2. Title + Status/Audited/Scope (Phases 44–50) header.
    3. `## Summary` — one-paragraph milestone result.
    4. `## Requirements Coverage` — per D-19, a table `| Requirement | Phase | Claim | Evidence (command or file:line) | Status |` with one row per TOKEN/FONT/COMP/NAV/DATA/GROUP/FLOW/COPY/A11Y/MOTION/GALLERY/FIXTURE/GUARD id. Every Evidence cell is a runnable command or a file:line; GUARD-03/04/05 rows cite the new tests from plan 01, GUARD-06 cites the `--manifest` mode + baseline + CI drift step from plan 02, GUARD-07 cites this document. Link the matrix by reference for per-cell `verified` status — do not copy the ledger.
    5. `## Phase Verification` — brief per-phase (44–50) verification line.
    6. `## Non-Regression Proofs` — the three proofs per D-20: public-API (`mix verify.public_api` exit 0 vs `priv/parapet/public_api_stable.json`); host-ownership (`operator_ui_compile_out_test.exs` + `operator_ui_integration_test.exs` + `on_exists: :skip` at parapet.gen.ui.ex:43/:54/:65 + the host-ownership grep-proofs in operator-audit-matrix.md); telemetry (`mix test test/telemetry_contract_test.exs` — operator UI is host-owned markup, adds no telemetry events).
    7. `## Idempotence Proof` — per D-22: `mix parapet.gen.ui --dry-run` zero file changes (cite parapet.gen.ui.ex:74 + Igniter `super/1`); the three `on_exists: :skip` sites (forward-only, host markup never overwritten); audit-matrix ledger cells advance todo→done→verified only.
    8. `## Font Package-Size Delta` — per D-23: (a) absolute woff2 ≈52.2 KB across 5 files, machine-checked under the 150 KB ceiling by operator_ui_fonts_test.exs, labeled "added this milestone"; (b) `mix hex.build` `Package size` tarball delta (pre-v1.6 vs post). Be honest that there is no pre-font baseline to subtract — report the reproducible pair.
    9. `## Verification Commands` — fenced bash block in the v1.5 style (`# N. <label>` comment heading, `# Expected: …`), then the PASTED real outputs from Task 1 (not fabricated).
    10. `## Result` — passed.

    Do NOT add a `telemetry_stable.json` manifest this phase (D-21) — note it as backlog. Keep the doc coherent-by-reference with operator-audit-matrix.md, not a duplication (D-19).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && test -f .planning/milestones/v1.6-MILESTONE-AUDIT.md && grep -q '^## Non-Regression Proofs' .planning/milestones/v1.6-MILESTONE-AUDIT.md && grep -q '^## Idempotence Proof' .planning/milestones/v1.6-MILESTONE-AUDIT.md && grep -q '^## Font Package-Size Delta' .planning/milestones/v1.6-MILESTONE-AUDIT.md && grep -q 'Requirement | Phase | Claim' .planning/milestones/v1.6-MILESTONE-AUDIT.md && echo OK || echo FAIL</automated>
  </verify>
  <done>
    The audit doc exists with all required sections (frontmatter, Summary, Requirements Coverage table with the D-19 columns, Phase Verification, Non-Regression Proofs, Idempotence Proof, Font Package-Size Delta, Verification Commands with pasted real output, Result); every requirement row resolves to a runnable command or file:line; no telemetry_stable.json added (D-21).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit claim → evidence | An audit claim that does not resolve to a re-runnable artifact gives false assurance; every row must bind to a command or file:line. |

This plan produces a single Markdown audit document binding existing gates to evidence. It introduces no new runtime attack surface, endpoints, user-input paths, or dependencies.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-06 | Repudiation | unverifiable audit claim | medium | mitigate | Every Requirements Coverage row carries a runnable command or file:line (D-19); Verification Commands paste real output (D-18), so any claim can be independently re-run and falsified. |
| T-50-07 | Tampering | public-API / telemetry drift going unrecorded | low | mitigate | The three non-regression proofs (D-20) bind to existing drift gates (`mix verify.public_api`, telemetry contract test) that fail closed on regression; the audit records their passing state, not a hand-wave. |
</threat_model>

<verification>
- `.planning/milestones/v1.6-MILESTONE-AUDIT.md` exists with all required sections in v1.4/v1.5 house style.
- Every Requirements Coverage Evidence cell is a runnable command or a file:line (no bare "see phase N").
- The three non-regression proofs and the idempotence proof cite real, re-runnable artifacts.
- Font delta reports both the absolute woff2 total and the `mix hex.build` Package size.
- No `telemetry_stable.json` added this phase (D-21 — backlog only).
</verification>

<success_criteria>
- GUARD-07: the milestone audit binds every regression claim to a re-runnable command/manifest/file:line, proves no public-API/telemetry/host-ownership regression, reports the font package-size delta, and proves forward-only idempotence — coherent-by-reference with operator-audit-matrix.md.
</success_criteria>

<output>
Create `.planning/phases/50-guardrails-parity-idempotence-gate/50-03-SUMMARY.md` when done.
</output>