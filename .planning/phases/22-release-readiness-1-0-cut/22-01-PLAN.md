---
phase: 22-release-readiness-1-0-cut
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [REL-01, REL-03]

must_haves:
  truths:
    - "CI has a dedicated release-quality lint lane"
    - "The lint lane runs compile/docs warnings-as-errors plus credo, dialyzer, and verify.public_api"
    - "The test job is narrowed back to mix test"
    - "release_gate remains the single fan-in branch-protection target and now depends on lint + test + demo"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "lint, test, demo, and release_gate topology"
      contains: "release_gate"
  key_links:
    - from: ".github/workflows/ci.yml lint"
      to: "release-quality commands"
      via: "warnings-as-errors + static gates"
      pattern: "warnings-as-errors"
    - from: ".github/workflows/ci.yml release_gate"
      to: "lint + test + demo"
      via: "needs fan-in"
      pattern: "needs: \\[lint, test, demo\\]"
---

<objective>
Harden CI for the 1.0 bar without changing runtime behavior: introduce a dedicated release-
quality `lint` job, move non-test release gates there, keep `mix test` isolated in `test`,
preserve the `demo` smoke job, and keep `release_gate` as the single branch-protection fan-in
check.

Purpose: REL-01 requires warnings-as-errors compile/docs enforcement in CI, and D-03/D-04
explicitly require a dedicated lint lane rather than overloading the test job. REL-03 also
depends on this topology because the proportionate release gate must be visible and honest.

Output: `.github/workflows/ci.yml` exposes four jobs: `lint`, `test`, `demo`, and
`release_gate`, with `release_gate` fanning in all three proof jobs.
</objective>

<context>
@.planning/phases/22-release-readiness-1-0-cut/22-CONTEXT.md
@.planning/phases/22-release-readiness-1-0-cut/22-RESEARCH.md
@.planning/phases/22-release-readiness-1-0-cut/22-PATTERNS.md

<interfaces>
- Current truth: root `mix compile --warnings-as-errors` and `mix docs --warnings-as-errors`
  already pass locally as of 2026-05-26.
- Current `.github/workflows/ci.yml` has `test`, `demo`, and `release_gate`, with
  `release_gate` already required in GitHub branch protection.
- The plan must preserve the operator-facing `release_gate` name and only restructure the
  upstream jobs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the dedicated lint lane and narrow test responsibility</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Edit `.github/workflows/ci.yml` so it has:

1. A new `lint` job that sets up Beam and runs:
   - `mix deps.get`
   - `mix format --check-formatted`
   - `mix compile --warnings-as-errors`
   - `mix compile --no-optional-deps --warnings-as-errors`
   - `mix docs --warnings-as-errors`
   - `mix credo --strict`
   - `mix dialyzer`
   - `mix verify.public_api`

2. A `test` job that keeps setup/caching but runs only:
   - `mix deps.get`
   - `mix test`

3. The existing `demo` job preserved as the demo smoke-contract lane.

4. `release_gate` updated to `needs: [lint, test, demo]`.

Do not rename `release_gate`. Do not remove `demo`. Do not move `mix test` into `lint`.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && rg -n "mix compile --warnings-as-errors|mix compile --no-optional-deps --warnings-as-errors|mix docs --warnings-as-errors|mix verify.public_api|needs: \\[lint, test, demo\\]" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a `lint:` job
    - The `lint` job runs both compile warnings-as-errors commands and `mix docs --warnings-as-errors`
    - The `lint` job also runs `mix credo --strict`, `mix dialyzer`, and `mix verify.public_api`
    - The `test` job remains present and runs `mix test`
    - `release_gate` fans in `lint`, `test`, and `demo`
  </acceptance_criteria>
  <done>CI exposes a dedicated release-quality lint lane and keeps `release_gate` as the stable fan-in check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source changes -> CI release proof | Whether a branch can claim release-quality green status |
| CI jobs -> branch protection | The surfaces that feed the required `release_gate` check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Repudiation/Bypass | release-quality checks hidden in test noise | mitigate | Split static/build/release-quality commands into `lint` and keep `release_gate` as the single fan-in required check. |
| T-22-01B | Tampering | operator-facing check contract | mitigate | Preserve the `release_gate` job name so existing branch protection remains meaningful. |
</threat_model>

<verification>
- `.github/workflows/ci.yml` parses as YAML.
- The `lint` job runs the exact release-quality commands required by REL-01.
- `release_gate` depends on `lint`, `test`, and `demo`.
</verification>

<success_criteria>
- REL-01 is enforceable in CI without changing runtime behavior.
- `release_gate` remains the single required merge target while now reflecting all three proof jobs.
</success_criteria>

<output>
Create `.planning/phases/22-release-readiness-1-0-cut/22-01-SUMMARY.md` when done.
</output>
