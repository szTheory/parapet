---
phase: 04-unified-install-path-dx
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - README.md
  - docs/operator-ui.md
autonomous: true
requirements:
  - DX-01
must_haves:
  truths:
    - "The public docs describe `mix parapet.install` as the single Day-1 paved road and point to `mix parapet.doctor` as the immediate verification step."
    - "The docs keep optional UI and delivery/async integrations explicit and host-owned."
    - "The docs explain the new doctor posture clearly enough for local and CI use without promising distributed guarantees the code cannot prove."
  artifacts:
    - path: "README.md"
      provides: "Canonical Day-1 install contract and next-step verification narrative"
      min_lines: 160
    - path: "docs/operator-ui.md"
      provides: "UI gating, host-auth ownership, and doctor verification wording aligned to the new installer"
      min_lines: 80
  key_links:
    - from: "README.md"
      to: "lib/mix/tasks/parapet.install.ex"
      via: "documented Day-1 command, explicit extras, and doctor follow-up"
      pattern: 'mix parapet\.install|mix parapet\.doctor'
    - from: "docs/operator-ui.md"
      to: "lib/mix/tasks/parapet.gen.ui.ex"
      via: "optional UI guidance and host-owned auth boundary"
      pattern: 'Parapet does \*\*not\*\* provide its own authentication system'
---

<objective>
Align the public docs with the installer and doctor contracts created by Plans 04-01 and 04-02.

Purpose: Make the paved road discoverable and honest so the README and operator docs do not drift from the actual CLI behavior.
Output: Updated install/docs narrative that reflects the final flags, opt-ins, summary posture, and doctor verification semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-unified-install-path-dx/04-CONTEXT.md
@README.md
@docs/operator-ui.md
@lib/mix/tasks/parapet.install.ex
@lib/mix/tasks/parapet.doctor.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the README Day-1 story around the unified install path</name>
  <read_first>
    README.md
    lib/mix/tasks/parapet.install.ex
    lib/mix/tasks/parapet.doctor.ex
    .planning/phases/04-unified-install-path-dx/04-CONTEXT.md
  </read_first>
  <files>README.md</files>
  <action>Update the README install and "Zero to First Alert" sections to reflect the final installer contract from D-01 through D-17. Use `docs/operator-ui.md` as the paired doc analog from `04-PATTERNS.md` so README wording mirrors the same host-owned, UI-optional posture where the subjects overlap. `mix parapet.install` must be documented as the single public Day-1 command that runs the core path automatically, with explicit examples for the final non-interactive extras flags from Plan 04-01: `--with-ui`, `--skip-ui`, `--with-mailglass`, and `--with-chimeway` if they remain public. Keep optional integrations host-owned and explicit, preserving the `Parapet.attach(adapters: [...])` contract and the provider-registration boundary. Tighten the doctor section to reflect the final severity/threshold posture from Plan 04-02, including `mix parapet.doctor` as the immediate next verification step, `mix parapet.doctor cluster` as the runtime-oriented cluster check, and `--ci` semantics if present.</action>
  <acceptance_criteria>
    - `rg -n 'mix parapet\\.install' README.md` shows the installer as the single Day-1 entrypoint.
    - `rg -n 'mix parapet\\.doctor' README.md` shows doctor as the immediate follow-up verification step.
    - `rg -n 'Parapet\\.attach\\(adapters:' README.md` keeps optional integrations explicit and host-owned.
    - `rg -n '--ci|warn|error|skip' README.md` documents the final doctor posture when those flags/statuses exist publicly.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'mix parapet\\.install|--with-ui|--skip-ui|--with-mailglass|--with-chimeway|mix parapet\\.doctor cluster|--ci|warn|error|skip|Parapet\\.attach\\(adapters:' README.md && rg -n 'with_ui|skip_ui|with_mailglass|with_chimeway|cluster|--ci|info|warn|error|skip' lib/mix/tasks/parapet.install.ex lib/mix/tasks/parapet.doctor.ex</automated>
  </verify>
  <done>The README's public install contract matches the actual installer and doctor behavior shipped in Plans 04-01 and 04-02.</done>
</task>

<task type="auto">
  <name>Task 2: Align the operator UI guide with install gating and doctor verification</name>
  <read_first>
    docs/operator-ui.md
    lib/mix/tasks/parapet.install.ex
    lib/mix/tasks/parapet.gen.ui.ex
    lib/mix/tasks/parapet.doctor.ex
    .planning/phases/04-unified-install-path-dx/04-CONTEXT.md
  </read_first>
  <files>docs/operator-ui.md</files>
  <action>Update `docs/operator-ui.md` so the prerequisites and installation story match the final installer behavior from Plan 04-01. Use `README.md` as the paired doc analog from `04-PATTERNS.md` so the guide mirrors the same Day-1 command names and verification sequencing while preserving UI-specific detail. The guide should say the UI remains optional, is only relevant when Phoenix LiveView is present, and still requires host-owned router auth per D-04. Add the new doctor verification wording from Plan 04-02 so the guide explains the relevant operator-UI and cluster-safety checks, including `mix parapet.doctor cluster`, without overclaiming what static analysis proves. Keep the tone evidence-first and least-surprise, consistent with D-24 and D-25.</action>
  <acceptance_criteria>
    - `rg -n 'mix parapet\\.install' docs/operator-ui.md` shows the guide depending on the unified install path.
    - `rg -n 'optional|LiveView' docs/operator-ui.md` states the UI gating clearly.
    - `rg -n 'Parapet does \\*\\*not\\*\\* provide its own authentication system' docs/operator-ui.md` preserves the host-owned auth boundary.
    - `rg -n 'mix parapet\\.doctor|distributed correctness|warning|warn' docs/operator-ui.md` reflects the final doctor verification posture.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'mix parapet\\.install|Parapet does \\*\\*not\\*\\* provide its own authentication system|mix parapet\\.doctor|mix parapet\\.doctor cluster|LiveView|optional|distributed correctness|warn|warning' docs/operator-ui.md && rg -n 'mix parapet\\.install|mix parapet\\.doctor cluster' README.md</automated>
  </verify>
  <done>The operator UI guide stays aligned with the new installer and doctor contracts while keeping UI auth and optionality clearly host-owned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs -> adopter actions | README and guide text shape what host apps install and how safely they mount or verify Parapet |
| docs -> CLI contract | inaccurate docs can cause adopters to miss required verification or accidentally widen optional surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | T | `README.md` | mitigate | Document `mix parapet.install` as the only paved-road command and keep extras explicit so docs cannot imply hidden automation beyond the shipped CLI. |
| T-04-10 | I | `docs/operator-ui.md` | mitigate | Preserve the exact host-owned auth warning and clarify that doctor findings are evidence-backed but not a proof of distributed correctness. |
| T-04-11 | R | public install narrative | mitigate | Point maintainers to `mix parapet.doctor` immediately after install and document the relevant threshold semantics so verification expectations are auditable. |
</threat_model>

<verification>
Run the `rg` commands in each task and manually compare the documented flags/status wording against the final CLI surfaces in `lib/mix/tasks/parapet.install.ex` and `lib/mix/tasks/parapet.doctor.ex`.
</verification>

<success_criteria>
The README and operator UI guide describe the same Day-1 install, optional UI/integration posture, and doctor verification model that the code actually ships.
</success_criteria>

<output>
After completion, create `.planning/phases/04-unified-install-path-dx/04-03-SUMMARY.md`
</output>
