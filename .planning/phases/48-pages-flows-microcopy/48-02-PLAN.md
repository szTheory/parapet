---
phase: 48-pages-flows-microcopy
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex
autonomous: true
requirements: [FLOW-02, FLOW-03, FLOW-05, COPY-02, COPY-03, COPY-04, COPY-05, A11Y-06]
must_haves:
  truths:
    - "operator_components.ex.eex exposes a new incident_not_found/1 panel with the D-02 locked copy that keeps brand chrome and renders @requested_id as escaped text only (FLOW-03, COPY-03)."
    - "incident_summary/1 takes a heading_level prop (default h2) so the same component can render h1 on the detail page and h2 embedded in the cockpit (FLOW-02 single-h1)."
    - "All non-hero empty states share one anatomy (icon + heading + body + optional single next-action link, dashed-border, no hover:/transition) and use --parapet-* tokens; the cockpit all-clear stays a distinct hero variant (FLOW-03)."
    - "The connected?-keyed skeleton (animate-pulse + aria-busy + aria-live, matched h-16 row geometry, no literal Loading text) is available to the Actions and History lists (FLOW-03)."
    - "The 5 component-side D-13 microcopy strings are present verbatim and no #{inspect(reason)} appears in any user-facing component string (COPY-03)."
    - "R1-R7 overflow fixes (max-h bottom sheet, break-all/min-w-0 machine strings, grid-cols-1 base collapse) hold with zero horizontal scroll at 390px (FLOW-05)."
  artifacts:
    - "priv/templates/parapet.gen.ui/operator_components.ex.eex"
    - "examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex"
  key_links:
    - "incident_not_found/1 and the heading_level prop are consumed by operator_detail_live in 48-03 — their names/signatures are the contract 48-03 depends on."
    - "@requested_id flows from the detail LiveView (48-03) into incident_not_found/1 and MUST be rendered as escaped text content only (security control T-48-01)."
  prohibitions:
    - "Do NOT churn the pinned _copy/1 helpers (~1535-1774) or the Phase-47 action-rail/risk/preview strings — they are on-voice and test-pinned (D-13/D-15 check 6)."
    - "Do NOT change incident_summary/1's success-shape or its other attrs — only add the additive heading_level prop (D-06)."
    - "Do NOT raw-interpolate @requested_id into any href/id/HTML attribute — escaped text content only (D-02, T-48-01)."
    - "Do NOT add any spinner; loading = skeleton-or-nothing, and never bake a literal Loading string (D-09)."
    - "Do NOT add hover:/transition to the non-interactive empty-state/not-found card (D-11 audit bug)."
    - "Do NOT introduce horizontal scroll or overflow-x-auto a table to fix 390px — fix vertically (D-17)."
    - "Do NOT let the R1 preview_panel overflow-y-auto trip the DATA-02 refute — that refute is scoped to @live_template_paths and preview_panel lives in components (D-16 R1)."
    - "Every .eex edit in this plan MUST land in its demo mirror operator_components.ex in the SAME task (demo-mirror parity)."
---

<objective>
Wave-2 green for the component layer: edit operator_components.ex.eex (and its demo mirror, same task) to add the designed not-found panel, the incident_summary heading-level prop, the standardized empty-state anatomy + uniform skeleton, the 5 component-side microcopy rewrites, and the R1-R7 mobile overflow fixes — flipping the corresponding 48-01 RED assertions green.

Purpose: D-19 wave-2 lands the template + demo-mirror edits in lockstep. This plan owns everything in operator_components.ex.eex; the shell/service wiring that consumes these components is 48-03.
Output: the re-skinned, on-brand operator_components template + byte-parity demo mirror.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-pages-flows-microcopy/48-CONTEXT.md
@.planning/phases/48-pages-flows-microcopy/48-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `incident_not_found/1` — new component (attrs: `operator_base_path` default "/parapet", `requested_id` required) rendering the D-02 locked not-found copy.
- `incident_summary/1` — new additive prop `heading_level` (default "h2"; detail page will pass "h1").
- Optional (Claude's discretion, D-11): extracted `empty_state/1` and/or `list_skeleton/1` shared components — only if byte-parity holds; otherwise inline.
- Standardized empty-state anatomy applied to the migrated `text-stone-*` empties (`:781`, `:961`) and a new History-empty designed state.
- 5 component-side microcopy strings (runbook_card title/description fallbacks, preview_panel line — D-13).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Not-found panel + incident_summary heading-level prop + component microcopy</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</files>
  <read_first>
    - priv/templates/parapet.gen.ui/operator_components.ex.eex (incident_summary attr block + h1 at 832-840; Actions empty state 728-738 as the not-found analog; runbook_card :1118/:1121; preview_panel line :1249; pinned _copy/1 helpers ~1535-1774 — DO NOT touch)
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex (the demo mirror — edit in the SAME task)
    - .planning/phases/48-pages-flows-microcopy/48-PATTERNS.md (sections A "incident_summary heading-level prop", C "incident_not_found panel", F "Microcopy rewrites"; Shared Patterns "Escape rule for untrusted URL input")
    - .planning/phases/48-pages-flows-microcopy/48-CONTEXT.md (D-02 locked not-found copy verbatim; D-06 heading-level prop; D-13 the 5 component strings; D-15 voice gate)
  </read_first>
  <action>
    In operator_components.ex.eex AND its demo mirror operator_components.ex (same task):
    (a) Add incident_not_found/1 (D-02): attr operator_base_path default "/parapet", attr requested_id required. Render the dashed-border, brand-chrome panel using the Actions-empty analog (728-738) anatomy — decorative aria-hidden single-line-weight icon, heading This incident isn't in the evidence store, body No durable incident matches this link. It may have been pruned by retention, or the link is stale. Active incidents stay in the response queue until resolved., primary link Back to active response navigating to @operator_base_path (reuse the detail_back_* convention), secondary link View resolved history to @operator_base_path <> "/history", and a de-emphasized mono line Requested id: <the escaped requested_id> rendered as escaped HEEx text content ONLY. Use --parapet-* token styles (heading var(--parapet-text), body/icon var(--parapet-text-muted)). No hover:/transition on the panel.
    (b) Add the heading_level prop to incident_summary/1 (D-06): new attr heading_level default "h2"; branch the incident title between an h1 and an h2 (same classes) on the prop value. Do not change any other incident_summary attr or its output shape. Keep the Phase-47 GROUP-02 voice strings untouched.
    (c) Apply the 3 component microcopy rewrites from D-13 verbatim: runbook_card title fallback (:1118) Untitled runbook; runbook_card description fallback (:1121) No runbook description was recorded. Follow the steps below; each one previews before it runs.; preview_panel line (:1249) This preview reflects scoped changes only — nothing has run. Confirm to execute, or close to discard. Leave all pinned _copy/1 and Phase-47 strings byte-for-byte unchanged.
    Mirror every edit into the demo file so assertion-pairing parity holds.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -15 && mix test test/parapet/operator_ui_integration_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - incident_not_found/1 exists with attrs operator_base_path + requested_id and renders the D-02 copy verbatim (heading + body + both action links + Requested id line).
    - @requested_id appears only as escaped text content; grep confirms it is NOT inside any href={...} / id={...} / attribute interpolation (T-48-01).
    - incident_summary/1 has a heading_level attr defaulting to "h2" and renders h1 when "h1" is passed; no other attr changed.
    - The 3 D-13 component strings are present verbatim; the prior fallback strings are gone.
    - The pinned _copy/1 + Phase-47 verbatim strings still pass (D-15 check 6).
    - Both the template and its demo mirror carry identical edits.
    - The COPY-03 / not-found-copy source-string assertions from 48-01 that target these strings now pass GREEN.
  </acceptance_criteria>
  <done>incident_not_found/1, the incident_summary heading_level prop, and the 3 component microcopy rewrites are landed in both the template and its demo mirror; the related 48-01 source-string asserts are green.</done>
</task>

<task type="auto">
  <name>Task 2: Standardized empty-state anatomy, uniform skeleton, and R1-R7 overflow fixes</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</files>
  <read_first>
    - priv/templates/parapet.gen.ui/operator_components.ex.eex (empty states :613 cockpit hero / :728 Actions / :781 bare-stone / :884 / :961 timeline / :1025 / :1322; skeleton geometry analog; reduced-motion block :471; preview_panel :1194-1310; action_item_card :1370; risk chips :1386-1401)
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex (demo mirror — edit in the SAME task)
    - .planning/phases/48-pages-flows-microcopy/48-PATTERNS.md (sections D "Empty-state anatomy + skeleton", E "Mobile overflow fixes R1/R2/R3/R5"; Shared Patterns "Token-driven empty/error states", "socket_connected guard")
    - .planning/phases/48-pages-flows-microcopy/48-CONTEXT.md (D-09 skeleton-or-nothing; D-10 socket_connected gate; D-11 one empty anatomy + token migration + History-empty; D-16 R1-R7 by severity; D-17 9-point 390px checklist)
  </read_first>
  <action>
    In operator_components.ex.eex AND its demo mirror (same task):
    (a) D-11 empty-state standardization: migrate the bare text-stone-* empties (:781, :961) to the one anatomy — dashed-border non-interactive container (no hover:/transition), aria-hidden single-line-weight icon (never sole carrier of meaning), text-sm font-semibold heading in var(--parapet-text), token-colored guidance body in var(--parapet-text-muted), and an optional single next-action link. Add a designed History-empty state in the same anatomy. Keep the cockpit all-clear (:613, No active incidents need response.) as the distinct hero variant (do not flatten it). Extract a shared empty_state/1 (and optionally list_skeleton/1) vs inline at your discretion as long as byte-parity holds (D-11).
    (b) D-09/D-10 skeleton: ensure the connected?-keyed skeleton block (animate-pulse + aria-live="polite" + aria-busy bound to !socket_connected, three matched h-16 token-bg rows, aria-hidden, no literal Loading text) is expressed as a component/markup the Actions and History lists can reuse (the lists themselves are wrapped in 48-03's shell). Reduced motion is already neutralized at :471 — verify it covers the new rows.
    (c) D-16 overflow fixes, all in components: R1 add max-h-[100dvh] to the preview_panel fixed wrapper (:1194) and max-h-[calc(100dvh-2rem)] overflow-y-auto overscroll-contain to the inner card (:1196) — exact calc offset is your discretion (D-17); confirm this does NOT trip the DATA-02 refute (scoped to @live_template_paths only). R2 action_item_card header (:1370 region) gets min-w-0 and the external_id value wrapped in break-all font-mono. R3 preview_panel data grid (:1205) grid-cols-2 → grid-cols-1 sm:grid-cols-2 and value <p>s get min-w-0 break-words. R4 suspect-changes flag chip (:1044, font-mono) add break-all min-w-0. R5 runbook step text wrapper (:1128) flex-1 → flex-1 min-w-0, h4/p get break-words, verify the button column is shrink-0. R6/R7 verify-then-fix: keep incident_row truncate (:819/:821, full value reachable on detail); add break-all min-w-0 to incident_summary trace span (:876) to match the timeline-link convention (:999). Apply the break-all (machine strings) vs break-words (prose) discipline from D-17.
    (d) Touch targets (verify-then-fix, D-16): confirm interactive controls clear 24x24 (buttons min-h-[40px], theme options min-h-[32px]); do NOT blanket-pad non-interactive chips/badges; only raise a genuine sub-24px interactive element if found.
    Mirror every edit into the demo file (parity in the same task).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -15 && mix test test/parapet/operator_ui_contrast_test.exs test/parapet/operator_ui_integration_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The :781 and :961 empties use --parapet-* tokens (no bare text-stone-500/text-stone-800) and the one shared anatomy; a designed History-empty exists; the :613 cockpit hero remains distinct.
    - No empty-state or not-found card carries hover: or transition (D-11).
    - The skeleton block uses animate-pulse + aria-busy + aria-live + three h-16 token-bg rows and contains no literal Loading text (D-09).
    - R1 preview_panel has max-h-[100dvh] on the wrapper and max-h-[calc(...)] overflow-y-auto overscroll-contain on the inner card; the DATA-02 @live_template_paths refute still passes (R1 lives in components).
    - R2/R3/R4/R5/R7 break-all/break-words/min-w-0/grid-cols-1-base utilities are applied at the named sites; R6 incident_row truncate retained.
    - mix compile --warnings-as-errors is clean; contrast + integration tests pass for the component-scoped assertions.
    - Template and demo mirror carry identical edits.
  </acceptance_criteria>
  <done>The standardized empty-state anatomy, the reusable uniform skeleton, and the R1-R7 overflow fixes are landed in both the template and its demo mirror; component-scoped 48-01 asserts are green and no horizontal-scroll/overflow regression is introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detail LiveView assigns → `incident_not_found/1` `@requested_id` | untrusted URL `:id` (set in 48-03) is rendered by this plan's component |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering/Info-disclosure (reflected XSS) | `incident_not_found/1` rendering `@requested_id` | high | mitigate | Render `@requested_id` as HEEx-escaped text content only (HEEx escapes `<%= %>` by default); never raw-interpolate into `href`/`id`/any HTML attribute. Verified by grep that `@requested_id` does not appear inside an attribute interpolation. (D-02.) |
| T-48-03 | Information disclosure | empty/not-found copy implying authz/outage | medium | accept | Not-found copy is scoped to no-data (pruned/stale) only; infra-unavailable and permission-denied are N/A-by-design and not rescued into this panel (D-03) — copy is fixed and does not leak state. |
| T-48-SC | Tampering | npm/pip/cargo installs | low | accept | No new dependency introduced (values/markup-only component edits); no install tasks. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/parapet/operator_ui_contrast_test.exs test/parapet/operator_ui_integration_test.exs` — component-scoped 48-01 asserts green.
- `grep` confirms `@requested_id` never appears inside an attribute interpolation in either the template or the demo mirror.
- Template and demo mirror are edited in lockstep (assertion-pairing parity).
</verification>

<success_criteria>
operator_components.ex.eex and its demo mirror expose the designed not-found panel, the incident_summary heading-level prop, the standardized empty-state anatomy + uniform skeleton, the 3 component microcopy rewrites, and the R1-R7 overflow fixes — turning the component-scoped 48-01 RED assertions green, with the @requested_id escape control verified and no pinned-string churn.
</success_criteria>

<output>
Create `.planning/phases/48-pages-flows-microcopy/48-02-SUMMARY.md` when done, listing the new/changed component symbols, which 48-01 asserts flipped green, the @requested_id escape verification, and confirmation of demo-mirror parity.
</output>
