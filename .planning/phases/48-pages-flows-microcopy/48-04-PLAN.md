---
phase: 48-pages-flows-microcopy
plan: 04
type: execute
wave: 4
depends_on: ["48-01", "48-02", "48-03"]
files_modified:
  - brandbook/notes/operator-audit-matrix.md
autonomous: false
requirements: [FLOW-01, FLOW-02, FLOW-03, FLOW-04, FLOW-05, COPY-01, COPY-02, COPY-03, COPY-04, COPY-05, A11Y-06]
user_setup: []
must_haves:
  truths:
    - "The full lib suite (mix test) and the full demo suite (cd examples/demo_app && mix test) both pass green — every 48-01 RED assertion is now satisfied (D-19 wave 4)."
    - "The operator-audit-matrix records FLOW/COPY/A11Y cells as done/verified, with unavailable and permission-denied entered as named N/A-by-Design rows with grep proof (D-12)."
    - "A human has walked the /parapet/_gallery route and confirmed the four human-only items pass (D-19): copy reading-flow at 390px, heading-hierarchy legibility after the h1 demotion, empty-state feels-designed, and keyboard focus-visibility."
  artifacts:
    - "brandbook/notes/operator-audit-matrix.md (FLOW/COPY/A11Y cells flipped; N/A-by-Design rows for unavailable + permission-denied)"
  key_links:
    - "The gallery route DemoAppWeb.Parapet.GalleryLive at /parapet/_gallery is the only seam for the blocking human visual walkthrough (demo-only, never generated)."
  prohibitions:
    - "Do NOT flip any audit-matrix cell to verified before the full lib + demo suites pass green."
    - "Do NOT auto-approve the human gallery checkpoint — it is a blocking human gate (visual/keyboard facts the automated suite cannot prove)."
    - "Do NOT stub unavailable/permission-denied panes — record them as N/A-by-Design rows with grep proof only (D-12)."
    - "Do NOT modify source/template/test files in this plan — it is a gate + ledger plan; any failure routes back to 48-02/48-03."
---

<objective>
Wave-4 closing gate for Phase 48: run the full lib and demo test suites to confirm every 48-01 assertion is green, flip the operator-audit-matrix FLOW/COPY/A11Y cells (recording unavailable + permission-denied as N/A-by-Design with grep proof), and run the blocking human /parapet/_gallery walkthrough for the four facts the automated suite cannot prove.

Purpose: D-19 wave-4 is the autonomous: false phase-completion gate mirroring Phase 47 — full-suite green + audit-matrix flip + a blocking human gallery walkthrough.
Output: green suites, an updated audit matrix ledger, and a recorded human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-pages-flows-microcopy/48-CONTEXT.md
@.planning/phases/48-pages-flows-microcopy/48-01-SUMMARY.md
@.planning/phases/48-pages-flows-microcopy/48-02-SUMMARY.md
@.planning/phases/48-pages-flows-microcopy/48-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- Updated `brandbook/notes/operator-audit-matrix.md` — FLOW/COPY/A11Y component×state cells set to done/verified; two named N/A-by-Design rows (unavailable, permission-denied) with grep-proof references (D-12).
- A recorded human gallery sign-off captured in the plan SUMMARY.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Full lib + demo suite gate and audit-matrix flip</name>
  <files>brandbook/notes/operator-audit-matrix.md</files>
  <read_first>
    - brandbook/notes/operator-audit-matrix.md (existing component × state ledger; Phase-47 GROUP-03/05/06 N/A-by-Design convention to reuse)
    - .planning/phases/48-pages-flows-microcopy/48-CONTEXT.md (D-12 N/A-by-Design rows + grep proof; D-18 the full assertion set; F test-gate strategy)
    - .planning/phases/48-pages-flows-microcopy/48-01-SUMMARY.md (the assertion → REQ-ID mapping to mark off)
  </read_first>
  <action>
    Run the full lib suite (mix test) and the full demo suite (cd examples/demo_app && mix test). Both MUST be green; if any 48-01 assertion still fails, STOP and route the failure back to 48-02 (component) or 48-03 (shell/service) by fact-type — do not edit source here. Once green, update brandbook/notes/operator-audit-matrix.md: flip the FLOW-01..05 / COPY-01..05 / A11Y-06 component×state cells to done/verified with a one-line evidence pointer each (the test name that proves it). Add two named N/A-by-Design rows reusing the Phase-47 GROUP-03/05/06 convention: unavailable (synchronous in-node repo reads → infra failure is the host's 5xx, grep proof: no assign_async/Task in the templates) and permission-denied (host-owned authz at the router; LiveViews reached only post-authorization, grep proof: router_snippet auth pipeline). Never stub a pane for these — ledger rows only.
  </action>
  <verify>
    <automated>mix test 2>&1 | tail -15 && (cd examples/demo_app && mix test 2>&1 | tail -15)</automated>
  </verify>
  <acceptance_criteria>
    - mix test (lib) and cd examples/demo_app && mix test (demo) both report 0 failures.
    - operator-audit-matrix.md has every FLOW/COPY/A11Y cell at done/verified with an evidence pointer.
    - unavailable and permission-denied appear as named N/A-by-Design rows with grep-proof references (not stubbed panes).
    - No source/template/test file was modified by this task.
  </acceptance_criteria>
  <done>Both full suites are green and the audit matrix records every Phase-48 cell as done/verified with the two N/A-by-Design rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Blocking human /parapet/_gallery walkthrough</name>
  <action>Run the blocking human gallery walkthrough (the four human-only facts the automated suite cannot prove). See what-built / how-to-verify below.</action>
  <what-built>
    The full Phase-48 operator flow rendered in the demo gallery: per-page single h1 (Response sr-only / Actions / History promoted / Detail), :page_title on each page, the designed in-page not-found panel (unknown + malformed id), the standardized empty states + uniform skeleton, the D-13 microcopy, and the R1-R7 mobile overflow fixes — all on-brand and token-driven.
  </what-built>
  <how-to-verify>
    1. Boot the demo gallery: from examples/demo_app run the demo server (e.g. mix phx.server, or the project's gallery boot command) and open http://localhost:<port>/parapet/_gallery (DemoAppWeb.Parapet.GalleryLive). Also walk the real flow at /parapet → /parapet/actions → /parapet/history → an incident detail, plus /parapet/<a-bogus-id> for the not-found panel.
    2. Copy reading-flow at 390px: shrink to 390px width. For each empty state, error flash, and the not-found panel, confirm the copy reads calmly as symptom → evidence → safe next action (no panic/blame/inspect leaks), with zero horizontal scroll.
    3. Heading-hierarchy legibility after the h1 demotion: confirm the nav banner Active response workbench reads as a banner (not a page heading) and each page's real h1 is the clear page subject; heading order looks monotonic.
    4. Empty-state feels designed not broken: confirm each empty/not-found state looks intentional (icon + heading + body + single next-action), not like a blank/error.
    5. Keyboard focus-visibility: Tab from page load — confirm the skip-to-content link appears and works, nav items show a visible focus ring, and the not-found panel's Back/History links are focusable with a visible ring.
  </how-to-verify>
  <resume-signal>Type "approved" if all four human-only checks pass, or describe the specific issue(s) to route back to 48-02 (component) or 48-03 (shell).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification-only) | This plan runs tests and edits a docs ledger; it introduces no new runtime trust boundary beyond those mitigated in 48-02/48-03. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering (reflected XSS) | `@requested_id` in not-found panel (mitigated in 48-02) | high | mitigate | Re-verified at the gate: the not-found rendered asserts (malformed id) pass and the human walkthrough confirms the requested id displays as inert text. |
| T-48-02 | DoS (uncaught 500) | malformed `:id` (mitigated in 48-03) | high | mitigate | Re-verified at the gate: the malformed-id rendered assert passes (in-page panel, not a 500). |
| T-48-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency introduced this phase; no install tasks. |
</threat_model>

<verification>
- `mix test` (lib) — 0 failures.
- `cd examples/demo_app && mix test` (demo) — 0 failures.
- `brandbook/notes/operator-audit-matrix.md` shows all Phase-48 FLOW/COPY/A11Y cells done/verified + 2 N/A-by-Design rows.
- Blocking human gallery walkthrough returned "approved" for all four human-only checks.
</verification>

<success_criteria>
Both full suites are green (every 48-01 assertion satisfied), the audit matrix records every Phase-48 cell as done/verified with the unavailable + permission-denied N/A-by-Design rows, and a human has signed off the four gallery-only facts (390px reading-flow, heading legibility, empty-state design, keyboard focus). Phase 48 is complete.
</success_criteria>

<output>
Create `.planning/phases/48-pages-flows-microcopy/48-04-SUMMARY.md` when done, recording the suite results (lib + demo pass counts), the audit-matrix cells flipped, and the human gallery sign-off verbatim.
</output>
