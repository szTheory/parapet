---
phase: 48-pages-flows-microcopy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/parapet/operator_ui_integration_test.exs
  - test/parapet/operator_ui_contrast_test.exs
  - examples/demo_app/test/demo_app/operator_smoke_test.exs
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03, FLOW-04, FLOW-05, COPY-01, COPY-02, COPY-03, COPY-04, COPY-05, A11Y-06]
must_haves:
  truths:
    - "Every Phase-48 FLOW/COPY/A11Y fact has at least one failing (RED) assertion before any source change lands (D-19 wave 1)."
    - "Source-string facts (COPY literals, route ordering, :page_title assign presence, A11Y attribute presence) are asserted in the paired @*_paths loops of the existing lib tests (D-18)."
    - "Rendered-state facts (single-h1 per page, page_title assign, not-found panel for unknown+malformed id, disconnected skeleton, main/nav landmarks) are asserted in the existing demo Phoenix.LiveViewTest harness (D-18)."
    - "Single-h1 is counted via a regex scan, not a source grep and not Floki (DOM backend is lazy_html) (D-18)."
    - "COPY-04 refutes use bounded regexes that do not match attribute=placeholder or the word today (D-18)."
  artifacts:
    - "test/parapet/operator_ui_integration_test.exs (extended with route-ordering, :page_title-assign, COPY pins, COPY-04 bounded refutes)"
    - "test/parapet/operator_ui_contrast_test.exs (extended only if a source-string contrast/refute fact needs a RED pin)"
    - "examples/demo_app/test/demo_app/operator_smoke_test.exs (extended with single-h1, page_title, not-found, skeleton, landmark rendered asserts)"
  key_links:
    - "Demo smoke harness (ConnCase + Ecto sandbox) is the only seam that can prove rendered single-h1 and not-found composition; lib source greps are structurally blind to these (D-18)."
  prohibitions:
    - "Do NOT create any new test file — extend the three existing files only (D-19 'no new test files')."
    - "Do NOT edit any source/template file in this plan — assertions must be RED against unchanged sources (D-19 wave 1)."
    - "Do NOT count h1 with Floki or source grep — use Regex.scan over rendered html (D-18)."
    - "Do NOT use bare /placeholder/i (matches placeholder= attrs) or bare /todo/i (matches today) in COPY-04 refutes (D-18)."
    - "Do NOT assert the host <title> string for page_title — assert the page_title(view) assign the host <.live_title> consumes (D-18)."
---

<objective>
Wave-1 RED scaffold for Phase 48. Add every Phase-48 verification assertion (FLOW-01..05, COPY-01..05, A11Y-06) to the three existing test files and confirm each fails RED against the unchanged sources, so wave-2/3 implementation can drive them green.

Purpose: D-19 mandates a 4-wave RED/green cadence mirroring Phase 47, with no new test files. This plan installs the failing gates that pin the exact facts wave-2 (48-02 component layer) and wave-3 (48-03 shell/service layer) must satisfy.
Output: Three extended test files whose new assertions are confirmed RED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-pages-flows-microcopy/48-CONTEXT.md
@.planning/phases/48-pages-flows-microcopy/48-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (48-01) produces no source symbols — it is test-only. It installs RED assertions that pin the symbols later plans create:
- `Parapet.Operator.fetch_incident_detail/1` (asserted indirectly via not-found rendered behavior; created in 48-03)
- `incident_not_found/1` component + locked copy `This incident isn't in the evidence store` (created in 48-02)
- `incident_summary/1` `heading_level` prop → exactly-one-h1 per page (prop created in 48-02, detail usage in 48-03)
- private `page_title/2` (operator_live) and `page_title/3` (operator_detail_live) → `:page_title` assigns (created in 48-03)
- the 10+1 microcopy strings (D-13) and landmark `nav[aria-label="Incident context"]` (created in 48-02/48-03)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: RED source-string assertions in lib integration + contrast tests</name>
  <files>test/parapet/operator_ui_integration_test.exs, test/parapet/operator_ui_contrast_test.exs</files>
  <read_first>
    - test/parapet/operator_ui_integration_test.exs (existing @*_paths paired loops; index_of/2 helper at lines 4-9; verbatim copy pins at 186-196)
    - test/parapet/operator_ui_contrast_test.exs (paired @*_paths loops; scroll-pb gate ~248; DATA-02 overflow-y-auto refute scoped to live templates ~276)
    - .planning/phases/48-pages-flows-microcopy/48-PATTERNS.md (section "Test files — assertion patterns": route-ordering, :page_title source assert, COPY-04 bounded refutes, verbatim copy pins)
    - .planning/phases/48-pages-flows-microcopy/48-CONTEXT.md (D-04 route ordering; D-13 the 10+1 locked strings; D-15 voice-consistency gate; D-18 source-string routing)
  </read_first>
  <action>
    Extend test/parapet/operator_ui_integration_test.exs using the existing index_of/2 helper and @*_paths content loops (do not create new files). Add, per D-18 source-string routing:
    (a) FLOW-04 route-ordering regression per D-04: assert index_of(content, the /parapet/incidents/:id route literal) is less than index_of(content, the /parapet/:id route literal) for both the router snippet template and the demo router; also assert the explanatory catch-all-last comment is present.
    (b) FLOW-02 :page_title source presence: assert the operator_live and operator_detail_live template content contains a :page_title assign and a page_title( helper definition.
    (c) COPY-01 nav labels Respond / Actions / History kept verbatim (per D-14, plain least-surprise — assert presence, do not change).
    (d) COPY-02/COPY-05 already-green Phase-47 strings remain present (verbatim pins, reuse the lines-186-196 style) — assert they survive (voice-consistency gate check 6, D-15).
    (e) COPY-03 verbatim pins for the new strings from D-13: each of the 10 re-authored strings plus the harmonized ack-success string, and the not-found heading + body copy, asserted verbatim against component/detail template content.
    (f) COPY-04 bounded-regex refutes per D-18: refute TODO/FIXME/XXX/HACK word-boundary regex, refute lorem-ipsum regex, refute placeholder-text regex. Do NOT use bare placeholder or bare todo patterns.
    (g) D-15 voice-consistency gate as refutes: no inspect( inside user-facing flash strings, no Oops/Sorry/successfully/something went wrong/Failed to bare-verb constructions in the operator-copy surfaces.
    (h) A11Y-06 source attribute presence: id="parapet-main", a nav landmark with aria-label, aria-current, and the new nav aria-label="Incident context" on the detail context strip.
    Only touch operator_ui_contrast_test.exs if a source-string contrast/refute fact (e.g. the preview_panel max-h R1 utility presence, or token-migrated empty-state vars) needs a RED pin that belongs in the contrast loop scope; otherwise leave it unchanged and note that in the summary. Confirm the DATA-02 overflow-y-auto refute scope is @live_template_paths only (R1 lives in components, so it must not trip that refute — D-16 R1 note).
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_integration_test.exs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The new assertions in operator_ui_integration_test.exs FAIL (RED) against unchanged sources (the D-13 strings, :page_title assigns, and nav aria-label do not yet exist).
    - The route-ordering assert uses index_of/2 and the /parapet/incidents/:id and /parapet/:id literals (D-04).
    - COPY-04 refutes match neither a placeholder= attribute nor the word today (bounded regexes, D-18).
    - No new test file was created; no source/template file was modified.
    - The 10+1 D-13 strings and the not-found heading/body are pinned verbatim (byte-for-byte from D-13).
  </acceptance_criteria>
  <done>operator_ui_integration_test.exs (and optionally operator_ui_contrast_test.exs) carry the full Phase-48 source-string gate set, all newly-added assertions failing RED, with no source edits.</done>
</task>

<task type="auto">
  <name>Task 2: RED rendered-state assertions in demo smoke test</name>
  <files>examples/demo_app/test/demo_app/operator_smoke_test.exs</files>
  <read_first>
    - examples/demo_app/test/demo_app/operator_smoke_test.exs (existing ConnCase + get/2 + live/2 + Ecto sandbox; seeded-per-test pattern)
    - .planning/phases/48-pages-flows-microcopy/48-PATTERNS.md (section "operator_smoke_test.exs — rendered assertions": single-h1, not-found unknown+malformed, page_title, disconnected skeleton, A11Y landmark)
    - .planning/phases/48-pages-flows-microcopy/48-CONTEXT.md (D-18 rendered routing; D-02 not-found locked copy; D-06 single-h1 incl. queue-with-selected-incident; D-09/D-10 skeleton + socket_connected gate)
  </read_first>
  <action>
    Extend examples/demo_app/test/demo_app/operator_smoke_test.exs (do not create a new file) with the D-18 rendered-state gates:
    (a) FLOW-02 single-h1: for each rendered page (/parapet, /parapet/actions, /parapet/history, the detail page, and the queue-with-a-selected-incident state), assert length(Regex.scan(over an h1-open-tag regex, html)) equals 1. Use the rendered html (get/2 resp_body or render(view)); NOT Floki, NOT a source grep (D-18 rationale: two legit h1 definitions compose onto one page).
    (b) FLOW-03 not-found: live(conn, the detail route with a fresh Ecto.UUID.generate() id) renders the designed not-found copy (This incident isn't in the evidence store); and live(conn, "/parapet/123") (malformed id) also renders the same not-found copy in-page (proves both Ecto.UUID.cast :error and Repo.get nil collapse to the panel, D-01).
    (c) FLOW-02 page_title: assert page_title(view) matches the per-page strings (Active response, Action queue, Resolved history, and Incident: <title> on detail) — read the assign the host <.live_title> consumes, never the host <title> string (D-18). This requires the demo layout patch (48-03) to be wired for the assign to surface; assert RED now.
    (d) FLOW-03 disconnected skeleton: get/2 static render of /parapet, /parapet/actions, /parapet/history resp_body contains animate-pulse and aria-busy="true" (D-09 uniform skeleton); and assert no literal Loading text is baked (D-09).
    (e) FLOW-03 empty-during-load gate: with no items seeded, the connected render shows the designed empty state, and the disconnected static render does NOT show the empty state (proves the @socket_connected and Enum.empty? gate, D-10) — assert RED.
    (f) A11Y-06 rendered cross-check: has_element?(view, "main#parapet-main") and has_element?(view, "nav[aria-label]") on a connected mount.
    Seed incidents per-test via the existing Parapet.Evidence create path used elsewhere in the file (resolved-state incident for the History page, an active incident for the queue-with-selected state).
  </action>
  <verify>
    <automated>cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - New rendered assertions FAIL (RED): pages currently emit two h1s, the not-found panel/route handling does not exist, :page_title is unset, and the Actions/History skeleton/empty-gate are not yet uniform.
    - Single-h1 is counted via Regex.scan over rendered html (no Floki, no source grep).
    - The malformed-id case uses the bare /parapet/123 route and expects the in-page not-found copy (not a 500, not a host error page) — D-01/D-02/D-03.
    - page_title is read via page_title(view), never the host <title> string.
    - No new test file created; no source/template file modified.
  </acceptance_criteria>
  <done>operator_smoke_test.exs carries the full Phase-48 rendered-state gate set (single-h1, not-found unknown+malformed, page_title, skeleton, empty-gate, landmarks), all newly-added assertions failing RED, with no source edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser URL → detail LiveView `:id` param | untrusted, attacker/scanner-controlled string enters via `/parapet/:id` |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering/Info-disclosure | not-found panel rendering `@requested_id` (untrusted URL input) | high | mitigate | This plan asserts (RED) that the malformed-id case renders the designed in-page not-found copy as escaped text — the wave-2/3 implementation must render `@requested_id` only as HEEx-escaped text content, never raw-interpolated into an attribute. (Per D-02.) |
| T-48-02 | DoS | `Repo.get!` + binary_id dual-exception on malformed `:id` (uncaught 500) | high | mitigate | This plan asserts (RED) the malformed `/parapet/123` route renders the not-found panel rather than a 500 stack trace; wave-3 adds the `Ecto.UUID.cast/1` precheck (D-01). |
| T-48-SC | Tampering | npm/pip/cargo installs | low | accept | No new runtime/test dependency is introduced this phase (CONTEXT.md milestone boundary); no install tasks exist, so no supply-chain checkpoint applies. |
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_integration_test.exs` — new source-string assertions present and RED.
- `cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs` — new rendered-state assertions present and RED.
- No new test files exist (`git status` shows only the three existing test files modified).
- No source/template file under `lib/` or `priv/templates/parapet.gen.ui/` or `examples/demo_app/lib/` is modified by this plan.
</verification>

<success_criteria>
Every Phase-48 FLOW/COPY/A11Y fact is pinned by a failing assertion in one of the three existing test files, routed by fact-type per D-18 (source-string → lib tests, rendered-state → demo smoke), with zero new test files and zero source edits. The RED set defines the exact done-bar for 48-02 and 48-03.
</success_criteria>

<output>
Create `.planning/phases/48-pages-flows-microcopy/48-01-SUMMARY.md` when done, listing each new assertion, which REQ-ID it pins, its fact-type routing (source vs rendered), and confirmation it failed RED.
</output>
