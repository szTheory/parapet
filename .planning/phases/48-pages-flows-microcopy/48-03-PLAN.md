---
phase: 48-pages-flows-microcopy
plan: 03
type: execute
wave: 3
depends_on: ["48-01", "48-02"]
files_modified:
  - lib/parapet/operator.ex
  - priv/templates/parapet.gen.ui/operator_live.ex.eex
  - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
  - priv/templates/parapet.gen.ui/router_snippet.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex
  - examples/demo_app/lib/demo_app_web/router.ex
  - examples/demo_app/lib/demo_app_web/components/layouts.ex
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-03, FLOW-04, COPY-03, COPY-05, A11Y-06]
must_haves:
  truths:
    - "Parapet.Operator.fetch_incident_detail/1 returns {:ok, detail} | {:error, :not_found}, collapsing both the Ecto.UUID.cast CastError class and the Repo.get nil/NoResultsError class (D-01)."
    - "The detail LiveView mount/handle_params and all 6 handle_event refresh sites call fetch_incident_detail/1 and degrade to the in-page not-found panel without push_navigate (D-01, D-02)."
    - "Each page has exactly one descriptive h1 (Response sr-only, Actions visible, History promoted, Detail via incident_summary heading_level=h1) and a distinct :page_title assigned in handle_params (D-06, D-07)."
    - "The /parapet/:id catch-all stays declared LAST after /parapet/incidents/:id, with a regression-asserted ordering (D-04, FLOW-04)."
    - "The detail back-link context strip is wrapped in nav aria-label=Incident context and the demo layout <.live_title> renders {assigns[:page_title]} (D-08, D-07, A11Y-06)."
    - "The 6 detail flash microcopy strings + harmonized ack-success are present verbatim with no #{inspect(reason)} in any user-facing flash (D-13, COPY-03)."
  artifacts:
    - "lib/parapet/operator.ex"
    - "priv/templates/parapet.gen.ui/operator_live.ex.eex + demo mirror"
    - "priv/templates/parapet.gen.ui/operator_detail_live.ex.eex + demo mirror"
    - "priv/templates/parapet.gen.ui/router_snippet.ex.eex + demo router"
    - "examples/demo_app/lib/demo_app_web/components/layouts.ex"
  key_links:
    - "Detail LiveView consumes incident_not_found/1 + incident_summary heading_level=h1 from 48-02 — those symbols must exist (depends_on 48-02)."
    - "Demo layout <.live_title> must render {assigns[:page_title]} or the page_title rendered asserts cannot pass (D-07)."
    - "incident_detail/1 stays public and unchanged in signature/success-shape — fetch_incident_detail/1 is additive only (D-01)."
  prohibitions:
    - "Do NOT change incident_detail/1's signature or success-shape — add fetch_incident_detail/1 additively and share the body via a private helper (D-01)."
    - "Do NOT rescue DBConnection / infra errors into the not-found panel — that would mask an outage as not-found (D-03)."
    - "Do NOT push_navigate away on not-found — render the in-page panel keeping operator nav + back link (D-02)."
    - "Do NOT auto-patch the host root layout — only patch the demo layout; add one host-doc sentence instead (D-07)."
    - "Do NOT keep #{inspect(reason)} in user-facing flashes — move inspect to Logger only (D-13/D-15 check 1)."
    - "Do NOT blanket-demote incident_summary to h2 — the detail page must pass heading_level=h1 (D-06)."
    - "Do NOT reorder routes so /parapet/:id precedes /parapet/incidents/:id — catch-all stays LAST (D-04)."
    - "Every .eex edit MUST land in its demo mirror in the SAME task; the router-snippet edit pairs with the demo router; layouts.ex is demo-only (demo-mirror parity)."
---

<objective>
Wave-3 green for the shell + service layer: add the additive fetch_incident_detail/1 failure-path helper, wire it through the detail LiveView mount/handle_params and all 6 handle_event refresh sites to the in-page not-found panel, give each page exactly one h1 + a :page_title via handle_params, wrap the detail context strip in a nav landmark, patch the demo layout for runtime titles, lock the route ordering with a regression comment, and apply the 6 detail flash microcopy rewrites — flipping the shell/service-scoped 48-01 RED assertions green.

Purpose: D-19 wave-3 is the optional shell-mirror wave; per Claude's discretion it is split out from 48-02 because the shell/service edits depend on the components 48-02 creates (incident_not_found/1, the heading_level prop) and form a coherent, separately-verifiable unit.
Output: the wired operator service + both shell LiveViews + router + demo layout, with byte-parity demo mirrors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-pages-flows-microcopy/48-CONTEXT.md
@.planning/phases/48-pages-flows-microcopy/48-PATTERNS.md
@.planning/phases/48-pages-flows-microcopy/48-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `Parapet.Operator.fetch_incident_detail/1 :: {:ok, detail} | {:error, :not_found}` — additive failure-path helper.
- `Parapet.Operator.build_detail/1` (private) — shared body extracted from `incident_detail/1` so both publics reuse it without duplication.
- `page_title/2` (private, operator_live) — `:index`/`:actions`/`:history` → Active response / Action queue / Resolved history.
- `page_title/3` (private, operator_detail_live) — `:show` → `Incident: <title>`, fallback `Incident detail`.
- detail LiveView assigns: `incident_not_found` (bool), `requested_id` (string).
- `nav aria-label="Incident context"` landmark wrapping the detail back-link context strip.
- demo layout `<.live_title suffix=" · Parapet">{assigns[:page_title] || "Demo App"}</.live_title>`.
- one host-install-guide doc sentence about `:page_title` / `<.live_title>`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Additive fetch_incident_detail/1 service helper</name>
  <files>lib/parapet/operator.ex</files>
  <read_first>
    - lib/parapet/operator.ex (existing incident_detail/1 ~107-152; @doc since: "1.0.0")
    - lib/parapet/spine/incident.ex (binary_id PK ~line 27 — root cause of the dual-exception not-found bug)
    - .planning/phases/48-pages-flows-microcopy/48-PATTERNS.md (section "operator.ex — additive fetch_incident_detail/1")
    - .planning/phases/48-pages-flows-microcopy/48-CONTEXT.md (D-01 additive helper; D-03 do-not-rescue-infra)
  </read_first>
  <action>
    In lib/parapet/operator.ex add fetch_incident_detail/1 returning {:ok, detail} | {:error, :not_found} per D-01: precheck Ecto.UUID.cast(incident_id) (collapses the CastError class — malformed ids return {:error, :not_found}), then use Evidence.repo().get (NOT get!) with a nil-guard (collapses the NoResultsError class — missing well-formed UUIDs return {:error, :not_found}). On a found incident, build the same workbench-ready detail map that incident_detail/1 returns and wrap it {:ok, detail}. Extract the shared post-fetch body of incident_detail/1 into a private build_detail/1 so both functions reuse it without duplication; keep incident_detail/1 public, its @doc since: "1.0.0", its signature, and its success-shape unchanged (make it nil-tolerant only as needed without breaking the shape — additive failure-path only). Do NOT rescue DBConnection or other infra exceptions here — those are the host's 5xx concern (D-03).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -10 && mix test test/parapet/ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - fetch_incident_detail/1 returns {:error, :not_found} for a malformed id (e.g. "123") and for a well-formed-but-missing UUID, and {:ok, detail} for an existing incident.
    - incident_detail/1 signature and success-shape are unchanged; build_detail/1 is the shared private body.
    - No rescue of DBConnection/infra errors (D-03).
    - mix compile --warnings-as-errors is clean; existing lib tests still pass.
  </acceptance_criteria>
  <done>Parapet.Operator.fetch_incident_detail/1 exists as an additive {:ok|:error} helper with a shared private build_detail/1, and incident_detail/1 is unchanged in contract.</done>
</task>

<task type="auto">
  <name>Task 2: Detail LiveView not-found wiring + detail page semantics + flash microcopy</name>
  <files>priv/templates/parapet.gen.ui/operator_detail_live.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex</files>
  <read_first>
    - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex (mount/handle_params 9-19; 6 handle_event refresh sites 10-192; flash strings :36/:40/:59/:81/:111/:115/:137/:181; context strip :215; main/aside landmarks :224/:237; detail_back_* 261-299; incident_summary usage :228)
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex (demo mirror — edit SAME task)
    - .planning/phases/48-pages-flows-microcopy/48-02-SUMMARY.md (the incident_not_found/1 + heading_level contract from 48-02)
    - .planning/phases/48-pages-flows-microcopy/48-PATTERNS.md (section "operator_detail_live.ex.eex": A mount switch, B page_title/3, C nav wrap, D heading_level usage, E render branch; section F microcopy table)
    - .planning/phases/48-pages-flows-microcopy/48-CONTEXT.md (D-01 6 refresh sites, D-02 in-page panel, D-06 detail h1, D-07 page_title, D-08 nav wrap, D-13 the 6 detail flash strings + ack harmonization, D-15 voice gate)
  </read_first>
  <action>
    In operator_detail_live.ex.eex AND its demo mirror (same task):
    (a) D-01/D-02 wiring: switch mount/handle_params to fetch_incident_detail/1, assigning on {:ok, detail} → incident: detail, incident_not_found: false, requested_id: id; on {:error, :not_found} → incident: nil, incident_not_found: true, requested_id: id. Update all 6 handle_event refresh sites that currently re-call incident_detail(id) to use fetch_incident_detail/1 and degrade to incident_not_found: true if the incident vanished mid-action. Add a top-level render branch: when @incident_not_found, render a <main id="parapet-main" tabindex="-1"> containing <.incident_not_found operator_base_path={@operator_base_path} requested_id={@requested_id} /> and KEEP operator nav/back-link chrome; do NOT push_navigate. Never raw-interpolate requested_id into an attribute (T-48-01 — the component already escapes it as text).
    (b) D-06 detail h1: change the incident_summary usage (:228) to pass heading_level="h1" so the incident title is the page's single h1.
    (c) D-07 page_title: add a private page_title/3 (:show with a bound title → Incident: <title>; fallback Incident detail) and assign :page_title in handle_params from socket.assigns.
    (d) D-08 landmark: wrap the back-link/context strip (:215) in <nav aria-label="Incident context"> (preserve its existing classes/children).
    (e) D-13 flash microcopy: replace the 6 detail flash strings verbatim (ack fail :40, resolve fail :59, escalation fail :81, suppress fail :111, invalid window :115, preview fail :137, confirm fail :181) and harmonize ack success (:36) to Incident acknowledged. Audit record and timeline entry written. Drop every #{inspect(reason)} from user-facing flashes; keep inspect inside Logger calls only (D-15 check 1).
    Mirror every edit into the demo file.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -10 && mix test test/parapet/operator_ui_integration_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - mount/handle_params and all 6 handle_event sites call fetch_incident_detail/1 and set incident_not_found appropriately; an unknown/malformed id renders the in-page not-found panel (no push_navigate, no 500).
    - The detail page passes heading_level="h1" to incident_summary (single descriptive h1).
    - page_title/3 exists and :page_title is assigned in handle_params (Incident: <title> / Incident detail).
    - The context strip is wrapped in nav aria-label="Incident context".
    - The 6 D-13 flash strings + harmonized ack-success are present verbatim; no #{inspect(reason)} remains in any user-facing flash; grep finds inspect only inside Logger calls.
    - Template and demo mirror carry identical edits.
  </acceptance_criteria>
  <done>The detail LiveView (and its mirror) renders the in-page not-found panel via fetch_incident_detail/1 across mount + 6 refresh sites, carries its single h1 + :page_title + nav landmark, and uses the D-13 flash microcopy with no inspect leaks.</done>
</task>

<task type="auto">
  <name>Task 3: Shell page semantics (h1 + page_title + skeleton wiring) + router ordering + demo layout</name>
  <files>priv/templates/parapet.gen.ui/operator_live.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex, priv/templates/parapet.gen.ui/router_snippet.ex.eex, examples/demo_app/lib/demo_app_web/router.ex, examples/demo_app/lib/demo_app_web/components/layouts.ex</files>
  <read_first>
    - priv/templates/parapet.gen.ui/operator_live.ex.eex (handle_params assign block 39-60; skeleton :247-253; History h2 :161; Response cockpit incident_summary usage :294; empty detail :307-318)
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex (demo mirror — edit SAME task)
    - priv/templates/parapet.gen.ui/router_snippet.ex.eex (route block 13-17, :id catch-all last) and examples/demo_app/lib/demo_app_web/router.ex (mirror :30-31)
    - examples/demo_app/lib/demo_app_web/components/layouts.ex (hardcoded <.live_title> :12)
    - .planning/phases/48-pages-flows-microcopy/48-02-SUMMARY.md (uniform skeleton + empty-state anatomy from 48-02)
    - .planning/phases/48-pages-flows-microcopy/48-PATTERNS.md (sections "operator_live.ex.eex" A/B/C; "router_snippet"; "layouts.ex")
    - .planning/phases/48-pages-flows-microcopy/48-CONTEXT.md (D-04 route order, D-06 per-page h1, D-07 page_title + demo layout patch + host-doc sentence, D-09/D-10 skeleton + socket_connected empty gate)
  </read_first>
  <action>
    In operator_live.ex.eex AND its demo mirror (same task):
    (a) D-06 per-page h1: Response → sr-only h1 Active response at top of <main>; Actions → visible h1 Action queue; History → promote the existing descriptive h2 (:161) to h1 and demote any now-conflicting sibling h2 to keep heading order monotonic. The Response cockpit incident_summary usage (:294) keeps the default heading_level (h2) — no change needed there.
    (b) D-07 page_title: add private page_title/2 (:index → Active response, :actions → Action queue, :history → Resolved history) and assign :page_title in handle_params from socket.assigns.live_action.
    (c) D-09/D-10 skeleton + empty gate: wrap the Actions and History lists in the same connected?-keyed skeleton block 48-02 standardized (animate-pulse + aria-busy + aria-live, three h-16 rows, no literal Loading text), and gate each empty state on @socket_connected and Enum.empty?(...) so no empty state renders during the disconnected load.
    Then:
    (d) D-04 router: ensure router_snippet.ex.eex AND the demo router keep /parapet/:id declared LAST (after /parapet/actions, /parapet/history, /parapet/incidents/:id) with the explanatory catch-all-last comment present (this is the regression the 48-01 index_of assert pins).
    (e) D-07 demo layout: patch examples/demo_app/.../components/layouts.ex line 12 to <.live_title suffix=" · Parapet">{assigns[:page_title] || "Demo App"}</.live_title> so the gallery proves runtime titles. Do NOT patch any host root layout. Add one host-install-guide sentence: Parapet pages set :page_title; ensure your root layout's <.live_title> renders {assigns[:page_title]} (locate the install guide doc and add the single sentence).
    Mirror every .eex edit into the demo mirror; pair the router-snippet edit with the demo router.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -10 && mix test test/parapet/operator_ui_integration_test.exs test/parapet/operator_ui_contrast_test.exs 2>&1 | tail -20 && (cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs 2>&1 | tail -25)</automated>
  </verify>
  <acceptance_criteria>
    - Response page has an sr-only h1 Active response; Actions has a visible h1 Action queue; History promotes its descriptive heading to h1 with monotonic heading order; the cockpit incident_summary stays h2.
    - Each rendered page emits exactly one h1 (the 48-01 demo single-h1 asserts go green for /parapet, /parapet/actions, /parapet/history, and the queue-with-selected state).
    - page_title/2 exists and :page_title is assigned in handle_params; the demo layout <.live_title> renders {assigns[:page_title] || "Demo App"} with suffix " · Parapet", so page_title(view) asserts go green.
    - Actions and History lists use the uniform connected?-keyed skeleton (no literal Loading text), and every empty state is gated on @socket_connected and Enum.empty?(...).
    - router_snippet.ex.eex and the demo router keep /parapet/:id LAST with the catch-all-last comment; the 48-01 index_of ordering assert passes (FLOW-04).
    - One host-install-guide sentence about :page_title / <.live_title> is added; no host root layout is auto-patched.
    - Template and demo mirrors carry identical edits; the router-snippet edit is paired with the demo router.
  </acceptance_criteria>
  <done>The shell LiveView (and mirror) gives each page one h1 + a :page_title, wraps Actions/History in the uniform skeleton with socket_connected-gated empties, the route ordering is locked with the regression comment, and the demo layout renders runtime page titles — turning the remaining shell-scoped 48-01 asserts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser URL → `OperatorDetailLive` `:id` param | untrusted, scanner/attacker-controlled string enters via `/parapet/:id` and `/parapet/incidents/:id` |
| host root layout ↔ Parapet `:page_title` assign | Parapet sets an assign the host-owned `<.live_title>` consumes (host-owned seam) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-02 | DoS / Info-disclosure | `Repo.get!` + binary_id dual-exception on malformed `:id` (uncaught 500 + stack trace) | high | mitigate | `fetch_incident_detail/1` prechecks `Ecto.UUID.cast/1` and uses `Repo.get` + nil-guard, collapsing both stale-link classes to `{:error, :not_found}` so malformed ids render the in-page not-found panel instead of a 500 (D-01) — reduces scanner-probe surface and removes the stack-trace leak. |
| T-48-01 | Tampering (reflected XSS) | `@requested_id` passed to `incident_not_found/1` | high | mitigate | The detail LiveView passes `requested_id` as a bare assign into the 48-02 component which renders it HEEx-escaped as text only; this plan must not interpolate it into any URL/attribute (e.g. never `push_navigate` to a `requested_id`-built path). (D-02.) |
| T-48-04 | Information disclosure | not-found panel masking infra outage or implying authz | high | mitigate | Do NOT rescue `DBConnection`/infra errors into the not-found panel (D-03) — infra-unavailable stays the host's 5xx; auth is host-owned (LiveViews are post-authorization), so the panel copy never implies permission-denied. |
| T-48-SC | Tampering | npm/pip/cargo installs | low | accept | No new runtime/test dependency introduced (additive Elixir helper + markup/value edits); no install tasks exist. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/parapet/operator_ui_integration_test.exs test/parapet/operator_ui_contrast_test.exs` — shell/service source-string asserts green.
- `cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs` — single-h1, not-found (unknown + malformed), page_title, skeleton, landmark rendered asserts green.
- `grep` confirms `inspect(` appears only inside `Logger` calls in `operator_detail_live.ex.eex` and its mirror.
- Each `.eex` edit is paired with its demo mirror; the router-snippet edit is paired with the demo router.
</verification>

<success_criteria>
The operator service exposes the additive fetch_incident_detail/1; the detail LiveView renders the in-page not-found panel across mount + 6 refresh sites with its single h1, :page_title, and nav landmark; the shell gives each page one h1 + a :page_title with a uniform skeleton and socket_connected-gated empties; the route ordering is regression-locked; and the demo layout proves runtime titles — turning the shell/service-scoped 48-01 RED assertions green with no inspect leaks, no infra-rescue, and no host-layout auto-patch.
</success_criteria>

<output>
Create `.planning/phases/48-pages-flows-microcopy/48-03-SUMMARY.md` when done, listing the new service/shell symbols, which 48-01 asserts flipped green, the no-inspect-leak + no-infra-rescue + escape verifications, and confirmation of demo-mirror + router parity.
</output>