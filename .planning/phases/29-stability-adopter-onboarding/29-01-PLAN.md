---
phase: 29-stability-adopter-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/recovery.ex
  - docs/stability.md
  - test/mix/tasks/verify_public_api_test.exs
autonomous: true
requirements: [STAB-07]
must_haves:
  truths:
    - "mix verify.public_api classifies Parapet.Recovery as :stable (D-04 regex auto-reclassifies on the moduledoc flip)"
    - "docs/stability.md lists Parapet.Recovery in the Stable Modules table and NOT in the Experimental table (D-01 row move)"
    - "docs/stability.md Deprecation/Compatibility Register names the additive {:short_circuited, reason} and {:conflicted, claim_id} variants of confirm_runbook_step/4 as additive, not-removed-in-1.x (D-06)"
    - "The 4 @callback signatures and __using__/1 macro are unchanged (D-02 frozen surface)"
  artifacts:
    - path: "lib/parapet/recovery.ex"
      provides: "Stable-tier moduledoc admonition + callback-freeze prose (D-01, D-03)"
      contains: "#### Stable {: .info}"
    - path: "docs/stability.md"
      provides: "Recovery row in Stable table + Deprecation Register additive-variant note (D-01, D-06)"
      contains: "Parapet.Recovery"
    - path: "test/mix/tasks/verify_public_api_test.exs"
      provides: "Wave-0 regression case asserting Stable reclassification (STAB-07)"
      contains: "Parapet.Recovery"
  key_links:
    - from: "lib/parapet/recovery.ex moduledoc admonition"
      to: "mix verify.public_api detect_tier_from_text/1"
      via: "regex match on '#### Stable {: .info}'"
      pattern: "####\\s+Stable\\s*\\{:\\s*\\.info\\}"
    - from: "docs/stability.md Stable table"
      to: "machine classification from verify.public_api"
      via: "human-readable mirror — both must move together or the invariant lies silently (D-04 Pitfall 2)"
      pattern: "Parapet\\.Recovery"
---

<objective>
Graduate `Parapet.Recovery` from Experimental → Stable by flipping exactly TWO anchors (D-01) and mirror the additive `confirm_runbook_step/4` return-variant migration note into the Deprecation/Compatibility Register (D-06). This is the code/doc half of STAB-07 (the CHANGELOG half is delivered as the commit body/footer per D-05 — NOT a file edit here).

Purpose: The v1.1 recovery surface joins the v1.0 stability contract so adopters can trust `Parapet.Recovery` under `~> 1.0`. The moduledoc admonition is the machine source of truth (`mix verify.public_api` regex-classifies it); `docs/stability.md` is the human mirror — they MUST move together or the "table mirrors classification" invariant breaks silently (RESEARCH.md Pitfall 2).

Output: Flipped moduledoc admonition + callback-freeze prose in `lib/parapet/recovery.ex`; `Parapet.Recovery` row moved from the Experimental table to the Stable table in `docs/stability.md`; a second row in the Deprecation/Compatibility Register; a Wave-0 regression test case in `test/mix/tasks/verify_public_api_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-stability-adopter-onboarding/29-CONTEXT.md
@.planning/phases/29-stability-adopter-onboarding/29-PATTERNS.md
@.planning/phases/29-stability-adopter-onboarding/29-VALIDATION.md

<frozen_surface>
DO NOT edit the 4 @callback signatures at recovery.ex:30,38,47,56 or the __using__/1 macro at recovery.ex:59-63 (D-02). DO NOT hand-edit CHANGELOG.md (D-05 — Release-Please-owned). DO NOT edit lib/mix/tasks/verify.public_api.ex (D-04 — its regex auto-reclassifies; only ADD a test case to the existing test file).
</frozen_surface>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip recovery.ex moduledoc admonition to Stable + add callback-freeze prose</name>
  <files>lib/parapet/recovery.ex</files>
  <read_first>
    - lib/parapet/recovery.ex (the file being modified — read the full moduledoc and confirm the Experimental admonition is at lines 16-20 and the 4 @callbacks at :30,38,47,56)
    - docs/stability.md lines 10-13 (the VERBATIM tier-callout strings — copy the Stable string exactly)
    - 29-PATTERNS.md § "lib/parapet/recovery.ex (modified — moduledoc admonition flip)" (the flip target + regex requirement)
    - lib/parapet/integration.ex moduledoc (the Stable-tier prose tone to mirror per D-03/Claude's Discretion)
  </read_first>
  <action>
    Replace the Experimental admonition block at recovery.ex:16-20. The first line MUST become the verbatim string `> #### Stable {: .info}` (with the curly-brace `{: .info}` syntax — NOT bare `info`) so verify.public_api's detect_tier_from_text/1 regex `~r/####\s+Stable\s*\{:\s*\.info\}/` matches. Replace the body prose: state that this module is Stable in v1.x, its API will not change in a breaking way within 1.x, and link to the Stability & Deprecation Policy (stability.html) — mirror the Stable tone in lib/parapet/integration.ex.
    Per D-03, add callback-freeze prose (as MODULEDOC PROSE, not code): a sentence stating the four callbacks `id/0`, `label/0`, `preview/2`, `execute/2` are now part of the Stable contract and frozen — no required callback will be added within 1.x. Do NOT add new module attributes, `@deprecated`, or runtime guards. Do NOT touch the @callback definitions at :30,38,47,56 or the __using__/1 macro at :59-63 (D-02 frozen surface).
  </action>
  <verify>
    <automated>grep -c "#### Stable {: .info}" lib/parapet/recovery.ex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "#### Stable {: .info}" lib/parapet/recovery.ex` matches (the Stable admonition string is present verbatim).
    - `grep -c "#### Experimental" lib/parapet/recovery.ex` returns 0 (the Experimental admonition is gone).
    - `grep -c "@callback" lib/parapet/recovery.ex` returns 4 (the frozen callbacks are unchanged in count).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>recovery.ex moduledoc carries the verbatim Stable admonition + callback-freeze prose; the 4 @callbacks and __using__/1 are byte-for-byte unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Move the Parapet.Recovery row to the Stable table + add the Deprecation Register additive-variant note</name>
  <files>docs/stability.md</files>
  <read_first>
    - docs/stability.md (the file being modified — read the Stable Modules table at :24-38, the Experimental row at :49, the outcome-vocabulary-freeze doctrine at :144, and the Deprecation/Compatibility Register at :190-198)
    - 29-PATTERNS.md § "docs/stability.md (modified — row move + compat note)" (exact source row text + register table shape)
    - lib/parapet/operator.ex confirm_runbook_step/4 return clauses (confirm the {:short_circuited, reason} and {:conflicted, claim_id} variant names the note must reference — landed in Phase 25)
  </read_first>
  <action>
    Move the `| `Parapet.Recovery` | Host-app-facing recovery action behaviour + activation function |` row OUT of the Experimental Modules table (currently line :49 — DELETE it there, do not leave it behind, do not duplicate) and INTO the Stable Modules table block (:24-38), placed in the table's existing ordering. Per D-03/Claude's Discretion, extend the Stable-row description to name that the 4 callbacks are part of the frozen Stable contract.
    Add a SECOND row to the Deprecation/Compatibility Register table (below the existing `Parapet.SLO.define/2` row at :197). Content per D-06: adopters who pattern-match `Parapet.Operator.confirm_runbook_step/4` must handle the additive `{:short_circuited, reason}` and `{:conflicted, claim_id}` return variants; these are ADDITIVE and will NOT be removed in 1.x. Phrase it as a concrete instance of the outcome-vocabulary-freeze doctrine already at :144. Use "Compatibility note" / "Additive" framing in the Kind column rather than "deprecated" (nothing is being deprecated — this is an additive-variant heads-up).
  </action>
  <verify>
    <automated>grep -c "Parapet.Recovery" docs/stability.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Parapet.Recovery" docs/stability.md` returns exactly 1 (the row exists once — moved, not duplicated).
    - The single `Parapet.Recovery` match falls within the Stable Modules table block (:24-38 region), NOT the Experimental table — verify by reading the surrounding lines.
    - `grep -n "short_circuited" docs/stability.md` and `grep -n "conflicted" docs/stability.md` both match within the Deprecation/Compatibility Register block (near :190-198).
    - `grep -n "confirm_runbook_step" docs/stability.md` matches in the new register row.
  </acceptance_criteria>
  <done>Recovery row lives only in the Stable table; the Deprecation/Compatibility Register has a new additive-variant row naming the two new confirm_runbook_step/4 tuples and the no-removal-in-1.x promise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Wave-0 regression case — verify.public_api classifies Recovery as Stable</name>
  <files>test/mix/tasks/verify_public_api_test.exs</files>
  <behavior>
    - detect_tier_from_text/1 returns :stable when given the verbatim `> #### Stable {: .info}` admonition string.
    - After the moduledoc flip (Task 1), Parapet.Recovery is classified in the :stable tier (not :experimental) by the task's classifier — assert against the public detect_tier_from_text/1 helper and/or the task's module-scan output, mirroring how existing cases in this file assert tiers.
  </behavior>
  <read_first>
    - test/mix/tasks/verify_public_api_test.exs (the file being modified — read existing test cases to mirror the assertion style: how it calls detect_tier_from_text/1 and/or scans modules)
    - lib/mix/tasks/verify.public_api.ex:104-114 (detect_tier_from_text/1 — the function under test; DO NOT edit this file, D-04)
    - 29-VALIDATION.md Per-Task Verification Map row "Recovery reclassifies to :stable after moduledoc flip" (the acceptance contract)
  </read_first>
  <action>
    Add a test case to the existing test/mix/tasks/verify_public_api_test.exs. Assert `Mix.Tasks.Verify.PublicApi.detect_tier_from_text("> #### Stable {: .info}")` returns `:stable` (mirror the existing detect_tier_from_text/1 cases in this file for call shape). If the file already exercises Parapet.Recovery's live classification via a module scan, add/adjust an assertion that Parapet.Recovery now resolves to :stable post-flip. Do NOT edit verify.public_api.ex itself — the regex already handles this (D-04); this task only adds the regression guard so a future moduledoc regression is caught.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/verify_public_api_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mix/tasks/verify_public_api_test.exs` exits 0.
    - The new case fails if the recovery.ex admonition is reverted to Experimental (the regression guard is real, not a tautology) — confirm the assertion targets the Stable string / Recovery classification, not a constant.
    - `git diff --stat lib/mix/tasks/verify.public_api.ex` shows no changes (D-04: task code is untouched).
  </acceptance_criteria>
  <done>verify_public_api_test.exs has a passing case asserting the Stable classification; verify.public_api.ex is unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits a moduledoc, a reference doc table, and a test file. No untrusted input crosses any boundary; no runtime code path changes; no dependency installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Information Disclosure | docs/stability.md (public stability claim) | accept | A doc that misstates a module's tier is a correctness bug, not a security threat. The verify.public_api regression test (Task 3) + the Pitfall-2 "move both together" rule mitigate the lie-risk. Residual risk: none security-relevant. |
| T-29-02 | Tampering | lib/parapet/recovery.ex (frozen callback surface) | mitigate | Task 1 edits only the moduledoc; acceptance criteria assert `@callback` count == 4 and __using__/1 is untouched, preventing accidental surface drift. |
| T-29-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase. Zero new dependencies (v1.1 constraint). |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/mix/tasks/verify_public_api_test.exs` exits 0.
- `mix verify.public_api` exits 0 and shows `Parapet.Recovery` in the Stable section (auto-reclassified by the moduledoc flip, D-04).
- `grep -c "Parapet.Recovery" docs/stability.md` returns 1, within the Stable table.
- `git diff --stat lib/mix/tasks/verify.public_api.ex CHANGELOG.md` shows no changes (D-04, D-05).
</verification>

<success_criteria>
- `Parapet.Recovery` is classified Stable by both the machine (`mix verify.public_api`) and the human mirror (`docs/stability.md` Stable table) — the three-way invariant moved together (Pitfall 2 avoided).
- The 4 callbacks and __using__/1 are byte-for-byte unchanged (D-02).
- The Deprecation/Compatibility Register names the additive `confirm_runbook_step/4` variants and the no-removal-in-1.x promise (D-06).
- CHANGELOG.md is untouched (D-05) — the migration note rides the commit body/footer at phase-commit time.
</success_criteria>

<output>
Create `.planning/phases/29-stability-adopter-onboarding/29-01-SUMMARY.md` when done.

**Commit-body reminder (D-05/D-06):** When the phase feat commit is authored, include in the Conventional-Commit body/footer the migration note: adopters who pattern-match `Parapet.Operator.confirm_runbook_step/4` must handle the additive `{:short_circuited, reason}` and `{:conflicted, claim_id}` variants — additive, not removed in 1.x. Release Please renders commit bodies into the generated CHANGELOG entry. Do NOT hand-edit CHANGELOG.md.
</output>
