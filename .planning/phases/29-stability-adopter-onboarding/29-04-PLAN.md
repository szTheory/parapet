---
phase: 29-stability-adopter-onboarding
plan: 04
type: execute
wave: 2
depends_on: [29-01, 29-02, 29-03]
files_modified:
  - docs/recovery-actions.md
  - mix.exs
  - docs/getting-started.md
  - docs/operator-ui.md
autonomous: true
requirements: [ADOP-03]
must_haves:
  truths:
    - "docs/recovery-actions.md exists and explains capability authoring (incl. `mix parapet.gen.recovery`), the Preview/Confirm UX (referencing operator-ui.md:179-208, not re-deriving it), and the error semantics :short_circuited / :conflicted / :recovery_failed (D-14)"
    - "The guide walks through FOUR worked examples — one per capability-backed playbook (:retry_async_item, :requeue_dead_letter, :revert_feature_flag, :disable_metric_label); the two guidance-only playbooks are excluded (D-15)"
    - "docs/recovery-actions.md is registered in BOTH the `extras` list AND the `groups_for_extras: Guides` group in mix.exs — omitting either renders nothing in HexDocs while the build stays green (D-16, Pitfall 1)"
    - "Cross-links exist: a Next-steps bullet in getting-started.md and a forward-reference in operator-ui.md's Preview-First Recovery section (D-17)"
    - "`mix docs` builds with no undefined/broken-reference warnings for recovery-actions.md (Pitfall 1)"
    - "Only mix.exs ExDoc config changes — NO `files:` edit (the docs glob already ships docs/*.md) (D-16)"
  artifacts:
    - path: "docs/recovery-actions.md"
      provides: "Adopter guide following slo-authoring-guide.md structure (D-14, D-15)"
      contains: "mix parapet.gen.recovery"
      min_lines: 120
    - path: "mix.exs"
      provides: "recovery-actions.md in extras + Guides group (D-16)"
      contains: "docs/recovery-actions.md"
    - path: "docs/getting-started.md"
      provides: "Next-steps cross-link to recovery-actions.md (D-17)"
      contains: "recovery-actions"
    - path: "docs/operator-ui.md"
      provides: "Forward-reference to recovery-actions.md from Preview-First Recovery (D-17)"
      contains: "recovery-actions"
  key_links:
    - from: "mix.exs extras list"
      to: "mix.exs groups_for_extras: Guides"
      via: "docs/recovery-actions.md must appear in BOTH (two-step ExDoc registration; either alone = silent failure)"
      pattern: "docs/recovery-actions\\.md"
    - from: "docs/recovery-actions.md"
      to: "docs/operator-ui.md:179-208 Preview-First Recovery"
      via: "reference (not re-derivation) of the Preview/Confirm UX"
      pattern: "operator-ui|Preview"
---

<objective>
Write `docs/recovery-actions.md` — the adopter guide for authoring host recovery capabilities — and wire it into ExDoc + cross-link it from the existing guides (D-14..D-17, ADOP-03). This is the DOCS half of the phase and lands in Wave 2 AFTER the code surfaces (Plans 01–03) so the guide never references an uncompilable or nonexistent surface (D-18, the v0.10 "code lands before docs" pattern).

Purpose: Close the "shipped ≠ adopted" gap (LEARN-22-C) by giving adopters a single authoritative guide that names the now-Stable `Parapet.Recovery` (Plan 01), the `mix parapet.gen.recovery` generator (Plan 02), and the Preview/Confirm error semantics — with four concrete worked examples.

Output: `docs/recovery-actions.md` (the guide), ExDoc wiring in `mix.exs` (extras + Guides group only — NO files: edit), and cross-links in `docs/getting-started.md` + `docs/operator-ui.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-stability-adopter-onboarding/29-CONTEXT.md
@.planning/phases/29-stability-adopter-onboarding/29-PATTERNS.md
@.planning/phases/29-stability-adopter-onboarding/29-VALIDATION.md

<upstream_summaries>
<!-- This plan NAMES the surfaces those plans ship. Read their SUMMARYs to use the exact emitted names. -->
@.planning/phases/29-stability-adopter-onboarding/29-01-SUMMARY.md
@.planning/phases/29-stability-adopter-onboarding/29-02-SUMMARY.md
@.planning/phases/29-stability-adopter-onboarding/29-03-SUMMARY.md
</upstream_summaries>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/recovery-actions.md adopter guide</name>
  <files>docs/recovery-actions.md</files>
  <read_first>
    - docs/slo-authoring-guide.md (the STRUCTURAL + tonal analog — headings at :1 (title/decision-frame intro), :9 (decision-frame), :39 (authoring), :57 (patterns), :137 (what-not-to-do); mirror this left-to-right ordering and the conviction-statement opening)
    - docs/operator-ui.md:179-208 (the Preview-First Recovery section — REFERENCE it, do NOT re-derive the Preview/Confirm UX, D-14)
    - lib/parapet/recovery.ex (the now-Stable behaviour + 4 frozen callbacks — describe id/0, label/0, preview/2, execute/2 accurately)
    - lib/parapet/capabilities.ex:14-20 (the 5-atom allowlist — the worked examples must use the 4 capability-backed atoms only)
    - priv/templates/parapet.gen.runbooks/ (the four capability-backed playbook templates from Phase 27: stalled async :retry_async_item, dead-letter drain :requeue_dead_letter, deploy-tied :revert_feature_flag, cardinality blowout :disable_metric_label — the 1:1 source for the 4 worked examples, D-15)
    - 29-PATTERNS.md § "docs/recovery-actions.md (new adopter guide)" (the section map + worked-example list + what-not-to-do mirror)
    - 29-02-SUMMARY.md (the exact gen.recovery invocation + scaffold paths to cite verbatim) and 29-03-SUMMARY.md (the exact doctor `recovery` check messages to reference)
  </read_first>
  <action>
    Create docs/recovery-actions.md mirroring slo-authoring-guide.md's structure. Sections (per 29-PATTERNS.md map):
      - Title + conviction-statement intro ("# Parapet Recovery Actions Guide").
      - "When to wire a recovery capability" — decision frame (when an automated mitigation helps vs. when guidance-only is correct; note that 2 of the 6 shipped playbooks are guidance-only by design).
      - "Authoring a recovery capability" — the `mix parapet.gen.recovery <NAME>` generator (cite the exact scaffold paths from 29-02-SUMMARY.md), the 4 frozen callbacks (id/0 must return one of the 5 allowlisted atoms; label/0; preview/2 returning {:ok, preview_map}; execute/2 returning {:ok, result}|{:error, reason}), attaching via Parapet.Recovery.attach/1 at boot, and the `mix parapet.doctor` recovery adoption signal (cite the check from 29-03-SUMMARY.md).
      - "Preview/Confirm UX" — REFERENCE operator-ui.md's Preview-First Recovery section (link to it; do not re-derive); summarize the operator flow at a pointer level.
      - "Error semantics" — explain `{:short_circuited, reason}` (breaker open / incident state changed / preview expired), `{:conflicted, claim_id}` (another node holds the claim), and the `:recovery_failed` TimelineEntry type (capability execute/2 returned {:error, _}). Note these are the additive variants now part of the Stable contract (cross-reference docs/stability.md).
      - "Worked examples" — FOUR examples, one per capability-backed playbook (D-15): Stalled Async (`:retry_async_item`), Dead-Letter Drain (`:requeue_dead_letter`), Deploy-Tied Incident (`:revert_feature_flag`), Cardinality Blowout (`:disable_metric_label`). Each shows a realistic preview map (count, target_refs, preconditions, warnings, summary) and an execute sketch. Do NOT include the two guidance-only playbooks (retry storm, suppression drift — nothing to author).
      - "What not to do" — mirror slo-authoring-guide.md:137-145 with recovery anti-patterns (e.g. "Do not make a capability swallow errors silently"; "Do not use a capability as a generic admin escape hatch"; "Do not register a capability id outside the 5-atom allowlist").
    Use relative `.html`/`.md` links that ExDoc resolves (e.g. `operator-ui.html`, `stability.html`) consistent with how other docs/*.md guides cross-link, to avoid undefined-reference warnings (Pitfall 1).
  </action>
  <verify>
    <automated>test -f docs/recovery-actions.md && grep -c "mix parapet.gen.recovery" docs/recovery-actions.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/recovery-actions.md` succeeds and the file is at least ~120 lines (substantive guide, not a stub).
    - `grep -c "mix parapet.gen.recovery" docs/recovery-actions.md` >= 1.
    - All four capability atoms are present: `grep -c ":retry_async_item" && :requeue_dead_letter && :revert_feature_flag && :disable_metric_label` each match.
    - The two guidance-only playbooks are NOT presented as worked examples: confirm the "Worked examples" section names exactly the four capability-backed ones.
    - `grep -niE "short_circuited|conflicted|recovery_failed" docs/recovery-actions.md` matches all three error-semantics terms.
    - The Preview/Confirm section links to operator-ui (references, does not re-derive): `grep -i "operator-ui" docs/recovery-actions.md` matches.
  </acceptance_criteria>
  <done>recovery-actions.md follows the slo-authoring-guide structure, covers authoring + Preview/Confirm (by reference) + the three error semantics + four worked examples (capability-backed only) + a what-not-to-do section.</done>
</task>

<task type="auto">
  <name>Task 2: Wire recovery-actions.md into ExDoc (extras + Guides group) — mix.exs only</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (the file being modified — read the `files:` glob at :43 to CONFIRM no edit is needed (docs/*.md already ships), the `extras` list at :59-80, and the `groups_for_extras: Guides` group at :84-92)
    - 29-PATTERNS.md § "mix.exs (modified — extras + groups_for_extras)" (the exact two lists + the both-or-silent-failure rule)
  </read_first>
  <action>
    Add `"docs/recovery-actions.md"` to BOTH:
      1. the `extras:` list (mix.exs:59-80) — place it alongside the other docs/*.md Guides entries (e.g. after `"docs/slo-authoring-guide.md"`).
      2. the `groups_for_extras: [Guides: [...]]` list (mix.exs:84-92) — add it to the `Guides:` group alongside operator-ui.md / slo-authoring-guide.md / troubleshooting.md.
    Do NOT edit the `files:` glob at :43 (D-16 — the `docs` entry already ships every docs/*.md to Hex; editing it is unnecessary and out of scope). Both lists MUST change in the same commit (Pitfall 1 — extras-only renders nothing in HexDocs; groups-only errors the build).
  </action>
  <verify>
    <automated>grep -c "docs/recovery-actions.md" mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "docs/recovery-actions.md" mix.exs` returns exactly 2 (one in `extras`, one in the `Guides` group).
    - Reading the surrounding lines confirms one match is inside the `extras:` list and one is inside the `groups_for_extras:` Guides list.
    - `git diff mix.exs` shows NO change to the `files:` glob at :43 (D-16).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>recovery-actions.md is registered in both the extras list and the Guides group; the files: glob is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add cross-links in getting-started.md and operator-ui.md + verify mix docs build</name>
  <files>docs/getting-started.md, docs/operator-ui.md</files>
  <read_first>
    - docs/getting-started.md:94-99 (the "## Next steps" bullet list — the insertion point for the 5th bullet)
    - docs/operator-ui.md:179-208 (the "Preview-First Recovery" section — the end-of-section/Named-Capabilities insertion point for a forward-reference sentence)
    - 29-PATTERNS.md § "docs/getting-started.md" and § "docs/operator-ui.md" (the exact bullet + sentence patterns to mirror)
  </read_first>
  <action>
    In docs/getting-started.md, add a 5th bullet to the "## Next steps" list (after the existing bullets at :94-99): `- [Recovery Actions Guide](docs/recovery-actions.md) — author host recovery capabilities and wire them to runbook steps` (match the existing bullets' relative-link + em-dash-description style).
    In docs/operator-ui.md, after the Preview-First Recovery section ends (~:208, after the Named Capabilities prose), add a forward-reference sentence: `See the [Recovery Actions Guide](recovery-actions.html) for step-by-step authoring instructions, worked examples, and the error semantics for each capability outcome.` (use the `.html` form consistent with intra-doc ExDoc links in operator-ui.md).
    Then run `mix docs` and confirm recovery-actions.md renders with no undefined/broken-reference warnings (Pitfall 1). If `mix docs` reports an undefined reference for any link in recovery-actions.md or the new cross-links, fix the link form (relative `.html` vs `.md`) to match the convention the build accepts.
  </action>
  <verify>
    <automated>grep -c "recovery-actions" docs/getting-started.md docs/operator-ui.md && mix docs 2>&1 | grep -i "undefined\|broken" | grep -i "recovery-actions"; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep "recovery-actions" docs/getting-started.md` matches (Next-steps bullet present).
    - `grep "recovery-actions" docs/operator-ui.md` matches (forward-reference present).
    - `mix docs` exits 0 and `mix docs 2>&1 | grep -i "undefined\|broken"` returns NOTHING referencing recovery-actions.md.
    - After `mix docs`, `ls doc/recovery-actions.html` succeeds (the guide rendered).
  </acceptance_criteria>
  <done>Both cross-links are in place; `mix docs` builds clean and renders doc/recovery-actions.html with the guide in the Guides sidebar group.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits documentation files and ExDoc config only. No runtime code path, no untrusted input, no dependency installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Information Disclosure | docs/recovery-actions.md (public guide) | accept | A docs surface carries no runtime threat (N/A per security_threat_model). The guide describes the host-owned, allowlist-bounded capability model; it introduces no secret material. Residual risk: none security-relevant. |
| T-29-08 | (build correctness, not security) | mix.exs ExDoc wiring | accept | The both-lists-or-silent-failure trap (Pitfall 1) is a doc-rendering correctness bug, mitigated by Task 2's grep==2 acceptance + Task 3's `mix docs` clean-build gate. Not a security threat. |
| T-29-SC | Tampering | npm/pip/cargo installs | N/A | No package installs; zero new dependencies (v1.1 constraint). |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix docs` exits 0 with no undefined/broken-reference warnings for recovery-actions.md; `doc/recovery-actions.html` exists.
- `grep -c "docs/recovery-actions.md" mix.exs` returns 2; `files:` glob unchanged (D-16).
- `grep "recovery-actions" docs/getting-started.md docs/operator-ui.md` both match (D-17).
- recovery-actions.md presents exactly the four capability-backed worked examples (D-15) and references operator-ui.md's Preview/Confirm UX rather than re-deriving it (D-14).
</verification>

<success_criteria>
- `docs/recovery-actions.md` exists, explains capability authoring + Preview/Confirm (by reference) + the three error semantics, and walks four worked examples (capability-backed playbooks only) (ADOP-03 / D-14 / D-15).
- The guide is wired into ExDoc via BOTH extras and the Guides group (D-16, Pitfall 1) with no files: edit.
- Cross-links exist in getting-started.md and operator-ui.md (D-17).
- `mix docs` builds clean and renders the guide in the Guides sidebar.
</success_criteria>

<output>
Create `.planning/phases/29-stability-adopter-onboarding/29-04-SUMMARY.md` when done.

**Manual sidebar check (folded from VALIDATION.md Manual-Only):** After `mix docs`, the executor should confirm `doc/recovery-actions.html` lists "Recovery Actions" under the Guides group in the sidebar — `ls doc/recovery-actions.html` + a one-line visual confirm in the SUMMARY is sufficient; no blocking checkpoint required.
</output>
