---
phase: 44-foundations-token-re-skin-fonts-audit-apparatus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/static/parapet/fonts/IBMPlexSans-Regular-latin.woff2
  - priv/static/parapet/fonts/IBMPlexSans-Medium-latin.woff2
  - priv/static/parapet/fonts/IBMPlexSans-SemiBold-latin.woff2
  - priv/static/parapet/fonts/IBMPlexMono-Regular-latin.woff2
  - priv/static/parapet/fonts/IBMPlexMono-Medium-latin.woff2
  - priv/static/parapet/fonts/LICENSE.txt
  - mix.exs
  - lib/mix/tasks/parapet.gen.ui.ex
  - examples/demo_app/lib/demo_app_web.ex
  - test/parapet/operator_ui_fonts_test.exs
autonomous: true
requirements: [FONT-01, FONT-03]
must_haves:
  truths:
    - "Five subsetted IBM Plex woff2 faces exist under priv/static/parapet/fonts/ (latin subset)"
    - "Total vendored font bytes are within the 150 KB ceiling"
    - "The IBM Plex OFL 1.1 LICENSE.txt ships alongside the fonts"
    - "The Hex files: whitelist explicitly lists the woff2 faces and the LICENSE.txt"
    - "Running the generator copies the vendored woff2 into the host priv/static path, refreshing on re-run"
    - "The demo app serves /parapet/* static assets (static_paths includes parapet)"
  artifacts:
    - path: "priv/static/parapet/fonts/IBMPlexSans-Regular-latin.woff2"
      provides: "IBM Plex Sans 400 latin woff2 face"
    - path: "priv/static/parapet/fonts/IBMPlexSans-Medium-latin.woff2"
      provides: "IBM Plex Sans 500 latin woff2 face"
    - path: "priv/static/parapet/fonts/IBMPlexSans-SemiBold-latin.woff2"
      provides: "IBM Plex Sans 600 latin woff2 face"
    - path: "priv/static/parapet/fonts/IBMPlexMono-Regular-latin.woff2"
      provides: "IBM Plex Mono 400 latin woff2 face"
    - path: "priv/static/parapet/fonts/IBMPlexMono-Medium-latin.woff2"
      provides: "IBM Plex Mono 500 latin woff2 face"
    - path: "priv/static/parapet/fonts/LICENSE.txt"
      provides: "IBM Plex OFL 1.1 license text"
    - path: "test/parapet/operator_ui_fonts_test.exs"
      provides: "FONT-01 budget/existence/license gate"
      contains: "153_600"
    - path: "lib/mix/tasks/parapet.gen.ui.ex"
      provides: "Generator run/1 override that copies fonts to host priv/static"
      contains: "File.cp!"
  key_links:
    - from: "lib/mix/tasks/parapet.gen.ui.ex"
      to: "priv/static/parapet/fonts/"
      via: "copy_fonts_to_host reads :code.priv_dir(:parapet)/static/parapet/fonts and File.cp! into host priv/static"
      pattern: "File\\.cp!"
    - from: "examples/demo_app/lib/demo_app_web.ex"
      to: "priv/static/parapet/fonts/"
      via: "static_paths includes \"parapet\" so Plug.Static serves /parapet/fonts/*.woff2"
      pattern: "static_paths.*parapet"
---

<objective>
Vendor the five subsetted IBM Plex woff2 faces (Sans 400/500/600 + Mono 400/500, latin subset) plus the OFL 1.1 license under `priv/static/parapet/fonts/`, whitelist them in the Hex package, teach the generator to copy them into a host's `priv/static`, wire the demo app to serve `/parapet/*`, and pin a Wave-0 font budget/existence test.

Purpose: This is the font foundation (FONT-01, FONT-03) that the `@font-face` wiring in Plan 02 and the contrast/demo-contract assertions in Plan 04 depend on. The woff2 binaries must exist on disk before those tests can pass.
Output: 5 woff2 faces + LICENSE.txt committed, `mix.exs` files: whitelist extended, generator `run/1` override, demo `static_paths` updated, new `operator_ui_fonts_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-CONTEXT.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-RESEARCH.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan introduces:
- New font asset paths: `priv/static/parapet/fonts/{IBMPlexSans-Regular-latin,IBMPlexSans-Medium-latin,IBMPlexSans-SemiBold-latin,IBMPlexMono-Regular-latin,IBMPlexMono-Medium-latin}.woff2`
- New vendored license: `priv/static/parapet/fonts/LICENSE.txt`
- New generator behavior: `copy_fonts_to_host/0` private function + `run/1` override in `Mix.Tasks.Parapet.Gen.Ui`
- New test module: `Parapet.OperatorUIFontsTest` (`test/parapet/operator_ui_fonts_test.exs`)
- New demo static path entry: `"parapet"` in `DemoAppWeb.static_paths/0`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Download IBM Plex TTFs, subset to five latin woff2, vendor woff2 + OFL license</name>
  <files>priv/static/parapet/fonts/IBMPlexSans-Regular-latin.woff2, priv/static/parapet/fonts/IBMPlexSans-Medium-latin.woff2, priv/static/parapet/fonts/IBMPlexSans-SemiBold-latin.woff2, priv/static/parapet/fonts/IBMPlexMono-Regular-latin.woff2, priv/static/parapet/fonts/IBMPlexMono-Medium-latin.woff2, priv/static/parapet/fonts/LICENSE.txt</files>
  <read_first>
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-RESEARCH.md (sections "Font Subsetting — Verified Mechanics", "Environment Availability", "Verified Budget")
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-CONTEXT.md (D-09, D-10)
  </read_first>
  <action>
    Fetch the five official IBM Plex latin TTFs from the IBM/plex repo (master branch) at these exact paths (D-09 corrected path — NO `unhinted/` subfolder for latin): `packages/plex-sans/fonts/complete/ttf/IBMPlexSans-Regular.ttf`, `IBMPlexSans-Medium.ttf`, `IBMPlexSans-SemiBold.ttf`, and `packages/plex-mono/fonts/complete/ttf/IBMPlexMono-Regular.ttf`, `IBMPlexMono-Medium.ttf`. Use `https://raw.githubusercontent.com/IBM/plex/master/<path>` via curl into the scratch dir. Also fetch `packages/plex-sans/LICENSE.txt` as the OFL 1.1 license.
    Subset each TTF to woff2 using `python3 -m fontTools.subset` (module form for PATH portability; `/Users/jon/Library/Python/3.14/bin/pyftsubset` is the binary form). Use the exact flags from RESEARCH.md "Subsetting Command": `--flavor=woff2`, the documented `--unicodes` latin range, `--layout-features="kern,liga,clig,calt,ccmp,locl,mark,mkmk"`, `--desubroutinize --no-hinting --drop-tables+=DSIG --notdef-outline --recalc-bounds`. The `WARNING: meta NOT subset` message is expected and harmless. `font-display` is CSS-side, not a subset flag.
    Output filenames (these exact names are referenced by @font-face in Plan 02 and the test below): `IBMPlexSans-Regular-latin.woff2`, `IBMPlexSans-Medium-latin.woff2`, `IBMPlexSans-SemiBold-latin.woff2`, `IBMPlexMono-Regular-latin.woff2`, `IBMPlexMono-Medium-latin.woff2`. Create the dir `priv/static/parapet/fonts/` and place all five woff2 there plus `LICENSE.txt`. Commit the woff2 as binaries (never via any text/EEx path). Expected total ≈58.5 KB (verified); ceiling ≤150 KB — if total exceeds the ceiling the `--unicodes` scope is mis-set, narrow it.
  </action>
  <verify>
    <automated>ls -l priv/static/parapet/fonts/*.woff2 | wc -l | grep -q 5 && du -ck priv/static/parapet/fonts/*.woff2 | tail -1 | awk '{exit ($1 <= 150) ? 0 : 1}' && test -f priv/static/parapet/fonts/LICENSE.txt</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist: `priv/static/parapet/fonts/IBMPlexSans-Regular-latin.woff2`, `IBMPlexSans-Medium-latin.woff2`, `IBMPlexSans-SemiBold-latin.woff2`, `IBMPlexMono-Regular-latin.woff2`, `IBMPlexMono-Medium-latin.woff2`
    - `priv/static/parapet/fonts/LICENSE.txt` exists and contains the IBM Plex OFL 1.1 text (begins with "Copyright" and references "SIL Open Font License")
    - Combined size of the five woff2 ≤ 153600 bytes (150 KB)
    - Each woff2 is a non-zero binary that begins with the woff2 magic signature `wOF2` (first 4 bytes)
  </acceptance_criteria>
  <done>Five latin-subset IBM Plex woff2 faces + OFL LICENSE.txt are vendored under priv/static/parapet/fonts/, total ≤150 KB.</done>
</task>

<task type="auto">
  <name>Task 2: Whitelist fonts in mix.exs, add generator font-copy step, wire demo static_paths, add font budget test</name>
  <files>mix.exs, lib/mix/tasks/parapet.gen.ui.ex, examples/demo_app/lib/demo_app_web.ex, test/parapet/operator_ui_fonts_test.exs</files>
  <read_first>
    - mix.exs (the `files:` key — RESEARCH.md "Complete File Inventory" notes lines 42–44)
    - lib/mix/tasks/parapet.gen.ui.ex (existing `info/2`, `igniter/1`; PATTERNS.md "lib/mix/tasks/parapet.gen.ui.ex" section)
    - examples/demo_app/lib/demo_app_web.ex (the `static_paths/0` function — PATTERNS.md section)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-PATTERNS.md (sections for mix.exs, generator, static_paths, and `test/parapet/operator_ui_fonts_test.exs`)
    - test/parapet/operator_ui_contrast_test.exs (ExUnit module structure analog: `use ExUnit.Case, async: true`)
  </read_first>
  <action>
    In `mix.exs`, extend the existing `files:` `~w(...)` list to explicitly include `priv/static/parapet/fonts/*.woff2` and `priv/static/parapet/fonts/LICENSE.txt` (additive — keep the existing `priv` glob and every other entry; this is belt-and-suspenders so the fonts cannot be silently pruned per D-10).
    In `lib/mix/tasks/parapet.gen.ui.ex`, add a `run/1` override (which is `defoverridable` in `Igniter.Mix.Task` per Igniter 0.7.9) that calls `super(argv)` first (runs the full Igniter pipeline / existing `copy_template` calls untouched) then calls a new private `copy_fonts_to_host/0`. `copy_fonts_to_host/0` resolves `source_dir = Path.join(:code.priv_dir(:parapet), "static/parapet/fonts")`, `dest_dir = Path.join(File.cwd!(), "priv/static/parapet/fonts")`, `File.mkdir_p!(dest_dir)`, then `File.cp!` each file (overwrite semantics so re-runs REFRESH fonts per D-12 — never `:skip`), and emits a `Mix.shell().info` line. Use `File.cp!` ONLY — never `Igniter.create_new_file` for the binary woff2 (it corrupts binary via UTF-8 string encoding).
    In `examples/demo_app/lib/demo_app_web.ex`, add `"parapet"` to the `static_paths/0` `~w(...)` list (additive) so Phoenix Plug.Static serves `/parapet/fonts/*.woff2`.
    Create `test/parapet/operator_ui_fonts_test.exs` as module `Parapet.OperatorUIFontsTest` with `use ExUnit.Case, async: true`, a `@font_dir "priv/static/parapet/fonts"`, `@font_budget_bytes 153_600`, and `@font_files` listing the five woff2 names. Add tests: (a) all five woff2 exist, (b) LICENSE.txt exists, (c) summed byte size ≤ `@font_budget_bytes`, (d) each woff2 size > 0. Follow the PATTERNS.md `operator_ui_fonts_test.exs` skeleton.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_fonts_test.exs && grep -q "parapet" examples/demo_app/lib/demo_app_web.ex && grep -q "File.cp!" lib/mix/tasks/parapet.gen.ui.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/parapet/operator_ui_fonts_test.exs` passes (4 tests green)
    - `mix.exs` `files:` list contains both `priv/static/parapet/fonts/*.woff2` and `priv/static/parapet/fonts/LICENSE.txt`
    - `lib/mix/tasks/parapet.gen.ui.ex` defines `run/1` that calls `super(argv)` and a `copy_fonts_to_host/0` using `File.cp!` (grep `File.cp!` returns a match); the existing `igniter/1` and its `copy_template` calls are unchanged
    - `examples/demo_app/lib/demo_app_web.ex` `static_paths/0` includes `"parapet"`
    - `test/parapet/operator_ui_fonts_test.exs` references `153_600` and lists all five woff2 filenames
  </acceptance_criteria>
  <done>Fonts are whitelisted for Hex, the generator refreshes them into host priv/static on every run, the demo serves them, and the FONT-01 budget gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IBM/plex GitHub → repo | Third-party font binaries enter the package supply chain |
| Hex package → host app | Vendored woff2 + license shipped to operator deployments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | Vendored woff2 binaries | mitigate | Fetch only from official `raw.githubusercontent.com/IBM/plex/master`; subset locally with fonttools; `operator_ui_fonts_test.exs` asserts existence, woff2 magic, and ≤150 KB budget so a corrupt/wrong-size binary fails CI |
| T-44-02 | Information Disclosure / License | Shipping fonts without OFL license | mitigate | Vendor OFL 1.1 `LICENSE.txt` next to the fonts (D-10); `mix.exs` `files:` whitelist lists both woff2 and LICENSE.txt explicitly so neither can be silently pruned; font test asserts LICENSE.txt presence |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase; fonttools/brotli are pre-installed dev tools (RESEARCH.md Environment Availability), not new runtime deps. No new Parapet runtime dependency added |
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_fonts_test.exs` green
- `du -ck priv/static/parapet/fonts/*.woff2` total ≤ 150 KB
- No new entry in `mix.exs` `deps/0` (zero new runtime deps)
</verification>

<success_criteria>
Five latin-subset IBM Plex woff2 + OFL license are vendored within budget, whitelisted for Hex, copied into host priv/static by the generator on every run, served by the demo, and gated by a passing font budget/existence test.
</success_criteria>

<output>
Create `.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-01-SUMMARY.md` when done
</output>
