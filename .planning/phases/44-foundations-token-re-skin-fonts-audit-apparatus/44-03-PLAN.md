---
phase: 44-foundations-token-re-skin-fonts-audit-apparatus
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/demo_app/lib/demo_app_web/live/parapet/gallery_live.ex
  - examples/demo_app/lib/demo_app_web/router.ex
  - brandbook/notes/operator-audit-matrix.md
autonomous: true
requirements: [GALLERY-01, GUARD-01]
must_haves:
  truths:
    - "A demo-only /parapet/_gallery route exists in the demo router"
    - "The gallery route lives in a NEW live_session :parapet_gallery placed after :parapet_operator, inside scope \"/\", before scope \"/ops\""
    - "The gallery renders every operator component across light/dark/empty/overflow/disabled/long-string states using hardcoded fixture data (no Repo/Ecto)"
    - "The gallery route is NEVER added to router_snippet.ex.eex or generated host UI"
    - "brandbook/notes/operator-audit-matrix.md exists as a committed component × state grid with todo/done/verified per cell"
    - "The audit matrix records the #7FB4C6 dark-link operator exception for Phase-50 GUARD-04"
  artifacts:
    - path: "examples/demo_app/lib/demo_app_web/live/parapet/gallery_live.ex"
      provides: "Demo-only GalleryLive rendering all components × states"
      contains: "DemoAppWeb.Parapet.GalleryLive"
    - path: "examples/demo_app/lib/demo_app_web/router.ex"
      provides: "Gallery route in a new live_session :parapet_gallery"
      contains: "live_session :parapet_gallery"
    - path: "brandbook/notes/operator-audit-matrix.md"
      provides: "Component × state idempotence ledger"
      contains: "verified"
  key_links:
    - from: "examples/demo_app/lib/demo_app_web/router.ex"
      to: "examples/demo_app/lib/demo_app_web/live/parapet/gallery_live.ex"
      via: "live(\"/parapet/_gallery\", DemoAppWeb.Parapet.GalleryLive, :index)"
      pattern: "/parapet/_gallery.*GalleryLive"
---

<objective>
Scaffold the demo-only `/parapet/_gallery` component lab (GALLERY-01) and create the committed `brandbook/notes/operator-audit-matrix.md` idempotence ledger (GUARD-01).

Purpose: The gallery is the manual + screenshot audit surface for every component × state; the audit matrix is the milestone's idempotence ledger that downstream phases (45–50) walk. Both are independent of the token/font work (different files) so they execute in Wave 1.
Output: `gallery_live.ex`, a new `live_session :parapet_gallery` in the demo router, and `operator-audit-matrix.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-CONTEXT.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-RESEARCH.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan introduces:
- New LiveView module: `DemoAppWeb.Parapet.GalleryLive` (`examples/demo_app/lib/demo_app_web/live/parapet/gallery_live.ex`) with `mount/3`, `render/1`, and a private `gallery_components/0`
- New router block: `live_session :parapet_gallery` with route `live("/parapet/_gallery", DemoAppWeb.Parapet.GalleryLive, :index)` (demo router ONLY)
- New committed doc: `brandbook/notes/operator-audit-matrix.md` (component × state grid, status cells `todo`/`done`/`verified`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add GalleryLive demo LiveView and the demo-only gallery route</name>
  <files>examples/demo_app/lib/demo_app_web/live/parapet/gallery_live.ex, examples/demo_app/lib/demo_app_web/router.ex</files>
  <read_first>
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex (module skeleton analog: `use DemoAppWeb, :live_view`, `import DemoAppWeb.Parapet.OperatorComponents`, mount/render)
    - examples/demo_app/lib/demo_app_web/router.ex (existing `scope "/"` with `live_session :parapet_operator` and the 5 routes; `scope "/ops"` after it)
    - test/parapet/operator_ui_demo_contract_test.exs (the 5 byte-exact `live(...)` substring assertions and the `scope "/" before scope "/ops"` ordering check — must NOT be disturbed, D-14)
    - priv/templates/parapet.gen.ui/operator_components.ex.eex (the 19 component function names to render in the gallery)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-PATTERNS.md (the `gallery_live.ex` and `router.ex` pattern sections; 19-component list)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-RESEARCH.md (Pattern 4 safe route insertion; security note "Gallery must use hardcoded fixture data, not Repo.all()")
  </read_first>
  <action>
    Create `examples/demo_app/lib/demo_app_web/live/parapet/gallery_live.ex` as `DemoAppWeb.Parapet.GalleryLive`: `@moduledoc false`, `use DemoAppWeb, :live_view`, `import DemoAppWeb.Parapet.OperatorComponents`. `mount/3` assigns hardcoded fixture data only — NO `Repo`/Ecto/context calls (security: a demo stress route must never expose live incident data). `render/1` emits `<.operator_theme_bootstrap />` then a `<div class="parapet-ui">` that iterates every one of the 19 operator components (explicit static list in a private `gallery_components/0`, not dynamic introspection) across the states {light, dark, empty, overflow, disabled, long-string} where applicable to each component. Render dark variants by wrapping a section in `data-parapet-theme="dark"` (or `force-system-dark`) so both themes appear on one page. No `handle_params/3`; `handle_event/3` only as no-op stubs if a component requires it.
    In `examples/demo_app/lib/demo_app_web/router.ex`, add a NEW `live_session :parapet_gallery do ... end` block containing exactly `live("/parapet/_gallery", DemoAppWeb.Parapet.GalleryLive, :index)`. Place it AFTER the existing `live_session :parapet_operator` block, still INSIDE `scope "/"`, and BEFORE `scope "/ops"` (Pattern 4). Do NOT modify the existing 5 routes, the `:parapet_operator` session, or the `scope "/ops"` block. Do NOT add this route to `priv/templates/parapet.gen.ui/router_snippet.ex.eex` or any generated host artifact (D-13 hard boundary — gallery is demo-only).
  </action>
  <verify>
    <automated>grep -q 'live_session :parapet_gallery' examples/demo_app/lib/demo_app_web/router.ex && grep -q '/parapet/_gallery' examples/demo_app/lib/demo_app_web/router.ex && grep -q 'DemoAppWeb.Parapet.GalleryLive' examples/demo_app/lib/demo_app_web/live/parapet/gallery_live.ex && ! grep -rq '_gallery' priv/templates/parapet.gen.ui/</automated>
  </verify>
  <acceptance_criteria>
    - `examples/demo_app/lib/demo_app_web/live/parapet/gallery_live.ex` defines `DemoAppWeb.Parapet.GalleryLive` with `mount/3` and `render/1`, no `Repo`/Ecto calls (grep for `Repo.` returns no match in the file)
    - The render iterates an explicit static list of all 19 components (`gallery_components/0`) and shows both light and dark contexts
    - `examples/demo_app/lib/demo_app_web/router.ex` contains a `live_session :parapet_gallery` with `live("/parapet/_gallery", DemoAppWeb.Parapet.GalleryLive, :index)`, placed after `:parapet_operator` and before `scope "/ops"`; the original 5 `live(...)` route strings are byte-unchanged
    - `priv/templates/parapet.gen.ui/` contains NO reference to `_gallery` (grep returns nothing)
    - Demo app compiles: `mix compile --warnings-as-errors` from `examples/demo_app`
  </acceptance_criteria>
  <done>A demo-only `/parapet/_gallery` route renders all 19 components × states from fixture data, isolated to the demo router and never generated into host UI.</done>
</task>

<task type="auto">
  <name>Task 2: Create the operator-audit-matrix.md component × state ledger</name>
  <files>brandbook/notes/operator-audit-matrix.md</files>
  <read_first>
    - brandbook/notes/accessibility.md (sibling doc; markdown-table format analog in the same directory)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-PATTERNS.md (the `operator-audit-matrix.md` table skeleton with all 19 rows)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-RESEARCH.md ("Audit Matrix Structure" — 19-component inventory, recommended columns)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-CONTEXT.md (D-15; D-08 the #7FB4C6 exception to record)
  </read_first>
  <action>
    Create `brandbook/notes/operator-audit-matrix.md` as a committed component × state grid (D-15, GUARD-01). Use the PATTERNS.md table skeleton: one row per the 19 components (operator_theme_bootstrap, operator_nav, theme_control, response_cockpit, nav_item, operator_overview, action_center, incident_list, incident_row, incident_summary, incident_timeline, suspect_changes_card, retrospective_card, runbook_card, preview_panel, action_rail, action_item_list, action_item_card, critical_journeys), columns for the relevant {light,dark} × {default,empty,overflow,disabled} state cells (use `—` for non-applicable cells), each applicable cell carrying a status of `todo` / `done` / `verified`. Initialize all applicable cells to `todo`. Include a header paragraph stating it is the Phase 44 idempotence ledger (re-runs revisit only non-`verified` or regressed cells). Add a "Notes / Exceptions" section that records the operator-specific dark-link exception: dark `--po-link` / `--link` uses `#7FB4C6` (NOT the brand book `#6FA8BC`) per D-07, to be admitted by the Phase-50 GUARD-04 off-palette-hex gate per D-08.
  </action>
  <verify>
    <automated>test -f brandbook/notes/operator-audit-matrix.md && grep -q 'operator_theme_bootstrap' brandbook/notes/operator-audit-matrix.md && grep -q 'critical_journeys' brandbook/notes/operator-audit-matrix.md && grep -q 'todo' brandbook/notes/operator-audit-matrix.md && grep -q '#7FB4C6' brandbook/notes/operator-audit-matrix.md</automated>
  </verify>
  <acceptance_criteria>
    - `brandbook/notes/operator-audit-matrix.md` exists
    - It contains all 19 component row labels (spot-check: `operator_theme_bootstrap` and `critical_journeys` both present)
    - Cells use the status vocabulary `todo` / `done` / `verified` (all applicable cells initialized to `todo`)
    - The file records the `#7FB4C6` dark-link operator exception with a reference to D-07/D-08 and Phase-50 GUARD-04
  </acceptance_criteria>
  <done>The committed audit-matrix ledger enumerates every component × state cell with a todo/done/verified status and records the dark-link exception.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo gallery route → browser | A stress/debug route that must never reach operator (host) deployments |
| Gallery LiveView → data | Must use fixture data, never live incident records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Elevation of Privilege / Exposure | `/parapet/_gallery` leaking into host UI | mitigate | Route added ONLY to the demo `router.ex`; acceptance criteria assert `priv/templates/parapet.gen.ui/` contains no `_gallery` reference (never in `router_snippet.ex.eex` or generated host UI) per D-13 |
| T-44-07 | Information Disclosure | Gallery exposing real incident data | mitigate | `GalleryLive.mount/3` uses hardcoded fixture assigns only; acceptance criteria assert no `Repo.`/Ecto calls in the file |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No installs; no new runtime dependency; demo-only LiveView + markdown doc |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` from `examples/demo_app` green
- `grep -rq '_gallery' priv/templates/parapet.gen.ui/` returns NOTHING (host boundary intact)
- `brandbook/notes/operator-audit-matrix.md` exists with all 19 rows and the #7FB4C6 exception
- Existing demo-contract test assertions remain untouched (verified in Plan 04 once it adds the gallery assertion)
</verification>

<success_criteria>
A demo-only gallery route renders every component × state from fixture data (never shipped to host), and a committed audit-matrix ledger enumerates every component × state cell with a todo/done/verified status plus the recorded dark-link exception.
</success_criteria>

<output>
Create `.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-03-SUMMARY.md` when done
</output>
