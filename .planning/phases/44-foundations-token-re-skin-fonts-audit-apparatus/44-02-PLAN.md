---
phase: 44-foundations-token-re-skin-fonts-audit-apparatus
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex
  - priv/templates/parapet.gen.ui/operator_live.ex.eex
  - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex
autonomous: true
requirements: [TOKEN-01, TOKEN-02, TOKEN-03, TOKEN-04, TOKEN-05, FONT-02, A11Y-01, MOTION-01]
must_haves:
  truths:
    - "All --parapet-* and --po-* var VALUES resolve to brand token hexes in light and BOTH dark blocks, mapped by semantic role per D-02: teal accents -> watch-blue/budget-moss, blue links -> watch-blue #256C82, indigo info -> ai-violet, amber warning -> beacon-amber, stone neutrals -> limestone/mortar/stone/wall-slate/deep-slate, and all six status triplets copied verbatim from brandbook/tokens/tokens.css"
    - "The explicit [data-parapet-theme=dark] block and the @media (prefers-color-scheme: dark) block are byte-identical in their var values"
    - "Type scale, 8px spacing grid, and radius scale (card 10px / control 8px / modal 14px / pill 999px) are applied"
    - "Motion tokens --motion-fast/--motion-base/--motion-ease are declared and zeroed under prefers-reduced-motion"
    - "Light focus ring is watch-blue #256C82; dark focus ring is limestone #F8F4EC"
    - "Dark --po-link / --link is #7FB4C6 (operator exception), NOT brandbook #6FA8BC"
    - "operator_theme_bootstrap/1 emits @font-face rules for IBM Plex Sans 400/500/600 + Mono 400/500 pointing at the host static path with font-display: swap and the system stack (--font-sans/--font-mono) as fallback — no FOUT breakage, no layout shift (D-11)"
    - "The theme attribute remains data-parapet-theme and is NOT renamed to the brand book's data-theme; brand is reconciled at the token-value level only, preserving the public switcher + screenshot-script contract (D-03)"
    - "The template and its demo mirror carry identical CSS variable values"
    - "No class, selector, JS, or markup-color edits — values-only"
  artifacts:
    - path: "priv/templates/parapet.gen.ui/operator_components.ex.eex"
      provides: "Re-skinned theme bootstrap: brand token values, @font-face, motion tokens"
      contains: "#256C82"
    - path: "examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex"
      provides: "Demo mirror with identical re-skinned token values"
      contains: "#256C82"
  key_links:
    - from: "priv/templates/parapet.gen.ui/operator_components.ex.eex"
      to: "priv/static/parapet/fonts/"
      via: "@font-face src url(/parapet/fonts/IBMPlexSans-Regular-latin.woff2)"
      pattern: "@font-face"
    - from: "examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex"
      to: "priv/templates/parapet.gen.ui/operator_components.ex.eex"
      via: "demo mirror must carry identical --parapet-*/--po-* values (contrast test reads both paths)"
      pattern: "--po-link: #256C82"
---

<objective>
Re-skin the operator theme to brand tokens — values-only — across all three CSS blocks (light `.parapet-ui`, explicit `[data-parapet-theme="dark"]`, and `@media (prefers-color-scheme: dark)`) in `operator_components.ex.eex` AND its demo mirror, add `@font-face` + `--font-sans`/`--font-mono`, wire motion tokens with reduced-motion zeroing, and set per-surface focus rings. Verify the two secondary templates (+ their demo mirrors) carry no un-intercepted off-brand color classes.

Purpose: Delivers the core token re-base (TOKEN-01..05), font wiring (FONT-02), focus-ring values (A11Y-01) and motion (MOTION-01). This is the single highest-risk plan — the `@media (prefers-color-scheme: dark)` block (D-04) and the demo mirror (D-16) are the two drift points the contrast test in Plan 04 cannot fully catch on its own.
Output: Re-skinned template + demo mirror with brand values, @font-face, motion tokens, focus rings; verified-clean secondary templates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-CONTEXT.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-RESEARCH.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-PATTERNS.md
@brandbook/tokens/tokens.css
</context>

<artifacts_this_phase_produces>
This plan introduces (values + rules only, no new symbols):
- New `@font-face` family declarations in `operator_theme_bootstrap/1`: family names `"IBM Plex Sans"` (weights 400/500/600) and `"IBM Plex Mono"` (weights 400/500), `font-display: swap`
- New CSS vars introduced into the `.parapet-ui` light block: `--font-sans`, `--font-mono`, `--motion-fast`, `--motion-base`, `--motion-ease`
- New CSS var VALUES (re-pointed, not new names) for ~16 `--parapet-*` and ~34 `--po-*` vars across three blocks (see RESEARCH.md "Complete CSS Variable Inventory" and PATTERNS.md replacement tables)
- The `#7FB4C6` dark-link operator exception value (D-07) — recorded for Phase-50 GUARD-04 in Plan 03's audit matrix
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin all three CSS blocks + @font-face + motion in template and demo mirror</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</files>
  <read_first>
    - priv/templates/parapet.gen.ui/operator_components.ex.eex (the `operator_theme_bootstrap/1` inline `<style>`: light block ~lines 8-60, explicit dark block ~lines 62-115, `@media (prefers-color-scheme: dark)` block ~lines 117-176, `prefers-reduced-motion` block ~lines 323-330)
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex (the demo mirror with the same `<style>` structure, EEx already evaluated)
    - brandbook/tokens/tokens.css (token source of truth — values copied verbatim, lines 25-30 for the six status triplets)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-PATTERNS.md (the full light + dark replacement value lists, motion block, @font-face block, and `--font-sans`/`--font-mono` additions under the `operator_components.ex.eex` section)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-RESEARCH.md ("Complete CSS Variable Inventory" tables; D-04 anti-pattern; D-07 dark-link exception)
  </read_first>
  <action>
    Apply VALUES-ONLY edits (D-01): change only the `--parapet-*` / `--po-*` declaration VALUES and add the new font/motion vars. Do NOT touch any selector, class rule, utility-interception rule (`.bg-stone-* → var(...)` etc.), `.po-*` rule, JS, or markup. The utility layer inherits brand values for free once vars are re-pointed.
    Re-point all 16 `--parapet-*` vars and all 34 `--po-*` vars to the brand hexes in the LIGHT block, then identically in the EXPLICIT DARK block, then identically AGAIN in the `@media (prefers-color-scheme: dark)` block. The two dark blocks MUST end byte-identical in their var values (D-04 — the media-query block is the single highest-risk omission; the contrast test reads static `@themes` constants and will NOT catch a stale media-query block). Use the exact light/dark replacement value lists in PATTERNS.md. Notable required values: light `--parapet-bg: #F8F4EC`, `--parapet-accent: #256C82`, `--po-link: #256C82`, `--po-focus: #256C82`; dark `--parapet-bg: #18232B`, `--parapet-accent: #7FB4C6`, `--po-link: #7FB4C6` (D-07 operator exception — NOT #6FA8BC), `--po-focus: #F8F4EC` (limestone, D-06). All six status chip triplets (neutral/success/warning/danger/info — exhausted maps to danger for Phase 44) come verbatim from tokens.css lines 25-30.
    Add the three motion vars to the light block: `--motion-fast: 120ms; --motion-base: 200ms; --motion-ease: cubic-bezier(.2, 0, 0, 1);`. In the existing `@media (prefers-reduced-motion: reduce)` block, add `:root { --motion-fast: 0ms; --motion-base: 0ms; }` while keeping the existing `.parapet-ui * { animation-duration / transition-duration: 0.01ms !important; ... }` rules.
    Add `--font-sans: "IBM Plex Sans", ui-sans-serif, system-ui, sans-serif;` and `--font-mono: "IBM Plex Mono", ui-monospace, monospace;` to the light block (system-stack fallback so layout renders correctly before fonts load — TOKEN-04 / FONT-02 no-layout-shift).
    Insert the five `@font-face` rules (IBM Plex Sans 400/500/600 + Mono 400/500, each `font-display: swap`, `src: url("/parapet/fonts/<exact-filename>.woff2") format("woff2")`) at the top of the `<style>` block before the `.parapet-ui { }` rule. Filenames must match Plan 01's output exactly.
    Apply type scale, 8px spacing grid, and radius scale (card 10px / control 8px / modal 14px / pill 999px) per D-05 from the brand tokens where these are expressed as token values in the bootstrap style (values-only; no structural markup change).
    Make EVERY edit above in BOTH `operator_components.ex.eex` AND the demo mirror `operator_components.ex` so they stay byte-identical in CSS variable values (D-16 — the contrast test reads both `@component_paths`). The demo mirror has EEx already evaluated; match the resulting CSS values exactly.
  </action>
  <verify>
    <automated>grep -c '#256C82' priv/templates/parapet.gen.ui/operator_components.ex.eex | awk '{exit ($1>=3)?0:1}' && grep -q '#7FB4C6' priv/templates/parapet.gen.ui/operator_components.ex.eex && grep -q '@font-face' priv/templates/parapet.gen.ui/operator_components.ex.eex && grep -q -- '--motion-fast' priv/templates/parapet.gen.ui/operator_components.ex.eex && grep -c '#256C82' examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex | awk '{exit ($1>=3)?0:1}' && grep -q '#7FB4C6' examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex && grep -q '@font-face' examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</automated>
    <automated># D-04 GATE: assert brand dark hexes appear INSIDE the @media (prefers-color-scheme: dark) block specifically (a stale media block sharing a hex with the explicit dark block must NOT pass). Extracts the media block range and greps the three brand dark hexes within it, for BOTH template and demo mirror.
for f in priv/templates/parapet.gen.ui/operator_components.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex; do
  block=$(awk '/@media \(prefers-color-scheme: dark\)/{c=1} c{print} c&&/^\s*}/{n++; if(n>=2) c=0}' "$f")
  for hex in '#18232B' '#7FB4C6' '#F8F4EC'; do
    printf '%s' "$block" | grep -qF "$hex" || { echo "FAIL: $hex not found inside @media (prefers-color-scheme: dark) block of $f"; exit 1; }
  done
done
    </automated>
  </verify>
  <acceptance_criteria>
    - Both `operator_components.ex.eex` and the demo mirror contain `#256C82` at least 3 times (light accent/link/focus across blocks) and `#7FB4C6` at least once (dark link/accent)
    - The new dark brand hexes (`#18232B`, `#7FB4C6`, `#F8F4EC`) are present INSIDE the extracted `@media (prefers-color-scheme: dark)` block range of BOTH the template and the demo mirror — proving the media-query dark block (D-04) was updated, not just the explicit `[data-parapet-theme="dark"]` block
    - Both files contain `@font-face`, `IBM Plex Sans`, `font-display: swap`, and the five exact woff2 filenames from Plan 01 (D-11: @font-face in `operator_theme_bootstrap/1` with `font-display: swap` and system-stack fallback)
    - The theme attribute remains `data-parapet-theme` — NOT renamed to `data-theme` (D-03: brand reconciled at the token-value level only, theme attribute is the public switcher + screenshot-script contract)
    - Both files contain `--motion-fast`, `--motion-base`, `--motion-ease`, and the string `--motion-fast: 0ms` inside the `prefers-reduced-motion` block
    - Both files contain `--font-sans` and `--font-mono` with the system-stack fallback
    - The explicit `[data-parapet-theme="dark"]` block and the `@media (prefers-color-scheme: dark)` block contain identical var VALUES (spot-grep: `#18232B`, `#7FB4C6`, `#F8F4EC` each appear in both dark contexts)
    - No selector/class/utility-interception/.po-* rule text changed (diff shows only value substitutions + the new @font-face/var/motion additions); `--po-link` dark value is `#7FB4C6` (NOT `#6FA8BC`)
    - `mix compile --warnings-as-errors` succeeds (templates remain valid EEx; demo mirror compiles)
  </acceptance_criteria>
  <done>Light + both dark blocks re-pointed to brand hexes identically in template and demo mirror, with @font-face, font vars, motion tokens (zeroed under reduced-motion), and per-surface focus rings.</done>
</task>

<task type="auto">
  <name>Task 2: Verify secondary templates and their demo mirrors carry no un-intercepted off-brand color classes</name>
  <files>priv/templates/parapet.gen.ui/operator_live.ex.eex, priv/templates/parapet.gen.ui/operator_detail_live.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex, examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex</files>
  <read_first>
    - priv/templates/parapet.gen.ui/operator_live.ex.eex
    - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex
    - priv/templates/parapet.gen.ui/operator_components.ex.eex (the utility-interception rules: `.parapet-ui .bg-stone-*`, `.text-teal-*` → var(...) — confirm which classes are intercepted)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-PATTERNS.md (the `operator_live.ex.eex` + `operator_detail_live.ex.eex` secondary-edits section)
  </read_first>
  <action>
    Grep each secondary template + mirror for hardcoded Tailwind color utility classes (e.g. `text-teal-*`, `bg-teal-*`, `text-blue-*`, `bg-stone-*`, `text-stone-*`, `bg-emerald-*`, `text-indigo-*`, `text-amber-*`). For each hit, confirm it is intercepted by a `.parapet-ui .<class> { ... → var(--...) }` rule in `operator_components.ex.eex` (intercepted classes inherit brand values for free — leave them). If any color class is NOT intercepted by the utility layer, replace it with the semantically equivalent intercepted class or `var(--po-*)`-backed class (values-only — do NOT change markup structure, attributes, or component arity). Make identical changes in the template and its demo mirror if any change is needed. If all color classes are intercepted, make no edits and record that finding in the SUMMARY.
  </action>
  <verify>
    <automated># HARD GATE (exit-code-checked): templates + demo mirror must compile cleanly.
mix compile --warnings-as-errors</automated>
    <automated># INFORMATIONAL ONLY — NOT a blocker at this point in the wave. The contrast test is still pinned to OLD Tailwind-era hexes until Plan 44-04 re-pins it, so it is EXPECTED to show failures here. Run it for visibility but do NOT treat a red result as a mid-wave blocker; 44-04 makes it green. (`|| true` keeps it non-blocking.)
mix test test/parapet/operator_ui_contrast_test.exs 2>&1 | tail -15 || true</automated>
  </verify>
  <acceptance_criteria>
    - Every color utility class remaining in the secondary templates and mirrors is covered by an interception rule in `operator_components.ex.eex` (each grep hit cross-referenced to a `.parapet-ui .<class>` rule), OR has been replaced with an intercepted/`var(--po-*)` equivalent
    - No markup structure, component attribute, or arity changed in any secondary template or mirror (diff shows only class-token swaps, if any)
    - `mix compile --warnings-as-errors` succeeds — this is the HARD GATE for this task
    - The `operator_ui_contrast_test.exs` run is informational only here (it is still pinned to old hexes until Plan 44-04); a red result is expected and is NOT a blocker for completing this task/wave
    - Any change made to a template is mirrored identically in its demo counterpart
  </acceptance_criteria>
  <done>Secondary templates and demo mirrors contain only brand-intercepted color classes; no off-brand teal/blue/indigo/emerald hue leaks remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template → generated host UI | CSS values flow into operator deployments via the generator |
| tokens.css → template values | Brand hexes copied verbatim (no dynamic input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03 | Tampering / Integrity | CSS token values | accept | Hex values are static, copied verbatim from `brandbook/tokens/tokens.css`; no dynamic/user input enters the inline `<style>` — no injection surface |
| T-44-04 | Repudiation / Drift | `@media (prefers-color-scheme: dark)` block left stale (D-04) | mitigate | Acceptance criteria require both dark blocks to carry identical values (`#18232B`/`#7FB4C6`/`#F8F4EC` grep in both); Plan 04 contrast test pins the values; SUMMARY records the both-blocks-updated assertion |
| T-44-05 | Tampering | Demo mirror divergence from template (D-16) | mitigate | Both files edited in the same task; verify greps run against both; Plan 04 contrast test reads both `@component_paths` |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No installs; no new runtime dependency; values-only edits |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` green (template EEx valid, demo mirror compiles)
- `mix test test/parapet/operator_ui_contrast_test.exs` — NOTE: the test is re-pinned in Plan 04; until then it may reference old hexes. After Plan 04 it must pass on the values set here.
- Grep confirms both dark blocks carry identical brand values; `--po-link` dark = `#7FB4C6`
</verification>

<success_criteria>
The operator theme template and its demo mirror are re-pointed to brand token values across all three CSS blocks with no selector/class/JS/markup changes, carrying @font-face + font vars + motion tokens + per-surface focus rings; secondary templates carry no un-intercepted off-brand color classes.
</success_criteria>

<output>
Create `.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-02-SUMMARY.md` when done
</output>
