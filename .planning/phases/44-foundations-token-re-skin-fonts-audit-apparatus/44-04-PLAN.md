---
phase: 44-foundations-token-re-skin-fonts-audit-apparatus
plan: 04
type: execute
wave: 2
depends_on: ["44-01", "44-02", "44-03"]
files_modified:
  - test/parapet/operator_ui_contrast_test.exs
  - test/parapet/operator_ui_demo_contract_test.exs
autonomous: true
requirements: [GUARD-02, TOKEN-01, TOKEN-02, TOKEN-03, TOKEN-05, FONT-02, FONT-03, A11Y-01, MOTION-01, GALLERY-01, GUARD-01]
must_haves:
  truths:
    - "Both @themes maps (light + dark) in the contrast test are re-pinned to brand token hexes"
    - "All six status triplets are asserted at 4.5:1 in both themes"
    - "Dark links are asserted on surface (#7FB4C6 on wall-slate) AND bg (#7FB4C6 on deep-slate)"
    - "Focus-ring contrast is asserted at the 3:1 UI floor per surface (watch-blue/panel light, limestone/bg dark)"
    - "The contrast test passes at WCAG AA on the re-skinned token values"
    - "String assertions confirm @font-face, font-display: swap, motion tokens, and reduced-motion zeroing in both component paths"
    - "The demo-contract test asserts the gallery route, the vendored font file, and the audit matrix exist — without disturbing the byte-exact 5-route assertions"
  artifacts:
    - path: "test/parapet/operator_ui_contrast_test.exs"
      provides: "Re-pinned contrast gate with status triplets, dark link-on-surface+bg, focus rings, motion/@font-face string asserts"
      contains: "focus_ring"
    - path: "test/parapet/operator_ui_demo_contract_test.exs"
      provides: "Additive gallery/font/audit-matrix assertions"
      contains: "live_session :parapet_gallery"
  key_links:
    - from: "test/parapet/operator_ui_contrast_test.exs"
      to: "examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex"
      via: "@component_paths reads both template and demo mirror; @themes pins values present in both"
      pattern: "@component_paths"
    - from: "test/parapet/operator_ui_demo_contract_test.exs"
      to: "priv/static/parapet/fonts/IBMPlexSans-Regular-latin.woff2"
      via: "File.exists? assertion for FONT-03"
      pattern: "File.exists\\?"
---

<objective>
Re-pin `operator_ui_contrast_test.exs` (`@themes` + assertions) to the brand token hexes — six status triplets, dark links on surface AND bg, focus rings at the 3:1 UI floor, plus string assertions for `@font-face`/motion — and add additive gallery/font/audit-matrix assertions to `operator_ui_demo_contract_test.exs`. Verify the full suite is green on the values set by Plans 01–03.

Purpose: GUARD-02 — the contrast gate is the automated enforcement that the re-skin (Plan 02), fonts (Plan 01), and audit apparatus (Plan 03) actually landed and pass WCAG AA. This plan is the Wave-2 gate: it depends on the token values, font files, and gallery/matrix artifacts existing.
Output: re-pinned contrast test (green) + additive demo-contract assertions (green); full `mix test` suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-CONTEXT.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-RESEARCH.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-PATTERNS.md
@.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan introduces (test assertions only):
- Re-pinned `@themes` module attribute (light + dark maps) in `operator_ui_contrast_test.exs` keyed to brand hexes
- New assertion keys in the contrast test: `link_on_panel`, `link_on_bg`, `focus_ring`, six chip assertions (`healthy_chip`/`watch_chip`/`burning_chip`/`exhausted_chip`/`unknown_chip`/`ai_chip`), `warning_button`
- New string assertions in the "semantic tokens" test: `@font-face`, `IBM Plex Sans`, `font-display: swap`, `--motion-fast`/`--motion-base`/`--motion-ease`, `prefers-reduced-motion`, `--motion-fast: 0ms`
- New additive assertions in `operator_ui_demo_contract_test.exs`: gallery route + `live_session :parapet_gallery`, font `File.exists?`, audit-matrix `File.exists?`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-pin @themes and assertions in operator_ui_contrast_test.exs</name>
  <files>test/parapet/operator_ui_contrast_test.exs</files>
  <read_first>
    - test/parapet/operator_ui_contrast_test.exs (current `@component_paths` lines ~4-7, `@themes` lines ~9-64, the "semantic operator tokens" test lines ~66-98, the "known dark-mode risk surfaces" test lines ~100-126, and the unchanged private helpers `assert_contrast/4`, `contrast_ratio/2`, `relative_luminance/1`, `rgb/1`, `linear_channel/1` lines ~128-157)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-PATTERNS.md (the `operator_ui_contrast_test.exs` section: full replacement `@themes` map + test body + the motion/@font-face string assertions)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-RESEARCH.md ("Contrast Test Re-pin Strategy": Required New @themes Map, New Assertions, Pre-verified Contrast Ratios)
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex (the re-skinned demo mirror values from Plan 02 — the test reads this path)
    - priv/templates/parapet.gen.ui/operator_components.ex.eex (the re-skinned template values from Plan 02 — the test reads this path)
  </read_first>
  <action>
    Replace the entire `@themes` module attribute with the brand-token map from PATTERNS.md / RESEARCH.md "Required New @themes Map": light keyed to limestone/white/parapet-black/watch-blue/etc. and dark keyed to deep-slate/wall-slate/parapet-black/`#7FB4C6`/limestone. Include `link_on_panel` + `link_on_bg` (dark `#7FB4C6` on both `panel`=#2E3A42 and `bg`=#18232B), `focus_ring` (light `#256C82`, dark `#F8F4EC`), all six status triplets (healthy/watch/burning/exhausted/unknown/ai bg+fg), and `warning_button_bg`/`warning_button_fg`.
    Rewrite the "semantic operator tokens meet contrast minimums" test body to iterate `@themes` and assert (per PATTERNS.md test body): header_title/header_bg 4.5, header_muted/header_bg 4.5, link_on_panel/panel 4.5, link_on_bg/bg 4.5, nav/header_bg 4.5, nav_hover 4.5, nav_active 4.5, theme_control 4.5, the six `*_chip` assertions fg/bg 4.5, warning_button fg/bg 4.5, and the GUARD-02 focus-ring assertion at 3.0 against `panel` (light) / `bg` (dark). Keep the private helpers (`assert_contrast`, `contrast_ratio`, `relative_luminance`, `rgb`, `linear_channel`) byte-unchanged.
    In the "operator components use semantic tokens for known dark-mode risk surfaces" test, append inside the `for path <- @component_paths` block string assertions: `content =~ "--motion-fast"`, `"--motion-base"`, `"--motion-ease"`, `"prefers-reduced-motion"`, `"--motion-fast: 0ms"`, `"@font-face"`, `"IBM Plex Sans"`, `"font-display: swap"`. Keep `@component_paths` reading BOTH the template and the demo mirror.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_contrast_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/parapet/operator_ui_contrast_test.exs` passes (all theme × assertion combinations green at WCAG AA / 3:1 floor for focus)
    - `@themes` light + dark maps contain brand hexes (`#F8F4EC`, `#256C82`, `#18232B`, `#7FB4C6`, `#F8F4EC` for focus); dark `link_on_panel` and `link_on_bg` are both `#7FB4C6`
    - The test asserts all six status chips in both themes, link-on-surface AND link-on-bg, and a focus-ring assertion at `3.0`
    - The "risk surfaces" test asserts `@font-face`, `font-display: swap`, and the three `--motion-*` strings plus `--motion-fast: 0ms`, against BOTH `@component_paths`
    - The private contrast-math helpers are unchanged from the prior version
  </acceptance_criteria>
  <done>The contrast gate is re-pinned to brand token hexes (six triplets, dark links on surface+bg, focus rings at 3:1, motion/@font-face string asserts) and passes at WCAG AA against both component paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add additive gallery/font/audit-matrix assertions to the demo-contract test and run full suite</name>
  <files>test/parapet/operator_ui_demo_contract_test.exs</files>
  <read_first>
    - test/parapet/operator_ui_demo_contract_test.exs (the existing 5 `live(...)` substring assertions ~lines 59-86, the `:binary.match` `scope "/"` before `scope "/ops"` ordering check ~lines 72-73 — do NOT disturb any of these, D-14)
    - .planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-PATTERNS.md (the `operator_ui_demo_contract_test.exs` additive-assertions block)
    - examples/demo_app/lib/demo_app_web/router.ex (the gallery route added in Plan 03)
    - brandbook/notes/operator-audit-matrix.md (created in Plan 03)
  </read_first>
  <action>
    Add ONLY new assertions to `operator_ui_demo_contract_test.exs` — do not modify or reorder any existing assertion (the 5 byte-exact `live(...)` substring checks and the `:binary.match` ordering check must stay intact, D-14). Append after the existing route assertions: (GALLERY-01) `assert router =~ ~S|live("/parapet/_gallery", DemoAppWeb.Parapet.GalleryLive, :index)|` and `assert router =~ "live_session :parapet_gallery"`; (FONT-03) `assert File.exists?("priv/static/parapet/fonts/IBMPlexSans-Regular-latin.woff2")`; (GUARD-01) `assert File.exists?("brandbook/notes/operator-audit-matrix.md")`. Use the `router`/file-read variables already in scope in that test, following the PATTERNS.md additive block. Then run the full suite to confirm phase-gate green.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_demo_contract_test.exs && mix test</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/parapet/operator_ui_demo_contract_test.exs` passes including the new gallery/font/audit-matrix assertions
    - The 5 original `live(...)` route substring assertions and the `scope "/" before scope "/ops"` ordering check are byte-unchanged (diff shows only added assertions)
    - The demo-contract test does NOT assert `/parapet/_gallery` inside the original 5-route block (gallery assertion is a separate added line per D-14)
    - Full `mix test` suite is green (phase gate)
  </acceptance_criteria>
  <done>The demo-contract test additively asserts the gallery route, vendored font, and audit matrix exist without disturbing the byte-exact route contract; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → CI gate | The contrast/demo-contract tests are the automated enforcement boundary for brand + a11y + host-isolation invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-08 | Repudiation / Drift | Contrast gate not catching the stale `@media` dark block | accept | Documented limitation: the gate reads static `@themes` constants, not the media query (D-04). Plan 02 acceptance criteria grep both dark blocks for identical values to compensate; this test pins the canonical values |
| T-44-09 | Tampering | Demo-contract route invariant weakened | mitigate | Gallery assertions are strictly additive; acceptance criteria require the 5 byte-exact `live(...)` assertions and the ordering check to stay unchanged (D-14) |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No installs; test-only assertion edits; no new runtime dependency |
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_contrast_test.exs` green (WCAG AA + 3:1 focus floor)
- `mix test test/parapet/operator_ui_demo_contract_test.exs` green (additive assertions; original contract intact)
- Full `mix test` green (phase gate before `/gsd-verify-work`)
</verification>

<success_criteria>
The contrast gate is re-pinned to brand token hexes (six status triplets, dark links on surface + bg, focus rings at the 3:1 UI floor, motion/@font-face string assertions) and passes at WCAG AA against both component paths; the demo-contract test additively asserts the gallery route, vendored font, and audit matrix; the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/44-foundations-token-re-skin-fonts-audit-apparatus/44-04-SUMMARY.md` when done
</output>
