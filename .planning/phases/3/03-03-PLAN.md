---
phase: "3"
plan: 3
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: ["lib/parapet/plug/mcp.ex", "test/parapet/plug/mcp_test.exs"]
autonomous: true
requirements: ["MCP-01"]
must_haves:
  truths:
    - "System exposes an MCP server interface via HTTP"
    - "System speaks Server-Sent Events (SSE) for MCP transport"
    - "System handles JSON-RPC payloads appropriately"
  artifacts:
    - path: "lib/parapet/plug/mcp.ex"
      provides: "HTTP Plug for SSE and JSON-RPC handling"
    - path: "test/parapet/plug/mcp_test.exs"
      provides: "Tests for the Plug SSE transport"
  key_links:
    - from: "lib/parapet/plug/mcp.ex"
      to: "Parapet.MCP.Server"
      via: "Function delegation after JSON-RPC parsing"
---

<objective>
Implement the MCP HTTP transport layer via a custom Plug that handles Server-Sent Events (SSE) and JSON-RPC.

Purpose: Provide a standard interface for external AI agents to connect to the Parapet MCP server without adding heavy third-party dependencies.
Output: `Parapet.Plug.MCP` module and its unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/3/RESEARCH.md
@.planning/phases/3/PATTERNS.md
@lib/parapet/plug/webhook.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create test scaffolding for Parapet.Plug.MCP</name>
  <files>test/parapet/plug/mcp_test.exs</files>
  <behavior>
    - Test 1: Rejects non-POST requests with 405 Method Not Allowed.
    - Test 2: Sets proper SSE headers (`text/event-stream`, `cache-control: no-cache`).
    - Test 3: Parses JSON-RPC request and delegates to Parapet.MCP.Server.
    - Test 4: Formats Server response back into JSON-RPC over SSE.
  </behavior>
  <action>
    Create `test/parapet/plug/mcp_test.exs`. Use `Plug.Test` helpers (like `conn(:post, "/mcp", ...)`) to simulate incoming MCP requests. Mock or expect calls to `Parapet.MCP.Server.execute_tool/2`.
  </action>
  <verify>
    <automated>mix test test/parapet/plug/mcp_test.exs</automated>
  </verify>
  <done>Test file created with failing tests representing correct HTTP handling and delegation.</done>
</task>

<task type="auto">
  <name>Task 1: Implement Parapet.Plug.MCP</name>
  <files>lib/parapet/plug/mcp.ex</files>
  <action>
    Implement `Parapet.Plug.MCP` adopting the `Plug` behaviour.
    - `call/2` should accept POST requests containing JSON-RPC payloads.
    - Upgrade the connection to SSE using `send_chunked(200)` and headers `content-type: text/event-stream`, `cache-control: no-cache`.
    - Parse the incoming JSON-RPC payload.
    - Extract the requested tool and arguments, and pass them to `Parapet.MCP.Server.execute_tool/2`.
    - Format the result as a JSON-RPC response.
    - Send the response back as an SSE chunk using `chunk(conn, ...)` with proper `event:` and `data:` formatting.
    - Implement a catch-all `call/2` that returns `405 Method Not Allowed` (following `lib/parapet/plug/webhook.ex` pattern).
  </action>
  <verify>
    <automated>mix test test/parapet/plug/mcp_test.exs</automated>
  </verify>
  <done>Plug properly routes, decodes, executes, and encodes MCP JSON-RPC messages over SSE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External Network -> Plug | Untrusted HTTP payloads are parsed as JSON-RPC before passing to the Server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-05 | Denial of Service | `mcp.ex` | mitigate | Ensure JSON payload size is constrained (rely on standard Plug parsers if possible). Avoid keeping idle SSE connections open indefinitely without timeouts or keepalives if deployed directly. |
| T-3-06 | Spoofing | `mcp.ex` | transfer | Authentication to the MCP endpoint should be handled by the host application's router pipeline before hitting `Parapet.Plug.MCP`. (Document this assumption). |
</threat_model>

<verification>
Ensure all tests in `test/parapet/plug/mcp_test.exs` pass.
</verification>

<success_criteria>
`Parapet.Plug.MCP` successfully accepts HTTP POST requests, establishes SSE streams, decodes JSON-RPC, delegates execution, and returns SSE-formatted JSON-RPC responses.
</success_criteria>

<output>
After completion, create `.planning/phases/3/03-03-SUMMARY.md`
</output>
