---
phase: "3"
plan: 2
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: ["lib/parapet/mcp/server.ex", "test/parapet/mcp/server_test.exs"]
autonomous: true
requirements: ["MCP-02"]
must_haves:
  truths:
    - "AI agents can query active incidents, timelines, and runbooks"
    - "Tools provided are strictly read-only"
    - "Runbook modules are safely loaded without arbitrary code execution risk"
  artifacts:
    - path: "lib/parapet/mcp/server.ex"
      provides: "Core MCP tool execution and routing"
    - path: "test/parapet/mcp/server_test.exs"
      provides: "Tests for MCP server tools"
  key_links:
    - from: "lib/parapet/mcp/server.ex"
      to: "Parapet.Evidence"
      via: "Read-only Ecto queries"
    - from: "lib/parapet/mcp/server.ex"
      to: "Parapet.MCP.PrometheusClient"
      via: "Function calls for metrics tools"
---

<objective>
Implement the core MCP tool execution logic in `Parapet.MCP.Server`.

Purpose: Provide a controlled, read-only interface for external AI agents to investigate incidents safely.
Output: `Parapet.MCP.Server` module containing handlers for specific read-only tools and associated unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/3/RESEARCH.md
@.planning/phases/3/PATTERNS.md
@lib/parapet/spine/alert_processor.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create test scaffolding for Parapet.MCP.Server</name>
  <files>test/parapet/mcp/server_test.exs</files>
  <behavior>
    - Test 1: list_incidents returns only active incidents via Parapet.Evidence
    - Test 2: get_incident_timeline returns timeline entries for a specific correlation_key
    - Test 3: read_runbook correctly and safely resolves module names and fetches the runbook schema
    - Test 4: get_slo_burn_rates delegates to Parapet.MCP.PrometheusClient
    - Test 5: unknown tool calls return an error
  </behavior>
  <action>
    Create the test file `test/parapet/mcp/server_test.exs`. Set up fixtures or mocks for `Parapet.Evidence` and `Parapet.MCP.PrometheusClient`. Write failing tests for each expected tool handler.
  </action>
  <verify>
    <automated>mix test test/parapet/mcp/server_test.exs</automated>
  </verify>
  <done>Test file created with failing tests representing the core tool execution requirements.</done>
</task>

<task type="auto">
  <name>Task 1: Implement Parapet.MCP.Server tool execution</name>
  <files>lib/parapet/mcp/server.ex</files>
  <action>
    Implement `Parapet.MCP.Server`.
    - Provide an `execute_tool(tool_name, arguments)` function.
    - Implement `list_incidents`: Use Ecto to query `Parapet.Spine.Incident` where `state == "open"`. Use `Parapet.Evidence.repo()`.
    - Implement `get_incident_timeline(correlation_key)`: Query `Parapet.Spine.TimelineEntry` by correlation key.
    - Implement `read_runbook(alertname)`: Look up SLO by alertname, fetch runbook module using safe atom loading (see `PATTERNS.md` and `Parapet.Spine.AlertProcessor.get_runbook_module/1`), and return `__runbook_schema__()` or similar read-only details.
    - Implement `get_slo_burn_rates(name)`: Delegate to `Parapet.MCP.PrometheusClient.get_slo_burn_rate/1`.
    - Ensure all operations are strictly read-only. Do not provide any mutation tools.
  </action>
  <verify>
    <automated>mix test test/parapet/mcp/server_test.exs</automated>
  </verify>
  <done>Server safely executes specific tools, preventing unbounded queries and arbitrary module loading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP Client -> Server | Untrusted tool requests and arguments are evaluated and mapped to internal system queries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Information Disclosure | `server.ex` | mitigate | Ensure incident queries only return relevant bounds (e.g. open state, specific time window) rather than full database dumps. |
| T-3-03 | Elevation of Privilege | `server.ex` | mitigate | Safely parse module names using `String.to_existing_atom` to prevent dynamic code execution or memory exhaustion from unbounded atom creation. |
| T-3-04 | Tampering | `server.ex` | mitigate | Only implement read-only tools. No tool should execute an Ecto `update`, `insert`, or `delete`. |
</threat_model>

<verification>
Ensure all tests in `test/parapet/mcp/server_test.exs` pass.
</verification>

<success_criteria>
`Parapet.MCP.Server` correctly processes defined read-only tools and interacts safely with the Database and PrometheusClient.
</success_criteria>

<output>
After completion, create `.planning/phases/3/03-02-SUMMARY.md`
</output>
