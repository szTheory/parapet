---
phase: "3"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: ["lib/parapet/mcp/prometheus_client.ex", "test/parapet/mcp/prometheus_client_test.exs"]
autonomous: true
requirements: ["MCP-03"]
must_haves:
  truths:
    - "System can fetch metrics using hardcoded, parameterized PromQL queries"
    - "System gracefully handles missing Prometheus configuration without crashing"
  artifacts:
    - path: "lib/parapet/mcp/prometheus_client.ex"
      provides: "Prometheus API proxy via Req"
    - path: "test/parapet/mcp/prometheus_client_test.exs"
      provides: "Tests for the Prometheus client"
  key_links:
    - from: "lib/parapet/mcp/prometheus_client.ex"
      to: "Prometheus HTTP API"
      via: "Req HTTP GET requests"
---

<objective>
Implement a read-only Prometheus HTTP API proxy using `Req` to allow external agents to retrieve predefined time-series metrics.

Purpose: AI copilots need access to current SLO burn rates and RED metrics to perform investigation triage without holding write-access or running arbitrary unconstrained PromQL.
Output: A new internal module `Parapet.MCP.PrometheusClient` and its unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/3/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create the test scaffolding for Parapet.MCP.PrometheusClient</name>
  <files>test/parapet/mcp/prometheus_client_test.exs</files>
  <behavior>
    - Test 1: Returns error tuple when Prometheus URL is not configured.
    - Test 2: Makes correct HTTP GET request via Req with the hardcoded parameterized query for SLO burn rates.
  </behavior>
  <action>
    Create the test file `test/parapet/mcp/prometheus_client_test.exs`. Mock the `Req` dependency or use bypass to test the HTTP request without requiring a real Prometheus instance. Alternatively, just test the query construction and config handling.
  </action>
  <verify>
    <automated>mix test test/parapet/mcp/prometheus_client_test.exs</automated>
  </verify>
  <done>Test file created with failing tests or skipped tests representing the required behavior.</done>
</task>

<task type="auto">
  <name>Task 1: Implement Parapet.MCP.PrometheusClient</name>
  <files>lib/parapet/mcp/prometheus_client.ex</files>
  <action>
    Implement the `Parapet.MCP.PrometheusClient` module.
    - Expose function `get_slo_burn_rate(name)`.
    - Fetch the Prometheus URL from `Application.get_env(:parapet, :prometheus_url)`.
    - If unconfigured, gracefully return `{:error, :not_configured}` or similar.
    - Use `Req` to execute HTTP GET to the Prometheus HTTP API (`/api/v1/query`).
    - Pass a hardcoded, parameterized PromQL query avoiding arbitrary PromQL injection.
  </action>
  <verify>
    <automated>mix test test/parapet/mcp/prometheus_client_test.exs</automated>
  </verify>
  <done>The module safely proxies hardcoded metric queries to Prometheus when configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP Client -> Prometheus Client | Untrusted agent input (metric names/SLO names) must be sanitized/parameterized before hitting Prometheus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Elevation of Privilege | `prometheus_client.ex` | mitigate | Hardcode the PromQL query structure; do not allow arbitrary PromQL execution. Parameterize only specific variables like metric name. |
</threat_model>

<verification>
Ensure all tests in `test/parapet/mcp/prometheus_client_test.exs` pass.
</verification>

<success_criteria>
`Parapet.MCP.PrometheusClient` correctly parses configuration, queries Prometheus using `Req` with parameterized fixed queries, and returns gracefully when Prometheus is not configured.
</success_criteria>

<output>
After completion, create `.planning/phases/3/03-01-SUMMARY.md`
</output>
