---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/spine/incident.ex
  - test/parapet/spine/incident_test.exs
  - lib/parapet/spine/timeline_entry.ex
  - test/parapet/spine/timeline_entry_test.exs
  - lib/parapet/spine/tool_audit.ex
  - test/parapet/spine/tool_audit_test.exs
autonomous: true
requirements: [SPINE-01, SPINE-02, SPINE-03]
must_haves:
  truths:
    - "Incident schema can be loaded and validates open/investigating/resolved states."
    - "TimelineEntry schema can be loaded and linked to an Incident."
    - "ToolAudit schema can be loaded and validates JSON map payloads."
  artifacts:
    - path: "lib/parapet/spine/incident.ex"
      provides: "Incident Ecto schema definition"
    - path: "lib/parapet/spine/timeline_entry.ex"
      provides: "TimelineEntry Ecto schema definition"
    - path: "lib/parapet/spine/tool_audit.ex"
      provides: "ToolAudit Ecto schema definition"
  key_links:
    - from: "lib/parapet/spine/timeline_entry.ex"
      to: "lib/parapet/spine/incident.ex"
      via: "belongs_to association"
---

<objective>
Create the foundational Ecto schemas for the Evidence Spine.

Purpose: Establish the data models for incidents, timeline entries, and tool audits. These schemas must define the database shape without being coupled to a specific Repo, using UUID primary keys, so they can be injected into host applications.
Output: Three valid `Ecto.Schema` modules with changeset validation, and their associated unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/1/RESEARCH.md
@.planning/phases/1/PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Incident schema</name>
  <files>lib/parapet/spine/incident.ex, test/parapet/spine/incident_test.exs</files>
  <behavior>
    - Defines `parapet_incidents` table with `@primary_key {:id, :binary_id, autogenerate: true}`.
    - Fields: state (string, default "open"), title (string), description (text).
    - Changeset validates state is one of "open", "investigating", "resolved".
    - Changeset requires title.
  </behavior>
  <action>
    Create the `Parapet.Spine.Incident` module using `Ecto.Schema` and `Ecto.Changeset`. Create tests that assert changeset validity for valid and invalid states without hitting the database (pure changeset tests).
  </action>
  <verify>
    <automated>mix test test/parapet/spine/incident_test.exs</automated>
  </verify>
  <done>Changeset correctly requires fields and restricts state values. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TimelineEntry schema</name>
  <files>lib/parapet/spine/timeline_entry.ex, test/parapet/spine/timeline_entry_test.exs</files>
  <behavior>
    - Defines `parapet_timeline_entries` table with `:binary_id` primary key.
    - Fields: type (string), payload (map), incident_id (binary_id).
    - Uses `belongs_to :incident, Parapet.Spine.Incident, type: :binary_id`.
    - Changeset requires type and incident_id.
  </behavior>
  <action>
    Create the `Parapet.Spine.TimelineEntry` module. Create tests asserting valid associations and changeset structure without hitting the database. Ensure foreign key uses UUID (`:binary_id`).
  </action>
  <verify>
    <automated>mix test test/parapet/spine/timeline_entry_test.exs</automated>
  </verify>
  <done>Schema establishes belongs_to relationship with Incident. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ToolAudit schema</name>
  <files>lib/parapet/spine/tool_audit.ex, test/parapet/spine/tool_audit_test.exs</files>
  <behavior>
    - Defines `parapet_tool_audits` table with `:binary_id` primary key.
    - Fields: tool_name (string), input (map), output (map), success (boolean), duration_ms (integer), timeline_entry_id (binary_id, nullable).
    - Uses `belongs_to :timeline_entry, Parapet.Spine.TimelineEntry, type: :binary_id`.
    - Changeset requires tool_name, input, success.
  </behavior>
  <action>
    Create the `Parapet.Spine.ToolAudit` module. Log AI/MCP tools. Tests assert pure changeset validation.
  </action>
  <verify>
    <automated>mix test test/parapet/spine/tool_audit_test.exs</automated>
  </verify>
  <done>Schema supports map fields for inputs/outputs. Tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application -> Database | Ecto Changesets protect the data integrity of incidents and auditable fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Incident | mitigate | Changesets rigidly enforce state machine values (open/investigating/resolved). |
| T-01-02 | Info Disclosure | ToolAudit | accept | Assuming tool inputs/outputs do not contain PII natively. This is an application-level concern. |
</threat_model>

<verification>
mix test
</verification>

<success_criteria>
Schemas exist, use `:binary_id` for UUIDs, and changesets validate correct inputs using `mix test`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-spine/01-01-PLAN-SUMMARY.md`
</output>