---
phase: 49-stress-fixtures-seed-coverage
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - examples/demo_app/priv/repo/demo_seed_scenarios.exs
autonomous: true
requirements: [FIXTURE-01, FIXTURE-02, FIXTURE-03, FIXTURE-04, FIXTURE-05]
must_haves:
  truths:
    - "PARAPET_DEMO_SCENARIO=long_string seeds ≥1 incident with long, hyphen-free, machine-shaped title/description/correlation_key/runbook-step labels/external_link URLs/ActionItem fields (FIXTURE-01)"
    - "PARAPET_DEMO_SCENARIO=empty seeds nothing — zero incidents and zero action items (FIXTURE-02)"
    - "PARAPET_DEMO_SCENARIO=max_items seeds more than 30 active incidents, crossing the queue page-size-30 boundary (FIXTURE-03)"
    - "PARAPET_DEMO_SCENARIO=mixed_status seeds incidents across open/investigating/resolved, escalation diversity via reused helpers, and one open action item per ActionItem kind (FIXTURE-04)"
    - "PARAPET_DEMO_SCENARIO=stress seeds the union of long_string+max_items+mixed_status and guarantees ≥1 active incident for the capture-script DETAIL_ID query (FIXTURE-05)"
    - "Each new name is present in DemoSeedScenarios.scenarios() so the typo-guard keeps rejecting unknown values (D-01)"
  artifacts:
    - "examples/demo_app/priv/repo/demo_seed_scenarios.exs (@scenarios extended; five new seed/1 clauses; composable private helpers)"
  key_links:
    - "seeds.exs:7 PARAPET_DEMO_SCENARIO → DemoSeedScenarios.seed/1 (existing seam — unchanged)"
    - "Parapet.Evidence.create_incident/1 + append/3 + ActionItem.changeset |> DemoApp.Repo.insert (only sanctioned write paths)"
    - "stress = long_string + max_items + mixed_status (composition, no duplication — D-06)"
---

<objective>
Extend `examples/demo_app/priv/repo/demo_seed_scenarios.exs` `@scenarios` (currently `response recovery escalation history all`) with five new named scenarios — `long_string`, `empty`, `max_items`, `mixed_status`, and the combined `stress` — all selectable through the EXISTING `PARAPET_DEMO_SCENARIO` seam (no new env var, no new seam). This flips the 49-01 fixture-existence pins from RED to green (D-01..D-06, D-12).

Purpose: Give the operator a re-runnable way to seed worst-case data (overflow + density + status diversity) so the audit can be re-run against the worst-case inputs the Phase-48 overflow/empty/not-found hardening was built to withstand. Demo-only: no public API, telemetry, markup, or UI change — only the demo seed *set* expands.
Output: Five new `seed/1` clauses plus small composable private helpers; the existing five scenarios untouched; each new name added to `@scenarios`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-stress-fixtures-seed-coverage/49-CONTEXT.md
@.planning/phases/49-stress-fixtures-seed-coverage/49-RESEARCH.md
@.planning/phases/49-stress-fixtures-seed-coverage/49-VALIDATION.md

# The seed module to extend (read fully — all existing helpers are the composition source):
@examples/demo_app/priv/repo/demo_seed_scenarios.exs
# The RED pins this plan turns green:
@examples/demo_app/test/demo_app/operator_smoke_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the five new scenarios and add empty + long_string</name>
  <read_first>
    - examples/demo_app/priv/repo/demo_seed_scenarios.exs (@scenarios at :4; the seed/1 dispatch clauses at :8-37; the typo-guard catch-all at :39-42 that raises ArgumentError for unknown names; create_incident + append/3 conventions throughout)
    - lib/parapet/spine/incident.ex (state inclusion at :57 — open/investigating/resolved are the only valid states)
    - lib/parapet/evidence.ex (create_incident/1 at :58 is the only sanctioned incident write path; append_timeline/2 at :91)
    - .planning/phases/49-stress-fixtures-seed-coverage/49-RESEARCH.md (Pitfall 1: empty MUST have its own explicit clause declared BEFORE the catch-all or it raises; D-02 long-string field list)
  </read_first>
  <files>examples/demo_app/priv/repo/demo_seed_scenarios.exs</files>
  <action>Extend `@scenarios` (:4) by adding `long_string empty max_items mixed_status stress` to the existing `~w(response recovery escalation history all)` list, so `scenarios()` reports them and the typo-guard keeps rejecting unknown names (D-01). Add a `def seed("empty"), do: :ok` clause declared ABOVE the catch-all `seed(scenario)` at :39 — it seeds nothing by design (FIXTURE-02 / D-03), and `:ok` is a value the seeds.exs entrypoint tolerates. Add a `def seed("long_string")` clause and a `defp long_string_incident/0` helper that creates ≥1 incident via `Parapet.Evidence.create_incident/1` whose `title`, `description`, and `correlation_key` are deliberately long (well over 60 chars), hyphen-free, and machine-shaped (long module-style names, unbroken IDs/UUID-like tokens), whose `runbook_data` has steps with long unbroken label/description strings, and which appends an `external_link` timeline event (via `append/3`) with a deep query-string URL, plus an `ActionItem` (via `ActionItem.changeset |> DemoApp.Repo.insert`) with a long machine-shaped `external_id` and `title` (FIXTURE-01 / D-02). Use a valid Incident state (e.g. "open") and a valid ActionItem `kind` from the @kinds list and `state: "open"`. Keep the existing five seed clauses and all existing helpers untouched. Do NOT attempt to seed a journey :down — journeys are hardcoded in the LiveView mount and cannot be influenced by seeds (RESEARCH Pitfall 2).</action>
  <verify>
    <automated>cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs 2>&1 | tail -15</automated>
  </verify>
  <done>`scenarios()` includes all five new names; `seed("empty")` produces zero incidents and zero action items in the sandbox; `seed("long_string")` produces ≥1 incident with a >60-char title; the empty/long_string/registry pins pass green.</done>
</task>

<task type="auto">
  <name>Task 2: Add max_items (dense) and mixed_status (status spread)</name>
  <read_first>
    - examples/demo_app/priv/repo/demo_seed_scenarios.exs (existing escalation helpers — checkout_webhook_failures/0 derives manual_trigger_requested, stalled_async_executor/0 derives suppressed, signup_email_resolved/0 derives recently_executed, retry_storm/0 derives recently_short_circuited; an incident with no escalation key derives idle. ActionItem insert convention at :229-239; append/3 at :348)
    - lib/parapet/operator.ex (@default_queue_page_size 30 at :23, @max_queue_page_size 100 at :24, @active_queue_states ["open","investigating"] at :22; list_incident_queue/1 at :78 returns has_next_page?)
    - lib/parapet/spine/action_item.ex (@kinds at :16-22: exact_follow_up, suppressed_delivery, stalled_workflow, orphaned_callback, dead_letter; required title/integration/external_id; state open/resolved)
    - lib/parapet/operator/workbench_contract.ex (derive_escalation_summary/2 at :267-349 — escalation status is DERIVED from runbook_data["escalation"] + a timeline event, never stored)
    - .planning/phases/49-stress-fixtures-seed-coverage/49-RESEARCH.md (Pattern 2 escalation mapping table; Open Questions: 35 active for max_items, one open ActionItem per kind for mixed_status)
  </read_first>
  <files>examples/demo_app/priv/repo/demo_seed_scenarios.exs</files>
  <action>Add `def seed("max_items")` plus a `defp max_items_dense/0` helper that seeds enough active incidents to exceed one queue page — create 35 incidents (Claude's discretion per D-04; ≥31 crosses page size 30 with margin, keeps seed time low) each with state in ["open","investigating"] via `Parapet.Evidence.create_incident/1` (e.g. an `Enum.each(1..35, ...)` loop with distinct correlation_keys), plus a dense set of `ActionItem`s and a longer timeline on at least one incident so dense-list rendering is exercised (FIXTURE-03 / D-04). Add `def seed("mixed_status")` plus a `defp mixed_status_spread/0` helper that surfaces every distinct rendered status visual in one seed (FIXTURE-04 / D-05): seed incidents spanning all three states open/investigating/resolved; achieve escalation diversity by REUSING the four existing escalation helpers (checkout_webhook_failures, stalled_async_executor, signup_email_resolved, retry_storm) plus one bare open incident for the idle status — each derives a distinct escalation status via the runbook_data+timeline mechanism (do not invent new escalation mechanics); and create one OPEN `ActionItem` per kind from the @kinds list (5 total: exact_follow_up, suppressed_delivery, stalled_workflow, orphaned_callback, dead_letter), each attached to a created incident, so the Actions page renders all five kinds. Do NOT seed or assert journey :down (N/A-by-design — hardcoded mount). Compose from helpers; do not duplicate the existing escalation helper bodies.</action>
  <verify>
    <automated>cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs 2>&1 | tail -15</automated>
  </verify>
  <done>`seed("max_items")` produces >30 active incidents (and `list_incident_queue(page_size: 30)` reports `has_next_page?` true); `seed("mixed_status")` produces incidents spanning all three states and ≥1 open ActionItem per kind; the max_items and mixed_status pins pass green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the combined stress scenario (union)</name>
  <read_first>
    - examples/demo_app/priv/repo/demo_seed_scenarios.exs (the new long_string_incident/0, max_items_dense/0, mixed_status_spread/0 helpers added in Tasks 1-2)
    - examples/demo_app/scripts/capture_operator_ui_screenshots.sh (DETAIL_ID query at :35-45 selects an active incident and exits non-zero if none — stress MUST seed ≥1 active incident)
    - .planning/phases/49-stress-fixtures-seed-coverage/49-RESEARCH.md (Pattern 1: stress is the union; Pitfall 5: stress missing an active incident breaks the capture script)
  </read_first>
  <files>examples/demo_app/priv/repo/demo_seed_scenarios.exs</files>
  <action>Add `def seed("stress")` declared above the catch-all that calls the three composable helpers in sequence — `long_string_incident()`, `max_items_dense()`, and `mixed_status_spread()` — so a single seed exercises worst-case overflow + density + status diversity as the union (FIXTURE-05 / D-06). Because `max_items_dense/0` seeds 35 active incidents and `mixed_status_spread/0` seeds open/investigating incidents, the union guarantees ≥1 active (open/investigating) incident, satisfying the capture-script DETAIL_ID precondition (Pitfall 5). Reuse the helpers — do not duplicate their bodies. Keep this clause above the `seed(scenario)` typo-guard catch-all.</action>
  <verify>
    <automated>cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs 2>&1 | tail -15</automated>
  </verify>
  <done>`seed("stress")` runs cleanly and seeds ≥1 active incident plus long-string + dense rows; all 49-01 fixture-existence pins (long_string, empty, max_items, mixed_status, stress, registry) pass green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → seed data | All seed values are author-controlled; the only validation surface is the Incident/ActionItem changeset enum inclusion |
| seeded strings → rendered HTML | long_string external_link URLs/IDs flow into HEEx, which auto-escapes interpolated strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01 | Tampering | Seeded incident state / ActionItem kind+state | low | mitigate | Use only valid enum values from incident.ex:57 and action_item.ex:16-22; an out-of-list value raises at changeset time and fails the 49-01 fixture pins — caught immediately |
| T-49-02 | Tampering/XSS | long_string external_link URLs/IDs in markup | low | accept | HEEx auto-escapes interpolated strings (verified 48-CONTEXT D-02); no raw interpolation introduced — accepted |
| T-49-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — no package install this phase (RESEARCH Package Legitimacy Audit: N/A); accepted |
</threat_model>

<verification>
- Per task: `cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs`
- Per wave merge: `cd examples/demo_app && mix test` (full demo suite) AND `mix test` at repo root (lib suite) — confirm no template/contract regression.
- Manual (deferred raster eyeball): none required this plan; the capture-script run is plan 49-03's concern.
</verification>

<success_criteria>
- `@scenarios` includes long_string, empty, max_items, mixed_status, stress; the existing five untouched.
- Five new `seed/1` clauses, all declared above the typo-guard catch-all; new private helpers compose (stress = union).
- All 49-01 fixture-existence pins green; full demo suite and lib suite green.
- No public API / telemetry / markup change; only the demo seed set expanded.
</success_criteria>

<output>
Create `.planning/phases/49-stress-fixtures-seed-coverage/49-02-SUMMARY.md` when done
</output>
