---
phase: 49-stress-fixtures-seed-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/demo_app/test/demo_app/operator_smoke_test.exs
autonomous: true
requirements: [FIXTURE-01, FIXTURE-02, FIXTURE-03, FIXTURE-04, FIXTURE-05, GALLERY-02]
must_haves:
  truths:
    - "Running the smoke suite before the seed/script edits land fails RED on the new fixture-existence pins (scenarios do not exist yet) per D-12"
    - "GET /parapet/_gallery returns 200 with operator-component markers, proving the route is DB-independent and not swallowed by the /parapet/:id catch-all (GALLERY-02)"
    - "A fixture-existence pin drives each new scenario via DemoSeedScenarios.seed/1 inside the Ecto sandbox and asserts the expected shape (FIXTURE-01..05)"
    - "A static grep pin asserts capture_operator_ui_screenshots.sh covers /parapet/_gallery four times (desktop+mobile, light+dark) (GALLERY-02 script coverage)"
  artifacts:
    - "examples/demo_app/test/demo_app/operator_smoke_test.exs (extended with a Phase-49 describe block)"
  key_links:
    - "DemoApp.DemoSeedScenarios.seed/1 and DemoSeedScenarios.scenarios/0 (asserted before they exist — RED)"
    - "Parapet.Evidence.repo() resolves to DemoApp.Repo, sandboxed by DemoAppWeb.ConnCase, so seed helpers write the rolled-back test DB"
---

<objective>
Add the Phase-49 RED scaffold to the existing demo-contract home `examples/demo_app/test/demo_app/operator_smoke_test.exs`: a gallery render contract test (GALLERY-02) and fixture-existence pins for the five new scenarios (FIXTURE-01..05), plus an optional static grep pin for the capture-script gallery coverage. These assertions are written FIRST (per D-12 RED cadence) — the fixture pins fail RED because `DemoSeedScenarios` does not yet expose the new scenarios, and they flip green when plans 49-02 (seed) and 49-03 (script) land.

Purpose: Establish the ground-truth signals (row counts, state spread, route 200 + markers, script grep) that pin all six requirements before any production-shape edit, so the seed/script work has a failing target to satisfy (D-12, no test-file proliferation — extend the existing file).
Output: An extended `operator_smoke_test.exs` with a `describe "Phase 49 …"` block. No new test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-stress-fixtures-seed-coverage/49-CONTEXT.md
@.planning/phases/49-stress-fixtures-seed-coverage/49-RESEARCH.md
@.planning/phases/49-stress-fixtures-seed-coverage/49-VALIDATION.md

# The test file to extend (read fully before editing):
@examples/demo_app/test/demo_app/operator_smoke_test.exs
# The gallery LiveView whose markers the contract test asserts (Repo-free):
@examples/demo_app/lib/demo_app_web/live/parapet/gallery_live.ex
# The router (live_session :parapet_gallery declared before the :id catch-all):
@examples/demo_app/lib/demo_app_web/router.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the gallery render contract test (GALLERY-02)</name>
  <read_first>
    - examples/demo_app/test/demo_app/operator_smoke_test.exs (ConnCase + Ecto sandbox; existing route/render assert convention; the existing Phase-48 describe block is the structural model)
    - examples/demo_app/lib/demo_app_web/live/parapet/gallery_live.ex (markers verified present: the wrapper class parapet-ui at :26, the gallery page heading text "Parapet Operator UI Gallery" at :28, and operator_nav rendered at :49 which emits the po-operator-title and po-chip component classes)
    - examples/demo_app/lib/demo_app_web/router.ex (live_session :parapet_gallery declared at :22-24 BEFORE the :id catch-all at :31 — the 200 + gallery markers prove the route is not swallowed)
  </read_first>
  <files>examples/demo_app/test/demo_app/operator_smoke_test.exs</files>
  <action>Append a new `describe "Phase 49 gallery + fixture coverage"` block at the end of the module (after the Phase-48 describe block, before the closing `end`). Add a test that does `conn = get(conn, "/parapet/_gallery")`, asserts `conn.status == 200`, and asserts `conn.resp_body =~` each of these stable operator-component markers: the gallery wrapper class `parapet-ui`, the gallery heading text `Parapet Operator UI Gallery`, the nav title class `po-operator-title`, and the status-chip class `po-chip`. Do NOT seed any data in this test — the assertion that the gallery renders 200 with full markers under an empty sandbox IS the DB-independence proof (gallery_live.ex mounts only hardcoded fixtures, no Repo — verified gallery_live.ex:7). Reuse the existing `%{conn: conn}` ConnCase context (per GALLERY-02 / D-10). This test may pass green immediately since the route already exists today — that is correct: it is a route-and-DB-independence regression guard, not a feature gate.</action>
  <verify>
    <automated>cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs -k "gallery"</automated>
  </verify>
  <done>A test named for the `/parapet/_gallery` render exists; running it asserts 200 plus the four markers (parapet-ui, Parapet Operator UI Gallery, po-operator-title, po-chip) against an unseeded sandbox.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fixture-existence pins for the five new scenarios (FIXTURE-01..05) — RED</name>
  <read_first>
    - examples/demo_app/test/demo_app/operator_smoke_test.exs (the "at least one seeded incident exists" test at :57-68 is the canonical self-seed-in-sandbox convention — drive seeds inside the test, assert via DemoApp.Repo.aggregate; never `mix run seeds.exs`)
    - examples/demo_app/priv/repo/demo_seed_scenarios.exs (scenarios/0 at :6 returns @scenarios at :4; seed/1 dispatch + typo-guard at :39-42 — these new names do NOT exist yet, so these assertions fail RED)
    - .planning/phases/49-stress-fixtures-seed-coverage/49-VALIDATION.md (ground-truth definitions: empty⇒count 0; max_items⇒>30 active; mixed_status⇒states ⊇ {open,investigating,resolved}; stress⇒≥1 active; journey diversity is N/A-by-design — do NOT assert a seeded :down journey)
    - lib/parapet/spine/incident.ex (state inclusion at :57: open/investigating/resolved) and lib/parapet/spine/action_item.ex (@kinds at :16-22)
  </read_first>
  <files>examples/demo_app/test/demo_app/operator_smoke_test.exs</files>
  <action>In the same Phase-49 describe block, add fixture-existence pin tests that self-seed inside the sandbox (mirroring the :57-68 convention). Cover each requirement: (1) registry — for each of long_string, empty, max_items, mixed_status, stress, assert the name is in `DemoApp.DemoSeedScenarios.scenarios()` (FIXTURE-05 also requires each name registered). (2) empty (FIXTURE-02) — call `DemoSeedScenarios.seed("empty")` then assert `DemoApp.Repo.aggregate(Parapet.Spine.Incident, :count) == 0` and `DemoApp.Repo.aggregate(Parapet.Spine.ActionItem, :count) == 0`. (3) max_items (FIXTURE-03) — call `seed("max_items")` then assert the count of incidents whose state is in ["open","investigating"] is greater than 30 (use `import Ecto.Query` and a `from i in Incident, where: i.state in ["open","investigating"]` aggregate); optionally also drive `Parapet.Operator.list_incident_queue(page_size: 30)` and assert its `has_next_page?` is true to prove the page boundary. (4) mixed_status (FIXTURE-04) — call `seed("mixed_status")` then assert the MapSet of distinct seeded incident states is a superset of `MapSet.new(["open","investigating","resolved"])`, and assert at least one open ActionItem exists per kind (or that the distinct open-item kind count is at least 5); do NOT assert journey :down (N/A-by-design — journeys are hardcoded in mount, verified operator_live.ex:14-19). (5) long_string (FIXTURE-01) — call `seed("long_string")` then assert at least one incident exists and that its title length exceeds a threshold (e.g. > 60 chars) confirming long machine-shaped fields seeded. (6) stress (FIXTURE-05) — call `seed("stress")` then assert at least one active (open/investigating) incident exists (the capture-script DETAIL_ID precondition). Put each scenario's seed call near its assertion; prefer one `test` block per scenario so each gets a fresh rolled-back sandbox and counts do not accumulate (per RESEARCH "Note on per-scenario isolation"). These assertions WILL fail RED now because the scenarios are not implemented — that is the intended D-12 RED state.</action>
  <verify>
    <automated>cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs -k "scenario or fixture or empty or max_items or mixed or stress" 2>&1 | tail -20</automated>
  </verify>
  <done>Fixture-pin tests exist for all five scenarios plus the registry check; running them fails RED with an ArgumentError/unknown-scenario or missing-name failure (proving the assertions are wired to the not-yet-implemented seed clauses).</done>
</task>

<task type="auto">
  <name>Task 3: Add the static grep pin for capture-script gallery coverage (GALLERY-02 script)</name>
  <read_first>
    - examples/demo_app/scripts/capture_operator_ui_screenshots.sh (existing capture() helper at :47-67; operator-page capture lines at :69-81 — the four /parapet/_gallery capture lines do NOT exist yet, so this assertion fails RED)
    - .planning/phases/49-stress-fixtures-seed-coverage/49-VALIDATION.md (the optional static grep pin: `grep -c '_gallery' …` ⇒ ≥4)
  </read_first>
  <files>examples/demo_app/test/demo_app/operator_smoke_test.exs</files>
  <action>In the Phase-49 describe block, add one cheap static-coverage test that reads `examples/demo_app/scripts/capture_operator_ui_screenshots.sh` from disk (build the path relative to the demo app root, e.g. `Path.join([File.cwd!(), "scripts", "capture_operator_ui_screenshots.sh"])`) and asserts the file contents contain at least four occurrences of the gallery route token by counting matches — assert the number of `_gallery` capture lines is at least 4 (one each for desktop-light, desktop-dark, mobile-light, mobile-dark). Compute the count by splitting the file on newlines and counting lines that contain both `capture` and `_gallery`, so plain comments mentioning the route do not inflate the count. This pins that plan 49-03 keeps the four gallery captures in the canonical audit-capture script (GALLERY-02 script coverage). It fails RED until 49-03 adds the capture lines.</action>
  <verify>
    <automated>cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs -k "capture or grep or script or coverage" 2>&1 | tail -10</automated>
  </verify>
  <done>A static-coverage test exists that counts gallery capture lines in the script and asserts ≥4; it fails RED now (the lines are absent) and will pass once 49-03 lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → seed data | Seed values are author-controlled (not user input); the only risk is invalid enum values that raise at changeset time |
| route → rendered HTML | Long-string fixtures feed external_link URLs/IDs into HEEx, which auto-escapes interpolated strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01 | Tampering | Seeded incident/action-item enum fields (kind/state) | low | mitigate | Fixture pins assert seeds run cleanly; seeds use validated enum values from Incident state (incident.ex:57) and ActionItem @kinds (action_item.ex:16-22) — an out-of-list value raises at changeset time and fails the pin |
| T-49-02 | Tampering/XSS | Long-string external_link URLs/IDs in rendered markup | low | accept | HEEx auto-escapes interpolated strings (verified 48-CONTEXT D-02); no raw interpolation introduced — accepted, no new control needed |
| T-49-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — no package install this phase (RESEARCH Package Legitimacy Audit: N/A); accepted |
</threat_model>

<verification>
- Quick (per task): `cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs`
- The Phase-49 fixture-pin and grep tests are EXPECTED to fail RED at the end of this plan (D-12). The gallery render contract test (Task 1) may pass green immediately (route exists today) — that is correct.
- Do NOT implement the seed scenarios or script edits in this plan; that is 49-02 and 49-03.
</verification>

<success_criteria>
- `operator_smoke_test.exs` has a Phase-49 describe block with: 1 gallery render contract test, fixture-existence pins for all five scenarios + the registry check, and 1 static grep pin for the script.
- Fixture-pin and grep tests fail RED (scenarios/capture lines not yet implemented); gallery render test passes (regression guard).
- No new test files created; no production source edited.
</success_criteria>

<output>
Create `.planning/phases/49-stress-fixtures-seed-coverage/49-01-SUMMARY.md` when done
</output>
