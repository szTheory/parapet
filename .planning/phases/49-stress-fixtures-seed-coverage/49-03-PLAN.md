---
phase: 49-stress-fixtures-seed-coverage
plan: 03
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - examples/demo_app/scripts/capture_operator_ui_screenshots.sh
autonomous: true
requirements: [FIXTURE-05, GALLERY-02]
must_haves:
  truths:
    - "capture_operator_ui_screenshots.sh adds four /parapet/_gallery captures — desktop+mobile, light+dark — reusing the existing capture() helper and ?parapet_theme= convention (GALLERY-02)"
    - "The gallery captures use tall window sizes so the long scrolling gallery page is not clipped (RESEARCH Pitfall 6)"
    - "The script is run against a PARAPET_DEMO_SCENARIO=stress-seeded DB so the combined stress scenario is covered across desktop+mobile and light+dark (FIXTURE-05)"
    - "gallery_preview.sh --shot is left untouched (DB-less dev preview is a separate concern — D-08)"
    - "No rasters committed — PNG output stays in the parameterized OUTPUT_DIR/tmp (D-09)"
  artifacts:
    - "examples/demo_app/scripts/capture_operator_ui_screenshots.sh (extended with four gallery capture lines)"
  key_links:
    - "capture() helper (name/size/path/theme) — reused, not re-implemented (already quotes CHROME_BIN, handles ?parapet_theme=)"
    - "gallery route /parapet/_gallery served by the same booted demo Endpoint, no DB dependency (server-up only)"
---

<objective>
Extend `examples/demo_app/scripts/capture_operator_ui_screenshots.sh` — the single canonical DB-backed audit-capture script — to add four `/parapet/_gallery` captures (desktop + mobile, light + dark), reusing the existing `capture()` helper and `?parapet_theme=` convention. Run against a `PARAPET_DEMO_SCENARIO=stress`-seeded DB, this gives the combined stress scenario coverage across desktop+mobile and light+dark (FIXTURE-05) AND covers the gallery route (GALLERY-02). This flips the 49-01 static grep pin green (D-07).

Purpose: Make one canonical audit-capture script cover both the stress operator pages and the gallery, so the operator can re-run the visual audit against worst-case data on demand. The gallery renders from hardcoded fixtures (Repo-free), so it needs no DB seed — only that the demo server is up.
Output: Four new gallery capture lines in the canonical script. `gallery_preview.sh` untouched; no rasters committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-stress-fixtures-seed-coverage/49-CONTEXT.md
@.planning/phases/49-stress-fixtures-seed-coverage/49-RESEARCH.md
@.planning/phases/49-stress-fixtures-seed-coverage/49-VALIDATION.md

# The script to extend (read fully — capture() helper + existing capture lines):
@examples/demo_app/scripts/capture_operator_ui_screenshots.sh
# The RED grep pin this plan turns green:
@examples/demo_app/test/demo_app/operator_smoke_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add four /parapet/_gallery captures to the canonical script</name>
  <read_first>
    - examples/demo_app/scripts/capture_operator_ui_screenshots.sh (capture() helper at :47-67 — takes name/size/path/theme, already quotes "$CHROME_BIN", handles the ?parapet_theme= vs &parapet_theme= query-string branching; existing operator-page capture lines at :69-81; the final `ls -1 "$OUTPUT_DIR"/*.png` at :82)
    - examples/demo_app/scripts/gallery_preview.sh (the sibling DB-less dev preview — DO NOT TOUCH; read only to copy its tall gallery window sizes 1440,5200 desktop / 414,7600 mobile, verified :130-133, because the gallery is a long scrolling page)
    - .planning/phases/49-stress-fixtures-seed-coverage/49-RESEARCH.md (Pitfall 6: a short window clips the long gallery — use tall windows; Pitfall 7: reuse capture() for correct quoting; the capture-script gallery extension code example with the four exact lines)
  </read_first>
  <files>examples/demo_app/scripts/capture_operator_ui_screenshots.sh</files>
  <action>After the existing operator-page capture lines (the mobile block ending at the `operator-response-dark-mobile` line, :81) and BEFORE the final `ls -1 "$OUTPUT_DIR"/*.png` (:82), add four gallery capture calls that reuse the existing `capture()` helper with the path `/parapet/_gallery`: a desktop-light, a desktop-dark, a mobile-light, and a mobile-dark capture (GALLERY-02 / D-07). Use TALL window sizes so the long scrolling gallery is not clipped (RESEARCH Pitfall 6) — `1440,5200` for the desktop captures and `414,7600` for the mobile captures, matching the sibling gallery_preview.sh heights (verified :130-133). Keep the existing operator-page captures and their `1440,1100` / `390,844` sizes unchanged (those are correct for the shorter pages). Do NOT inline a new chrome invocation, do NOT introduce a new theming/env seam (reuse `?parapet_theme=` via capture()), and do NOT modify gallery_preview.sh (D-08, separate DB-less dev preview). The gallery needs no DB seed — it is served by the same booted Endpoint from hardcoded fixtures. PNG output continues to land in the parameterized OUTPUT_DIR (no rasters committed — D-09).</action>
  <verify>
    <automated>bash -n examples/demo_app/scripts/capture_operator_ui_screenshots.sh && [ "$(grep -c '^capture .*_gallery' examples/demo_app/scripts/capture_operator_ui_screenshots.sh)" -ge 4 ] && echo OK</automated>
  </verify>
  <done>The script parses cleanly (`bash -n`) and contains exactly four `_gallery` capture lines (desktop+mobile, light+dark) using tall window sizes; the four lines sit between the operator-page captures and the final `ls`.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the static grep pin flips green</name>
  <read_first>
    - examples/demo_app/test/demo_app/operator_smoke_test.exs (the Phase-49 static-coverage test added in 49-01 that counts `capture … _gallery` lines and asserts ≥4)
  </read_first>
  <files>examples/demo_app/scripts/capture_operator_ui_screenshots.sh</files>
  <action>Run the 49-01 static grep pin to confirm the four gallery capture lines satisfy its ≥4 assertion. If the pin still fails, the capture lines are not in the form the pin counts (it counts lines containing both `capture` and `_gallery`) — adjust the capture lines (not the test) so each gallery line literally calls the `capture` helper with the `_gallery` path on one line. No production behavior changes here; this task is the green-flip confirmation for GALLERY-02 script coverage. Do NOT run headless Chrome in CI — the test gate asserts the script's coverage statically, never by executing capture (RESEARCH Environment Availability: Chrome not required for the test gate).</action>
  <verify>
    <automated>cd examples/demo_app && mix test test/demo_app/operator_smoke_test.exs 2>&1 | tail -10</automated>
  </verify>
  <done>The 49-01 static grep pin passes green (script contains ≥4 gallery capture lines); no test edits were needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → bash script | OUTPUT_DIR/BASE_URL/CHROME_BIN are operator-supplied; the existing script quotes all expansions |
| capture() → headless Chrome | The gallery path has no query string; capture() appends ?parapet_theme= (the else branch) — correct |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-03 | Tampering | Bash env interpolation in the capture script | low | mitigate | Reuse the existing capture() helper for the gallery lines — it already quotes "$CHROME_BIN" and handles the theme query-string branching; no new unquoted expansion is introduced |
| T-49-04 | Information Disclosure | Committed PNG rasters leaking demo data | low | mitigate | No rasters committed (D-09) — PNG output stays in the parameterized OUTPUT_DIR/tmp; committed baselines are Phase 50 |
| T-49-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — no package install this phase (RESEARCH Package Legitimacy Audit: N/A); accepted |
</threat_model>

<verification>
- Per task: `bash -n examples/demo_app/scripts/capture_operator_ui_screenshots.sh` (syntax) and the 49-01 static grep pin via `mix test`.
- Per wave merge: `cd examples/demo_app && mix test` AND `mix test` at repo root — no regression.
- Manual (deferred): the operator runs the script once against a PARAPET_DEMO_SCENARIO=stress-seeded DB to eyeball PNGs (operator pages + gallery, desktop+mobile, light+dark) into tmp; committed baselines/manifest are Phase 50 (D-09). Not a CI gate.
</verification>

<success_criteria>
- Four `/parapet/_gallery` captures (desktop+mobile, light+dark) added to the canonical script, reusing capture() and tall window sizes.
- `bash -n` passes; the 49-01 static grep pin is green.
- gallery_preview.sh untouched; no rasters committed; no new theming/env seam.
</success_criteria>

<output>
Create `.planning/phases/49-stress-fixtures-seed-coverage/49-03-SUMMARY.md` when done
</output>
