---
phase: "01"
plan: "04"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["lib/mix/tasks/parapet.install.ex"]
autonomous: true
requirements: ["INST-01", "INST-02", "INST-03", "INST-04", "INST-05"]
must_haves:
  truths:
    - "A developer can run mix parapet.install to bootstrap host-owned artifacts"
    - "The installer modifies the Endpoint idempotently"
    - "Dry-run capabilities are native via Igniter"
  artifacts:
    - path: "lib/mix/tasks/parapet.install.ex"
      provides: "Igniter installer orchestration"
  key_links:
    - from: "Endpoint"
      to: "Parapet.Plug.Metrics"
      via: "plug injection via Igniter"
      pattern: "plug Parapet\\.Plug\\.Metrics"
---

<objective>
Build the `mix parapet.install` Igniter-based code generator.

Purpose: Provide a frictionless, idempotent setup experience that places host-owned instrumentation hooks into the user's application cleanly.
Output: A Mix task leveraging Igniter for safe AST patching of `Endpoint` and `config.exs`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer Generator Orchestration</name>
  <files>lib/mix/tasks/parapet.install.ex</files>
  <read_first>lib/mix/tasks/parapet.install.ex</read_first>
  <action>
Implement `Mix.Tasks.Parapet.Install`.
It must `use Igniter.Mix.Task`.
In `def igniter(igniter)`, it should:
1. Identify the host application name via `Igniter.Project.Application.app_name(igniter)`.
2. Scaffold a new module `#{app_name_camel}.ParapetInstrumenter` at `lib/#{app_name}/parapet_instrumenter.ex` utilizing `Igniter.Project.Module.create_module/3`.
3. Use `Igniter.Project.Config.configure(igniter, "config.exs", :parapet, [:instrumenter], {:__aliases__, [alias: false], [app_name_camel, :ParapetInstrumenter]})` or similar to inject config with comments.
4. Modify the `Endpoint` module to prepend or append `plug Parapet.Plug.Metrics`. (Use Igniter AST functions or `Igniter.Code.Function.argument_equals?` logic to safely patch without duplicating).
  </action>
  <acceptance_criteria>
    - `lib/mix/tasks/parapet.install.ex` contains exact string `use Igniter.Mix.Task`
    - `lib/mix/tasks/parapet.install.ex` contains exact string `plug Parapet.Plug.Metrics`
    - `lib/mix/tasks/parapet.install.ex` contains exact string `Igniter.Project.Config.configure`
  </acceptance_criteria>
  <verify>
    <automated>grep "use Igniter.Mix.Task" lib/mix/tasks/parapet.install.ex</automated>
  </verify>
  <done>Installer orchestrates code injection via Igniter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System | Installer runs at dev time, modifying user configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Installer | mitigate | Rely on Igniter AST-aware patching instead of brittle sed/regex to avoid corrupting host app files. |
</threat_model>

<verification>
mix compile
</verification>

<success_criteria>
The installer compiles cleanly and provides standard Igniter `--dry-run` operations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>