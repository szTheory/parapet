---
phase: 51-prefix-core-test-seam
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - lib/parapet/spine/incident.ex
  - lib/parapet/spine/action_item.ex
  - lib/parapet/spine/system_event.ex
  - lib/parapet/spine/tool_audit.ex
  - lib/parapet/spine/timeline_entry.ex
  - lib/parapet/spine/action_claim.ex
autonomous: true
requirements: [PREFIX-01, PREFIX-02]
must_haves:
  truths:
    - "All six spine schemas use Parapet.Spine.Schema instead of use Ecto.Schema"
    - "Mod.__schema__(:prefix) == \"parapet\" for Incident, ActionItem, SystemEvent, ToolAudit, TimelineEntry, ActionClaim under default config"
    - "Interleaved module attributes (@triage_fields, @type t, alias) are preserved in every file"
  artifacts:
    - lib/parapet/spine/incident.ex
    - lib/parapet/spine/action_item.ex
    - lib/parapet/spine/action_claim.ex
  key_links:
    - "use Parapet.Spine.Schema injects @schema_prefix Parapet.Spine.Schema.__prefix__() into each schema module"
    - "schema_test.exs compiled-prefix-across-six block goes GREEN once all six are switched"
---

<objective>
Switch all six spine schemas from `use Ecto.Schema` to `use Parapet.Spine.Schema`, deleting
the four now-duplicated boilerplate lines from each. This is pure subtraction (D-02) — the
macro from Plan 01 re-injects the identical `@primary_key`/`@foreign_key_type`/`import
Ecto.Changeset`/`use Ecto.Schema` plus the new `@schema_prefix`, so behavior is unchanged
except that every spine schema now carries `@schema_prefix "parapet"`.

Purpose: Make the compile-time prefix ride all six spine schemas through the single shared
macro, deduping the repeated boilerplate (PREFIX-01) and giving `__schema__(:prefix)` its
`"parapet"` value (PREFIX-02).

Output: Six edited files under `lib/parapet/spine/`; the `schema_test.exs`
compiled-prefix-across-six block turns GREEN.

Implements: D-02 (D-03 by exclusion — `action_payload.ex` untouched). Requirements PREFIX-01, PREFIX-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-prefix-core-test-seam/51-CONTEXT.md
@.planning/phases/51-prefix-core-test-seam/51-RESEARCH.md
@.planning/phases/51-prefix-core-test-seam/51-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
No NEW public symbols — this plan only swaps the `use` directive and removes duplicated
attributes. The `@schema_prefix` value on each of the six modules is the observable change.
Excluded from drift verification: nothing net-new (the macro and tests were produced by Plan 01).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Switch Incident, ActionItem, ActionClaim to use Parapet.Spine.Schema</name>
  <files>lib/parapet/spine/incident.ex, lib/parapet/spine/action_item.ex, lib/parapet/spine/action_claim.ex</files>
  <read_first>
    - lib/parapet/spine/incident.ex (full — note @triage_fields at 14-25 and @type t at 29-39 sit BETWEEN the lines to delete: import at 12, @primary_key at 27)
    - lib/parapet/spine/action_item.ex (full)
    - lib/parapet/spine/action_claim.ex (full)
    - lib/parapet/spine/schema.ex (the macro produced by Plan 01 — confirm it injects all four boilerplate lines)
    - .planning/phases/51-prefix-core-test-seam/51-RESEARCH.md (Pattern 2 before/after; Pitfall 5 interleaved-attribute hazard; D-02 line map: incident.ex:11-12,27-28; action_item.ex:11-12,24-25; action_claim.ex:11-12,27-28)
  </read_first>
  <action>
    In each of the three files, replace the line `use Ecto.Schema` with `use Parapet.Spine.Schema`, and
    DELETE the three now-duplicated lines `import Ecto.Changeset`, `@primary_key {:id, :binary_id,
    autogenerate: true}`, and `@foreign_key_type :binary_id`. These four lines are NOT contiguous in
    incident.ex (and possibly others): `@triage_fields` (incident.ex:14-25) and `@type t` (incident.ex:29-39)
    sit between `import` (12) and `@primary_key` (27). Surgically remove ONLY the four boilerplate lines;
    preserve every interleaved attribute, `alias`, `@type t`, `@triage_fields`, the moduledoc, the `schema
    "parapet_*" do` block, and all field/changeset code. Do NOT rename any table. Do NOT touch
    `lib/parapet/operator/action_payload.ex` (D-03).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -10 && mix test test/parapet/spine/incident_test.exs test/parapet/spine/action_item_test.exs test/parapet/spine/action_claim_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three files contains `use Parapet.Spine.Schema` and NO bare `use Ecto.Schema`.
    - Each file no longer declares `import Ecto.Changeset`, `@primary_key {:id, :binary_id, autogenerate: true}`, or `@foreign_key_type :binary_id` at module level (the macro supplies them).
    - `@triage_fields`, `@type t`, the `schema "parapet_incidents"`/`"parapet_action_items"`/`"parapet_action_claims"` blocks, and all changeset functions are intact.
    - `mix compile --warnings-as-errors` clean; the three existing per-schema tests pass.
    - `Parapet.Spine.Incident.__schema__(:prefix) == "parapet"` (and likewise ActionItem, ActionClaim) via `iex -S mix`.
  </acceptance_criteria>
  <done>Incident, ActionItem, ActionClaim use the shared macro; boilerplate deduped; interleaved attributes preserved; prefix is "parapet".</done>
</task>

<task type="auto">
  <name>Task 2: Switch SystemEvent, ToolAudit, TimelineEntry to use Parapet.Spine.Schema</name>
  <files>lib/parapet/spine/system_event.ex, lib/parapet/spine/tool_audit.ex, lib/parapet/spine/timeline_entry.ex</files>
  <read_first>
    - lib/parapet/spine/system_event.ex (full — boilerplate at 12-13,15-16)
    - lib/parapet/spine/tool_audit.ex (full — boilerplate at 11-12,16-17)
    - lib/parapet/spine/timeline_entry.ex (full — boilerplate at 11-12,30-31)
    - lib/parapet/spine/schema.ex (the macro from Plan 01)
    - .planning/phases/51-prefix-core-test-seam/51-RESEARCH.md (Pattern 2; Pitfall 5; D-02 line map for these three)
  </read_first>
  <action>
    Apply the identical pure-subtraction swap to the remaining three: replace `use Ecto.Schema` with
    `use Parapet.Spine.Schema` and delete the three duplicated lines (`import Ecto.Changeset`, `@primary_key
    {:id, :binary_id, autogenerate: true}`, `@foreign_key_type :binary_id`). Watch for interleaved attributes
    in each file and preserve them along with the moduledoc, `@type t`, the `schema "parapet_*" do` block, and
    all field/changeset code. Tables keep their names. action_payload.ex remains untouched (D-03).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -10 && mix test test/parapet/spine/timeline_entry_test.exs test/parapet/spine/tool_audit_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three files uses `use Parapet.Spine.Schema` with no bare `use Ecto.Schema` and no module-level `@primary_key`/`@foreign_key_type`/`import Ecto.Changeset`.
    - Interleaved attributes, `@type t`, schema blocks, and changesets intact in all three.
    - `mix compile --warnings-as-errors` clean; existing timeline_entry and tool_audit tests pass.
    - `Parapet.Spine.SystemEvent.__schema__(:prefix) == "parapet"` (and ToolAudit, TimelineEntry).
  </acceptance_criteria>
  <done>All six spine schemas now use the shared macro; the full set carries `@schema_prefix "parapet"`.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN the compiled-prefix-across-six assertions</name>
  <files>test/parapet/spine/schema_test.exs</files>
  <read_first>
    - test/parapet/spine/schema_test.exs (the compiled-prefix-across-six block from Plan 01 Task 1, now expected GREEN)
    - .planning/phases/51-prefix-core-test-seam/51-VALIDATION.md (check 2 — `__schema__(:prefix) == "parapet"` for all six)
  </read_first>
  <action>
    Run the schema_test.exs suite and confirm the compiled-prefix-across-six block (asserting
    `Mod.__schema__(:prefix) == "parapet"` for all six modules) is now GREEN as a direct consequence of
    Tasks 1-2. Do NOT modify assertions to force green — the value comes from the macro + config seam already
    in place. If any module still returns a non-"parapet" prefix, the fix is in that schema file's `use`
    directive (a missed swap), not in the test. No production code should change in this task unless a missed
    swap is found.
  </action>
  <verify>
    <automated>mix test test/parapet/spine/schema_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/parapet/spine/schema_test.exs` is fully GREEN (all three describe blocks pass).
    - The compiled-prefix assertion passes for all six modules: Incident, ActionItem, SystemEvent, ToolAudit, TimelineEntry, ActionClaim.
    - No assertion was weakened to achieve green.
  </acceptance_criteria>
  <done>PREFIX-01 and PREFIX-02 fully verified: all six schemas share the macro and report `__schema__(:prefix) == "parapet"`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macro → schema module | `use Parapet.Spine.Schema` injects compile-time attributes into each spine module |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-04 | Tampering | surgical deletion accidentally removing interleaved attributes (@triage_fields, @type t, alias) | medium | mitigate | Pitfall 5 enforced: delete only the four boilerplate lines; `mix compile --warnings-as-errors` + per-schema tests catch a removed attribute |
| T-51-05 | Denial of Service | a missed swap leaving one schema unprefixed (split-brain across the six) | medium | mitigate | compiled-prefix-across-six test (Task 3) asserts `"parapet"` on all six explicitly; one miss → red |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean after all six swaps.
- `mix test test/parapet/spine/schema_test.exs` fully GREEN.
- Each spine file: `use Parapet.Spine.Schema`, no bare `use Ecto.Schema`, no module-level `@primary_key`/`@foreign_key_type`/`import Ecto.Changeset`.
- `lib/parapet/operator/action_payload.ex` unchanged (D-03).
</verification>

<success_criteria>
- All six spine schemas use the shared macro (pure subtraction; behavior unchanged).
- `__schema__(:prefix) == "parapet"` for all six under default config.
- Interleaved attributes and table names preserved.
</success_criteria>

<output>
Create `.planning/phases/51-prefix-core-test-seam/51-02-SUMMARY.md` when done.
</output>
