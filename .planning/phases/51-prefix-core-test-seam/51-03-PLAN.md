---
phase: 51-prefix-core-test-seam
plan: 03
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - test/support/concurrency_bootstrap.ex
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "concurrency_bootstrap.ex qualifies all DDL by the SAME resolved prefix the macro uses (not a hardcoded \"parapet\")"
    - "Under schema_prefix: parapet, bootstrap!/0 issues CREATE SCHEMA IF NOT EXISTS \"parapet\" then qualified DDL"
    - "Index ON targets are qualified; index NAMES stay bare"
    - "Full suite (mix test) is green under schema_prefix: parapet"
  artifacts:
    - test/support/concurrency_bootstrap.ex
  key_links:
    - "@prefix read from Application.compile_env(:parapet, :schema_prefix, \"parapet\") with the D-04 normalization == the compiled @schema_prefix on the six schemas"
    - "q(table) helper qualifies CREATE TABLE / REFERENCES / index ON targets / TRUNCATE list; public leg (prefix nil) stays unprefixed"
---

<objective>
Hand-qualify `test/support/concurrency_bootstrap.ex` so the hand-written test DDL targets the
same schema the compiled `@schema_prefix` points at, then prove the full suite is green under
`schema_prefix: parapet` (D-10's done-criterion). The bootstrap reads the SAME resolved prefix
the macro computes (parameterized, not a literal "parapet") so the `public`/unprefixed leg
stays byte-identical for Phase 52.

Purpose: Without this, every spine read/write under the prefixed leg hits a `parapet`-schema
table that the bootstrap created in `public` → `relation does not exist`. Qualifying the DDL is
what lets the existing suite run at all under the prefix (TEST-02).

Output: Edited `concurrency_bootstrap.ex` (CREATE SCHEMA prelude + qualified 6 tables / 4
references / 12 index ON targets / TRUNCATE list); `mix test` green.

Implements: D-09, D-10. Requirement TEST-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-prefix-core-test-seam/51-CONTEXT.md
@.planning/phases/51-prefix-core-test-seam/51-RESEARCH.md
@.planning/phases/51-prefix-core-test-seam/51-VALIDATION.md
@.planning/phases/51-prefix-core-test-seam/51-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
No NEW public symbols. Internal helpers added to `Parapet.TestSupport.ConcurrencyBootstrap`:
a module attribute `@prefix` (resolved prefix) and a private `q/1` qualification helper. These
are test-support internals, excluded from drift / public-API verification.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize the bootstrap prefix + add CREATE SCHEMA prelude and q/1 helper</name>
  <files>test/support/concurrency_bootstrap.ex</files>
  <read_first>
    - test/support/concurrency_bootstrap.ex (full — `@tables` at 7-14, `bootstrap!/0` at 16-18, `reset!/0` TRUNCATE at 20-26, `ddl_statements/0` at 30-163)
    - lib/parapet/spine/schema.ex (the `__prefix__/0` normalization — the bootstrap must resolve the SAME value the same way)
    - .planning/research/v1.7/04-TEST-STRATEGY.md (§3.2 — bootstrap qualification adapted to live file)
    - .planning/phases/51-prefix-core-test-seam/51-RESEARCH.md (Pattern 5; Anti-Patterns "Hardcoding parapet", "Qualifying the index name"; Pitfalls 1, 3, 4)
  </read_first>
  <action>
    Add a module attribute resolving the prefix exactly as the macro does — `@prefix` from
    `Application.compile_env(:parapet, :schema_prefix, "parapet")` normalized via the D-04 `case`
    (`p in [nil, "", "public"] -> nil`; binary -> itself). Do NOT hardcode `"parapet"`: reading the resolved
    value is what keeps the `public` leg unprefixed for Phase 52 (Anti-Pattern).

    Add a private `q/1` that, when `@prefix` is non-nil, returns the double-quoted `"<prefix>"."<table>"`
    qualified identifier, and when `@prefix` is nil returns the bare `"<table>"`. Update `bootstrap!/0` to,
    when `@prefix` is non-nil, first issue `CREATE SCHEMA IF NOT EXISTS "<prefix>"` before the DDL statements;
    when nil, skip the CREATE SCHEMA entirely.

    Do NOT yet rewrite the individual DDL strings (Task 2 does that) — but make `@tables`/`reset!`'s TRUNCATE
    list use `q/1` so the truncate targets the qualified tables. Leave `schema_migrations` alone — it does NOT
    appear anywhere in this file (0 occurrences), so there is no action for it here (Pitfall 4). Index NAMES
    must remain bare in the next task — `q/1` qualifies table TARGETS only.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -10 && grep -n 'compile_env(:parapet, :schema_prefix' test/support/concurrency_bootstrap.ex</automated>
  </verify>
  <acceptance_criteria>
    - `@prefix` is read via `Application.compile_env(:parapet, :schema_prefix, "parapet")` with the D-04 normalization — NO bare `"parapet"` literal drives qualification.
    - `q/1` returns `"<prefix>"."<table>"` when prefixed and bare `"<table>"` when `@prefix` is nil.
    - `bootstrap!/0` issues `CREATE SCHEMA IF NOT EXISTS "<prefix>"` only when `@prefix` is non-nil.
    - `reset!/0` TRUNCATE list is built from `q/1` (qualified under the prefixed leg).
    - No `schema_migrations` reference is introduced (it is absent from this file by design).
    - `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>Bootstrap resolves the same prefix the macro uses; CREATE SCHEMA prelude + `q/1` helper in place; TRUNCATE qualified; public leg stays unprefixed.</done>
</task>

<task type="auto">
  <name>Task 2: Qualify all 6 CREATE TABLE / 4 REFERENCES / 12 index ON targets</name>
  <files>test/support/concurrency_bootstrap.ex</files>
  <read_first>
    - test/support/concurrency_bootstrap.ex (the `ddl_statements/0` strings — 6 CREATE TABLE, 4 inline REFERENCES at lines 68/78/99/128, 12 CREATE INDEX ON targets at lines 47,52,57,85,89,105,109,122,147,151,155,159)
    - .planning/phases/51-prefix-core-test-seam/51-RESEARCH.md (Pitfall 1 index-name footgun; Pitfall 3 — 12 not 11 ON targets; Pattern 5)
    - .planning/phases/51-prefix-core-test-seam/51-VALIDATION.md (check 5 — 6 tables / 4 references / 12 index ON targets / TRUNCATE)
  </read_first>
  <action>
    Rewrite the `ddl_statements/0` DDL so every table identifier flows through `q/1` (or its qualified form):
    qualify all 6 `CREATE TABLE IF NOT EXISTS <target>`, all 4 inline `REFERENCES <target>(id)` (lines
    68, 78, 99, 128 — action_items→incidents, timeline_entries→incidents, tool_audits→timeline_entries,
    action_claims→incidents), and all 12 `CREATE [UNIQUE] INDEX ... ON <target>` TARGETS (lines
    47,52,57,85,89,105,109,122,147,151,155,159). The count is **12**, not 11 (the locked D-09 text undercounts
    by one — confirmed by `grep -c "CREATE.*INDEX"` returning 12).

    CRITICAL: qualify only the `ON <table>` TARGET and the `REFERENCES <table>` target. Do NOT qualify the
    index NAME — `CREATE INDEX "parapet"."idx_name" ON ...` is invalid Postgres; an index lives in its table's
    schema automatically (Pitfall 1). Leave all 12 index names bare. Since the DDL strings are heredocs,
    interpolate the qualified target via `q/1` (the strings become interpolated heredocs or are assembled so
    the resolved qualified identifier is substituted). Under `@prefix` nil the output must be byte-identical to
    the current unprefixed DDL.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -10 && grep -c 'CREATE.*INDEX' test/support/concurrency_bootstrap.ex</automated>
  </verify>
  <acceptance_criteria>
    - All 6 CREATE TABLE targets, all 4 REFERENCES targets, and all 12 index ON targets route through the `@prefix`-aware qualification.
    - All 12 index NAMES remain bare (no `"<prefix>"."idx_*"` anywhere); `grep -c 'CREATE.*INDEX'` still returns 12.
    - Under `@prefix` nil the emitted DDL is the unprefixed legacy form (public leg unchanged).
    - `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>Every CREATE TABLE / REFERENCES / index ON target is prefix-qualified; index names stay bare; 12 ON targets handled.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite green under schema_prefix: parapet (D-10 done-criterion)</name>
  <files>test/support/concurrency_bootstrap.ex</files>
  <read_first>
    - test/test_helper.exs (boots ConcurrencyRepo + ConcurrencyBootstrap.bootstrap!/0 + Sandbox manual mode)
    - .planning/phases/51-prefix-core-test-seam/51-VALIDATION.md (check 4 — full suite green; §"Sampling Rate")
    - .planning/phases/51-prefix-core-test-seam/51-CONTEXT.md (D-10 — done-criterion is suite green under parapet; dual-leg is Phase 52)
  </read_first>
  <action>
    Run the entire `mix test` suite under the default config (`schema_prefix: parapet`). The bootstrap now
    creates `CREATE SCHEMA IF NOT EXISTS "parapet"` and all spine tables/indexes under `"parapet"."parapet_*"`,
    matching the compiled `@schema_prefix` on the six schemas. Any missed qualification surfaces as
    `relation "..." does not exist` — fix the offending DDL line in the bootstrap (do not relax the test or
    the schemas). This is D-10's done-criterion. Do NOT attempt the unprefixed/`public` leg or a recompiling
    matrix here — that honest dual-leg proof is Phase 52 (out of scope).
  </action>
  <verify>
    <automated>mix test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `mix test` exits 0 (full suite green) under default config `schema_prefix: parapet`.
    - No `relation ... does not exist` errors from the bootstrap.
    - The spine tables are created under the `parapet` schema (suite-green is the structural proof per VALIDATION check 5).
    - No Phase 52 work introduced (no dual-leg matrix, no `mix compile --force`, no `_build` namespacing).
  </acceptance_criteria>
  <done>TEST-02 satisfied: full suite green under `schema_prefix: parapet` with the hand-qualified bootstrap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bootstrap DDL → test Postgres | hand-written DDL strings provision the test schema; must match the compiled `@schema_prefix` |
| resolved prefix → DDL identifiers | `@prefix` interpolated into quoted SQL identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-06 | Denial of Service | a missed `ON`/REFERENCES/TABLE qualification creating a table in the wrong schema | high | mitigate | Full-suite-green gate (Task 3) — any miss → `relation does not exist`; 12-count grep + per-line audit in Task 2 |
| T-51-07 | Tampering | hardcoding "parapet" in the bootstrap, breaking the Phase 52 public leg | medium | mitigate | `@prefix` read from compile_env with D-04 normalization; verify gate greps for the compile_env read, not a literal |
| T-51-08 | Tampering | qualifying an index NAME → invalid DDL (`syntax error at or near "."`) | low | mitigate | Task 2 explicitly leaves all 12 index names bare; q/1 targets `ON <table>` only; compile/suite catches a malformed CREATE INDEX |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `grep -c 'CREATE.*INDEX'` returns 12; index names verified bare.
- `mix test` exits 0 (full suite green under `schema_prefix: parapet`).
- `@prefix` driven by `Application.compile_env(:parapet, :schema_prefix, "parapet")`, not a literal.
</verification>

<success_criteria>
- Bootstrap qualifies CREATE SCHEMA + 6 tables + 4 references + 12 index ON targets + TRUNCATE by the resolved prefix.
- Index names stay bare; `schema_migrations` untouched (absent by design).
- Full suite green under `schema_prefix: parapet` (D-10).
- Public/unprefixed leg parameterized for Phase 52 (no hardcoded prefix).
</success_criteria>

<output>
Create `.planning/phases/51-prefix-core-test-seam/51-03-SUMMARY.md` when done.
</output>
