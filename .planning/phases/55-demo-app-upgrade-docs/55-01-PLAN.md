---
phase: 55-demo-app-upgrade-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/demo_app/priv/repo/migrations/00000000000000_create_parapet_schema.exs
  - examples/demo_app/test/demo_app/operator_smoke_test.exs
  - .github/workflows/ci.yml
autonomous: true
requirements: [SAFE-03]
must_haves:
  truths:
    - "cd examples/demo_app && mix ecto.create && mix ecto.migrate succeeds (parapet schema exists before spine-table migrations run)"
    - "The demo smoke lane asserts all six spine tables exist in the resolved schema via information_schema.tables"
    - "The demo smoke lane asserts Ecto.get_meta(create_incident_record, :prefix) == Parapet.Evidence.schema_prefix() (leg-agnostic, never == \"parapet\" literally)"
    - "The demo CI job runs mix compile --no-optional-deps --warnings-as-errors before the smoke test and it passes clean"
    - "release_gate's needs list [lint, test, demo] is unchanged (no new CI job added)"
  artifacts:
    - "examples/demo_app/priv/repo/migrations/00000000000000_create_parapet_schema.exs (NEW — sentinel schema migration)"
    - "examples/demo_app/test/demo_app/operator_smoke_test.exs (EXTENDED — two new @moduletag :smoke tests)"
    - ".github/workflows/ci.yml (EXTENDED — one new compile step in the existing demo job)"
  key_links:
    - "Sentinel migration version 0 sorts strictly before 20260525000000 so the schema exists before any prefix: \"parapet\" CREATE TABLE runs"
    - "New smoke tests sit OUTSIDE any describe block so @moduletag :smoke applies and mix test --only smoke picks them up"
    - "New CI compile step lives inside the existing demo job (not a new job) so release_gate needs is untouched"
---

<objective>
Prove the schema-prefix lands data on a real Phoenix host (SAFE-03). First add the missing
sentinel migration that creates the `parapet` schema so the demo CI can `mix ecto.migrate` at
all (RESEARCH drift flag). Then extend the existing demo smoke test with a six-table existence
assertion and a `get_meta` round-trip assertion, and add a demo-scoped compile-out-clean step to
the existing CI `demo` job.

Purpose: Close the SAFE-03 requirement — the demo app is the real-host proof that the default
`parapet` prefix migrates and writes data correctly, and that the demo compiles clean under a
host that never sets `schema_prefix`.

Output: New sentinel migration; two new smoke assertions; one new CI compile step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/55-demo-app-upgrade-docs/55-CONTEXT.md
@.planning/phases/55-demo-app-upgrade-docs/55-RESEARCH.md
@.planning/phases/55-demo-app-upgrade-docs/55-VALIDATION.md

# Existing files this plan edits / mirrors:
@examples/demo_app/test/demo_app/operator_smoke_test.exs
@examples/demo_app/config/config.exs
@lib/parapet/evidence.ex
@test/parapet/spine/prefix_propagation_test.exs
@.github/workflows/ci.yml
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New files:
- `examples/demo_app/priv/repo/migrations/00000000000000_create_parapet_schema.exs` — sentinel
  migration defining module `DemoApp.Repo.Migrations.CreateParapetSchema`.

New symbols:
- `DemoApp.Repo.Migrations.CreateParapetSchema` (migration module, `up/0` + `down/0`).
- Two new test functions in `DemoApp.OperatorSmokeTest` (a `get_meta` round-trip test and a
  six-table existence test), both tagged `:smoke` via the module tag.

Extended files (no new symbols):
- `.github/workflows/ci.yml` — one new step in the existing `demo` job.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the sentinel schema migration (Wave 0 prerequisite)</name>
  <files>examples/demo_app/priv/repo/migrations/00000000000000_create_parapet_schema.exs</files>
  <read_first>
    - .planning/phases/55-demo-app-upgrade-docs/55-RESEARCH.md — "Drift / Flags" section and the
      "Sentinel migration (Wave 0, Task 0)" code example (the exact migration body).
    - .planning/phases/55-demo-app-upgrade-docs/55-CONTEXT.md D-12 — why this is infrastructure,
      NOT a prohibited demo spine migration.
    - examples/demo_app/priv/repo/migrations/20260525000000_add_parapet_spine_tables.exs — the
      first spine migration whose `prefix: @prefix` = "parapet" needs the schema to already exist.
  </read_first>
  <action>
    Create the sentinel migration file (version 0 so it sorts strictly before the 20260525000000
    spine migrations). Define module DemoApp.Repo.Migrations.CreateParapetSchema using
    Ecto.Migration. `up/0` executes a CREATE SCHEMA IF NOT EXISTS for the `parapet` schema. `down/0`
    executes a DROP SCHEMA IF EXISTS for `parapet`, non-cascading (fail-closed per 53-CONTEXT D-14 —
    if spine tables still exist, dropping raises rather than silently deleting data; roll back the
    spine table migrations first). This unblocks the demo CI drift flag: without it,
    `mix ecto.migrate` fails with ERROR 3F000 (schema "parapet" does not exist) on the first spine
    migration. Match the body pattern generated by `mix parapet.gen.spine`
    (lib/mix/tasks/parapet.gen.spine.ex:187-200). Per D-12 this is prerequisite schema
    infrastructure, not a new spine-table migration, so it is permitted. Leg note: the demo compiles
    with no `schema_prefix` config, so `@prefix` resolves to "parapet"; the sentinel creates exactly
    that schema. On a `nil`/`public` leg the created-schema name would differ, but the demo CI never
    runs that leg (ci.yml demo job has no PARAPET_SCHEMA_PREFIX env), so the concrete `parapet`
    CREATE SCHEMA is correct for this app. Keep `IF NOT EXISTS` so a re-run / contributor stale DB is
    safe.
  </action>
  <verify>
    <automated>cd examples/demo_app && mix ecto.drop --quiet 2>/dev/null; mix ecto.create && mix ecto.migrate</automated>
  </verify>
  <acceptance_criteria>
    - File `examples/demo_app/priv/repo/migrations/00000000000000_create_parapet_schema.exs` exists.
    - `mix ecto.create && mix ecto.migrate` in examples/demo_app succeeds with no ERROR 3F000.
    - The migration is re-runnable (IF NOT EXISTS) and `down/0` is non-cascading.
  </acceptance_criteria>
  <done>Demo database migrates cleanly; the `parapet` schema is created before any spine table.</done>
</task>

<task type="auto">
  <name>Task 2: Add two SAFE-03 smoke assertions to the existing demo smoke test</name>
  <files>examples/demo_app/test/demo_app/operator_smoke_test.exs</files>
  <read_first>
    - .planning/phases/55-demo-app-upgrade-docs/55-RESEARCH.md — "Code Examples > SAFE-03 smoke test
      additions" (the two verbatim test bodies), Landmine 1 (never assert == "parapet"), Landmine 4
      (information_schema visibility under sandbox), Landmine 6 (ConnCase / DemoApp.Repo, not
      ConcurrencyRepo).
    - .planning/phases/55-demo-app-upgrade-docs/55-CONTEXT.md D-01/D-02/D-03.
    - examples/demo_app/test/demo_app/operator_smoke_test.exs — existing structure; insertion point
      is after the "resolved incident detail" test (~L129), BEFORE the `describe "Phase 48 ..."`
      block, so the two new tests are top-level (module `@moduletag :smoke` applies).
    - test/parapet/spine/prefix_propagation_test.exs:77,138 — house `get_meta` assertion precedent.
  </read_first>
  <action>
    Add two tests to DemoApp.OperatorSmokeTest at top level (NOT inside any describe block, so the
    module-level `@moduletag :smoke` applies and `mix test --only smoke` runs them). Place them
    after the "resolved incident detail" test and before the `describe "Phase 48 ..."` block.

    Test A (round-trip, D-02): call `Parapet.Evidence.create_incident/1` with a small attrs map
    (e.g. title + state "open"), pattern-match `{:ok, incident}`, then assert
    `Ecto.get_meta(incident, :prefix) == Parapet.Evidence.schema_prefix()`. NEVER assert against the
    literal "parapet" — that encodes a default-on-only claim D-17/D-18 forbid and goes red-but-correct
    on the public leg. Include a failure message inspecting both sides. This mirrors
    prefix_propagation_test.exs:77.

    Test B (six-table existence, D-03): bind `prefix = Parapet.Evidence.schema_prefix() || "public"`
    (nil-safe for the unprefixed leg), then run a parameterized query via
    `Ecto.Adapters.SQL.query!(DemoApp.Repo, "SELECT table_name FROM information_schema.tables WHERE
    table_schema = $1", [prefix])`. Use the parameterized `$1` binding (no string interpolation —
    defense-in-depth against SQL injection, threat T-55-01). Build a MapSet of returned table names,
    diff against the expected six spine tables (parapet_action_items, parapet_incidents,
    parapet_timeline_entries, parapet_tool_audits, parapet_system_events, parapet_action_claims), and
    assert the missing set is empty with a message listing any missing tables. Query through
    DemoApp.Repo (the sandboxed connection from DemoAppWeb.ConnCase); information_schema is
    catalog-level so migrated tables are visible regardless of sandbox rollback. Do NOT use
    Parapet.TestSupport.ConcurrencyRepo (library-only). Do NOT add or change demo config, and do NOT
    add a new test file (D-01/D-12).
  </action>
  <verify>
    <automated>cd examples/demo_app && mix test --only smoke</automated>
  </verify>
  <acceptance_criteria>
    - Both new tests are top-level in DemoApp.OperatorSmokeTest and run under `--only smoke`.
    - The round-trip assertion compares against `Parapet.Evidence.schema_prefix()`, not a literal.
    - The six-table query uses parameterized `$1` binding and `schema_prefix() || "public"`.
    - `mix test --only smoke` passes green in examples/demo_app.
    - `grep -n '"parapet"' examples/demo_app/test/demo_app/operator_smoke_test.exs` shows no literal
      "parapet" used as an assertion target for the prefix (route/URL literals are unrelated).
  </acceptance_criteria>
  <done>Demo smoke lane proves six tables land in the resolved schema and the write round-trip carries the compiled prefix.</done>
</task>

<task type="auto">
  <name>Task 3: Add demo compile-out-clean step to the existing CI demo job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/55-demo-app-upgrade-docs/55-RESEARCH.md — "ci.yml demo job — Current step
      sequence" (exact steps L141-167) and D-04 verification (placement rules), Pitfall 4.
    - .planning/phases/55-demo-app-upgrade-docs/55-CONTEXT.md D-04.
    - .github/workflows/ci.yml — the existing `demo` job and the `release_gate` job's `needs` list.
  </read_first>
  <action>
    Add ONE new step to the EXISTING `demo` job in ci.yml (do NOT create a new job): name it
    "Compile demo (warnings-as-errors, no-optional-deps)" running
    `cd examples/demo_app && mix compile --no-optional-deps --warnings-as-errors`. Place it AFTER the
    demo `mix deps.get` step and BEFORE the `mix test --only smoke` step (placing before
    `mix ecto.create && mix ecto.migrate` is fine — compile does not need the DB), so it reuses the
    warm cached `_build`. Per D-04 this proves the real-host compile-out-clean claim the library
    `lint` job cannot (the demo is a separate mix project with its own Bandit/esbuild/tailwind/Phoenix
    deps and config, exercising a host that never sets `schema_prefix`). Do NOT touch the
    `release_gate` job's `needs: [lint, test, demo]` list — it stays unchanged because no new job is
    added. Do NOT add PARAPET_SCHEMA_PREFIX to the demo job env (confirmed non-leak in RESEARCH
    Landmine 2 — the demo must inherit the default "parapet").
  </action>
  <verify>
    <automated>cd examples/demo_app && mix compile --no-optional-deps --warnings-as-errors && grep -q 'no-optional-deps --warnings-as-errors' ../../.github/workflows/ci.yml && grep -q 'needs: \[lint, test, demo\]' ../../.github/workflows/ci.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml `demo` job has a new compile step with `mix compile --no-optional-deps --warnings-as-errors`, placed before the smoke-test step.
    - No new CI job was added; `release_gate` `needs: [lint, test, demo]` is unchanged.
    - `cd examples/demo_app && mix compile --no-optional-deps --warnings-as-errors` exits clean locally.
  </acceptance_criteria>
  <done>The demo job proves compile-out-clean on a real host before running the smoke lane; release_gate wiring is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → Postgres catalog | The six-table smoke assertion sends a schema name into an `information_schema` query. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-01 | Tampering (SQL injection) | information_schema six-table query in operator_smoke_test.exs | low | mitigate | Parameterized `$1` binding via `Ecto.Adapters.SQL.query!/3`; the value is a compile-frozen `schema_prefix()`, never string-interpolated. Defense-in-depth even though the source is trusted. |
| T-55-02 | Repudiation (doc/behavior drift) | demo prefix assertions vs library behavior | low | accept | The demo assertions target `Parapet.Evidence.schema_prefix()` (single source), so the demo cannot drift from the library's compiled prefix; no new mechanism introduced. |

No package-manager installs in this plan (no npm/pip/cargo/hex adds) — package legitimacy gate N/A.
</threat_model>

<verification>
- `cd examples/demo_app && mix ecto.drop --quiet; mix ecto.create && mix ecto.migrate` succeeds (sentinel unblocks migrate).
- `cd examples/demo_app && mix test --only smoke` passes (both new assertions green).
- `cd examples/demo_app && mix compile --no-optional-deps --warnings-as-errors` exits clean.
- `grep 'needs: \[lint, test, demo\]' .github/workflows/ci.yml` still matches (no job restructure).
- No literal `"parapet"` used as a prefix assertion target in operator_smoke_test.exs.
</verification>

<success_criteria>
SAFE-03 satisfied: the demo smoke lane asserts all six tables land in the resolved schema and the
evidence write round-trip carries `Ecto.get_meta(record, :prefix) == schema_prefix()`; the demo job
proves compile-out-clean; the sentinel migration lets `mix ecto.migrate` succeed; `release_gate`
`needs` is unchanged.
</success_criteria>

<output>
Create `.planning/phases/55-demo-app-upgrade-docs/55-01-SUMMARY.md` when done.
</output>
