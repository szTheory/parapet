---
phase: 55-demo-app-upgrade-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/upgrade-1.x.md
  - mix.exs
  - docs/migration-v1.md
  - docs/deployment.md
  - README.md
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "docs/upgrade-1.x.md exists and is the sole home for Track A/B mechanics (D-05)"
    - "upgrade-1.x.md leads with the TL;DR reassurance, then the honest 'action required' caveat, then Track A, Track B, GRANTs, recompile-order, rollback incl. half-migrated recovery, then FAQ (D-07 order)"
    - "Every config block in upgrade-1.x.md is immediately followed by its `mix deps.compile parapet --force` recompile line (DOC-01)"
    - "upgrade-1.x.md is registered in BOTH mix.exs extras: AND the Guides groups_for_extras group (D-08)"
    - "migration-v1.md has a new Step 3 'Choose your schema location (v1.7+)' and the old Steps 3-6 are renumbered 4-7 (D-09)"
    - "deployment.md has a net-new schema subsection near the durable-evidence migration content, routing to upgrade-1.x.md (D-10)"
    - "README Installation section carries a one-line schema note routing to migration-v1.md Step 3 (D-11)"
    - "mix docs --warnings-as-errors passes (registration + all cross-links resolve)"
  artifacts:
    - "docs/upgrade-1.x.md (NEW — DOC-01 single-source upgrade guide)"
    - "mix.exs (EXTENDED — extras + groups_for_extras registration)"
    - "docs/migration-v1.md (EXTENDED — new Step 3, renumber 3-6 -> 4-7)"
    - "docs/deployment.md (EXTENDED — schema subsection under Step 4)"
    - "README.md (EXTENDED — Installation schema note)"
  key_links:
    - "upgrade-1.x.md is the single source; migration-v1.md, deployment.md, README all ROUTE to it and never restate mechanics (D-05)"
    - "Both mix.exs extras: and groups_for_extras Guides: are edited in one task, or mix docs --warnings-as-errors breaks the DOC-02 cross-links (Landmine 3)"
    - "Track A/B copy is lifted VERBATIM from 54-CONTEXT D-04/D-06/D-17/D-18 so doc, doctor output, and generated migration tell one identical story (D-06)"
---

<objective>
Close the audited #1 documentation gap (no adopter-facing DB-schema upgrade story). Create
`docs/upgrade-1.x.md` as the single source of truth for the Track A / Track B schema-prefix upgrade
mechanics (DOC-01), register it on HexDocs, and add reassure-then-route pointers from
migration-v1.md, deployment.md, and README (DOC-02) — none of which restate mechanics.

Purpose: Adopters get one copy-paste upgrade story with an honest "action required" framing, and the
three secondary surfaces route to it without drift.

Output: One new doc; mix.exs registration; three routing edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/55-demo-app-upgrade-docs/55-CONTEXT.md
@.planning/phases/55-demo-app-upgrade-docs/55-RESEARCH.md
@.planning/phases/55-demo-app-upgrade-docs/55-VALIDATION.md
@.planning/phases/54-upgrade-path-doctor/54-CONTEXT.md

# Existing files this plan edits / mirrors:
@mix.exs
@docs/migration-v1.md
@docs/deployment.md
@README.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New files:
- `docs/upgrade-1.x.md` — the DOC-01 single-source Track A/B upgrade guide.

Extended files (no new symbols):
- `mix.exs` — `docs/upgrade-1.x.md` added to `extras:` and to `groups_for_extras` `Guides:`.
- `docs/migration-v1.md` — new Step 3 inserted; old Steps 3-6 renumbered to 4-7.
- `docs/deployment.md` — net-new schema subsection under Step 4.
- `README.md` — one-line schema note in the Installation section.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/upgrade-1.x.md and register it in mix.exs (both lists)</name>
  <files>docs/upgrade-1.x.md, mix.exs</files>
  <read_first>
    - .planning/phases/55-demo-app-upgrade-docs/55-RESEARCH.md — "Verbatim Phase-54 Copy for
      upgrade-1.x.md" (Track A, Track B, GRANTs, doctor drift microcopy, honest framing — copy these
      strings verbatim), Landmine 3 (both mix.exs lists), Landmine 7 (stale-DB contributor note is
      FAQ-only, do not alarm adopters), and the mix.exs extras/groups_for_extras current content.
    - .planning/phases/55-demo-app-upgrade-docs/55-CONTEXT.md D-05/D-06/D-07/D-08.
    - .planning/phases/54-upgrade-path-doctor/54-CONTEXT.md — D-04 (doctor drift microcopy),
      D-06/D-08 (gen.schema.move body), D-17 (Track A = nil + `--force`), D-18 (honest "action
      required" framing). Mirror these VERBATIM.
    - mix.exs (extras: ~L62-86, groups_for_extras Guides: ~L88-108).
  </read_first>
  <action>
    Create docs/upgrade-1.x.md as the SOLE home for Track A/B mechanics (D-05). Section order per
    D-07: (1) TL;DR reassurance — "your data does not move unless you choose"; (2) the honest
    "action required for existing adopters" caveat (D-18 — NOT "no action required"; a do-nothing
    upgrader who wants the new default hits `relation "parapet.parapet_incidents" does not exist`);
    (3) Track A (stay on `public`): `config :parapet, schema_prefix: nil` immediately followed by
    `mix deps.compile parapet --force`, then `mix parapet.doctor`; (4) Track B (move to `parapet`):
    `mix parapet.gen.schema.move` then `mix ecto.migrate`, describing the generated reversible
    migration (SET LOCAL lock_timeout 5s in after_begin, CREATE SCHEMA IF NOT EXISTS, six explicit
    ALTER TABLE public.<t> SET SCHEMA parapet lines, down reverses in LIFO, down never DROP SCHEMA);
    (5) least-privilege GRANTs for --no-create-schema / DBA-managed schema (CREATE SCHEMA ...
    AUTHORIZATION, split-role GRANT USAGE/CREATE); (6) recompile-order guidance; (7) rollback
    including half-migrated recovery; (8) FAQ. Lift every config/SQL block VERBATIM from the RESEARCH
    "Verbatim Phase-54 Copy" section and 54-CONTEXT D-04/D-06/D-17/D-18 so the doc, doctor output, and
    generated migration tell one identical story (D-06). Ensure EVERY config block is immediately
    followed by its `mix deps.compile parapet --force` recompile line (DOC-01 requirement). Define the
    canonical D-18 "action required" sentence HERE (other surfaces quote/route to it). Put the
    contributor stale-DB `mix demo.reset` note in the FAQ / contributor section only (Landmine 7) —
    do not alarm adopters. Use only relative links that resolve on HexDocs.

    In mix.exs, add `"docs/upgrade-1.x.md"` in BOTH places in a SINGLE edit pass (Landmine 3): after
    `"docs/migration-v1.md"` in the `extras:` list AND after `"docs/migration-v1.md"` in the
    `groups_for_extras` `Guides:` sublist. Omitting either list breaks the DOC-02 cross-links under
    `mix docs --warnings-as-errors` (ci.yml:48).
  </action>
  <verify>
    <automated>test -f docs/upgrade-1.x.md && grep -c 'upgrade-1.x.md' mix.exs | grep -qx 2 && mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - docs/upgrade-1.x.md exists with the D-07 section order (TL;DR -> action-required -> Track A -> Track B -> GRANTs -> recompile-order -> rollback incl. half-migrated -> FAQ).
    - Every config block is immediately followed by `mix deps.compile parapet --force`.
    - `grep -c 'upgrade-1.x.md' mix.exs` equals 2 (extras + groups_for_extras).
    - `mix docs --warnings-as-errors` exits clean.
  </acceptance_criteria>
  <done>The single-source upgrade guide exists, is HexDocs-registered in both lists, and docs build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Insert migration-v1.md Step 3 and renumber Steps 3-6 -> 4-7</name>
  <files>docs/migration-v1.md</files>
  <read_first>
    - .planning/phases/55-demo-app-upgrade-docs/55-RESEARCH.md — "migration-v1.md — Current step
      list" and the "migration-v1.md Step 3 insert" sketch (reassure -> instruct -> route), D-09
      verification (renumber map 3->4, 4->5, 5->6, 6->7), Pitfall 5 (do not restate mechanics).
    - .planning/phases/55-demo-app-upgrade-docs/55-CONTEXT.md D-09 and the <specifics> Step 3 wording
      sketch.
    - docs/migration-v1.md — current six steps (verify the exact current numbering before editing).
  </read_first>
  <action>
    Insert a new dedicated early step "Step 3: Choose your schema location (v1.7+)" into
    docs/migration-v1.md BEFORE the current Step 3, and renumber the existing Steps 3-6 -> 4-7 (old
    Step 3 "Move custom SLOs" -> Step 4; old Step 4 "Re-check generated host surfaces" -> Step 5; old
    Step 5 "Review recovery and operator surfaces" -> Step 6; old Step 6 "Run the safe-upgrade
    checklist" -> Step 7). The new Step 3 reassures-then-instructs INLINE (data never moves
    automatically -> action required or the first spine query fails with
    `relation "parapet.parapet_incidents" does not exist`) and carries ONLY the decision + the single
    Track-A config line (`config :parapet, schema_prefix: nil`) + its `mix deps.compile parapet
    --force` recompile line + a route to [Upgrade Guide (1.x)](upgrade-1.x.md) for Track B and full
    mechanics. Do NOT bury a one-line pointer in the checklist step (rejected C1 — the do-nothing
    upgrader skims past it). Do NOT restate Track A/B mechanics beyond the single Track-A line —
    everything else routes to upgrade-1.x.md (single-source, D-05/D-09). The old Step 6's existing
    `mix parapet.doctor --ci` line moves to Step 7 UNCHANGED (D-09). Verify any intra-doc references
    that name step numbers are updated consistently after the renumber.
  </action>
  <verify>
    <automated>grep -q 'Step 3: Choose your schema location' docs/migration-v1.md && grep -q 'Step 7' docs/migration-v1.md && grep -q 'upgrade-1.x.md' docs/migration-v1.md && mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - New "Step 3: Choose your schema location (v1.7+)" exists as a top-level step (not a checklist bullet).
    - Steps are numbered 1-7 with the old 3-6 content renumbered to 4-7; `mix parapet.doctor --ci` now in Step 7.
    - Step 3 routes to upgrade-1.x.md and restates no Track B mechanics.
    - `mix docs --warnings-as-errors` exits clean.
  </acceptance_criteria>
  <done>The audited #1 gap is closed at the point the do-nothing upgrader will see it — a first-class step, not a buried bullet.</done>
</task>

<task type="auto">
  <name>Task 3: Add deployment.md schema subsection and README Installation note</name>
  <files>docs/deployment.md, README.md</files>
  <read_first>
    - .planning/phases/55-demo-app-upgrade-docs/55-RESEARCH.md — D-10 verification (deployment.md
      Step 4 = durable-evidence migrations, subsection goes here) and D-11 verification (README
      Installation L43-79, note after the `mix parapet.install` block ~L62), Pitfall 5.
    - .planning/phases/55-demo-app-upgrade-docs/55-CONTEXT.md D-10/D-11.
    - docs/deployment.md (Step 4 durable-evidence migration content) and README.md (Installation
      section) — read current content before editing.
  </read_first>
  <action>
    deployment.md (D-10): add a NET-NEW schema subsection near the durable-evidence migration content
    (Step 4), stating that `mix ecto.migrate` creates/uses the default `parapet` schema and that
    existing adopters choose their migration path in [Upgrade Guide (1.x)](upgrade-1.x.md). Do NOT
    fork the Track A/B prose here — reassure-then-route only (single-source, D-05).

    README.md (D-11): add a short one-line note in the Installation section, after the
    `mix parapet.install` block (~L62): new installs default to the `parapet` schema; existing
    adopters follow migration-v1.md Step 3 (route to migration-v1.md, which routes to upgrade-1.x.md).
    Reassure-then-route; never restate mechanics. Use a relative link that resolves under
    `mix docs --warnings-as-errors`.
  </action>
  <verify>
    <automated>grep -qi 'schema' docs/deployment.md && grep -q 'upgrade-1.x.md' docs/deployment.md && grep -qi 'parapet.*schema' README.md && mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - deployment.md has a net-new schema subsection near Step 4 routing to upgrade-1.x.md, with no Track A/B mechanics restated.
    - README Installation carries a one-line schema note routing to migration-v1.md Step 3.
    - `mix docs --warnings-as-errors` exits clean (all cross-links resolve).
  </acceptance_criteria>
  <done>The two secondary surfaces route adopters to the single-source upgrade guide with no drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter docs → operator actions | Upgrade docs instruct adopters to run schema-changing migrations; wrong/drifting copy could cause data-location errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-02 | Repudiation (doc/behavior drift) | upgrade-1.x.md vs doctor output vs generated migration | medium | mitigate | Single-source rule (D-05): upgrade-1.x.md is the only place mechanics live; Track A/B copy lifted verbatim from 54-CONTEXT (D-06); other surfaces route, never restate; `mix docs --warnings-as-errors` gates cross-link integrity. |
| T-55-03 | Information disclosure (over-broad GRANTs) | GRANT examples in upgrade-1.x.md | low | mitigate | Document least-privilege GRANTs verbatim from 54-CONTEXT D-12b / schema_move_notice.ex (GRANT USAGE/CREATE on the schema only), not blanket superuser grants. |

No package-manager installs in this plan (no npm/pip/cargo/hex adds) — package legitimacy gate N/A.
</threat_model>

<verification>
- `test -f docs/upgrade-1.x.md` and `grep -c 'upgrade-1.x.md' mix.exs` == 2.
- `mix docs --warnings-as-errors` exits clean (registration + all cross-links resolve).
- migration-v1.md has Step 3 (new) through Step 7; deployment.md has a schema subsection; README has the note.
- No Track A/B mechanics restated outside upgrade-1.x.md (single-source; manual UAT confirms verbatim copy + tone).
</verification>

<success_criteria>
DOC-01 satisfied: upgrade-1.x.md exists with the D-07 order, verbatim Track A/B copy, per-block
`--force` recompile lines, GRANTs, recompile-order, rollback incl. half-migrated recovery, and FAQ,
and is HexDocs-registered. DOC-02 satisfied: migration-v1.md Step 3, deployment.md subsection, and
README note all route to it; `mix docs --warnings-as-errors` is clean.
</success_criteria>

<output>
Create `.planning/phases/55-demo-app-upgrade-docs/55-02-SUMMARY.md` when done.
</output>
