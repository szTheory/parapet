---
phase: 54-upgrade-path-doctor
plan: 03
type: execute
wave: 2
depends_on: ["54-02"]
files_modified:
  - test/parapet/repo/migrations/move_spine_to_parapet_schema_test.exs
autonomous: true
requirements: [UPG-04, UPG-03]
must_haves:
  truths:
    - "a Track B round-trip against a dedicated throwaway DB proves: fixture spine created in public → up → six tables resolve under parapet → down → all six back in public (UPG-04, D-13/14/15/16)"
    - "after up: FK cascade BEHAVIOR holds (insert parent+child, delete parent, child cascaded) and the partial index survives byte-identically with its predicate still rendering under parapet (D-16b/c)"
    - "after down: all tables back in public AND the parapet schema still exists but is empty of spine tables (proves the deliberate no-DROP-SCHEMA, D-16d)"
    - "the abort leg: dropping a fixture table before up makes the migration RAISE and nothing moves (UPG-03 authoritative gate, D-10)"
  artifacts:
    - "test/parapet/repo/migrations/move_spine_to_parapet_schema_test.exs — :unboxed round-trip cloning add_lease_until_backfill_test.exs against throwaway DB parapet_schema_move_roundtrip_test"
  key_links:
    - "Code.require_file loads the SAME committed fixture Plan 02's golden test snapshots (D-13, Pitfall 5)"
    - "dedicated throwaway DB via Ecto.Adapters.Postgres.storage_up/1 + storage_down/1 — NOT the shared parapet_concurrency_test (D-14, Pitfall 4)"
    - "fixture spine hand-written in bare public DDL (NOT ConcurrencyBootstrap.q/1) → byte-identical on BOTH CI legs (D-15, Pitfall 1)"
---

<objective>
Prove the Track B move mechanics end-to-end with a DB round-trip test: stand up an isolated fixture spine in `public` on a dedicated throwaway DB, run the committed move migration up (via `Ecto.Migrator`), assert the six tables now resolve under `parapet` with FK cascade behavior and partial indexes intact, run it down, and assert everything is restored to `public` with the `parapet` schema deliberately NOT dropped. Also exercise the migrate-time abort leg (UPG-03).

Purpose: Satisfy UPG-04 (round-trip proof) and provide the DB-level authoritative-gate proof for UPG-03 (D-10 abort). This is the mechanics test; Plan 02's golden test is the output test — both reference the SAME committed fixture so they cannot drift.
Output: One `:unboxed` round-trip test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-upgrade-path-doctor/54-CONTEXT.md
@.planning/phases/54-upgrade-path-doctor/54-RESEARCH.md
@.planning/phases/54-upgrade-path-doctor/54-VALIDATION.md
@.planning/phases/54-upgrade-path-doctor/54-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW files introduced by THIS plan (drift verifier should exclude as newly-created):
- `test/parapet/repo/migrations/move_spine_to_parapet_schema_test.exs` (its private `MigrationTestRepo` module + fixture-DDL helper)
Consumes (does NOT create): the committed fixture `priv/repo/migrations/<ts>_move_parapet_spine_to_schema.exs` from Plan 02.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Round-trip up/down DB test (throwaway DB, isolated public fixture)</name>
  <files>test/parapet/repo/migrations/move_spine_to_parapet_schema_test.exs</files>
  <read_first>
    - test/parapet/repo/migrations/add_lease_until_backfill_test.exs (the FULL clone template: :14 use ExUnit.Case async: false; :44-49 Code.require_file; :55-60 private MigrationTestRepo on Postgres adapter; :62-165 setup_all with db_config, DBConnection.ConnectionPool override, bare Postgrex conn, schema_migrations create, on_exit teardown; :167-262 the @tag :unboxed test with Ecto.Migrator.up + Postgrex assertions)
    - test/support/concurrency_repo.ex:8-22 (database_config/0 — the base config to Keyword.put the throwaway :database onto)
    - .planning/phases/54-upgrade-path-doctor/54-RESEARCH.md § Pattern 5 (the setup_all deltas: storage_up/storage_down, dedicated DB name, create_public_fixture_spine!), § Common Pitfalls Pitfall 1 (fixture must be bare public DDL) + Pitfall 4 (dedicated DB, not shared), § Code Examples "six canonical spine tables + FK/partial-index shapes" (the minimal 2-table fixture: parapet_incidents parent + parapet_action_claims child, ON DELETE CASCADE FK, WHERE status='claimed' partial index)
    - deps/ecto_sql/lib/ecto/adapters/postgres.ex (storage_up/1 :208, storage_down/1 :247 — the throwaway-DB lifecycle)
    - .planning/phases/54-upgrade-path-doctor/54-02-SUMMARY.md (the exact committed-fixture path + module name to Code.require_file)
  </read_first>
  <action>
    Create `test/parapet/repo/migrations/move_spine_to_parapet_schema_test.exs`, `use ExUnit.Case, async: false`, cloning `add_lease_until_backfill_test.exs`. Key deltas (D-13/14/15/16):

    - `Code.require_file` the committed fixture from Plan 02 (path + module name per 54-02-SUMMARY) so `Ecto.Migrator.up/4` + `down/4` can drive it. Do NOT generate the migration live in this test (D-13).
    - Private `MigrationTestRepo` on `Ecto.Adapters.Postgres` (same as the clone), started with `DBConnection.ConnectionPool` + `pool_size: 5` (Migrator needs ≥2 connections; sandbox can't provide them).
    - `setup_all`: build the config from `ConcurrencyRepo.database_config()` |> `Keyword.put(:database, "parapet_schema_move_roundtrip_test")` |> `Keyword.put(:pool, DBConnection.ConnectionPool)` |> `Keyword.put(:pool_size, 5)` |> `Keyword.delete(:ownership_timeout)`. Call `Ecto.Adapters.Postgres.storage_up(cfg)` to CREATE the throwaway DB (D-14 — NOT the shared parapet_concurrency_test, Pitfall 4). Start `MigrationTestRepo`, create `schema_migrations` in it. Start a bare `Postgrex` conn for out-of-transaction DDL/assertions. Call a private `create_public_fixture_spine!/1` that hand-writes BARE `public` DDL (Pitfall 1 — do NOT route through `ConcurrencyBootstrap.q/1`): `parapet_incidents` (parent, binary_id PK) + `parapet_action_claims` (child, binary_id PK, `incident_id` FK → parapet_incidents `ON DELETE CASCADE`, plus the partial index `parapet_action_claims_lease_until_claimed_index ON (lease_until) WHERE status='claimed'`). `on_exit`: `GenServer.stop(conn)` then `Ecto.Adapters.Postgres.storage_down(cfg)` to NUKE the throwaway DB (single-call idempotent teardown, D-14).

    Because the fixture is always built in `public` and the migration uses literal schema names, the test runs BYTE-IDENTICALLY on both CI legs (D-15) — do not branch on `@prefix`.

    - `@tag :unboxed` test 1 — round-trip up/down:
      * `Ecto.Migrator.up(MigrationTestRepo, <version>, <FixtureModule>, log: false)`; assert result in `[:ok, :already_up]`.
      * (D-16a) Membership: query `pg_class` JOIN `pg_namespace` and assert both fixture tables now live in namespace `parapet` (not `public`).
      * (D-16b) FK cascade BEHAVIOR: insert a parent incident + a child claim (via the bare Postgrex conn, qualifying to `parapet`), delete the parent, assert the child row is gone (cascade fired) — stronger than a pure-catalog FK check.
      * (D-16c) Partial index: assert the index `parapet_action_claims_lease_until_claimed_index` exists under `parapet` with its byte-identical name and that `pg_get_expr(indpred, indrelid)` still renders the `status = 'claimed'` predicate.
      * Then `Ecto.Migrator.down(MigrationTestRepo, <version>, <FixtureModule>, log: false)`.
      * (D-16d) Membership after down: both tables back in `public`; AND the `parapet` schema STILL EXISTS (query pg_namespace) but is EMPTY of the spine tables — proves the deliberate no-DROP-SCHEMA (D-08).

    - `@tag :unboxed` test 2 — abort leg (UPG-03, D-10): drop one fixture spine table from `public` before running up (or run against a DB missing a table), invoke `Ecto.Migrator.up`, and assert it RAISES (the `DO $$ RAISE EXCEPTION $$` guard fires) and that NOTHING moved (the surviving tables are still in `public`, nothing landed in `parapet`). Ensure this test restores/re-creates the dropped fixture table in its own teardown so it does not poison test 1 (or keep them order-independent by re-building the fixture per test). Because the throwaway DB is dedicated (D-14), a mid-test abort cannot poison other `:unboxed` modules.

    Reference D-10, D-13, D-14, D-15, D-16 in doc comments so the checker can trace each assertion to its decision.
  </action>
  <verify>
    <automated>mix test test/parapet/repo/migrations/move_spine_to_parapet_schema_test.exs</automated>
  </verify>
  <done>The round-trip test passes on both CI legs: up moves both fixture tables to `parapet` with FK cascade behavior + partial index intact; down restores them to `public` with `parapet` schema surviving-but-empty; the abort leg raises on a missing table and moves nothing; the throwaway DB is created and dropped via storage_up/storage_down; the fixture is bare-public DDL (no ConcurrencyBootstrap.q).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → throwaway Postgres DB | `storage_up`/`storage_down` create and destroy a real database on the same PG server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-08 | Denial of Service | round-trip test poisoning shared state | medium | mitigate | Dedicated throwaway DB `parapet_schema_move_roundtrip_test` via storage_up/storage_down — a mid-test abort cannot leave the spine half-moved in the shared `parapet_concurrency_test` (D-14, Pitfall 4). |
| T-54-09 | Tampering | fixture DDL routed through the prefix-aware qualifier | low | mitigate | Fixture is hand-written BARE `public` DDL, not `ConcurrencyBootstrap.q/1` — keeps the test byte-identical across both CI legs and prevents the `public.<t>` abort guard from mis-firing on the parapet leg (D-15, Pitfall 1). |
</threat_model>

<verification>
- `mix test test/parapet/repo/migrations/move_spine_to_parapet_schema_test.exs` green under BOTH CI legs (`PARAPET_SCHEMA_PREFIX=parapet` and `PARAPET_SCHEMA_PREFIX=''`) — the test is engineered to be leg-agnostic (D-15).
- The `:unboxed` tag runs in the normal `mix test` lane (no new CI wiring; `:unboxed` is only a sandbox marker, excluded nowhere).
- Requires a live PostgreSQL test service (already the project's test-DB requirement; CI uses postgres:16-alpine).
</verification>

<success_criteria>
UPG-04 satisfied: the round-trip proves created-in-public → up → resolves-under-parapet (FK cascade + partial indexes intact) → down → restored-to-public (schema empty, not dropped). UPG-03's authoritative migrate-time abort gate is proven at the DB layer. Both directions execute the SAME committed fixture Plan 02's golden test snapshots.
</success_criteria>

<output>
Create `.planning/phases/54-upgrade-path-doctor/54-03-SUMMARY.md` when done.
</output>
