---
phase: 54-upgrade-path-doctor
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - test/parapet/spine/prefix_propagation_test.exs
  - test/parapet/upgrade_never_forces_move_test.exs
autonomous: true
requirements: [UPG-01, UPG-05]
must_haves:
  truths:
    - "on the nil CI leg, a test proves schema_prefix: nil emits BARE unqualified SQL (no schema qualifier) — the load-bearing 'proving unprefixed' artifact UPG-01 demands (D-19)"
    - "a live write-path round-trip proves Ecto.get_meta(record, :prefix) == nil + green read-back (covers Multi/insert_all paths to_sql cannot reach) (D-19)"
    - "a regex fitness function statically proves the installer/generators NEVER reference the move task — the installer path can never auto-chain a data migration (D-20, UPG-05)"
    - "Parapet.Spine.Schema.resolve_prefix(nil, nil) == {:ok, \"parapet\"} pins 'default = parapet for new installs' (D-20)"
  artifacts:
    - "test/parapet/spine/prefix_propagation_test.exs — new describe \"UPG-01 Track A\" guarded by if is_nil(@prefix)"
    - "test/parapet/upgrade_never_forces_move_test.exs — regex fitness fn + resolve_prefix(nil,nil) default pin"
  key_links:
    - "the Track A describe block is guarded by if is_nil(@prefix) so it is a no-op on the parapet leg and load-bearing proof on the nil leg (D-19)"
    - "the fitness fn clones the schema_prefix_guard_test.exs glob→read→line-scan→assert offenders==[] shape (D-20)"
    - "no new repo-booting integration test — the concurrency harness + dual-prefix matrix already exist (D-19)"
---

<objective>
Pin the two honest-truth requirements of the upgrade story. UPG-01 (Track A): prove that `schema_prefix: nil` emits bare unqualified SQL with green queries — a legible artifact, because the green suite alone is not proof (D-19). UPG-05: prove the installer/generators can NEVER auto-chain a schema-move data migration (a regex fitness function), and pin the "default = parapet for new installs" literal via `resolve_prefix(nil, nil) == {:ok, "parapet"}` (D-20).

Purpose: Satisfy UPG-01 + UPG-05. These are the guardrails behind D-17's honest truth: the default flips for new installs only (who benefits), there is NO runtime install-detection, and no upgrade path ever auto-runs a data migration.
Output: An extended `prefix_propagation_test.exs` + a new fitness-function test file. Both DB-less-cheap (the fitness fn is source-scanning; the Track A block rides the existing nil CI leg).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-upgrade-path-doctor/54-CONTEXT.md
@.planning/phases/54-upgrade-path-doctor/54-RESEARCH.md
@.planning/phases/54-upgrade-path-doctor/54-VALIDATION.md
</context>

<artifacts_this_phase_produces>
NEW symbols/files introduced by THIS plan (drift verifier should exclude as newly-created):
- `describe "UPG-01 Track A"` block in `test/parapet/spine/prefix_propagation_test.exs`
- `test/parapet/upgrade_never_forces_move_test.exs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the UPG-01 Track A pin to prefix_propagation_test.exs</name>
  <files>test/parapet/spine/prefix_propagation_test.exs</files>
  <read_first>
    - test/parapet/spine/prefix_propagation_test.exs:1-14 (module setup: use ConcurrencyCase, aliases incl. Evidence + Incident + ConcurrencyRepo, @prefix = Schema.__prefix__()), :21-60 (the existing to_sql describe with the if @prefix / else refute-bare pattern to mirror), :67-103 (the get_meta write-path pattern with Evidence.create_incident + Ecto.get_meta(rec, :prefix))
    - .planning/phases/54-upgrade-path-doctor/54-RESEARCH.md § Code Examples "Track A pin (D-19)" (the describe block skeleton with both to_sql + get_meta assertions)
    - lib/parapet/evidence.ex:75 (create_incident/1 signature for the live write-path round-trip)
  </read_first>
  <action>
    Add a new `describe "UPG-01 Track A: schema_prefix nil emits unprefixed SQL"` block to `prefix_propagation_test.exs`, guarded so its assertions only fire on the nil leg. Use `if is_nil(@prefix)` (the module already binds `@prefix = Parapet.Spine.Schema.__prefix__()` at :14) — mirror the existing else-branch pattern at :39-42 so the block is a no-op on the `parapet` leg and the load-bearing proof on the nil leg (D-19). Two tests:

    1. to_sql bare-name proof: build a query `from(i in Incident, select: i.id)`, `{sql, _} = Ecto.Adapters.SQL.to_sql(:all, ConcurrencyRepo, query)`, then `refute sql =~ ~s("parapet".)` (no schema qualifier) AND `assert sql =~ "parapet_incidents"` (the bare, unqualified table name is present). This is the legible "proving unprefixed" artifact UPG-01's wording demands.

    2. write-path round-trip: `{:ok, incident} = Evidence.create_incident(%{title: "track-a", state: "open"})`; `assert Ecto.get_meta(incident, :prefix) == nil`; `assert ConcurrencyRepo.get(Incident, incident.id)` (green read-back). This covers the Multi/insert_all paths `to_sql` structurally cannot reach (D-19).

    Do NOT build a new repo-booting integration test — the ConcurrencyCase harness + dual-prefix matrix already exist (D-19). Keep `async: false` consistent with the module. Reference D-19 in the describe doc/comment.
  </action>
  <verify>
    <automated>PARAPET_SCHEMA_PREFIX='' mix test test/parapet/spine/prefix_propagation_test.exs</automated>
  </verify>
  <done>On the nil leg, the UPG-01 Track A block runs and passes: to_sql carries `parapet_incidents` bare with no `"parapet".` qualifier; the live write round-trip gives `get_meta(:prefix) == nil` + green read-back. On the parapet leg the block is a no-op (guarded by `if is_nil(@prefix)`), and the existing propagation tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: UPG-05 regex fitness function + default-prefix pin</name>
  <files>test/parapet/upgrade_never_forces_move_test.exs</files>
  <read_first>
    - test/parapet/schema_prefix_guard_test.exs:1-63 (the fitness-function template: @lib_files glob → for file/read/line-scan → assert offenders == [] with a teaching failure message)
    - lib/parapet/spine/schema.ex:150-179 (resolve_prefix/2 — resolve_prefix(nil, nil) returns {:ok, normalize("parapet")} == {:ok, "parapet"})
    - .planning/phases/54-upgrade-path-doctor/54-RESEARCH.md § Pattern 6 (fitness fn for the move task — target files + forbidden pattern)
  </read_first>
  <action>
    Create `test/parapet/upgrade_never_forces_move_test.exs`, `use ExUnit.Case, async: true`, cloning the `schema_prefix_guard_test.exs` shape. Two tests:

    1. Fitness function (D-20): glob the installer/generator source files `lib/mix/tasks/parapet.install.ex`, `lib/mix/tasks/parapet.gen.spine.ex`, `lib/mix/tasks/parapet.gen.archive_indexes.ex` (use explicit paths, not a wildcard that would sweep in the move task itself). Line-scan each for the forbidden pattern `~r/parapet\.(gen\.)?schema\.move/` (and/or the move module name `Mix.Tasks.Parapet.Gen.Schema.Move`). Assert `offenders == []` with a teaching failure message explaining that the installer path must NEVER auto-chain a schema-move data migration (D-17/D-20) — mirror the PROP-02 guard's `format_guard_failure` style. This goes red the day someone wires the move into the installer.

    2. Default-prefix pin (D-20): `assert Parapet.Spine.Schema.resolve_prefix(nil, nil) == {:ok, "parapet"}` — pins the "default = parapet for new installs" literal.

    IMPORTANT: do NOT include the move task's own file in the fitness-fn glob (it legitimately names the move module). Only the installer + gen.spine + gen.archive_indexes are scanned. Reference D-17, D-20 in comments.
  </action>
  <verify>
    <automated>mix test test/parapet/upgrade_never_forces_move_test.exs</automated>
  </verify>
  <done>`mix test test/parapet/upgrade_never_forces_move_test.exs` passes: the installer/generators contain no reference to the move task (offenders == []); `resolve_prefix(nil, nil) == {:ok, "parapet"}`. DB-less, sub-second, green on both legs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator/installer source → adopter upgrade | a future edit could wire the move task into the auto-run install path, silently forcing a data migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-10 | Tampering | installer auto-chaining a schema-move data migration | medium | mitigate | The UPG-05 regex fitness function statically fails the build if `parapet.install.ex` / `parapet.gen.spine.ex` / `parapet.gen.archive_indexes.ex` ever reference the move task or module (D-20) — pins D-17's "no upgrade path ever auto-runs a data migration" truth. |
| T-54-11 | Spoofing | silent default drift for new installs | low | mitigate | `resolve_prefix(nil, nil) == {:ok, "parapet"}` pin fails if the default ever changes unnoticed (D-20). |
</threat_model>

<verification>
- `PARAPET_SCHEMA_PREFIX='' mix test test/parapet/spine/prefix_propagation_test.exs` green (Track A block fires on the nil leg).
- `PARAPET_SCHEMA_PREFIX=parapet mix test test/parapet/spine/prefix_propagation_test.exs` green (Track A block no-op; existing tests unaffected).
- `mix test test/parapet/upgrade_never_forces_move_test.exs` green on both legs.
</verification>

<success_criteria>
UPG-01 + UPG-05 satisfied: Track A is pinned by a legible test proving `schema_prefix: nil` emits unprefixed SQL with green queries; the installer/generators are statically proven to never force a schema migration; and the "default = parapet for new installs" literal is pinned.
</success_criteria>

<output>
Create `.planning/phases/54-upgrade-path-doctor/54-04-SUMMARY.md` when done.
</output>
