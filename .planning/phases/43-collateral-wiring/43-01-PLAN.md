---
phase: 43-collateral-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/examples/components.html
  - brandbook/examples/landing-section.html
  - brandbook/examples/readme-header.svg
autonomous: true
requirements: [COLLAT-01]

must_haves:
  truths:
    - "brandbook/examples/components.html opens from file:// and renders buttons, badges, callouts, SLO card, form inputs, and a code block in both light and data-theme=dark"
    - "brandbook/examples/landing-section.html opens from file:// and renders a Deep Slate hero using parapet-inverse.svg with a brand-voice headline and install snippet"
    - "brandbook/examples/readme-header.svg renders a wide 1280x320 banner with the horizontal lockup geometry and tagline, using only palette hex"
    - "Every color/size/spacing value in the two HTML files is driven by tokens.css custom properties (no hardcoded hex/px in HTML/CSS)"
  artifacts:
    - path: "brandbook/examples/components.html"
      provides: "Token-driven component gallery (light + dark)"
      contains: "../tokens/tokens.css"
    - path: "brandbook/examples/landing-section.html"
      provides: "Deep Slate hero landing section example"
      contains: "../assets/parapet-inverse.svg"
    - path: "brandbook/examples/readme-header.svg"
      provides: "Wide README/social banner with horizontal lockup + tagline"
      contains: "viewBox=\"0 0 1280 320\""
  key_links:
    - from: "brandbook/examples/components.html"
      to: "brandbook/tokens/tokens.css"
      via: "stylesheet link with relative path ../tokens/tokens.css"
      pattern: "\\.\\./tokens/tokens\\.css"
    - from: "brandbook/examples/landing-section.html"
      to: "brandbook/assets/parapet-inverse.svg"
      via: "img src relative path ../assets/parapet-inverse.svg"
      pattern: "\\.\\./assets/parapet-inverse\\.svg"
---

<objective>
Build the three COLLAT-01 collateral artifacts under `brandbook/examples/`: a token-driven component gallery (light + dark), a Deep Slate landing-section hero, and a wide README/social banner SVG. All three open directly from `file://` with no build step and (for the HTML) IBM Plex via Google Fonts degrading to the system stack offline.

Purpose: Give adopters implementation-ready, copy-pasteable examples that prove the token system drives real UI surfaces and that the locked logo composes into marketing collateral.
Output: `brandbook/examples/components.html`, `brandbook/examples/landing-section.html`, `brandbook/examples/readme-header.svg`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-collateral-wiring/43-CONTEXT.md
@.planning/phases/43-collateral-wiring/43-RESEARCH.md
@brandbook/index.html
@brandbook/tokens/tokens.css
@brandbook/assets/parapet-horizontal.svg
@brandbook/assets/parapet-inverse.svg
@brandbook/assets/parapet-tagline.svg
</context>

<artifacts_produced>
This plan produces:
- `brandbook/examples/components.html` — token-driven component gallery (buttons, badges, callouts, SLO card, form inputs, cards, code block) in light + `data-theme="dark"`
- `brandbook/examples/landing-section.html` — Deep Slate hero using `parapet-inverse.svg`, brand-voice headline, install snippet
- `brandbook/examples/readme-header.svg` — 1280×320 README/social banner embedding `parapet-horizontal.svg` path geometry + tagline
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Build components.html (token-driven, light + dark)</name>
  <files>brandbook/examples/components.html</files>
  <read_first>
    - brandbook/index.html — lift the `#components` section markup and the `<style>` rules for `.btn`/`.btn-primary`/`.btn-ghost`/`.btn-danger`, `.badge`/`.dot`, `.callout`, `.panel`, `.slo`, `.codeblock`; copy the `<head>` pattern (charset, title, Google Fonts link for IBM Plex Sans/Mono)
    - brandbook/tokens/tokens.css — confirm exact token names referenced; the dark theme activates via the `[data-theme="dark"]` attribute selector (NOT a `.dark` class swap)
  </read_first>
  <action>
    Create `brandbook/examples/components.html` as a self-contained page that links `../tokens/tokens.css` (relative path is one level up from `examples/` — never `tokens/tokens.css`). Lift the component markup and styles from `brandbook/index.html` §components and extend.

    Include, all driven by tokens (no hardcoded hex or px in the HTML or inline `<style>` — use the `var(--…)` custom properties): a light panel with primary/ghost/danger buttons (`.btn` family); a status badge row (healthy, watch, burning) using `.badge` + `.dot` with status-set tokens (`--healthy-*`, `--watch-*`, `--burning-*`); an SLO card (`.slo`) with the metric in `--font-mono` at `--fs-metric-lg`; a `.callout` (left border `--watch-blue`, `--mortar` background); a 2-up `.card`/`.panel` grid showing `--shadow-card`; form inputs (input, select, textarea) styled with `--border-light`, `--radius-sm`, and `--focus-ring`; and a `.codeblock` (Deep Slate background, `--font-mono`). Then duplicate the surface inside a container carrying `data-theme="dark"` so reviewers see the dark remap (ghost buttons + callout + SLO card on dark), using `--focus-ring-on-dark` for focus on dark surfaces.

    Load IBM Plex Sans + Mono via the Google Fonts link used in index.html so it degrades to the system stack offline. Do NOT add any font binaries. Do NOT introduce off-palette hex (status-set colors live only here in HTML/CSS, not in any SVG).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && grep -q '\.\./tokens/tokens\.css' brandbook/examples/components.html && grep -q 'data-theme="dark"' brandbook/examples/components.html && grep -ivE 'tokens\.css|googleapis|gstatic' brandbook/examples/components.html | grep -oiE '#[0-9a-f]{6}' | grep -viE '101820|18232B|2E3A42|D8D0C3|EAE2D4|F8F4EC|256C82|B45309|D97706|567236|B13A32|6D5BD0|3F5E28|EFF6E8|B6C99A' | grep -q . && echo "OFF-PALETTE OR HARDCODED HEX FOUND" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '\.\./tokens/tokens\.css' brandbook/examples/components.html` matches (correct relative path, not `tokens/tokens.css`)
    - `grep 'data-theme="dark"'` matches (dark surface present)
    - The hardcoded-hex grep above prints `OK` (no off-palette hex; values come from tokens)
    - Headless-Chrome screenshot renders without missing-style fallout: `CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"; "$CHROME" --headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=2 --window-size=1200,2000 --virtual-time-budget=4500 --screenshot=/tmp/components.png "file://$PWD/brandbook/examples/components.html"` produces a non-empty `/tmp/components.png` showing buttons, badges, SLO card, callout, form inputs, code block in light AND a visibly darker `data-theme="dark"` block
  </acceptance_criteria>
  <done>components.html opens from file://, links ../tokens/tokens.css, shows the full component set in light + dark, and contains no off-palette or hardcoded hex.</done>
</task>

<task type="auto">
  <name>Task 2: Build landing-section.html (Deep Slate hero)</name>
  <files>brandbook/examples/landing-section.html</files>
  <read_first>
    - brandbook/index.html — header hero pattern (uses `<img src="assets/parapet-inverse.svg">`) and the voice/essence section for brand-voice phrasing ("A protective edge, not a fortress")
    - brandbook/tokens/tokens.css — `--deep-slate`, `--limestone`, `--stone`, `--wall-slate`, `--fs-display`, `--space-8`, `--font-mono`
    - brandbook/assets/parapet-inverse.svg — the inverse stacked lockup used on the dark hero
  </read_first>
  <action>
    Create `brandbook/examples/landing-section.html` as a self-contained, restrained Deep Slate hero. Link `../tokens/tokens.css` and the same Google Fonts as components.html. The hero `<section>` uses `background: var(--deep-slate); color: var(--limestone)` with `var(--space-8)` vertical padding. Place `<img src="../assets/parapet-inverse.svg" alt="Parapet">` at a modest height (~64px — restrained, not full-bleed). Add a headline at `var(--fs-display)` in brand voice (calm/architectural — e.g. derived from index.html essence; keep it short and reliability-themed for Phoenix), a muted subhead in `var(--stone)`, and an install snippet rendered in a `.codeblock`-style element (`var(--wall-slate)` background, `var(--font-mono)`) showing the canonical Parapet install/usage line.

    All colors/sizes/spacing come from `var(--…)` tokens — no hardcoded hex or px. Use `--font-mono` for the snippet. Use `--link` / lightened Watch Blue for any link on the dark surface. Keep it minimal: hero only, no nav bar, no footer chrome.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && grep -q '\.\./tokens/tokens\.css' brandbook/examples/landing-section.html && grep -q '\.\./assets/parapet-inverse\.svg' brandbook/examples/landing-section.html && grep -q 'var(--deep-slate)' brandbook/examples/landing-section.html && grep -ivE 'tokens\.css|googleapis|gstatic' brandbook/examples/landing-section.html | grep -oiE '#[0-9a-f]{6}' | grep -viE '101820|18232B|2E3A42|D8D0C3|EAE2D4|F8F4EC|256C82|B45309|D97706|567236|B13A32|6D5BD0' | grep -q . && echo "OFF-PALETTE OR HARDCODED HEX FOUND" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '\.\./tokens/tokens\.css'` and `grep '\.\./assets/parapet-inverse\.svg'` both match
    - `grep 'var(--deep-slate)'` matches (hero uses the Deep Slate token, not a raw hex)
    - The hardcoded-hex grep prints `OK`
    - Headless-Chrome screenshot: `CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"; "$CHROME" --headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=2 --window-size=1200,800 --virtual-time-budget=4500 --screenshot=/tmp/landing.png "file://$PWD/brandbook/examples/landing-section.html"` produces a non-empty `/tmp/landing.png` showing the inverse logo on a Deep Slate hero with headline + subhead + install snippet
  </acceptance_criteria>
  <done>landing-section.html opens from file://, renders a restrained Deep Slate hero using parapet-inverse.svg and token-driven type/spacing, with a brand-voice headline and install snippet; no hardcoded hex.</done>
</task>

<task type="auto">
  <name>Task 3: Build readme-header.svg (1280x320 banner)</name>
  <files>brandbook/examples/readme-header.svg</files>
  <read_first>
    - brandbook/assets/parapet-horizontal.svg — source horizontal lockup (viewBox 0 0 252.0 62.5); copy/transform its outlined path data (tower-left + PARAPET wordmark) into the banner
    - brandbook/assets/parapet-tagline.svg — source of the tagline wording ("reliability for Phoenix")
    - .planning/phases/43-collateral-wiring/43-RESEARCH.md §6 (readme-header authoring) and Pitfall 1/Pitfall 4 — SVGs loaded as files do not resolve CSS var(); hardcode palette hex only; no status-set colors
  </read_first>
  <action>
    Create `brandbook/examples/readme-header.svg` as a standalone banner with `viewBox="0 0 1280 320"` (4:1 wide README/social banner). Background is transparent OR a single Limestone `#F8F4EC` rect — transparent is the brand default; if a fill is used it must be Limestone only. Embed the horizontal lockup geometry from `parapet-horizontal.svg` (the corbelled tower mark to the left of the PARAPET wordmark), scaled and centered, using a `<g transform>` to position the existing outlined path data. Add the tagline text "reliability for Phoenix" below or beside the lockup.

    Because the SVG is consumed as a file (not inline in a styled page), CSS custom properties do NOT resolve — hardcode hex from the 12-value palette only: `#101820` (Parapet Black) for the ink, `#256C82` (Watch Blue) for the single loophole accent, and `#F8F4EC` (Limestone) only if a background fill is used. Do NOT use any status-set colors (e.g. `#3F5E28`, `#EFF6E8`) — they will fail the SVG palette audit. Keep it outlined/path-based and repo-lean (no editor metadata bloat). Do NOT add a full-viewBox dark rectangular background cage.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && grep -q 'viewBox="0 0 1280 320"' brandbook/examples/readme-header.svg && grep -ohiE '#[0-9a-f]{6}' brandbook/examples/readme-header.svg | sort -u | grep -viE '101820|18232B|2E3A42|D8D0C3|EAE2D4|F8F4EC|256C82|B45309|D97706|567236|B13A32|6D5BD0' | grep -q . && echo "OFF-PALETTE FOUND" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'viewBox="0 0 1280 320"'` matches (correct banner dimensions)
    - The palette grep prints `OK` (only the 12 allowed hex values appear; no status-set or Tailwind hex)
    - No full-viewBox dark rect cage: `grep -E '<rect[^>]*fill="#101820"' brandbook/examples/readme-header.svg` should NOT match a full-banner background rect (Limestone-only or transparent background)
    - Headless-Chrome screenshot: `CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"; "$CHROME" --headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=2 --window-size=1280,320 --virtual-time-budget=4500 --screenshot=/tmp/readme-header.png "file://$PWD/brandbook/examples/readme-header.svg"` produces a non-empty `/tmp/readme-header.png` showing the horizontal tower+PARAPET lockup with the Watch Blue loophole accent and the tagline
  </acceptance_criteria>
  <done>readme-header.svg has a 1280×320 viewBox, embeds the horizontal lockup geometry + tagline, uses only palette hex, has no background cage, and renders correctly as a standalone file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase produces only static brand assets (HTML/SVG/CSS). No untrusted runtime input crosses any boundary; the artifacts are author-blind static files opened from `file://`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | Off-palette / off-brand hex slipping into authored assets | mitigate | Palette grep gate in every task `<verify>` (allow-list of 12 palette hex); SVG task additionally forbids status-set colors |
| T-43-02 | Information Disclosure | Embedded raster/font binary bloating the repo | accept | No binaries are authored in this plan; the binary-scan find runs in Plan 03 (COLLAT-03 gate) |
</threat_model>

<verification>
- All three files exist under `brandbook/examples/`
- Both HTML files link `../tokens/tokens.css` (correct relative path) and contain no off-palette/hardcoded hex
- `landing-section.html` references `../assets/parapet-inverse.svg`
- `readme-header.svg` uses `viewBox="0 0 1280 320"` and passes the palette grep
- Headless-Chrome screenshots produced for all three (assets are author-blind — visual confirmation required)
</verification>

<success_criteria>
ROADMAP Phase 43 success criterion 1 is satisfied: three collateral artifacts open from `file://` — `components.html` (buttons, cards, badges, callouts), `landing-section.html` (Deep Slate hero with restrained stepped mark), `readme-header.svg` (README/social banner) — each driven by the token system.
</success_criteria>

<output>
Create `.planning/phases/43-collateral-wiring/43-01-SUMMARY.md` when done.
</output>
