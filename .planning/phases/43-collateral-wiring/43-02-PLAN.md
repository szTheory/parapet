---
phase: 43-collateral-wiring
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - docs/assets/parapet-logo.svg
  - docs/assets/favicon.svg
  - brandbook/notes/logo-options.html
  - brandbook/notes/logo-round-2.html
  - brandbook/notes/logo-round-3.html
  - brandbook/notes/logo-round-4.html
  - brandbook/notes/logo-round-5.html
autonomous: true
requirements: [COLLAT-02]

must_haves:
  truths:
    - "docs/assets/parapet-logo.svg is an on-brand outlined SVG (transparent, palette-locked) replacing the off-brand Tailwind-slate/Arial asset"
    - "docs/assets/favicon.svg is the on-brand brandbook favicon"
    - "mix.exs logo/favicon paths are unchanged (zero-config swap)"
    - "mix docs builds clean (no error or warning) and renders the new mark in the sidebar"
    - "Intermediate exploration HTMLs (logo-options, logo-round-2..5) are deleted; logo-round-6.html is kept as the final-round record"
  artifacts:
    - path: "docs/assets/parapet-logo.svg"
      provides: "On-brand ExDoc sidebar logo (outlined, palette-locked)"
      contains: "256C82"
    - path: "docs/assets/favicon.svg"
      provides: "On-brand ExDoc favicon"
      contains: "256C82"
  key_links:
    - from: "mix.exs"
      to: "docs/assets/parapet-logo.svg"
      via: "ExDoc docs/0 logo: path (unchanged, path-stable swap)"
      pattern: "logo:\\s*\"docs/assets/parapet-logo\\.svg\""
    - from: "docs/assets/parapet-logo.svg"
      to: "brandbook/assets"
      via: "file copied from the locked brandbook asset set"
      pattern: "256C82"
---

<objective>
Wire the locked v1.5 identity into HexDocs (the one shipping change) by copying an on-brand outlined SVG over the off-brand `docs/assets/parapet-logo.svg` and `docs/assets/favicon.svg`, leaving `mix.exs` doc-block paths unchanged so the swap is zero-config. Then trim the dead-weight exploration HTMLs to keep the brand book repo-lean.

Purpose: Replace the off-brand Tailwind-slate/Arial mark (the first visual a HexDocs/GitHub visitor sees, documented as a defect in D-001) with the locked corbelled-tower identity, and shed superseded exploration pages.
Output: replaced `docs/assets/parapet-logo.svg` + `docs/assets/favicon.svg`; deleted `brandbook/notes/logo-options.html` and `logo-round-2..5.html`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-collateral-wiring/43-CONTEXT.md
@.planning/phases/43-collateral-wiring/43-RESEARCH.md
@mix.exs
@brandbook/assets/parapet-mark.svg
@brandbook/assets/parapet-horizontal.svg
@brandbook/assets/favicon.svg
</context>

<artifacts_produced>
This plan produces:
- `docs/assets/parapet-logo.svg` — REPLACED with on-brand outlined asset (starts from `parapet-mark.svg`; fall back to `parapet-horizontal.svg` if the mark reads too spare in the sidebar)
- `docs/assets/favicon.svg` — REPLACED with `brandbook/assets/favicon.svg`
- Deletions: `brandbook/notes/logo-options.html`, `logo-round-2.html`, `logo-round-3.html`, `logo-round-4.html`, `logo-round-5.html` (keep `logo-round-6.html`)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Swap HexDocs logo + favicon (zero-config) and verify mix docs</name>
  <files>docs/assets/parapet-logo.svg, docs/assets/favicon.svg</files>
  <read_first>
    - mix.exs (lines ~55-62) — confirm `logo: "docs/assets/parapet-logo.svg"` and `favicon: "docs/assets/favicon.svg"`; these paths stay UNCHANGED. Do NOT edit mix.exs.
    - brandbook/assets/parapet-mark.svg — recommended ExDoc sidebar logo (compact 32×52 viewBox, reads best small per RESEARCH §2)
    - brandbook/assets/parapet-horizontal.svg — fallback if the mark reads too spare without the wordmark
    - brandbook/assets/favicon.svg — the favicon to copy
    - .planning/phases/43-collateral-wiring/43-RESEARCH.md §3 (ExDoc wiring) and Pitfall 3 (stacked logo clipping)
  </read_first>
  <action>
    Copy the on-brand favicon and logo into `docs/assets/`, overwriting the off-brand assets. Use `gsd-tools query commit`-style filesystem copies — copy `brandbook/assets/favicon.svg` to `docs/assets/favicon.svg`, and copy `brandbook/assets/parapet-mark.svg` to `docs/assets/parapet-logo.svg` (start with the mark — RESEARCH recommends it as the most compact for the small/square ExDoc sidebar). Do NOT touch `mix.exs` — the paths are already correct and path-stable, so this is a zero-config swap.

    Then build the docs and eyeball the sidebar. Run `mix docs` and confirm it completes with no error or warning, then open `doc/index.html` and look at the sidebar logo. If the mark reads too spare/clipped at the ExDoc sidebar size, fall back: copy `brandbook/assets/parapet-horizontal.svg` to `docs/assets/parapet-logo.svg` instead and rebuild. Record in the SUMMARY which asset was chosen and why.

    Do NOT introduce any background cage, off-palette hex, or font binary — the source brandbook assets are already transparent, outlined, and palette-locked, so a straight copy preserves all non-negotiables.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && grep -q 'logo: "docs/assets/parapet-logo.svg"' mix.exs && grep -q 'favicon: "docs/assets/favicon.svg"' mix.exs && for f in docs/assets/parapet-logo.svg docs/assets/favicon.svg; do grep -qi '256C82' "$f" || { echo "MISSING WATCH BLUE in $f"; exit 1; }; grep -oiE '#[0-9a-f]{6}' "$f" | sort -u | grep -viE '101820|18232B|2E3A42|D8D0C3|EAE2D4|F8F4EC|256C82|B45309|D97706|567236|B13A32|6D5BD0' | grep -q . && { echo "OFF-PALETTE in $f"; exit 1; }; grep -qiE '0f172a|38bdf8|94a3b8|Arial' "$f" && { echo "OLD OFF-BRAND CONTENT STILL PRESENT in $f"; exit 1; }; done; echo "OK"; mix docs 2>&1 | tee /tmp/mixdocs.log | grep -iE 'error|warning' && { echo "MIX DOCS NOT CLEAN"; exit 1; } || echo "MIX DOCS CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` still contains `logo: "docs/assets/parapet-logo.svg"` and `favicon: "docs/assets/favicon.svg"` (unchanged — zero-config)
    - Both `docs/assets/parapet-logo.svg` and `docs/assets/favicon.svg` contain `#256C82` (Watch Blue loophole) and ZERO off-palette hex
    - Neither file contains the old off-brand markers (`#0f172a`, `#38bdf8`, `#94a3b8`, `Arial`)
    - `mix docs` exits 0 with no `error`/`warning` lines in output; `doc/index.html` exists
    - Sidebar logo visually confirmed via `open doc/index.html` (chosen asset recorded in SUMMARY)
  </acceptance_criteria>
  <done>docs/assets/parapet-logo.svg and favicon.svg are on-brand outlined palette-locked assets, mix.exs is unchanged, and mix docs builds clean and renders the new mark.</done>
</task>

<task type="auto">
  <name>Task 2: Trim superseded exploration HTMLs (repo-lean)</name>
  <files>brandbook/notes/logo-options.html, brandbook/notes/logo-round-2.html, brandbook/notes/logo-round-3.html, brandbook/notes/logo-round-4.html, brandbook/notes/logo-round-5.html</files>
  <read_first>
    - .planning/phases/43-collateral-wiring/43-CONTEXT.md (Cleanup section) and 43-RESEARCH.md §5 (Cleanup Scope) — delete logo-options.html + rounds 2-5; KEEP logo-round-6.html as the final-round record
    - brandbook/notes/decision-log.md D-003 — references the round files as provenance; round-6 is the final-round record to retain
  </read_first>
  <action>
    Delete the five superseded intermediate exploration pages: `brandbook/notes/logo-options.html`, `logo-round-2.html`, `logo-round-3.html`, `logo-round-4.html`, `logo-round-5.html` (~49 KB combined). KEEP `brandbook/notes/logo-round-6.html` — it documents the final stacked-emblem tournament that produced the locked identity (D-003). Do NOT delete `brandbook/assets/explorations/*.svg` (small round-1 provenance SVGs, retained). Do NOT touch any tokens, assets, or notes other than the five named HTMLs.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && for f in logo-options logo-round-2 logo-round-3 logo-round-4 logo-round-5; do test ! -e "brandbook/notes/$f.html" || { echo "$f.html STILL PRESENT"; exit 1; }; done && test -e brandbook/notes/logo-round-6.html && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `brandbook/notes/logo-options.html` and `logo-round-2.html`..`logo-round-5.html` no longer exist
    - `brandbook/notes/logo-round-6.html` still exists (final-round record kept)
    - `brandbook/assets/explorations/` is untouched
  </acceptance_criteria>
  <done>The five superseded exploration HTMLs are deleted; logo-round-6.html and explorations SVGs remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

The only shipping change crosses the repo→HexDocs publish boundary: `docs/assets/*.svg` become the public-facing logo/favicon. No runtime input is involved.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02a | Tampering | Off-brand/off-palette content surviving in the swapped HexDocs assets | mitigate | Verify grep asserts presence of Watch Blue, absence of off-palette hex, and absence of the old off-brand markers (`#0f172a`/`#38bdf8`/`Arial`) before commit |
| T-43-02b | Spoofing | Wrong/empty asset copied (logo not rendering in ExDoc) | mitigate | `mix docs` must build clean and `doc/index.html` is eyeballed; chosen asset recorded in SUMMARY |
| T-43-02c | Tampering | Unintended edits to `mix.exs` widening the diff | mitigate | Verify asserts the two doc-block paths are unchanged; task action explicitly forbids editing mix.exs |
</threat_model>

<verification>
- `mix.exs` doc-block paths unchanged
- Both swapped assets contain Watch Blue, no off-palette hex, no old off-brand markers
- `mix docs` builds clean (no error/warning) and renders the new sidebar mark
- Five exploration HTMLs deleted; logo-round-6.html retained
</verification>

<success_criteria>
ROADMAP Phase 43 success criterion 2 is satisfied: `docs/assets/parapet-logo.svg` and `docs/assets/favicon.svg` are replaced with the on-brand winners, `mix.exs` doc-block paths are unchanged, and `mix docs` renders the new mark without error or warning.
</success_criteria>

<output>
Create `.planning/phases/43-collateral-wiring/43-02-SUMMARY.md` when done.
</output>
