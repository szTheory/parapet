---
phase: 13-repair-generated-operator-resolve-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/parapet.gen.ui/operator_live.ex.eex
  - test/parapet/generated_operator_live_paging_test.exs
  - test/parapet/operator_ui_integration_test.exs
  - test/mix/tasks/parapet.gen.ui_test.exs
autonomous: true
requirements:
  - SCALE-01.c
must_haves:
  truths:
    - "The generated queue LiveView `\"resolve\"` action performs a real incident lifecycle transition through `Parapet.Operator.resolve_incident/2` per D-01, D-02, and D-04."
    - "Queue-side resolve stays inside the generated UI to `Parapet.Operator` seam and does not reimplement lifecycle semantics in the template per D-03 and D-05."
    - "The quick generated-runtime proof lane fails if queue-side resolve stops removing incidents from the active queue or stops surfacing them in resolved history per D-06, D-07, and D-08."
  artifacts:
    - path: priv/templates/parapet.gen.ui/operator_live.ex.eex
      provides: Generated queue LiveView resolve handler wired to `Parapet.Operator.resolve_incident/2`.
    - path: test/parapet/generated_operator_live_paging_test.exs
      provides: Runtime proof that queue resolve mutates state and changes rendered active/resolved scopes.
    - path: test/parapet/operator_ui_integration_test.exs
      provides: Source-contract assertion that queue resolve uses `resolve_incident` and not `record_note`.
    - path: test/mix/tasks/parapet.gen.ui_test.exs
      provides: Generator-output contract assertion for the emitted queue resolve seam.
  key_links:
    - from: priv/templates/parapet.gen.ui/operator_live.ex.eex
      to: lib/parapet/operator.ex
      via: `handle_event("resolve", ...)` calling `Parapet.Operator.resolve_incident/2`
      pattern: 'handle_event\("resolve"|Parapet\.Operator\.resolve_incident'
    - from: test/parapet/generated_operator_live_paging_test.exs
      to: priv/templates/parapet.gen.ui/operator_live.ex.eex
      via: compiled generated LiveView exercising the queue resolve event
      pattern: 'handle_event\("resolve"|resolved|active'
    - from: test/parapet/operator_ui_integration_test.exs
      to: priv/templates/parapet.gen.ui/operator_live.ex.eex
      via: source-contract assertion on the queue resolve seam
      pattern: 'resolve_incident|record_note'
---

<objective>
Repair the generated operator queue resolve seam and add a cheap, durable regression backstop in the existing targeted proof lane.

Purpose: restore the real queue-side incident lifecycle path so the generated operator UI once again tells the truth about active-queue to resolved-history behavior.
Output: repaired queue template plus source-contract and generated-runtime assertions covering the queue resolve path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-repair-generated-operator-resolve-flow/13-CONTEXT.md
@.planning/phases/13-repair-generated-operator-resolve-flow/13-RESEARCH.md
@.planning/phases/13-repair-generated-operator-resolve-flow/13-PATTERNS.md
@.planning/phases/13-repair-generated-operator-resolve-flow/13-VALIDATION.md
@priv/templates/parapet.gen.ui/operator_live.ex.eex
@priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
@lib/parapet/operator.ex
@lib/parapet/operator/action_payload.ex
@lib/parapet/evidence.ex
@test/parapet/generated_operator_live_paging_test.exs
@test/parapet/operator_ui_integration_test.exs
@test/mix/tasks/parapet.gen.ui_test.exs
@test/parapet/evidence_test.exs

<interfaces>
From `lib/parapet/operator.ex`:
```elixir
def resolve_incident(%Incident{} = incident, %ActionPayload{} = payload) do
  incident_changeset = Ecto.Changeset.change(incident, %{state: "resolved", runbook_data: runbook_data})
  timeline_attrs = %{type: "status_change", payload: %{"new_state" => "resolved"}}
  audit_attrs = build_audit("operator_resolve_incident", payload)
  Evidence.run_operator_command(
    incident_changeset: incident_changeset,
    timeline_attrs: timeline_attrs,
    audit_attrs: audit_attrs
  )
end
```

From `priv/templates/parapet.gen.ui/operator_detail_live.ex.eex`:
```elixir
def handle_event("resolve", %{"id" => id}, socket) do
  incident = Test.Repo.get!(Parapet.Spine.Incident, id)
  payload = %Parapet.Operator.ActionPayload{actor: "operator_ui", reason: "Resolved via UI", correlation_id: Ecto.UUID.generate(), action_type: :resolve}

  case Parapet.Operator.resolve_incident(incident, payload) do
    {:ok, _result} -> {:noreply, push_navigate(socket, to: "/parapet/#{id}")}
    {:error, _reason} -> {:noreply, put_flash(socket, :error, "Failed to resolve")}
  end
end
```

From `lib/parapet/operator/action_payload.ex`:
```elixir
field(:action_type, Ecto.Enum, values: [:immutable_fact, :narrative_edit, :execute_mitigation, :acknowledge, :resolve])
```

From `test/parapet/evidence_test.exs`:
```elixir
def transaction(%Ecto.Multi{} = multi) do
  ops = Ecto.Multi.to_list(multi)
  # update/run operations can be intercepted to persist fake repo state during tests
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire generated queue resolve to the public operator seam</name>
  <read_first>
    .planning/phases/13-repair-generated-operator-resolve-flow/13-CONTEXT.md
    .planning/phases/13-repair-generated-operator-resolve-flow/13-PATTERNS.md
    .planning/phases/13-repair-generated-operator-resolve-flow/13-VALIDATION.md
    priv/templates/parapet.gen.ui/operator_live.ex.eex
    priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
    lib/parapet/operator.ex
    lib/parapet/operator/action_payload.ex
    test/parapet/operator_ui_integration_test.exs
    test/mix/tasks/parapet.gen.ui_test.exs
  </read_first>
  <files>
    priv/templates/parapet.gen.ui/operator_live.ex.eex
    test/parapet/operator_ui_integration_test.exs
    test/mix/tasks/parapet.gen.ui_test.exs
  </files>
  <action>Update the generated queue LiveView `handle_event("resolve", %{"id" => id}, socket)` in `priv/templates/parapet.gen.ui/operator_live.ex.eex` so it mirrors the detail LiveView seam per D-01 through D-05: build the same `%Parapet.Operator.ActionPayload{actor: "operator_ui", reason: "Resolved via UI", correlation_id: Ecto.UUID.generate(), action_type: :resolve}` and call `Parapet.Operator.resolve_incident(incident, payload)` instead of `Parapet.Operator.record_note(incident, "Resolved", payload)`. Keep the queue-side navigation as `push_patch(socket, to: queue_path(socket, %{"id" => id}))` so the current LiveView reloads through `handle_params/3`, not a new direct state machine in the template. Then tighten the source-contract tests in `test/parapet/operator_ui_integration_test.exs` and `test/mix/tasks/parapet.gen.ui_test.exs` so they explicitly assert the queue template/generated output contains `Parapet.Operator.resolve_incident`, still contains `handle_event("resolve"`, and does not route queue resolve through `record_note`.</action>
  <acceptance_criteria>
    - `rg -n 'handle_event\\("resolve"|Parapet\\.Operator\\.resolve_incident|record_note' priv/templates/parapet.gen.ui/operator_live.ex.eex` shows `handle_event("resolve"` and `Parapet.Operator.resolve_incident` but no `record_note`.
    - `rg -n 'resolve_incident|record_note|handle_event\\("resolve"' test/parapet/operator_ui_integration_test.exs test/mix/tasks/parapet.gen.ui_test.exs` shows positive assertions for `resolve_incident` and a negative assertion for `record_note`.
    - `mix test test/parapet/operator_ui_integration_test.exs test/mix/tasks/parapet.gen.ui_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/parapet/operator_ui_integration_test.exs test/mix/tasks/parapet.gen.ui_test.exs</automated>
  </verify>
  <done>The generated queue resolve handler and the cheap source-contract guardrails all point at `Parapet.Operator.resolve_incident/2`, preserving the public generated-UI seam.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the generated runtime lane to prove queue resolve changes rendered lifecycle state</name>
  <read_first>
    .planning/phases/13-repair-generated-operator-resolve-flow/13-CONTEXT.md
    .planning/phases/13-repair-generated-operator-resolve-flow/13-RESEARCH.md
    .planning/phases/13-repair-generated-operator-resolve-flow/13-PATTERNS.md
    .planning/phases/13-repair-generated-operator-resolve-flow/13-VALIDATION.md
    test/parapet/generated_operator_live_paging_test.exs
    test/parapet/evidence_test.exs
    lib/parapet/operator.ex
    lib/parapet/evidence.ex
    priv/templates/parapet.gen.ui/operator_live.ex.eex
  </read_first>
  <files>test/parapet/generated_operator_live_paging_test.exs</files>
  <action>Extend `test/parapet/generated_operator_live_paging_test.exs` per D-06 through D-08 without adding any browser harness. Upgrade the local `Test.Repo` fake so `Parapet.Operator.resolve_incident/2` can complete through `Parapet.Evidence.run_operator_command/1`: add `update/2` and `transaction/1` support for `Ecto.Multi.update`, `Ecto.Multi.insert`, and `Ecto.Multi.run`, persist incident state updates back into the `Agent`, append inserted `TimelineEntry` records into the per-incident `entries` map, and answer `all/1` for both active and resolved queue queries instead of filtering to active only. Keep the existing bounded paging assertions, then add a narrow runtime test that mounts the compiled generated LiveView, triggers `handle_event("resolve", %{"id" => active_incident_id}, socket)`, re-runs `handle_params/3` for active scope, and asserts the resolved incident disappears from the active queue. In the same test or a second focused test, switch to `%{"status" => "resolved"}` and assert that the same incident title appears in resolved history, proving the repaired queue action now drives the real lifecycle outcome rather than just a note side effect.</action>
  <acceptance_criteria>
    - `rg -n 'def update|def transaction|state: \"resolved\"|status\" => \"resolved\"|handle_event\\(\"resolve\"' test/parapet/generated_operator_live_paging_test.exs` finds the fake-repo mutation hooks and the new resolve-flow assertions.
    - `mix test test/parapet/generated_operator_live_paging_test.exs` exits 0.
    - The resolve runtime test contains one assertion that a formerly active incident title is absent from the active queue after resolve and one assertion that the same title is present in resolved history after switching queue status.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/parapet/generated_operator_live_paging_test.exs</automated>
  </verify>
  <done>The existing quick generated-runtime lane now proves the queue resolve action performs a real active-to-resolved lifecycle transition and will fail if future template edits bypass `Parapet.Operator.resolve_incident/2`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated operator UI event -> `Parapet.Operator` mutation seam | untrusted operator-side event names and IDs cross from LiveView templates into durable incident mutation code |
| generated runtime test harness -> fake repo transaction layer | proof code must simulate the real audited mutation path closely enough to catch seam bypasses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | S | `priv/templates/parapet.gen.ui/operator_live.ex.eex` | mitigate | Keep queue-side resolve on `Parapet.Operator.resolve_incident/2` with a real `ActionPayload` carrying actor, reason, correlation ID, and `action_type: :resolve` per D-01 and D-04. |
| T-13-02 | T | queue resolve handler | mitigate | Remove `record_note` from the queue resolve path and add source-contract assertions that fail if the template or generator output reintroduces it per D-06. |
| T-13-03 | R | generated runtime proof lane | mitigate | Extend the runtime test to assert durable rendered outcomes in both active and resolved scopes so future regressions cannot claim resolve happened without evidence-backed state change per D-08. |
</threat_model>

<verification>
Run the existing quick proof lane commands from `13-VALIDATION.md`: `mix test test/parapet/operator_ui_integration_test.exs test/mix/tasks/parapet.gen.ui_test.exs` and `mix test test/parapet/generated_operator_live_paging_test.exs`.
</verification>

<success_criteria>
- The generated queue template routes `"resolve"` through `Parapet.Operator.resolve_incident/2` and no longer routes it through `record_note/3`.
- Cheap source-contract tests fail if the generated queue seam drifts away from `resolve_incident/2`.
- The generated runtime test lane proves an incident leaves the active queue and appears in resolved history after queue-side resolve.
</success_criteria>

<output>
After completion, create `.planning/phases/13-repair-generated-operator-resolve-flow/13-01-SUMMARY.md`
</output>
