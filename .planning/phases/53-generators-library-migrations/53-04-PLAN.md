---
phase: 53-generators-library-migrations
plan: 04
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - priv/repo/migrations/20260511000000_add_runbook_data_to_incidents.exs
  - priv/repo/migrations/20260516233447_add_trace_id_to_incidents.exs
  - priv/repo/migrations/20260517000000_add_parapet_system_events.exs
  - priv/repo/migrations/20260521010000_create_parapet_action_claims.exs
  - priv/repo/migrations/20260528010000_add_lease_until_to_parapet_action_claims.exs
  - examples/demo_app/priv/repo/migrations/20260525000000_add_parapet_spine_tables.exs
  - examples/demo_app/priv/repo/migrations/20260525000001_add_action_item_kind_and_incident_id.exs
  - examples/demo_app/priv/repo/migrations/20260525000002_add_parapet_action_claims.exs
autonomous: true
requirements: [GEN-06]
must_haves:
  truths:
    - "All 5 committed library migrations and all 3 demo migrations bind the prefix via a module attribute @prefix Parapet.Spine.Schema.__prefix__() and pass @prefix into every create/alter table, references/2, and create index/unique_index (D-10, D-11, GEN-06)."
    - "The raw SQL UPDATE in add_lease_until_to_parapet_action_claims.exs is schema-qualified via @prefix so it hits parapet.parapet_action_claims under the prefixed leg and the bare table under the nil leg (RESEARCH landmine, D-11)."
    - "Under the public (nil) CI leg, @prefix resolves to nil and every prefixed call is byte-equivalent to the pre-v1.7 output, so the public matrix leg stays green with no diff in behavior (D-11)."
    - "Migration timestamps are unchanged; the one-time contributor stale-DB reset (mix demo.reset + drop parapet_concurrency_test) is documented in the SUMMARY/PR body for Phase 55 to carry into the upgrade doc (D-12)."
    - "The full suite is green under the parapet leg (PARAPET_SCHEMA_PREFIX=parapet) and the nil/public leg (via the Phase-52 dual-prefix matrix)."
  artifacts:
    - priv/repo/migrations/20260511000000_add_runbook_data_to_incidents.exs
    - priv/repo/migrations/20260516233447_add_trace_id_to_incidents.exs
    - priv/repo/migrations/20260517000000_add_parapet_system_events.exs
    - priv/repo/migrations/20260521010000_create_parapet_action_claims.exs
    - priv/repo/migrations/20260528010000_add_lease_until_to_parapet_action_claims.exs
    - examples/demo_app/priv/repo/migrations/20260525000000_add_parapet_spine_tables.exs
    - examples/demo_app/priv/repo/migrations/20260525000001_add_action_item_kind_and_incident_id.exs
    - examples/demo_app/priv/repo/migrations/20260525000002_add_parapet_action_claims.exs
  key_links:
    - "committed migration @prefix module attribute → Parapet.Spine.Schema.__prefix__() (compile-time, recompiled per CI leg — the counterpart to the generated literal)"
    - "add_lease_until raw execute/2 → @prefix-qualified table name (the one non-Ecto site)"
  prohibitions:
    - "Committed migrations must NOT hard-code a \"parapet\" literal and must NOT call the generate-time resolve_prefix/2 — they bind via compile-time __prefix__() only (D-11). A literal breaks the public CI leg."
    - "Do NOT rename or re-timestamp any migration file — the add_lease_until version pin and history depend on it (D-12)."
    - "Do NOT add an additive SET SCHEMA move migration — that is Phase 54's job (D-10, out of scope)."
    - "Do NOT put prefix: as an Ecto option on the raw execute/2 UPDATE — qualify the table name inside the SQL string instead."
---

<objective>
Edit the 5 committed library migrations and 3 demo migrations in place (D-10) to bind the Postgres schema prefix via the compile-time `Parapet.Spine.Schema.__prefix__()` (D-11), including schema-qualifying the one raw-SQL `UPDATE` landmine in `add_lease_until`, so Parapet's own tables are created under the schema (GEN-06) and both CI-matrix legs stay green.

Purpose: This is the committed-migration counterpart to the generated adopter output (Plan 02). The load-bearing nuance (D-11): these files ARE recompiled under the Phase-52 dual-prefix matrix, so they must reference `__prefix__()` (recompiled, leg-aware) — NOT a literal (would break the public leg) and NOT `resolve_prefix/2` (generate-time). This plan owns only migration files, so it runs in Wave 2 parallel to Plans 02 and 03.
Output: 8 edited migration files; a documented one-time contributor stale-DB reset step in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-generators-library-migrations/53-CONTEXT.md
@.planning/phases/53-generators-library-migrations/53-RESEARCH.md
@priv/repo/migrations/20260528010000_add_lease_until_to_parapet_action_claims.exs

# Interface: the compile-time prefix source consumed by @prefix:
@lib/parapet/spine/schema.ex
</context>

<artifacts_produced>
This plan introduces NO new public symbols — it edits existing committed migrations in place. The only new construct is a per-file `@prefix Parapet.Spine.Schema.__prefix__()` module attribute (a compile-time constant), which is not a public API and is exempt from drift/no-new-symbol checks.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compile-time config → committed migration DDL | `__prefix__()` (frozen library value) reaches the raw `UPDATE` SQL string in add_lease_until. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-04 | Tampering | `@prefix`-interpolated raw `UPDATE` in add_lease_until | low | mitigate | `__prefix__()` returns the library's compile-time value, which was itself passed through `normalize/1` → `safe_ident!/1` (allowlist) when `@prefix` was built in `Parapet.Spine.Schema`. The value interpolated into the `UPDATE` string is therefore already allowlist-validated; no operator-supplied string reaches these committed files at runtime. |

No high-severity threats: committed migrations bind a frozen, pre-validated compile-time value, not live operator input.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp @prefix into the 5 committed library migrations (incl. the raw-SQL landmine)</name>
  <files>priv/repo/migrations/20260511000000_add_runbook_data_to_incidents.exs, priv/repo/migrations/20260516233447_add_trace_id_to_incidents.exs, priv/repo/migrations/20260517000000_add_parapet_system_events.exs, priv/repo/migrations/20260521010000_create_parapet_action_claims.exs, priv/repo/migrations/20260528010000_add_lease_until_to_parapet_action_claims.exs</files>
  <read_first>
    - All 5 files above — the exact `alter table`/`create table`/`references/2`/`create index`/`create unique_index` calls (RESEARCH "Committed Migration File Audit" table, lines 634-655, lists the per-file operations).
    - priv/repo/migrations/20260528010000_add_lease_until_to_parapet_action_claims.exs — the `execute("UPDATE parapet_action_claims SET lease_until = ...")` on lines 39-42 (the landmine).
    - lib/parapet/spine/schema.ex — confirm `__prefix__/0` returns `nil` under the public leg (so `prefix: nil` is a no-op) and `"parapet"` under the default leg.
    - .planning/phases/53-generators-library-migrations/53-RESEARCH.md Pattern 3 (@prefix module attribute, lines 515-538), Pitfall 3 (must be a module attribute, not inline call), Open Question #2 (raw-SQL resolution pattern, lines 801-809).
  </read_first>
  <behavior>
    - Under the parapet leg: all 5 migrations create/alter tables, references, and indexes in the `parapet` schema; the add_lease_until `UPDATE` targets `parapet.parapet_action_claims`.
    - Under the public/nil leg: `@prefix` is nil, every prefixed call is byte-equivalent to the pre-v1.7 output, and the `UPDATE` targets the bare `parapet_action_claims`.
    - FK constraint / index names are byte-identical across both legs (Ecto uses `:name` only, D-03).
  </behavior>
  <action>
    In each of the 5 files, add a module-level `@prefix Parapet.Spine.Schema.__prefix__()` attribute at the top of the migration module (per Pitfall 3 — it MUST be a module attribute evaluated at compile time, never an inline call inside the macro), then thread `prefix: @prefix` into every `alter table`, `create table`, each `references/2` (its own explicit `prefix: @prefix`, D-02), and every `create index`/`create unique_index` per the RESEARCH per-file audit table. For the `add_lease_until` raw-SQL landmine, do NOT add a `prefix:` Ecto option to `execute/2` — instead schema-qualify the table name inside the SQL string using `@prefix`: define a compile-time `@table` (e.g. `if @prefix, do: ~s("#{@prefix}"."parapet_action_claims"), else: "parapet_action_claims"`) and interpolate `@table` into both the `up` UPDATE string and keep the empty `down` string as-is (per Open Question #2). Bind via `__prefix__()` ONLY — never a `"parapet"` literal, never `resolve_prefix/2` (D-11). Keep every constraint/index NAME string literal untouched (D-03). Do NOT rename or re-timestamp any file (D-12). Do NOT add any SET SCHEMA move (D-10).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5 && grep -L 'Parapet.Spine.Schema.__prefix__' priv/repo/migrations/2026*.exs</automated>
  </verify>
  <done>All 5 library migrations compile clean and each references `Parapet.Spine.Schema.__prefix__()` via a module attribute (grep -L returns no file); the add_lease_until raw UPDATE is schema-qualified via `@prefix`; no timestamps changed; no literal `"parapet"` prefix and no `resolve_prefix` appears in any of the 5 files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stamp @prefix into the 3 demo migrations</name>
  <files>examples/demo_app/priv/repo/migrations/20260525000000_add_parapet_spine_tables.exs, examples/demo_app/priv/repo/migrations/20260525000001_add_action_item_kind_and_incident_id.exs, examples/demo_app/priv/repo/migrations/20260525000002_add_parapet_action_claims.exs</files>
  <read_first>
    - All 3 demo files — the RESEARCH "Demo migrations" audit table (lines 657-665) lists per-file operations (5 create table + 2 references + 8 indexes in the first; alter+references+index in the second; create table + references + 4 indexes in the third).
    - .planning/phases/53-generators-library-migrations/53-RESEARCH.md Pattern 3 + Pitfall 3 (module attribute form).
  </read_first>
  <behavior>
    - Under the parapet leg: `mix ecto.migrate` in the demo lands all spine tables in the `parapet` schema (the demo's plain migrate self-creates the schema via the migrator ordering — GEN-06 "no --prefix needed").
    - Under the nil leg: byte-equivalent to today's demo output.
  </behavior>
  <action>
    Add `@prefix Parapet.Spine.Schema.__prefix__()` to each demo migration module and thread `prefix: @prefix` into every `create table`, `alter table`, each `references/2` (explicit `prefix: @prefix`), and every `create index`/`create unique_index` per the RESEARCH demo audit table. These files inline all columns (no incremental raw-SQL landmine on the demo side), so edits are straightforward. Bind via `__prefix__()` only (D-11); keep constraint/index name literals and timestamps unchanged (D-03/D-12). Confirm `Parapet.Spine.Schema` is loadable from the demo migration compile context (it is a dep of the demo app); if the demo app namespaces its migrations under `DemoApp.Repo.Migrations.*`, leave the module names unchanged — only add the attribute and the `prefix: @prefix` opts.
  </action>
  <verify>
    <automated>grep -L 'Parapet.Spine.Schema.__prefix__' examples/demo_app/priv/repo/migrations/2026*.exs</automated>
  </verify>
  <done>All 3 demo migrations reference `__prefix__()` via a module attribute (grep -L returns no file) and carry `prefix: @prefix` on every table/reference/index; no literal prefix, no `resolve_prefix`, no timestamp/name changes.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full suite under both prefix legs; document the one-time contributor stale-DB reset</name>
  <files>.planning/phases/53-generators-library-migrations/53-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/53-generators-library-migrations/53-CONTEXT.md D-12 (the one-time reset step wording) and 53-VALIDATION.md "Manual-Only Verifications" (the reset row).
    - test/support/concurrency_bootstrap.ex — confirm the bootstrap's `CREATE ... IF NOT EXISTS` two-leg behavior so a fresh run is deterministic.
  </read_first>
  <action>
    Run the full test suite under both matrix legs to prove the committed-migration edits are green: `PARAPET_SCHEMA_PREFIX=parapet mix test` (parapet leg) and the default/nil leg (`PARAPET_SCHEMA_PREFIX=public mix compile --force && mix test`, matching the Phase-52 dual-prefix matrix recompile-per-leg rule). If a stale local DB causes failures, run the one-time reset (`mix demo.reset` for the demo + drop the `parapet_concurrency_test` database for the lib) as documented in D-12 — then re-run. In the SUMMARY, record: (a) both legs green, and (b) the one-time contributor stale-DB reset step verbatim (for Phase 55 to carry into the CHANGELOG/PR body and `docs/upgrade-1.x.md`, per D-12). Do NOT edit CHANGELOG or upgrade docs here — that is Phase 55's charter; this task only records the step in the SUMMARY.
  </action>
  <verify>
    <automated>PARAPET_SCHEMA_PREFIX=parapet mix test 2>&1 | tail -15</automated>
  </verify>
  <done>Full suite green under the parapet leg; the nil/public leg is verified via a force-recompile run; the SUMMARY records both-legs-green and the verbatim one-time stale-DB reset step for Phase 55.</done>
</task>

</tasks>

<verification>
- `PARAPET_SCHEMA_PREFIX=parapet mix test` is green.
- The nil/public leg is green after `mix compile --force` (dual-prefix matrix parity).
- `grep -L 'Parapet.Spine.Schema.__prefix__'` over all 8 migration files returns nothing (every file bound via `__prefix__()`).
- No migration filename/timestamp changed (git status shows only content edits on the 8 files).
</verification>

<success_criteria>
- GEN-06: all 5 lib + 3 demo committed migrations create their tables under the schema via `__prefix__()`; the demo's plain `mix ecto.migrate` self-creates the schema; the raw-SQL UPDATE landmine is schema-qualified.
- Both CI-matrix legs (parapet + public) are green; the public leg is byte-equivalent to pre-v1.7 (D-11).
- The one-time contributor stale-DB reset is documented for Phase 55 (D-12).
</success_criteria>

<output>
Create `.planning/phases/53-generators-library-migrations/53-04-SUMMARY.md` when done.
</output>
