---
phase: 25
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - test/parapet/operator_test.exs
  - test/parapet/operator/preview_lifecycle_test.exs
  - test/parapet/operator/confirm_concurrency_test.exs
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04
tags:
  - tests
  - concurrency
  - preview-lifecycle

must_haves:
  truths:
    - "The existing assertion at test/parapet/operator_test.exs:524 is updated from {:error, :stale_preview} to {:short_circuited, :preview_expired}."
    - "A new unit test asserts confirm_runbook_step/4 returns {:short_circuited, :target_refs_drift} when stored target_refs no longer match the recorded hash (DummyRepo + Process.put mock pattern)."
    - "A new unit test asserts confirm_runbook_step/4 returns {:short_circuited, :preview_expired} (separate from the legacy-updated assertion if needed for isolation)."
    - "A new multi-node concurrency test (@tag :unboxed) asserts that two operators racing Confirm against the same (incident_id, action_kind: \"operator\", action_key) after each taking a valid Preview yield exactly one {:ok, _} and exactly one {:conflicted, _claim_id}, with the claim_id matching the winner's row (action_kind == \"operator\", status == \"executed\")."
    - "The concurrency test resets Parapet.Capabilities Agent state inside unboxed_run before register_recovery (Pitfall 4 — ConcurrencyCase does NOT reset the Capabilities Agent)."
  artifacts:
    - path: "test/parapet/operator/confirm_concurrency_test.exs"
      provides: "Multi-node race proof for operator-confirm path"
      contains:
        - "use Parapet.TestSupport.ConcurrencyCase"
        - "@tag :unboxed"
        - "action_kind == \"operator\""
        - "Agent.update(Parapet.Capabilities"
        - "Operator.confirm_runbook_step("
    - path: "test/parapet/operator/preview_lifecycle_test.exs"
      provides: "Unit-test coverage for :preview_expired and :target_refs_drift short-circuit branches"
      contains:
        - ":target_refs_drift"
        - ":preview_expired"
        - "Process.put(:mock_entries"
    - path: "test/parapet/operator_test.exs"
      provides: "Updated :stale_preview -> :preview_expired assertion at :524"
      contains:
        - "{:short_circuited, :preview_expired}"
  key_links:
    - from: "test/parapet/operator/confirm_concurrency_test.exs"
      to: "Parapet.Operator.confirm_runbook_step/4 + Parapet.Automation.ClaimService.claim_action/1"
      via: "Task.async rendezvous + :go broadcast (verbatim pattern from executor_concurrency_test.exs:54-86)"
    - from: "test/parapet/operator/preview_lifecycle_test.exs"
      to: "Parapet.Operator.confirm_runbook_step/4"
      via: "DummyRepo + Process.put(:mock_entries, [...]) sync mock"
---

<objective>
Provide the Wave-0-equivalent verification scaffolding for Phase 25: prove via tests that `Parapet.Operator.confirm_runbook_step/4` correctly returns each of the new variants (`{:short_circuited, :preview_expired}`, `{:short_circuited, :target_refs_drift}`, `{:conflicted, claim_id}`) and routes through `ClaimService.claim_action/1` with `action_kind: "operator"` under multi-node contention.

Purpose: UI-02 ("Confirm routes through ClaimService") and UI-04 ("conflicted branch") cannot be proven by compile-checks alone. UI-03 (preview lifecycle short-circuits) needs unit coverage. RESEARCH.md Validation Architecture explicitly lists these as Wave 0 gaps.

Output: One updated assertion in the existing `test/parapet/operator_test.exs`, one new unit-test file `test/parapet/operator/preview_lifecycle_test.exs`, and one new concurrency-test file `test/parapet/operator/confirm_concurrency_test.exs`. All three tests pass after plan 25-01 lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-CONTEXT.md
@.planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-RESEARCH.md
@.planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-PATTERNS.md
@.planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-VALIDATION.md

# The function under test (post-plan-01)
@lib/parapet/operator.ex

# Test infrastructure
@test/parapet/operator_test.exs
@test/support/concurrency_case.ex
@test/support/concurrency_bootstrap.ex

# THE template for the concurrency test
@test/parapet/automation/executor_concurrency_test.exs

# Second concurrency precedent (for cross-reference)
@test/parapet/automation/claim_service_test.exs

# ActionPayload used by the test
@lib/parapet/operator/action_payload.ex

# ActionClaim — for the claim-row assertion at end of concurrency test
@lib/parapet/spine/action_claim.ex

<interfaces>
<!-- Test harness contracts -->

From test/support/concurrency_case.ex:
```elixir
use Parapet.TestSupport.ConcurrencyCase, async: false
# Provides: unboxed_run/1, ConcurrencyRepo, ConcurrencyBootstrap.reset!/0
# CRITICAL: Does NOT reset Parapet.Capabilities Agent — test must do it explicitly (Pitfall 4)
```

From lib/parapet/automation/claim_service.ex spec (post-plan-01 the operator path becomes a third caller):
```elixir
@spec claim_action(keyword()) ::
  {:won, ActionClaim.t()}
  | {:short_circuited, ActionClaim.t(), String.t()}
  | {:conflicted, ActionClaim.t()}
  | {:error, term()}
```

From lib/parapet/operator/action_payload.ex (:execute_mitigation requires :idempotency_key):
```elixir
%ActionPayload{
  actor: "operator_ui",
  reason: String.t(),
  correlation_id: String.t(),
  action_type: :execute_mitigation,
  idempotency_key: String.t()  # required
}
```

From test/parapet/operator_test.exs:1-20 (existing DummyRepo + mock_entries pattern):
```elixir
# DummyRepo implements .all/1 returning Process.get(:mock_entries, [])
# Configured via Application.put_env(:parapet, :repo, DummyRepo)
# `setup` block (around :455) typically Agent.update(Parapet.Capabilities, fn _ -> %{recovery: %{}} end)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update :stale_preview assertion in operator_test.exs to expect {:short_circuited, :preview_expired}</name>
  <files>test/parapet/operator_test.exs</files>
  <read_first>
    - test/parapet/operator_test.exs (focus :495-541 — the existing "confirm_runbook_step executes and rejects stale previews" test, particularly :524)
    - .planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-RESEARCH.md "Runtime State Inventory" (:401-414 — confirms :524 is the only test-side reference to :stale_preview)
    - .planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-PATTERNS.md "Edit pattern for the existing test" (:606-616)
  </read_first>
  <behavior>
    - The existing test "confirm_runbook_step executes and rejects stale previews" continues to construct an expired TimelineEntry via Process.put(:mock_entries, [expired_entry]).
    - The single assertion at :524 changes from `{:error, :stale_preview}` to `{:short_circuited, :preview_expired}`.
    - All other lines in the test (:495-541) are unchanged. The test name may stay verbatim or be tightened — executor's call; tightening is preferred (e.g., "...rejects expired previews with :short_circuited") but optional.
  </behavior>
  <action>
    Edit test/parapet/operator_test.exs. Locate the assertion currently at :524 that reads (with whatever whitespace):

      `assert {:error, :stale_preview} = Operator.confirm_runbook_step(incident, :retry, token, payload)`

    Replace it with:

      `assert {:short_circuited, :preview_expired} = Operator.confirm_runbook_step(incident, :retry, token, payload)`

    Verify no other reference to `:stale_preview` remains in the test file via grep. If the test name string at the `test "..." do` line (around :495) contains "stale", consider tightening to reflect the new vocabulary (optional — executor discretion).

    Do NOT change any other assertions or setup blocks in this file. Do NOT add new tests here (those live in the new file in Task 2).
  </action>
  <verify>
    <automated>mix test test/parapet/operator_test.exs --only describe:"confirm_runbook_step" 2>&1 | tee /tmp/p25-03-t1.log; grep -E "0 failures|[0-9]+ tests, 0 failures" /tmp/p25-03-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nc '{:error, :stale_preview}' test/parapet/operator_test.exs` returns 0.
    - `grep -nc '{:short_circuited, :preview_expired}' test/parapet/operator_test.exs` returns >= 1.
    - `mix test test/parapet/operator_test.exs` exits 0 (all existing operator tests still pass with the updated assertion).
  </acceptance_criteria>
  <done>The legacy :stale_preview test-side reference is gone; the corresponding test passes with the new return shape.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create test/parapet/operator/preview_lifecycle_test.exs covering :preview_expired (isolated) and :target_refs_drift</name>
  <files>test/parapet/operator/preview_lifecycle_test.exs</files>
  <read_first>
    - test/parapet/operator_test.exs full file (DummyRepo at :8-20, setup block ~:455, the existing :stale_preview test at :495-541 — the structural twin)
    - lib/parapet/operator.ex post-plan-01 (compute_preview/3 with target_refs_hash, find_recent_preview/3 surfacing the hash, the drift gate in confirm_runbook_step/4)
    - .planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-PATTERNS.md "preview_lifecycle_test.exs" section (:541-637 — including the new target_refs_drift test pattern)
    - .planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-RESEARCH.md "Pitfall 5" (:382-389 — atom-vs-string round-trip canonicalization)
  </read_first>
  <behavior>
    - The new file uses `use ExUnit.Case, async: false` and aliases `Parapet.Operator`, `Parapet.Operator.ActionPayload`, `Parapet.Spine.{Incident, TimelineEntry}` — mirrors test/parapet/operator_test.exs:1-6 header.
    - Setup block installs the same DummyRepo via Application.put_env (or imports/uses the shared setup helper from operator_test.exs if one is exported — executor checks).
    - Test 1 ("confirm_runbook_step returns :short_circuited with :preview_expired for expired tokens"): clones the structural pattern at operator_test.exs:495-525. Builds a preview via Operator.preview_runbook_step/3, mutates the stored payload to set expires_at into the past, Process.put(:mock_entries, [entry]), asserts `{:short_circuited, :preview_expired} = Operator.confirm_runbook_step(...)`. (Acceptable duplication with the test from Task 1 — this file is the canonical lifecycle home; the task-1 update preserves the existing test as a regression marker.)
    - Test 2 ("confirm_runbook_step returns :short_circuited with :target_refs_drift when stored target_refs no longer match recorded hash"): builds a preview, then mutates the stored payload's "target_refs" key to a different value WITHOUT recomputing the hash (so the stored hash reflects the original target_refs and the stored target_refs now reflects the tampered value — when confirm_runbook_step recomputes the hash over the tampered target_refs, it differs from the stored hash). Process.put the tampered entry. Assert `{:short_circuited, :target_refs_drift} = Operator.confirm_runbook_step(...)`.
    - Test 3 ("nil target_refs_hash skips the drift gate — legacy preview compat"): builds a preview, then mutates the stored payload to REMOVE the "target_refs_hash" key (simulating a pre-Phase-25 stored preview). Process.put the entry. Assert the confirm path proceeds normally (does NOT short-circuit on :target_refs_drift). The exact non-error return depends on what capability.execute does in this test harness; assert at minimum that the return is NOT `{:short_circuited, :target_refs_drift}` (use `refute match?({:short_circuited, :target_refs_drift}, ...)` or assert any of `{:ok, _}`, `{:error, _}`, `{:short_circuited, other}`). RESEARCH Open Question #2 backward-compat guarantee.
    - Optional Test 4 ("target_refs canonicalization is stable across atom-vs-string round-trip"): builds a preview where the host capability returns `target_refs: [:item_a, :item_b]` (atoms), then verifies that confirming against the jsonb-rehydrated stored preview (target_refs as strings `["item_a", "item_b"]`) does NOT short-circuit on drift. The canonicalization (`Enum.map(&to_string/1) |> Enum.sort`) must produce the same hash for atoms in-memory and strings post-rehydrate. (Pitfall 5 regression guard.)
  </behavior>
  <action>
    Create the directory `test/parapet/operator/` if it does not exist (note: `test/parapet/operator/action_payload_test.exs`, `queue_pagination_test.exs`, and `workbench_contract_test.exs` already exist there per `ls`, so the directory is present).

    Create test/parapet/operator/preview_lifecycle_test.exs as a new file. Module name: `Parapet.Operator.PreviewLifecycleTest`. Header:

      defmodule Parapet.Operator.PreviewLifecycleTest do
        use ExUnit.Case, async: false
        alias Parapet.Operator
        alias Parapet.Operator.ActionPayload
        alias Parapet.Spine.{Incident, TimelineEntry}
        # plus any aliases the DummyRepo setup requires

    Build setup blocks that mirror test/parapet/operator_test.exs:1-80 for DummyRepo wiring + Capabilities reset. Inspect the existing setup block in operator_test.exs and replicate or factor as fits.

    Write the four tests described in <behavior>. Use Process.put(:mock_entries, [entry]) to inject preview state. For Test 2, the simplest tamper is: capture the result of `preview_runbook_step` (which writes the hash), then build the entry with `Map.put(preview, "target_refs", ["tampered-ref"])` BEFORE Process.put — the stored target_refs are now ["tampered-ref"] but the stored target_refs_hash still encodes the original target_refs list.

    For Test 3, the tamper is `Map.delete(preview, "target_refs_hash")` — simulates a pre-Phase-25 stored preview that has no hash field.

    For Test 4, use a capability whose preview/2 returns `{:ok, %{"target_refs" => ["item_a", "item_b"]}}` (strings, NOT atoms — once the host_data goes through `for {k, v} <- host_data, into: %{}, do: {to_string(k), v}` at operator.ex:771 it stringifies KEYS but not VALUES, so atom target_refs would remain atoms in-memory and become strings only on the jsonb round-trip). Compute the expected hash both ways using `target_refs_hash([:item_a, :item_b])` and `target_refs_hash(["item_a", "item_b"])` (you can't call the private function directly — but you can verify the gate doesn't short-circuit by storing a stringified-target_refs entry with the hash from atom-input and asserting the confirm path does NOT short-circuit on drift). If this gets too convoluted, downgrade Test 4 to a pending TODO with `@tag :skip` and a comment pointing back to Pitfall 5 — executor's discretion.

    Tests 1, 2, 3 are required. Test 4 is recommended but may be deferred.
  </action>
  <verify>
    <automated>mix test test/parapet/operator/preview_lifecycle_test.exs 2>&1 | tee /tmp/p25-03-t2.log; grep -E "[0-9]+ tests, 0 failures" /tmp/p25-03-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - `test/parapet/operator/preview_lifecycle_test.exs` exists.
    - The file contains `use ExUnit.Case, async: false`.
    - `grep -c ':target_refs_drift' test/parapet/operator/preview_lifecycle_test.exs` returns >= 1.
    - `grep -c ':preview_expired' test/parapet/operator/preview_lifecycle_test.exs` returns >= 1.
    - `grep -c 'Process.put(:mock_entries' test/parapet/operator/preview_lifecycle_test.exs` returns >= 2 (Tests 1, 2, 3 all use the mock pattern).
    - At least three `test "..."` declarations are present.
    - `mix test test/parapet/operator/preview_lifecycle_test.exs` exits 0 (zero failures).
    - The :target_refs_drift test fails IF plan 25-01's drift gate is not in place — confirms the test is exercising the new code path (verify by reading the test logic, not by running against a broken state).
  </acceptance_criteria>
  <done>preview_lifecycle_test.exs covers the two new short-circuit branches plus the nil-hash legacy-preview backward-compat case; suite passes against the post-plan-01 operator.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create test/parapet/operator/confirm_concurrency_test.exs proving multi-node Confirm race</name>
  <files>test/parapet/operator/confirm_concurrency_test.exs</files>
  <read_first>
    - test/parapet/automation/executor_concurrency_test.exs full file (:1-146 — THE template; clone-and-substitute)
    - test/parapet/automation/claim_service_test.exs (:1-50 — second precedent for the rendezvous pattern)
    - test/support/concurrency_case.ex full file (use macro, unboxed_run, ConcurrencyRepo)
    - test/support/concurrency_bootstrap.ex full file (schema DDL, ConcurrencyBootstrap.reset!/0)
    - lib/parapet/operator.ex (post-plan-01)
    - lib/parapet/spine/action_claim.ex (claim schema: action_kind, action_key, status, idempotency_key)
    - .planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-CONTEXT.md D-13, D-14 (test design)
    - .planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-RESEARCH.md "Example 5: Multi-node Concurrency Test Skeleton" (:553-660 — full reference implementation)
    - .planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-RESEARCH.md "Pitfall 4" (:367-379 — Capabilities Agent reset)
    - .planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-PATTERNS.md "confirm_concurrency_test.exs" section (:377-538 — including the inline runbook + capability registration)
  </read_first>
  <behavior>
    - The new file uses `use Parapet.TestSupport.ConcurrencyCase, async: false` (required because @tag :unboxed runs outside SQL Sandbox).
    - Module name: `Parapet.Operator.ConfirmConcurrencyTest`.
    - Defines an inline `ConcurrencyRunbook` module with a `:op_step` step typed `:mitigation` and capability `:retry_async_item` and target_kind `"async_item"`.
    - Inside `unboxed_run`: calls `ConcurrencyBootstrap.reset!()`, then `Agent.update(Parapet.Capabilities, fn _ -> %{recovery: %{}} end)` (Pitfall 4), then `Parapet.Capabilities.register_recovery(:retry_async_item, name: ..., target_kind: "async_item", preview: ..., execute: ...)`. The execute callback sends `{:executed, node()}` to the test pid (stashed via `Application.put_env(:parapet, :operator_test_pid, self())`) and sleeps ~75ms to make the race observable.
    - Inserts an Incident via `ConcurrencyRepo.insert/1` with runbook_data %{"module" => to_string(ConcurrencyRunbook)}.
    - Calls `Operator.preview_runbook_step(incident, :op_step, preview_payload)` to obtain a real preview_token; returns `{incident, preview_token}` from unboxed_run.
    - Spawns two Task.async-wrapped contenders that each `send(parent, {:ready, self()})`, then `receive :go ->` invoke `Operator.confirm_runbook_step(incident, :op_step, preview_token, payload)`. Each contender builds its own ActionPayload with `action_type: :execute_mitigation` and `idempotency_key: "operator_confirm_#{incident.id}_op_step"` (the same key — that's what makes them race for the SAME claim).
    - After `assert_receive {:ready, _} x2`, broadcast `:go` to both Task pids.
    - Collect results via `Task.await/2`. Assertions:
        - `Enum.count(results, &match?({:ok, _}, &1)) == 1`
        - `Enum.count(results, &match?({:conflicted, _}, &1)) == 1`
        - `assert_receive {:executed, _node}, 1_000` (winner ran)
        - `refute_receive {:executed, _node}, 200` (loser did NOT run)
    - The conflicted claim_id is captured; another unboxed_run fetches the ActionClaim row via `ConcurrencyRepo.get(ActionClaim, conflicted_claim_id)` and asserts:
        - `claim.status == "executed"` (the winner finished and marked itself)
        - `claim.action_kind == "operator"`
        - `claim.action_key == "op_step"`
    - on_exit cleans up the Application env keys.
  </behavior>
  <action>
    Create test/parapet/operator/confirm_concurrency_test.exs.

    Use RESEARCH Example 5 (`.planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-RESEARCH.md:553-660`) as the reference implementation. The structural skeleton is verbatim usable; verify each line against the actual `executor_concurrency_test.exs` template before committing — RESEARCH was written from-source but the executor MUST cross-check against the live `test/parapet/automation/executor_concurrency_test.exs` for any signature drift.

    Critical implementation points:
      - `use Parapet.TestSupport.ConcurrencyCase, async: false`
      - Inline `defmodule ConcurrencyRunbook do ... step(:op_step, type: :mitigation, capability: :retry_async_item, target_kind: "async_item") ... end`. The operator path uses Recovery capabilities (registered via `register_recovery`), NOT the Runbook's own `execute_mitigation/2` callback — that callback is for the Oban auto-execution path. So the inline runbook just declares the step; the actual execute is wired by `register_recovery`.
      - `@tag :unboxed` on the test function — required so default `mix test` excludes it; invoke with `--include unboxed`.
      - Inside the first `unboxed_run`: ConcurrencyBootstrap.reset! → Agent.update(Parapet.Capabilities, fn _ -> %{recovery: %{}} end) → Parapet.Capabilities.register_recovery(...). The Pitfall-4 Agent reset is REQUIRED — ConcurrencyCase only resets DB tables, NOT the Capabilities Agent.
      - The preview must be a REAL one (not synthesized): `{:ok, %{preview: preview}} = Operator.preview_runbook_step(incident, :op_step, preview_payload)` and `preview_token = preview["preview_token"]`. The preview write also populates the new `target_refs_hash` field (delivered by plan 25-01) — the test exercises the full happy path.
      - Each contender's ActionPayload uses the SAME idempotency_key: `"operator_confirm_#{incident.id}_op_step"`. This is what makes them race for the SAME (incident_id, action_kind, action_key) unique-constraint row. Different idempotency_keys would not collide.
      - The execute callback inside `register_recovery` should send `{:executed, node()}` to the test pid and sleep ~75ms (matches the Executor concurrency test's rhythm).
      - After Task.await, capture the conflicted claim_id via pattern match: `[{:conflicted, conflicted_claim_id}] = Enum.filter(results, &match?({:conflicted, _}, &1))`. Then run a fresh `unboxed_run` to fetch the ActionClaim row and assert action_kind/action_key/status.
      - `on_exit` cleanup: Application.delete_env(:parapet, :automation), Application.delete_env(:parapet, :operator_test_pid), Application.delete_env(:parapet, :repo).
      - Set up Application.put_env(:parapet, :automation, max_executions: 3, within: 3600) at the top of the test to permit execution under the automation gate (matches executor_concurrency_test.exs:30).

    Run `mix test test/parapet/operator/confirm_concurrency_test.exs --include unboxed` to verify. The test should pass against the post-plan-01 operator.ex.
  </action>
  <verify>
    <automated>mix test test/parapet/operator/confirm_concurrency_test.exs --include unboxed 2>&1 | tee /tmp/p25-03-t3.log; grep -E "[0-9]+ tests, 0 failures" /tmp/p25-03-t3.log</automated>
  </verify>
  <acceptance_criteria>
    - `test/parapet/operator/confirm_concurrency_test.exs` exists.
    - `grep -c 'use Parapet.TestSupport.ConcurrencyCase' test/parapet/operator/confirm_concurrency_test.exs` returns 1.
    - `grep -c '@tag :unboxed' test/parapet/operator/confirm_concurrency_test.exs` returns >= 1.
    - `grep -c 'Agent.update(Parapet.Capabilities' test/parapet/operator/confirm_concurrency_test.exs` returns >= 1 (Pitfall 4 mitigation present).
    - `grep -c 'Operator.confirm_runbook_step(' test/parapet/operator/confirm_concurrency_test.exs` returns >= 1.
    - `grep -c 'action_kind == "operator"' test/parapet/operator/confirm_concurrency_test.exs` returns >= 1 (the claim-row assertion).
    - `grep -c 'match?({:conflicted, _}' test/parapet/operator/confirm_concurrency_test.exs` returns >= 1.
    - `grep -c 'match?({:ok, _}' test/parapet/operator/confirm_concurrency_test.exs` returns >= 1.
    - `grep -c 'assert_receive {:executed' test/parapet/operator/confirm_concurrency_test.exs` returns >= 1.
    - `grep -c 'refute_receive {:executed' test/parapet/operator/confirm_concurrency_test.exs` returns >= 1.
    - `mix test test/parapet/operator/confirm_concurrency_test.exs --include unboxed` exits 0.
    - The test passes deterministically across at least 3 consecutive runs (executor runs it 3x to check for flakes — `for i in 1 2 3; do mix test test/parapet/operator/confirm_concurrency_test.exs --include unboxed --seed $i || exit 1; done`).
  </acceptance_criteria>
  <done>confirm_concurrency_test.exs proves UI-02 + UI-04: the operator-confirm path is claim-protected, racing operators see exactly one winner + one {:conflicted, _claim_id}, the winner's claim has action_kind="operator" and status="executed".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → production code | Tests exercise `Parapet.Operator.confirm_runbook_step/4` directly; no new trust boundary added — tests are inside the project tree. |
| Multi-node concurrency simulation | The `unboxed_run` + `Task.async` rendezvous simulates multi-node contention from within a single BEAM. Real production multi-node behavior depends on the Postgres unique-constraint (already in place via FND-01) — the test proves the same path that production uses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-T-01 | Tampering | Test ordering causes Parapet.Capabilities Agent state leak across tests | mitigate | Task 3 explicitly resets the Agent inside `unboxed_run` before `register_recovery` (Pitfall 4 mitigation). on_exit cleans up Application env. |
| T-25-T-02 | Repudiation | Flaky concurrency test passes once but fails under load | mitigate | Acceptance criterion runs the test 3x with different seeds; deterministic claim arbitration via Postgres unique constraint should yield identical results across runs. |
| T-25-T-03 | Information Disclosure | Test fixture target_refs containing PII | accept | Test fixtures are synthetic ("item-1", etc.); no real-data exposure. |
| T-25-T-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Phase 25 (and this plan) installs zero new packages; `ex_unit` is BEAM stdlib. |
</threat_model>

<verification>
1. `mix test test/parapet/operator_test.exs` exits 0 (legacy :stale_preview assertion updated, all other tests still pass).
2. `mix test test/parapet/operator/preview_lifecycle_test.exs` exits 0.
3. `mix test test/parapet/operator/confirm_concurrency_test.exs --include unboxed` exits 0.
4. Full suite: `mix test --include unboxed` exits 0.
5. Three consecutive runs of the concurrency test with different seeds all pass (flake check).
6. `grep -nc ':stale_preview' test/` (recursive) returns 0 — no stale references anywhere in the test tree.
</verification>

<success_criteria>
- The existing test/parapet/operator_test.exs:524 assertion is updated to expect `{:short_circuited, :preview_expired}`.
- test/parapet/operator/preview_lifecycle_test.exs covers `:preview_expired`, `:target_refs_drift`, and the nil-hash backward-compat case.
- test/parapet/operator/confirm_concurrency_test.exs proves multi-node claim arbitration for the operator path.
- All three test files exit 0 under `mix test --include unboxed`.
- Concurrency test passes 3x in a row with varying seeds.
</success_criteria>

<output>
Create `.planning/phases/25-wire-confirm-through-claimservice-preview-confirm-ux/25-03-SUMMARY.md` when done.
</output>
