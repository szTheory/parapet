---
phase: 28-demo-seed-ci-lane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/demo_app/lib/demo_app/runbooks/stalled_executor.ex
  - examples/demo_app/lib/demo_app/recovery/retry_async_item.ex
autonomous: true
requirements: [DEMO-05]
must_haves:
  truths:
    - "A compiled runbook module DemoApp.Runbooks.StalledExecutor exists with a :retry_item capability step"
    - "A compiled capability module DemoApp.Recovery.RetryAsyncItem exists implementing the 4 Parapet.Recovery callbacks"
    - "RetryAsyncItem.id/0 returns the allowlisted atom :retry_async_item"
    - "RetryAsyncItem.execute/2 mutates a Parapet.Spine.ActionItem row linked to the incident"
  artifacts:
    - path: "examples/demo_app/lib/demo_app/runbooks/stalled_executor.ex"
      provides: "3-step runbook (guidance -> capability -> guidance); mitigate step declares capability: :retry_async_item, target_kind: :async_item, requires_preview: true"
      contains: "use Parapet.Runbook"
    - path: "examples/demo_app/lib/demo_app/recovery/retry_async_item.ex"
      provides: "Parapet.Recovery capability impl: id/0, label/0, preview/2 (5-field map), execute/2 (ActionItem mutation)"
      contains: "use Parapet.Recovery"
  key_links:
    - from: "examples/demo_app/lib/demo_app/runbooks/stalled_executor.ex"
      to: ":retry_async_item"
      via: "capability: field on the mitigate step"
      pattern: "capability:\\s*:retry_async_item"
    - from: "examples/demo_app/lib/demo_app/recovery/retry_async_item.ex"
      to: "Parapet.Spine.ActionItem"
      via: "Repo.update_all in execute/2"
      pattern: "Parapet\\.Spine\\.ActionItem"
---

<objective>
Author the two compiled demo modules that the seed (Plan 04), boot wiring (Plan 03), and CI scenarios (Plan 05) all depend on: a `use Parapet.Runbook` runbook module and a `use Parapet.Recovery` capability module. These are the load-bearing prerequisites — `extract_module/1` resolves the runbook module by string, and `attach/1` registers the capability module at boot.

Purpose: Wire the demo to actually use the Preview → Confirm recovery loop that already ships in core (Phases 23–27). The recovery primitives exist; the demo lacks a host capability + a compiled runbook for `runbook_data["module"]` to point at.

Output: `examples/demo_app/lib/demo_app/runbooks/stalled_executor.ex` and `examples/demo_app/lib/demo_app/recovery/retry_async_item.ex`, both compiling cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-demo-seed-ci-lane/28-CONTEXT.md
@.planning/phases/28-demo-seed-ci-lane/28-RESEARCH.md
@.planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md

<interfaces>
<!-- Parapet.Recovery behaviour contract (lib/parapet/recovery.ex:30-56) — implement exactly these 4 callbacks -->
@callback id() :: atom()
@callback label() :: String.t()
@callback preview(incident :: any(), step :: any()) :: {:ok, map()} | {:error, term()}
@callback execute(incident :: any(), target_refs :: any()) :: {:ok, map()} | {:error, term()}

<!-- execute/2 is invoked by core as: capability.execute.(incident, preview_entry.target_refs) (operator.ex:820) -->
<!-- preview/2 return map is consumed by compute_preview/3 (operator.ex:1003-1051): keys count, target_refs, preconditions, warnings, summary -->
<!-- Allowlisted capability atoms: lib/parapet/capabilities.ex:14-20 — :retry_async_item is one of them. A non-allowlisted id raises ArgumentError at attach/1. -->
<!-- Parapet.Spine.ActionItem columns: id, title, integration, external_id, kind, state, incident_id -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author DemoApp.Runbooks.StalledExecutor</name>
  <files>examples/demo_app/lib/demo_app/runbooks/stalled_executor.ex</files>
  <read_first>
    - priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex (the source template this module is a rendered near-copy of — the full 36-line shape is reproduced in 28-PATTERNS.md lines 31-68)
    - .planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md (the "stalled_executor.ex" pattern section, including the planner notes on the :retry_item step id and the :retry_async_item capability field)
  </read_first>
  <action>
    Create `DemoApp.Runbooks.StalledExecutor` with `use Parapet.Runbook`, rendered from the `stalled_executor.ex.eex` template with the module prefix resolved to `DemoApp.Runbooks` (the only EEx substitution). Keep the exact 3-step shape: `step(:investigate_logs, ...)` guidance (preview_only), `step(:retry_item, ...)` mitigation declaring `type: :mitigation, kind: :capability, capability: :retry_async_item, target_kind: :async_item, requires_preview: true`, and `step(:verify_recovery, ...)` guidance (preview_only). Include `title(...)` and `description(...)`. Do NOT rename the `:retry_item` step id (per D-01 / 28-PATTERNS.md — all four CI scenarios pass `"retry_item"` as the step_id string). Do NOT change `capability: :retry_async_item` (per D-02 — it must match the frozen allowlist atom exactly; a non-allowlisted id breaks boot at attach/1).
  </action>
  <verify>
    <automated>cd examples/demo_app && mix compile --warnings-as-errors 2>&1 | tail -5; grep -c "capability:.*:retry_async_item" lib/demo_app/runbooks/stalled_executor.ex</automated>
  </verify>
  <acceptance_criteria>
    - File `examples/demo_app/lib/demo_app/runbooks/stalled_executor.ex` exists and contains `use Parapet.Runbook`
    - Contains a step with id `:retry_item` declaring `capability: :retry_async_item` and `target_kind: :async_item` and `requires_preview: true`
    - Contains exactly the three step ids `:investigate_logs`, `:retry_item`, `:verify_recovery`
    - `cd examples/demo_app && mix compile --warnings-as-errors` exits 0
    - `grep "capability:" examples/demo_app/lib/demo_app/runbooks/stalled_executor.ex` shows `:retry_async_item` (never an invented atom like `:restart_worker`)
  </acceptance_criteria>
  <done>The runbook module compiles; the mitigate step references the allowlisted `:retry_async_item` capability with step id `:retry_item`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author DemoApp.Recovery.RetryAsyncItem capability</name>
  <files>examples/demo_app/lib/demo_app/recovery/retry_async_item.ex</files>
  <read_first>
    - lib/parapet/recovery.ex (lines 30-63 — the 4 @callback specs and the `use Parapet.Recovery` `__using__` macro that injects `@behaviour`)
    - .planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md (the "retry_async_item.ex" pattern section: the full production module shape at lines 105-143, plus the planner notes on the 5-field preview map and DemoApp.Repo access)
    - examples/demo_app/lib/demo_app/application.ex (read to confirm DemoApp.Repo is the configured repo this module mutates against)
  </read_first>
  <action>
    Create `DemoApp.Recovery.RetryAsyncItem` with `use Parapet.Recovery` and `@impl Parapet.Recovery` on each callback. Implement: `id/0` returning the atom `:retry_async_item` (per D-02 — the frozen allowlist atom, NOT a new id); `label/0` returning a human string e.g. `"Retry Async Item"`; `preview/2` returning `{:ok, map}` where the map has the documented 5 keys consumed by `compute_preview/3` — `count`, `target_refs` (a list referencing the incident, e.g. `[incident.id]`), `preconditions`, `warnings`, `summary` (exact labels/text are Claude's discretion per CONTEXT, but all 5 keys MUST be present); `execute/2` performing a real demo-DB mutation per D-04 by calling `DemoApp.Repo.update_all` over a `from(a in Parapet.Spine.ActionItem, where: a.incident_id == ^incident.id and a.state == "open", limit: 1)` query setting `state: "retrying"`, then returning `{:ok, %{retried_count: n}}` (or `{:ok, %{retried_count: 0, note: ...}}` when no rows matched). Use `DemoApp.Repo` directly (demo-only code; the ExUnit sandbox shared mode propagates to it within the test PID span — RESEARCH Open Question #2). Do NOT use `Application.get_env(:parapet, :repo)` indirection. The `execute/2` contract is `{:ok, map()} | {:error, term()}`.
  </action>
  <verify>
    <automated>cd examples/demo_app && mix compile --warnings-as-errors 2>&1 | tail -5; grep -c "def id, do: :retry_async_item\|def id(), do: :retry_async_item" lib/demo_app/recovery/retry_async_item.ex</automated>
  </verify>
  <acceptance_criteria>
    - File `examples/demo_app/lib/demo_app/recovery/retry_async_item.ex` exists and contains `use Parapet.Recovery`
    - `examples/demo_app/lib/demo_app/recovery/retry_async_item.ex` contains a `def id` clause returning `:retry_async_item`
    - `preview/2` returns `{:ok, map}` and the map literal contains all of: `count`, `target_refs`, `preconditions`, `warnings`, `summary`
    - `execute/2` references `Parapet.Spine.ActionItem` and calls `DemoApp.Repo.update_all` (mutates demo DB state per D-04)
    - `execute/2` returns an `{:ok, map}` tuple (matches the `{:ok, map()} | {:error, term()}` contract)
    - `cd examples/demo_app && mix compile --warnings-as-errors` exits 0 (the `@impl` annotations confirm all 4 callbacks are present with no behaviour warnings)
  </acceptance_criteria>
  <done>The capability module compiles with all 4 callbacks implemented; `id/0` returns the allowlisted atom; `execute/2` mutates an `ActionItem` and `preview/2` returns the 5-field map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces NO new trust boundary. Both modules are demo-app host code consumed only by the in-process Parapet recovery loop. No new HTTP endpoint, authentication surface, external service call, or network egress is added.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | `RetryAsyncItem.execute/2` DB mutation | accept | `execute/2` only flips a `Parapet.Spine.ActionItem` already scoped by `incident_id` (the where-clause is `a.incident_id == ^incident.id`); it cannot touch rows outside the incident. Demo-only code, no external input reaches it. |
| T-28-02 | Elevation of Privilege | `:retry_async_item` capability id | mitigate | Reuse the frozen-allowlist atom (`capabilities.ex:14-20`); a non-allowlisted id raises `ArgumentError` at `attach/1`, so invalid capability ids cannot register. Enforced by core, asserted by compile + Plan 03 boot. |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages). No legitimacy checkpoint required. |

No high-severity threats. Per RESEARCH Security Domain, no ASVS categories apply: the capability `execute/2` operates only on demo-DB rows already scoped to the incident.
</threat_model>

<verification>
- `cd examples/demo_app && mix compile --warnings-as-errors` exits 0 with both new modules present.
- The runbook module's mitigate step uses `capability: :retry_async_item` (allowlist-safe).
- The capability module's `id/0` returns `:retry_async_item`; `preview/2` returns the 5-field map; `execute/2` mutates a `Parapet.Spine.ActionItem`.
</verification>

<success_criteria>
- Both `DemoApp.Runbooks.StalledExecutor` and `DemoApp.Recovery.RetryAsyncItem` compile cleanly under `--warnings-as-errors`.
- The modules are loadable so that downstream `extract_module/1` (Plan 04 seed) and `attach/1` (Plan 03 boot) resolve them by string/module.
- No core module under `lib/parapet/**` was edited.
</success_criteria>

<output>
Create `.planning/phases/28-demo-seed-ci-lane/28-01-SUMMARY.md` when done.
</output>
