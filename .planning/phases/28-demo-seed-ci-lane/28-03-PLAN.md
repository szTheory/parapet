---
phase: 28-demo-seed-ci-lane
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - examples/demo_app/lib/demo_app/application.ex
autonomous: true
requirements: [DEMO-05, DEMO-06]
must_haves:
  truths:
    - "The demo app boots with Parapet.Capabilities running as a supervision child"
    - "At application boot, DemoApp.Recovery.RetryAsyncItem is registered via Parapet.Recovery.attach/1"
    - "Parapet.Capabilities.get_recovery(:retry_async_item) answers for both mix phx.server and the CI test process"
  artifacts:
    - path: "examples/demo_app/lib/demo_app/application.ex"
      provides: "Parapet.Capabilities child in the supervision tree + boot-time attach([DemoApp.Recovery.RetryAsyncItem])"
      contains: "Parapet.Capabilities"
  key_links:
    - from: "examples/demo_app/lib/demo_app/application.ex"
      to: "DemoApp.Recovery.RetryAsyncItem"
      via: "Parapet.Recovery.attach/1 after Supervisor.start_link/2"
      pattern: "Parapet\\.Recovery\\.attach\\("
    - from: "examples/demo_app/lib/demo_app/application.ex"
      to: "Parapet.Capabilities"
      via: "supervision child listed before attach/1 runs"
      pattern: "Parapet\\.Capabilities"
---

<objective>
Wire `Parapet.Capabilities` into the demo supervision tree and register `DemoApp.Recovery.RetryAsyncItem` at application boot via `Parapet.Recovery.attach/1`. This is the single boot-time registration that serves BOTH `mix phx.server` and every CI test process — the named-singleton agent must answer `get_recovery(:retry_async_item)` before any Preview/Confirm runs, otherwise the operator API returns `{:error, :capability_unwired}`.

Purpose: Without this, the seeded incident's capability step (Plan 04) and the four CI scenarios (Plan 05) both fail — Preview/Confirm cannot resolve the capability. Implements D-09.

Output: Modified `examples/demo_app/lib/demo_app/application.ex`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-demo-seed-ci-lane/28-CONTEXT.md
@.planning/phases/28-demo-seed-ci-lane/28-RESEARCH.md
@.planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md
@.planning/phases/28-demo-seed-ci-lane/28-01-SUMMARY.md

<interfaces>
<!-- Parapet.Capabilities is `use Agent` (capabilities.ex:12) with bare start_link(_opts) registering as __MODULE__ (capabilities.ex:22-23). Pass it bare in `children` (auto child_spec/1), NOT as a tuple. -->
<!-- Parapet.Recovery.attach(modules) returns {:ok, registered_ids} (recovery.ex:87-105). registered_ids is the list of id() atoms that registered. -->
<!-- attach/1 calls register_recovery/2 -> Agent.update(Parapet.Capabilities, ...) synchronously. The agent MUST already be started, or this raises (exit) no process. -->
<!-- DemoApp.Recovery.RetryAsyncItem (from Plan 01) has id/0 => :retry_async_item, which is in the allowlist, so attach/1 succeeds. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Parapet.Capabilities child + boot-time attach to DemoApp.Application</name>
  <files>examples/demo_app/lib/demo_app/application.ex</files>
  <read_first>
    - examples/demo_app/lib/demo_app/application.ex (current `start/2` — the children list and the one-line `Supervisor.start_link(children, opts)`)
    - lib/parapet/capabilities.ex (lines 12-24 — `use Agent`, `start_link/1` registering as `__MODULE__`; confirms it is passed bare in children)
    - lib/parapet/recovery.ex (lines 78-106 — `attach/1` return shape `{:ok, registered}` and that it calls `register_recovery/2` synchronously via Agent.update)
    - .planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md (the "application.ex" pattern section: current `start/2` body and the exact target state, lines 415-454, including the planner notes on ordering)
  </read_first>
  <action>
    Modify `start/2`: (1) add `Parapet.Capabilities` to the `children` list, placed BEFORE `DemoAppWeb.Endpoint` (and before any other child that might trigger recovery). Pass it bare (not as a `{Parapet.Capabilities, opts}` tuple) — it is a `use Agent` named singleton. (2) Capture the supervisor result as `{:ok, sup} = Supervisor.start_link(children, opts)` instead of the bare one-liner. (3) After `start_link/2` returns, call `{:ok, _registered} = Parapet.Recovery.attach([DemoApp.Recovery.RetryAsyncItem])` (inline registration — D-09 leaves inline-vs-dedicated-child to discretion; inline is simpler and runs once at boot). (4) Return `{:ok, sup}`. Ordering is load-bearing per D-09 and RESEARCH Pitfall 2: `Parapet.Capabilities` MUST be a started child before `attach/1` is invoked, or `Agent.update` raises `(exit) no process`. Do NOT register the capability in `seeds.exs` instead (that would leave the CI test process unregistered → `{:error, :capability_unwired}`).
  </action>
  <verify>
    <automated>cd examples/demo_app && mix compile --warnings-as-errors 2>&1 | tail -5; grep -c "Parapet.Capabilities" lib/demo_app/application.ex; grep -c "Parapet.Recovery.attach" lib/demo_app/application.ex</automated>
  </verify>
  <acceptance_criteria>
    - `examples/demo_app/lib/demo_app/application.ex` contains `Parapet.Capabilities` in the `children` list (bare, not a tuple)
    - `examples/demo_app/lib/demo_app/application.ex` contains `Parapet.Recovery.attach([DemoApp.Recovery.RetryAsyncItem])` called AFTER `Supervisor.start_link/2`
    - `start/2` returns `{:ok, sup}` (the captured supervisor pid)
    - `cd examples/demo_app && mix compile --warnings-as-errors` exits 0
    - Boot smoke: `cd examples/demo_app && mix run -e "{:ok, r} = Parapet.Recovery.attach([DemoApp.Recovery.RetryAsyncItem]); IO.inspect(Parapet.Capabilities.get_recovery(:retry_async_item))"` prints a non-nil capability (proves the agent starts and the capability resolves on boot)
  </acceptance_criteria>
  <done>The demo app boots with `Parapet.Capabilities` supervised and `:retry_async_item` registered; `get_recovery(:retry_async_item)` returns the capability for both server and test processes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan wires an in-process supervision child and a boot-time registration call. No HTTP endpoint, auth surface, or external service is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Denial of Service | boot-time `attach/1` ordering | mitigate | `Parapet.Capabilities` is listed in `children` before `attach/1` runs; the acceptance boot-smoke proves the agent is alive before registration, preventing a boot crash. Enforced by ordering (D-09) + verify step. |
| T-28-03-02 | Spoofing | capability registry contents | accept | Only `DemoApp.Recovery.RetryAsyncItem` (a compiled, allowlisted host module) is attached; the allowlist in `capabilities.ex` rejects unknown ids at `attach/1`. Demo-only, single trusted module. |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase. No legitimacy checkpoint required. |

No high-severity threats. No ASVS categories apply (no new HTTP/auth/external surfaces).
</threat_model>

<verification>
- `cd examples/demo_app && mix compile --warnings-as-errors` exits 0.
- `Parapet.Capabilities` appears in `children`; `Parapet.Recovery.attach/1` is called after `Supervisor.start_link/2`.
- Boot smoke resolves `:retry_async_item` from the registry.
</verification>

<success_criteria>
- The demo boots with the capabilities agent supervised and the capability registered at boot (D-09).
- One registration serves both `mix phx.server` and the CI test process.
- No core module under `lib/parapet/**` was edited.
</success_criteria>

<output>
Create `.planning/phases/28-demo-seed-ci-lane/28-03-SUMMARY.md` when done.
</output>
