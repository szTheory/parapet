---
phase: 28-demo-seed-ci-lane
plan: 05
type: execute
wave: 3
depends_on: ["28-01", "28-03"]
files_modified:
  - examples/demo_app/test/demo_app/recovery_loop_test.exs
autonomous: true
requirements: [DEMO-06]
must_haves:
  truths:
    - "Happy-path Confirm returns {:ok, _} and writes a recovery_confirmed TimelineEntry + a ToolAudit row"
    - "An expired preview token forces re-Preview: Confirm returns {:short_circuited, :preview_expired}"
    - "An incident resolved between Preview and Confirm short-circuits: Confirm returns {:short_circuited, :incident_resolved}"
    - "A claim-conflict between two simulated operators resolves cleanly: first Confirm {:ok, _}, second {:conflicted, _}"
    - "All four scenarios are :smoke-tagged so mix test --only smoke runs them, and any failure breaks the demo CI job wired into release_gate"
  artifacts:
    - path: "examples/demo_app/test/demo_app/recovery_loop_test.exs"
      provides: "@moduletag :smoke ExUnit module with 4 scenario tests driven through the Parapet.Operator API"
      contains: "@moduletag :smoke"
  key_links:
    - from: "examples/demo_app/test/demo_app/recovery_loop_test.exs"
      to: "Parapet.Operator.confirm_runbook_step/4"
      via: "each scenario previews then confirms through the operator API (no LiveViewTest, no Wallaby)"
      pattern: "Parapet.Operator.confirm_runbook_step"
    - from: ".github/workflows/ci.yml"
      to: "examples/demo_app/test/demo_app/recovery_loop_test.exs"
      via: "demo job 'mix test --only smoke' picks up :smoke-tagged scenarios; release_gate needs: [lint, test, demo]"
      pattern: "mix test --only smoke"
---

<objective>
Add the four headless `:smoke`-tagged CI scenarios that contract-test the recovery loop (DEMO-06 / success criteria #2 and #3): happy-path Confirm, expired-preview retry, resolved-mid-flow short-circuit, and sequential claim-conflict. All four are driven purely through the `Parapet.Operator` API (no `Phoenix.LiveViewTest`, no Wallaby — D-05). The plan also verifies the existing CI `demo` job picks the tests up and `release_gate` gates on them (D-07 — no workflow edits needed).

Purpose: Prove the Preview → Confirm loop end-to-end and lock it as a contract-tested release gate. Implements D-05, D-06, D-07, D-08.

Output: `examples/demo_app/test/demo_app/recovery_loop_test.exs` with four scenarios; confirmation that `.github/workflows/ci.yml` requires no edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-demo-seed-ci-lane/28-CONTEXT.md
@.planning/phases/28-demo-seed-ci-lane/28-RESEARCH.md
@.planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md
@.planning/phases/28-demo-seed-ci-lane/28-01-SUMMARY.md
@.planning/phases/28-demo-seed-ci-lane/28-03-SUMMARY.md

<interfaces>
<!-- preview_runbook_step/3 (operator.ex:670) returns {:ok, %{... preview: full_preview_map}}; read token via result.preview["preview_token"]. WorkbenchContract.find_active_preview/1 is PRIVATE and filters expired tokens — do NOT use it. -->
<!-- confirm_runbook_step/4 (operator.ex:749) signature: (incident, step_id, preview_token, %ActionPayload{}). Returns one of:
       {:ok, _} | {:short_circuited, reason} | {:conflicted, claim_id} | {:error, reason}. -->
<!-- step_id for all scenarios is the string "retry_item" (the runbook's mitigate step id from Plan 01). -->
<!-- action_key = to_string(step_id_atom) = "retry_item" (operator.ex:784). Conflict uniqueness is (incident_id, action_kind="operator", action_key="retry_item") — both confirms use the SAME step_id so the second conflicts. -->
<!-- Expiry gate (operator.ex:767-769) reads expires_at from the recovery_preview TimelineEntry payload via find_recent_preview/3 (operator.ex:1053-1094); it parses both %DateTime{} and ISO 8601 strings. There is NO `now:` opt. -->
<!-- Resolved gate (operator.ex:790, 996-1000): allowed_states ["open","investigating"]; resolving the incident yields {:short_circuited, :incident_resolved}. -->
<!-- Happy-path success arm (operator.ex:857-885) writes a recovery_confirmed TimelineEntry and a ToolAudit row with tool_name "operator_confirm_recovery" (confirmed lib/parapet/operator.ex:869,903; Parapet.Spine.ToolAudit has field tool_name). -->
<!-- Parapet.Evidence.create_incident/1: partial unique index on correlation_key where state != 'resolved' — use a unique correlation_key per test (System.unique_integer([:positive])) or omit it. -->
<!-- ActionPayload struct (lib/parapet/operator/action_payload.ex): actor, reason, correlation_id, action_type, idempotency_key. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author recovery_loop_test.exs with the happy-path + setup, and verify it runs green</name>
  <files>examples/demo_app/test/demo_app/recovery_loop_test.exs</files>
  <read_first>
    - examples/demo_app/test/demo_app/operator_smoke_test.exs (the `@moduletag :smoke` + `use DemoAppWeb.ConnCase` + self-contained incident-creation pattern this file mirrors)
    - examples/demo_app/test/support/conn_case.ex (the sandbox checkout + shared-mode setup that runs automatically per test)
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex (lines 103-166 — the literal %ActionPayload{} shapes the headless test mirrors)
    - .planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md (the "recovery_loop_test.exs" pattern section: full module skeleton lines 220-396, plus the planner notes on token read-back, unique correlation_key, idempotency_key, and the no-Task.async rule)
    - lib/parapet/operator.ex (lines 670-728 for preview return shape; 857-885 for the success-arm TimelineEntry + ToolAudit writes)
  </read_first>
  <action>
    Create `DemoApp.RecoveryLoopTest` with `use DemoAppWeb.ConnCase` and `@moduletag :smoke` (D-05). Add a `setup` block that, in the sandbox, creates a fresh capability-backed incident via `Parapet.Evidence.create_incident/1` with `state: "open"`, a UNIQUE `correlation_key` using `System.unique_integer([:positive])` (RESEARCH Pitfall 4), and `runbook_data: %{"module" => to_string(DemoApp.Runbooks.StalledExecutor)}`, AND inserts a `Parapet.Spine.ActionItem` row (kind "async_item", state "open", linked by `incident_id`) for `execute/2` to mutate. Return `%{incident: incident}`. Do NOT add a separate sandbox `setup` — `use DemoAppWeb.ConnCase` supplies it. Do NOT depend on seeded data (the sandbox is isolated from the seeds DB — RESEARCH Pitfall 3).
    Then implement Scenario 1 (happy path): build a preview `%ActionPayload{action_type: :preview_mitigation}`, call `Parapet.Operator.preview_runbook_step(incident, "retry_item", preview_payload)`, read the token via `preview_result.preview["preview_token"]` (NOT via the private `find_active_preview/1`), build a confirm `%ActionPayload{action_type: :execute_mitigation, idempotency_key: Ecto.UUID.generate()}`, call `confirm_runbook_step(incident, "retry_item", token, confirm_payload)` and assert `{:ok, _}`. Then assert via `DemoApp.Repo.exists?` that (a) a `Parapet.Spine.TimelineEntry` with `type == "recovery_confirmed"` exists for the incident, and (b) a `Parapet.Spine.ToolAudit` with `tool_name == "operator_confirm_recovery"` exists.
  </action>
  <verify>
    <automated>cd examples/demo_app && mix test --only smoke test/demo_app/recovery_loop_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `examples/demo_app/test/demo_app/recovery_loop_test.exs` contains `use DemoAppWeb.ConnCase` and `@moduletag :smoke`
    - The `setup` block creates an incident with `runbook_data: %{"module" => to_string(DemoApp.Runbooks.StalledExecutor)}` and a `correlation_key` containing `System.unique_integer`
    - The happy-path test reads the token from `preview_result.preview["preview_token"]` (file does NOT reference `find_active_preview` or `WorkbenchContract.find_active_preview`)
    - The happy-path test asserts `{:ok, _}` from `confirm_runbook_step`, a `recovery_confirmed` TimelineEntry, and a `tool_name == "operator_confirm_recovery"` ToolAudit
    - `cd examples/demo_app && mix test --only smoke test/demo_app/recovery_loop_test.exs` exits 0 with the happy-path test passing
  </acceptance_criteria>
  <done>The smoke test module exists with a self-contained setup and a passing happy-path scenario asserting `{:ok, _}` + TimelineEntry + ToolAudit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the three short-circuit/conflict scenarios and confirm CI wiring</name>
  <files>examples/demo_app/test/demo_app/recovery_loop_test.exs</files>
  <read_first>
    - examples/demo_app/test/demo_app/recovery_loop_test.exs (the file produced in Task 1 — add three tests to it)
    - .planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md (Scenarios 2-4 in the recovery_loop_test.exs skeleton, lines 289-396, including the expiry jsonb-merge update, the resolved-state update, and the sequential double-confirm)
    - .planning/phases/28-demo-seed-ci-lane/28-RESEARCH.md (Pattern 2 expired-preview injection lines 193-213; Pattern 3 sequential claim-conflict lines 215-245; Pitfall 5 idempotency_key; Pitfall 6 find_active_preview filters expired)
    - .github/workflows/ci.yml (lines 94-142 — the `demo` job steps and `release_gate needs: [lint, test, demo]`; READ-ONLY — verify, do not edit)
  </read_first>
  <action>
    Add three more tests to `recovery_loop_test.exs`:
    Scenario 2 (expired preview): preview, read token, then age the preview by updating the `recovery_preview` TimelineEntry payload — `DemoApp.Repo.update_all(from(t in Parapet.Spine.TimelineEntry, where: t.incident_id == ^incident.id and t.type == "recovery_preview"), set: [payload: fragment("payload || ?::jsonb", ^%{"expires_at" => past_iso8601})])` where `past_iso8601 = DateTime.utc_now() |> DateTime.add(-600, :second) |> DateTime.to_iso8601()` (D-06 — direct payload mutation; there is no `now:` opt). Assert `{:short_circuited, :preview_expired}` from confirm.
    Scenario 3 (resolved mid-flow): preview, read token, flip the incident to resolved — `DemoApp.Repo.update_all(from(i in Parapet.Spine.Incident, where: i.id == ^incident.id), set: [state: "resolved"])` — then confirm and assert `{:short_circuited, :incident_resolved}`.
    Scenario 4 (sequential claim-conflict, D-08): preview once, read token, build TWO confirm `%ActionPayload{}` with DISTINCT `idempotency_key` values (RESEARCH Pitfall 5) but the SAME step_id `"retry_item"` (so `action_key` collides). Call `confirm_runbook_step(incident, "retry_item", token, payload_1)` then `..., payload_2)` SEQUENTIALLY (NOT via Task.async — spawned tasks lose the shared sandbox connection, RESEARCH Pitfall 6/anti-pattern). Assert the first is `{:ok, _}` and the second is `{:conflicted, _claim_id}`.
    After the scenarios pass, verify CI continuity (D-07): confirm `.github/workflows/ci.yml` `demo` job runs `mix test --only smoke` and `release_gate needs: [lint, test, demo]` — record in the SUMMARY that NO workflow edits are needed (the `:smoke` tag means `mix test --only smoke` picks the new file up automatically). Do NOT edit `ci.yml`.
  </action>
  <verify>
    <automated>cd examples/demo_app && mix test --only smoke test/demo_app/recovery_loop_test.exs 2>&1 | tail -20; grep -c "mix test --only smoke" ../../.github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - The expired-preview test ages `payload["expires_at"]` via an `update_all` jsonb merge and asserts `{:short_circuited, :preview_expired}`
    - The resolved test flips the incident to `state: "resolved"` and asserts `{:short_circuited, :incident_resolved}`
    - The conflict test calls `confirm_runbook_step` twice SEQUENTIALLY with the same step_id `"retry_item"` and DISTINCT `idempotency_key`s, asserting first `{:ok, _}` and second `{:conflicted, _}`
    - The file does NOT use `Task.async` for the conflict scenario
    - `cd examples/demo_app && mix test --only smoke test/demo_app/recovery_loop_test.exs` exits 0 with all four scenarios passing
    - `.github/workflows/ci.yml` is unchanged (`git diff --stat -- .github/workflows/ci.yml` is empty) and still contains `mix test --only smoke` and `release_gate` with `needs: [lint, test, demo]`
  </acceptance_criteria>
  <done>All four scenarios pass under `mix test --only smoke`; CI wiring confirmed to require no edits (the `:smoke` tag + existing `release_gate` gate cover DEMO-06 success criteria #2 and #3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. The four scenarios are in-process ExUnit tests exercising the existing `Parapet.Operator` API against the Ecto sandbox. No HTTP endpoint, auth surface, or external service is introduced. The CI `demo` job is read-only verification.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | Repudiation | recovery-loop contract coverage | mitigate | The four scenarios assert the full audit trail (recovery_confirmed TimelineEntry + ToolAudit on success; deterministic short-circuit/conflict tuples on the failure arms), and the demo job gates `release_gate` — a regression that drops the audit writes or breaks the conflict guard fails CI. Specific mitigation: assert `tool_name == "operator_confirm_recovery"` and the exact `:short_circuited`/`:conflicted` tuples. |
| T-28-05-02 | Tampering | claim-conflict guard | mitigate | The conflict scenario contract-tests the `(incident_id, action_kind, action_key)` unique constraint so a second operator cannot double-execute the same action — a deterministic test that the `on_conflict: :nothing` guard holds. |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages). No legitimacy checkpoint required. |

No high-severity threats. Per RESEARCH Security Domain, no ASVS categories apply — the scenarios add no HTTP/auth/external surfaces; `execute/2` operates only on demo-DB rows already scoped to the incident.
</threat_model>

<verification>
- `cd examples/demo_app && mix test --only smoke test/demo_app/recovery_loop_test.exs` exits 0 with all four scenarios passing.
- Each scenario drives the `Parapet.Operator` API only (no `Phoenix.LiveViewTest`, no Wallaby, no `Task.async`).
- `.github/workflows/ci.yml` is unedited and still wires `demo` into `release_gate`.
</verification>

<success_criteria>
- Four `:smoke`-tagged scenarios assert `{:ok,_}`+TimelineEntry+ToolAudit, `{:short_circuited, :preview_expired}`, `{:short_circuited, :incident_resolved}`, and `{:ok,_}`/`{:conflicted,_}` (DEMO-06, success criterion #2).
- `mix test --only smoke` picks the file up; any failure breaks the `demo` CI job which `release_gate` requires (success criterion #3) — confirmed with no `ci.yml` edits (D-07).
- No core module under `lib/parapet/**` was edited.
</success_criteria>

<output>
Create `.planning/phases/28-demo-seed-ci-lane/28-05-SUMMARY.md` when done.
</output>
