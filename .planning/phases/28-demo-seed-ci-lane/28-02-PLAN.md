---
phase: 28-demo-seed-ci-lane
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/demo_app/mix.exs
autonomous: true
requirements: [DEMO-05]
must_haves:
  truths:
    - "mix demo.reset drops, recreates, migrates, and re-seeds the demo DB in one command"
    - "The demo seed is replayable without manual database cleanup"
  artifacts:
    - path: "examples/demo_app/mix.exs"
      provides: "demo.reset alias = [ecto.drop, ecto.create, ecto.migrate, run priv/repo/seeds.exs]"
      contains: "demo.reset"
  key_links:
    - from: "examples/demo_app/mix.exs"
      to: "priv/repo/seeds.exs"
      via: "the demo.reset alias 'run priv/repo/seeds.exs' step"
      pattern: "run priv/repo/seeds.exs"
---

<objective>
Add the `mix demo.reset` alias to the demo app so the recovery seed is replayable (success criterion #4) without manual DB cleanup. Drop+recreate makes replayability free — the seed stays always-insert, relying on the drop to clear stale state (the `correlation_key` partial unique index would otherwise reject a naive re-seed).

Purpose: Adopters can run the recovery smoke loop repeatedly. Implements D-10.

Output: A new `"demo.reset"` alias in `examples/demo_app/mix.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-demo-seed-ci-lane/28-CONTEXT.md
@.planning/phases/28-demo-seed-ci-lane/28-RESEARCH.md
@.planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the demo.reset alias to mix.exs</name>
  <files>examples/demo_app/mix.exs</files>
  <read_first>
    - examples/demo_app/mix.exs (the `aliases/0` private function — the `setup` alias chain it mirrors lives there at ~line 51)
    - .planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md (the "mix.exs" pattern section: current `aliases/0` body and the exact target state after adding `demo.reset`, lines 528-548)
  </read_first>
  <action>
    In `aliases/0`, add a new entry `"demo.reset": ["ecto.drop", "ecto.create", "ecto.migrate", "run priv/repo/seeds.exs"]` per D-10. Place it directly after the existing `setup:` entry (mirror the `setup` chain but lead with `ecto.drop` instead of `deps.get`). The `"run priv/repo/seeds.exs"` string must be byte-identical to the one in the `setup` alias. Do NOT modify the `setup`, `assets.build`, or `assets.deploy` aliases. Do NOT make seeds idempotent — replayability comes from the drop, and seeds stay always-insert (D-10).
  </action>
  <verify>
    <automated>cd examples/demo_app && mix help demo.reset 2>&1 | head -3; grep -c "demo.reset" mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - `examples/demo_app/mix.exs` contains a `"demo.reset":` alias whose value list is exactly `["ecto.drop", "ecto.create", "ecto.migrate", "run priv/repo/seeds.exs"]`
    - The `setup:` alias is unchanged (still leads with `deps.get`)
    - `cd examples/demo_app && mix help demo.reset` resolves the alias (does not error with "unknown task")
    - `grep "demo.reset" examples/demo_app/mix.exs` shows the `ecto.drop` step first in the chain
  </acceptance_criteria>
  <done>The `demo.reset` alias exists and is recognized by mix; it drops, recreates, migrates, and re-seeds in sequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan adds a mix alias that runs only local Ecto tasks plus the existing seeds script against the developer's own demo DB. No network, no external input, no new endpoint.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Denial of Service | `demo.reset` drops the DB | accept | `ecto.drop` targets the demo's configured DB only (per `config/config.exs`); this is a local developer/CI convenience command, not a production surface. Documented intent (D-10). |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase. No legitimacy checkpoint required. |

No high-severity threats. No ASVS categories apply (no HTTP/auth/external surfaces).
</threat_model>

<verification>
- `cd examples/demo_app && mix help demo.reset` resolves the alias.
- The alias chain is `ecto.drop -> ecto.create -> ecto.migrate -> run priv/repo/seeds.exs`.
- (Full replay smoke — `cd examples/demo_app && mix demo.reset` exits 0 — runs against a provisioned DB; verified at the phase gate alongside the seed, since the seed block lands in Plan 04.)
</verification>

<success_criteria>
- `mix demo.reset` is a recognized alias with the D-10 chain.
- The `setup` alias and other aliases are untouched.
- No core module under `lib/parapet/**` was edited.
</success_criteria>

<output>
Create `.planning/phases/28-demo-seed-ci-lane/28-02-SUMMARY.md` when done.
</output>
