---
phase: 28-demo-seed-ci-lane
plan: 04
type: execute
wave: 3
depends_on: ["28-01"]
files_modified:
  - examples/demo_app/priv/repo/seeds.exs
autonomous: true
requirements: [DEMO-05]
must_haves:
  truths:
    - "A fresh clone seeded via mix setup surfaces an open incident with a Preview-able + Confirm-able runbook step"
    - "The seeded recovery incident references the compiled runbook module via runbook_data['module']"
    - "mix run priv/repo/seeds.exs exits 0 with the new capability-backed incident inserted"
  artifacts:
    - path: "examples/demo_app/priv/repo/seeds.exs"
      provides: "4th incident block (open, capability-backed) with runbook_data['module'] => to_string(DemoApp.Runbooks.StalledExecutor); updated trailing IO.puts count"
      contains: "DemoApp.Runbooks.StalledExecutor"
  key_links:
    - from: "examples/demo_app/priv/repo/seeds.exs"
      to: "DemoApp.Runbooks.StalledExecutor"
      via: "runbook_data['module'] string key resolved by extract_module/1"
      pattern: "\"module\"\\s*=>\\s*to_string\\(DemoApp.Runbooks.StalledExecutor\\)"
---

<objective>
Add the capability-backed seeded incident so a fresh clone (`mix setup`) surfaces an open incident with a Preview-able + Confirm-able runbook step (success criterion #1). The single load-bearing detail: the incident's `runbook_data` MUST set the `"module"` string key pointing at `DemoApp.Runbooks.StalledExecutor` — inline `"steps"` is display-only and breaks Preview/Confirm with `{:error, :missing_runbook}`.

Purpose: This seeded incident is what an adopter clicks through (Preview → Confirm) in the browser — the worked reference Phase 29's docs will point at. Implements D-03 and D-11.

Output: A new 4th incident block in `examples/demo_app/priv/repo/seeds.exs` plus an updated trailing count line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-demo-seed-ci-lane/28-CONTEXT.md
@.planning/phases/28-demo-seed-ci-lane/28-RESEARCH.md
@.planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md
@.planning/phases/28-demo-seed-ci-lane/28-01-SUMMARY.md

<interfaces>
<!-- extract_module/1 (operator.ex:1097-1113) calls String.to_existing_atom on runbook_data["module"]; requires a "module" (or :module) binary, else {:error, :missing_runbook}. -->
<!-- to_string(DemoApp.Runbooks.StalledExecutor) produces "Elixir.DemoApp.Runbooks.StalledExecutor"; the module atom exists at runtime because `mix run` loads the full app. -->
<!-- Parapet.Evidence.create_incident/1 enforces a partial unique index on correlation_key where state != 'resolved'. Use a distinct correlation_key for this new open incident. -->
<!-- seeds.exs line 3 already sets Application.put_env(:parapet, :repo, DemoApp.Repo) — do NOT add a second call. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the capability-backed incident seed block</name>
  <files>examples/demo_app/priv/repo/seeds.exs</files>
  <read_first>
    - examples/demo_app/priv/repo/seeds.exs (the existing 3 incident blocks — especially Incident 1 with `runbook_data`, the `Application.put_env` at line 3, and the trailing `IO.puts` count line)
    - .planning/phases/28-demo-seed-ci-lane/28-PATTERNS.md (the "seeds.exs" pattern section: the exact 4th incident block and updated count line, lines 487-512, plus the planner notes on the `"module"` key and the no-inline-"steps" rule)
    - lib/parapet/operator.ex (lines 1097-1113 — `extract_module/1`, to confirm the `"module"` key requirement)
  </read_first>
  <action>
    Add a 4th incident block (alongside the existing 3, NOT replacing them) creating an OPEN, capability-backed incident via `Parapet.Evidence.create_incident/1`. Set `state: "open"`, a distinct `correlation_key` (e.g. `"stalled-async-executor"`), a descriptive `title`/`description`, and crucially `runbook_data: %{"module" => to_string(DemoApp.Runbooks.StalledExecutor)}` per D-03 (the `"module"` string key — NOT inline `"steps"`, which is display-only and breaks Preview/Confirm). Optionally append one `Parapet.Evidence.append_timeline/2` note for narrative (per 28-PATTERNS.md example). Then update the trailing `IO.puts(...)` count line to reflect 4 incidents (e.g. "4 incidents (open x2/investigating/resolved), 7 timeline entries, 1 tool audit") — keep the count accurate to what the file actually inserts. Do NOT add a second `Application.put_env(:parapet, :repo, ...)` (line 3 already handles it).
  </action>
  <verify>
    <automated>cd examples/demo_app && (mix ecto.create >/dev/null 2>&1; mix ecto.migrate >/dev/null 2>&1; mix run priv/repo/seeds.exs) 2>&1 | tail -5; grep -c '"module" => to_string(DemoApp.Runbooks.StalledExecutor)' priv/repo/seeds.exs</automated>
  </verify>
  <acceptance_criteria>
    - `examples/demo_app/priv/repo/seeds.exs` contains `"module" => to_string(DemoApp.Runbooks.StalledExecutor)` inside a `create_incident` call with `state: "open"`
    - The new incident block does NOT use an inline `"steps"` list (no `"steps" =>` inside the new block)
    - The existing 3 incident blocks are unchanged
    - The trailing `IO.puts` count line reflects 4 incidents
    - `cd examples/demo_app && mix run priv/repo/seeds.exs` exits 0 (seed inserts without `{:error, :missing_runbook}` or a unique-constraint crash)
  </acceptance_criteria>
  <done>The seed inserts a 4th open capability-backed incident referencing the compiled runbook module by string; `mix run priv/repo/seeds.exs` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan adds a static seed row to the local demo DB. No external input, no HTTP endpoint, no auth surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Tampering | `runbook_data["module"]` string resolution | mitigate | `extract_module/1` uses `String.to_existing_atom/1`, so only already-loaded module atoms resolve; an arbitrary/typo string yields `{:error, :missing_runbook}` rather than loading an attacker-chosen module. Demo-only seed data, no external input. |
| T-28-04-02 | Information Disclosure | seeded incident content | accept | Seed text is synthetic demo narrative (no PII, no secrets). Low-value local data. |
| T-28-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase. No legitimacy checkpoint required. |

No high-severity threats. No ASVS categories apply (no HTTP/auth/external surfaces).
</threat_model>

<verification>
- `cd examples/demo_app && mix run priv/repo/seeds.exs` exits 0 against a provisioned DB.
- The 4th incident is OPEN and its `runbook_data` carries `"module" => to_string(DemoApp.Runbooks.StalledExecutor)`.
- No inline `"steps"` on the new block; existing 3 incidents untouched.
- (Full replay — `cd examples/demo_app && mix demo.reset` exits 0 — is the phase-gate check, combining this seed with the Plan 02 alias.)
</verification>

<success_criteria>
- A fresh clone's `mix setup` seeds an open capability-backed incident with a Preview-able/Confirm-able step (DEMO-05, success criterion #1).
- The seed is replayable via `mix demo.reset` (with Plan 02's alias).
- No core module under `lib/parapet/**` was edited.
</success_criteria>

<output>
Create `.planning/phases/28-demo-seed-ci-lane/28-04-SUMMARY.md` when done.
</output>
