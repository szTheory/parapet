---
phase: 47-component-groups-meta-components
plan: 03
type: execute
wave: 3
depends_on: ["47-02"]
files_modified:
  - brandbook/notes/operator-audit-matrix.md
autonomous: false
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-04, GROUP-05, GROUP-06, A11Y-05, MOTION-03]
must_haves:
  truths:
    - "The full suite (mix test --exclude unboxed) is GREEN — Phase 47 introduces no regression anywhere in the project."
    - "The audit matrix records GROUP-03/05/06 + A11Y-05 overlay requirements as N/A-by-design with documented ARIA-Disclosure-vs-Dialog + WCAG rationale (D-04/D-16), and flips the Phase-47-touched component cells to done/verified per D-16."
    - "A human has walked the gallery render-only cells (390px cockpit composition, preview disclosure open/close + focus-to-close on mobile, MOTION-03 reveal feel) and approved them (D-18)."
  artifacts:
    - "brandbook/notes/operator-audit-matrix.md — N/A-by-design overlay exception note + flipped component cells"
  prohibitions:
    - "No focus-trap, scrim, aria-modal, or role=dialog added to resolve the N/A cells (D-01) — they are documented as intentional absences, not implemented."
    - "No 'failed' audit-outcome state added; no ActionItem schema change (D-07/D-08)."
  key_links:
    - "The blocking human walkthrough is the ONLY verification path for the three render-only cells in 47-VALIDATION.md 'Manual-Only Verifications' — they are not string-assertable."
---

<objective>
Phase gate: prove the full suite is GREEN, record the N/A-by-design overlay resolution and
component-cell flips in the audit matrix, and run the BLOCKING human gallery walkthrough for the
three genuinely-rendered cells that no string assertion can cover.

This is the plan that the locked D-15 cadence calls "47-04"; because the optional shell wave was
folded into 47-02 (single additive `<main>` class, no file conflict), no empty plan was emitted and
this gate is the sequential 47-03.

Purpose: Close Phase 47 with a green full suite, a defensible audit ledger, and human-confirmed
visual/interaction behavior.
Output: Updated audit matrix; human walkthrough approved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-component-groups-meta-components/47-CONTEXT.md
@.planning/phases/47-component-groups-meta-components/47-VALIDATION.md
@brandbook/notes/operator-audit-matrix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite gate</name>
  <files>(none — verification only)</files>
  <read_first>
    47-VALIDATION.md "## Sampling Rate" — the phase gate is `mix test --exclude unboxed` GREEN before
    sign-off.
  </read_first>
  <action>
    Run the full suite `mix test --exclude unboxed`. It MUST exit 0. If any failure surfaces (including
    a demo-mirror divergence from 47-02), fix the divergence in the offending file and re-run before
    proceeding. Do not weaken or skip any assertion to make the suite pass.
  </action>
  <verify>
    <automated>mix test --exclude unboxed 2>&1 | tail -5</automated>
  </verify>
  <done>`mix test --exclude unboxed` exits 0 with 0 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Flip audit-matrix cells + record N/A-by-design overlay exception</name>
  <files>brandbook/notes/operator-audit-matrix.md</files>
  <read_first>
    brandbook/notes/operator-audit-matrix.md — the "## Component × State Grid" table (lines 14-34) and
    the "## Notes / Exceptions" section. CONTEXT D-04 (mark GROUP-03/05/06 + A11Y-05 overlay cells n/a
    with ARIA Disclosure-vs-Dialog + WCAG 2.4.3/2.4.7/2.4.11 rationale) and D-16.
  </read_first>
  <action>
    Add a new subsection under "## Notes / Exceptions" titled for the Phase-47 overlay resolution that
    records GROUP-03, GROUP-05, GROUP-06, and A11Y-05 as **N/A-by-design** with the rationale per D-04:
    a repo-wide grep for modal/overlay/drawer/scrim/dialog/aria-modal/role="dialog" returns zero
    matches; preview_panel is the ARIA **Disclosure** pattern (not Dialog), runbook_card is an inline
    `<div>`; cite ARIA APG Disclosure-vs-Dialog and WCAG 2.4.3 (focus order — content follows trigger in
    DOM), 2.4.7 (focus visible — existing close button), and 2.4.11 (focus not obscured — satisfied by
    the D-02 scroll-pb-72 fix shipped in 47-02). State explicitly that adding focus-trap/scrim/aria-modal
    is intentionally NOT done because it would contradict the no-modals reality, trap keyboard users in
    non-modal content, and require new JS (milestone-boundary violation).

    Then flip the Phase-47-touched component cells (D-16): for the components materially changed this
    phase — `response_cockpit` (break-words overflow hardening), `incident_summary` (voice re-author),
    `action_item_card` / `action_item_list` (risk + audit-outcome chips), `preview_panel` (reveal +
    landmark) — update the relevant grid cells from `todo` to `done` (and append a short note citing the
    Phase-47 change). Do NOT mark cells `verified` that depend on the human walkthrough until Task 3 is
    approved; mark those `done` and let the walkthrough sign-off elevate them in the SUMMARY. Do NOT
    invent new overlay/modal rows.
  </action>
  <verify>
    <automated>grep -qiE "n/?a-by-design|N/A-by-design|not applicable" brandbook/notes/operator-audit-matrix.md && grep -qi "Disclosure" brandbook/notes/operator-audit-matrix.md && echo MATRIX-OK || (echo MATRIX-MISSING && exit 1)</automated>
  </verify>
  <done>The audit matrix has a Phase-47 N/A-by-design overlay exception subsection citing Disclosure-vs-Dialog + WCAG 2.4.3/2.4.7/2.4.11, and the touched component cells are flipped from todo to done with Phase-47 notes. No focus-trap/scrim implemented; no new overlay rows added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Blocking human gallery walkthrough (render-only cells)</name>
  <action>
    BLOCKING human checkpoint. Present the gallery walkthrough steps below to the developer and wait
    for an explicit "approved" before finalizing Phase 47. Do not auto-advance — this gate verifies
    the three render-only cells in 47-VALIDATION.md that no string assertion can cover.
  </action>
  <what-built>
    Phase 47 shipped the brand-voice incident_summary re-author, action-item risk + audit-outcome
    chips, the aria-disabled affordance, the preview_panel CSS keyframe reveal + Disclosure landmark,
    the cockpit break-words overflow hardening, and the WCAG 2.4.11 scroll-padding fix — all verified
    by the GREEN contrast suite. Three render-only behaviors remain that no string assertion can cover
    (47-VALIDATION.md "Manual-Only Verifications").
  </what-built>
  <how-to-verify>
    1. Start the demo gallery (the demo app's `/parapet/_gallery` route — DB-less on a free loopback
       port per the project's gallery DX). Confirm it boots.
    2. GROUP-01 — 390px cockpit composition: set the viewport to 390px wide. Confirm `response_cockpit`
       collapses to a single column with `gap-6`, the source order reads title → impact → evidence →
       journeys/counts (impact-first), and a long unbroken incident title wraps (break-words) without
       blowing out the width.
    3. A11Y-05 — preview disclosure on mobile: on a short mobile viewport, open the Recovery Preview.
       Confirm the just-activated trigger is NOT obscured by the bottom sheet (D-02 scroll-pb-72).
       Close via the close button and confirm focus returns to the trigger. Confirm a screen
       reader announces the "Recovery Preview" region (role=region + aria-label).
    4. MOTION-03 — reveal feel + interruptibility: open the preview and confirm the panel lifts +
       fades in with brand easing (not an instant pop). Click Confirm/Cancel mid-animation — the
       buttons must stay live (interruptible). Enable OS "reduce motion" and confirm the reveal is
       neutralized (no animation) while the panel still appears.
    Expected outcome: all three behaviors feel correct; no width blowout, no focus loss, no janky or
    blocking animation.
  </how-to-verify>
  <resume-signal>Type "approved" to finalize Phase 47, or describe any issue (e.g. cockpit reflow, focus-to-close, reveal feel, scroll-pb value too small) for a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan runs tests, edits a markdown ledger, and runs a human walkthrough. No runtime code change, no new input/network/auth/data surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-03 | Repudiation | audit-matrix N/A justification | low | accept | The N/A-by-design rationale is documented in-repo with ARIA/WCAG citations, creating an auditable record; the negative-guard tests (47-01) keep the absence enforced. No mitigation beyond documentation required. |

No new attack surface: documentation + verification only. ASVS L1 / block-on-high — nothing
approaches high severity.
</threat_model>

<verification>
- `mix test --exclude unboxed` exits 0.
- The audit matrix records the N/A-by-design overlay exception with ARIA/WCAG rationale and flips the
  touched component cells.
- The blocking human walkthrough is approved for the three render-only gallery cells.
</verification>

<success_criteria>
- Full suite GREEN.
- Audit matrix documents GROUP-03/05/06 + A11Y-05 as N/A-by-design (not todo/done) with rationale; no
  focus-trap/scrim/aria-modal implemented.
- Human approves the 390px cockpit, preview disclosure open/close + focus-to-close, and MOTION-03
  reveal feel.
</success_criteria>

<output>
Create `.planning/phases/47-component-groups-meta-components/47-03-SUMMARY.md` when done, recording
the human walkthrough outcome and any cells elevated to verified.
</output>
