---
phase: 47-component-groups-meta-components
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex
  - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-04, GROUP-05, GROUP-06, A11Y-05, MOTION-03]
must_haves:
  truths:
    - "incident_summary/1 copy reads top-to-bottom as the brand formula symptom → evidence → safe next action → where to inspect, using the D-11 sentence-case labels and rewritten fallbacks (GROUP-02)."
    - "Each action-item card conveys risk by color + icon + label (never color alone) via action_item_risk/1, and surfaces audit outcome (Resolved · audited / Pending) derived honestly from state — no schema change (GROUP-04, D-07/D-08)."
    - "Shown-but-unavailable controls use aria-disabled='true' (focusable, AT-announced) with an [aria-disabled='true'] CSS rule matching control_base() disabled visuals (GROUP-04, D-09)."
    - "preview_panel reveals with a compositor-only CSS @keyframes (opacity + translateY) on insert, brand-eased, reduced-motion-safe via the existing block, with no JS (MOTION-03, D-13/D-14)."
    - "preview_panel announces as a landmark (role=region + aria-label='Recovery Preview') while staying a non-modal Disclosure; the cockpit <h2> gains break-words; the mobile detail <main> gains scroll-pb-72 md:scroll-pb-0 so the bottom sheet never obscures the focused trigger (GROUP-01, A11Y-05, D-02/D-03)."
    - "Every edit lands byte-identically in the demo mirror in the same task, flipping all 47-01 assertions GREEN (D-18)."
  artifacts:
    - "priv/templates/parapet.gen.ui/operator_components.ex.eex — keyframe + .po-preview-reveal + [aria-disabled] CSS, relabeled incident_summary, action_item_risk + audit-outcome chips, preview_panel landmark + reveal class, cockpit <h2> break-words"
    - "examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex — byte-identical mirror"
    - "priv/templates/parapet.gen.ui/operator_detail_live.ex.eex — <main> scroll-pb-72 md:scroll-pb-0"
    - "examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex — byte-identical mirror"
  prohibitions:
    - "NO role=dialog, aria-modal, scrim (class='fixed inset-0'), focus-trap, or backdrop added (D-01) — preview_panel stays a Disclosure. Guard inset-0 (scrim), never inset-x-0 (the legitimate preview_panel positioning)."
    - "NO ActionItem schema/migration change (D-07) — risk + audit outcome derived in the component layer from existing kind/state."
    - "NO 'failed' audit-outcome state fabricated (D-08) — open maps to Pending only."
    - "NO new tokens, dependencies, JS, component libraries, or new reduced-motion rule (D-13/D-14); NO change to any CSS variable value."
    - "NO edits to runbook_card/preview_panel copy, incident_timeline empty state, cross-page microcopy, or shared _copy/1 helpers — Phase 48 bright line (D-12). Edit ONLY incident_summary/1 for voice."
  key_links:
    - ".po-preview-reveal must be on the OUTERMOST preview_panel <div> (the fixed-positioning container), not the inner content card, or the translateY reveal causes a layout jump (47-RESEARCH.md Pitfall 5)."
    - "The @keyframes/.po-preview-reveal/[aria-disabled] CSS goes in the base .parapet-ui scope after the prefers-reduced-motion block — NOT in the dark-theme override block; opacity/transform are theme-independent (47-RESEARCH.md Pitfall 6)."
    - "Demo-mirror parity: the @component_paths and @detail_template_paths test loops read BOTH paths — any divergence fails RED (47-RESEARCH.md Pitfall 2)."
---

<objective>
Apply every locked Phase-47 markup, CSS, and copy edit to
`operator_components.ex.eex` and `operator_detail_live.ex.eex` AND their byte-parity demo mirrors in
the same task (D-18 assertion-pairing), flipping all 47-01 assertions RED→GREEN.

Per D-15 / Claude's Discretion, the optional 47-03 shell wave (the D-02 `scroll-pb-72` edit to
`operator_detail_live`) is FOLDED into this plan: it is a single additive class on one element with
no file-ownership conflict, so a separate wave adds overhead without benefit. The gate plan that
would have been 47-04 is therefore the sequential 47-03 (no empty plan emitted).

Purpose: Deliver GROUP-01/02/04, the A11Y-05 Disclosure landmark + WCAG 2.4.11 fix, the MOTION-03
keyframe reveal, and the D-09 aria-disabled affordance — all additive, all CSS/markup/copy only.
Output: Four files edited (two templates + two mirrors); contrast test GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-component-groups-meta-components/47-CONTEXT.md
@.planning/phases/47-component-groups-meta-components/47-RESEARCH.md
@.planning/phases/47-component-groups-meta-components/47-PATTERNS.md
@priv/templates/parapet.gen.ui/operator_components.ex.eex
@priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
@examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex
@examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS — @keyframes po-preview-reveal, .po-preview-reveal, [aria-disabled='true']</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</files>
  <read_first>
    operator_components.ex.eex lines 109-111 (motion tokens), lines 466-478 (the prefers-reduced-motion
    block ending the motion section), and 47-PATTERNS.md sections 1 and 2 (verbatim CSS to insert) plus
    47-RESEARCH.md "## Code Examples" (verbatim CSS). Locate the closing `</style>` of
    operator_theme_bootstrap/1.
  </read_first>
  <action>
    In the operator_theme_bootstrap/1 `<style>` block, in the base `.parapet-ui` scope (NOT the
    `html[data-parapet-theme="dark"]` override), after the prefers-reduced-motion media block and
    before `</style>`, add three rules verbatim from 47-PATTERNS.md sections 1 and 2:

    1. `@keyframes po-preview-reveal` animating `from { opacity: 0; transform: translateY(8px); }` to
       `to { opacity: 1; transform: translateY(0); }` (MOTION-03, D-13). Compositor-only props.
    2. `.parapet-ui .po-preview-reveal { animation: po-preview-reveal var(--motion-base)
       var(--motion-ease) both; }` (D-13). Add an inline comment noting the prefers-reduced-motion
       block above already zeroes animation-duration — no new reduced-motion rule needed (D-14).
    3. `.parapet-ui [aria-disabled="true"] { opacity: 0.5; cursor: not-allowed; pointer-events: none; }`
       (D-09) — matches control_base() disabled visuals; additive, does not conflict with existing
       `disabled:opacity-50` utilities.

    No dark-mode override for any of these (opacity/transform/aria-disabled are theme-independent —
    47-RESEARCH.md Pitfall 6). Do NOT change any CSS variable value. Apply byte-identically to the
    demo mirror in this same task.
  </action>
  <verify>
    <automated>grep -L 'po-preview-reveal' priv/templates/parapet.gen.ui/operator_components.ex.eex examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex | grep -q . && (echo MISSING-MIRROR && exit 1) || echo PRESENT-BOTH</automated>
  </verify>
  <done>Both the template and the demo mirror contain `@keyframes po-preview-reveal`, `.po-preview-reveal { animation: po-preview-reveal var(--motion-base) var(--motion-ease)`, and the `[aria-disabled="true"]` rule with `opacity: 0.5`. No dark-block override added. No CSS variable value changed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-author incident_summary/1 brand voice + cockpit break-words + preview_panel landmark/reveal</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex</files>
  <read_first>
    operator_components.ex.eex: response_cockpit <h2> at ≈line 561; incident_summary/1 body at lines
    810-928 (header card 812-821, escalation amber card 823-889, top-facts/observability grid 891-925);
    preview_panel/1 outer/inner divs at lines 1169-1170. 47-PATTERNS.md sections 4, 5, 6 give the exact
    label renames, fallback rewrites, and div edits verbatim. Search for the current strings rather than
    relying on line numbers (prior edits shift lines).
  </read_first>
  <action>
    GROUP-01 (D-06): add `break-words` to the response_cockpit `<h2>` class (matches the
    incident_summary `<h1>` overflow-hardening precedent). Search-and-edit the exact `<h2>` at
    ≈line 561; do not touch other headings.

    GROUP-02 incident_summary/1 voice (D-10/D-11) — edit ONLY this component (D-12 bright line; do NOT
    touch runbook_card/preview_panel copy or _copy/1 helpers). Rename the five section labels in
    sentence case verbatim: `Impact Summary` → `What users are seeing`; `Escalation Status` →
    `Escalation status`; `Next Step` → `Safe next step`; `Top Facts` → `Evidence on record`;
    `Observability` → `Where to inspect`. Rewrite the two in-summary fallbacks verbatim:
    `No impact summary recorded.` → `No user-facing impact has been recorded yet.`;
    `No external links attached.` → `No trace or external links are attached to this incident yet.`.
    Reorder the three card containers so reading top-to-bottom traces the formula symptom → evidence →
    safe next action → where to inspect (per 47-PATTERNS.md section 5 card-reorder note and
    47-RESEARCH.md Open Question 3 — move the evidence/observability content ahead of the escalation
    card so evidence precedes safe-next-action). Keep ALL `@detail.*` / `@detail.derived.*` field
    accesses identical — zero data-shape change. Keep the register neutral and blameless (no apology,
    no "oops", no alarmist flavor — D-12).

    A11Y-05 + MOTION-03 preview_panel (D-03/D-13): on the OUTERMOST preview_panel `<div>` (the
    `fixed inset-x-0 bottom-0 z-50 ...` positioning container, ≈line 1169) prepend the
    `po-preview-reveal` class — outermost only, never the inner card (47-RESEARCH.md Pitfall 5). On the
    INNER content `<div>` (≈line 1170) add `role="region"` and `aria-label="Recovery Preview"`. Do NOT
    add `role="dialog"`, `aria-modal`, `aria-expanded`, a scrim, or a focus-trap (D-01).

    Apply every edit byte-identically to the demo mirror in this same task.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_contrast_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>The cockpit `<h2>` has `break-words`. incident_summary/1 uses the four new labels + two new fallbacks and reads in formula order; old labels `Impact Summary`/`Top Facts` are gone from the component. preview_panel outer div has `po-preview-reveal`; inner div has `role="region"` + `aria-label="Recovery Preview"`; no `role="dialog"`/`aria-modal`. Demo mirror matches. Field accesses unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add action_item_risk/audit-outcome chips to action_item_card + helpers; add detail <main> scroll-pb</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex, priv/templates/parapet.gen.ui/operator_detail_live.ex.eex, examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex</files>
  <read_first>
    operator_components.ex.eex: action_item_card/1 at lines 1305-1337 (header row with state chip at
    1320-1321); severity_color/1 + state_color/1 clause style at lines 1449-1459; chip_class(:execution,
    :executed) at lines 1408-1410. operator_detail_live.ex.eex: `<main id="parapet-main" tabindex="-1"
    class="flex-1 bg-stone-50 px-4 py-6 md:px-8">` at ≈line 224. 47-PATTERNS.md section 3 gives the
    verbatim helper clauses and the updated action_item_card header markup; section 7 gives the <main>
    edit verbatim. 47-RESEARCH.md Patterns 4 and 5 confirm the helper bodies.
  </read_first>
  <action>
    GROUP-04 risk (D-07): add private helpers after action_item_card/1, copying the severity_color/1
    clause style — `action_item_risk/1` mapping kind → tier (dead_letter → :danger; orphaned_callback
    and stalled_workflow → :warning; suppressed_delivery → :info; exact_follow_up and catch-all _ →
    :neutral); `risk_chip_class/1` (:danger → "po-chip-danger", :warning → "po-chip-warning", :info →
    "po-chip-info", _ → ""); `risk_label/1` (:danger → "High risk", :warning → "Medium risk", :info →
    "Low risk", _ → "Routine"). NO ActionItem schema/migration change — derive from existing kind.

    GROUP-04 audit outcome (D-08): add `audit_outcome_chip_class/1` (state "resolved" reuses
    `chip_class(:execution, :executed)`; catch-all → the neutral `po-chip rounded px-1.5 py-0.5 text-xs
    font-semibold uppercase tracking-wide` string) and `audit_outcome_label/1` ("resolved" → "Resolved ·
    audited"; catch-all → "Pending"). Do NOT fabricate a "failed" state — open maps to Pending only.

    In the action_item_card/1 header row (≈line 1320), alongside the existing state chip, render two new
    chips inside a `flex items-center gap-1.5` wrapper per 47-PATTERNS.md section 3: a risk chip
    (`po-chip` + `risk_chip_class(action_item_risk(@item.kind))` + sizing classes + `flex items-center
    gap-1`) carrying a tier-appropriate `aria-hidden="true"` SVG icon AND the `risk_label(...)` text —
    color + icon + label, never color alone (WCAG 1.4.1); and an audit-outcome chip
    (`audit_outcome_chip_class(@item.state)` with `audit_outcome_label(@item.state)`). Icon glyphs are
    at your discretion (suggested: exclamation-triangle/circle, information-circle, check-circle) — the
    label text is what the gate asserts.

    D-09: this card's chips are display-only. If wiring an aria-disabled control, use
    `aria-disabled={if cond, do: "true", else: nil}` (NOT native `disabled`) on shown-but-unavailable
    controls so they stay focusable and AT-announced; the CSS rule from Task 1 supplies the visual. The
    `[aria-disabled="true"]` CSS literal already satisfies the gate assertion via Task 1.

    D-02 (A11Y-05, WCAG 2.4.11 / C43): on the operator_detail_live `<main id="parapet-main">` element,
    append `scroll-pb-72 md:scroll-pb-0` to the class (47-PATTERNS.md section 7). `scroll-pb-72` (288px)
    is a conservative over-estimate per your discretion (D-02) — tune in the 47-03 gallery walkthrough if
    the tallest sheet overflows. Optional Esc-to-cancel (Claude's Discretion): you MAY add
    `phx-window-keydown="cancel_preview" phx-key="escape"` to the detail LiveView root for keyboard
    dismiss parity; it is not a conformance requirement. State your decision in the SUMMARY.

    Apply every edit byte-identically to both demo mirrors in this same task.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_contrast_test.exs 2>&1 | grep -qiE "0 failures" && echo GREEN || (mix test test/parapet/operator_ui_contrast_test.exs 2>&1 | tail -8 && exit 1)</automated>
  </verify>
  <done>action_item_risk/1, risk_chip_class/1, risk_label/1, audit_outcome_chip_class/1, audit_outcome_label/1 exist with the specified clauses. action_item_card renders a risk chip (color+icon+label) and an audit-outcome chip ("Resolved · audited"/"Pending"). The detail `<main>` has `scroll-pb-72 md:scroll-pb-0`. Both demo mirrors match. `mix test test/parapet/operator_ui_contrast_test.exs` passes (all 47-01 assertions GREEN). No ActionItem schema change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Edits are confined to EEx template markup, inline CSS custom-property rules, static copy/label literals, and private Elixir mapping functions. No new network/auth/input/data-flow surface; no untrusted data interpolated unescaped (HEEx auto-escapes; all new copy/role/aria-label strings are static literals). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-02-T1 | Tampering | new aria-label / role / copy literals in HEEx | low | accept | All values are static string literals — no interpolation. HEEx auto-escapes any embedded expression. No injection vector introduced. |
| T-47-02-T2 | Information Disclosure | audit-outcome chip derived from @item.state | low | accept | Outcome is derived honestly from the already-rendered state field; surfaces no new data beyond what action_item_card already displays. No new disclosure. |
| T-47-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (47-RESEARCH.md Package Legitimacy Audit: none). No new runtime/JS dependency. |

Per the security contribution: this is a values-only/markup/copy re-skin with NO new attack surface.
ASVS L1 / block-on-high — no genuine high-severity new exposure exists, so nothing blocks.
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_contrast_test.exs` is GREEN — every 47-01 assertion now passes
  against both the template and its demo mirror.
- No `role="dialog"`, `aria-modal`, or `class="fixed inset-0"` scrim appears in the components
  (negative-guards stay green).
- No `ActionItem` schema/migration file is touched; no CSS variable value changed; no `_copy/1`,
  `runbook_card`, or `preview_panel` copy edited.
</verification>

<success_criteria>
- GROUP-01: cockpit `<h2>` has `break-words`; grid collapse behavior unchanged (verified visually in 47-03).
- GROUP-02: incident_summary uses the formula labels + rewritten fallbacks and reads in formula order.
- GROUP-04: risk chip (color+icon+label) + audit-outcome chip render; aria-disabled affordance available.
- A11Y-05: preview_panel is a Disclosure landmark; detail `<main>` has scroll-pb-72 md:scroll-pb-0.
- MOTION-03: keyframe reveal applied to the outermost preview_panel div; reduced-motion-safe; no JS.
- All four files edited; demo mirrors byte-match; contrast test GREEN.
</success_criteria>

<output>
Create `.planning/phases/47-component-groups-meta-components/47-02-SUMMARY.md` when done.
Record the Esc-to-cancel decision and the scroll-pb value chosen.
</output>
