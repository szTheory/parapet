---
phase: 47-component-groups-meta-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/parapet/operator_ui_contrast_test.exs
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-04, GROUP-05, GROUP-06, A11Y-05, MOTION-03]
must_haves:
  truths:
    - "Running the contrast test puts every Phase-47 acceptance assertion on record, and they fail RED because no markup has shipped yet (D-15 test-first cadence)."
    - "The N/A-by-design overlay requirements (GROUP-03/05/06, A11Y-05) are encoded as positive negative-guards, not silent omissions (D-16)."
  artifacts:
    - "test/parapet/operator_ui_contrast_test.exs — extended @component_paths loop + extended @detail_template_paths loop with all Phase-47 assertions"
  prohibitions:
    - "No new test files created (D-15) — all assertions are additive to the existing operator_ui_contrast_test.exs."
    - "No template, component, CSS, or copy edits in this plan — assertions must be RED."
  key_links:
    - "The @component_paths loop reads BOTH the .eex template and its demo mirror, so every assertion implicitly enforces demo-mirror parity (D-18)."
---

<objective>
Wave 0 RED test scaffold. Add every Phase-47 acceptance assertion to the existing
`test/parapet/operator_ui_contrast_test.exs` — additive only, no new test files (D-15). These
assertions cover the verification side of all eight phase requirements and MUST fail RED on first
run because the markup ships in plan 47-02.

Purpose: Lock the binding contract for GROUP-01/02/04, the N/A-by-design overlay guards
(GROUP-03/05/06, A11Y-05), MOTION-03, and the D-02 WCAG 2.4.11 fix into the gate before any
implementation, so 47-02 has a concrete red→green target.
Output: Extended `@component_paths` and `@detail_template_paths` test loops; all new assertions RED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-component-groups-meta-components/47-CONTEXT.md
@.planning/phases/47-component-groups-meta-components/47-RESEARCH.md
@.planning/phases/47-component-groups-meta-components/47-PATTERNS.md
@.planning/phases/47-component-groups-meta-components/47-VALIDATION.md
@test/parapet/operator_ui_contrast_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase-47 assertions to the @component_paths loop (RED)</name>
  <files>test/parapet/operator_ui_contrast_test.exs</files>
  <read_first>
    test/parapet/operator_ui_contrast_test.exs lines 92-191 — the existing
    "operator components use semantic tokens for known dark-mode risk surfaces" test that loops over
    @component_paths (both the .eex template and the demo mirror). New assertions go INSIDE this
    `for path <- @component_paths do ... end` loop, before the closing `end`. Note line 107 already
    asserts `aria-label="Close Recovery Preview"` and line 143 already refutes `transition-all` — do
    NOT duplicate these.
  </read_first>
  <action>
    Append the following assertions inside the existing `@component_paths` loop (per 47-RESEARCH.md
    "## Validation Architecture" / "New Assertions to Add" and 47-PATTERNS.md section 8). All cite
    their decision IDs in inline comments for traceability.

    GROUP-01 (D-06): assert content matches `break-words`.

    GROUP-02 brand-voice formula labels (D-11): assert content matches each of `What users are seeing`,
    `Evidence on record`, `Where to inspect`, `Safe next step`. Assert content matches the two rewritten
    fallback strings `No user-facing impact has been recorded yet.` and `No trace or external links are
    attached to this incident yet.`. Refute content matches the two old labels `Impact Summary` and
    `Top Facts`. Do NOT refute the bare word for the old observability label — per 47-RESEARCH.md
    Pitfall 3 it may appear in comments/helpers; rely on the positive assert of `Where to inspect`
    instead (this is the safer guard).

    GROUP-03/05/06 + A11Y-05 N/A negative-guards (D-16): refute content matches `role="dialog"`; refute
    content matches `aria-modal`; refute content matches the full-screen scrim literal `class="fixed
    inset-0` using `~S` sigil so the embedded quote is literal. CRITICAL per 47-RESEARCH.md Pitfall 1:
    guard the four-side scrim `class="fixed inset-0` ONLY — preview_panel legitimately uses
    `fixed inset-x-0 bottom-0`, a different string; do not refute `inset-0` bare. Then assert the
    Disclosure shape is present: assert content matches `md:relative md:inset-auto`.

    A11Y-05 Disclosure landmark (D-03): assert content matches `role="region"` (use `~S` sigil) and
    assert content matches `aria-label="Recovery Preview"` (use `~S` sigil).

    GROUP-04 risk + audit-outcome + aria-disabled (D-07/D-08/D-09): assert content matches
    `action_item_risk`; assert content matches the audit label `Resolved · audited`; assert content
    matches the CSS selector literal `[aria-disabled="true"]` using `~S` sigil.

    MOTION-03 keyframe reveal (D-13): assert content matches `@keyframes po-preview-reveal`; assert
    content matches `animation: po-preview-reveal var(--motion-base) var(--motion-ease)`. The
    `transition-all` refute (line 143) and `--motion-fast: 0ms` reduced-motion assert (line 124) are
    already present — do NOT duplicate.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_contrast_test.exs 2>&1 | grep -qE "(failure|Failure|test failed)" && echo "RED-CONFIRMED" || (echo "EXPECTED RED BUT GOT GREEN" && exit 1)</automated>
  </verify>
  <done>All new `@component_paths` assertions are present and the test fails RED (markup not yet shipped). Compilation succeeds (no syntax errors in the test). The `~S` sigil is used for any assertion string containing an embedded double-quote or square-bracket selector.</done>
</task>

<task type="auto">
  <name>Task 2: Add D-02 scroll-padding assertion to the @detail_template_paths loop (RED)</name>
  <files>test/parapet/operator_ui_contrast_test.exs</files>
  <read_first>
    test/parapet/operator_ui_contrast_test.exs lines 198-210 — the @detail_template_paths declaration
    and the existing "operator detail templates have correct landmarks" test that loops over both the
    detail .eex template and its demo mirror.
  </read_first>
  <action>
    Inside the existing `for path <- @detail_template_paths do ... end` loop (the
    "operator detail templates have correct landmarks" test), append one assertion per D-02 / WCAG
    2.4.11 (technique C43): assert content matches `scroll-pb-72`. Add an inline comment citing
    `D-02 (A11Y-05): WCAG 2.4.11 focus-not-obscured scroll-padding-bottom`.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_contrast_test.exs 2>&1 | grep -qE "(failure|Failure|test failed)" && echo "RED-CONFIRMED" || (echo "EXPECTED RED BUT GOT GREEN" && exit 1)</automated>
  </verify>
  <done>The `scroll-pb-72` assertion is present inside the `@detail_template_paths` loop and fails RED (the `<main>` class not yet updated). Test file compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only edits. No runtime code, no inputs, no network/auth/data-flow surface. Assertions read static file content via `File.read!/1` on first-party repo paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Tampering | test assertion strings | low | accept | Assertions are static literals in a first-party test file; no interpolation of untrusted data. No mitigation required — a wrong literal simply fails the gate, which is the intended behavior. |

No new attack surface: this plan adds ExUnit string assertions only. No new inputs, no new sinks,
no new dependency, no runtime change. ASVS L1 / block-on-high: nothing in this plan approaches high
severity.
</threat_model>

<verification>
- `mix test test/parapet/operator_ui_contrast_test.exs` compiles and runs; the new Phase-47
  assertions fail RED (proving the contract is unmet until 47-02 ships markup).
- No new test files exist (`git status` shows only `operator_ui_contrast_test.exs` modified).
</verification>

<success_criteria>
- All assertions enumerated in 47-RESEARCH.md "New Assertions to Add" and 47-PATTERNS.md section 8
  are present in the two existing loops.
- The test file compiles (no syntax error) and the new assertions are RED.
- No duplication of already-present assertions (`aria-label="Close Recovery Preview"` line 107,
  `transition-all` refute line 143, `--motion-fast: 0ms` line 124).
</success_criteria>

<output>
Create `.planning/phases/47-component-groups-meta-components/47-01-SUMMARY.md` when done.
</output>
