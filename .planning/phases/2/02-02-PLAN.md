---
phase: 2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/integrations/accrue.ex
  - lib/parapet/metrics/accrue.ex
autonomous: true
requirements:
  - JTBD-02
must_haves:
  truths:
    - Accrue emits telemetry for checkout and webhook events.
    - Parapet metrics track checkout outcome and webhook latency.
  artifacts:
    - path: lib/parapet/integrations/accrue.ex
      provides: Telemetry translation for billing
      exports: ["setup/0", "handle_event/4"]
    - path: lib/parapet/metrics/accrue.ex
      provides: Prometheus metric definitions for Accrue
      exports: ["setup/0", "metrics/0"]
  key_links:
    - from: lib/parapet/integrations/accrue.ex
      to: lib/parapet/metrics/accrue.ex
      via: setup/0
      pattern: Parapet.Metrics.Accrue.setup()
---

<objective>
Translate Accrue billing events into concrete SLO-backed metrics, specifically targeting checkout success and webhook latency.
Purpose: Move from generic billing tracking to explicit Critical Journeys that affect business health.
Output: Modified Accrue integration and new Accrue metrics module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/parapet/integrations/accrue.ex
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Parapet.Metrics.Accrue</name>
  <files>lib/parapet/metrics/accrue.ex</files>
  <action>
    Create a new module `Parapet.Metrics.Accrue`.
    Implement `setup/0` which returns `:ok`.
    Implement `metrics/0` which imports `Telemetry.Metrics` and defines:
    - `counter("parapet.journey.billing.checkout.count", event_name: [:parapet, :journey, :billing, :checkout], tags: [:outcome, :plan])`
    - `distribution("parapet.journey.billing.webhook.duration", event_name: [:parapet, :journey, :billing, :webhook], tags: [:outcome, :event_type], measurement: :duration)`
  </action>
  <verify>
    <automated>mix run -e 'Code.ensure_loaded?(Parapet.Metrics.Accrue)'</automated>
  </verify>
  <done>Module exists and exports `metrics/0`</done>
</task>

<task type="auto">
  <name>Task 2: Update Accrue Integration</name>
  <files>lib/parapet/integrations/accrue.ex</files>
  <action>
    Modify `Parapet.Integrations.Accrue`.
    In `setup/0`, add telemetry attachments for `[:accrue, :billing, :checkout, :stop]`, `[:accrue, :billing, :checkout, :exception]`, `[:accrue, :billing, :webhook, :stop]`, and `[:accrue, :billing, :webhook, :exception]`.
    Delegate to `Parapet.Metrics.Accrue.setup()` at the end of `setup/0`.
    Implement `handle_event/4` and delegate to `process_event`.
    Following the `scoria.ex` analog and the "Error Handling in Telemetry Handlers" pattern detailed in `PATTERNS.md`, wrap the delegation in a `rescue` block to log exceptions and return `:ok`.
    Add `process_event` clauses for:
    - `[:accrue, :billing, :checkout, state]` mapping to `[:parapet, :journey, :billing, :checkout]`, passing tags for `outcome` and `plan`.
    - `[:accrue, :billing, :webhook, state]` mapping to `[:parapet, :journey, :billing, :webhook]`, passing tags for `outcome` and `event_type`.
    Pass measurements along, specifically `:duration` for the webhook events. Ensure PII is stripped.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Accrue integration intercepts checkout/webhook telemetry and translates it correctly</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telemetry bus | Event data from Accrue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | accrue.ex | mitigate | Ensure PII is not leaked into metrics tags by filtering metadata. |
</threat_model>

<verification>
Ensure `mix compile` succeeds and both modules exist and form correct `Telemetry.Metrics`.
</verification>

<success_criteria>
Accrue checkout counts and webhook latency are defined as Prometheus metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/2/02-02-SUMMARY.md`
</output>
