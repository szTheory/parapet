---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/slo/provider.ex
  - lib/parapet/slo/resolvable.ex
  - lib/parapet/slo.ex
  - lib/mix/tasks/parapet.gen.prometheus.ex
autonomous: true
requirements:
  - AI-SLO-01
must_haves:
  truths:
    - Operators can define SLOs using a data-first `Provider` behaviour rather than runtime mutation.
    - `Parapet.SLO.all/0` correctly aggregates from configured providers and resolves them to standard SLO structs.
    - The Prometheus generator outputs multi-burn-rate alerts purely based on `SLO.all/0` without hardcoded registrations.
  artifacts:
    - path: lib/parapet/slo/provider.ex
      provides: Provider behaviour
    - path: lib/parapet/slo/resolvable.ex
      provides: Protocol to transform provider structs to `Parapet.SLO.t()`
    - path: lib/parapet/slo.ex
      provides: Registry aggregator
  key_links:
    - from: lib/parapet/slo.ex
      to: lib/parapet/slo/provider.ex
      via: "Application.get_env(:parapet, :providers)"
---

<objective>
Migrate the `Parapet.SLO` architecture from a runtime mutation-based registry (`define/2`) to a declarative, data-first `Provider` behaviour, ensuring compile-time safety and GitOps readiness while supporting the multi-burn-rate PromQL generation.

Purpose: Eliminate "magic" environment mutation and enable host application developers to centrally define composable SLOs (including the new `ScoriaEval` SLOs) in a single module.
Output: The `Parapet.SLO.Provider` behaviour, `Parapet.SLO.Resolvable` protocol, and an updated `mix parapet.gen.prometheus` task.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/2/RESEARCH.md
@.planning/phases/2/DECISIONS.md
@.planning/phases/2/PATTERNS.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Configuration Layer | Host application module defining system objectives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Parapet.SLO.all | mitigate | Ensure resolved SLO properties (like PromQL queries) are safely formatted without newlines to prevent YAML injection in downstream generators. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Provider Behaviour and Resolvable Protocol</name>
  <files>
    lib/parapet/slo/provider.ex,
    lib/parapet/slo/resolvable.ex
  </files>
  <behavior>
    - Test 1: Resolvable protocol falls back or implements `to_slo/1` returning a `Parapet.SLO.t()`.
  </behavior>
  <action>
    Create `Parapet.SLO.Provider` with a single callback `@callback slos() :: [struct()]`. Reference analog `lib/parapet/notifier.ex` from `PATTERNS.md` and use the pattern excerpts for behaviour definition.
    Create `Parapet.SLO.Resolvable` protocol with `to_slo(struct)`. Provide a default implementation for `%Parapet.SLO{}` that simply returns itself.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Provider and Resolvable protocol exist.</done>
</task>

<task type="auto">
  <name>Task 2: Update SLO Registry Aggregation</name>
  <files>
    lib/parapet/slo.ex,
    test/parapet/slo_test.exs
  </files>
  <action>
    Update `Parapet.SLO.all/0`. Instead of just returning `Application.get_env(:parapet, :slos, [])`, it must now also read `Application.get_env(:parapet, :providers, [])`. Map over these provider modules, call `slos/0`, flatten the list, and map each item through `Parapet.SLO.Resolvable.to_slo/1`. Merge these with the legacy `:slos` list (for backward compatibility).
    Add `@deprecated "Use a Parapet.SLO.Provider module instead"` to `define/2` but keep its functionality intact.
  </action>
  <verify>
    <automated>mix test test/parapet/slo_test.exs</automated>
  </verify>
  <done>Parapet.SLO aggregates legacy definitions and provider definitions.</done>
</task>

<task type="auto">
  <name>Task 3: Update Prometheus Generator Task</name>
  <files>
    lib/mix/tasks/parapet.gen.prometheus.ex
  </files>
  <action>
    Modify `Mix.Tasks.Parapet.Gen.Prometheus.igniter/1`. Remove all hardcoded `register()` calls (`SLO.HTTP.register()`, `SLO.Oban.register()`, etc.). The task should now exclusively rely on `slos = SLO.all()` to fetch SLOs, expecting that the user configures their built-in or custom providers correctly in their host config. The existing EEx template already supports multi-burn-rate alerts, so just the data source changes.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Generator relies exclusively on the updated SLO registry.</done>
</task>

</tasks>

<success_criteria>
- `Parapet.SLO.all/0` seamlessly merges legacy environment state and data-first providers.
- `Parapet.SLO.Resolvable` allows any custom struct to be mapped to a Prometheus-compatible SLO.
</success_criteria>

<output>
After completion, create `.planning/phases/2/02-01-SUMMARY.md`
</output>
