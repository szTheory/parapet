---
phase: 2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/integrations/sigra.ex
  - lib/parapet/metrics/sigra.ex
autonomous: true
requirements:
  - JTBD-01
must_haves:
  truths:
    - Sigra emits telemetry for signup events.
    - Sigra emits telemetry for login events.
    - Parapet metrics track signup outcome and provider.
    - Parapet metrics track login outcome.
  artifacts:
    - path: lib/parapet/integrations/sigra.ex
      provides: Telemetry translation for signup and login
      exports: ["setup/0", "handle_event/4"]
    - path: lib/parapet/metrics/sigra.ex
      provides: Prometheus metric definitions for Sigra
      exports: ["setup/0", "metrics/0"]
  key_links:
    - from: lib/parapet/integrations/sigra.ex
      to: lib/parapet/metrics/sigra.ex
      via: setup/0
      pattern: Parapet.Metrics.Sigra.setup()
---

<objective>
Translate Sigra authentication events into concrete SLO-backed metrics, specifically targeting login and signup success rates.
Purpose: Move from generic error tracking to specific Critical Journeys.
Output: Modified Sigra integration and new Sigra metrics module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/parapet/integrations/sigra.ex
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Parapet.Metrics.Sigra</name>
  <files>lib/parapet/metrics/sigra.ex</files>
  <action>
    Create a new module `Parapet.Metrics.Sigra`.
    Implement `setup/0` which returns `:ok`.
    Implement `metrics/0` which imports `Telemetry.Metrics` and defines:
    - `counter("parapet.journey.login.count", event_name: [:parapet, :journey, :login], tags: [:outcome])`
    - `counter("parapet.journey.signup.count", event_name: [:parapet, :journey, :signup], tags: [:outcome, :provider])`
  </action>
  <verify>
    <automated>mix run -e 'Code.ensure_loaded?(Parapet.Metrics.Sigra)'</automated>
  </verify>
  <done>Module exists and exports `metrics/0`</done>
</task>

<task type="auto">
  <name>Task 2: Update Sigra Integration</name>
  <files>lib/parapet/integrations/sigra.ex</files>
  <action>
    Modify `Parapet.Integrations.Sigra`.
    In `setup/0`, add telemetry attachment for `[:sigra, :auth, :signup, :stop]`, `[:sigra, :auth, :signup, :exception]`, `[:sigra, :auth, :login, :stop]`, and `[:sigra, :auth, :login, :exception]`.
    Delegate to `Parapet.Metrics.Sigra.setup()` at the end of `setup/0`.
    Implement `handle_event/4` and delegate to `process_event`.
    Following the `scoria.ex` analog and the "Error Handling in Telemetry Handlers" pattern detailed in `PATTERNS.md`, wrap the delegation in a `rescue` block to log exceptions and return `:ok`.
    Add `process_event` clause for `[:sigra, :auth, :signup, state]` mapping to `[:parapet, :journey, :signup]` with tags for `outcome` and `provider` (from metadata). Ensure you don't leak PII.
    Add `process_event` clause for `[:sigra, :auth, :login, state]` mapping to `[:parapet, :journey, :login]` with tags for `outcome`.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Sigra integration intercepts signup and login telemetry and translates it correctly</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telemetry bus | Event data from Sigra |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | sigra.ex | mitigate | Ensure PII is not leaked into metrics tags by explicitly allowing only specific keys. |
</threat_model>

<verification>
Ensure `mix compile` succeeds and both modules exist and form correct `Telemetry.Metrics`.
</verification>

<success_criteria>
Sigra signup and login journeys are defined as Prometheus counters.
</success_criteria>

<output>
After completion, create `.planning/phases/2/02-01-SUMMARY.md`
</output>
