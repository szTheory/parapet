---
phase: 02-rulestead-flag-correlation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
autonomous: true
requirements:
  - RUL-02
must_haves:
  truths:
    - Operator can immediately see a highlighted list of suspect flag changes above the incident timeline
    - Timeline entries natively differentiate between automatic system events and human actions
  artifacts:
    - path: priv/templates/parapet.gen.ui/operator_components.ex.eex
      provides: Suspect changes card and timeline markers
  key_links:
    - from: priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
      to: priv/templates/parapet.gen.ui/operator_components.ex.eex
      via: Component invocation
---

<objective>
Update the Operator UI to correlate and highlight proximate Rulestead flag changes on Incident pages, surfacing root cause candidates instantly.

Purpose: Reduce TTR (Time-To-Resolution) by making it visually obvious when an incident aligns with a recent deploy or feature flag toggle.
Output: UI templates updated with the new "Suspect Changes" hero card and distinctive timeline markers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/v0.6-phases/2/RESEARCH.md
@.planning/v0.6-phases/2/UI-SPEC.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Suspect Changes Card UI</name>
  <files>
    - priv/templates/parapet.gen.ui/operator_components.ex.eex
    - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
  </files>
  <action>
    In `operator_components.ex.eex`, add a new `suspect_changes_card` function component. Design it according to `UI-SPEC.md` (using the 30% accent `bg-blue-50` base, hero card layout).
    It should accept a list of `system_events` (or timeline entries of type `rulestead_flag_change`) and list them concisely. If the list is empty, render nothing.
    In `operator_detail_live.ex.eex`, invoke `<.suspect_changes_card entries={@suspect_entries} />` prominently above the runbook card or incident summary. (Filter `assigns.incident.timeline_entries` to find suspect entries where type is `rulestead_flag_change` to pass as the `entries` prop).
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_integration_test.exs</automated>
  </verify>
  <done>The card renders correctly when relevant events are present</done>
</task>

<task type="auto">
  <name>Task 2: Distinct Timeline Markers</name>
  <files>
    - priv/templates/parapet.gen.ui/operator_components.ex.eex
  </files>
  <action>
    Modify the existing `incident_timeline` component in `operator_components.ex.eex`. 
    Add pattern matching or conditional styling inside the `for entry &lt;- @entries` comprehension. When `entry.type == "rulestead_flag_change"`, use the highlight color palette (`bg-purple-500 ring-purple-200`) for the timeline dot, and print the actor, flag name, and payload scope distinctly.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_integration_test.exs</automated>
  </verify>
  <done>Timeline natively distinguishes between system events and normal events visually</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI | Rendering operator-facing HTML |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | `suspect_changes_card` | mitigate | Ensure HTML escaping is active (HEEx default) when rendering `payload` text to avoid XSS if an external system sent malicious telemetry strings. |
</threat_model>
