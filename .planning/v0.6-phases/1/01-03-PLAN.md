---
phase: "01"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/YYMMDDHHMMSS_add_trace_id_to_incidents.exs
  - lib/parapet/spine/incident.ex
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - test/parapet/spine/incident_test.exs
autonomous: true
requirements: [OTL-01.3]
must_haves:
  truths:
    - Incidents can be persisted with a trace_id.
    - The Operator UI renders the trace link dynamically based on config.
  artifacts:
    - path: lib/parapet/spine/incident.ex
      provides: incident Ecto schema with trace_id
    - path: priv/templates/parapet.gen.ui/operator_components.ex.eex
      provides: trace link UI elements in the external links list
  key_links:
    - from: priv/templates/parapet.gen.ui/operator_components.ex.eex
      to: Application.get_env(:parapet, :trace_url_template)
      via: string replacement inside the view template
---

<objective>
Store trace_id on Incident schemas and surface dynamic trace links in Operator UI.

Purpose: When an incident occurs, the context (trace) that triggered it should be immutably stored with the incident and directly clickable in the UI.
Output: Database migration for trace_id and updated UI templates linking to tracing backends.
</objective>

<context>
@.planning/v0.6-phases/1/RESEARCH.md
@.planning/v0.6-phases/1/PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trace_id to Incident schema</name>
  <files>priv/repo/migrations/[timestamp]_add_trace_id_to_incidents.exs, lib/parapet/spine/incident.ex, test/parapet/spine/incident_test.exs</files>
  <behavior>
    - Ecto migration adds `trace_id` (string) to `parapet_incidents` table.
    - `Parapet.Spine.Incident` schema has `:trace_id` field.
    - `changeset/2` permits and casts `:trace_id`.
  </behavior>
  <action>Generate an Ecto migration to add `trace_id` (type string) to the `parapet_incidents` table. Update the `Parapet.Spine.Incident` schema and its changeset to support casting `:trace_id` according to the patterns in `PATTERNS.md`.</action>
  <verify>
    <automated>mix test test/parapet/spine/incident_test.exs</automated>
  </verify>
  <done>Incident schema supports `trace_id` persistence</done>
</task>

<task type="auto">
  <name>Task 2: Update Operator UI Templates</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex</files>
  <action>Update the LiveView component templates (e.g., `operator_components.ex.eex`) to conditionally render the trace link if `@detail.incident.trace_id` (or appropriate binding) exists. Use `Application.get_env(:parapet, :trace_url_template, "#")` and `String.replace/3` to inject `{trace_id}` into the dynamic URL. Place the trace link gracefully in the UI alongside other external links.</action>
  <verify>
    <automated>mix test test/parapet/operator_ui_compile_out_test.exs</automated>
  </verify>
  <done>UI component code handles `trace_id` rendering using `trace_url_template`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI | Trace link URLs generated in view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | UI Components | mitigate | Fallback gracefully if `trace_url_template` is unconfigured, avoiding injection via unvalidated template strings. |
</threat_model>

<verification>
- `mix compile` passes.
- Schema tests pass.
- Operator UI integration/compilation tests pass.
</verification>

<success_criteria>
- Incidents store `trace_id`.
- The UI properly formats external trace URLs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-open-telemetry-trace-exemplars/01-03-SUMMARY.md`
</output>
