---
phase: 02-database-scale
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/parapet/evidence/archiver.ex
  - test/parapet/evidence/archiver_test.exs
autonomous: true
requirements:
  - SCALE-01
must_haves:
  truths:
    - Operators can archive old incidents to a file on disk.
    - Archiving old incidents removes them from the primary database.
    - The host application's database performance is not degraded during the archival process.
  artifacts:
    - path: lib/parapet/evidence/archiver.ex
      provides: Data Archival logic.
    - path: test/parapet/evidence/archiver_test.exs
      provides: Archiver integration tests.
  key_links:
    - from: Archiver chunk loop
      to: File System
      via: File.write! with :append
      pattern: jsonl
---

<objective>
Implement the `Parapet.Evidence.Archiver` to soft-delete (by exporting to JSONL and hard deleting) old incidents.

Purpose: Automate Ecto table pruning without memory leaks or locking the host DB excessively.
Output: Archiver module and tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/v0.9-phases/2/ASSUMPTIONS.md
@.planning/v0.9-phases/2/RESEARCH.md
@.planning/v0.9-phases/2/PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement `Parapet.Evidence.Archiver`</name>
  <files>lib/parapet/evidence/archiver.ex, test/parapet/evidence/archiver_test.exs</files>
  <behavior>
    - Exposes `archive/3` accepting Repo module, output path, and retention days integer.
    - Wraps operations in `repo.transaction`.
    - Queries `Incident` where `state != "open"` and `inserted_at` is older than `retention_days`.
    - Streams the query, chunking by 100 or max configurable size.
    - Preloads `[timeline_entries: :tool_audits]`.
    - Formats to JSONL and appends to the output file using `File.write!(path, jsonl <> "\n", [:append, :utf8])`.
    - Issues `repo.delete_all` for the chunk's IDs, relying on PostgreSQL cascading deletes to handle timeline entries and tool audits.
  </behavior>
  <action>
    Implement the `Archiver` per `Pattern 2: Chunked Stream + Hard Delete` in `RESEARCH.md`.
    For the test file, insert dummy data including resolved incidents older and newer than the retention window. Assert only older incidents are written to the JSONL file and deleted from the DB.
  </action>
  <verify>
    <automated>mix test test/parapet/evidence/archiver_test.exs</automated>
  </verify>
  <done>Archival executes in memory-safe chunks and correctly exports data while hard-deleting.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Service -> File System | Archiver writes to local disk. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | JSONL Output File | accept | The export file permissions will be default; it's the host environment's responsibility to secure the filesystem. |
| T-02-04 | Denial of Service | Repo Stream memory leak | mitigate | Use strict `Stream.chunk_every` and run within a `transaction` without preloading everything into memory. |
</threat_model>

<verification>
mix test test/parapet/evidence/archiver_test.exs
</verification>

<success_criteria>
Archiver streams, writes JSONL, and leverages `delete_all` successfully without OOM or transaction faults.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-scale/02-02-SUMMARY.md`
</output>
