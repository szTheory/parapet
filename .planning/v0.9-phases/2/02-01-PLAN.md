---
phase: 02-database-scale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/parapet.gen.archive_indexes.ex
  - test/mix/tasks/parapet.gen.archive_indexes_test.exs
  - lib/mix/tasks/parapet.gen.spine.ex
  - test/mix/tasks/parapet.gen.spine_test.exs
autonomous: true
requirements:
  - SCALE-01
must_haves:
  truths:
    - Existing users can upgrade their database with appropriate cascading deletes and indexes.
    - New users get the cascading deletes and correct indexing upon initial installation.
  artifacts:
    - path: lib/mix/tasks/parapet.gen.archive_indexes.ex
      provides: Igniter generator for upgrade migration.
    - path: lib/mix/tasks/parapet.gen.spine.ex
      provides: Initial database schema generator.
  key_links:
    - from: parapet_tool_audits
      to: parapet_timeline_entries
      via: timeline_entry_id constraint
      pattern: on_delete: :delete_all
---

<objective>
Update and create Ecto migrations via Igniter to enforce `on_delete: :delete_all` for `parapet_tool_audits` and add composite indexes to Ecto Evidence tables.

Purpose: Prevent host DB bloat by ensuring cascading deletes handle old tool audits, and speed up UI/archiver queries at >100k row scale.
Output: Igniter generator `parapet.gen.archive_indexes` and updated `parapet.gen.spine`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/v0.9-phases/2/ASSUMPTIONS.md
@.planning/v0.9-phases/2/RESEARCH.md
@.planning/v0.9-phases/2/PATTERNS.md
@lib/mix/tasks/parapet.gen.spine.ex
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create `parapet.gen.archive_indexes` Upgrade Generator</name>
  <files>lib/mix/tasks/parapet.gen.archive_indexes.ex, test/mix/tasks/parapet.gen.archive_indexes_test.exs</files>
  <behavior>
    - Runs Igniter to generate an Ecto migration called "update_parapet_evidence_indexes_and_constraints"
    - The migration has explicit `def up` and `def down` functions
    - `up` drops constraint `parapet_tool_audits_timeline_entry_id_fkey` and alters `parapet_tool_audits` to modify `timeline_entry_id` with `references(:parapet_timeline_entries, type: :binary_id, on_delete: :delete_all)`
    - `up` creates composite indexes: `[:state, :inserted_at]` on `parapet_incidents`, `[:incident_id, :inserted_at]` on `parapet_timeline_entries`, and `[:timeline_entry_id, :inserted_at]` on `parapet_tool_audits`
    - `down` reverts the FK constraint to `:nilify_all` and drops the three composite indexes
  </behavior>
  <action>
    Implement `Mix.Tasks.Parapet.Gen.ArchiveIndexes` using `Igniter.Mix.Task`.
    Use `Igniter.Libs.Ecto.gen_migration/4` to create the migration file with exact `up` and `down` logic as specified in `RESEARCH.md`.
    In the test file, verify that `Igniter.Test.assert_creates_file/2` correctly verifies the AST structure of the generated migration.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/parapet.gen.archive_indexes_test.exs</automated>
  </verify>
  <done>Task generates the upgrade migration correctly and passes tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update `parapet.gen.spine` with new schema definitions</name>
  <files>lib/mix/tasks/parapet.gen.spine.ex, test/mix/tasks/parapet.gen.spine_test.exs</files>
  <behavior>
    - New installs generate `parapet_tool_audits` with `on_delete: :delete_all` instead of `:nilify_all`
    - New installs generate the 3 composite indexes specified in Task 1
  </behavior>
  <action>
    Modify `lib/mix/tasks/parapet.gen.spine.ex` to alter the `create table(:parapet_tool_audits)` block and add the `create index` statements for `[:state, :inserted_at]` on incidents, `[:incident_id, :inserted_at]` on timeline_entries, and `[:timeline_entry_id, :inserted_at]` on tool_audits. Keep existing code intact otherwise. Update its corresponding test file to assert the presence of these new AST segments.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/parapet.gen.spine_test.exs</automated>
  </verify>
  <done>Spine generator correctly updated and tests pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI -> Migrations | Developer executing migrations locally on their host DB. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Migration logic | accept | Migrations execute locally with developer privileges; standard Ecto security assumptions apply. |
| T-02-02 | Denial of Service | DB Table Locks | mitigate | Ensure composite indexes are generated correctly to prevent long-running table lock timeouts. |
</threat_model>

<verification>
mix test test/mix/tasks/parapet.gen.archive_indexes_test.exs test/mix/tasks/parapet.gen.spine_test.exs
</verification>

<success_criteria>
Migrations accurately enforce the `on_delete: :delete_all` constraint required for the hard-delete archiver and add the 3 composite indexes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-scale/02-01-SUMMARY.md`
</output>
