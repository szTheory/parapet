---
phase: 09-reconcile-milestone-closure-artifacts
plan: 04
type: execute
wave: 4
depends_on:
  - 09-03
files_modified:
  - AGENTS.md
autonomous: true
requirements:
  - milestone closure readiness
must_haves:
  truths:
    - "The repo now has a canonical repo-root instruction surface that centralizes the recommendation-first, assumptions-mode, low-escalation doctrine already locked in recent phase context."
    - "`AGENTS.md` documents only existing doctrine; it does not widen product scope, workflow config, or milestone-closure claims."
    - "The doctrine file names the exact escalation triggers already locked in Phase 9 context so future agents stop re-deriving them from phase-local artifacts."
  artifacts:
    - path: AGENTS.md
      provides: Canonical repo-root instruction surface implementing locked decision D-20.
    - path: .planning/v0.9-phases/9/09-CONTEXT.md
      provides: Locked doctrine source for recommendation-first planning, assumptions mode, and escalation boundaries.
  key_links:
    - from: AGENTS.md
      to: .planning/v0.9-phases/9/09-CONTEXT.md
      via: repo-root doctrine mirrors locked decisions D-18 through D-22 without broadening them
      pattern: recommendation-first|assumptions|escalate only
---

<objective>
Implement the remaining locked doctrine decision in a bounded repo-root surface.

Purpose: satisfy D-20 by creating one canonical `AGENTS.md` that captures the repo’s recommendation-first, `assumptions`-mode, low-escalation planning posture without widening this phase into broader governance work.
Output: new `AGENTS.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v0.9-phases/9/09-CONTEXT.md
@.planning/v0.9-phases/9/RESEARCH.md
@.planning/v0.9-phases/9/PATTERNS.md
@.planning/phases/05-multi-node-safety-verification/05-CONTEXT.md
@.planning/v0.9-phases/6/06-CONTEXT.md
@.planning/v0.9-phases/7/07-CONTEXT.md
@.planning/v0.9-phases/8/08-CONTEXT.md
@.planning/v0.9-phases/9/09-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a narrow repo-root doctrine file from already-locked context</name>
  <files>AGENTS.md</files>
  <action>
    Create `AGENTS.md` at repo root as the canonical instruction surface requested by D-20. Keep it narrow and descriptive, not aspirational: document the recommendation-first, codebase-first planning posture; the fact that `workflow.discuss_mode = assumptions` is the default repo preference; and the exact escalation triggers already locked in D-21 and D-22. Do not add new governance, contributor process, coding standards, or product rules that are not already locked by the existing phase context.
  </action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
text = Path("AGENTS.md").read_text()
assert "recommendation-first" in text
assert "assumptions" in text
assert "public CLI/API contract" in text
assert "durable evidence truth model" in text
assert "two medium-impact concerns" in text
print("AGENTS.md captures the locked doctrine and escalation boundaries.")
PY</automated>
  </verify>
  <done>`AGENTS.md` exists and centralizes the repo’s already-locked planning doctrine.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the doctrine file bounded to existing locked scope</name>
  <files>AGENTS.md</files>
  <action>
    Review the new `AGENTS.md` and trim anything that drifts beyond the locked doctrine. The file should help future agents stop re-deriving the same planning posture from phase-local context, but it must not redefine milestone closure semantics, runtime behavior, or product support scope.
  </action>
  <verify>
    <automated>rg -n "recommendation-first|assumptions|escalate only|public CLI/API contract|default install contents|runtime behavior|durable evidence truth model|two medium-impact concerns" AGENTS.md</automated>
  </verify>
  <done>The doctrine file stays bounded to the existing locked posture and does not widen scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-local doctrine -> repo-root doctrine | The new file must faithfully centralize existing locked guidance rather than inventing new policy. |
| Repo-root doctrine -> future planning behavior | The file should reduce repeated ambiguity without broadening milestone or product scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | `AGENTS.md` scope | mitigate | Copy only doctrine already locked in D-18 through D-22 and recent context files. |
| T-09-11 | Denial of Service | governance scope creep | mitigate | Keep the file focused on planning posture and escalation thresholds only. |
</threat_model>

<verification>
- Confirm `AGENTS.md` exists at repo root.
- Confirm it documents recommendation-first planning, assumptions mode, and the locked escalation triggers.
- Confirm it does not claim audit success or redefine product/runtime scope.
</verification>

<success_criteria>
- Locked decision D-20 is implemented by a dedicated, bounded plan.
- Future agents can read one repo-root doctrine surface instead of mining repeated phase-local context files.
- The doctrine file stays within existing locked scope.
</success_criteria>

<output>
After completion, create `.planning/v0.9-phases/9/09-04-SUMMARY.md`
</output>
