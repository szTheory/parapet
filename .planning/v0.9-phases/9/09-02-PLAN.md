---
phase: 09-reconcile-milestone-closure-artifacts
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - milestone closure readiness
must_haves:
  truths:
    - "`.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/STATE.md` all tell the same current-state story."
    - "The live milestone posture is explicit: phase proofs verified, artifacts reconciled, and v0.9 still awaiting a fresh milestone audit."
    - "The active tracker files do not imply that the historical 2026-05-21 audit has already been rerun or passed."
  artifacts:
    - path: .planning/ROADMAP.md
      provides: Active phase ledger updated to the verified, reconciled, re-audit-ready posture.
    - path: .planning/REQUIREMENTS.md
      provides: Internally consistent requirement checklist and traceability rows aligned to existing proof.
    - path: .planning/STATE.md
      provides: Current milestone narrative aligned to the same post-Phase-9 reconciliation position.
  key_links:
    - from: .planning/ROADMAP.md
      to: .planning/v0.9-phases/1/VERIFICATION.md
      via: Phase 9 closure wording cites canonical proof surfaces rather than summaries
      pattern: VERIFICATION.md|re-audit-ready|fresh milestone audit
    - from: .planning/REQUIREMENTS.md
      to: .planning/v0.9-phases/5/VERIFICATION.md
      via: remaining stale checklist truth is reconciled to existing verified evidence
      pattern: SCALE-02|Verified
    - from: .planning/STATE.md
      to: .planning/ROADMAP.md
      via: current-position prose and progress describe the same reconciliation posture
      pattern: re-audit-ready|Phase 9|fresh audit
---

<objective>
Synchronize the live milestone truth surfaces after the proof-adjacent cleanup is complete.

Purpose: implement D-01 through D-07 and D-15 through D-16 by making the top-level tracking files agree on verified proof, reconciled artifacts, and the still-open milestone audit state.
Output: updated `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/STATE.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v0.9-MILESTONE-AUDIT.md
@.planning/v0.9-phases/9/09-CONTEXT.md
@.planning/v0.9-phases/9/RESEARCH.md
@.planning/v0.9-phases/9/PATTERNS.md
@.planning/v0.9-phases/1/VERIFICATION.md
@.planning/v0.9-phases/3/VERIFICATION.md
@.planning/v0.9-phases/4/VERIFICATION.md
@.planning/v0.9-phases/5/VERIFICATION.md
@.planning/v0.9-phases/9/09-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the active roadmap ledger to the verified-but-not-yet-passed milestone posture</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update the Phase 9 roadmap block per D-01, D-02, D-03, D-12, D-13, and D-14 so it explicitly says the earlier verification gaps are now closed by the existing Phase 1, 3, 4, and 5 `VERIFICATION.md` artifacts, while making clear that a fresh milestone audit still needs to be rerun. Keep the historical milestone audit out of the success claim and do not rewrite earlier phase closure lines beyond what is required to keep the active Phase 9 entry accurate.
  </action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
roadmap = Path(".planning/ROADMAP.md").read_text()
start = roadmap.index("### Phase 9: Reconcile Milestone Closure Artifacts")
phase9 = roadmap[start:]
assert "re-audit-ready" in phase9
assert "VERIFICATION.md" in phase9
assert "$gsd-audit-milestone" in phase9 or "fresh milestone audit" in phase9
assert "audit passed" not in phase9.lower()
print("ROADMAP Phase 9 block is aligned to verified/reconciled/re-audit-ready posture.")
PY</automated>
  </verify>
  <done>The active roadmap describes Phase 9 as reconciliation and re-audit readiness, not a retroactive milestone pass.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile requirement checklist truth and traceability rows to existing proof only</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Per D-04, D-05, D-07, and D-16, align `.planning/REQUIREMENTS.md` to the proof that already exists. Fix any remaining internal drift between top-level checklist bullets and the traceability table, especially where a requirement row already says `Verified` but the checklist line is still unchecked. Keep the edits narrow and evidence-backed; do not introduce new requirement IDs, new status taxonomies, or milestone-pass wording.
  </action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
req = Path(".planning/REQUIREMENTS.md").read_text()
assert "- [x] System test suite includes multi-node or concurrency simulation tests verifying that Ecto-backed circuit breakers prevent race conditions when multiple nodes attempt auto-mitigation simultaneously." in req
assert "| PERF-01.a | Phase 6 | Verified |" in req
assert "| PERF-01.b | Phase 6 | Verified |" in req
assert "| SCALE-01.c | Phase 7 | Verified |" in req
assert "| DX-01.a | Phase 8 | Verified |" in req
assert "| DX-01.b | Phase 8 | Verified |" in req
assert "| SCALE-02 | Phase 5 | Verified |" in req
assert "| AC-01 | Phase 8 | Verified |" in req
assert "| AC-03 | Phase 7 | Verified |" in req
print("REQUIREMENTS.md is internally consistent with existing proof rows.")
PY</automated>
  </verify>
  <done>The milestone requirement checklist and traceability table agree with the existing verification artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile project state to the same current truth without claiming milestone closure</name>
  <files>.planning/STATE.md</files>
  <action>
    Update `.planning/STATE.md` per D-01, D-02, D-03, D-12, D-13, and D-14 so the current position moves from "Phase 8 complete" to the post-Phase-9 reality: Phase 9 reconciliation complete or in final closure state, proof gaps closed, and the milestone awaiting a fresh audit rerun. Keep status language short of milestone passed/closed, and align the progress narrative and last-activity notes with the same story told in `ROADMAP.md` and `REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
roadmap = Path(".planning/ROADMAP.md").read_text()
requirements = Path(".planning/REQUIREMENTS.md").read_text()
state = Path(".planning/STATE.md").read_text()
assert "Phase 9" in state
assert "re-audit-ready" in state.lower() or "fresh audit" in state.lower()
assert "Phase 8 complete" not in state
assert "milestone passed" not in state.lower()
assert "audit passed" not in state.lower()
assert "re-audit-ready" in roadmap.lower()
assert "| SCALE-02 | Phase 5 | Verified |" in requirements
assert "audit passed" not in (roadmap + requirements + state).lower()
print("ROADMAP.md, REQUIREMENTS.md, and STATE.md reflect the same re-audit-ready posture.")
PY</automated>
  </verify>
  <done>The project state file matches the roadmap and requirements posture: verified proof, reconciled trackers, fresh audit still pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical verification artifacts -> live tracker files | Present-tense milestone status must be derived from proof artifacts, not historical summaries or the stale audit alone. |
| Live tracker files -> future audit decision | The trackers must stop short of claiming a rerun outcome they do not yet have. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Repudiation | `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md` | mitigate | Tie current-state claims to existing `VERIFICATION.md` artifacts only, per D-04 through D-07. |
| T-09-05 | Tampering | milestone status wording | mitigate | Use the exact verified/reconciled/re-audit-ready posture from D-01 and D-02 and reject "audit passed" wording per D-14. |
| T-09-06 | Denial of Service | tracker drift recurrence | mitigate | Update all three live truth surfaces together in one plan so they cannot diverge immediately again. |
</threat_model>

<verification>
- Confirm the Phase 9 roadmap block cites existing proof and still requires a fresh audit rerun.
- Confirm `REQUIREMENTS.md` checklist bullets and traceability rows agree for all already-verified v0.9 items.
- Confirm `STATE.md` no longer stops at Phase 8 and does not claim milestone pass.
</verification>

<success_criteria>
- The three live tracker files tell one coherent current-state story.
- That story is "verified, reconciled, re-audit-ready", not "milestone passed".
- No historical artifact is rewritten in this plan.
</success_criteria>

<output>
After completion, create `.planning/v0.9-phases/9/09-02-SUMMARY.md`
</output>
