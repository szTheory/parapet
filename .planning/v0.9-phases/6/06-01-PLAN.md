---
phase: 06-verify-cardinality-protection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v0.9-phases/1/VERIFICATION.md
autonomous: true
requirements:
  - PERF-01.a
  - PERF-01.b
must_haves:
  truths:
    - Phase 1 has a dedicated closure-grade `VERIFICATION.md` that proves existing TSDB cardinality protection through rerunnable commands and direct code citations.
    - The verification artifact distinguishes implementation-existence proof from current-behavior proof per D-06, so a live `skip` result is not misreported as requirement-closing evidence.
    - `PERF-01.a` is proven by the doctor implementation plus targeted doctor tests, and `PERF-01.b` is proven by the validator implementation plus forced compile and validator tests.
  artifacts:
    - path: .planning/v0.9-phases/1/VERIFICATION.md
      provides: Phase 1 closure-grade verification report in the repo's v0.9 observable-truth-first format.
    - path: lib/mix/tasks/parapet.doctor.ex
      provides: Implementation-existence anchor for the `cardinality` doctor subcommand and its `skip`/`error` exit semantics.
    - path: lib/parapet/metrics/validator.ex
      provides: Implementation-existence anchor for compile-time label-count and unsafe-label enforcement.
    - path: test/mix/tasks/parapet.doctor_test.exs
      provides: Current-behavior proof lane for safe and unsafe SLO cardinality analysis.
    - path: test/parapet/metrics/validator_test.exs
      provides: Current-behavior proof lane for compile-time validator behavior.
  key_links:
    - from: .planning/v0.9-phases/1/VERIFICATION.md
      to: lib/mix/tasks/parapet.doctor.ex
      via: implementation-existence citations for the `cardinality` check and exit-threshold semantics
      pattern: check_cardinality|Exit codes
    - from: .planning/v0.9-phases/1/VERIFICATION.md
      to: test/mix/tasks/parapet.doctor_test.exs
      via: behavioral proof row for safe and unsafe SLO outcomes
      pattern: cardinality security checks
    - from: .planning/v0.9-phases/1/VERIFICATION.md
      to: lib/parapet/metrics/validator.ex
      via: implementation-existence citations for `@after_compile` and `@max_labels`
      pattern: @after_compile|@max_labels
    - from: .planning/v0.9-phases/1/VERIFICATION.md
      to: test/parapet/metrics/validator_test.exs
      via: behavioral proof row for compile-time label-limit and unsafe-label enforcement
      pattern: exceeds max cardinality limit|High cardinality label rejected
---

<objective>
Create the missing Phase 1 closure-grade verification artifact first, using executable evidence as the primary proof surface.

Purpose: close the audit gap for `PERF-01.a` and `PERF-01.b` without changing product behavior, while keeping the repo's recommendation-first, low-escalation proof posture per D-01 through D-15.
Output: `.planning/v0.9-phases/1/VERIFICATION.md` aligned with the stronger v0.9 verification style used by Phases 2 and 5.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v0.9-MILESTONE-AUDIT.md
@.planning/v0.9-phases/6/06-CONTEXT.md
@.planning/v0.9-phases/6/RESEARCH.md
@.planning/v0.9-phases/6/06-PATTERNS.md
@.planning/v0.9-phases/1/PLAN.md
@.planning/v0.9-phases/2/VERIFICATION.md
@.planning/v0.9-phases/5/VERIFICATION.md
@.planning/phases/01-cardinality-protection/01-01-SUMMARY.md
@.planning/phases/01-cardinality-protection/01-UAT.md
@lib/mix/tasks/parapet.doctor.ex
@lib/parapet/metrics/validator.ex
@lib/parapet/internal/label_policy.ex
@test/mix/tasks/parapet.doctor_test.exs
@test/parapet/metrics/validator_test.exs

<interfaces>
From lib/mix/tasks/parapet.doctor.ex:
```elixir
@static_checks ~w(runbooks router operator_ui endpoint cardinality cluster_static)

defp run_static_check("cardinality"), do: check_cardinality()

# Exit codes:
# - 0: no findings at or above the active threshold
# - 1: at least one finding at or above the active threshold
# - 2: doctor execution failed or a runtime probe could not run
```

From lib/parapet/metrics/validator.ex:
```elixir
defmacro __using__(opts) do
  quote do
    @max_labels Keyword.get(unquote(opts), :max_labels, 10)
    @after_compile __MODULE__
  end
end
```

From lib/parapet/internal/label_policy.ex:
```elixir
def assert_safe!(labels)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the bounded proof lanes and capture the exact evidence boundary per D-04, D-05, and D-06</name>
  <files>.planning/v0.9-phases/1/VERIFICATION.md</files>
  <action>
    Re-run the three primary proof commands from D-04 exactly as written: `mix compile --force --warnings-as-errors`, `mix test test/parapet/metrics/validator_test.exs`, and `mix test test/mix/tasks/parapet.doctor_test.exs`. Record their current outcomes in the verification artifact as current-behavior proof, not as implementation-history notes. Also run `mix parapet.doctor cardinality` once as the advisory spot-check from D-05 and capture its actual workspace result honestly: in this workspace it can legitimately return `skip` when no SLOs are configured, so the report must say that this spot-check confirms command availability and `skip` semantics but does not close `PERF-01.a` on its own. Do not replace targeted tests with shell transcript prose, and do not overreact to the existing deprecation warning from `Parapet.SLO.define/2`; it is out of scope unless it breaks the proof lane.
  </action>
  <verify>
    <automated>mix compile --force --warnings-as-errors && mix test test/parapet/metrics/validator_test.exs && mix test test/mix/tasks/parapet.doctor_test.exs && mix parapet.doctor cardinality</automated>
  </verify>
  <done>The proof commands have been rerun for this session, their outcomes are ready to cite in `.planning/v0.9-phases/1/VERIFICATION.md`, and the advisory live doctor invocation is explicitly bounded as non-primary evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase 1 verification report in the v0.9 hybrid proof format per D-01, D-02, D-03, and D-07</name>
  <files>.planning/v0.9-phases/1/VERIFICATION.md</files>
  <action>
    Create `.planning/v0.9-phases/1/VERIFICATION.md` using the same frontmatter and section order as `.planning/v0.9-phases/2/VERIFICATION.md` and `.planning/v0.9-phases/5/VERIFICATION.md`: top summary, `Goal Achievement`, `Observable Truths`, `Behavioral Spot-Checks`, `Plan Output Check`, `Requirements Coverage`, `Human Verification Required`, and `Gaps Summary`. Lead with observable truths rather than Phase 1 task order per D-02. Separate proof-of-existence from proof-of-current-behavior per D-06: cite `lib/parapet/metrics/validator.ex`, `lib/parapet/internal/label_policy.ex`, and `lib/mix/tasks/parapet.doctor.ex` as the implementation anchors, then cite the fresh compile and targeted test runs as behavioral proof. Make the `Behavioral Spot-Checks` table explicit about current known outcomes in this workspace: `mix compile --force --warnings-as-errors` succeeds, `mix test test/parapet/metrics/validator_test.exs` is `3 tests, 0 failures`, `mix test test/mix/tasks/parapet.doctor_test.exs` is `10 tests, 0 failures`, and `mix parapet.doctor cardinality` currently returns `skip` because no SLOs are configured. In `Requirements Coverage`, mark `PERF-01.a` satisfied only through doctor implementation citations plus targeted doctor tests, and mark `PERF-01.b` satisfied through validator implementation citations plus forced compile and validator tests. Keep the tone calm, evidence-first, and low-noise per D-11 through D-13.
  </action>
  <verify>
    <automated>test -f .planning/v0.9-phases/1/VERIFICATION.md && rg -n "Observable Truths|Behavioral Spot-Checks|Requirements Coverage|skip" .planning/v0.9-phases/1/VERIFICATION.md</automated>
  </verify>
  <done>The new verification report exists, matches the repo's v0.9 verification structure, and makes the implementation-vs-behavior distinction explicit without treating a live `skip` as sufficient proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mix proof commands -> planning artifact | Command outcomes must be represented faithfully or the repo can falsely close a safety requirement. |
| Source citations -> audit conclusions | File-level existence proof must not be conflated with current-behavior proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Repudiation | `.planning/v0.9-phases/1/VERIFICATION.md` | mitigate | Cite exact rerun commands and direct source anchors so the report is re-checkable rather than summary-only. |
| T-06-02 | Tampering | doctor proof surface | mitigate | State explicitly that `mix parapet.doctor cardinality` returning `skip` only proves workspace posture and command semantics, not requirement closure by itself. |
| T-06-03 | Information Disclosure | verification narrative scope | accept | The report cites repository code and local test output only; no secrets or external runtime data are involved. |
</threat_model>

<verification>
- Run `mix compile --force --warnings-as-errors`.
- Run `mix test test/parapet/metrics/validator_test.exs`.
- Run `mix test test/mix/tasks/parapet.doctor_test.exs`.
- Run `mix parapet.doctor cardinality` only as an advisory spot-check and ensure the report explains its `skip` boundary honestly.
</verification>

<success_criteria>
- `.planning/v0.9-phases/1/VERIFICATION.md` exists and is closure-grade, not summary-grade.
- The report proves `PERF-01.a` and `PERF-01.b` through executable evidence first and code citations second.
- The artifact explicitly distinguishes implementation existence from current behavior and preserves recommendation-first, low-escalation repo posture.
</success_criteria>

<output>
After completion, create `.planning/v0.9-phases/6/06-01-SUMMARY.md`
</output>
