---
phase: 43-collateral-wiring
plan: 03
type: execute
wave: 3
depends_on: ["43-01", "43-02"]
files_modified:
  - .planning/milestones/v1.5-MILESTONE-AUDIT.md
  - .planning/MILESTONES.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [COLLAT-03]

must_haves:
  truths:
    - "Off-palette hex audit over brandbook/assets/*.svg AND brandbook/examples/* prints OK"
    - "Binary scan over brandbook/ produces zero hits (no png/jpg/woff/ttf/otf)"
    - "du -sh brandbook is within the <= ~250 KB budget"
    - "git diff --name-only is scoped exclusively to brandbook/, docs/assets/*.svg, and (no) mix.exs change plus .planning/ docs"
    - "v1.5-MILESTONE-AUDIT.md exists mirroring the v1.4 format; MILESTONES.md has a v1.5 entry; COLLAT-01..03 marked complete in REQUIREMENTS.md; Phase 43 + progress table checked in ROADMAP.md"
  artifacts:
    - path: ".planning/milestones/v1.5-MILESTONE-AUDIT.md"
      provides: "v1.5 milestone audit mirroring v1.4 format"
      contains: "milestone: v1.5"
    - path: ".planning/MILESTONES.md"
      provides: "v1.5 shipped entry (newest first)"
      contains: "v1.5"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "COLLAT-01..03"
      via: "Traceability table rows marked Complete"
      pattern: "COLLAT-0[123].*Complete"
    - from: ".planning/ROADMAP.md"
      to: "Phase 43"
      via: "Phase 43 checkbox + progress table updated to Complete"
      pattern: "\\[x\\].*Phase 43"
---

<objective>
Run the COLLAT-03 repo-hygiene QA gate over the finished brand book + collateral, then write the v1.5 milestone audit and update the planning ledger (MILESTONES.md, REQUIREMENTS.md traceability, ROADMAP.md Phase 43 + progress table). This plan scopes the entire milestone diff and marks the requirements complete, so it must run last.

Purpose: Prove the brand book stays repo-lean, palette-clean, binary-free, and scoped, and produce the milestone-close artifacts mirroring the v1.4 audit format.
Output: `.planning/milestones/v1.5-MILESTONE-AUDIT.md`, updated `.planning/MILESTONES.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@.planning/milestones/v1.4-MILESTONE-AUDIT.md
@.planning/phases/43-collateral-wiring/43-CONTEXT.md
@.planning/phases/43-collateral-wiring/43-RESEARCH.md
</context>

<artifacts_produced>
This plan produces:
- `.planning/milestones/v1.5-MILESTONE-AUDIT.md` — v1.5 milestone audit (mirrors v1.4 structure: frontmatter scores, requirements coverage, phase verification, cross-phase integration, E2E flows, Nyquist note, verification commands, result)
- `.planning/MILESTONES.md` — new top-of-file v1.5 entry (Phases completed / Key accomplishments / Audit line)
- `.planning/REQUIREMENTS.md` — COLLAT-01, COLLAT-02, COLLAT-03 marked complete (checkboxes + Traceability table)
- `.planning/ROADMAP.md` — Phase 43 checkbox + Progress Table updated to Complete
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Run the COLLAT-03 QA/hygiene gate and record results</name>
  <files>.planning/phases/43-collateral-wiring/43-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/43-collateral-wiring/43-CONTEXT.md (QA checks section) — the four QA commands verbatim
    - .planning/phases/43-collateral-wiring/43-RESEARCH.md §4 (QA/Audit Mechanics) — palette allow-list (12 hex), binary scan, size budget, scoped-diff expectation
  </read_first>
  <action>
    Run the full COLLAT-03 QA gate from the repo root and record each pass/fail in the SUMMARY. (1) Off-palette hex audit — run the palette grep over `brandbook/assets/*.svg` AND extend it to `brandbook/examples/*.svg` and the non-token hex in `brandbook/examples/*.html`; the allow-list is the 12 palette values `101820 18232B 2E3A42 D8D0C3 EAE2D4 F8F4EC 256C82 B45309 D97706 567236 B13A32 6D5BD0` (HTML/CSS may additionally contain status-set colors, which are NOT in SVGs). (2) Binary scan — `find brandbook \( -name '*.png' -o -name '*.jpg' -o -name '*.woff*' -o -name '*.ttf' -o -name '*.otf' \) -print` must produce no output. (3) Size budget — `du -sh brandbook` must be ≤ ~250 KB. (4) Scoped diff — `git diff --name-only HEAD` (and untracked via `git status --porcelain`) must be limited to `brandbook/**`, `docs/assets/*.svg`, and `.planning/**`; `mix.exs` must NOT appear.

    If any check fails, STOP and fix the producing artifact (do not paper over it in the audit). Record the exact command outputs in the SUMMARY as evidence.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && PAL='101820|18232B|2E3A42|D8D0C3|EAE2D4|F8F4EC|256C82|B45309|D97706|567236|B13A32|6D5BD0'; grep -rohiE '#[0-9a-f]{6}' brandbook/assets/*.svg brandbook/examples/*.svg 2>/dev/null | sort -u | grep -viE "$PAL" | grep -q . && { echo "OFF-PALETTE FOUND"; exit 1; } || echo "PALETTE OK"; test -z "$(find brandbook \( -name '*.png' -o -name '*.jpg' -o -name '*.woff*' -o -name '*.ttf' -o -name '*.otf' \) -print)" && echo "BINARIES OK" || { echo "BINARY FOUND"; exit 1; }; SZ=$(du -sk brandbook | cut -f1); test "$SZ" -le 256 && echo "SIZE OK ($SZ KB)" || { echo "OVER BUDGET ($SZ KB)"; exit 1; }; git status --porcelain | awk '{print $2}' | grep -vE '^(brandbook/|docs/assets/.*\.svg|\.planning/)' | grep -q . && { echo "OUT-OF-SCOPE DIFF"; git status --porcelain; exit 1; } || echo "SCOPE OK"</automated>
  </verify>
  <acceptance_criteria>
    - Palette grep over `brandbook/assets/*.svg` + `brandbook/examples/*.svg` prints `PALETTE OK` (zero off-palette hex)
    - Binary scan prints `BINARIES OK` (zero png/jpg/woff/ttf/otf in brandbook/)
    - `du -sk brandbook` ≤ 256 KB → prints `SIZE OK`
    - `git status --porcelain` scope check prints `SCOPE OK` (only `brandbook/`, `docs/assets/*.svg`, `.planning/`; no `mix.exs`)
    - All four outputs recorded verbatim in 43-03-SUMMARY.md
  </acceptance_criteria>
  <done>The COLLAT-03 QA gate passes all four checks (palette, binaries, size, scope) and the evidence is recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Write v1.5 milestone audit + update MILESTONES.md</name>
  <files>.planning/milestones/v1.5-MILESTONE-AUDIT.md, .planning/MILESTONES.md</files>
  <read_first>
    - .planning/milestones/v1.4-MILESTONE-AUDIT.md — the exact format to mirror (frontmatter scores/gaps/nyquist; body sections: Summary, Requirements Coverage table, Phase Verification table, Cross-Phase Integration, E2E Flows, Nyquist Coverage, Verification Commands, Result)
    - .planning/MILESTONES.md — entry format (H2 `## vX.Y Name (Shipped: YYYY-MM-DD)` + Phases completed + Key accomplishments + Audit line + Known deferred items), newest-first at top
    - .planning/REQUIREMENTS.md — the 13 v1.5 requirements and their phase mapping
    - .planning/phases/43-collateral-wiring/43-RESEARCH.md §7 (Milestone Audit Format) — the v1.5 frontmatter template
  </read_first>
  <action>
    Create `.planning/milestones/v1.5-MILESTONE-AUDIT.md` mirroring the v1.4 audit. Frontmatter: `milestone: v1.5`, `milestone_name: Brand Book & Logo System`, `audited: <ISO datetime>`, `status: passed`, `scores: requirements: 13/13`, `phases: 4/4` (Phases 40, 41, 42, 43), integration/flows counts you derive from the artifacts, empty `gaps`, empty `tech_debt`, `nyquist.compliant_phases: [40, 41, 42, 43]`, `overall: compliant`. Body sections in v1.4 order: a Summary paragraph; a Requirements Coverage table covering all 13 v1.5 requirements (BRAND-01..03, LOGO-01..04, TOKEN-01..03, COLLAT-01..03) with phase + evidence + final status; a Phase Verification table (40/41/42/43 with plans + result); a Cross-Phase Integration table tracing token system → brand book → collateral → HexDocs swap; an End-to-End Flows section (e.g. "Maintainer opens brand book / examples from file:// and sees the locked logo", "HexDocs visitor sees the on-brand mark after mix docs"); a Nyquist Coverage section noting explicitly that v1.5 is brand/design assets with NO ExUnit tests — the validation surface is the COLLAT-03 bash QA gate + `mix docs` + headless-Chrome; a Verification Commands section listing the QA bash commands + `mix docs`; and a Result line.

    Then prepend a v1.5 entry to the TOP of `.planning/MILESTONES.md` (newest-first): `## v1.5 Brand Book & Logo System (Shipped: 2026-06-24)`, with `**Phases completed:**` (4 phases, plan count, task count), a `**Key accomplishments:**` bullet list (distilled brand reference + WCAG matrix + anti-criteria; locked corbelled-tower identity over 6 rounds; tokens.css/json + self-contained brand book; collateral examples + zero-config HexDocs swap + repo-hygiene gate), an `**Audit:**` one-liner linking `milestones/v1.5-MILESTONE-AUDIT.md`, and `**Known deferred items at close:**` listing the deferred future requirements (self-hosted webfont, raster exports, Tailwind/daisyUI generator, Operator UI retheme, animated logo/Figma/PDF).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && test -f .planning/milestones/v1.5-MILESTONE-AUDIT.md && grep -q 'milestone: v1.5' .planning/milestones/v1.5-MILESTONE-AUDIT.md && grep -q 'requirements: 13/13' .planning/milestones/v1.5-MILESTONE-AUDIT.md && grep -q 'COLLAT-01' .planning/milestones/v1.5-MILESTONE-AUDIT.md && grep -q 'COLLAT-03' .planning/milestones/v1.5-MILESTONE-AUDIT.md && head -5 .planning/MILESTONES.md | grep -q 'v1.5' && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.5-MILESTONE-AUDIT.md` exists with `milestone: v1.5`, `requirements: 13/13`, `phases: 4/4`, and `nyquist.compliant_phases: [40, 41, 42, 43]`
    - The audit body has all v1.4-equivalent sections and covers all 13 requirements including COLLAT-01..03
    - `.planning/MILESTONES.md` has the v1.5 entry at the TOP (above v1.4)
    - The Nyquist section explicitly states v1.5 has no ExUnit tests and the QA gate is the validation surface
  </acceptance_criteria>
  <done>v1.5-MILESTONE-AUDIT.md mirrors the v1.4 format with 13/13 requirements + 4/4 phases, and MILESTONES.md leads with the v1.5 entry.</done>
</task>

<task type="auto">
  <name>Task 3: Mark COLLAT-01..03 complete in REQUIREMENTS.md + ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — Collateral checklist (lines ~33-35) and Traceability table rows for COLLAT-01..03
    - .planning/ROADMAP.md — Phase 43 checkbox (line ~25), Phase 43 detail block, and the Progress Table row "43. Collateral, Wiring & QA/Audit Gate"
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`: change the three Collateral checkboxes from `- [ ]` to `- [x]` for COLLAT-01, COLLAT-02, COLLAT-03, and update the Traceability table rows so COLLAT-01/02/03 read `Complete` instead of `Pending`.

    In `.planning/ROADMAP.md`: tick the Phase 43 summary checkbox (`- [ ] **Phase 43:` → `- [x] **Phase 43:`) and remove the `*(blocked on Phase 42)*` note; update the Progress Table row for Phase 43 to `3/3 | Complete | 2026-06-24`; and update the Phase 43 detail block `**Plans**: TBD` to `**Plans**: 3 plans` with the plan list (43-01 collateral examples, 43-02 HexDocs swap + cleanup, 43-03 QA gate + milestone audit).

    Use scoped Edit replacements — do NOT rewrite either whole file; touch only the COLLAT/Phase-43 lines.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/parapet && grep -q '\[x\] \*\*COLLAT-01\*\*' .planning/REQUIREMENTS.md && grep -q '\[x\] \*\*COLLAT-02\*\*' .planning/REQUIREMENTS.md && grep -q '\[x\] \*\*COLLAT-03\*\*' .planning/REQUIREMENTS.md && grep -E 'COLLAT-0[123].*Complete' .planning/REQUIREMENTS.md | wc -l | grep -q 3 && grep -q '\[x\] \*\*Phase 43' .planning/ROADMAP.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md shows `- [x] **COLLAT-01**`, `- [x] **COLLAT-02**`, `- [x] **COLLAT-03**`
    - REQUIREMENTS.md Traceability table shows COLLAT-01, COLLAT-02, COLLAT-03 as `Complete`
    - ROADMAP.md Phase 43 summary checkbox is `[x]` and the blocked-on note is gone
    - ROADMAP.md Progress Table row for Phase 43 reads `3/3 | Complete | 2026-06-24`
    - ROADMAP.md Phase 43 `**Plans**:` lists the 3 plans
  </acceptance_criteria>
  <done>COLLAT-01..03 are marked complete in REQUIREMENTS.md (checkboxes + traceability) and Phase 43 is ticked with a 3/3 Complete progress row and plan list in ROADMAP.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan only reads brand artifacts and writes planning ledger docs. No runtime trust boundary; the QA gate is itself the tampering control for the milestone.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03a | Tampering | Off-palette / binary / oversize content slipping into the shipped brand book | mitigate | The COLLAT-03 QA gate (palette grep + binary find + du + scoped git status) gates the milestone; failures stop the plan |
| T-43-03b | Repudiation | Milestone marked complete without evidence | mitigate | Audit records verbatim QA command outputs; REQUIREMENTS/ROADMAP edits cross-reference the audit file |
| T-43-03c | Tampering | Out-of-scope files committed under cover of the milestone | mitigate | `git status --porcelain` scope check asserts only `brandbook/`, `docs/assets/*.svg`, `.planning/`; `mix.exs` must not appear |
</threat_model>

<verification>
- COLLAT-03 QA gate: palette OK, binaries OK, size OK, scope OK (all recorded)
- v1.5-MILESTONE-AUDIT.md exists mirroring v1.4 (13/13 req, 4/4 phases, nyquist note)
- MILESTONES.md leads with v1.5 entry
- REQUIREMENTS.md: COLLAT-01..03 checked + Complete in traceability
- ROADMAP.md: Phase 43 checked, 3/3 Complete progress row, plan list filled
</verification>

<success_criteria>
ROADMAP Phase 43 success criteria 3 and 4 are satisfied: the repo-hygiene audit passes (size ≤ ~250 KB, zero binaries, no full-viewBox cage, no off-palette hex), the milestone diff is scoped exclusively to `brandbook/`, the two `docs/assets/*.svg`, and `.planning/` docs (no `mix.exs` change), and the milestone-close ledger (audit + MILESTONES + REQUIREMENTS + ROADMAP) is written.
</success_criteria>

<output>
Create `.planning/phases/43-collateral-wiring/43-03-SUMMARY.md` when done.
</output>
