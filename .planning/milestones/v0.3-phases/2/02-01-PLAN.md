---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/runbook.ex
  - priv/repo/migrations/YYYYMMDDHHMMSS_add_runbook_data_to_incidents.exs
  - lib/parapet/spine/incident.ex
  - lib/parapet/spine/alert_processor.ex
  - test/parapet/runbook_test.exs
autonomous: true
requirements: [RUNBOOK-01, RUNBOOK-02]
must_haves:
  truths:
    - User can define a runbook using `Parapet.Runbook` DSL.
    - Incident schema can store runbook data as a snapshot.
  artifacts:
    - path: lib/parapet/runbook.ex
      provides: Macro DSL for runbooks
    - path: lib/parapet/spine/incident.ex
      provides: runbook_data field
    - path: lib/parapet/spine/alert_processor.ex
      provides: attach runbook data logic
  key_links: []
---

<objective>
# 02-01-PLAN.md — Create the `Parapet.Runbook` DSL and mapping logic

**Description:** Implement `Parapet.Runbook` as a module-based DSL using Elixir macros to fulfill RUNBOOK-01 and RUNBOOK-02. Add a `runbook_data` field to the `Incident` Ecto schema to preserve immutable facts through JSON snapshotting. Attach the mapped runbook snapshot automatically when an incident is generated.
</objective>

<context>
@.planning/milestones/v0.3-phases/2/PATTERNS.md
@.planning/milestones/v0.3-phases/2/RESEARCH.md
</context>

<dependencies>
This is the first plan in Phase 2, Wave 1. No dependencies.
</dependencies>

<goal_backward_verification>
**How do we know this specific plan is done?**
- A new file `lib/parapet/runbook.ex` exists and exposes `__using__`, `step`, `title`, and `description` macros.
- A database migration adds `runbook_data` of type `:map` to the `parapet_incidents` table.
- `lib/parapet/spine/incident.ex` casts the `runbook_data` field.
- `Parapet.Spine.AlertProcessor` automatically attaches the runbook snapshot when processing a firing alert, looking up the runbook module by alertname.
- `mix test test/parapet/runbook_test.exs` passes verifying that the DSL generates a static schema map via `__runbook_schema__()`.
</goal_backward_verification>

<tasks>
<task type="auto" tdd="true">
  <name>Step-by-Step Task Breakdown</name>
  <files>
    - lib/parapet/runbook.ex
    - priv/repo/migrations/YYYYMMDDHHMMSS_add_runbook_data_to_incidents.exs
    - test/parapet/runbook_test.exs
    - lib/parapet/spine/incident.ex
    - lib/parapet/spine/alert_processor.ex
  </files>
  <action>
  - [ ] **Generate Migration:** Create a new migration file `priv/repo/migrations/*_add_runbook_data_to_incidents.exs` adding `add :runbook_data, :map` to the `parapet_incidents` table.
  - [ ] **Update Incident Schema:** Add `field :runbook_data, :map` to `lib/parapet/spine/incident.ex` and include it in the `cast` parameters of the `changeset/2` function.
  - [ ] **Implement DSL Scaffold:** Create `lib/parapet/runbook.ex` providing a `__using__` macro that registers a `:steps` module attribute (accumulate: true) and `__before_compile__` hook. Provide a default `def execute_mitigation(_step, _incident), do: {:error, :not_implemented}` with `defoverridable`.
  - [ ] **Implement DSL Macros:** Inside `lib/parapet/runbook.ex`, create `title/1`, `description/1`, and `step/2` macros to populate the module attributes.
  - [ ] **Implement Schema Hook:** Implement `__before_compile__` to define a `__runbook_schema__()` function that returns a map with `%{module: to_string(__MODULE__), title: title, description: description, steps: steps}`.
  - [ ] **Runbook Mapping & Attachment:** Update `Parapet.Spine.AlertProcessor.process_firing_alert/1` to look up the corresponding runbook module for the incoming alert (e.g., matching alert name to an SLOs runbook module). If found, call `__runbook_schema__()` and put the result in the `runbook_data` field of the new `Incident`.
  - [ ] **Write Tests:** Create `test/parapet/runbook_test.exs` to define a test runbook module, calling its `__runbook_schema__()` function to assert the output matches the expected JSON-compatible schema map structure, and test the `AlertProcessor` modification.
  </action>
  <verify>
    <automated>mix test test/parapet/runbook_test.exs || echo "Tests pending implementation"</automated>
  </verify>
  <done>All checkboxes are implemented and tests pass.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Incident Schema | mitigate | Ecto type checking of `runbook_data` as `:map` prevents execution injection at the storage layer. |
</threat_model>