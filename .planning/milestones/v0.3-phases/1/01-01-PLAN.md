---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/plug/webhook.ex
  - test/parapet/plug/webhook_test.exs
  - lib/parapet/spine/alert_processor.ex
autonomous: true
requirements:
  - ROUTING-01
must_haves:
  truths:
    - Host applications can mount a Plug to receive webhooks
    - Valid JSON payload returns 202 Accepted
    - Invalid HTTP methods return 405 Method Not Allowed
  artifacts:
    - path: lib/parapet/plug/webhook.ex
      provides: Webhook receiver interface
    - path: lib/parapet/spine/alert_processor.ex
      provides: Stubbed processor interface
  key_links:
    - from: lib/parapet/plug/webhook.ex
      to: lib/parapet/spine/alert_processor.ex
      via: process_batch/1 call
---

<objective>
Create the webhook receiver endpoint plug and routing logic.

Purpose: To expose a `Plug` that host applications can mount in their router to receive Prometheus Alertmanager webhooks.
Output: A testable Plug that delegates payload processing to the `AlertProcessor` context.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v0.3-phases/1/PATTERNS.md
@.planning/milestones/v0.3-phases/1/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define AlertProcessor skeleton interface</name>
  <files>lib/parapet/spine/alert_processor.ex</files>
  <action>
    Create the module `Parapet.Spine.AlertProcessor`.
    Define a public function `process_batch(payload)` that currently does nothing or returns `:ok`. 
    This establishes the contract that the Webhook plug will call. We build this interface first so the Plug can depend on it immediately.
  </action>
  <verify>
    <automated>elixirc lib/parapet/spine/alert_processor.ex</automated>
  </verify>
  <done>File exists and successfully compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Webhook Plug</name>
  <files>lib/parapet/plug/webhook.ex, test/parapet/plug/webhook_test.exs</files>
  <behavior>
    - Test 1: POST request with body returns 202 Accepted and JSON status 'accepted'
    - Test 2: GET request returns 405 Method Not Allowed
    - Test 3: Ensure Plug calls AlertProcessor.process_batch(payload) (can be tested implicitly or via a lightweight mock if needed)
  </behavior>
  <action>
    Implement `Parapet.Plug.Webhook` following the standard Plug behaviour (`init/1` and `call/2`).
    *Note: Reference analog pattern from `lib/parapet/plug/metrics.ex` (via PATTERNS.md) for core Plug setup:*
    ```elixir
      @behaviour Plug
      import Plug.Conn
      @impl true
      def init(opts), do: opts
      @impl true
      def call(conn, _opts) do
    ```
    Pattern match on `%{method: "POST"}` for the call.
    Extract the parsed JSON from `conn.body_params`. (Assume the host application has already parsed it using `Plug.Parsers`).
    Call `Parapet.Spine.AlertProcessor.process_batch(conn.body_params)`.
    Return a `202 Accepted` response with content type `application/json` and `{"status": "accepted"}` encoded via Jason.
    For any other HTTP methods, return `405 Method Not Allowed`.
  </action>
  <verify>
    <automated>mix test test/parapet/plug/webhook_test.exs</automated>
  </verify>
  <done>Plug receives POST requests, delegates to AlertProcessor, and returns appropriate responses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External → Plug | Webhooks from external Alertmanager instances enter the system here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | Webhook Plug | accept | Assume host application implements rate limiting and payload size limits in the router pipeline before the Plug. |
| T-01-02 | Authentication | Webhook Plug | accept | Assume host application handles authentication (e.g., via basic auth or secret headers) before reaching Parapet's plug. |
</threat_model>

<verification>
- `mix test test/parapet/plug/webhook_test.exs` passes.
- No compiler warnings.
</verification>

<success_criteria>
System provides a standalone webhook plug that receives POSTed JSON from Alertmanager and routes it to the domain context.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3-phases/1/01-01-SUMMARY.md`
</output>
