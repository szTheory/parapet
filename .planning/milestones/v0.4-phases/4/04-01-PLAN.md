---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/spine/action_item.ex
  - lib/parapet/evidence.ex
  - lib/mix/tasks/parapet.gen.spine.ex
  - test/parapet/spine/action_item_test.exs
  - test/parapet/evidence/action_item_test.exs
autonomous: true
requirements:
  - AI-HITL-01
must_haves:
  truths:
    - "System persists workflow approval state across restarts."
    - "Operators can track which workflows are waiting for human intervention."
  artifacts:
    - path: "lib/parapet/spine/action_item.ex"
      provides: "ActionItem Ecto Schema"
      contains: "schema \"parapet_action_items\""
    - path: "lib/parapet/evidence.ex"
      provides: "Context API for ActionItems"
      contains: "def create_action_item"
  key_links:
    - from: "lib/parapet/evidence.ex"
      to: "lib/parapet/spine/action_item.ex"
      via: "Ecto inserts and updates"
      pattern: "ActionItem.changeset"
---

<objective>
Implement the ActionItem domain model to store durable pointers to Scoria workflow approvals without duplicating state.

Purpose: AI-HITL-01 requires monitoring workflow approval pauses. A durable pointer (ActionItem) is needed for Track 2 observability.
Output: `Parapet.Spine.ActionItem` schema and Evidence context methods.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/parapet/spine/incident.ex
@lib/parapet/evidence.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ActionItem Schema</name>
  <files>lib/parapet/spine/action_item.ex, test/parapet/spine/action_item_test.exs</files>
  <behavior>
    - Test 1: Changeset requires title, integration, and external_id.
    - Test 2: Changeset state defaults to "open".
    - Test 3: State can be "open" or "resolved".
  </behavior>
  <action>Create Ecto Schema `Parapet.Spine.ActionItem` mapping to `parapet_action_items`. Use the Core Ecto Schema and Changeset Validation patterns from `lib/parapet/spine/incident.ex` as noted in PATTERNS.md. Fields: `id` (binary_id), `title` (string), `integration` (string), `external_id` (string), `state` (string, default "open"). Add `timestamps()`. Implement `changeset/2`.</action>
  <verify>
    <automated>mix test test/parapet/spine/action_item_test.exs</automated>
  </verify>
  <done>Schema and tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evidence API for ActionItems</name>
  <files>lib/parapet/evidence.ex, test/parapet/evidence/action_item_test.exs</files>
  <behavior>
    - Test 1: `create_action_item/1` inserts an ActionItem.
    - Test 2: `resolve_action_item/1` idempotently marks an ActionItem as "resolved".
  </behavior>
  <action>Add `create_action_item(attrs)` and `resolve_action_item(id)` to `Parapet.Evidence`. `resolve_action_item` should update state to "resolved". Follow the Idempotent Ecto Insertion pattern from `lib/parapet/evidence.ex` as noted in PATTERNS.md. Rely on existing test setup to mock the Ecto repo if needed.</action>
  <verify>
    <automated>mix test test/parapet/evidence/action_item_test.exs</automated>
  </verify>
  <done>Context functions work and are tested.</done>
</task>

<task type="auto">
  <name>Task 3: Update Generator Migration</name>
  <files>lib/mix/tasks/parapet.gen.spine.ex</files>
  <action>Modify the generator's `add_parapet_spine_tables` migration body to include `create table(:parapet_action_items, primary_key: false) do ... end`. Match the fields from Task 1.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Generator updated to scaffold ActionItem table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> App | Database schema and Ecto Changeset validations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | ActionItem Changeset | mitigate | Enforce `validate_inclusion` for `state` and `validate_required` for essential fields. |
</threat_model>
