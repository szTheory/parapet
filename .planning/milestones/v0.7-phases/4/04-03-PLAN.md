---
phase: 04
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/parapet/integrations/rindle.ex
  - test/parapet/integrations/rindle_test.exs
autonomous: true
requirements:
  - TRIAGE-01
must_haves:
  truths:
    - "Rindle emits normalized async event families instead of coarse journey success/failure events."
    - "Retryable async failure remains distinct from discarded or exhausted work."
    - "Callback or reconciliation delay remains distinct from queue backlog."
  artifacts:
    - path: "lib/parapet/integrations/rindle.ex"
      provides: "Rindle translation onto the Phase 4 async contract"
  key_links:
    - from: "lib/parapet/integrations/rindle.ex"
      to: "lib/parapet/telemetry/async_delivery.ex"
      via: "shared async contract helpers"
---

<objective>
Migrate `Rindle` to the normalized Phase 4 async contract.

Purpose: give later phases a bounded async vocabulary that separates stage progress, backlog symptoms, callback delay, retryable failure, and discarded work.
Output: a `Rindle` adapter and tests that emit `[:parapet, :async, ...]` public events with bounded metadata.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-phases/4/4-CONTEXT.md
@.planning/v0.7-phases/4/RESEARCH.md
@.planning/v0.7-phases/4/PATTERNS.md
@.planning/v0.7-phases/4/04-01-PLAN.md
@lib/parapet/integrations/rindle.ex
@lib/parapet/integrations/sigra.ex
@lib/parapet/integrations/scoria.ex
@test/parapet/integrations/rindle_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rework Rindle onto normalized async families</name>
  <files>lib/parapet/integrations/rindle.ex, test/parapet/integrations/rindle_test.exs</files>
  <behavior>
    - Test 1: stage progress emits `[:parapet, :async, :stage]` with bounded `pipeline_stage`, `outcome`, and `retry_state` fields.
    - Test 2: retryable async failures emit a bounded retryable outcome and do not collapse into terminal discard.
    - Test 3: discarded or exhausted work emits a distinct bounded terminal outcome.
    - Test 4: queue backlog emits `[:parapet, :async, :backlog]`.
    - Test 5: callback or reconciliation delay emits `[:parapet, :async, :callback]` and does not masquerade as generic backlog.
    - Test 6: high-cardinality refs such as job ids or media ids are absent from public tags.
  </behavior>
  <action>Rewrite `Parapet.Integrations.Rindle` to emit the new async families using explicit event-pattern clauses and the shared contract helpers from plan 01. Replace the current `:success` / `:failure` plus pass-through metadata shape with bounded async outcomes such as `started`, `succeeded`, `retryable_failed`, `discarded`, and `delayed`, along with safe `queue`, `pipeline_stage`, `delay_bucket`, `fault_plane`, and `retry_state` fields as supported by the upstream events. Preserve the explicit adapter seam and handler crash safety.</action>
  <verify>
    <automated>mix test test/parapet/integrations/rindle_test.exs</automated>
  </verify>
  <done>Rindle emits the normalized async contract and its tests prove backlog/callback separation plus retry-state semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Async worker and callback metadata -> public async telemetry | Internal execution payloads cross into public Parapet events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | Rindle adapter | mitigate | Keep queue and stage metadata bounded and route job/media identifiers out of label-safe fields. |
| T-04-03-02 | Tampering | Async outcome normalization | mitigate | Encode retryable vs discarded outcomes explicitly in the contract helper layer and verify through focused tests. |
</threat_model>

<verification>
- Run the focused Rindle integration test file.
- Confirm `Rindle` still participates in explicit host-owned adapter activation rather than hidden auto-discovery.
- Confirm the public async families separate stage, backlog, and callback signals.
</verification>

<success_criteria>
- Rindle no longer emits the old coarse journey event as the primary public contract.
- Retryable failure, discard, backlog, and callback delay are distinguishable through bounded public fields.
- Async public metadata remains label-safe and low cardinality.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/4/04-03-SUMMARY.md`
</output>
