---
phase: 04
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - docs/telemetry.md
  - README.md
  - test/parapet_test.exs
  - test/mix/tasks/verify.public_api_test.exs
autonomous: true
requirements:
  - DELV-01
  - TRIAGE-01
must_haves:
  truths:
    - "The new async/delivery telemetry contract is documented as public API."
    - "Adopters have one blessed activation and verification path for the new integrations."
    - "Any newly public contract module is covered by the repo's public API proof surface."
    - "Explicit host-owned activation and optional compile-out behavior remain intact for Mailglass, Chimeway, and Rindle."
  artifacts:
    - path: "docs/telemetry.md"
      provides: "Phase 4 public contract documentation"
    - path: "README.md"
      provides: "High-level adopter guidance for the new async/delivery contract"
  key_links:
    - from: "docs/telemetry.md"
      to: "lib/parapet/telemetry/async_delivery.ex"
      via: "documented event families and safe metadata"
---

<objective>
Document the Phase 4 public contract and wire it into the repo's proof surfaces.

Purpose: Phase 4 is about freezing a public telemetry API; the docs and verification path must make that contract explicit for adopters and for later phases.
Output: updated docs and proof-surface tests that describe the normalized async/delivery namespace and its safe metadata contract.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v0.7-phases/4/4-CONTEXT.md
@.planning/v0.7-phases/4/RESEARCH.md
@.planning/v0.7-phases/4/04-VALIDATION.md
@docs/telemetry.md
@README.md
@lib/mix/tasks/verify.public_api.ex
@test/mix/tasks/verify.public_api_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the new async/delivery public contract</name>
  <files>docs/telemetry.md, README.md</files>
  <action>Update `docs/telemetry.md` to describe the Phase 4 event families, the bounded metadata contract, the difference between label-safe fields and `refs`, and the key semantic distinctions the contract preserves: `provider_accepted` vs `delivered`, `retryable_failed` vs `discarded`, and callback delay vs backlog. Update `README.md` only enough to surface the blessed adoption path and point readers to the telemetry documentation without turning the README into a full reference manual.</action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>The public docs explain the new namespace, safe metadata policy, and adopter path clearly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend public API proof surface if a new public module was introduced</name>
  <files>test/mix/tasks/verify.public_api_test.exs</files>
  <behavior>
    - Test 1: `verify.public_api` includes any new public telemetry contract module in its manifest.
    - Test 2: missing docs still fail the proof surface.
  </behavior>
  <action>If plan 01 introduced a public contract module under the `Parapet.` namespace, update the `verify.public_api` test coverage so the new module is included in the expected proof surface. Keep the proof narrow and implementation-aware; do not rewrite the mix task unless execution exposes a real gap.</action>
  <verify>
    <automated>mix test test/mix/tasks/verify.public_api_test.exs && mix run -e 'Mix.Task.run("verify.public_api")'</automated>
  </verify>
  <done>The repo's public API verification path now covers the new contract documentation surface where applicable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove activation and compile-out behavior stayed intact</name>
  <files>test/parapet_test.exs, test/parapet/integrations/mailglass_test.exs, test/parapet/integrations/chimeway_test.exs, test/parapet/integrations/rindle_test.exs</files>
  <behavior>
    - Test 1: `Parapet.attach(adapters: [...])` remains the explicit activation seam for the optional integrations in scope.
    - Test 2: repeated setup or activation does not silently detach observability or leave the suite in a broken duplicate-handler state.
    - Test 3: the phase continues to compile cleanly with warnings as errors in the normal optional-dependency environment.
  </behavior>
  <action>Add explicit proof that the Phase 4 adapter work preserved D-34 and roadmap success criterion 3. Extend `test/parapet_test.exs` and any focused integration tests needed so execution verifies activation still flows through `Parapet.attach(adapters: [...])`, optional integrations remain explicit and host-owned, and repeated setup behavior is either made safe or made intentionally test-disciplined and documented. Use `mix compile --warnings-as-errors` as the compile-out guardrail rather than relying on narrative assurances.</action>
  <verify>
    <automated>mix test test/parapet_test.exs test/parapet/integrations/mailglass_test.exs test/parapet/integrations/chimeway_test.exs test/parapet/integrations/rindle_test.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <done>Phase 4 owns compile-out and activation safety explicitly, with executable proof instead of implication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs -> adopters and downstream phases | Inaccurate documentation would freeze the wrong contract semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | Contract documentation | mitigate | Keep docs aligned to code-facing contract primitives and verify the public module participates in the repo's API proof surface. |
</threat_model>

<verification>
- Review telemetry docs for bounded metadata rules and event-family definitions.
- Run `mix compile --warnings-as-errors`.
- Run `mix run -e 'Mix.Task.run("verify.public_api")'` if a new public `Parapet.*` module is introduced.
- Run the activation and compile-out proof tests.
</verification>

<success_criteria>
- The new async/delivery namespace is documented as public API.
- Adopters can tell which fields are safe labels and which belong in refs.
- The repo's proof surfaces acknowledge the new public contract module when applicable.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/4/04-04-SUMMARY.md`
</output>
