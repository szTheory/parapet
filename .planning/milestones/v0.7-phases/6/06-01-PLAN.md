---
phase: 06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/spine/alert_processor.ex
  - lib/parapet/spine/incident.ex
  - lib/parapet/spine/timeline_entry.ex
  - test/parapet/spine/alert_processor_test.exs
autonomous: true
requirements:
  - TRIAGE-02
  - TRIAGE-03
must_haves:
  truths:
    - "Async and delivery alerts are classified into a bounded current-state summary at incident-ingestion time instead of being left for UI-only inference."
    - "Classification chronology is preserved through typed `triage_snapshot` timeline entries rather than mutable prose."
    - "Incident titles stay symptom-first while fault-plane meaning lives in structured summary data."
  artifacts:
    - path: "lib/parapet/spine/alert_processor.ex"
      provides: "Transactional alert-to-incident enrichment for bounded triage summaries and snapshots"
    - path: "lib/parapet/spine/incident.ex"
      provides: "Durable current-state summary seam for Phase 6 incident classification"
    - path: "lib/parapet/spine/timeline_entry.ex"
      provides: "Append-only typed chronology seam for triage snapshots"
  key_links:
    - from: "lib/parapet/spine/alert_processor.ex"
      to: "lib/parapet/spine/incident.ex"
      via: "bounded current-state summary writes into `runbook_data`"
    - from: "lib/parapet/spine/alert_processor.ex"
      to: "lib/parapet/spine/timeline_entry.ex"
      via: "append-only `triage_snapshot` chronology"
---

<objective>
Create the Phase 6 enrichment foundation inside the evidence spine.

Purpose: convert bounded async and delivery alert semantics into durable incident classification and ordered evidence before the operator workbench consumes them.
Output: enriched alert-ingestion logic, durable summary shape, and `triage_snapshot` chronology tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-phases/6/6-CONTEXT.md
@.planning/v0.7-phases/6/RESEARCH.md
@.planning/v0.7-phases/6/PATTERNS.md
@.planning/v0.7-phases/6/06-VALIDATION.md
@.planning/v0.7-phases/5/05-03-SUMMARY.md
@docs/telemetry.md
@lib/parapet/spine/alert_processor.ex
@lib/parapet/spine/incident.ex
@lib/parapet/spine/timeline_entry.ex
@test/parapet/spine/alert_processor_test.exs

<interfaces>
From lib/parapet/spine/incident.ex:
```elixir
schema "parapet_incidents" do
  field(:title, :string)
  field(:description, :string)
  field(:state, :string, default: "open")
  field(:correlation_key, :string)
  field(:trace_id, :string)
  field(:runbook_data, :map)
end
```

From lib/parapet/spine/timeline_entry.ex:
```elixir
schema "parapet_timeline_entries" do
  field(:type, :string)
  field(:payload, :map)
  belongs_to(:incident, Incident, type: :binary_id)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich firing alerts into a bounded current-state summary</name>
  <files>lib/parapet/spine/alert_processor.ex, lib/parapet/spine/incident.ex, test/parapet/spine/alert_processor_test.exs</files>
  <behavior>
    - Test 1: async or delivery alert labels and annotations are normalized into a bounded current summary stored in `incident.runbook_data`.
    - Test 2: incident titles remain symptom-first and do not hardcode inferred fault-plane wording unless directly observed.
    - Test 3: repeated correlated firing alerts do not create duplicate conflicting current-state records.
  </behavior>
  <action>Extend `Parapet.Spine.AlertProcessor` so Phase 5 async and delivery alerts are classified at ingestion time rather than deferred to UI heuristics. Extract only bounded fields such as `integration`, `fault_plane`, `queue`, `pipeline_stage`, `delay_bucket`, `failure_class`, `symptom`, `impact`, `next_safe_action`, and `confidence` from alert labels or structured annotations, then write them into `incident.runbook_data` as the current-state summary. Keep `runbook_data` narrow and current-state only per the Phase 6 context; do not store raw alert payloads or turn it into a hidden timeline. Preserve existing correlation-key semantics, runbook attachment behavior, and notifier flow.</action>
  <verify>
    <automated>mix test test/parapet/spine/alert_processor_test.exs</automated>
  </verify>
  <done>The evidence spine stores one bounded current-state classification summary for async and delivery incidents without widening alert or metadata cardinality.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append durable `triage_snapshot` chronology for classification rationale</name>
  <files>lib/parapet/spine/alert_processor.ex, lib/parapet/spine/timeline_entry.ex, test/parapet/spine/alert_processor_test.exs</files>
  <behavior>
    - Test 1: a new or changed async or delivery classification appends a `triage_snapshot` timeline entry.
    - Test 2: the snapshot payload carries bounded rationale facts and the summary fields needed for later operator rendering.
    - Test 3: chronology remains append-only and does not duplicate sequence into mutable summary prose.
  </behavior>
  <action>Add a typed chronology path for Phase 6 by appending `TimelineEntry` rows of type `triage_snapshot` whenever a bounded classification is first created or materially changes. The payload should include the same bounded summary fields plus a short ordered `evidence_facts` list explaining why the plane was chosen. Keep `AlertProcessor` as the single general alert-ingestion seam and ensure summary updates plus snapshot append happen transactionally or through one coherent persistence path so the two layers cannot drift.</action>
  <verify>
    <automated>mix test test/parapet/spine/alert_processor_test.exs</automated>
  </verify>
  <done>Phase 6 classification now has durable chronology that an operator can inspect without reconstructing meaning from mutable current-state text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alert payload labels and annotations -> durable incident evidence | Alert payloads are semi-structured and may contain unsafe or misleading detail if copied through too broadly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | `AlertProcessor` summary enrichment | mitigate | Copy only bounded classification fields into `runbook_data` and keep raw or exact identifiers out of durable generic incident metadata. |
| T-06-01-02 | Tampering | Summary and chronology coherence | mitigate | Write current-state summary and `triage_snapshot` through one transactional ingestion path so chronology cannot drift from the latest durable summary. |
</threat_model>

<verification>
- Run the focused alert-processor test suite.
- Confirm current-state summary fields stay bounded and symptom-first.
- Confirm `triage_snapshot` entries are created as append-only chronology rather than summary duplicates.
</verification>

<success_criteria>
- Async and delivery alerts create a bounded current-state summary in `incident.runbook_data`.
- Classification rationale is preserved in typed `triage_snapshot` entries.
- Fault-plane classification is captured durably before the operator layer renders it.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/6/06-01-SUMMARY.md`
</output>
