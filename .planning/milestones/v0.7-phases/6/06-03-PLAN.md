---
phase: 06
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - lib/parapet/spine/action_item.ex
  - lib/parapet/evidence.ex
  - docs/operator-ui.md
  - test/parapet/spine/action_item_test.exs
  - test/parapet/evidence/action_item_test.exs
autonomous: true
requirements:
  - RNBK-03
  - TRIAGE-03
must_haves:
  truths:
    - "Action items remain an exact-object follow-up seam rather than becoming a generic incident-task subsystem."
    - "High-cardinality identifiers stay in the exact-item seam and out of metrics labels or generic incident summaries."
    - "Operator docs continue to describe the workbench as evidence-first, host-owned, and not a provider-console or autonomous recovery layer."
  artifacts:
    - path: "lib/parapet/spine/action_item.ex"
      provides: "Narrow exact-item follow-up schema for Phase 6 async and delivery incidents"
    - path: "lib/parapet/evidence.ex"
      provides: "Exact-item create or resolve helpers that preserve idempotent posture"
    - path: "docs/operator-ui.md"
      provides: "Updated evidence-first operator guidance reflecting Phase 6 triage and exact follow-up seams"
  key_links:
    - from: "lib/parapet/evidence.ex"
      to: "lib/parapet/spine/action_item.ex"
      via: "exact-item creation and idempotent resolution"
    - from: "docs/operator-ui.md"
      to: "lib/parapet/operator.ex"
      via: "host-owned operator boundary and evidence-first detail payload"
---

<objective>
Finish Phase 6 by hardening the exact follow-up seam and documenting the operator contract.

Purpose: preserve a narrow object-scoped operator workflow boundary so Phase 7 can layer runbooks and recovery actions on top without inheriting a generic task system.
Output: exact-item schema or API refinements, idempotent tests, and updated operator guidance.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-phases/6/6-CONTEXT.md
@.planning/v0.7-phases/6/RESEARCH.md
@.planning/v0.7-phases/6/PATTERNS.md
@.planning/v0.7-phases/6/06-VALIDATION.md
@.planning/v0.7-phases/6/06-01-PLAN.md
@.planning/v0.7-phases/6/06-02-PLAN.md
@docs/operator-ui.md
@lib/parapet/spine/action_item.ex
@lib/parapet/evidence.ex
@lib/parapet/integrations/scoria.ex
@test/parapet/spine/action_item_test.exs
@test/parapet/evidence/action_item_test.exs

<interfaces>
From lib/parapet/spine/action_item.ex:
```elixir
schema "parapet_action_items" do
  field(:title, :string)
  field(:integration, :string)
  field(:external_id, :string)
  field(:state, :string, default: "open")
end
```

From lib/parapet/evidence.ex:
```elixir
def create_action_item(attrs \\ %{}) do
  %ActionItem{}
  |> ActionItem.changeset(attrs)
  |> repo().insert()
end

def resolve_action_item(criteria) when is_list(criteria)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten the exact-item follow-up seam for Phase 6 objects</name>
  <files>lib/parapet/spine/action_item.ex, lib/parapet/evidence.ex, test/parapet/spine/action_item_test.exs, test/parapet/evidence/action_item_test.exs</files>
  <behavior>
    - Test 1: any new Phase 6 fields remain narrow, bounded, and specific to exact-object follow-up.
    - Test 2: `create_action_item/1` supports the exact async or delivery object patterns required by Phase 6 without implying broad task management.
    - Test 3: idempotent resolve behavior still works for direct IDs and exact lookup criteria.
  </behavior>
  <action>Refine the `ActionItem` seam only as much as Phase 6 requires. If exact async or delivery follow-up needs stronger linkage, prefer narrow additions such as `incident_id` or a bounded `kind` field instead of owner, SLA, free-form notes, or generic task state. Keep `Parapet.Evidence` creation and resolution helpers explicit and idempotent, and preserve the posture that exact identifiers live here rather than in metrics labels or generic incident metadata. Use the existing `Scoria` object-follow-up pattern as the analog for when a durable exact-item record is appropriate.</action>
  <verify>
    <automated>mix test test/parapet/spine/action_item_test.exs test/parapet/evidence/action_item_test.exs</automated>
  </verify>
  <done>Phase 6 has a narrow durable seam for exact operator-owned work without growing a generic incident task system.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update operator guidance for triage and exact follow-up</name>
  <files>docs/operator-ui.md</files>
  <behavior>
    - Documentation states that the Phase 6 top triage block is evidence-backed and derived from durable summary plus chronology.
    - Documentation explains that exact action items exist only for concrete operator-owned objects.
    - Documentation preserves the host-owned, evidence-first, and non-autonomous operator posture.
  </behavior>
  <action>Update `docs/operator-ui.md` so the docs reflect the new Phase 6 operator contract: a compact triage block over durable evidence, normalized chronology as the main evidence surface, and exact object follow-up items only when one concrete async or delivery object needs manual action. Do not imply provider-console replacement, broad task management, or automatic remediation. The documentation should make Phase 7 deferral clear so adopters understand that recovery actions remain host-wired and audited later.</action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>The operator guide describes the Phase 6 evidence-first contract and the narrow exact follow-up seam accurately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Exact-item identifiers -> durable follow-up system | High-cardinality identifiers must survive operator workflows without contaminating labels or broad incident metadata |
| Operator docs -> adopters | Incorrect guidance would imply broader mutation authority or task-management scope than the phase actually supports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | `ActionItem` seam | mitigate | Keep exact identifiers confined to `ActionItem.external_id` and any narrow exact-item linkage fields, not generic summaries. |
| T-06-03-02 | Elevation of Privilege | Operator guidance | mitigate | Document clearly that Phase 6 adds evidence and exact follow-up only; host-owned recovery actions remain Phase 7 work. |
</threat_model>

<verification>
- Run the focused action-item tests.
- Confirm any schema additions are narrow and bounded.
- Confirm operator docs still describe a host-owned, evidence-first surface with no autonomous recovery claims.
</verification>

<success_criteria>
- Exact operator-owned async and delivery work can be represented durably without becoming a generic task system.
- Action-item APIs retain explicit and idempotent semantics.
- Operator docs accurately reflect Phase 6 triage and follow-up behavior.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/6/06-03-SUMMARY.md`
</output>
