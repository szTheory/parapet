---
phase: 04
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/parapet/operator/workbench_contract.ex
  - lib/parapet/operator.ex
  - test/parapet/operator/workbench_contract_test.exs
  - test/parapet/operator_test.exs
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "The detail payload exposes escalation status and system-action facts as a deterministic projection from durable evidence."
    - "One canonical chronology remains the source of sequence while the summary panel acts as a compact index into that evidence."
    - "Actor classes are explicit and stable enough that the UI never has to infer system versus human action from free-form text."
  artifacts:
    - path: lib/parapet/operator/workbench_contract.ex
      provides: Escalation-aware derived workbench contract
    - path: lib/parapet/operator.ex
      provides: Incident detail payload carrying escalation summary and actor-distinction helpers
  key_links:
    - from: lib/parapet/operator/workbench_contract.ex
      to: lib/parapet/escalation/worker.ex
      via: typed chronology consumed for escalation summary derivation
    - from: lib/parapet/operator.ex
      to: lib/parapet/operator/workbench_contract.ex
      via: public detail payload for generated UI consumption
---

<objective>
Turn the new escalation evidence into a stable workbench contract.

Purpose: let the generated operator surface answer current escalation status, whether the system already acted, and what the next safe operator action is without creating a UI-owned state machine.
Output: richer derived detail payload and pure derivation tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v0.8-phases/4/4-CONTEXT.md
@.planning/milestones/v0.8-phases/4/RESEARCH.md
@.planning/milestones/v0.8-phases/4/PATTERNS.md
@.planning/milestones/v0.8-phases/4/VALIDATION.md
@.planning/milestones/v0.8-phases/4/04-01-PLAN.md
@lib/parapet/operator/workbench_contract.ex
@lib/parapet/operator.ex
@lib/parapet/automation/executor.ex
@lib/parapet/escalation/worker.ex
@test/parapet/operator/workbench_contract_test.exs
@test/parapet/operator_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive escalation summary and actor classes from durable evidence</name>
  <files>lib/parapet/operator/workbench_contract.ex, test/parapet/operator/workbench_contract_test.exs</files>
  <behavior>
    - Test 1: current escalation status, suppression state, and next-step facts are derived from bounded incident summary plus the latest relevant chronology.
    - Test 2: system actions are classified distinctly from operator or AI/copilot actions without relying on color or payload dumps.
    - Test 3: chronology timestamps remain authoritative and the derived summary does not become a second state machine.
  </behavior>
  <action>Extend `Parapet.Operator.WorkbenchContract` with a compact escalation projection. Include fields such as current escalation status, suppression window metadata, whether the system executed or short-circuited recently, and timeline presentation hints for explicit actor classes. Prefer narrow current-state incident data, then refine or corroborate it with the latest typed chronology entries. Keep any countdown or next-step value clearly derived and non-authoritative.</action>
  <verify>
    <automated>mix test test/parapet/operator/workbench_contract_test.exs</automated>
  </verify>
  <done>The workbench contract now exposes escalation and system-action facts deterministically from durable evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return an escalation-aware incident detail payload for generated UI</name>
  <files>lib/parapet/operator.ex, test/parapet/operator_test.exs</files>
  <behavior>
    - Test 1: `incident_detail/1` returns the incident, ordered entries, derived escalation summary, and any actor-distinction helpers coherently.
    - Test 2: external links and existing runbook or triage data continue to coexist with the new escalation fields.
    - Test 3: the payload remains read-oriented and evidence-first rather than embedding UI-owned escalation logic.
  </behavior>
  <action>Update `Parapet.Operator.incident_detail/1` so the public payload cleanly exposes the richer workbench contract from Task 1 while preserving the existing single-chronology and runbook surfaces. If small helper keys are needed for generated UI ergonomics, derive them here or in `WorkbenchContract`; do not push escalation heuristics down into HEEx templates.</action>
  <verify>
    <automated>mix test test/parapet/operator_test.exs</automated>
  </verify>
  <done>The generated UI can now consume one stable detail payload with escalation status and actor-distinction semantics already resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable evidence -> derived operator state | Incorrect projection could hide chronology or mislabel system actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | `WorkbenchContract` derivation | mitigate | Derive from bounded incident state and typed chronology only, never free-form UI heuristics. |
| T-04-02-02 | Repudiation | Actor-distinction projection | mitigate | Keep explicit actor classes and retain the full ordered chronology in the detail payload. |
</threat_model>

<verification>
- Run the workbench-contract and operator tests.
- Confirm escalation summary fields remain projections of durable truth.
- Confirm the detail payload still exposes chronology as the canonical evidence surface.
</verification>

<success_criteria>
- `WorkbenchContract` exposes escalation-aware summary fields and explicit actor semantics.
- `incident_detail/1` returns one stable evidence-first payload for the generated operator UI.
- The UI will not need to invent escalation state or actor classification in templates.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.8-phases/4/04-02-SUMMARY.md`
</output>
