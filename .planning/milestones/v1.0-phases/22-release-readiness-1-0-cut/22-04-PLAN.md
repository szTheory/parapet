---
phase: 22-release-readiness-1-0-cut
plan: 04
type: execute
wave: 4
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - release-please-config.json
  - .planning/phases/22-release-readiness-1-0-cut/22-VERIFICATION.md
  - .planning/phases/22-release-readiness-1-0-cut/22-CONTEXT.md
autonomous: false
requirements: [REL-04]

must_haves:
  truths:
    - "The 0.10.0 pin is preserved until the real v0.10.0 tag exists"
    - "The 1.0.0 one-time pin is added only after all prep work lands"
    - "The post-cut cleanup removes release-as: 1.0.0 and both pre-major bump flags"
    - "The plan does not hand-edit .release-please-manifest.json"
  artifacts:
    - path: "release-please-config.json"
      provides: "Config choreography for the 1.0 graduation sequence"
      contains: "release-as"
  key_links:
    - from: "release-please-config.json"
      to: "Release Please generated version"
      via: "one-time release-as pins"
      pattern: "release-as"
---

<objective>
Land the explicit `0.10.0 -> 1.0.0` Release Please graduation sequence without lying about
external state: preserve the `0.10.0` pin until the real tag exists, stage the one-time
`1.0.0` pin only after prep work merges, and capture the immediate post-cut cleanup of the
pre-major flags and one-time pin.

Purpose: REL-04 is the final release semantics task. It is inherently staged on external tag
truth and therefore must include a blocking human checkpoint rather than pretending the entire
cut can complete inside the repo today.

Output: the release configuration and release-checklist truth surface are ready for the real
graduation cut, with the external blocker and resume signal documented explicitly.
</objective>

<context>
@.planning/phases/22-release-readiness-1-0-cut/22-CONTEXT.md
@.planning/phases/22-release-readiness-1-0-cut/22-RESEARCH.md
@.planning/phases/22-release-readiness-1-0-cut/22-PATTERNS.md

<interfaces>
- Current config still pins `release-as: "0.10.0"` and still carries both pre-major bump flags.
- Current manifest still reads `0.9.0`; it is read-only state.
- Local tags include `v0.10` but not `v0.10.0`.
- The final plan must treat the real `v0.10.0` tag as a hard checkpoint before removing the pin.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the release config and checklist for the staged graduation sequence</name>
  <files>release-please-config.json, .planning/phases/22-release-readiness-1-0-cut/22-VERIFICATION.md, .planning/phases/22-release-readiness-1-0-cut/22-CONTEXT.md</files>
  <action>
Update the release configuration/checklist surfaces so they explicitly encode:

1. Keep `release-as: "0.10.0"` until the exact `v0.10.0` tag exists.
2. After that tag exists, remove the `0.10.0` pin.
3. After all Phase 22 prep work is merged, add `release-as: "1.0.0"` for the graduation PR.
4. Immediately after the 1.0.0 tag exists, remove:
   - `release-as: "1.0.0"`
   - `bump-minor-pre-major`
   - `bump-patch-for-minor-pre-major`
5. Never hand-edit `.release-please-manifest.json`.

Keep the artifact honest about what can be committed now versus what is blocked on external
tag/release truth.
  </action>
  <verify>
    <automated>rg -n 'release-as|bump-minor-pre-major|bump-patch-for-minor-pre-major|manifest' release-please-config.json .planning/phases/22-release-readiness-1-0-cut/22-VERIFICATION.md .planning/phases/22-release-readiness-1-0-cut/22-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - The staged sequence is explicit in committed artifacts
    - The `v0.10.0` tag remains the hard precondition for removing the current pin
    - Post-1.0 cleanup of both pre-major flags is explicitly captured
    - No artifact instructs a manual manifest edit
  </acceptance_criteria>
  <done>The repo artifacts describe the exact 1.0 graduation choreography without pretending external tag truth already exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Execute the real 0.10.0 -> 1.0.0 cut once external truth is ready</name>
  <files>(GitHub releases/tags, Hex.pm, HexDocs — external state)</files>
  <action>
Pause for the operator at the real graduation boundary.

Before resuming:
1. Confirm the exact `v0.10.0` tag exists.
2. Confirm all Phase 22 prep work is merged and green.
3. Confirm `HEX_API_KEY` exists and the publish workflow is ready.
4. Run the canonical Phase 22 verification gate.
5. Merge the 1.0.0 Release Please PR and confirm both Hex and HexDocs resolution.

Only after those truths exist should the cleanup commit removing `release-as: "1.0.0"` and
the two pre-major bump flags be made.
  </action>
  <what-built>
    The repo-side release choreography, workflow automation, and verification truth surface
    are ready. What remains is the real external cut: tags, release PRs, Hex publish, and
    HexDocs resolution.
  </what-built>
  <how-to-verify>
1. Confirm `git tag --list 'v0.10.0'` or the GitHub release view shows the exact `v0.10.0` tag.
2. Run the canonical REL-03 gate from `22-VERIFICATION.md`.
3. Confirm Release Please proposes `1.0.0` only after the one-time pin is applied.
4. After merge, confirm `https://hexdocs.pm/parapet/1.0.0/` resolves and the Hex package is visible.
5. Make the immediate cleanup commit removing the `1.0.0` pin and both pre-major bump flags.
  </how-to-verify>
  <verify>
    <human-check>Exact v0.10.0 tag exists, 1.0.0 release PR merged, Hex and HexDocs resolve, and post-cut config cleanup is complete.</human-check>
  </verify>
  <resume-signal>
Type "approved" once the exact `v0.10.0` tag exists, the 1.0.0 cut is complete, Hex and
HexDocs resolve, and the post-cut config cleanup is merged. Otherwise describe which external
truth is still missing.
  </resume-signal>
  <done>Parapet is honestly cut as 1.0.0 and the release config no longer carries pre-1.0 scaffolding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo config -> Release Please state machine | How version numbers are proposed and advanced |
| Git tags/releases -> operator claims | Whether the final cut can be claimed honestly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering | version choreography | mitigate | Keep all version steering in `release-please-config.json`, treat the manifest as read-only, and stage the pin changes on real tag truth. |
| T-22-04B | Repudiation | premature completion claims | mitigate | Use a blocking human checkpoint requiring the real `v0.10.0` tag, real 1.0.0 PR/tag, and post-publish resolution before marking the phase complete. |
</threat_model>

<verification>
- Release config/checklist artifacts explicitly encode the staged sequence.
- The final human checkpoint requires real tag/release/package/docs truth before completion.
</verification>

<success_criteria>
- REL-04 is executable without manifest edits or version ambiguity.
- Phase completion remains blocked on real external release truth, not paper configuration alone.
</success_criteria>

<output>
Create `.planning/phases/22-release-readiness-1-0-cut/22-04-SUMMARY.md` when done.
</output>
