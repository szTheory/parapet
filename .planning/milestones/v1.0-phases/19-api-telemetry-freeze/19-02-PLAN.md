---
phase: 19-api-telemetry-freeze
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/telemetry_contract_test.exs
  - test/parapet/slo_test.exs
autonomous: true
requirements: [STAB-05, STAB-06]
must_haves:
  truths:
    - "A contract test fails CI when any documented [:parapet, …] event family, measurement key, metadata key, or outcome-atom vocab drifts from its committed fixture"
    - "The 6 async/delivery families are pinned against the resolved AsyncDelivery.event_families/0, not source tokens"
    - "All 27 frozen [:parapet, …] families are enumerated in the fixture"
    - "Calling Parapet.SLO.define/2 emits a compile-time deprecation warning naming Parapet.SLO.Provider"
  artifacts:
    - path: "test/telemetry_contract_test.exs"
      provides: "Drift-detecting fixture for all 27 frozen telemetry families"
      contains: "AsyncDelivery.event_families"
    - path: "test/parapet/slo_test.exs"
      provides: "STAB-06 compile-time deprecation warning assertion"
      contains: "deprecated"
  key_links:
    - from: "test/telemetry_contract_test.exs"
      to: "Parapet.Telemetry.AsyncDelivery.event_families/0"
      via: "compile-time module attribute (D-07)"
      pattern: "AsyncDelivery\\.event_families"
    - from: "test/parapet/slo_test.exs"
      to: "Parapet.SLO.define/2"
      via: "Code.compile_string + capture_io(:stderr)"
      pattern: "capture_io\\(:stderr"
---

<objective>
Lock the telemetry contract (STAB-05) and verify the existing SLO deprecation (STAB-06) with
two ExUnit test files. Both are Wave 0 artifacts (do not exist yet) and both are independent of
the verify.public_api gate, so this plan runs in parallel with Plan 01.

1. `test/telemetry_contract_test.exs` (NEW) — assert committed fixtures of (event family,
   measurement keys, metadata keys, outcome-atom vocab) for ALL 27 frozen `[:parapet, …]`
   families (the full surface per D-05, including the raw ecto/http/oban passthroughs). Pin the
   6 async/delivery families against the RESOLVED `AsyncDelivery.event_families/0` list (D-06/D-07),
   not the `[:parapet, :async, family]` source token. Generalize the existing
   `test/parapet/telemetry/async_delivery_test.exs` pattern.
2. `test/parapet/slo_test.exs` (NEW) — assert `Parapet.SLO.define/2` emits a compile-time
   deprecation warning naming `Parapet.SLO.Provider` (D-14). The `@deprecated` already exists at
   `lib/parapet/slo.ex:29`; this is verification only, no production change.

Purpose: Drift in the telemetry contract is caught in CI, not in production dashboards. The
deprecation warning is proven to fire so adopters get the compile-time nudge.

Output: Two new test files; no production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-api-telemetry-freeze/19-CONTEXT.md
@.planning/phases/19-api-telemetry-freeze/19-RESEARCH.md
@.planning/phases/19-api-telemetry-freeze/19-PATTERNS.md

<interfaces>
<!-- Extracted from codebase + RESEARCH catalog — executor needs no exploration. -->

`Parapet.Telemetry.AsyncDelivery` (existing contract module — the seed + analog):
- `event_families/0` -> the 6 delivery/async families in this exact order:
  `[:parapet,:delivery,:outbound]`, `[:parapet,:delivery,:provider_feedback]`,
  `[:parapet,:delivery,:webhook_ingest]`, `[:parapet,:async,:stage]`,
  `[:parapet,:async,:backlog]`, `[:parapet,:async,:callback]`.
- `allowed_public_keys/1` -> the permitted metadata keys for a given family.

Analog test `test/parapet/telemetry/async_delivery_test.exs` asserts
`AsyncDelivery.event_families()` against a hardcoded list and that shaped metadata has only
expected keys. Generalize this.

The 27-family catalog (groups 2-7, to hardcode as fixtures) — full measurements/metadata in
RESEARCH.md lines 396-489. The remaining 21 families beyond AsyncDelivery's 6:
  Journey (5): [:parapet,:journey,:login] {outcome}, [:parapet,:journey,:signup] {outcome,provider},
    [:parapet,:journey,:billing] {outcome}, [:parapet,:journey,:billing,:checkout] {outcome,plan},
    [:parapet,:journey,:billing,:webhook] {outcome,event_type}
  Scoria (6): [:parapet,:scoria,:metrics], [:parapet,:scoria,:mcp,:error] {reason,tool_name},
    [:parapet,:scoria,:metrics,:stale] {workflow_id}, [:parapet,:scoria,:metrics,:expired] {workflow_id},
    [:parapet,:scoria,:metrics,:resumed] {workflow_id}, [:parapet,:scoria,:eval,:completed]
    {guardrail,passed,model_name}
  Operator (1): [:parapet,:operator,:queue,:page] meas {duration_native,duration_ms}
    meta {scope,direction,page_size_bucket,result_size_bucket}
  Probe (3): [:parapet,:probe,:run] meas {duration_ms} meta {probe,status},
    [:parapet,:probe,:run,:stop], [:parapet,:probe,:run,:exception]
  Infrastructure (4): [:parapet,:ecto,:query] meas {query_time_ms,queue_time_ms} meta {source},
    [:parapet,:http,:request] meas {duration_ms,status_code} meta {route,method,status_class},
    [:parapet,:oban,:job] meas {duration_ms} meta {worker,queue,state},
    [:parapet,:deploy,:mark] meas {system_time} meta {caller-controlled — do NOT fixture keys}
  Evidence/Rulestead (2): [:parapet,:audit,:created] meas {} meta {audit_attrs — open},
    [:parapet,:rulestead,:flag_change] meas {} meta {ruleset, + others — open}

CONFIRMED: `@safe_labels` in `lib/parapet/integrations/scoria.ex:12` = `[:model, :provider, :tool_name]`.
Scoria metrics/stale/expired/resumed families add these safe labels (+ workflow_id for stale/
expired/resumed). Use this exact list — no need to re-read the file.

STAB-06: `lib/parapet/slo.ex:29` already has `@deprecated "Use a Parapet.SLO.Provider module instead"`
on `def define(name, opts)`. `define/2` requires opts: objective, good_events, total_events, runbook.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the telemetry contract test for all 27 frozen families</name>
  <files>test/telemetry_contract_test.exs</files>
  <read_first>
    - test/parapet/telemetry/async_delivery_test.exs (the analog — assertion style + event_families pattern)
    - lib/parapet/telemetry/async_delivery.ex (event_families/0, allowed_public_keys/1, frozen vocab attributes)
    - .planning/phases/19-api-telemetry-freeze/19-RESEARCH.md (STAB-05 catalog lines 396-609 — full fixture data, all 27 families + measurement/metadata maps)
    - .planning/phases/19-api-telemetry-freeze/19-PATTERNS.md (telemetry_contract_test.exs pattern, lines 211-271)
  </read_first>
  <behavior>
    - The test asserts `length(AsyncDelivery.event_families()) == 6` (the resolved delivery/async set).
    - The test asserts the total enumerated family count == 27 (6 async/delivery + 21 hardcoded).
    - For each delivery/async family, `AsyncDelivery.allowed_public_keys(family)` equals the
      documented metadata fixture (sorted compare) — drift in the module's vocab fails the test.
    - For each family with a known measurement fixture, the documented measurement keys match
      the committed fixture (a removed/renamed measurement key would require updating the fixture,
      surfacing the drift in review).
    - Families with caller-controlled/open metadata ([:parapet,:deploy,:mark], [:parapet,:audit,:created],
      [:parapet,:rulestead,:flag_change]) are enumerated but their metadata keys are NOT over-constrained.
    - Outcome-atom vocab for delivery (`:attempted,:provider_accepted,:delivered,:failed,:bounced,
      :complained,:suppressed`) and async (`:started,:succeeded,:retryable_failed,:discarded,:delayed`)
      is asserted against the AsyncDelivery frozen vocab attributes.
  </behavior>
  <action>
    Create `test/telemetry_contract_test.exs` as `Parapet.TelemetryContractTest`
    (`use ExUnit.Case, async: true`). Follow the analog's module-attribute-as-fixture style.
    Seed `@async_delivery_families Parapet.Telemetry.AsyncDelivery.event_families()` at compile
    time (D-07 — do NOT hardcode these 6; derive from the module so they stay the single source
    of truth). Hardcode `@other_documented_families` = the 21 families listed in the interfaces
    block above. Set `@all_documented_families @async_delivery_families ++ @other_documented_families`
    and assert its length is 27. Add `@documented_measurements` and `@documented_metadata_keys`
    maps using the RESEARCH fixture data (lines 538-570) — for scoria metrics/stale/expired/resumed,
    include the CONFIRMED `@safe_labels` `[:model, :provider, :tool_name]` (+ `:workflow_id` for
    stale/expired/resumed). For each delivery/async family, generate a test (use a `for` comprehension
    over `AsyncDelivery.event_families()`) asserting `Enum.sort(AsyncDelivery.allowed_public_keys(family))
    == Enum.sort(@documented_metadata_keys[family])`. Add a `describe` block asserting the delivery
    and async outcome vocabularies match the AsyncDelivery frozen vocab functions/attributes. Do NOT
    assert keys for the three open-metadata families. Each test failure message must instruct
    "Update docs/telemetry.md and this fixture together."
  </action>
  <verify>
    <automated>mix test test/telemetry_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/telemetry_contract_test.exs` exits 0
    - `grep -q "AsyncDelivery.event_families" test/telemetry_contract_test.exs` (families derived, not hardcoded for the 6)
    - The fixture enumerates 27 families (a test asserts the count == 27)
    - The file contains the scoria safe labels `:model`, `:provider`, `:tool_name`
    - No metadata-key assertion exists for `[:parapet, :deploy, :mark]` (open metadata)
  </acceptance_criteria>
  <done>The contract test passes green against the current code and will fail if any of the 27 family names, the AsyncDelivery metadata vocab, or a fixtured measurement/metadata key drifts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert the Parapet.SLO.define/2 compile-time deprecation warning (STAB-06)</name>
  <files>test/parapet/slo_test.exs</files>
  <read_first>
    - lib/parapet/slo.ex (the @deprecated at line 29 + define/2 signature and required opts)
    - .planning/phases/19-api-telemetry-freeze/19-RESEARCH.md (STAB-06 section lines 663-706 — Code.compile_string + capture_io(:stderr) pattern)
    - .planning/phases/19-api-telemetry-freeze/19-PATTERNS.md (CaptureIO for gate testing, lines 456-467)
  </read_first>
  <behavior>
    - Compiling a module whose body calls `Parapet.SLO.define/2` emits stderr output containing "deprecated".
    - That stderr output also names "Parapet.SLO.Provider" (the replacement).
  </behavior>
  <action>
    Create `test/parapet/slo_test.exs` as `Parapet.SLOTest` (`use ExUnit.Case`, `import ExUnit.CaptureIO`).
    Add a test that captures `:stderr` while `Code.compile_string/1` compiles a throwaway module whose
    function calls `Parapet.SLO.define(:test_slo, objective: 99.0, good_events: "x", total_events: "y",
    runbook: "http://example.com")` (supply all required opts so compilation reaches the deprecation
    check, not an arg error). Assert the captured output `=~ "deprecated"` AND `=~ "Parapet.SLO.Provider"`.
    If `Code.compile_string` proves not to surface the `@deprecated` warning in this runtime (RESEARCH
    assumption A3), fall back to the documented manual-verify alternative: tag the test
    `@tag :manual` and replace the assertion with a `@moduledoc`/comment instruction to run
    `mix compile` against a fixture call site and observe the warning — but try the automated
    capture first and keep it if it works.
  </action>
  <verify>
    <automated>mix test test/parapet/slo_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/parapet/slo_test.exs` exits 0
    - `grep -q "capture_io(:stderr" test/parapet/slo_test.exs`
    - The test asserts the captured warning contains both "deprecated" and "Parapet.SLO.Provider"
    - No change is made to `lib/parapet/slo.ex` (verification only — `@deprecated` already exists)
  </acceptance_criteria>
  <done>An automated test proves `Parapet.SLO.define/2` emits a compile-time deprecation warning naming `Parapet.SLO.Provider` (or, if the capture is unreliable in this runtime, a documented manual-verify test is in place per D-14).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| telemetry emit site -> downstream consumer | Event names/keys are the public contract; undetected drift leaks/breaks downstream metrics and SLOs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Information Disclosure / contract drift | `[:parapet, …]` telemetry families | mitigate | `test/telemetry_contract_test.exs` enumerates all 27 families + AsyncDelivery vocab and fails CI on drift; required `mix test` gate |
| T-19-04 | Repudiation (silent deprecation) | `Parapet.SLO.define/2` | mitigate | Test asserts the compile-time warning fires naming the replacement, so the deprecation is verifiably surfaced to adopters |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable) |

No HIGH-severity threats: tests only, no runtime surface, no user input, no installs. Severity LOW
(contract-integrity), fully mitigated by the two test files running as CI gates.
</threat_model>

<verification>
- `mix test test/telemetry_contract_test.exs` exits 0 and enumerates 27 families
- `mix test test/parapet/slo_test.exs` exits 0
- The 6 delivery/async families are derived from `AsyncDelivery.event_families()` (not hardcoded)
- No production code in `lib/` is modified by this plan
</verification>

<success_criteria>
- STAB-05: A telemetry contract test fails CI when documented event families, measurement keys, metadata keys, or outcome-atom vocab drift from fixtures, covering all 27 frozen `[:parapet, …]` families.
- STAB-06: `Parapet.SLO.define/2` is proven to emit a compile-time warning naming `Parapet.SLO.Provider`.
</success_criteria>

<output>
Create `.planning/phases/19-api-telemetry-freeze/19-02-SUMMARY.md` when done.
</output>
