---
phase: 21-runnable-demo-app
plan: 05
type: execute
wave: 5
depends_on: ["21-04"]
gap_closure: true
files_modified:
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex
  - examples/demo_app/assets/js/app.js
  - examples/demo_app/mix.exs
  - examples/demo_app/config/config.exs
  - examples/demo_app/lib/demo_app_web/components/layouts.ex
autonomous: true
requirements: [DEMO-01, DEMO-02]

must_haves:
  truths:
    - "The History tab renders resolved incidents without a KeyError crash"
    - "resolved_history_page/1 maps each %Incident{} through WorkbenchContract.queue_row/1 before queue_stream_item/1"
    - "examples/demo_app/assets/js/app.js exists, imports phoenix_live_view, builds a LiveSocket, and calls liveSocket.connect()"
    - "mix.exs declares an :esbuild dep and an assets.build alias that runs esbuild"
    - "config/config.exs configures the esbuild :demo_app profile bundling js/app.js to priv/static/assets"
    - "The root layout emits a CSRF meta tag and a <script src={~p\"/assets/app.js\"} defer> tag so the LiveView WebSocket connects"
  artifacts:
    - path: "examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex"
      provides: "Crash-free resolved history page via WorkbenchContract.queue_row/1"
      contains: "WorkbenchContract.queue_row"
    - path: "examples/demo_app/assets/js/app.js"
      provides: "LiveView JS entry point that connects the WebSocket"
      contains: "liveSocket.connect()"
    - path: "examples/demo_app/mix.exs"
      provides: "esbuild dep + assets.build alias"
      contains: "esbuild"
    - path: "examples/demo_app/config/config.exs"
      provides: "esbuild :demo_app build profile"
      contains: "config :esbuild"
    - path: "examples/demo_app/lib/demo_app_web/components/layouts.ex"
      provides: "Root layout with CSRF meta + app.js script tag"
      contains: "app.js"
  key_links:
    - from: "operator_live.ex resolved_history_page/1"
      to: "Parapet.Operator.WorkbenchContract.queue_row/1"
      via: "Enum.map over resolved %Incident{} structs"
      pattern: "WorkbenchContract\\.queue_row"
    - from: "layouts.ex root/1"
      to: "priv/static/assets/app.js"
      via: "<script src={~p\"/assets/app.js\"} defer>"
      pattern: "/assets/app\\.js"
    - from: "assets/js/app.js"
      to: "phoenix_live_view LiveSocket"
      via: "new LiveSocket(... ).connect()"
      pattern: "liveSocket\\.connect\\(\\)"
---

<objective>
Close the two BLOCKER gaps from 21-VERIFICATION.md that prevent the Operator UI from
working: (CR-01) the History tab crashes with `KeyError: key :incident_id not found in
%Parapet.Spine.Incident{}` because `resolved_history_page/1` passes raw `%Incident{}`
structs to `queue_stream_item/1`; and (CR-02) the demo has no JavaScript wiring, so the
LiveView WebSocket never connects and every `phx-click` event (Acknowledge, Resolve,
History navigation, queue refresh, preview/confirm) is silently dropped.

Purpose: DEMO-01 requires `/parapet` to load populated with seeded incidents including
RESOLVED state — which is unreachable while the History tab crashes. DEMO-02 requires the
seeded resolved incident to be viewable. Both DEMO-01 and DEMO-02 require the UI to be
interactive, which is impossible without the LiveView JS bundle. These are code-level fixes
that can be executed autonomously.

Output: a crash-free resolved history view and a working LiveView JS bundle (esbuild dep +
build profile + script tag) so the demo is a live reactive UI rather than a static snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-runnable-demo-app/21-CONTEXT.md
@.planning/phases/21-runnable-demo-app/21-RESEARCH.md
@.planning/phases/21-runnable-demo-app/21-VERIFICATION.md

<interfaces>
<!-- All values below are verified against the live repo. -->

CR-01 — the bug (examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex, verified):
- `queue_stream_item/1` (line 232) does `item |> Map.put(:id, item.incident_id) |> Map.put_new(:title, item.title || item.incident_id)`. It REQUIRES the input to be a queue-row map with an `:incident_id` key and a `:title` key.
- `resolved_history_page/1` (lines 316-343) builds `incidents` by querying `Parapet.Spine.Incident` directly (`DemoApp.Repo.all`), so `visible_items` are raw `%Parapet.Spine.Incident{}` structs. The `%Incident{}` schema has NO `:incident_id` field (only `:id`).
- Line 336 currently does `items: Enum.map(visible_items, &queue_stream_item/1)` — this raises `KeyError` on `item.incident_id` for every resolved incident.
- The active (non-resolved) path works because `Parapet.Operator.list_incident_queue/1` already returns queue-row maps (with `:incident_id`), so `queue_stream_item/1` succeeds there.

CR-01 — the fix:
- Add `alias Parapet.Operator.WorkbenchContract` near the top of the module (after the existing `import` lines, before `@default_page_size`).
- `Parapet.Operator.WorkbenchContract.queue_row/1` (lib/parapet/operator/workbench_contract.ex line 99, verified) takes `%Incident{} = incident` and returns a map shaped exactly like the active-queue rows: `%{incident_id: incident.id, state:, title:, severity:, secondary_line:, updated_at:, updated_at_label:, attention_chip:}`. This is the canonical struct→queue-row projection.
- Change ONLY the `items:` value on line 336 so each raw incident is piped through `WorkbenchContract.queue_row/1` THEN `queue_stream_item/1`. Concretely: `items: Enum.map(visible_items, fn incident -> incident |> WorkbenchContract.queue_row() |> queue_stream_item() end)`.
- CRITICAL: do NOT change `visible_items` itself. The cursor helpers `history_next_cursor/4` and `history_previous_cursor/4` (lines 399-407) call `encode_queue_cursor/1` (line 411) which pattern-matches `%{updated_at: %DateTime{} = updated_at, id: incident_id}` — those still operate on the RAW `%Incident{}` structs in `visible_items` (which have `:updated_at` and `:id`). Mapping only the `items:` field preserves correct cursor pagination.

CR-02 — current JS state (verified):
- `examples/demo_app/assets/js/` directory does NOT exist. `assets/` currently holds only `css/app.css` and `tailwind.config.js`.
- The endpoint already defines the LiveView socket: `socket "/live", Phoenix.LiveView.Socket, websocket: [connect_info: [session: @session_options]]` (endpoint.ex line 11). No endpoint change is needed.
- `Plug.Static` serves `DemoAppWeb.static_paths()` which already includes `assets` (demo_app_web.ex line 20: `~w(assets fonts images favicon.ico robots.txt)`). So `priv/static/assets/app.js` will be served at `/assets/app.js`, and `~p"/assets/app.js"` will pass verified-route checks (statics include `assets`).
- The root layout (`:html`) imports `get_csrf_token: 0` (demo_app_web.ex lines 53-54), so `get_csrf_token()` is callable inside `DemoAppWeb.Layouts.root/1`.
- Tailwind already outputs CSS to `../priv/static/assets/app.css` via the `tailwind` dep and `config :tailwind` profile (config.exs lines 27-36); the existing `assets.build` alias is `["tailwind default"]` (mix.exs line 51). esbuild must output `app.js` to the SAME `priv/static/assets/` directory and chain into the same `assets.build` alias.

CR-02 — app.js content requirements (Phoenix 1.8 / phoenix_live_view ~> 1.1 standard entry point):
- Import the CSRF token from the `<meta name="csrf-token">` tag.
- Import `LiveSocket` from `"phoenix_live_view"` and `Socket` from `"phoenix"`.
- Import `"phoenix_html"` for its side effects (form/link behaviors).
- Construct `let liveSocket = new LiveSocket("/live", Socket, {params: {_csrf_token: csrfToken}})` and call `liveSocket.connect()`. Expose `window.liveSocket = liveSocket` for debugging. No custom Hooks are required by the generated Operator UI (none are referenced), so an empty/omitted hooks option is fine.

CR-02 — mix.exs additions (verified current state, mix.exs lines 26-54):
- Add `{:esbuild, "~> 0.8", runtime: Mix.env() == :dev}` to the deps list (alongside the existing `{:tailwind, "~> 0.2", runtime: Mix.env() == :dev}`).
- Change the `assets.build` alias so it runs BOTH esbuild and tailwind. Target value: `"assets.build": ["tailwind default", "esbuild demo_app"]`.
- Change the `assets.deploy` alias to also bundle+minify JS. Target value: `"assets.deploy": ["tailwind default --minify", "esbuild demo_app --minify", "phx.digest"]`.

CR-02 — config/config.exs esbuild profile (verified current state ends with `import_config` at line 38; add the esbuild block before that line, mirroring the existing `config :tailwind` block at lines 27-36):
- Add `config :esbuild, version: "0.21.5", demo_app: [args: ~w(js/app.js --bundle --target=es2017 --outdir=../priv/static/assets), cd: Path.expand("../assets", __DIR__), env: %{"NODE_PATH" => Path.expand("../deps", __DIR__)}]`.
- The `NODE_PATH` pointing at `../deps` lets esbuild resolve `phoenix`, `phoenix_html`, and `phoenix_live_view` from the Hex deps' bundled JS (their `priv/static/` / `package.json` entry points), which is the standard Phoenix esbuild setup.

CR-02 — layouts.ex root/1 additions (verified current state, layouts.ex lines 5-20):
- In `<head>` (after the `<.live_title>` / before or after the existing app.css `<link>`), add `<meta name="csrf-token" content={get_csrf_token()} />`.
- At the END of `<body>` (after `{@inner_content}`), add `<script defer src={~p"/assets/app.js"}></script>`.
- Keep the existing app.css `<link>` unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CR-01 — map resolved incidents through WorkbenchContract.queue_row/1</name>
  <files>examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex</files>
  <read_first>
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex (the full file — confirm queue_stream_item/1 at line 232, resolved_history_page/1 at lines 316-343, the cursor helpers at lines 399-414, and the existing import block at lines 5-6)
    - lib/parapet/operator/workbench_contract.ex lines 95-113 (the queue_row/1 doc + body — confirm it takes %Incident{} and returns a map with :incident_id, :title, :state, :updated_at)
    - .planning/phases/21-runnable-demo-app/21-VERIFICATION.md (Gap 1 / CR-01 — the KeyError description)
  </read_first>
  <action>
Edit `examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex` to close CR-01.

1. Add `alias Parapet.Operator.WorkbenchContract` immediately after the existing
   `import DemoAppWeb.Parapet.OperatorComponents` line (line 6), before `@default_page_size`.

2. In `resolved_history_page/1`, change ONLY the `items:` value (currently line 336:
   `items: Enum.map(visible_items, &queue_stream_item/1)`) so each raw `%Incident{}` in
   `visible_items` is first projected to a queue-row map via `WorkbenchContract.queue_row/1`
   and THEN passed through the existing `queue_stream_item/1`. Use:
   `items: Enum.map(visible_items, fn incident -> incident |> WorkbenchContract.queue_row() |> queue_stream_item() end)`.

Do NOT modify `visible_items` itself, and do NOT touch the cursor helpers
(`history_next_cursor/4`, `history_previous_cursor/4`, `encode_queue_cursor/1`) — they must
keep operating on the raw `%Incident{}` structs (which carry `:updated_at` and `:id`) so
pagination cursors stay correct. Do not change `queue_stream_item/1` itself — the active
queue path depends on its current behavior.

Rationale: `WorkbenchContract.queue_row/1` is the same struct→queue-row projection
`Parapet.Operator.list_incident_queue/1` uses for the active queue, so the resolved path now
produces map items with `:incident_id` and `:title`, exactly what `queue_stream_item/1`
(`item.incident_id`, `item.title`) requires — eliminating the KeyError.
  </action>
  <verify>
    <automated>grep -q 'alias Parapet.Operator.WorkbenchContract' examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex && grep -q 'WorkbenchContract.queue_row' examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex && cd examples/demo_app && mix compile --warnings-as-errors 2>&1 | grep -vq 'KeyError' && echo CR01_OK</automated>
  </verify>
  <acceptance_criteria>
    - `operator_live.ex` contains `alias Parapet.Operator.WorkbenchContract`
    - `resolved_history_page/1` `items:` value pipes each incident through `WorkbenchContract.queue_row()` then `queue_stream_item()`
    - The cursor helpers and `visible_items` binding are unchanged (still operate on raw `%Incident{}` structs)
    - `queue_stream_item/1` body is unchanged
    - `cd examples/demo_app && mix compile` succeeds with no new warnings
    - No literal `Enum.map(visible_items, &queue_stream_item/1)` remains in `resolved_history_page/1`
  </acceptance_criteria>
  <done>The History tab maps resolved %Incident{} structs through WorkbenchContract.queue_row/1 before queue_stream_item/1, so visiting the resolved history no longer raises KeyError.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-02 — add the LiveView JS bundle (app.js + esbuild dep + config + script tag)</name>
  <files>examples/demo_app/assets/js/app.js, examples/demo_app/mix.exs, examples/demo_app/config/config.exs, examples/demo_app/lib/demo_app_web/components/layouts.ex</files>
  <read_first>
    - examples/demo_app/mix.exs (the full file — deps at lines 26-46, aliases at lines 48-54; note the existing tailwind dep + assets.build alias to mirror)
    - examples/demo_app/config/config.exs (the full file — the config :tailwind block at lines 27-36 to mirror; the import_config at line 38 to insert before)
    - examples/demo_app/lib/demo_app_web/components/layouts.ex (the full file — root/1 at lines 5-20; the app.css link at line 13)
    - examples/demo_app/lib/demo_app_web/endpoint.ex (confirms the "/live" LiveView socket already exists at line 11 — no change needed)
    - examples/demo_app/lib/demo_app_web.ex (confirms static_paths includes "assets" at line 20 and get_csrf_token: 0 is imported for :html at lines 53-54)
    - .planning/phases/21-runnable-demo-app/21-VERIFICATION.md (Gap 2 / CR-02 — the four missing pieces)
  </read_first>
  <action>
Close CR-02 with four coordinated edits so the LiveView WebSocket connects and phx-click
events fire.

1. CREATE `examples/demo_app/assets/js/app.js` as the standard Phoenix 1.8 LiveView entry
   point: read the CSRF token from `document.querySelector("meta[name='csrf-token']")`'s
   `content` attribute; `import "phoenix_html"`; `import {Socket} from "phoenix"`;
   `import {LiveSocket} from "phoenix_live_view"`; construct
   `let liveSocket = new LiveSocket("/live", Socket, {params: {_csrf_token: csrfToken}})`;
   call `liveSocket.connect()`; and assign `window.liveSocket = liveSocket`. No custom Hooks
   are needed (the generated Operator UI references none).

2. EDIT `examples/demo_app/mix.exs`:
   - Add `{:esbuild, "~> 0.8", runtime: Mix.env() == :dev}` to `deps/0` (next to the existing
     `{:tailwind, "~> 0.2", runtime: Mix.env() == :dev}`).
   - Change the `assets.build` alias value to `["tailwind default", "esbuild demo_app"]`.
   - Change the `assets.deploy` alias value to
     `["tailwind default --minify", "esbuild demo_app --minify", "phx.digest"]`.

3. EDIT `examples/demo_app/config/config.exs`: add an esbuild profile block BEFORE the
   `import_config "#{config_env()}.exs"` line (mirror the existing `config :tailwind` block):
   `config :esbuild, version: "0.21.5", demo_app: [args: ~w(js/app.js --bundle --target=es2017 --outdir=../priv/static/assets), cd: Path.expand("../assets", __DIR__), env: %{"NODE_PATH" => Path.expand("../deps", __DIR__)}]`.
   The `NODE_PATH` -> `../deps` lets esbuild resolve `phoenix`, `phoenix_html`, and
   `phoenix_live_view` JS from the fetched Hex deps.

4. EDIT `examples/demo_app/lib/demo_app_web/components/layouts.ex` `root/1`:
   - In `<head>`, add `<meta name="csrf-token" content={get_csrf_token()} />`.
   - At the END of `<body>` (after `{@inner_content}`), add
     `<script defer src={~p"/assets/app.js"}></script>`.
   - Leave the existing `<link rel="stylesheet" href={~p"/assets/app.css"} />` unchanged.

Do NOT modify endpoint.ex (the `/live` socket and the `assets` static path already exist).
The script tag points at `/assets/app.js`, which `Plug.Static` serves from
`priv/static/assets/app.js` (esbuild's `--outdir`), and `~p"/assets/app.js"` verifies because
`static_paths()` includes `assets`.
  </action>
  <verify>
    <automated>test -f examples/demo_app/assets/js/app.js && grep -q 'liveSocket.connect()' examples/demo_app/assets/js/app.js && grep -q 'phoenix_live_view' examples/demo_app/assets/js/app.js && grep -q ':esbuild' examples/demo_app/mix.exs && grep -q 'esbuild demo_app' examples/demo_app/mix.exs && grep -q 'config :esbuild' examples/demo_app/config/config.exs && grep -q 'csrf-token' examples/demo_app/lib/demo_app_web/components/layouts.ex && grep -q '/assets/app.js' examples/demo_app/lib/demo_app_web/components/layouts.ex && echo CR02_OK</automated>
  </verify>
  <acceptance_criteria>
    - `examples/demo_app/assets/js/app.js` exists, imports `phoenix_live_view` and `phoenix`, builds a `LiveSocket("/live", Socket, ...)`, and calls `liveSocket.connect()`
    - `examples/demo_app/mix.exs` deps contains `{:esbuild, "~> 0.8", ...}`
    - `examples/demo_app/mix.exs` `assets.build` alias runs `esbuild demo_app` (and still runs `tailwind default`)
    - `examples/demo_app/config/config.exs` contains a `config :esbuild` block with a `demo_app:` profile whose args bundle `js/app.js` to `../priv/static/assets` and set `NODE_PATH` to `../deps`
    - `examples/demo_app/lib/demo_app_web/components/layouts.ex` root/1 `<head>` contains `<meta name="csrf-token" content={get_csrf_token()} />`
    - `examples/demo_app/lib/demo_app_web/components/layouts.ex` root/1 `<body>` contains `<script defer src={~p"/assets/app.js"}></script>`
    - endpoint.ex is unchanged (the `/live` socket already present)
  </acceptance_criteria>
  <done>The demo has a LiveView JS entry point bundled by esbuild and loaded via a deferred <script> tag with a CSRF meta tag, so the LiveView WebSocket connects and phx-click events (Acknowledge, Resolve, History, refresh) are delivered instead of silently dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → LiveView WebSocket | untrusted client connects to `/live`; CSRF token gates the connection |
| Demo DB → Operator UI render | resolved %Incident{} structs cross into the queue-row rendering path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-13 | Denial of Service | History tab KeyError crash | mitigate | CR-01 fix routes resolved incidents through `WorkbenchContract.queue_row/1` so the documented `:incident_id` shape is always present, eliminating the crash that took down the History view for every visitor. |
| T-21-14 | Spoofing/Tampering | LiveView WebSocket connect | mitigate | app.js passes `_csrf_token` from the `<meta name="csrf-token">` tag into the LiveSocket params; the endpoint's `protect_from_forgery` pipeline + signed session validate it (standard Phoenix LiveView CSRF flow). |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | The only package added is `:esbuild` (hex.pm, phoenixframework/esbuild — official Phoenix asset-build wrapper, long-established). No npm/node packages are added; esbuild resolves `phoenix`/`phoenix_html`/`phoenix_live_view` JS from already-vetted Hex deps via NODE_PATH. No [ASSUMED]/[SUS]/[SLOP] packages → no blocking checkpoint required. |
</threat_model>

<verification>
- `operator_live.ex` aliases `WorkbenchContract` and maps resolved incidents through `queue_row/1` before `queue_stream_item/1` (CR-01).
- `cd examples/demo_app && mix compile` succeeds.
- `assets/js/app.js` exists and connects a LiveSocket; mix.exs has the esbuild dep + assets.build alias; config.exs has the esbuild profile; layouts.ex root/1 has the CSRF meta + app.js script tag (CR-02).
- Human re-verification (per 21-VERIFICATION.md): after `mix setup && mix assets.build && mix phx.server`, the History tab renders resolved incidents without crashing and clicking an incident opens an interactive detail panel.
</verification>

<success_criteria>
- The History tab renders the seeded resolved incident with no KeyError (CR-01 closed).
- The LiveView WebSocket connects and phx-click events are delivered (CR-02 closed).
- DEMO-01 (resolved state visible) and DEMO-02 (resolved incident viewable, interactive UI) are unblocked.
</success_criteria>

<output>
Create `.planning/phases/21-runnable-demo-app/21-05-SUMMARY.md` when done.
</output>
