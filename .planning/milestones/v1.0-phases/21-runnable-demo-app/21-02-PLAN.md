---
phase: 21-runnable-demo-app
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - examples/demo_app/mix.exs
  - examples/demo_app/config/config.exs
  - examples/demo_app/lib/demo_app_web/router.ex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex
  - examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex
  - examples/demo_app/lib/demo_app_web/components/layouts.ex
  - examples/demo_app/lib/demo_app_web/controllers/error_html.ex
  - examples/demo_app/assets/css/app.css
  - examples/demo_app/assets/tailwind.config.js
  - examples/demo_app/README.md
autonomous: true
requirements: [DEMO-01]

must_haves:
  truths:
    - "GET /parapet routes to DemoAppWeb.Parapet.OperatorLive (open, no auth)"
    - "The three generated Operator LiveView files compile as DemoAppWeb.Parapet.* modules"
    - "mix compile succeeds for the whole demo app"
    - "Tailwind is configured to scan the generated LiveView files so the UI renders styled"
  artifacts:
    - path: "examples/demo_app/lib/demo_app_web/router.ex"
      provides: "Open /parapet routes with WARNING comment"
      contains: "DemoAppWeb.Parapet.OperatorLive"
    - path: "examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex"
      provides: "Generated Operator queue LiveView"
      contains: "defmodule DemoAppWeb.Parapet.OperatorLive"
    - path: "examples/demo_app/assets/tailwind.config.js"
      provides: "Tailwind content globs covering generated LiveView files"
      contains: "live/parapet"
    - path: "examples/demo_app/README.md"
      provides: "Quick start + demo-only auth warning"
      contains: "mix setup"
  key_links:
    - from: "lib/demo_app_web/router.ex"
      to: "DemoAppWeb.Parapet.OperatorLive"
      via: "live \"/parapet\" route"
      pattern: "live \"/parapet\", DemoAppWeb.Parapet.OperatorLive"
    - from: "lib/demo_app_web/live/parapet/operator_live.ex"
      to: "Parapet.Operator"
      via: "list_incident_queue / incident_detail calls"
      pattern: "Parapet.Operator\\."
---

<objective>
Wire the Operator UI into the demo app: generate (and commit) the three Parapet Operator
LiveView files via `mix parapet.gen.ui` run against the DemoApp scaffold, add an OPEN
/parapet router scope with the demo-only warning, supply the supporting layouts/error view
the endpoint references, configure Tailwind so the committed templates render styled, and
write the README. After this plan, `mix compile` and `mix ecto.migrate` succeed for the
whole app.

Purpose: DEMO-01 requires the Operator UI to be served at /parapet. The LiveView files are
the COMMITTED OUTPUT of the parapet generator (proving the generator works), not
hand-authored. The router uses open routes (D-12) because the DEMO-03 smoke test requires
GET /parapet to return 200 directly (not 302/401).

Output: A compilable Phoenix app whose /parapet route resolves to the generated Operator
LiveView, with Tailwind configured and a README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-runnable-demo-app/21-CONTEXT.md
@.planning/phases/21-runnable-demo-app/21-RESEARCH.md
@.planning/phases/21-runnable-demo-app/21-PATTERNS.md
@.planning/phases/21-runnable-demo-app/21-UI-SPEC.md
@.planning/phases/21-runnable-demo-app/21-01-SUMMARY.md

<interfaces>
<!-- Verified against the parapet codebase. -->

`mix parapet.gen.ui` (lib/mix/tasks/parapet.gen.ui.ex) generates 3 files into
`lib/<app_name>_web/live/parapet/`: operator_live.ex, operator_detail_live.ex,
operator_components.ex. Generator assigns derived from the app: web_module=DemoAppWeb,
app_name=:demo_app, repo_module=DemoApp.Repo. Generated module names:
  DemoAppWeb.Parapet.OperatorLive
  DemoAppWeb.Parapet.OperatorDetailLive
  DemoAppWeb.Parapet.OperatorComponents

The generated OperatorLive (template operator_live.ex.eex) calls:
  use DemoAppWeb, :live_view
  import DemoAppWeb.Parapet.OperatorComponents
  DemoApp.Repo.all(Parapet.Operator.action_items_query())   # mount
  Parapet.Operator.incident_detail(id)                       # handle_params
  Parapet.Operator.list_incident_queue(queue_params)         # load_queue_page
  DemoApp.Repo.get!(Parapet.Spine.Incident, id)              # acknowledge/resolve
DemoApp.Repo must be the configured :parapet repo (done in Plan 01) and must exist (Plan 01).

WARNING ABOUT THE GENERATOR ROUTER SNIPPET: `parapet.gen.ui` ALSO prints (does not inject)
a commented router snippet (priv/templates/parapet.gen.ui/router_snippet.ex.eex) that
references AUTHENTICATED scopes and the WRONG module names
(`Parapet.OperatorLive.Index` / `Parapet.OperatorDetailLive.Show`). DO NOT use that snippet.
Use RESEARCH.md Pattern 2 (open routes) with the CORRECT module names
(DemoAppWeb.Parapet.OperatorLive / DemoAppWeb.Parapet.OperatorDetailLive).

Open route pattern (RESEARCH.md Pattern 2, lines 573-598):
  scope "/", DemoAppWeb do
    pipe_through :browser
    live_session :parapet_operator do
      live "/parapet", DemoAppWeb.Parapet.OperatorLive, :index
      live "/parapet/:id", DemoAppWeb.Parapet.OperatorDetailLive, :show
    end
  end

UI-SPEC.md: the visual design is FIXED by the committed templates. Do not alter copy,
colors, spacing, or typography in the generated files. The only UI decision in this plan is
configuring Tailwind so the existing classes render. The Tailwind content glob MUST include
the generated LiveView files (UI-SPEC.md "Tailwind Build Requirement").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit the Operator LiveView files; add layouts and error view</name>
  <files>examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex, examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex, examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex, examples/demo_app/lib/demo_app_web/components/layouts.ex, examples/demo_app/lib/demo_app_web/controllers/error_html.ex</files>
  <read_first>
    - lib/mix/tasks/parapet.gen.ui.ex (generator assigns + output paths)
    - priv/templates/parapet.gen.ui/operator_live.ex.eex (the template that becomes operator_live.ex)
    - priv/templates/parapet.gen.ui/operator_components.ex.eex (component module + Tailwind classes)
    - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
    - examples/demo_app/lib/demo_app_web.ex (the DemoAppWeb entry module from Plan 01 — confirms live_view/0 and html/0 quotes)
    - examples/demo_app/config/config.exs (confirms render_errors references DemoAppWeb.ErrorHTML)
    - .planning/phases/21-runnable-demo-app/21-PATTERNS.md (generated LiveView pattern section lines 277-293)
    - .planning/phases/21-runnable-demo-app/21-UI-SPEC.md (design contract — do not deviate)
  </read_first>
  <action>
Generate the Operator UI by running the parapet generator against the demo app, then commit
its output. Preferred mechanism: from inside `examples/demo_app`, run
`mix parapet.gen.ui --yes` (Igniter). If the generator cannot run in this context, fall back
to hand-copying the three templates with EEx assigns resolved manually: web_module=DemoAppWeb,
app_name=:demo_app, repo_module=DemoApp.Repo — substituting `<%= inspect(@web_module) %>` →
`DemoAppWeb`, `<%= inspect(@repo_module) %>` → `DemoApp.Repo`, `<%= @app_name %>` →
`demo_app`. The committed files must contain NO unresolved EEx tags.

The three committed files MUST be:
  - lib/demo_app_web/live/parapet/operator_live.ex defining
    `DemoAppWeb.Parapet.OperatorLive` (uses `use DemoAppWeb, :live_view`, imports
    `DemoAppWeb.Parapet.OperatorComponents`, calls `DemoApp.Repo` and `Parapet.Operator.*`)
  - lib/demo_app_web/live/parapet/operator_detail_live.ex defining
    `DemoAppWeb.Parapet.OperatorDetailLive`
  - lib/demo_app_web/live/parapet/operator_components.ex defining
    `DemoAppWeb.Parapet.OperatorComponents`
Do NOT modify the template-produced copy, classes, or logic (UI-SPEC.md fidelity contract).

Create `lib/demo_app_web/components/layouts.ex` as `DemoAppWeb.Layouts`. If Plan 01 created
a minimal Layouts inline in demo_app_web.ex, MOVE it here as the canonical module and remove
the inline version to avoid a duplicate definition. `use DemoAppWeb, :html`. Provide
`def root(assigns)` returning a full HTML document shell: `<!DOCTYPE html>`, `<html>`,
`<head>` with charset + viewport meta, a `<.live_title>` or static `<title>`, a
`<link rel="stylesheet" href={~p"/assets/app.css"}>`, and `<body>` rendering `{@inner_content}`.
Provide `def app(assigns)` rendering `{@inner_content}` (the Operator LiveView supplies its
own chrome). `put_root_layout` in the router (Task 2) resolves to `{DemoAppWeb.Layouts, :root}`.

Create `lib/demo_app_web/controllers/error_html.ex` as `DemoAppWeb.ErrorHTML`:
`use DemoAppWeb, :html` and `def render(template, _assigns)` returning
`Phoenix.Controller.status_message_from_template(template)` so the endpoint's
`render_errors: [formats: [html: DemoAppWeb.ErrorHTML]]` config resolves without templates.
  </action>
  <verify>
    <automated>grep -q 'defmodule DemoAppWeb.Parapet.OperatorLive' examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex && grep -q 'defmodule DemoAppWeb.Parapet.OperatorDetailLive' examples/demo_app/lib/demo_app_web/live/parapet/operator_detail_live.ex && grep -q 'defmodule DemoAppWeb.Parapet.OperatorComponents' examples/demo_app/lib/demo_app_web/live/parapet/operator_components.ex && grep -q 'defmodule DemoAppWeb.Layouts' examples/demo_app/lib/demo_app_web/components/layouts.ex && ! grep -rq '<%=' examples/demo_app/lib/demo_app_web/live/parapet/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `operator_live.ex` defines `DemoAppWeb.Parapet.OperatorLive` and contains `use DemoAppWeb, :live_view`
    - `operator_live.ex` contains `Parapet.Operator.` and `DemoApp.Repo` references (resolved, not literal `<%= %>`)
    - No file under `live/parapet/` contains an unresolved `<%=` EEx tag
    - `operator_detail_live.ex` defines `DemoAppWeb.Parapet.OperatorDetailLive`
    - `operator_components.ex` defines `DemoAppWeb.Parapet.OperatorComponents`
    - `layouts.ex` defines `DemoAppWeb.Layouts` with `def root(assigns)`
    - `error_html.ex` defines `DemoAppWeb.ErrorHTML` and contains `status_message_from_template`
  </acceptance_criteria>
  <done>The three Operator LiveView files are committed with DemoAppWeb.Parapet.* module names and no unresolved EEx tags; Layouts and ErrorHTML support modules exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add the open /parapet router, Tailwind assets, README; verify full compile + migrate</name>
  <files>examples/demo_app/lib/demo_app_web/router.ex, examples/demo_app/mix.exs, examples/demo_app/config/config.exs, examples/demo_app/assets/css/app.css, examples/demo_app/assets/tailwind.config.js, examples/demo_app/README.md</files>
  <read_first>
    - examples/demo_app/lib/demo_app_web/live/parapet/operator_live.ex (confirm exact module names to route to — produced in Task 1)
    - examples/demo_app/mix.exs (current deps + aliases from Plan 01 — add tailwind/heroicons + assets aliases)
    - examples/demo_app/config/config.exs (add :tailwind config)
    - .planning/phases/21-runnable-demo-app/21-RESEARCH.md (Pattern 2 open router lines 568-598; Pitfall 4 browser pipeline lines 824-828; Standard Stack Tailwind note lines 384-388)
    - .planning/phases/21-runnable-demo-app/21-UI-SPEC.md (Tailwind Build Requirement lines 174-191; Copywriting Contract README rows lines 134-137)
    - .planning/phases/21-runnable-demo-app/21-PATTERNS.md (router pattern lines 243-273)
  </read_first>
  <action>
Create `lib/demo_app_web/router.ex` as `DemoAppWeb.Router` per RESEARCH.md Pattern 2. Define
a `:browser` pipeline: `plug :accepts, ["html"]`, `plug :fetch_session`,
`plug :fetch_live_flash`, `plug :put_root_layout, html: {DemoAppWeb.Layouts, :root}`,
`plug :protect_from_forgery`, `plug :put_secure_browser_headers`. Then an OPEN scope with
the EXACT demo-only warning comment from D-12/UI-SPEC.md verbatim:
`# WARNING: demo only — do not copy to production.` plus two follow-on comment lines stating
Parapet does not provide its own auth and that production must use an authenticated scope.
Inside `scope "/", DemoAppWeb do pipe_through :browser` add a `live_session :parapet_operator
do` block with `live "/parapet", DemoAppWeb.Parapet.OperatorLive, :index` and
`live "/parapet/:id", DemoAppWeb.Parapet.OperatorDetailLive, :show`. Do NOT use the
generator's authenticated router_snippet; do NOT use `Parapet.OperatorLive.Index`/`.Show`.

Add asset tooling for Tailwind so the committed templates render styled (UI-SPEC.md). In
`examples/demo_app/mix.exs` add deps `{:tailwind, "~> 0.2", runtime: Mix.env() == :dev}` and
`{:heroicons, github: "tailwindlabs/heroicons", tag: "v2.1.1", sparse: "optimized",
app: false, compile: false, depth: 1}`, and add an `"assets.build": ["tailwind default"]`
and `"assets.deploy": ["tailwind default --minify", "phx.digest"]` alias. In
`examples/demo_app/config/config.exs` add `config :tailwind, version: "3.4.3", default:
[args: ~w(--config=tailwind.config.js --input=css/app.css
--output=../priv/static/assets/app.css), cd: Path.expand("../assets", __DIR__)]`.

Create `examples/demo_app/assets/css/app.css` importing Tailwind layers
(`@import "tailwindcss/base"; @import "tailwindcss/components"; @import
"tailwindcss/utilities";`). Create `examples/demo_app/assets/tailwind.config.js` whose
`content` array includes `"./js/**/*.js"`, `"../lib/demo_app_web/**/*.{ex,heex}"`,
`"../lib/demo_app_web/live/parapet/**/*.ex"` (committed Operator UI output), AND the parapet
template source `"../../../priv/templates/parapet.gen.ui/*.eex"` so every Tailwind class used
by the Operator UI is retained after purge (UI-SPEC.md Tailwind Build Requirement).

Create `examples/demo_app/README.md` with the EXACT strings from UI-SPEC.md Copywriting
Contract: a primary CTA "Clone the repo, run `mix setup`, then `mix phx.server`"; an
error-guidance line "If seeds fail, verify PostgreSQL is running on localhost:5432 with user
`postgres` / password `postgres`."; and the warning "WARNING: demo only — do not copy router
config to production. Parapet does not provide its own auth." Include a section noting the
demo serves the Operator UI at http://localhost:4000/parapet, that styling requires
`mix assets.build` if not auto-built in dev, and that the demo is excluded from the published
Hex package.

Then verify the entire app compiles and the schema applies end to end:
`cd examples/demo_app && mix deps.get && mix compile` must succeed, and
`cd examples/demo_app && MIX_ENV=test mix ecto.create && MIX_ENV=test mix ecto.migrate`
must succeed against local Postgres (this exercises the Plan 01 migration).
  </action>
  <verify>
    <automated>cd examples/demo_app && mix deps.get >/dev/null 2>&1; mix compile 2>&1 | tail -5; grep -q 'live "/parapet", DemoAppWeb.Parapet.OperatorLive' lib/demo_app_web/router.ex && ! grep -q 'OperatorLive.Index' lib/demo_app_web/router.ex && grep -q 'live/parapet' assets/tailwind.config.js && grep -q 'mix setup' README.md && echo VERIFY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `router.ex` contains `live "/parapet", DemoAppWeb.Parapet.OperatorLive, :index` and `live "/parapet/:id", DemoAppWeb.Parapet.OperatorDetailLive, :show`
    - `router.ex` contains the verbatim comment `# WARNING: demo only — do not copy to production.`
    - `router.ex` does NOT contain `OperatorLive.Index` or `OperatorDetailLive.Show`
    - `router.ex` `:browser` pipeline contains `plug :put_root_layout, html: {DemoAppWeb.Layouts, :root}`
    - `assets/tailwind.config.js` `content` includes a `live/parapet` glob
    - `README.md` contains "mix setup", the demo-only warning, and the localhost:5432 seed-failure guidance
    - `cd examples/demo_app && mix compile` exits 0
    - `cd examples/demo_app && MIX_ENV=test mix ecto.create && MIX_ENV=test mix ecto.migrate` exits 0
  </acceptance_criteria>
  <done>The whole demo app compiles, /parapet routes to the open Operator LiveView, Tailwind is configured to retain Operator UI classes, README has the required strings, and the migration applies cleanly to a test DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /parapet | unauthenticated browser requests reach the Operator UI |
| asset dep fetch | tailwind/heroicons pulled from Hex + GitHub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Elevation of Privilege | open /parapet route (no auth) | accept | Intentional per D-12 — the DEMO-03 smoke test requires GET /parapet to return 200 directly. Mitigated by a verbatim `# WARNING: demo only — do not copy to production.` comment in the router AND a matching warning in README.md. The demo app is never published (Plan 04 verifies Hex exclusion) and ships no real user data. |
| T-21-05 | Tampering | router CSRF / secure headers | mitigate | The `:browser` pipeline keeps `plug :protect_from_forgery` and `plug :put_secure_browser_headers` — auth is omitted but CSRF and secure-header protections remain in place. |
| T-21-06 | Tampering | heroicons GitHub dep | accept | Pinned to immutable tag `v2.1.1` from the official tailwindlabs/heroicons repo (RESEARCH.md Standard Stack); compile:false/app:false so it ships no runtime code. |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No npm/pip/cargo installs. Tailwind binary is fetched by the `tailwind` Hex package at build time (standard Phoenix tooling); heroicons is a tag-pinned GitHub Hex dep. No [ASSUMED]/[SUS] packages → no blocking human checkpoint. |
</threat_model>

<verification>
- `cd examples/demo_app && mix compile` exits 0 (whole app compiles, including generated LiveViews).
- `/parapet` open route present and points at correct module names; generator's wrong-name authenticated snippet NOT used.
- Tailwind content glob covers the committed Operator UI files.
- `MIX_ENV=test mix ecto.migrate` applies the Plan 01 migration cleanly (runbook_data/trace_id columns confirmed present at the DB level).
</verification>

<success_criteria>
- The demo app compiles end to end.
- GET /parapet is wired to the open generated Operator LiveView (verified structurally; runtime 200 verified by the smoke test in Plan 03).
- README and router carry the exact demo-only warning copy.
- Tailwind is configured so the Operator UI renders styled for human visitors.
</success_criteria>

<output>
Create `.planning/phases/21-runnable-demo-app/21-02-SUMMARY.md` when done.
</output>
