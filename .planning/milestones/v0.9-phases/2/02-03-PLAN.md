---
phase: 02-database-scale
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - lib/mix/tasks/parapet.archive.ex
  - test/mix/tasks/parapet.archive_test.exs
  - lib/parapet/evidence/archive_worker.ex
  - test/parapet/evidence/archive_worker_test.exs
autonomous: true
requirements:
  - SCALE-01
must_haves:
  truths:
    - OS cron users can run `mix parapet.archive` to prune evidence.
    - Oban users have `Parapet.Evidence.ArchiveWorker` available as an optional background job.
  artifacts:
    - path: lib/mix/tasks/parapet.archive.ex
      provides: CLI task for archival.
    - path: lib/parapet/evidence/archive_worker.ex
      provides: Oban worker for archival.
  key_links:
    - from: Executors
      to: Parapet.Evidence.Archiver
      via: archive/3 call
      pattern: Archiver.archive
---

<objective>
Expose the Archival logic via a Mix Task (`mix parapet.archive`) and a conditionally-compiled Oban worker (`Parapet.Evidence.ArchiveWorker`).

Purpose: Allow flexible scheduling of database pruning based on the host application's infrastructure.
Output: Archival executors and tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/v0.9-phases/2/ASSUMPTIONS.md
@.planning/v0.9-phases/2/RESEARCH.md
@.planning/v0.9-phases/2/PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement `mix parapet.archive` task</name>
  <files>lib/mix/tasks/parapet.archive.ex, test/mix/tasks/parapet.archive_test.exs</files>
  <behavior>
    - Parses `--days` (integer, default 90) and `--path` (string, default "priv/parapet/archive.jsonl") via `OptionParser`.
    - Looks up the host Repo via `Application.fetch_env!(:parapet, :repo)`.
    - Calls `Parapet.Evidence.Archiver.archive(Repo, path, days)`.
    - Outputs a JSON formatted result via `Mix.shell().info/1`.
  </behavior>
  <action>
    Implement the standard Mix Task following `mix parapet.doctor` pattern. Provide a unit test simulating the CLI arguments and asserting it correctly extracts the repo and invokes the archiver.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/parapet.archive_test.exs</automated>
  </verify>
  <done>CLI correctly parses args and calls Archiver.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `Parapet.Evidence.ArchiveWorker`</name>
  <files>lib/parapet/evidence/archive_worker.ex, test/parapet/evidence/archive_worker_test.exs</files>
  <behavior>
    - Conditionally compiled `if Code.ensure_loaded?(Oban) do`.
    - Uses `Oban.Worker`.
    - The `perform` function extracts `"days"` and `"path"` from the job args (using defaults if absent).
    - Calls `Parapet.Evidence.Archiver.archive(Repo, path, days)`.
  </behavior>
  <action>
    Implement the worker conditionally, similar to `Parapet.Probe.ObanScheduler`. The test should also mock/assert `Code.ensure_loaded?(Oban)` behavior or simply test the worker logic safely.
  </action>
  <verify>
    <automated>mix test test/parapet/evidence/archive_worker_test.exs</automated>
  </verify>
  <done>Worker invokes Archiver correctly when Oban is present.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Job Args -> Archiver | Oban job args injected by operator. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Job Arguments | accept | Job arguments are trusted since they are created by the application code/cron itself, not user input. |
</threat_model>

<verification>
mix test test/mix/tasks/parapet.archive_test.exs test/parapet/evidence/archive_worker_test.exs
</verification>

<success_criteria>
Both OS cron and Oban based schedulers are able to execute the database pruning strategy correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-scale/03-SUMMARY.md`
</output>
