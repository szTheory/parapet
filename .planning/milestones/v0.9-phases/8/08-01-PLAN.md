---
phase: 08-close-day-1-install-and-doctor-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v0.9-phases/4/VERIFICATION.md
autonomous: true
requirements:
  - DX-01.a
  - DX-01.b
  - AC-01
must_haves:
  truths:
    - "Phase 4 has a dedicated closure-grade `.planning/v0.9-phases/4/VERIFICATION.md` that proves the shipped Day-1 install and doctor flow with fresh evidence."
    - "The verification artifact proves the public installer and doctor contracts through targeted task tests, doc-contract checks, and one fresh Phoenix host smoke lane per D-04, D-05, and D-06."
    - "The manual fresh-host smoke lane is preserved as human proof inside the verification artifact, not widened into a permanent ExUnit harness, per D-04 and D-15."
    - "The report stays honest about scope: core install by default, optional operator UI only when explicitly requested and available, and `mix parapet.doctor cluster` as an honesty/reporting check rather than a distributed-correctness proof, per D-07, D-10, D-12, and D-14."
  artifacts:
    - path: .planning/v0.9-phases/4/VERIFICATION.md
      provides: Canonical closure artifact for the existing Phase 4 implementation per D-01 and D-02.
    - path: lib/mix/tasks/parapet.install.ex
      provides: Shipped public Day-1 installer contract and summary notice cited by the verification report.
    - path: lib/mix/tasks/parapet.doctor.ex
      provides: Shipped doctor severity, threshold, and runtime cluster-mode contract cited by the verification report.
    - path: scripts/setup_sandbox.sh
      provides: Fresh-host smoke-lane helper that can be used as the manual proof scaffold without becoming a permanent merge gate.
  key_links:
    - from: .planning/v0.9-phases/4/VERIFICATION.md
      to: test/mix/tasks/parapet.install_test.exs
      via: captured rerun output for installer composition, explicit extras, and summary notice
      pattern: parapet.install|Parapet install summary|with_ui|skip_ui
    - from: .planning/v0.9-phases/4/VERIFICATION.md
      to: test/mix/tasks/parapet.doctor_test.exs
      via: captured rerun output for severity thresholds, cluster_static posture, and runtime cluster mode
      pattern: threshold|cluster_static|cluster|warn|error
    - from: .planning/v0.9-phases/4/VERIFICATION.md
      to: README.md
      via: doc-contract grep and smoke-lane handoff to `mix parapet.doctor`
      pattern: mix parapet\\.install|mix parapet\\.doctor|--with-ui|--skip-ui
    - from: .planning/v0.9-phases/4/VERIFICATION.md
      to: docs/operator-ui.md
      via: optional UI and host-owned auth wording checked against the shipped contract
      pattern: mix parapet\\.install --with-ui|mix parapet\\.doctor|does \\*\\*not\\*\\* provide its own authentication system
---

<objective>
Create the canonical Phase 4 closure artifact first.

Purpose: prove the already-shipped Day-1 installer, doctor, and docs handoff surfaces with fresh executable evidence and one recorded manual smoke lane, without changing product posture.
Output: `.planning/v0.9-phases/4/VERIFICATION.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.9-phases/8/08-CONTEXT.md
@.planning/v0.9-phases/8/RESEARCH.md
@.planning/v0.9-phases/8/PATTERNS.md
@.planning/v0.9-phases/8/08-VALIDATION.md
@.planning/phases/04-unified-install-path-dx/04-CONTEXT.md
@.planning/phases/04-unified-install-path-dx/04-VALIDATION.md
@.planning/phases/04-unified-install-path-dx/04-01-SUMMARY.md
@.planning/phases/04-unified-install-path-dx/04-02-SUMMARY.md
@.planning/phases/04-unified-install-path-dx/04-03-SUMMARY.md
@.planning/v0.9-phases/2/VERIFICATION.md
@.planning/v0.9-phases/5/VERIFICATION.md
@lib/mix/tasks/parapet.install.ex
@lib/mix/tasks/parapet.doctor.ex
@test/mix/tasks/parapet.install_test.exs
@test/mix/tasks/parapet.doctor_test.exs
@README.md
@docs/operator-ui.md
@scripts/setup_sandbox.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the Day-1 proof stack and capture exact evidence for the closure report</name>
  <read_first>
    .planning/v0.9-phases/8/08-CONTEXT.md
    .planning/v0.9-phases/8/08-VALIDATION.md
    .planning/v0.9-phases/5/VERIFICATION.md
    test/mix/tasks/parapet.install_test.exs
    test/mix/tasks/parapet.doctor_test.exs
    README.md
    docs/operator-ui.md
    scripts/setup_sandbox.sh
  </read_first>
  <files>.planning/v0.9-phases/4/VERIFICATION.md</files>
  <action>
    Per D-04 through D-10, rerun the layered Phase 8 proof stack in this exact order and preserve the raw outcomes for the verification artifact: `mix test test/mix/tasks/parapet.install_test.exs`, `mix test test/mix/tasks/parapet.doctor_test.exs`, then `rg -n 'mix parapet\\.install|mix parapet\\.doctor|--with-ui|--skip-ui|cluster|does \\*\\*not\\*\\* provide its own authentication system' README.md docs/operator-ui.md`. After the automated proof lanes, execute one fresh Phoenix host smoke lane as manual proof using `scripts/setup_sandbox.sh` or an equivalent disposable-host flow, then record the exact command sequence and observed outcomes for `mix deps.get`, `mix parapet.install`, generated host-owned artifacts/config, `mix parapet.doctor`, and optional `mix parapet.doctor cluster`. Treat the smoke lane as required recorded evidence per D-04, D-05, and D-06, but do not convert it into a new permanent ExUnit harness or widen the proof to external Prometheus/Grafana/provider runtime claims per D-09.
  </action>
  <acceptance_criteria>
    - Exact pass/fail outcomes from the installer test file are available to cite.
    - Exact pass/fail outcomes from the doctor test file are available to cite.
    - The doc-contract grep results cover README and operator UI wording for install, doctor, optional UI, and cluster-mode honesty.
    - A fresh-host manual transcript exists for `mix parapet.install -> mix parapet.doctor`, with any `cluster` invocation recorded only as an honesty/reporting check.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/parapet.install_test.exs && mix test test/mix/tasks/parapet.doctor_test.exs && rg -n 'mix parapet\.install|mix parapet\.doctor|--with-ui|--skip-ui|cluster|does \*\*not\*\* provide its own authentication system' README.md docs/operator-ui.md && test -x scripts/setup_sandbox.sh</automated>
  </verify>
  <done>The targeted proof lanes and the manual smoke-lane transcript are ready to be cited exactly in `.planning/v0.9-phases/4/VERIFICATION.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Write the canonical Phase 4 verification report in the closure-grade repo format</name>
  <read_first>
    .planning/v0.9-phases/4/VERIFICATION.md
    .planning/v0.9-phases/5/VERIFICATION.md
    .planning/v0.9-phases/2/VERIFICATION.md
    .planning/v0.9-phases/8/08-CONTEXT.md
    .planning/phases/04-unified-install-path-dx/04-01-SUMMARY.md
    .planning/phases/04-unified-install-path-dx/04-02-SUMMARY.md
    .planning/phases/04-unified-install-path-dx/04-03-SUMMARY.md
    lib/mix/tasks/parapet.install.ex
    lib/mix/tasks/parapet.doctor.ex
    README.md
    docs/operator-ui.md
  </read_first>
  <files>.planning/v0.9-phases/4/VERIFICATION.md</files>
  <action>
    Create `.planning/v0.9-phases/4/VERIFICATION.md` per D-01, D-02, and D-03 using the same section order as Phase 2 and Phase 5: frontmatter, `Goal Achievement`, `Observable Truths`, `Behavioral Spot-Checks`, `Plan Output Check`, `Requirements Coverage`, `Human Verification Required`, and `Gaps Summary`. In `Observable Truths`, prove the shipped contract rather than redesigning it: default install composes the core Day-1 path, doctor exposes severity/threshold/runtime cluster posture honestly, docs match the shipped default-vs-optional UI posture, and a fresh Phoenix host can follow install -> doctor -> docs handoff without Parapet auto-owning auth or optional dependencies per D-08, D-12, and D-14. In `Behavioral Spot-Checks`, record the exact commands and outcomes from Task 1, including the manual smoke transcript. In `Requirements Coverage`, mark `DX-01.a`, `DX-01.b`, and corrected `AC-01` satisfied with explicit evidence, using the corrected AC wording from D-13 rather than the stale UI-by-default wording from the old requirement text. Keep `Human Verification Required` non-empty and point it at the recorded fresh-host smoke lane. Keep `Gaps Summary` honest about excluded runtime surfaces per D-09 and D-10.
  </action>
  <acceptance_criteria>
    - `.planning/v0.9-phases/4/VERIFICATION.md` exists and follows the repo’s verification section order exactly.
    - The report cites exact rerun commands and exact smoke-lane observations rather than summary-only prose.
    - `DX-01.a`, `DX-01.b`, and corrected `AC-01` each have explicit evidence rows.
    - The report states the optional UI boundary and `doctor cluster` honesty boundary without widening shipped scope.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/v0.9-phases/4/VERIFICATION.md && rg -n 'Goal Achievement|Observable Truths|Behavioral Spot-Checks|Plan Output Check|Requirements Coverage|Human Verification Required|Gaps Summary|DX-01.a|DX-01.b|AC-01|mix parapet\\.install|mix parapet\\.doctor' .planning/v0.9-phases/4/VERIFICATION.md</automated>
  </verify>
  <done>The new Phase 4 verification artifact is the canonical closure report and is ready for narrow traceability reconciliation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test/doc/smoke outputs -> verification artifact | Closure claims must match fresh evidence exactly or the milestone state becomes misleading. |
| Existing implementation summaries -> final proof claims | Summary files can support the report, but they cannot replace rerun-based proof. |
| Manual smoke lane -> canonical artifact | Human-observed public install flow must be recorded faithfully without turning into a misleading automated guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Repudiation | `.planning/v0.9-phases/4/VERIFICATION.md` | mitigate | Cite exact commands, exact results, and exact source anchors from installer tests, doctor tests, doc grep, and smoke transcript. |
| T-08-02 | Tampering | AC-01 closure claim | mitigate | Use corrected AC wording per D-11 through D-13 instead of overstating UI-by-default behavior. |
| T-08-03 | Information Disclosure | fresh-host smoke transcript | accept | The smoke lane uses local disposable Phoenix host output only and does not expose third-party credentials or runtime infrastructure. |
| T-08-04 | Denial of Service | proof scope | mitigate | Keep proof bounded to install/doctor/docs surfaces and explicitly exclude external runtime claims per D-09. |
</threat_model>

<verification>
- Run `mix test test/mix/tasks/parapet.install_test.exs`.
- Run `mix test test/mix/tasks/parapet.doctor_test.exs`.
- Run `rg -n 'mix parapet\.install|mix parapet\.doctor|--with-ui|--skip-ui|cluster|does \*\*not\*\* provide its own authentication system' README.md docs/operator-ui.md`.
- Run one fresh-host smoke lane and record the exact transcript in `.planning/v0.9-phases/4/VERIFICATION.md`.
</verification>

<success_criteria>
- `.planning/v0.9-phases/4/VERIFICATION.md` exists and is the canonical closure artifact for Phase 4.
- The report proves the installer, doctor, docs handoff, and manual smoke lane with fresh evidence.
- The report preserves shipped product posture: core install by default, optional UI explicit, cluster mode honest but not overclaimed.
</success_criteria>

<output>
After completion, create `.planning/v0.9-phases/8/08-01-SUMMARY.md`
</output>
