---
phase: 10-tighten-archive-retention-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - .planning/v0.9-phases/2/VERIFICATION.md
  - .planning/v0.9-phases/10/VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SCALE-01.b
  - AC-02
must_haves:
  truths:
    - "Phase 2 closure proof now states the repaired resolved-only archive contract instead of the contradicted non-open contract."
    - "Phase 10 has rerunnable verification evidence that points to the targeted archive test commands and the preserved active-work rule."
    - "Milestone truth artifacts mark `SCALE-01.b` and `AC-02` as verified only after the corrected runtime proof exists."
    - "The historical milestone audit remains a recorded gap surface until a fresh audit rerun replaces it."
  artifacts:
    - path: ".planning/v0.9-phases/2/VERIFICATION.md"
      provides: "Corrected inherited Phase 2 proof wording and evidence for resolved-only archival"
      min_lines: 50
    - path: ".planning/v0.9-phases/10/VERIFICATION.md"
      provides: "Phase-local closure proof for the archive-retention contract repair"
      min_lines: 45
    - path: ".planning/REQUIREMENTS.md"
      provides: "Verified traceability rows for `SCALE-01.b` and `AC-02` once proof exists"
      min_lines: 60
    - path: ".planning/ROADMAP.md"
      provides: "Phase 10 plan list and closure note linked to the corrected verification surface"
      min_lines: 120
  key_links:
    - from: ".planning/v0.9-phases/10/VERIFICATION.md"
      to: "test/parapet/evidence/archiver_test.exs"
      via: "targeted resolved-only archive proof command"
      pattern: 'mix test .*archiver_test\.exs'
    - from: ".planning/v0.9-phases/2/VERIFICATION.md"
      to: "lib/parapet/evidence/archiver.ex"
      via: "truth statement describing the repaired archive predicate"
      pattern: 'resolved'
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/v0.9-phases/10/VERIFICATION.md"
      via: "traceability row and acceptance evidence for `SCALE-01.b` / `AC-02`"
      pattern: 'SCALE-01\.b|AC-02'
---

<objective>
Reconcile the proof surfaces and milestone truth artifacts to the repaired archive contract without rewriting historical gap evidence out of sequence.

Purpose: Make the closure chain truthful and rerunnable after the runtime fix lands.
Output: Updated Phase 2 verification, a new Phase 10 verification report, and active milestone truth artifacts that trace `SCALE-01.b` and `AC-02` to corrected proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-tighten-archive-retention-semantics/10-RESEARCH.md
@.planning/phases/10-tighten-archive-retention-semantics/10-VALIDATION.md
@.planning/v0.9-phases/2/VERIFICATION.md
@.planning/v0.9-phases/10
@.planning/v0.9-MILESTONE-AUDIT.md
@lib/parapet/evidence/archiver.ex
@lib/mix/tasks/parapet.archive.ex
@lib/parapet/evidence/archive_worker.ex
@lib/parapet/operator.ex
@docs/operator-ui.md
@test/parapet/evidence/archiver_test.exs
@test/mix/tasks/parapet.archive_test.exs
@test/parapet/evidence/archive_worker_test.exs

<interfaces>
From `.planning/v0.9-phases/2/VERIFICATION.md`:
```markdown
| 3 | Old non-open incidents are exported to JSONL in bounded chunks and then hard-deleted through the repo layer. | ✓ VERIFIED | ... |
...
| `SCALE-01` archival/export scope | ✓ SATISFIED | Archiver, mix task, and worker tests passed in this session. |
```

From `.planning/REQUIREMENTS.md`:
```markdown
| SCALE-01.b | Phase 10 | Pending |
| AC-02 | Phase 10 | Pending |
```

From `.planning/v0.9-MILESTONE-AUDIT.md`:
```markdown
- id: "SCALE-01.b"
  status: "unsatisfied"
  evidence: "... archives every incident where state != \"open\" ..."
```

From `lib/parapet/operator.ex` and `docs/operator-ui.md`:
```elixir
@active_queue_states ["open", "investigating"]
```

```markdown
- The default queue remains active-only (`open` and `investigating`).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish closure-grade verification for the repaired archive contract</name>
  <read_first>
    .planning/v0.9-phases/2/VERIFICATION.md
    .planning/v0.9-MILESTONE-AUDIT.md
    .planning/phases/10-tighten-archive-retention-semantics/10-RESEARCH.md
    .planning/phases/10-tighten-archive-retention-semantics/10-VALIDATION.md
    lib/parapet/evidence/archiver.ex
    lib/parapet/operator.ex
    docs/operator-ui.md
    test/parapet/evidence/archiver_test.exs
    test/mix/tasks/parapet.archive_test.exs
    test/parapet/evidence/archive_worker_test.exs
  </read_first>
  <files>.planning/v0.9-phases/2/VERIFICATION.md, .planning/v0.9-phases/10/VERIFICATION.md</files>
  <action>Own only the two verification artifacts in this task. Follow the proof-surface analog in `10-PATTERNS.md`: use `.planning/v0.9-phases/3/VERIFICATION.md` as the structure model for frontmatter, observable truths, behavioral spot-checks, requirements coverage, and gaps summary. First, create `.planning/v0.9-phases/10/VERIFICATION.md` as the phase-local closure proof for Phase 10: it must state the repaired goal, cite the targeted archive commands from `10-VALIDATION.md`, and make the core truth explicit that resolved incidents older than the cutoff archive while `investigating` remains active. Second, rewrite the contradicted Phase 2 wording in `.planning/v0.9-phases/2/VERIFICATION.md` so it no longer claims "non-open" archival; the inherited proof must now describe resolved-only archival, preserved active queue semantics, and the same CLI/worker entry surfaces. Do not edit `.planning/v0.9-MILESTONE-AUDIT.md` here; it stays as the historical pre-fix gap artifact until a fresh audit rerun exists.</action>
  <acceptance_criteria>
    - `.planning/v0.9-phases/10/VERIFICATION.md` exists and includes the exact phrase `resolved incidents older than the retention window`.
    - `.planning/v0.9-phases/10/VERIFICATION.md` includes the exact phrase `` `investigating` remains active work ``.
    - `rg -n 'non-open incidents' .planning/v0.9-phases/2/VERIFICATION.md` returns no matches.
    - `rg -n 'resolved-only|resolved incidents older than the retention window|investigating' .planning/v0.9-phases/2/VERIFICATION.md .planning/v0.9-phases/10/VERIFICATION.md` finds the repaired contract language in both verification surfaces.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'resolved incidents older than the retention window|investigating|mix test test/parapet/evidence/archiver_test\\.exs|mix test test/mix/tasks/parapet.archive_test\\.exs|mix test test/parapet/evidence/archive_worker_test\\.exs' .planning/v0.9-phases/2/VERIFICATION.md .planning/v0.9-phases/10/VERIFICATION.md</automated>
  </verify>
  <done>The phase-local and inherited verification reports both describe the same repaired archive contract and point to rerunnable targeted evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Align active milestone truth artifacts to the repaired proof chain</name>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/v0.9-phases/10/VERIFICATION.md
    .planning/v0.9-phases/2/VERIFICATION.md
    .planning/v0.9-MILESTONE-AUDIT.md
    .planning/STATE.md
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>Limit edits to `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`. Follow the active-truth analogs already mapped in `10-PATTERNS.md`: preserve the current roadmap phase-entry format and requirement traceability-table structure while only swapping the contradicted archive semantics for the repaired resolved-only story. After the repaired verification files exist, mark `SCALE-01.b` and `AC-02` as verified in the traceability section of `REQUIREMENTS.md` and make the acceptance/requirement wording match the resolved-only archive contract instead of the contradicted broader story. Update the Phase 10 roadmap entry so it lists the two plan files and, once the plan is executed, can close against the new verification surfaces without implying the milestone audit itself has already been rerun. Keep the historical `gaps_found` audit truth separate: do not overwrite or "fix" `.planning/v0.9-MILESTONE-AUDIT.md`; instead, make the roadmap/requirements language explicitly point forward to the new verification evidence as the closure bridge.</action>
  <acceptance_criteria>
    - `rg -n '\\| SCALE-01\\.b \\| Phase 10 \\| Verified \\|' .planning/REQUIREMENTS.md` matches the updated traceability row.
    - `rg -n '\\| AC-02 \\| Phase 10 \\| Verified \\|' .planning/REQUIREMENTS.md` matches the updated traceability row.
    - `rg -n 'resolved incidents older than a configurable window|resolved incidents older than the retention window' .planning/REQUIREMENTS.md .planning/ROADMAP.md` shows the repaired contract language.
    - `rg -n '10-01-PLAN\\.md|10-02-PLAN\\.md' .planning/ROADMAP.md` shows the Phase 10 plan list.
    - `rg -n 'fresh .*audit|rerun' .planning/ROADMAP.md` preserves the distinction that a milestone audit rerun is a separate step from this phase closure.
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'SCALE-01\\.b|AC-02|10-01-PLAN\\.md|10-02-PLAN\\.md|resolved incidents older than the retention window|fresh .*audit|rerun' .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <done>`REQUIREMENTS.md` and `ROADMAP.md` truthfully trace `SCALE-01.b` and `AC-02` to corrected verification evidence without rewriting the historical milestone audit out of order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| green runtime proof -> verification artifacts | docs become milestone truth only if they faithfully mirror the executed code/tests |
| verification artifacts -> roadmap/requirements | active milestone status surfaces inherit whatever claims the proof docs make |
| historical audit -> closure bridge | the old gap report must remain historical until a new audit replaces it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | R | `.planning/v0.9-phases/2/VERIFICATION.md` | mitigate | Treat contradictory wording as a blocker: rewrite inherited proof text only after the targeted archive tests are green and reference the concrete rerunnable commands. |
| T-10-06 | T | `.planning/REQUIREMENTS.md` / `.planning/ROADMAP.md` | mitigate | Promote `SCALE-01.b` and `AC-02` to verified only when the new Phase 10 verification file exists and cites the repaired runtime proof. |
| T-10-07 | R | `.planning/v0.9-MILESTONE-AUDIT.md` | mitigate | Do not rewrite the historical `gaps_found` audit in this plan; preserve it as prior evidence until a fresh audit rerun is executed. |
| T-10-08 | I | verification narrative | accept | Verification files intentionally summarize tested archive behavior; Phase 10 does not introduce new sensitive evidence classes beyond the existing archive/test surfaces. |
</threat_model>

<verification>
Run the `rg` checks in both tasks after Plan 10-01 is complete. The proof surfaces are valid only if the repaired verification files and active milestone truth artifacts all use the same resolved-only archive language and continue to distinguish a later milestone audit rerun from this phase closure.
</verification>

<success_criteria>
Phase 10 has its own rerunnable verification report, Phase 2's inherited proof no longer contradicts the runtime contract, and the active milestone truth surfaces promote `SCALE-01.b` and `AC-02` based on corrected evidence without overwriting the historical audit.
</success_criteria>

<output>
After completion, create `.planning/phases/10-tighten-archive-retention-semantics/10-02-SUMMARY.md`
</output>
