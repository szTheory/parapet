---
phase: 05-multi-node-safety-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - lib/parapet/automation/executor.ex
  - test/parapet/automation/executor_test.exs
  - test/parapet/automation/executor_concurrency_test.exs
  - test/parapet/automation/executor_cluster_smoke_test.exs
autonomous: true
requirements:
  - P5-01
  - P5-02
  - SCALE-02
must_haves:
  truths:
    - Multiple nodes racing the same mitigation can produce at most one external mitigation path.
    - Circuit-breaker refusal and claim conflicts are recorded as calm, typed, durable no-op outcomes.
    - The automation proof surface includes real Repo contention tests and one lightweight multi-BEAM smoke canary.
  artifacts:
    - path: lib/parapet/automation/executor.ex
      provides: Claim-service-backed automation execution semantics.
    - path: test/parapet/automation/executor_concurrency_test.exs
      provides: Real Postgres contention proof for executor races and breaker gating.
    - path: test/parapet/automation/executor_cluster_smoke_test.exs
      provides: Multi-node canary proving the executor semantics survive BEAM node fan-out.
  key_links:
    - from: lib/parapet/automation/executor.ex
      to: lib/parapet/automation/claim_service.ex
      via: logical action claim before any mitigation side effect
      pattern: ClaimService
    - from: lib/parapet/automation/executor.ex
      to: Parapet.Operator.execute_runbook_step/3
      via: winner-only mitigation execution with durable idempotency key
      pattern: execute_runbook_step
    - from: test/parapet/automation/executor_concurrency_test.exs
      to: lib/parapet/automation/executor.ex
      via: concurrent calls with one executed effect and loser/no-op outcomes
      pattern: automation_claim_conflicted
---

<objective>
Move the automation executor onto the claim contract and prove mitigation safety under contention.

Purpose: close the current read-before-write race in `Parapet.Automation.Executor` while keeping Oban uniqueness as outer pressure relief rather than the core guarantee.
Output: claim-backed executor logic, updated seam tests, real contention tests, and a multi-node executor smoke canary.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.9-phases/5/RESEARCH.md
@.planning/v0.9-phases/5/PATTERNS.md
@lib/parapet/automation/executor.ex
@lib/parapet/operator/action_payload.ex
@test/parapet/automation/executor_test.exs

<interfaces>
From lib/parapet/automation/executor.ex:
```elixir
use Oban.Worker,
  queue: :default,
  unique: [period: 3600, keys: [:incident_id, :step_id]]

def perform(%Oban.Job{args: %{"incident_id" => incident_id, "step_id" => step_id}})
```

From lib/parapet/operator/action_payload.ex:
```elixir
embedded_schema do
  field(:actor, :string)
  field(:reason, :string)
  field(:correlation_id, :string)
  field(:idempotency_key, :string)
  field(:action_type, Ecto.Enum, values: [:immutable_fact, :narrative_edit, :execute_mitigation, :acknowledge, :resolve])
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire `Parapet.Automation.Executor` to claim first and execute once</name>
  <files>lib/parapet/automation/executor.ex, test/parapet/automation/executor_test.exs</files>
  <behavior>
    - Executor uniqueness remains in place as outer enqueue relief but the actual safety decision comes from the DB-backed claim service per D-01 and D-02.
    - The winner path is the only path that calls `Operator.execute_runbook_step/3`, reusing a durable idempotency key derived from `incident_id + step_id` per D-08 and D-09.
    - Losers and breaker-closed outcomes persist calm typed chronology such as `automation_claim_conflicted` or `automation_short_circuited` instead of duplicate `mitigation_executed` facts per D-03, D-04, D-19, and D-20.
  </behavior>
  <action>
    Replace the current `allow?/2` plus direct side-effect flow in `lib/parapet/automation/executor.ex` with a call into `Parapet.Automation.ClaimService`. Preserve the existing logical idempotency shape `auto_exec_#{incident_id}_#{step_id}` and the host-owned `system:automation:executor` identity, but make the claim outcome decide whether the worker executes the mitigation, short-circuits, or discards as a durable no-op. Update `test/parapet/automation/executor_test.exs` so the fast DummyRepo lane still covers branch shapes, but now asserts the executor delegates through the claim contract and emits the new durable outcome types instead of relying on `aggregate/3` as the only guard.
  </action>
  <verify>
    <automated>mix test test/parapet/automation/executor_test.exs</automated>
  </verify>
  <done>The automation executor only performs the mitigation on the claim-winning path and records stable loser or breaker outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real contention and cluster smoke tests for automation execution</name>
  <files>test/parapet/automation/executor_concurrency_test.exs, test/parapet/automation/executor_cluster_smoke_test.exs</files>
  <behavior>
    - Real Postgres tests prove that concurrent executor attempts produce one executed mitigation path, one winning claim, and coherent evidence rows per D-13 through D-16.
    - At least one multi-BEAM smoke canary exercises the same logical mitigation from separate nodes without claiming stronger guarantees than the DB-backed proof per D-14 and D-17.
    - The tests assert durable end-state invariants, not just mailboxes: claim rows, timeline types, and tool-audit counts.
  </behavior>
  <action>
    Add `test/parapet/automation/executor_concurrency_test.exs` using the new real Postgres concurrency helpers from Plan 01. Seed one incident, race two executor jobs for the same `step_id`, and assert exactly one executed mitigation evidence path, exactly one logical claim row marked executed, and loser outcomes recorded as typed no-ops. Add `test/parapet/automation/executor_cluster_smoke_test.exs` as a narrow multi-node canary using a lightweight BEAM node harness such as `:peer` or `Node.spawn_link`; keep it intentionally small and honest so the main proof stays DB-first. Do not add generic workflow infrastructure or a broad cluster framework.
  </action>
  <verify>
    <automated>mix test test/parapet/automation/executor_concurrency_test.exs test/parapet/automation/executor_cluster_smoke_test.exs</automated>
  </verify>
  <done>Automation contention is proven against real Postgres state, with a small multi-node canary validating the same claim contract across nodes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent Oban jobs -> mitigation callback | Multiple workers may attempt the same external effect simultaneously |
| Multi-BEAM test nodes -> shared host DB | Separate nodes contend through the same Postgres truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Elevation of Privilege | Automation executor side-effect path | mitigate | Only the claim winner calls `Operator.execute_runbook_step/3` under the existing system identity. |
| T-05-05 | Tampering | Circuit-breaker race window | mitigate | Move breaker re-checks into the claim transaction and assert one executed path in real contention tests. |
| T-05-06 | Repudiation | Duplicate mitigation evidence | mitigate | Persist typed loser and short-circuit outcomes so duplicate attempts do not masquerade as executed mitigations. |
</threat_model>

<verification>
- Run `mix test test/parapet/automation/executor_test.exs`.
- Run `mix test test/parapet/automation/executor_concurrency_test.exs test/parapet/automation/executor_cluster_smoke_test.exs`.
- Confirm the multi-node smoke test is a canary only and the real Postgres contention suite remains the primary proof surface.
</verification>

<success_criteria>
- `Parapet.Automation.Executor` no longer relies on a historical count query as its final concurrency guard.
- Automation races produce one effect path, one executed claim, and calm typed loser/no-op evidence.
- SCALE-02 is materially proven for the automation half of Phase 5 through DB-backed tests plus a narrow cluster canary.
</success_criteria>

<output>
After completion, create `.planning/v0.9-phases/5/05-02-SUMMARY.md`
</output>
