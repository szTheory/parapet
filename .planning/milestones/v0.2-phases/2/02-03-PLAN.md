---
phase: "02-in-app-operator-ui"
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - lib/mix/tasks/parapet.doctor.ex
  - test/mix/tasks/parapet.doctor_test.exs
  - docs/operator-ui.md
  - README.md
autonomous: true
requirements:
  - UI-02
  - UI-04
must_haves:
  truths:
    - `mix parapet.doctor` can flag insecure operator UI mounts instead of assuming the generator was used correctly.
    - The repo documents the host-authenticated mounting contract and the action-audit/security model clearly enough for adopters to follow.
    - Doctor output stays CI-friendly and human-readable while reporting operator UI findings separately from existing checks.
  artifacts:
    - path: lib/mix/tasks/parapet.doctor.ex
      provides: operator UI auth-scope verification
    - path: test/mix/tasks/parapet.doctor_test.exs
      provides: regression coverage for secure vs insecure router mounts
    - path: docs/operator-ui.md
      provides: install, mount, and security guide for the generated UI
    - path: README.md
      provides: top-level entry point to the operator UI workflow
  key_links:
    - from: lib/mix/tasks/parapet.doctor.ex
      to: lib/<host>_web/router.ex
      via: static analysis of authenticated LiveView mount placement
    - from: docs/operator-ui.md
      to: lib/mix/tasks/parapet.gen.ui.ex
      via: generated workflow and manual review guidance
---

<objective>
Add the security verification and operator UI documentation layer so the generated LiveView surface is auditable in host apps, not just scaffolded.

Purpose: Close UI-04 and make the auth/router seam explicit, testable, and CI-checkable.
Output: An extended doctor task, doctor tests, and operator UI docs wired into the README.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v0.2-ROADMAP.md
@.planning/milestones/v0.2-REQUIREMENTS.md
@.planning/milestones/v0.2-phases/2-in-app-operator-ui/2-CONTEXT.md
@.planning/milestones/v0.2-phases/2-in-app-operator-ui/RESEARCH.md
@.planning/milestones/v0.2-phases/2-in-app-operator-ui/PATTERNS.md
@lib/mix/tasks/parapet.doctor.ex

<interfaces>
From `lib/mix/tasks/parapet.doctor.ex`:

```elixir
def run(args)
defp check_runbooks
defp check_router
defp check_endpoint
defp print_human(results)
defp print_json(results, exit_code)
```

Doctor extension rule:
- Add operator UI findings as a named check rather than burying them inside generic router messaging.
- Preserve the current exit-code model and `--ci` output path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add operator UI auth-mount checks to doctor</name>
  <files>lib/mix/tasks/parapet.doctor.ex, test/mix/tasks/parapet.doctor_test.exs</files>
  <behavior>
    - Test 1: Doctor reports `:ok` when generated Parapet LiveViews are mounted only inside an authenticated scope/live_session.
    - Test 2: Doctor warns when Parapet operator LiveViews appear in an unauthenticated scope or outside an expected auth marker.
    - Test 3: `--ci` output includes the operator UI check as a distinct key with stable machine-readable status/messages.
  </behavior>
  <action>Extend `mix parapet.doctor` with a dedicated `operator_ui` check per D-13, D-14, D-15, and D-16. Reuse the current static-analysis posture and exit-code behavior, but do not hide operator UI findings inside the existing generic router check. The new check should look for generated Parapet LiveView route markers/mounts and verify they sit inside an authenticated host scope or equivalent protected `live_session`, while keeping the host as the owner of the actual auth policy.</action>
  <verify>
    <automated>mix test test/mix/tasks/parapet.doctor_test.exs</automated>
  </verify>
  <done>Doctor has a separate operator UI security check with test coverage for secure and insecure mount placement in both human and CI modes.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the operator UI install and security guide</name>
  <files>docs/operator-ui.md, README.md</files>
  <action>Document the generated UI workflow end to end: prerequisites, running `mix parapet.gen.ui`, mounting inside host auth, what `mix parapet.doctor` validates, and how the operator workbench preserves evidence-first behavior. Reference D-05 so Grafana/runbooks remain external, D-07 through D-09 so the surface is limited to first-class actions, D-10 through D-12 so factual timeline entries stay immutable, and D-17 through D-19 so every mutating action carries audit context. Update `README.md` to point adopters to this guide without duplicating the full document.</action>
  <verify>
    <automated>mix docs</automated>
  </verify>
  <done>The repo contains a focused operator UI guide and the README directs adopters to it and the doctor verification step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host router/auth config -> doctor static analysis | Parapet inspects but does not own the host's security policy. |
| Documentation -> adopter implementation | Incomplete guidance can lead to insecure mounts even if the generator works. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Elevation of Privilege | `mix parapet.doctor` router analysis | mitigate | Detect operator UI mounts outside authenticated scope/live_session and return warnings with CI-readable status, per D-16. |
| T-02-11 | Repudiation | docs/operator-ui.md | mitigate | Document required audit metadata and immutable factual timeline semantics so adopters understand what the generated actions guarantee. |
| T-02-12 | Tampering | README / docs drift | mitigate | Keep README concise and point to a single operator UI guide to reduce contradictory setup/security instructions. |
</threat_model>

<verification>
mix test test/mix/tasks/parapet.doctor_test.exs
mix docs
</verification>

<success_criteria>
Doctor can detect insecure operator UI mounts.
Adopters have a single operator UI guide covering generation, secure mounting, and audit behavior.
The README clearly routes users to the operator UI docs and doctor verification workflow.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2-phases/2-in-app-operator-ui/02-03-SUMMARY.md`
</output>
