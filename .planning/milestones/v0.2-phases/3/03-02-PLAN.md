---
phase: 3
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/parapet/integrations/rulestead.ex
  - test/parapet/integrations/rulestead_test.exs
autonomous: true
requirements:
  - ECO-01
  - ECO-04
must_haves:
  truths:
    - "Rulestead adapter compiles out cleanly without Rulestead dependency"
    - "Rulestead adapter attaches telemetry for flag changes"
    - "Rulestead adapter registers the 'toggle_flag' mitigation"
  artifacts:
    - path: "lib/parapet/integrations/rulestead.ex"
      provides: "Rulestead integration logic"
      contains: "if Code.ensure_loaded?(Rulestead) do"
  key_links:
    - from: "lib/parapet/integrations/rulestead.ex"
      to: "Parapet.Capabilities"
      via: "Capabilities registration"
      pattern: "Parapet.Capabilities.register_mitigation"
---

<objective>
Implement the Rulestead adapter to track feature flag changes and register UI capabilities.

Purpose: Satisfy ECO-01 safely using the `Code.ensure_loaded?` boundary.
Output: Tested Rulestead integration module.
</objective>

<context>
@.planning/phases/3/PATTERNS.md
@.planning/phases/3/RESEARCH.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Rulestead Adapter</name>
  <files>lib/parapet/integrations/rulestead.ex, test/parapet/integrations/rulestead_test.exs</files>
  <behavior>
    - Test: setup() calls telemetry attach and capabilities register.
    - Test: handle_event emits safely when Rulestead events occur.
  </behavior>
  <action>
    Create `Parapet.Integrations.Rulestead`. Wrap the entire module in `if Code.ensure_loaded?(Rulestead) do`.
    Implement `setup/0` which attaches telemetry to `[:rulestead, :flag, :changed]` and calls `Parapet.Capabilities.register_mitigation(:rulestead, "toggle_flag", %{name: "Toggle Feature Flag", schema: [flag_name: :string, state: :boolean]})`.
    Implement `handle_event/4` wrapped in a `rescue` block (per PATTERNS) to process flag changes securely.
    The new adapter module must be modeled strictly after the closest analog pattern: `lib/parapet/integrations/sigra.ex` (using `Code.ensure_loaded?/1` guards and telemetry safety `rescue` blocks).
  </action>
  <verify>
    <automated>mix test test/parapet/integrations/rulestead_test.exs</automated>
  </verify>
  <done>Rulestead module loads conditionally and passes unit tests simulating telemetry.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Telemetry Handler | Incoming events from sibling library Rulestead |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | DoS | handle_event/4 | mitigate | Wrap `handle_event/4` body in `rescue e -> Logger.error(...)` to prevent telemetry process crashes. |
</threat_model>

<success_criteria>
- Rulestead adapter compiles cleanly.
- `setup/0` registers capability correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/3/03-02-SUMMARY.md`
</output>