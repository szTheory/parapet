---
phase: 3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/capabilities.ex
  - lib/parapet/internal/application.ex
  - lib/parapet.ex
  - lib/parapet/operator.ex
  - test/parapet/capabilities_test.exs
autonomous: true
requirements:
  - ECO-01
  - ECO-04
must_haves:
  truths:
    - "System stores capability data dynamically via Parapet.Capabilities"
    - "Operator UI can retrieve registered capabilities by type"
    - "Host application can activate adapters via Parapet.attach(adapters: [...])"
  artifacts:
    - path: "lib/parapet/capabilities.ex"
      provides: "Registry Agent for capabilities"
      contains: "use Agent"
    - path: "lib/parapet.ex"
      provides: "Adapter activation seam"
      contains: "def attach(adapters: adapters)"
  key_links:
    - from: "lib/parapet/internal/application.ex"
      to: "lib/parapet/capabilities.ex"
      via: "Application supervisor child spec"
      pattern: "{Parapet.Capabilities, \\[\\]}"
---

<objective>
Introduce the capability registry to power UI extensibility and update `Parapet.attach/1` to accept explicit adapter configurations.

Purpose: Establish the compile-out architectural seam required for all phase 3 ecosystem integrations.
Output: Working capability registry and attachment router.
</objective>

<context>
@.planning/phases/3/PATTERNS.md
@.planning/phases/3/RESEARCH.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create Capabilities Registry</name>
  <files>lib/parapet/capabilities.ex, test/parapet/capabilities_test.exs, lib/parapet/internal/application.ex</files>
  <behavior>
    - Test: register_mitigation adds capability to list without duplicates (idempotent via ID).
    - Test: capabilities(:mitigation) returns list of registered capabilities.
  </behavior>
  <action>
    Create `Parapet.Capabilities` backed by an `Agent`. Provide `start_link/1`, `register_mitigation/3`, and `capabilities/1`.
    Update `lib/parapet/internal/application.ex` to start the `Parapet.Capabilities` agent in the supervisor tree.
  </action>
  <verify>
    <automated>mix test test/parapet/capabilities_test.exs</automated>
  </verify>
  <done>Registry can store and retrieve capability schemas idempotently.</done>
</task>

<task type="auto">
  <name>Task 2: Expose Capabilities via Operator Context</name>
  <files>lib/parapet/operator.ex</files>
  <action>
    Add `capabilities(type)` to `Parapet.Operator` that delegates to `Parapet.Capabilities.capabilities(type)`. This is the seam the LiveView UI will use.
  </action>
  <verify>
    <automated>mix run -e "Parapet.Operator.module_info()"</automated>
  </verify>
  <done>Operator context delegates capability queries.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Adapter Activation Seam</name>
  <files>lib/parapet.ex</files>
  <action>
    Modify `Parapet` module to add `def attach(adapters: adapters)` or `def attach(opts)` when `Keyword.keyword?(opts)`.
    It should iterate through the adapters list. For known adapters (e.g., `:rulestead`, `:mailglass`, `:chimeway`, `:accrue`, `:rindle`, `:threadline`), it should conditionally invoke `TargetModule.setup()` only if `Code.ensure_loaded?(TargetModule)` is true. Retain the existing `attach(%{handler_id: ...})` signature intact.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Parapet.attach/1 safely routes adapter activations.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Capability Input | Adapters provide UI parameters via the registry |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | DoS | Parapet.Capabilities | mitigate | Ensure registry insertions are idempotent using map key structures or unique ID checks so memory doesn't leak on repetitive calls. |
</threat_model>

<success_criteria>
- `Parapet.attach/1` supports keyword list `adapters: [...]`.
- Capabilities registry acts as a stateful container in the supervision tree.
</success_criteria>

<output>
After completion, create `.planning/phases/3/03-01-SUMMARY.md`
</output>