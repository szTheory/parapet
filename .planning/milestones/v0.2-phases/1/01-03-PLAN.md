---
phase: 01
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/mix/tasks/parapet.gen.spine.ex
  - test/mix/tasks/parapet.gen.spine_test.exs
autonomous: true
requirements: [SPINE-01, SPINE-02, SPINE-03, SPINE-04]
must_haves:
  truths:
    - "Developers can generate host application migrations for Parapet Spine schemas."
    - "The generator configures `:parapet, :repo` in the host application's config.exs."
  artifacts:
    - path: "lib/mix/tasks/parapet.gen.spine.ex"
      provides: "Igniter task to inject migration and config"
  key_links:
    - from: "lib/mix/tasks/parapet.gen.spine.ex"
      to: "host config.exs"
      via: "Igniter.Project.Config.configure"
---

<objective>
Create the `mix parapet.gen.spine` task.

Purpose: Provide the installation mechanism for host applications to generate the tables required by the Evidence Spine (`parapet_incidents`, `parapet_timeline_entries`, `parapet_tool_audits`) and configure the Ecto Repo.
Output: A Mix task utilizing Igniter to write the migration file and patch `config.exs`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/1/RESEARCH.md
@.planning/phases/1/PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Igniter generator task</name>
  <files>lib/mix/tasks/parapet.gen.spine.ex, test/mix/tasks/parapet.gen.spine_test.exs</files>
  <behavior>
    - `igniter/1` finds the host app Repo using `Igniter.Libs.Ecto.list_repos`.
    - Generates a migration file containing `create table(:parapet_incidents, primary_key: false) do add :id, :binary_id, primary_key: true ...` and the other two tables.
    - Patches `config.exs` to add `config :parapet, repo: HostApp.Repo`.
  </behavior>
  <action>
    Implement `Mix.Tasks.Parapet.Gen.Spine` following the analog pattern from `lib/mix/tasks/parapet.install.ex` (found in PATTERNS.md). Use `Igniter.Libs.Ecto.gen_migration/4` to safely insert the migration with UTC timestamps. The test should use `Igniter.Test` to assert that the migration file is created and the configuration is updated correctly.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/parapet.gen.spine_test.exs</automated>
  </verify>
  <done>Task generates valid Elixir migration syntax and patches config using Igniter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator -> Host App | Igniter manipulates the host file system and configuration safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing | Table Names | mitigate | All tables must be prefixed with `parapet_` to prevent collisions with host application tables. |
</threat_model>

<verification>
mix test test/mix/tasks/parapet.gen.spine_test.exs
</verification>

<success_criteria>
The generator creates migration files properly configuring the `parapet_*` tables and updating `config.exs`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-spine/01-03-PLAN-SUMMARY.md`
</output>