---
phase: 18-adoption-authoring-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/getting-started.md
  - docs/troubleshooting.md
autonomous: true
requirements: [ADOPT-03, ADOPT-04]

must_haves:
  truths:
    - "A new adopter can follow getting-started.md from install to a generated alert with zero raw PromQL"
    - "getting-started.md uses config :parapet, providers: [...] and never the legacy :slos env"
    - "getting-started.md states all three files mix parapet.gen.prometheus writes"
    - "troubleshooting.md answers all five seeded questions with behaviors matching the live code"
    - "troubleshooting.md states --ci is the STRICTER doctor gate, not looser"
  artifacts:
    - path: "docs/getting-started.md"
      provides: "Cold-start tutorial: install to providers config to gen.prometheus to doctor"
      min_lines: 60
    - path: "docs/troubleshooting.md"
      provides: "Five-seed Q&A reference"
      min_lines: 50
  key_links:
    - from: "docs/getting-started.md"
      to: "Parapet.SLO.StarterPack.WebSaaS"
      via: "providers config line"
      pattern: "providers:.*WebSaaS"
    - from: "docs/troubleshooting.md"
      to: "mix parapet.doctor --ci"
      via: "exit-code direction explanation"
      pattern: "--ci"
---

<objective>
Author `docs/getting-started.md` (ADOPT-03 / AC-01) — the cold-start tutorial that takes a stranger from install to a first generated Prometheus alert in under 30 minutes with zero raw PromQL — and `docs/troubleshooting.md` (ADOPT-04) — the five-seed Q&A for the first obstacle.

Purpose: These two docs are the entry point and the recovery net of the adopter funnel. Every command, file path, config key, and behavior must match the live code exactly; the dominant risk is documentation drift.

Output: Two Markdown files in `docs/`, anchored to the existing doc voice and the verified facts in RESEARCH.md. (Registration in `mix.exs` extras and the full `mix docs` build gate happen in the final-wave plan 18-05.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-adoption-authoring-docs/18-CONTEXT.md
@.planning/phases/18-adoption-authoring-docs/18-RESEARCH.md
@.planning/phases/18-adoption-authoring-docs/18-PATTERNS.md

<voice_and_facts>
Voice/analog (D-03): mirror docs/operator-ui.md (one-sentence H1, orienting paragraph, Prerequisites bullet list, sequential bash/elixir fences) and docs/slo-reference.md (short factual sections, prose then fence then one-line explanation). Sentence-case headings, second-person prose, elixir/bash fences only, no emojis.

VERIFIED facts to anchor every claim (cite by symbol in prose, never by line number):
- Cold-start sequence: add the :parapet dep, then mix deps.get and mix parapet.install, then add config :parapet, providers: [Parapet.SLO.StarterPack.WebSaaS], then mix parapet.gen.prometheus, then mix parapet.doctor
- mix parapet.install does NOT auto-add the WebSaaS provider — the providers config line is a manual step the adopter adds after install
- mix parapet.gen.prometheus writes THREE files: priv/parapet/prometheus/recording_rules.yml, alerts.yml, rules.yml (alerts.yml is the first-adopter actionable one)
- WebSaaS pack provides three slices: web_saas_http_availability, web_saas_login_journey, web_saas_oban_job_success — the Generator renders all PromQL (zero raw PromQL promise, D-05)
- Login slice needs parapet_journey_login_count (emitted by the Sigra integration). Without Sigra or another emitter the slice has no data; the min_total_rate guard prevents flapping, but "no data" is not "green" (D-06)
- doctor severity: info=0, warn=1, error=2. mix parapet.doctor defaults to threshold :error (exit 1 only on error); mix parapet.doctor --ci flips to :warn (exit 1 on warn OR error) — STRICTER, not looser (D-15.1)
- doctor checks: endpoint (Parapet.Plug.Metrics presence, warn if missing), router (/metrics route), cluster_static (escalation worker missing unique: emits ERROR)
- Oban metrics: Parapet.Metrics.Oban is wrapped in if Code.ensure_loaded?(Oban), Oban is optional: true — missing because Oban not in deps
- Fly.io: mix parapet.install writes rel/hooks/post_start.sh calling bin/<app> rpc with Parapet.Deploy.mark and $RELEASE_VERSION. Scope to Parapet's side; link OUT to Fly.io docs for scrape/firewall (OQ-2)

NEVER show config :parapet, :slos or Parapet.SLO.define/2 — legacy degraded path (single-warning alerts, no multi-burn). (D-04, RESEARCH pitfall 3)

Cross-link targets (D-02 — link, do not duplicate): docs/adopter-flows.md (conceptual depth), docs/slo-authoring-guide.md (custom SLOs), docs/integrations/sigra.md (login-slice prereq), docs/slo-reference.md (provider/slice catalog plus legacy explanation).
</voice_and_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/getting-started.md (ADOPT-03 / AC-01)</name>
  <files>docs/getting-started.md</files>
  <read_first>
    - docs/getting-started.md (the file being created — confirm it does not exist)
    - docs/operator-ui.md (analog: H1 plus Prerequisites plus sequential command cadence, per PATTERNS.md)
    - docs/slo-reference.md (elixir config fence style, generated-artifacts section listing the three files)
    - lib/parapet/slo/starter_pack/web_saas.ex (the providers line, three slices, login metric dependency)
    - lib/mix/tasks/parapet.install.ex (what install actually scaffolds: instrumenter, endpoint plug, deploy hook; does NOT add providers)
    - lib/mix/tasks/parapet.gen.prometheus.ex (the three output paths)
    - lib/mix/tasks/parapet.doctor.ex (the --ci threshold flip and the endpoint/router/cluster checks)
  </read_first>
  <action>
    Write the cold-start tutorial following the operator-ui.md cadence (one-sentence H1 such as "# Parapet Getting Started", two-sentence orienting paragraph, Prerequisites bullet list, then sequential steps each as: one-sentence lead-in, fenced code, one-sentence elaboration). The exact step sequence (D-04):
    1. Add :parapet to deps (elixir fence for the mix.exs deps entry), then mix deps.get
    2. mix parapet.install (bash fence); state explicitly it scaffolds the instrumenter, endpoint plug, and deploy hook but does NOT activate any SLOs
    3. Add config :parapet, providers: [Parapet.SLO.StarterPack.WebSaaS] to config/config.exs (elixir fence); state this is the manual one-line activation and that the adopter writes ZERO raw PromQL — the WebSaaS pack passes metric names and matchers and the Generator renders all PromQL (D-05)
    4. mix parapet.gen.prometheus (bash fence); name all THREE generated files (recording_rules.yml, alerts.yml, rules.yml under priv/parapet/prometheus/) and identify alerts.yml as the first actionable artifact ("your first generated alert")
    5. mix parapet.doctor (bash fence); explain it exits 1 only on :error locally, and show mix parapet.doctor --ci as the STRICTER CI gate (exits 1 on :warn or :error) — match operator-ui.md's directional framing exactly

    Include the login-slice data caveat (D-06) as a short callout: the web_saas_login_journey slice needs parapet_journey_login_count (emitted by the Sigra integration); without it the slice has no data, the min_total_rate guard prevents false alerts, but "no data" is not "green" — link to docs/integrations/sigra.md.

    End with a "Next steps" section (Claude's discretion, recommended) cross-linking docs/adopter-flows.md (mental model), docs/slo-authoring-guide.md (author your own SLOs), docs/integrations/sigra.md (wire the login slice). Use relative repo-root paths for links (PATTERNS.md cross-linking convention).

    Do NOT mention config :parapet, :slos or Parapet.SLO.define/2 anywhere. No emojis. Do not paste full file contents beyond the small snippets above.
  </action>
  <verify>
    <automated>test -f docs/getting-started.md && grep -q "providers:.*WebSaaS" docs/getting-started.md && grep -q "recording_rules.yml" docs/getting-started.md && grep -q "rules.yml" docs/getting-started.md && grep -q -- "--ci" docs/getting-started.md && grep -q "parapet_journey_login_count" docs/getting-started.md && ! grep -q "config :parapet, :slos" docs/getting-started.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/getting-started.md exists
    - Contains config :parapet, providers: [Parapet.SLO.StarterPack.WebSaaS] (grep providers:.*WebSaaS matches)
    - grep "config :parapet, :slos" docs/getting-started.md returns 0 results (anti-drift check 3)
    - Names all three generated files (recording_rules.yml, alerts.yml, rules.yml all present)
    - Mentions --ci and frames it as the stricter gate (the word "stricter" or equivalent appears near the --ci example)
    - Names the login-slice prerequisite parapet_journey_login_count
    - No emojis present
  </acceptance_criteria>
  <done>getting-started.md walks install to providers config to gen.prometheus (3 files) to doctor, with zero raw PromQL, the login-slice caveat, the correct --ci direction, and no legacy :slos path.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/troubleshooting.md (ADOPT-04) with the five seeds (D-15)</name>
  <files>docs/troubleshooting.md</files>
  <read_first>
    - docs/troubleshooting.md (the file being created — confirm it does not exist)
    - docs/slo-reference.md (analog: short factual Q&A sections, prose then fence then one-line resolution)
    - lib/mix/tasks/parapet.doctor.ex (severity model info/warn/error, parse_threshold --ci flip, check_endpoint, check_router, check_cluster_static unique: ERROR message text)
    - lib/parapet/metrics/oban.ex (Code.ensure_loaded?(Oban) compile-out guard)
    - lib/mix/tasks/parapet.install.ex (deploy hook rel/hooks/post_start.sh, RELEASE_VERSION, Parapet.Deploy.mark/1)
    - lib/mix/tasks/parapet.gen.prometheus.ex (the three output files, for the blank-target seed)
  </read_first>
  <action>
    Write five Q&A sections, each a ## heading phrased as a symptom, one-sentence diagnosis prose, optional bash fence, one-sentence resolution (slo-reference.md rhythm). The exact five seeds with their VERIFIED behaviors (D-15):

    1. Blank Prometheus target — diagnose via the doctor endpoint check (warns if Parapet.Plug.Metrics is absent), the router check (/metrics route), and confirm mix parapet.gen.prometheus wrote the three files under priv/parapet/prometheus/. Show mix parapet.doctor. Not a generic Prometheus tip.
    2. Doctor warn-vs-error / "will CI fail?" — explain severity info=0, warn=1, error=2; mix parapet.doctor fails (exit 1) only on :error; mix parapet.doctor --ci fails on :warn OR :error (STRICTER — get the direction right; this is the most common drift). Show both bash forms.
    3. Oban metrics missing after install — explain Parapet.Metrics.Oban is wrapped in if Code.ensure_loaded?(Oban) and Oban is optional: true; resolution: add {:oban, ">= 0.0.0"} to deps.
    4. Multi-node uniqueness ("concurrent nodes could execute the same escalation twice") — the cluster_static doctor check emits an ERROR when the escalation worker lacks unique:; quote the doctor error message and show the resolution (add Oban unique: to the worker).
    5. Fly.io config — scope to Parapet's side: mix parapet.install writes rel/hooks/post_start.sh calling bin/<app> rpc with Parapet.Deploy.mark and $RELEASE_VERSION. Link OUT to Fly.io's docs (for example https://fly.io/docs/) for scrape config and firewall — do not assert Fly-internal specifics (OQ-2).

    Cross-link to docs/getting-started.md (for the happy path) and docs/operator-ui.md (for UI-specific doctor checks) where relevant. Sentence-case headings, no emojis. Use backtick inline code for module/metric/config/file/task names (PATTERNS.md).
  </action>
  <verify>
    <automated>test -f docs/troubleshooting.md && grep -qi "oban" docs/troubleshooting.md && grep -q -- "--ci" docs/troubleshooting.md && grep -qi "unique" docs/troubleshooting.md && grep -qi "RELEASE_VERSION" docs/troubleshooting.md && grep -qi "Parapet.Plug.Metrics" docs/troubleshooting.md && grep -c "^## " docs/troubleshooting.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/troubleshooting.md exists
    - At least five ## symptom sections present (grep -c "^## " returns 5 or more; seeds 1-5: blank Prometheus target, doctor warn-vs-error, Oban compile-out, multi-node uniqueness, Fly.io config)
    - Names Oban, --ci, unique, RELEASE_VERSION, and Parapet.Plug.Metrics (each verified surface present)
    - The --ci section states it is the STRICTER gate (catches :warn and :error), not looser
    - Fly.io section scopes to the Parapet deploy hook and links OUT for Fly-internal scrape config
    - No emojis present
  </acceptance_criteria>
  <done>troubleshooting.md answers all five seeds with live-code-accurate behavior, the correct --ci direction, and an honest Fly.io boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter to docs | Read-only Markdown; no input handling, no execution surface |
| docs to adopter's config | Docs steer config; the only residual risk is steering toward an insecure or degraded configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Information Disclosure | getting-started.md / troubleshooting.md guidance | mitigate | Docs must not steer adopters to insecure config: the /metrics exposure note ties to the router check (doctor warns on unsecured /metrics), and the legacy degraded :slos path is never shown. |
| T-18-02-02 | Tampering (drift) | doctor --ci exit-code direction | mitigate | State --ci is the STRICTER gate; a backwards claim would let :warn findings silently pass adopter CI. Verified against parse_threshold. |
| T-18-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — documentation only. |

Attack surface is essentially nil: these are Markdown files with no input parsing, auth, cryptography, or I/O. The only residual concern is config-steering (covered above).
</threat_model>

<verification>
- docs/getting-started.md and docs/troubleshooting.md exist
- Anti-drift greps pass: no "config :parapet, :slos" in getting-started; three gen.prometheus files named; --ci direction correct; five troubleshooting seeds present
- No emojis in either file
- Full mix docs --warnings-as-errors cross-link validation is deferred to plan 18-05 (after mix.exs extras registration)
</verification>

<success_criteria>
- A stranger can follow getting-started.md install to providers config to gen.prometheus to doctor and reach a generated alert, writing zero raw PromQL (ADOPT-03 / AC-01)
- troubleshooting.md answers all five seeded obstacles with behavior matching the live code (ADOPT-04)
- Neither doc shows the legacy :slos path or claims --ci is looser
</success_criteria>

<output>
Create `.planning/phases/18-adoption-authoring-docs/18-02-SUMMARY.md` when done
</output>
