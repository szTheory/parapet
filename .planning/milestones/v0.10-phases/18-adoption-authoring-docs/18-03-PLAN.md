---
phase: 18-adoption-authoring-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/slo-authoring-guide.md
autonomous: true
requirements: [SLO-03, SLO-04]

must_haves:
  truths:
    - "An adopter unsure how to slice SLOs can read a decision tree anchored to the litmus: does this failure directly prevent a user task"
    - "The low-traffic section names the actual engine guard, the min_total_rate 0.01 default, and the six multi-burn windows"
    - "The guide names synthetic probes as the real fallback and names lower-the-objective as the anti-pattern"
  artifacts:
    - path: "docs/slo-authoring-guide.md"
      provides: "Journey-slicing decision tree plus Low-Traffic and Low-Volume Services section"
      min_lines: 70
  key_links:
    - from: "docs/slo-authoring-guide.md"
      to: "Parapet.SLO.Generator guard expression"
      via: "verbatim guard shape and windows"
      pattern: "min_total_rate"
    - from: "docs/slo-authoring-guide.md"
      to: "docs/slo-reference.md"
      via: "cross-link to the slice catalog"
      pattern: "slo-reference"
---

<objective>
Author `docs/slo-authoring-guide.md` (SLO-03, SLO-04) — the conceptual guide teaching good-vs-bad journey slicing via a decision tree, plus a named "Low-Traffic and Low-Volume Services" section that describes the EXACT engine output (the rendered alert guard, the `min_total_rate: 0.01` default, the six multi-burn windows and multipliers), the synthetic-probe fallback, and the named lower-the-objective anti-pattern.

Purpose: This is the doc that turns "I installed Parapet" into "I can author the SLOs that matter for my service." The low-traffic section is the highest-drift-risk part — it must quote the engine's real rendered output, not generic SRE folklore.

Output: One Markdown file in `docs/`, anchored to the adopter-flows.md voice and the verified Generator/SliceSpec values. (mix.exs registration and the full mix docs gate happen in plan 18-05.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-adoption-authoring-docs/18-CONTEXT.md
@.planning/phases/18-adoption-authoring-docs/18-RESEARCH.md
@.planning/phases/18-adoption-authoring-docs/18-PATTERNS.md

<voice_and_facts>
Voice/analog (D-03): mirror docs/adopter-flows.md — prose-led jobs-to-be-done framing, enumerated ### sub-sections, bold "**Label:**" pattern (use **Litmus:** / **Good examples:** / **Bad examples:** / **Real anchor:** instead of Mermaid; default to a nested bullet decision tree per Claude's-discretion, no new tooling). Sentence-case headings, second-person prose, no emojis.

VERIFIED engine values to quote EXACTLY (D-12, cite by symbol, not line number):
- Default min_total_rate: 0.01 (Parapet.SLO.SliceSpec struct default; overridable per-slice)
- Multi-burn windows: ["5m", "30m", "1h", "2h", "6h", "3d"] (Parapet.SLO.Generator @windows)
- Alert multipliers: :page -> 5m window, 14.4x ; :ticket -> 30m window, 6.0x ; :warning -> 6h window, 1.0x (Parapet.SLO.Generator)
- Rendered guard shape (Generator): parapet:<slice_name>:error_ratio:<window> > <threshold> and parapet:<slice_name>:total_rate:<window> > <min_total_rate>
- Concrete example for web_saas_login_journey at :page (99.9% objective -> 0.001 * 14.4 = 0.0144 threshold, 5m window):
  parapet:web_saas_login_journey:error_ratio:5m > 0.0144 and parapet:web_saas_login_journey:total_rate:5m > 0.01
  Use a PLAIN (no language tag) fence for rendered Prometheus expressions (PATTERNS.md)
- Synthetic-probe fallback is real: Parapet.Metrics.Probe emits parapet.probe.run.total and parapet.probe.run.duration.ms (D-13)

Decision tree spine (D-14): "Does this failure directly prevent a user task? -> journey SLO." Anchor good-vs-bad examples to the three REAL WebSaaS slices: web_saas_http_availability, web_saas_login_journey, web_saas_oban_job_success (from Parapet.SLO.StarterPack.WebSaaS).

Anti-pattern (D-13): name "lower-the-objective to silence noise" explicitly as the WRONG move; the right move for low traffic is the min_total_rate denominator guard plus extended windows (the 6h/3d windows the Generator already emits) plus synthetic probes.

Cross-link (D-02 — link, do not duplicate): docs/slo-reference.md for the full provider/slice catalog (do NOT re-list the slices), docs/getting-started.md for the cold start.
</voice_and_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the journey-slicing decision tree (SLO-03)</name>
  <files>docs/slo-authoring-guide.md</files>
  <read_first>
    - docs/slo-authoring-guide.md (the file being created — confirm it does not exist)
    - docs/adopter-flows.md (analog: prose intro with no sub-heading, enumerated ### sections, bold-label rhythm)
    - lib/parapet/slo/starter_pack/web_saas.ex (the three real slices that anchor good/bad examples)
    - docs/slo-reference.md (the slice catalog to LINK to, not duplicate)
  </read_first>
  <action>
    Open with prose (no sub-heading) framing journey slicing the way adopter-flows.md frames reliability: an SLO should track a user-visible journey, not raw system health. Then a "## How to decide what to slice" section containing the decision tree as a nested bullet tree whose spine is the litmus **"Does this failure directly prevent a user task?"** -> if yes, a journey SLO; if no (infrastructure-only signal), not a journey SLO.

    Provide good-vs-bad examples using the bold-label rhythm from adopter-flows.md (**Litmus:** / **Good examples:** / **Bad examples:** / **Real anchor:**), anchored to the three real WebSaaS slices:
    - web_saas_login_journey (good journey SLO — a failed login directly blocks the user)
    - web_saas_http_availability (good — request availability is the user's baseline)
    - web_saas_oban_job_success (good — background job success when it gates a user-visible outcome)
    - Bad example: a CPU or memory gauge SLO (does not directly prevent a user task)

    Cross-link to docs/slo-reference.md for the full slice catalog rather than re-listing slices. Sentence-case headings, no emojis.
  </action>
  <verify>
    <automated>test -f docs/slo-authoring-guide.md && grep -qi "directly prevent a user task" docs/slo-authoring-guide.md && grep -q "web_saas_login_journey" docs/slo-authoring-guide.md && grep -q "web_saas_http_availability" docs/slo-authoring-guide.md && grep -q "web_saas_oban_job_success" docs/slo-authoring-guide.md && grep -q "slo-reference" docs/slo-authoring-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/slo-authoring-guide.md exists
    - Contains the litmus phrase "directly prevent a user task" (D-14 spine)
    - Names all three real WebSaaS slices as anchors
    - Cross-links to docs/slo-reference.md (does not re-list the slice catalog)
    - No emojis present
  </acceptance_criteria>
  <done>The guide opens with journey-slicing framing and presents a decision tree anchored to the real WebSaaS slices and the user-task litmus.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Low-Traffic and Low-Volume Services section (SLO-04)</name>
  <files>docs/slo-authoring-guide.md</files>
  <read_first>
    - docs/slo-authoring-guide.md (the file being extended — Task 1 created it)
    - lib/parapet/slo/slice_spec.ex (min_total_rate default 0.01, overridable per-slice)
    - lib/parapet/slo/generator.ex (@windows list, the rendered guard expression, the page/ticket/warning multipliers)
    - lib/parapet/metrics/probe.ex (the synthetic-probe metrics: parapet.probe.run.total, parapet.probe.run.duration.ms)
  </read_first>
  <action>
    Add a "## Low-traffic and low-volume services" section that describes the EXACT engine output (D-12), not generic thresholds:
    - State the denominator guard the Generator renders, quoting the shape verbatim in a PLAIN fence: parapet:<slice_name>:error_ratio:<window> > <threshold> and parapet:<slice_name>:total_rate:<window> > <min_total_rate>
    - Name the default min_total_rate: 0.01 and state it is overridable per-slice on the SliceSpec
    - List the six multi-burn windows verbatim: ["5m", "30m", "1h", "2h", "6h", "3d"] and the multipliers (14.4x at 5m for page, 6.0x at 30m for ticket, 1.0x at 6h for warning)
    - Give the concrete web_saas_login_journey :page example in a plain fence: parapet:web_saas_login_journey:error_ratio:5m > 0.0144 and parapet:web_saas_login_journey:total_rate:5m > 0.01 (99.9% objective -> 0.001 * 14.4 = 0.0144)
    - Map the "extended-window approach" to the real 6h/3d windows the Generator already emits
    - Name Parapet.Metrics.Probe as the real synthetic-probe fallback (emits parapet.probe.run.total and parapet.probe.run.duration.ms)

    Add a short "## What not to do" (or equivalent) bullet section naming "lower-the-objective to silence noise" as the explicit anti-pattern (D-13) — the wrong alternative to the min_total_rate guard plus extended windows plus synthetic probes. Use the adopter-flows.md "What Parapet Is Not" bullet style.

    Sentence-case headings, no emojis. Use backtick inline code for module/metric/config-key names; plain fence for rendered Prometheus expressions.
  </action>
  <verify>
    <automated>grep -q "min_total_rate" docs/slo-authoring-guide.md && grep -q "0.01" docs/slo-authoring-guide.md && grep -E '"5m".*"30m".*"1h".*"6h".*"3d"' docs/slo-authoring-guide.md && grep -q "0.0144" docs/slo-authoring-guide.md && grep -qi "probe" docs/slo-authoring-guide.md && grep -qi "lower" docs/slo-authoring-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - The "Low-traffic and low-volume services" section names min_total_rate with the 0.01 default (anti-drift check 4)
    - Lists the six windows verbatim so grep -E '"5m".*"30m".*"1h".*"6h".*"3d"' matches (anti-drift check 5)
    - Quotes the rendered guard shape and the concrete 0.0144 page example
    - Names Parapet.Metrics.Probe as the synthetic-probe fallback
    - Names the "lower-the-objective" anti-pattern explicitly
    - No emojis present
  </acceptance_criteria>
  <done>The low-traffic section quotes the engine's real guard, windows, multipliers, and min_total_rate default; cites synthetic probes; and names the lower-the-objective anti-pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter to docs | Read-only Markdown; no input handling, no execution surface |
| docs to adopter's SLO config | Guidance steers SLO objectives and thresholds; residual risk is steering to a misleading alerting posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Tampering (drift) | low-traffic guard description | mitigate | Quote the engine's actual rendered guard, min_total_rate 0.01, and the six windows verbatim — drift here would teach adopters a guard the engine does not emit. Verified against SliceSpec and Generator. |
| T-18-03-02 | Repudiation (silent failure) | lower-the-objective anti-pattern | mitigate | Explicitly name lowering the objective as the WRONG move so adopters do not silence real burn; steer to the denominator guard plus extended windows plus synthetic probes. |
| T-18-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — documentation only. |

Attack surface is essentially nil: a single Markdown file with no input parsing, auth, cryptography, or I/O. The only residual concern is guidance accuracy (covered above).
</threat_model>

<verification>
- docs/slo-authoring-guide.md exists with both the decision tree and the low-traffic section
- Anti-drift greps 4 and 5 pass (min_total_rate 0.01 named; six windows verbatim)
- Decision-tree litmus and the three real WebSaaS slices present; slo-reference cross-link present
- No emojis
- Full mix docs --warnings-as-errors cross-link validation is deferred to plan 18-05
</verification>

<success_criteria>
- An adopter can read the decision tree and decide whether a failure warrants a journey SLO (SLO-03)
- The low-traffic section describes the engine's real guard, windows, min_total_rate default, synthetic-probe fallback, and the lower-the-objective anti-pattern (SLO-04)
</success_criteria>

<output>
Create `.planning/phases/18-adoption-authoring-docs/18-03-SUMMARY.md` when done
</output>
