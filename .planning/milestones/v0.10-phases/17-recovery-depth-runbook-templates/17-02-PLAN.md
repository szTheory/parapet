---
phase: 17-recovery-depth-runbook-templates
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - priv/templates/parapet.gen.runbooks/dead_letter.ex.eex
  - priv/templates/parapet.gen.runbooks/callback_delay.ex.eex
  - priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex
  - priv/templates/parapet.gen.runbooks/provider_outage.ex.eex
autonomous: true
requirements: [RCV-01]
must_haves:
  truths:
    - "dead_letter has a precondition (kind: :guidance), a scoped preview (requires_preview: true), >=1 warning:, a bounded mitigation, and a distinct verification step"
    - "callback_delay (previously 1 guidance step) now has a precondition, a guidance-only mitigation, >=1 warning:, and a verification step"
    - "stalled_executor has >=1 warning: and a distinct verification step added to its existing 2 steps"
    - "provider_outage has >=1 warning: and a distinct verification step added to its existing 2 steps"
    - "every wired mitigation step references ONLY an allowlisted capability and keeps requires_preview: true (preview-first)"
  artifacts:
    - path: "priv/templates/parapet.gen.runbooks/dead_letter.ex.eex"
      provides: "deepened dead_letter template with warning + verify_recovery"
      contains: "warning:"
    - path: "priv/templates/parapet.gen.runbooks/callback_delay.ex.eex"
      provides: "deepened callback_delay with guidance-only mitigation + warning + verification"
      contains: "warning:"
    - path: "priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex"
      provides: "deepened stalled_executor with warning + verification"
      contains: "warning:"
    - path: "priv/templates/parapet.gen.runbooks/provider_outage.ex.eex"
      provides: "deepened provider_outage with warning + verification"
      contains: "warning:"
  key_links:
    - from: "deepened template warning: lines"
      to: "Parapet.Runbook.step/2 warning key (from plan 17-01)"
      via: "DSL macro consumes warning: option"
      pattern: "warning:"
    - from: "dead_letter/stalled_executor mitigation steps"
      to: "Parapet.Capabilities allowlist"
      via: "capability: :requeue_dead_letter / :retry_async_item (preview-first)"
      pattern: "capability: :(requeue_dead_letter|retry_async_item)"
---

<objective>
Deepen the four existing runbook templates (`dead_letter`, `callback_delay`, `stalled_executor`, `provider_outage`) to the RCV-01 depth checklist: a precondition (`type: :manual, kind: :guidance`), a scoped preview step (`requires_preview: true`), at least one `warning:` annotation, a bounded mitigation, and a distinct post-action verification step (`type: :manual, kind: :guidance`). None of the four currently has any `warning:` or a dedicated verification step (D-09).

Purpose: These are 1-2 step stubs today. An operator must find real, trustworthy depth — not a thin sketch. The gaps are non-uniform per D-09 (callback_delay is the thinnest: 1 step, no mitigation).

Output: Four deepened `.ex.eex` templates, each satisfying the full depth checklist, with mitigations that reference only allowlisted capabilities (or are guidance-only).

DEPENDS ON 17-01: This plan adds `warning:` lines to templates. Per the sequencing constraint, the `warning:` DSL surface from 17-01 MUST already render before these templates use it — otherwise Elixir silently swallows the keyword and the warnings never appear (D-01). 17-01's schema + projection regression tests are the contract guaranteeing the foundation is in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-recovery-depth-runbook-templates/17-RESEARCH.md
@.planning/phases/17-recovery-depth-runbook-templates/17-PATTERNS.md

<interfaces>
<!-- Allowlisted capabilities (lib/parapet/capabilities.ex:8-12) — ONLY these three are valid (D-05). -->
valid capabilities: :retry_async_item, :requeue_dead_letter, :request_manual_provider_check
<!-- An unwired capability compiles but returns {:error, :capability_unwired} at runtime. -->

<!-- Step DSL shapes (full implementation excerpts in 17-PATTERNS.md per template). -->
precondition / verification step: type: :manual, kind: :guidance, preview_only: true  (renders as guidance block, no action button; preview_only: true -> state: :guidance)
capability-backed mitigation: type: :mitigation, kind: :capability, capability: <allowlisted>, target_kind: <free-form>, requires_preview: true  (preview-first, scoped)
guidance-only mitigation: type: :mitigation, kind: :guidance, preview_only: true  (no capability, host-overridable execute_mitigation/2)
<!-- warning: <string> is OPTIONAL on any step; renders as an amber block (added in 17-01). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deepen dead_letter and stalled_executor</name>
  <files>priv/templates/parapet.gen.runbooks/dead_letter.ex.eex, priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex</files>
  <read_first>
    - priv/templates/parapet.gen.runbooks/dead_letter.ex.eex (the file being modified — full current 2-step file, lines 1-25)
    - priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex (the file being modified — full current 2-step file, lines 1-25; also the source-of-truth analog for the verification step shape)
    - .planning/phases/17-recovery-depth-runbook-templates/17-PATTERNS.md (sections "dead_letter.ex.eex — deepen existing template" and "stalled_executor.ex.eex — deepen existing template" — concrete step skeletons)
    - lib/parapet/capabilities.ex (lines 8-12 — confirm :requeue_dead_letter and :retry_async_item are allowlisted)
  </read_first>
  <action>
    dead_letter.ex.eex: keep the existing `:investigate_error` precondition (type: :manual, kind: :guidance, preview_only: true) and the existing `:requeue_item` mitigation (type: :mitigation, kind: :capability, capability: :requeue_dead_letter, target_kind: :async_item, requires_preview: true — already preview-first/scoped). Add a `warning:` to `:investigate_error` (e.g. abort condition before requeue) and a `warning:` to `:requeue_item` (re-processing already-failed items). Add a NEW distinct `:verify_recovery` step AFTER the mitigation: type: :manual, kind: :guidance, preview_only: true, with guidance confirming the item transitions out of the dead letter queue. Keep `capability: :requeue_dead_letter` unchanged (do NOT introduce a new capability).

    stalled_executor.ex.eex: keep the existing `:investigate_logs` precondition and the existing `:retry_item` mitigation (capability: :retry_async_item, target_kind: :async_item, requires_preview: true). Add a `warning:` to `:investigate_logs` (warn if logs show the item still actively executing) and a `warning:` to `:retry_item` (retrying a deadlocked item without root-cause may reproduce the deadlock). Add a NEW distinct `:verify_recovery` step AFTER `:retry_item`: type: :manual, kind: :guidance, preview_only: true. Keep `capability: :retry_async_item` unchanged.

    Express precondition and verification as DISTINCT type: :manual, kind: :guidance steps — do not fold verification into the mitigation description (D-10). Preserve the EEx header `defmodule <%= inspect(@module_prefix) %>.<Name> do` and `use Parapet.Runbook` unchanged. Author label/description/guidance/warning text in the existing templates' voice (Claude's discretion, anchored to PATTERNS.md skeletons).
  </action>
  <verify>
    <automated>grep -c 'warning:' priv/templates/parapet.gen.runbooks/dead_letter.ex.eex priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'warning:' priv/templates/parapet.gen.runbooks/dead_letter.ex.eex` >= 1
    - `grep -c 'warning:' priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex` >= 1
    - each file contains a step with id :verify_recovery and `type: :manual` and `kind: :guidance`
    - dead_letter still contains `capability: :requeue_dead_letter` and `requires_preview: true`
    - stalled_executor still contains `capability: :retry_async_item` and `requires_preview: true`
    - neither file references any capability outside the allowlist: `grep -E 'capability: :' priv/templates/parapet.gen.runbooks/dead_letter.ex.eex priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex` shows only :requeue_dead_letter / :retry_async_item
  </acceptance_criteria>
  <done>dead_letter and stalled_executor each have a precondition, scoped preview-first mitigation, >=1 warning:, and a distinct verification step; both reuse only their existing allowlisted capability.</done>
</task>

<task type="auto">
  <name>Task 2: Deepen provider_outage and callback_delay</name>
  <files>priv/templates/parapet.gen.runbooks/provider_outage.ex.eex, priv/templates/parapet.gen.runbooks/callback_delay.ex.eex</files>
  <read_first>
    - priv/templates/parapet.gen.runbooks/provider_outage.ex.eex (the file being modified — full current 2-step file, lines 1-25)
    - priv/templates/parapet.gen.runbooks/callback_delay.ex.eex (the file being modified — full current 1-step file, lines 1-15; the thinnest template, needs the most work)
    - .planning/phases/17-recovery-depth-runbook-templates/17-PATTERNS.md (sections "provider_outage.ex.eex — deepen existing template" and "callback_delay.ex.eex — deepen existing template (most work)" — concrete step skeletons including the guidance-only mitigation pattern)
    - lib/parapet/capabilities.ex (lines 8-12 — confirm :request_manual_provider_check is allowlisted; confirm NO capability fits callback delay)
  </read_first>
  <action>
    provider_outage.ex.eex: keep the existing `:check_status` precondition and the existing `:request_manual_check` mitigation (capability: :request_manual_provider_check, target_kind: :provider, requires_preview: true). Add a `warning:` to `:check_status` (warn the provider status page itself may be unreachable during a full outage) and a `warning:` to `:request_manual_check` (creates a team task; avoid duplicate flags). Add a NEW distinct `:verify_recovery` step AFTER the mitigation: type: :manual, kind: :guidance, preview_only: true. Keep `capability: :request_manual_provider_check` unchanged.

    callback_delay.ex.eex (currently 1 guidance step only — no mitigation): keep the existing `:verify_receipt` precondition and add a `warning:` to it. Add a NEW guidance-only mitigation step `:mitigate_delay` (type: :mitigation, kind: :guidance, preview_only: true) — NO capability, because none of the three allowlisted capabilities fits callback-delay remediation (D-06); include a `warning:` about duplicate delivery / double-processing. Add a NEW distinct `:verify_recovery` step AFTER the mitigation: type: :manual, kind: :guidance, preview_only: true.

    Express precondition and verification as DISTINCT type: :manual, kind: :guidance steps (D-10). Do NOT introduce any new capability: id. Preserve the EEx header and `use Parapet.Runbook` unchanged. Author text in the existing templates' voice (Claude's discretion, anchored to PATTERNS.md skeletons).
  </action>
  <verify>
    <automated>grep -c 'warning:' priv/templates/parapet.gen.runbooks/provider_outage.ex.eex priv/templates/parapet.gen.runbooks/callback_delay.ex.eex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'warning:' priv/templates/parapet.gen.runbooks/provider_outage.ex.eex` >= 1
    - `grep -c 'warning:' priv/templates/parapet.gen.runbooks/callback_delay.ex.eex` >= 1
    - each file contains a step with id :verify_recovery and `type: :manual` and `kind: :guidance`
    - callback_delay contains a `type: :mitigation` step with `kind: :guidance` and NO `capability:` key on that step
    - provider_outage still contains `capability: :request_manual_provider_check` and `requires_preview: true`
    - callback_delay contains NO `capability:` reference at all: `grep -c 'capability:' priv/templates/parapet.gen.runbooks/callback_delay.ex.eex` == 0
  </acceptance_criteria>
  <done>provider_outage has a warning + distinct verification step on its existing capability flow; callback_delay is brought to full depth with a guidance-only mitigation, warning, and verification — no new capability introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template `capability:` -> Parapet.Capabilities allowlist | a wired mitigation must name one of three real capabilities, else broken Preview/Execute |
| generated runbook file -> adopter host app | templates copied into the host's lib/; host-owned and editable after generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Elevation/Correctness | a deepened template naming an unwired capability | mitigate | All four templates reuse only their existing allowlisted capability (:requeue_dead_letter / :retry_async_item / :request_manual_provider_check); callback_delay's new mitigation is guidance-only (no capability) — acceptance criteria assert the allowlist conformance per file. Generator-content test in 17-03 cross-checks at the suite level |
| T-17-06 | Tampering/Safety | a destructive mitigation auto-executing without a scoped preview | mitigate | Every wired mitigation keeps `requires_preview: true` (preview-first, bounded); guidance-only mitigations dispatch no capability and are inherently safe (host-overridable execute_mitigation/2) |
| T-17-07 | Information Disclosure | warning text rendered in operator UI | accept | Static compile-time literals authored by the library; HEEx auto-escapes; no user input |
</threat_model>

<verification>
- Per-task `grep` gates confirm warning: presence, verify_recovery presence, and allowlist conformance.
- Suite-level proof of generated content (each deepened file contains `warning:`) is asserted by the generator-content test extended in plan 17-03 — run `mix test test/mix/tasks/parapet.gen.runbooks_test.exs` after both Wave 2 plans land.
- Manual smoke (AC-03, phase gate): open the operator UI against a deepened template; confirm precondition + scoped preview + bounded mitigation + amber warning + verification render.
</verification>

<success_criteria>
- All four existing templates satisfy the RCV-01 depth checklist: precondition, scoped preview (requires_preview: true on a wired mitigation, or the precondition/guidance flow for guidance-only), >=1 warning:, a bounded mitigation, and a distinct verification step.
- No new capability: id introduced; wired mitigations reuse only allowlisted capabilities; callback_delay's mitigation is guidance-only.
- Every wired mitigation remains preview-first (requires_preview: true).
</success_criteria>

<output>
Create `.planning/phases/17-recovery-depth-runbook-templates/17-02-SUMMARY.md` when done
</output>
