---
phase: 15-packaging-credibility-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - docs/HISTORY.md
  - release-please-config.json
  - .release-please-manifest.json
autonomous: true
requirements: [ADOPT-02]
must_haves:
  truths:
    - "A root CHANGELOG.md exists as a header-only stub with no hand-written ## version sections"
    - "The CHANGELOG.md stub links to docs/HISTORY.md so it covers v0.1-v0.9 without conflicting with Release Please generation"
    - "docs/HISTORY.md records all nine milestones v0.1 through v0.9 sourced from .planning/MILESTONES.md"
    - "release-please-config.json pins the first computed release to 0.10.0 via release-as with pre-1.0 bump flags"
    - ".release-please-manifest.json seeds the last-released version at 0.9.0 (non-zero, avoiding the 0.0.0 boundary bug)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Release-Please-owned changelog (header-only stub)"
      contains: "# Changelog"
    - path: "docs/HISTORY.md"
      provides: "Retroactive v0.1-v0.9 milestone history"
      contains: "# Parapet Milestone History"
    - path: "release-please-config.json"
      provides: "Release Please version strategy (bump flags + release-as pin)"
      contains: "release-as"
    - path: ".release-please-manifest.json"
      provides: "Release Please version seed"
      contains: "0.9.0"
  key_links:
    - from: "CHANGELOG.md"
      to: "docs/HISTORY.md"
      via: "markdown link in stub prose"
      pattern: "docs/HISTORY.md"
    - from: "release-please-config.json"
      to: ".release-please-manifest.json"
      via: "RP manifest-mode pairing (seed 0.9.0 + release-as 0.10.0 = first release 0.10.0)"
      pattern: "\"release-as\": \"0.10.0\""
---

<objective>
Create the four new static files that the wiring plan (15-02) depends on:
the Release-Please-owned `CHANGELOG.md` stub, the retroactive `docs/HISTORY.md`
milestone history, and the Release Please `release-please-config.json` +
`.release-please-manifest.json` version-strategy pair.

Purpose: These files MUST exist on disk before `mix.exs` adds them to ex_doc
`extras:` and before the workflow references the config/manifest — otherwise
`mix verify.public_api` (= `mix docs --warnings-as-errors`) errors on missing
extras, and the workflow runs in action-input mode ignoring the config. This is
Wave 0/1 of the hard ordering constraint. Satisfies ADOPT-02 (changelog +
retroactive history) and lays the version-strategy groundwork that lands the
first release on a deliberate `0.10.0` rather than an accidental `1.0.0`.

Output: `CHANGELOG.md`, `docs/HISTORY.md`, `release-please-config.json`,
`.release-please-manifest.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-packaging-credibility-gate/15-CONTEXT.md
@.planning/phases/15-packaging-credibility-gate/15-RESEARCH.md
@.planning/phases/15-packaging-credibility-gate/15-PATTERNS.md
@.planning/MILESTONES.md

<critical_reconciliation>
RESEARCH.md supersedes CONTEXT.md D-09's stated *rationale*. There are ZERO
`BREAKING CHANGE` footers in git history (verified `git log --all --format="%B"`).
The accidental `1.0.0` came from release-please's first-release heuristic (no
manifest + no config + accumulated `feat:` commits), NOT from commit `8268889`.
The correct fix is the explicit `release-as: "0.10.0"` pin plus seeding the
manifest at `"0.9.0"`. With `bump-patch-for-minor-pre-major: true`, accumulated
feats alone would only bump 0.9.0 -> 0.9.1; the `release-as` pin is what lands
exactly on 0.10.0. Seed at `"0.9.0"` (NOT `"0.0.0"`) per release-please issue
#2087 — a `0.0.0` seed makes the pre-major bump options ignored. The `release-as`
pin is ONE-TIME — record a TODO to remove it after the 0.10.0 release PR merges
(post-Phase-18, D-10). The skill's gotcha #4 ("baseline at 0.0.0") is overridden
by this RESEARCH finding; gotcha #5 (one-time pin, remove after publish) holds.
</critical_reconciliation>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Release Please config + manifest pair and the CHANGELOG.md stub</name>
  <files>release-please-config.json, .release-please-manifest.json, CHANGELOG.md</files>
  <read_first>
    - /Users/jon/projects/oarlock/release-please-config.json (canonical szTheory RP config; copy verbatim then apply the 3 Parapet adaptations)
    - /Users/jon/projects/oarlock/.release-please-manifest.json (canonical manifest format — single `"."` key)
    - /Users/jon/projects/oarlock/CHANGELOG.md (real post-RP file: shows the human stub prose preserved above the RP-inserted `## version` sections — read lines 1-15)
    - .planning/phases/15-packaging-credibility-gate/15-PATTERNS.md (Pattern 2, Pattern 3, Pattern 5 sections — exact target file contents)
    - .planning/phases/15-packaging-credibility-gate/15-RESEARCH.md ("Common Pitfalls" #1, #3 — version strategy rationale; issue #2087 boundary)
  </read_first>
  <action>
    Create `release-please-config.json` mirroring oarlock's file with THREE adaptations
    (per D-09 / RESEARCH Pattern 2): keep `"$schema"`, `"release-type": "elixir"`,
    `"bump-patch-for-minor-pre-major": true`, and the `packages["."]` keys
    `"changelog-path": "CHANGELOG.md"` and `"include-v-in-tag": true` verbatim; FLIP
    `"bump-minor-pre-major"` from `false` to `true`; ADD `"release-as": "0.10.0"` inside
    `packages["."]`.

    Create `.release-please-manifest.json` with a single key: `{ ".": "0.9.0" }`. Seed at
    `"0.9.0"` (NOT `"0.0.0"` — issue #2087: a 0.0.0 seed makes pre-major bump options
    ignored and may still propose 1.0.0). The `release-as: "0.10.0"` pin overrides
    commit-based math so the first computed release is exactly `0.10.0`.

    Create `CHANGELOG.md` as a HEADER-ONLY stub using Pattern 5 / PATTERNS.md exact text:
    `# Changelog` title, the standard Keep-a-Changelog + SemVer preamble prose, and a
    `## Planning milestones vs Hex releases` prose section whose body links to
    `docs/HISTORY.md` (relative link `[docs/HISTORY.md](docs/HISTORY.md)` for the v0.1-v0.9
    history) and to `.planning/MILESTONES.md`. Do NOT write ANY `## <version>` section
    (e.g. `## 0.10.0`) — Release Please owns those; a hand-written version heading creates
    a duplicate on the next RP run (D-06, RESEARCH pitfall #3).

    Record a TODO (in the SUMMARY and as a code comment is NOT possible in JSON, so note
    it in the SUMMARY only): "Remove `release-as: 0.10.0` from release-please-config.json
    after the chore(main): release 0.10.0 PR merges (post-Phase-18, D-10) — leaving it in
    pins all future releases to 0.10.0 and breaks re-publish."
  </action>
  <verify>
    <automated>test -f release-please-config.json && test -f .release-please-manifest.json && test -f CHANGELOG.md && python3 -c "import json; c=json.load(open('release-please-config.json')); m=json.load(open('.release-please-manifest.json')); assert c['bump-minor-pre-major'] is True; assert c['bump-patch-for-minor-pre-major'] is True; assert c['release-type']=='elixir'; assert c['packages']['.']['release-as']=='0.10.0'; assert c['packages']['.']['changelog-path']=='CHANGELOG.md'; assert m['.']=='0.9.0'; print('config+manifest OK')"</automated>
    <automated>grep -q '^# Changelog' CHANGELOG.md && grep -q 'docs/HISTORY.md' CHANGELOG.md && ! grep -Eq '^## [0-9]+\.[0-9]+\.[0-9]+' CHANGELOG.md && echo "changelog stub OK"</automated>
  </verify>
  <acceptance_criteria>
    - `release-please-config.json` parses as JSON and has `release-type: "elixir"`, `bump-minor-pre-major: true`, `bump-patch-for-minor-pre-major: true`, and `packages["."].release-as == "0.10.0"`, `packages["."].changelog-path == "CHANGELOG.md"`, `packages["."].include-v-in-tag == true`.
    - `.release-please-manifest.json` parses as JSON and equals `{ ".": "0.9.0" }` (NOT `"0.0.0"`).
    - `CHANGELOG.md` contains the `# Changelog` H1 and a relative link to `docs/HISTORY.md`.
    - `CHANGELOG.md` contains ZERO `## X.Y.Z` semver version headings (the `grep -Eq '^## [0-9]'` check returns no match).
    - SUMMARY records the one-time `release-as` removal TODO.
  </acceptance_criteria>
  <done>All three files exist; JSON files validate against the asserted keys/values; CHANGELOG.md is a header-only stub with no version sections and links to docs/HISTORY.md; the release-as removal TODO is captured in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/HISTORY.md from the v0.1-v0.9 milestone source</name>
  <files>docs/HISTORY.md</files>
  <read_first>
    - .planning/MILESTONES.md (FULL file — the source of truth; nine milestone blocks v0.1 at top through v0.9, each with Date, Stats, Accomplishments, Known Gaps)
    - .planning/phases/15-packaging-credibility-gate/15-PATTERNS.md (Pattern 6 / `docs/HISTORY.md` section — header pattern + per-milestone section pattern + reverse-chronological ordering)
    - CHANGELOG.md (just created in Task 1 — confirm the relative link target `docs/HISTORY.md` matches this file's location)
  </read_first>
  <action>
    Create `docs/HISTORY.md` transforming `.planning/MILESTONES.md` into shipped adopter
    documentation (D-07). Lead with the Pattern 6 header: `# Parapet Milestone History`,
    a short framing paragraph stating these are planning tranches NOT Hex release versions
    (the package was not published to hex.pm during v0.1-v0.9), and a relative link to the
    changelog `[CHANGELOG.md](../CHANGELOG.md)` for published Hex releases (v0.10+). Then a
    `---` rule.

    For each milestone, emit a `## vX.Y — <Title> (<Date>)` H2 section in REVERSE
    chronological order (v0.9 first, v0.1 last) so adopters see the most recent work first.
    Each section's body is a prose/bullet transform of that milestone's `### Accomplishments`
    list from MILESTONES.md, followed by a `**Stats:**` line drawn from the milestone's
    `**Stats:**` block (Phases/Plans/LOC/dates). Cover ALL nine milestones: v0.9
    Performance, Scale & DX; v0.8 Deterministic Escalation & Bounded Mitigation; v0.7 Async
    & Delivery Reliability; v0.6 Change Correlation & Audit Trailing; v0.5 Proactive
    Resilience & Copilot Triage; v0.4 Scoria AI Integration; v0.3 Runbooks & Alert Routing;
    v0.2 Durable Spine and Operator UI; v0.1 Trustworthy Spine.

    Frame as MILESTONE history, not hex-version history (milestone names != hex versions —
    package sat at 0.1.0). Do NOT invent content — every accomplishment and stat comes from
    MILESTONES.md. Discretion is allowed only on narrative tone/wording, not on which
    milestones or facts appear. Keep it plain markdown (no `Module.fun/arity` literal
    patterns that ex_doc would try to auto-link — this file is in the docs/ extras set
    consumed by `mix verify.public_api` in plan 15-02).
  </action>
  <verify>
    <automated>test -f docs/HISTORY.md && grep -q '^# Parapet Milestone History' docs/HISTORY.md && grep -q 'CHANGELOG.md' docs/HISTORY.md && [ "$(grep -cE '^## v0\.[1-9]' docs/HISTORY.md)" -eq 9 ] && echo "HISTORY 9 milestones OK"</automated>
    <automated>grep -q '^## v0\.9' docs/HISTORY.md && grep -q '^## v0\.1' docs/HISTORY.md && echo "v0.9 and v0.1 present"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/HISTORY.md` exists and opens with the `# Parapet Milestone History` H1 and the planning-tranches-not-hex-versions framing.
    - It contains exactly nine `## v0.X — Title (Date)` H2 sections — one per milestone v0.1 through v0.9 — verified by `grep -cE '^## v0\.[1-9]'` returning 9.
    - Sections are in reverse-chronological order (the first `## v0.X` match is `## v0.9`, the last is `## v0.1`).
    - Each milestone section's accomplishment content and `**Stats:**` line trace to that milestone's block in `.planning/MILESTONES.md` (no invented features).
    - The file contains a relative link to `../CHANGELOG.md`.
  </acceptance_criteria>
  <done>docs/HISTORY.md exists with all nine milestone sections in reverse-chronological order, sourced faithfully from MILESTONES.md, with the framing header and cross-link to CHANGELOG.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan creates only static repository files (`.json`, `.md`). No untrusted input is parsed at runtime, no network/auth/session boundary is introduced or crossed. |

## STRIDE Threat Register

Per RESEARCH "Security Domain": this phase makes no authentication, session,
input-validation, cryptographic, or access-control changes. ASVS V2-V6 do not
apply. Threat surface is LOW and limited to release-tooling configuration.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `release-please-config.json` version strategy | mitigate | Pin first release explicitly via `release-as: "0.10.0"` + non-zero manifest seed `"0.9.0"`; a wrong/accidental `1.0.0` is the realistic failure mode and is neutralized by the pin (verified vs release-please issue #2087). |
| T-15-02 | Information Disclosure | `docs/HISTORY.md` shipped in package tarball | accept | Content is sourced from already-public planning milestones; no secrets, credentials, or PII. `docs/` already ships via the Hex `files:` whitelist. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this phase (RESEARCH "Package Legitimacy Audit": zero external packages). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages. |
</threat_model>

<verification>
After both tasks:
- `python3 -c "import json; json.load(open('release-please-config.json')); json.load(open('.release-please-manifest.json'))"` — both JSON files valid.
- `test -f CHANGELOG.md && test -f docs/HISTORY.md && test -f release-please-config.json && test -f .release-please-manifest.json` — all four artifacts exist (Wave 0 dependency for plan 15-02 satisfied).
- `! grep -Eq '^## [0-9]+\.[0-9]+\.[0-9]+' CHANGELOG.md` — no hand-written version sections in the changelog stub.
- `grep -cE '^## v0\.[1-9]' docs/HISTORY.md` returns 9 — all milestones covered.
- Do NOT run `mix verify.public_api` in this plan — `mix.exs` does not yet reference these extras; that gate runs in plan 15-02 after the wiring.
</verification>

<success_criteria>
- All four new files exist on disk (the Wave 0/1 ordering prerequisite for 15-02).
- `release-please-config.json` + `.release-please-manifest.json` together pin the first release to `0.10.0` (seed `0.9.0` + `release-as: 0.10.0`, pre-1.0 bump flags set).
- `CHANGELOG.md` is a header-only stub linking to `docs/HISTORY.md`, with zero version sections (Release Please owns the body — D-06).
- `docs/HISTORY.md` retroactively covers all nine milestones v0.1-v0.9 from MILESTONES.md (ADOPT-02).
- The one-time `release-as` removal TODO is recorded in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/15-packaging-credibility-gate/15-01-SUMMARY.md` when done.
</output>
