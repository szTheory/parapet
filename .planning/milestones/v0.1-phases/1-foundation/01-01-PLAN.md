---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["mix.exs", ".github/workflows/ci.yml", ".github/workflows/release-please.yml", "docs/telemetry.md"]
autonomous: true
requirements: ["PKG-01", "PKG-02", "OSS-01", "OSS-03", "OSS-04", "DOCS-02", "DOCS-04", "TELE-01", "TELE-02"]
must_haves:
  truths:
    - "Project is initialized as an Elixir supervised application"
    - "CI pipeline runs all formatting, linting, and typing checks"
    - "Telemetry schema and semver contract are explicitly documented"
  artifacts:
    - path: "mix.exs"
      provides: "Package definition and dependencies whitelist"
    - path: ".github/workflows/ci.yml"
      provides: "CI enforcement"
    - path: "docs/telemetry.md"
      provides: "Telemetry schema version contract"
  key_links: []
---

<objective>
Initialize the Parapet project, configure Hex package rules, and establish strict CI/CD pipelines.

Purpose: Lay the foundation for an OSS library with zero tolerance for undocumented code, bad formatting, or bloated hex package artifacts.
Output: Supervised library skeleton, CI workflows, and initial telemetry documentation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1-foundation/1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Skeleton & Hex Config</name>
  <files>mix.exs</files>
  <read_first>mix.exs</read_first>
  <action>
If `mix.exs` does not exist, generate the application skeleton via `mix new . --app parapet --sup`.
Then modify `mix.exs` to include:
1. `version: "0.1.0"` in the `def project` block.
2. `package: [files: ~w(lib .formatter.exs mix.exs README* docs), licenses: ["MIT"], links: %{}]` in the `def project` block.
3. Add `{:igniter, "~> 0.7.9"}` and `{:telemetry, "~> 1.2"}` to `defp deps`.
4. Add `{:oban, ">= 0.0.0", optional: true}` and `{:sigra, ">= 0.0.0", optional: true}` to `defp deps` to satisfy PKG-02.
  </action>
  <acceptance_criteria>
    - `mix.exs` contains exact string `files: ~w(lib .formatter.exs mix.exs README* docs)`
    - `mix.exs` contains exact string `{:igniter, "~> 0.7.9"}`
    - `mix.exs` contains exact string `{:telemetry, "~> 1.2"}`
    - `mix.exs` contains exact string `{:oban, ">= 0.0.0", optional: true}`
    - `mix.exs` contains exact string `{:sigra, ">= 0.0.0", optional: true}`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "files: \~w" mix.exs && grep -q "optional: true" mix.exs</automated>
  </verify>
  <done>mix.exs is securely whitelisted and has core and optional dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: CI & Release Engineering</name>
  <files>.github/workflows/ci.yml, .github/workflows/release-please.yml</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>
Create `.github/workflows/ci.yml` that runs on `push` to `main` and `pull_request`. It must execute:
- `mix format --check-formatted`
- `mix credo --strict`
- `mix dialyzer`
- `mix test`
- `mix verify.public_api`

Create `.github/workflows/release-please.yml` using `google-github-actions/release-please-action@v4` pointing to the `main` branch to auto-generate `CHANGELOG.md` via Conventional Commits.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains exact string `mix format --check-formatted`
    - `.github/workflows/ci.yml` contains exact string `mix credo --strict`
    - `.github/workflows/ci.yml` contains exact string `mix verify.public_api`
    - `.github/workflows/release-please.yml` contains exact string `release-please-action`
  </acceptance_criteria>
  <verify>
    <automated>grep "credo --strict" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI strictly enforces linting and OSS release workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Telemetry Documentation Stubs</name>
  <files>docs/telemetry.md</files>
  <read_first>docs/telemetry.md</read_first>
  <action>
Create `docs/telemetry.md` defining the library telemetry schema. It must include the following literal section:
"The telemetry event schema version is tied to the package version. Renaming or removing measurement or metadata fields constitutes a semver-major breaking change."
  </action>
  <acceptance_criteria>
    - `docs/telemetry.md` contains exact string `semver-major breaking change`
    - `docs/telemetry.md` contains exact string `telemetry event schema version`
  </acceptance_criteria>
  <verify>
    <automated>grep "semver-major" docs/telemetry.md</automated>
  </verify>
  <done>Telemetry version contract explicitly documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tooling | Hex publish boundary prevents prompt and planning leakages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Package Builder | mitigate | Explicit `files: ~w(lib ...)` whitelist in mix.exs to exclude `.planning/` |
</threat_model>

<verification>
mix deps.get
grep "files:" mix.exs
grep "optional: true" mix.exs
</verification>

<success_criteria>
Project initialized, packages defined, and CI workflows exist.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>