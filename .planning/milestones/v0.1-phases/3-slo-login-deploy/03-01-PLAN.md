---
phase: "03-slo-login-deploy"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/parapet/slo.ex"
  - "lib/parapet/slo/generator.ex"
  - "test/parapet/slo_test.exs"
autonomous: true
requirements:
  - SLO-01
  - SLO-02
  - SLO-03
  - SLO-04
  - SLO-05
must_haves:
  truths:
    - "An SLO defined via Parapet.SLO.define produces multi-window burn-rate recording and alerting rules"
    - "Generated PromQL uses sum(rate(good[w])) / sum(rate(total[w])) and never rate(sum(...))"
  artifacts:
    - path: "lib/parapet/slo.ex"
      provides: "SLO definition struct and DSL API"
    - path: "lib/parapet/slo/generator.ex"
      provides: "Prometheus YAML template generation"
    - path: "test/parapet/slo_test.exs"
      provides: "Snapshot tests for PromQL generation"
  key_links:
    - from: "lib/parapet/slo.ex"
      to: "lib/parapet/slo/generator.ex"
      via: "SLO definition structs passed for generation"
---

<objective>
Implement the core Service-Level Objective (SLO) definition module and the generator that converts these definitions into correct Prometheus multi-window burn-rate YAML rules.

Purpose: Allow adopters to express SLOs in Elixir code and reliably generate robust PromQL that correctly handles counter resets without manual string manipulation.
Output: The `Parapet.SLO` struct, `Parapet.SLO.Generator` module, and a comprehensive test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/3-slo-login-deploy/RESEARCH.md
@.planning/phases/3-slo-login-deploy/PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement SLO DSL and Definition</name>
  <files>lib/parapet/slo.ex, test/parapet/slo_test.exs</files>
  <behavior>
    - Defines a `%Parapet.SLO{}` struct containing fields for `name`, `objective` (e.g. 99.9), `good_events`, `total_events`, and `runbook` URL.
    - Provides `Parapet.SLO.define/2` to easily initialize and validate this struct, and stores the defined SLO in application config (e.g. `Application.put_env(:parapet, :slos, ...)`) or via a similar registry mechanism.
    - Tests verify the struct is created correctly, stored for discovery, and missing fields raise an ArgumentError.
  </behavior>
  <action>
    Create the `%Parapet.SLO{}` struct and `define/2` function. Enforce that all required properties (especially `runbook`) are provided. Implement an application configuration or registry push in `define/2` (and a `Parapet.SLO.all/0` reader) so that later tools like `parapet.doctor` can discover all defined SLOs. This is the contract that consumers will use to define their SLOs.
  </action>
  <verify>
    <automated>mix test test/parapet/slo_test.exs</automated>
  </verify>
  <done>Parapet.SLO module and tests are implemented and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PromQL YAML Generator</name>
  <files>lib/parapet/slo/generator.ex, test/parapet/slo_test.exs</files>
  <behavior>
    - `Parapet.SLO.Generator.generate_yaml(slo)` takes a `%Parapet.SLO{}` and returns a formatted YAML string.
    - Uses multi-window burn-rate alerting rules (fast burn and slow burn).
    - Crucially, it must generate PromQL like `sum(rate(good[w])) / sum(rate(total[w]))` and avoid `rate(sum(...))`.
    - Tests assert the generated string exactly matches expected multi-window YAML via simple string/snapshot assertions.
    - Tests optionally verify the output via a system call to `promtool check rules`.
  </behavior>
  <action>
    Create `Parapet.SLO.Generator`. Use EEx embedded templates to generate the Prometheus YAML correctly instead of string concatenation. This prevents YAML indentation errors. Write thorough tests that ensure the PromQL ratio formula is strictly followed. In the test, add a verification step to run `promtool check rules` on the generated YAML output (e.g., via `System.cmd`) if `promtool` is available, to validate the generated rules and satisfy Success Criteria 1.
  </action>
  <verify>
    <automated>mix test test/parapet/slo_test.exs</automated>
  </verify>
  <done>Generator evaluates EEx template to valid Prometheus YAML syntax using the correct PromQL logic.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Config → SLO DSL | User provides input for SLO definitions which compiles into PromQL YAML |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Injection | `lib/parapet/slo/generator.ex` | mitigate | Ensure metrics names and labels are strictly sanitized/validated before interpolating them into PromQL strings via EEx. |
</threat_model>

<verification>
1. `mix test test/parapet/slo_test.exs` passes without errors.
2. The generated YAML must contain valid `sum(rate(good[w])) / sum(rate(total[w]))` structure.
3. `promtool check rules` verifies the correctness of the generated Prometheus YAML.
</verification>

<success_criteria>
- `Parapet.SLO` provides a clean DSL for declaring objectives.
- `Parapet.SLO.Generator` produces Prometheus-compatible YAML using EEx without syntax errors.
- Test coverage ensures no regression on PromQL rate ordering.
</success_criteria>

<output>
After completion, create `.planning/phases/03-slo-login-deploy/03-01-SUMMARY.md`
</output>
