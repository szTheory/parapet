---
phase: "03-slo-login-deploy"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/parapet/integrations/sigra.ex"
  - "test/parapet/integrations/sigra_test.exs"
  - "lib/parapet/slo/login_journey.ex"
  - "lib/mix/tasks/parapet.install.ex"
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "parapet.journey.login telemetry events are emitted for every auth attempt with :outcome"
    - "The application compiles and starts safely when Sigra is absent"
  artifacts:
    - path: "lib/parapet/integrations/sigra.ex"
      provides: "Sigra integration event handler"
    - path: "test/parapet/integrations/sigra_test.exs"
      provides: "Unit tests mocking Sigra telemetry events"
  key_links:
    - from: "lib/parapet/integrations/sigra.ex"
      to: "[:parapet, :journey, :login]"
      via: ":telemetry.execute"
---

<objective>
Implement the optional Sigra integration to translate native Sigra authentication events into standard Parapet login journey metrics, and wire up the SLO and install scripts.

Purpose: Provide out-of-the-box observability for user logins (a critical journey) if the adopter uses the Sigra authentication library, without introducing a hard dependency.
Output: A safely compiled telemetry event handler module `Parapet.Integrations.Sigra`, a LoginJourney SLO, and an updated install task.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/3-slo-login-deploy/RESEARCH.md
@.planning/phases/3-slo-login-deploy/PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create Optional Sigra Integration Handler</name>
  <files>lib/parapet/integrations/sigra.ex, test/parapet/integrations/sigra_test.exs</files>
  <behavior>
    - Conditionally compiled: Wraps module in `if Code.ensure_loaded?(Sigra) do ... end`.
    - `setup/0` attaches to `[:sigra, :auth, :login, :stop]` and `[:sigra, :auth, :login, :exception]`.
    - `handle_event/4` maps `state == :stop` to `outcome: :success` and exceptions to `outcome: :failure`.
    - Emits standard `[:parapet, :journey, :login]` telemetry event with the duration and outcome.
    - Includes `try/rescue` block from `SafeHandler` pattern for robustness.
  </behavior>
  <action>
    Implement `Parapet.Integrations.Sigra` following the `Oban` integration pattern from PATTERNS.md. Listen for Sigra events and execute the new Parapet standard telemetry events. Ensure `Code.ensure_loaded?(Sigra)` wraps the implementation so users without Sigra don't crash.
  </action>
  <verify>
    <automated>mix test test/parapet/integrations/sigra_test.exs</automated>
  </verify>
  <done>Sigra events correctly trigger Parapet telemetry emissions with the :outcome metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Create LoginJourney SLO</name>
  <files>lib/parapet/slo/login_journey.ex</files>
  <action>
    Implement `Parapet.SLO.LoginJourney` as specified in AUTH-02 to provide an out-of-the-box SLO definition for the standard `[:parapet, :journey, :login]` telemetry event. Provide sensible defaults for objectives and total/good events.
  </action>
  <verify>
    <automated>test -f lib/parapet/slo/login_journey.ex || mix compile</automated>
  </verify>
  <done>LoginJourney SLO module exists and is pre-configured.</done>
</task>

<task type="auto">
  <name>Task 3: Update Install Generator for Sigra Config</name>
  <files>lib/mix/tasks/parapet.install.ex</files>
  <action>
    Modify the install generator (`lib/mix/tasks/parapet.install.ex`) to support the `--with-sigra` flag (AUTH-03). When provided, generate the necessary configuration block to attach `Parapet.Integrations.Sigra` on application start.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Install generator parses --with-sigra and outputs the config block.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internal Event Bus | Application telemetry events emitted by Sigra |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Info Disclosure | `lib/parapet/integrations/sigra.ex` | mitigate | Ensure no PII (e.g. user IDs, emails, IP addresses) from the Sigra event metadata is leaked into the Parapet metric label dimensions. |
</threat_model>

<verification>
1. `mix test test/parapet/integrations/sigra_test.exs` passes.
2. The module handles `[:sigra, :auth, :login, ...]` events securely without logging sensitive metadata.
</verification>

<success_criteria>
- Telemetry events `[:parapet, :journey, :login]` are reliably generated.
- The package does not fail to compile or start if `sigra` is not in the host's `deps`.
- `Parapet.SLO.LoginJourney` exists and is documented.
- Install task supports `--with-sigra`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-slo-login-deploy/03-02-SUMMARY.md`
</output>
