---
phase: "03-slo-login-deploy"
plan: "03"
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - "lib/parapet/deploy.ex"
  - "test/parapet/deploy_test.exs"
  - "lib/mix/tasks/parapet.install.ex"
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-03
  - DEPL-04
must_haves:
  truths:
    - "Deploy events can be manually marked via Parapet.Deploy.mark/1"
    - "A monotonic sequence is attached to deploy markers for Grafana annotation ingestion"
  artifacts:
    - path: "lib/parapet/deploy.ex"
      provides: "API to trigger deploy metric markers"
    - path: "test/parapet/deploy_test.exs"
      provides: "Tests verifying marker emission logic"
  key_links:
    - from: "lib/parapet/deploy.ex"
      to: "[:parapet, :deploy, :mark]"
      via: ":telemetry.execute"
---

<objective>
Implement the Deploy Markers API to allow adopters to signal when a deployment occurs, which enables Grafana vertical annotations across SLO charts, and generate deploy hooks.

Purpose: Facilitate root cause analysis by visually correlating SLO burn-rate spikes with system deployments.
Output: `Parapet.Deploy` module exposing a `mark/1` function, and a post-deploy hook generator.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/3-slo-login-deploy/RESEARCH.md
@.planning/phases/3-slo-login-deploy/PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create Deploy Marker API</name>
  <files>lib/parapet/deploy.ex, test/parapet/deploy_test.exs</files>
  <behavior>
    - `Parapet.Deploy.mark(opts \\ [])` executes a `[:parapet, :deploy, :mark]` telemetry event.
    - Internally increments or associates a monotonic sequence (e.g., `System.system_time(:millisecond)`) to distinguish consecutive deploys in Prometheus/Grafana.
    - Accepts optional metadata such as `version` or `revision`.
  </behavior>
  <action>
    Implement `Parapet.Deploy.mark/1` which emits a specific telemetry event designed to be converted into a Prometheus counter/gauge that Grafana can consume as an annotation query. Write unit tests capturing and verifying the telemetry emission.
  </action>
  <verify>
    <automated>mix test test/parapet/deploy_test.exs</automated>
  </verify>
  <done>Telemetry deploy events are successfully emitted with temporal uniqueness.</done>
</task>

<task type="auto">
  <name>Task 2: Update Install Generator for Deploy Hook</name>
  <files>lib/mix/tasks/parapet.install.ex</files>
  <action>
    Update the install generator (`lib/mix/tasks/parapet.install.ex`) to create or modify the deploy hook script (`rel/hooks/post_start.sh`) as defined in DEPL-03. The hook should invoke an Elixir script or RPC call to emit `Parapet.Deploy.mark/1` upon deployment.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Install generator handles creation of rel/hooks/post_start.sh.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Application API | Developer calling `Parapet.Deploy.mark/1` |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Spoofing | `lib/parapet/deploy.ex` | accept | Deploy events are emitted internally by trusted application code on startup or via internal tooling. Low risk of external spoofing. |
</threat_model>

<verification>
1. `mix test test/parapet/deploy_test.exs` passes.
2. The emitted event includes `system_time` or equivalent monotonic sequence data.
</verification>

<success_criteria>
- Applications can explicitly mark deployments via a simple Elixir API.
- The telemetry footprint enables simple Grafana dashboard correlation annotations.
- Deploy hook generation is supported.
</success_criteria>

<output>
After completion, create `.planning/phases/03-slo-login-deploy/03-03-SUMMARY.md`
</output>
