---
phase: 02-rulestead-flag-correlation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/parapet/metrics/rulestead.ex
  - lib/parapet/integrations/rulestead.ex
  - lib/parapet/spine/alert_processor.ex
  - test/parapet/spine/alert_processor_test.exs
autonomous: true
requirements:
  - RUL-01
  - RUL-02
must_haves:
  truths:
    - Rulestead flag changes emit specific Prometheus metrics for Grafana overlay
    - When a new incident is created, recent system events are automatically copied to its timeline
  artifacts:
    - path: lib/parapet/metrics/rulestead.ex
      provides: Prometheus metric definitions
    - path: lib/parapet/spine/alert_processor.ex
      provides: Incident correlation logic
  key_links:
    - from: lib/parapet/spine/alert_processor.ex
      to: lib/parapet/spine/system_event.ex
      via: Ecto query on new incident creation
---

<objective>
Emit Prometheus metrics for Rulestead flag changes and automatically correlate recent `SystemEvent` records by creating `TimelineEntry` records when a new `Incident` triggers.

Purpose: Allow operators to see flag changes natively in Grafana and permanently durably record them in the incident timeline for RCA.
Output: Telemetry metrics definition and automated event correlation in AlertProcessor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/v0.6-phases/2/RESEARCH.md
@.planning/ROADMAP.md
@lib/parapet/spine/alert_processor.ex

<interfaces>
From `lib/parapet/metrics/scoria.ex`:
```elixir
defmodule Parapet.Metrics.Scoria do
  def metrics do
    import Telemetry.Metrics
    [
      counter("scoria_evaluation_total",
        event_name: [:parapet, :scoria, :eval, :completed],
        tags: [:guardrail, :passed, :model_name]
      )
    ]
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Rulestead Metric Definitions</name>
  <files>
    - lib/parapet/metrics/rulestead.ex
    - lib/parapet/integrations/rulestead.ex
  </files>
  <behavior>
    - Test 1: `Parapet.Metrics.Rulestead.metrics/0` returns the `Telemetry.Metrics.counter` for flag changes.
    - Test 2: Firing the `Rulestead` telemetry event causes the `Parapet` adapter to execute `[:parapet, :rulestead, :flag_change]`.
  </behavior>
  <action>
    Create `Parapet.Metrics.Rulestead`. Define a `metrics/0` function that returns a `counter("parapet_rulestead_flag_change_total", ...)` matching the telemetry event `[:parapet, :rulestead, :flag_change]`. 
    Update `Parapet.Integrations.Rulestead.handle_event/4` to execute `:telemetry.execute([:parapet, :rulestead, :flag_change], measurements, metadata)` AFTER inserting the `SystemEvent`.
  </action>
  <verify>
    <automated>mix test test/parapet/integrations/rulestead_test.exs</automated>
  </verify>
  <done>Metric definitions exist and are emitted by the adapter</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correlate SystemEvents to new Incidents</name>
  <files>
    - lib/parapet/spine/alert_processor.ex
    - test/parapet/spine/alert_processor_test.exs
  </files>
  <behavior>
    - Test 1: When `process_firing_alert` creates a *new* Incident, it copies recent `SystemEvent` records as `TimelineEntry` records for that incident.
    - Test 2: When `process_firing_alert` processes an *existing* Incident (returns un-inserted struct), it does NOT duplicate `TimelineEntry` records.
  </behavior>
  <action>
    Update `Parapet.Spine.AlertProcessor.process_firing_alert/1`.
    Inside the `{:ok, inserted_incident}` match, if `inserted_incident.id` is not `nil`, it means a new incident was created.
    Query `Parapet.Spine.SystemEvent` for records inserted in the last 60 minutes.
    Iterate over these events and insert a `Parapet.Spine.TimelineEntry` for each one, setting `incident_id` to the new incident, and copying `type` and `payload`.
  </action>
  <verify>
    <automated>mix test test/parapet/spine/alert_processor_test.exs</automated>
  </verify>
  <done>Recent system events appear in the incident timeline on creation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internal | AlertProcessor reads from internal SystemEvent buffer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | `AlertProcessor` | mitigate | Ensure the SystemEvent query uses a strict timeframe (e.g. 60 mins) to prevent massive memory loads during correlation. |
</threat_model>
