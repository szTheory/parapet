---
phase: "03-threadline-compliance"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/integrations/threadline.ex
autonomous: true
requirements: ["THR-01"]
must_haves:
  truths:
    - "System can safely process audit telemetry events without crashing if Threadline is absent"
    - "Threadline integration telemetry handler correctly formats audit attributes to Threadline schema"
  artifacts:
    - path: "lib/parapet/integrations/threadline.ex"
      provides: "Telemetry event handler handle_event/4"
  key_links:
    - from: "lib/parapet/integrations/threadline.ex"
      to: "Threadline"
      via: "Code.ensure_loaded?/1"
---

<objective>
Implement telemetry event handling in `Parapet.Integrations.Threadline` to allow syncing audit records to the Threadline library without introducing a hard dependency.

Purpose: Guarantee that all Parapet operator actions satisfy strict compliance requirements by mirroring to Threadline when it is installed.
Output: Updated Threadline integration module with a telemetry `handle_event/4` capability.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v0.6-ROADMAP.md
@.planning/v0.6-REQUIREMENTS.md
@.planning/v0.6-phases/3/RESEARCH.md
@.planning/v0.6-phases/3/PATTERNS.md
@lib/parapet/integrations/threadline.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add telemetry event handler to Threadline integration</name>
  <files>lib/parapet/integrations/threadline.ex</files>
  <action>
    Implement a telemetry event handler to consume `[:parapet, :audit, :created]` events, following the analog pattern from `lib/parapet/integrations/mailglass.ex` (as documented in PATTERNS.md).
    
    Implementation details:
    1. Define `handle_event(event, measurements, metadata, _config)` that matches `[:parapet, :audit, :created]`.
    2. Use `Code.ensure_loaded?(Threadline)` to check if the Threadline library is present in the host application.
    3. If true, map the `metadata.audit_attrs` map to Threadline's expected shape. You can use the existing `to_threadline_shape/1` logic but adapted for a Map instead of a struct.
    4. Call the expected Threadline API via `apply(Threadline, :log_audit, [mapped_attrs])`.
    5. Handle errors gracefully using a `rescue` block to log exceptions without crashing the telemetry pipeline, identical to the pattern in `mailglass.ex`.
    6. If `Code.ensure_loaded?(Threadline)` is false, simply return `:ok`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>The `handle_event/4` function exists, uses `Code.ensure_loaded?/1`, rescues exceptions gracefully, and bridges to Threadline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parapet -> Threadline | Audit payload passed to external library |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Threadline.ex | accept | Payload is internal system state (audit logs), no user input. |
</threat_model>

<verification>
- Ensure module compiles cleanly without Threadline present.
</verification>

<success_criteria>
- The integration handles Map payload structures for audit records via telemetry.
- Optional dependency check is robust and guarantees clean compile-out.
</success_criteria>

<output>
After completion, create `.planning/v0.6-phases/3/03-01-SUMMARY.md`
</output>
