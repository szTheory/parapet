---
phase: "03-threadline-compliance"
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/parapet/evidence.ex
autonomous: true
requirements: ["THR-01"]
must_haves:
  truths:
    - "System exposes `config :parapet, audit_mode` for administrators"
    - "Evidence handles `:dual_write` by writing ToolAudit to DB and broadcasting telemetry"
    - "Evidence handles `:threadline_deferred` by bypassing DB and broadcasting telemetry directly"
  artifacts:
    - path: "lib/parapet/evidence.ex"
      provides: "Configured transaction logic in run_operator_command/1"
  key_links:
    - from: "lib/parapet/evidence.ex"
      to: "telemetry"
      via: ":telemetry.execute/3"
---

<objective>
Update `Parapet.Evidence` to support `audit_mode` configuration, enabling compliance parity by either dual-writing or deferring audit storage via telemetry events.

Purpose: Allow operators to maintain a single source of truth for administrative actions in compliance-heavy environments without data duplication.
Output: Conditional execution paths in `Evidence` methods utilizing `:telemetry.execute`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v0.6-ROADMAP.md
@.planning/v0.6-REQUIREMENTS.md
@.planning/v0.6-phases/3/RESEARCH.md
@.planning/v0.6-phases/3/PATTERNS.md
@lib/parapet/evidence.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update `run_operator_command/1` with audit_mode conditional and telemetry</name>
  <files>lib/parapet/evidence.ex</files>
  <action>
    Update `run_operator_command/1` to read `audit_mode = Application.get_env(:parapet, :audit_mode, :dual_write)` using the configuration-driven injection pattern (analog: `lib/parapet/notifier.ex`).
    
    Branch the `Ecto.Multi` logic:
    - If `:threadline_deferred`: skip inserting `ToolAudit` into the DB. Instead, use `Ecto.Multi.run(:tool_audit, ...)` to return `{:ok, :deferred}` and invoke `:telemetry.execute([:parapet, :audit, :created], %{}, %{audit_attrs: audit_attrs})` to broadcast the audit.
    - If `:dual_write` (default): keep the original `Ecto.Multi.insert(:tool_audit, ...)` behavior. Use an `Ecto.Multi.run(:broadcast_audit, ...)` or a post-transaction block to execute the same `:telemetry.execute([:parapet, :audit, :created], ...)` call upon successful insert.
    
    Do NOT hardcode direct calls to Threadline integration. Rely entirely on the telemetry event dispatcher.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>The Multi operation dynamically adapts to `audit_mode` and broadcasts via `:telemetry.execute`.</done>
</task>

<task type="auto">
  <name>Task 2: Update `log_tool_audit/1` with audit_mode conditional</name>
  <files>lib/parapet/evidence.ex</files>
  <action>
    Similarly, update `log_tool_audit/1` to respect `audit_mode` via `Application.get_env/3`.
    - If `:threadline_deferred`: bypass DB insertion, call `:telemetry.execute([:parapet, :audit, :created], %{}, %{audit_attrs: attrs})`, and return `{:ok, :deferred}`.
    - If `:dual_write` (default): insert to DB. On success, trigger the telemetry event and return the `{:ok, struct}`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Direct `log_tool_audit/1` calls honor the deferred storage requirements using decoupled telemetry dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parapet API -> Ecto | Audits persisting to DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | Evidence.ex | mitigate | Ensure sensitive fields are not inappropriately logged to DB when deferred mode is expected. Branching must be strict. |
</threat_model>

<verification>
- Ecto multi structure must reliably return the `:tool_audit` key even when deferred to prevent consumer crashes.
</verification>

<success_criteria>
- `run_operator_command/1` successfully avoids writing ToolAudits to the database when in deferred mode.
- Both modes successfully dispatch `[:parapet, :audit, :created]` telemetry events.
</success_criteria>

<output>
After completion, create `.planning/v0.6-phases/3/03-02-SUMMARY.md`
</output>
