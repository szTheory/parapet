---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/runbook.ex
  - lib/mix/tasks/parapet.gen.runbooks.ex
  - priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex
  - priv/templates/parapet.gen.runbooks/dead_letter.ex.eex
  - priv/templates/parapet.gen.runbooks/provider_outage.ex.eex
  - priv/templates/parapet.gen.runbooks/callback_delay.ex.eex
  - test/parapet/runbook_test.exs
  - test/mix/tasks/parapet.gen.runbooks_test.exs
autonomous: true
requirements:
  - RNBK-01
must_haves:
  truths:
    - "Phase 7 ships a fixed host-generated runbook catalog instead of a generic workflow DSL."
    - "Generated runbook steps have stable identities and can express guidance-only versus capability-backed recovery."
    - "Hosts own the generated modules and can edit copy or disable capabilities without forking Parapet internals."
  artifacts:
    - path: "lib/mix/tasks/parapet.gen.runbooks.ex"
      provides: "Igniter-based generator for the fixed Phase 7 runbook catalog"
    - path: "lib/parapet/runbook.ex"
      provides: "Runbook schema contract with capability-aware step metadata"
    - path: "priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex"
      provides: "Generated host-owned runbook template for stalled executor incidents"
  key_links:
    - from: "lib/mix/tasks/parapet.gen.runbooks.ex"
      to: "lib/parapet/runbook.ex"
      via: "generated modules implement the enriched runbook DSL"
    - from: "priv/templates/parapet.gen.runbooks/*.eex"
      to: ".planning/v0.7-phases/7/7-CONTEXT.md"
      via: "fixed catalog identities and locked capability vocabulary"
---

<objective>
Build the fixed host-owned runbook catalog and enrich the runbook schema contract so Phase 7 has concrete generated modules to attach to incidents and render in the operator UI.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-phases/7/7-CONTEXT.md
@.planning/v0.7-phases/7/RESEARCH.md
@.planning/v0.7-phases/7/PATTERNS.md
@.planning/v0.7-phases/7/07-VALIDATION.md
@lib/parapet/runbook.ex
@lib/mix/tasks/parapet.gen.ui.ex
@test/parapet/runbook_test.exs
</context>

<tasks>

<task type="execute">
  <name>Task 1: Enrich the runbook DSL for Phase 7 capability-aware steps</name>
  <read_first>
    - lib/parapet/runbook.ex
    - test/parapet/runbook_test.exs
    - .planning/v0.7-phases/7/7-CONTEXT.md
    - .planning/v0.7-phases/7/RESEARCH.md
  </read_first>
  <files>lib/parapet/runbook.ex, test/parapet/runbook_test.exs</files>
  <action>Extend `Parapet.Runbook.step/2` and `__runbook_schema__/0` so each emitted step map can carry concrete Phase 7 metadata keys: `kind`, `capability`, `target_kind`, `requires_preview`, `preview_only`, and `guidance`, while preserving existing `id`, `label`, `description`, and `type` keys. Keep the top-level schema shape `%{module, title, description, steps}` stable. Preserve compatibility for existing simple `type: :mitigation` definitions by defaulting new keys sanely instead of breaking current tests.</action>
  <acceptance_criteria>
    - `lib/parapet/runbook.ex` contains `kind:`, `capability:`, `target_kind:`, `requires_preview:`, `preview_only:`, and `guidance:` inside the emitted step schema.
    - `lib/parapet/runbook.ex` still defines `def __runbook_schema__()` and returns `module`, `title`, `description`, and `steps`.
    - `test/parapet/runbook_test.exs` contains assertions for at least one capability-backed step and one guidance-only step schema.
    - `mix test test/parapet/runbook_test.exs` exits `0`.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Add a fixed-catalog runbook generator for host-owned modules</name>
  <read_first>
    - lib/mix/tasks/parapet.gen.ui.ex
    - lib/parapet/runbook.ex
    - .planning/v0.7-phases/7/7-CONTEXT.md
    - .planning/v0.7-phases/7/PATTERNS.md
  </read_first>
  <files>lib/mix/tasks/parapet.gen.runbooks.ex, priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex, priv/templates/parapet.gen.runbooks/dead_letter.ex.eex, priv/templates/parapet.gen.runbooks/provider_outage.ex.eex, priv/templates/parapet.gen.runbooks/callback_delay.ex.eex, test/mix/tasks/parapet.gen.runbooks_test.exs</files>
  <action>Create `Mix.Tasks.Parapet.Gen.Runbooks` as an `Igniter.Mix.Task` that writes four host-owned runbook modules from templates: `StalledExecutor`, `DeadLetter`, `ProviderOutage`, and `CallbackDelay` under the host app namespace. Each template must define concrete steps using the enriched `Parapet.Runbook` DSL and the locked capability names `retry_async_item`, `requeue_dead_letter`, and `request_manual_provider_check`; guidance-only investigative steps should use `preview_only: true` or omit capability wiring explicitly. Follow the copy-template and host-module discovery pattern already used by `mix parapet.gen.ui`.</action>
  <acceptance_criteria>
    - `lib/mix/tasks/parapet.gen.runbooks.ex` defines `defmodule Mix.Tasks.Parapet.Gen.Runbooks`.
    - The generator copies exactly four catalog templates: `stalled_executor`, `dead_letter`, `provider_outage`, and `callback_delay`.
    - Each template contains `use Parapet.Runbook` and at least one `step(` call.
    - At least one template contains `capability: :retry_async_item`, one contains `capability: :requeue_dead_letter`, and one contains `capability: :request_manual_provider_check`.
    - `test/mix/tasks/parapet.gen.runbooks_test.exs` asserts generated output includes the four module files and exits `0`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Library-owned runbook schema -> host-generated modules | Phase 7 emits host-editable source that must remain bounded and explicit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | generated runbook templates | mitigate | Generate a fixed catalog with explicit capability names and stable step ids rather than a free-form workflow DSL. |
| T-07-01-02 | Elevation of Privilege | runbook step schema | mitigate | Distinguish guidance-only steps from capability-backed executable steps in the schema itself. |
</threat_model>

<verification>
- Run `mix test test/parapet/runbook_test.exs`.
- Run `mix test test/mix/tasks/parapet.gen.runbooks_test.exs`.
- Confirm the generator emits only the four locked catalog members.
</verification>

<success_criteria>
- The runbook DSL can express Phase 7 capability-aware and guidance-only steps.
- Hosts can generate and inspect a fixed runbook catalog without relying on opaque runtime magic.
- `RNBK-01` has a concrete implementation foundation in source, not just docs.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/7/07-01-SUMMARY.md`
</output>
