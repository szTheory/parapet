---
phase: 07
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - lib/parapet/operator/workbench_contract.ex
  - lib/parapet/spine/alert_processor.ex
  - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - docs/operator-ui.md
  - test/parapet/operator/workbench_contract_test.exs
  - test/parapet/spine/alert_processor_test.exs
autonomous: true
requirements:
  - RNBK-01
  - RNBK-02
must_haves:
  truths:
    - "Operator detail remains evidence-first, with recovery rendered as a bounded follow-up layer rather than a broad action console."
    - "Generated UI shows Preview or Guidance states instead of one-click mutation."
    - "Incident runbook attachment stays bounded and deterministic on top of the Phase 6 triage and exact-item seams."
  artifacts:
    - path: "lib/parapet/operator/workbench_contract.ex"
      provides: "Derived preview-ready runbook and recovery state for the operator detail surface"
    - path: "priv/templates/parapet.gen.ui/operator_components.ex.eex"
      provides: "Preview-first runbook rendering and exact-item recovery affordances"
    - path: "docs/operator-ui.md"
      provides: "Updated operator doctrine for safe investigation and scoped recovery"
  key_links:
    - from: "lib/parapet/spine/alert_processor.ex"
      to: "lib/parapet/operator/workbench_contract.ex"
      via: "incident runbook data feeds deterministic operator recovery rendering"
    - from: "priv/templates/parapet.gen.ui/operator_detail_live.ex.eex"
      to: "lib/parapet/operator.ex"
      via: "LiveView events call preview and confirm, not direct execute"
---

<objective>
Close Phase 7 by wiring bounded runbook attachment, preview-first operator rendering, and doctrine updates into the generated workbench.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-phases/7/7-CONTEXT.md
@.planning/v0.7-phases/7/RESEARCH.md
@.planning/v0.7-phases/7/PATTERNS.md
@.planning/v0.7-phases/7/07-VALIDATION.md
@lib/parapet/operator/workbench_contract.ex
@lib/parapet/spine/alert_processor.ex
@priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
@priv/templates/parapet.gen.ui/operator_components.ex.eex
@docs/operator-ui.md
@test/parapet/operator/workbench_contract_test.exs
@test/parapet/spine/alert_processor_test.exs
</context>

<tasks>

<task type="execute">
  <name>Task 1: Derive preview-ready recovery state and bounded runbook attachment</name>
  <read_first>
    - lib/parapet/operator/workbench_contract.ex
    - lib/parapet/spine/alert_processor.ex
    - test/parapet/operator/workbench_contract_test.exs
    - test/parapet/spine/alert_processor_test.exs
    - .planning/v0.7-phases/6/6-CONTEXT.md
    - .planning/v0.7-phases/7/7-CONTEXT.md
  </read_first>
  <files>lib/parapet/operator/workbench_contract.ex, lib/parapet/spine/alert_processor.ex, test/parapet/operator/workbench_contract_test.exs, test/parapet/spine/alert_processor_test.exs</files>
  <action>Extend `Parapet.Operator.WorkbenchContract` so incident detail can expose a bounded recovery view model derived from durable evidence: previewable runbook steps, guidance-only steps, exact `ActionItem` targeting hints, and any current preview state needed by the UI. In `Parapet.Spine.AlertProcessor`, keep runbook attachment deterministic and bounded by selecting or merging the correct generated runbook schema based on Phase 6 triage or exact-item facts without broadening storage or pushing execution logic into ingestion. Preserve chronology-first semantics and keep `runbook_data` compact.</action>
  <acceptance_criteria>
    - `lib/parapet/operator/workbench_contract.ex` derives runbook or recovery fields beyond raw triage, with names indicating preview or guidance state.
    - `lib/parapet/spine/alert_processor.ex` still uses `build_runbook_data` or an equivalent bounded helper instead of embedding execution logic.
    - `test/parapet/operator/workbench_contract_test.exs` asserts a detail payload with previewable and guidance-only step distinctions.
    - `test/parapet/spine/alert_processor_test.exs` asserts bounded runbook attachment remains nested alongside triage summary.
    - `mix test test/parapet/operator/workbench_contract_test.exs test/parapet/spine/alert_processor_test.exs` exits `0`.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Update the generated operator UI and docs for preview-first recovery</name>
  <read_first>
    - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
    - priv/templates/parapet.gen.ui/operator_components.ex.eex
    - docs/operator-ui.md
    - .planning/v0.7-phases/7/RESEARCH.md
    - .planning/v0.7-phases/7/07-VALIDATION.md
  </read_first>
  <files>priv/templates/parapet.gen.ui/operator_detail_live.ex.eex, priv/templates/parapet.gen.ui/operator_components.ex.eex, docs/operator-ui.md</files>
  <action>Replace the current one-click mitigation flow in the generated LiveView templates with explicit preview and confirm handling. `operator_detail_live.ex.eex` should call the new operator preview and confirm APIs and store preview state in assigns. `operator_components.ex.eex` should render Preview buttons for executable capability-backed steps, Guidance styling for preview-only or unwired steps, and a dedicated panel or modal that shows exact scope, warnings, preconditions, idempotency caveats, and stale-preview handling before confirm. Update `docs/operator-ui.md` so the operator doctrine explicitly says chronology and triage come first, recovery actions are bounded follow-up work, and exact-item recovery is preferred over bulk replay or opaque automation.</action>
  <acceptance_criteria>
    - `priv/templates/parapet.gen.ui/operator_detail_live.ex.eex` no longer calls `execute_runbook_step` directly from the button click path.
    - `priv/templates/parapet.gen.ui/operator_components.ex.eex` contains `Preview` text for executable steps and separate guidance rendering for non-executable steps.
    - `docs/operator-ui.md` contains language covering preview-first recovery, exact-item scope, and auditability.
    - `mix compile --warnings-as-errors` exits `0`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable runbook and triage state -> generated operator UI | The UI must render bounded recovery options without inventing new mutation scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Spoofing | UI-triggered recovery intent | mitigate | Render capability identity and exact preview scope explicitly before confirm. |
| T-07-03-02 | Information Disclosure | runbook and preview payload rendering | mitigate | Keep runbook and preview data bounded and avoid storing or displaying raw provider/job streams. |
</threat_model>

<verification>
- Run `mix test test/parapet/operator/workbench_contract_test.exs test/parapet/spine/alert_processor_test.exs`.
- Run `mix compile --warnings-as-errors`.
- Confirm the generated UI no longer exposes one-click execution for mutating recovery.
</verification>

<success_criteria>
- The generated workbench presents bounded preview-first recovery on top of Phase 6 evidence.
- Runbook attachment remains deterministic and compact.
- Phase 7 closes with code and docs aligned to safe investigation and scoped host-owned recovery.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/7/07-03-SUMMARY.md`
</output>
