---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/telemetry/async_delivery.ex
  - lib/parapet/internal/label_policy.ex
  - test/parapet/telemetry/async_delivery_test.exs
  - test/parapet/internal/label_policy_test.exs
  - test/parapet_test.exs
autonomous: true
requirements:
  - DELV-01
  - TRIAGE-01
must_haves:
  truths:
    - "System defines one bounded public async/delivery telemetry vocabulary for downstream phases."
    - "System enforces family-aware allowed tag and metadata keys instead of relying only on regex denylist checks."
    - "Exact identifiers are either diverted into refs or rejected from public label-safe metadata."
  artifacts:
    - path: "lib/parapet/telemetry/async_delivery.ex"
      provides: "Phase 4 contract primitives for event names, normalization, and safe metadata shaping"
    - path: "lib/parapet/internal/label_policy.ex"
      provides: "Contract-aware label policy helpers"
  key_links:
    - from: "lib/parapet/telemetry/async_delivery.ex"
      to: "lib/parapet/internal/label_policy.ex"
      via: "allowed tag and metadata contracts"
---

<objective>
Create the shared async/delivery contract primitives that all Phase 4 adapters will target.

Purpose: lock the public vocabulary, tag safety rules, ref splitting, and normalization helpers before adapter migration starts.
Output: a contract module and label policy extensions that make the new `[:parapet, :delivery, ...]` and `[:parapet, :async, ...]` families explicit and testable.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-phases/4/4-CONTEXT.md
@.planning/v0.7-phases/4/RESEARCH.md
@.planning/v0.7-phases/4/PATTERNS.md
@lib/parapet/internal/label_policy.ex
@lib/parapet/metrics/oban.ex
@lib/parapet/metrics/scoria.ex
@test/parapet/internal/label_policy_test.exs
@test/parapet_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce async/delivery contract module</name>
  <files>lib/parapet/telemetry/async_delivery.ex, test/parapet/telemetry/async_delivery_test.exs</files>
  <behavior>
    - Test 1: public event families are fixed to the six families locked in the phase context.
    - Test 2: normalization helpers map delivery and async outcomes into bounded enums only.
    - Test 3: high-cardinality identifiers are shaped into `refs` or dropped from public metadata.
    - Test 4: delay values are bucketed into bounded buckets instead of becoming raw tags.
  </behavior>
  <action>Create a dedicated contract module, likely `Parapet.Telemetry.AsyncDelivery`, that owns the Phase 4 public vocabulary. Include helpers for canonical event names, delivery outcome normalization, async outcome normalization, fault-plane normalization, retry-state normalization, delay bucket mapping, and metadata shaping into `{public_metadata, refs}` or an equivalent safe form. Follow the allowlisted extraction style mapped in `PATTERNS.md` from `Scoria`, not pass-through metadata patterns from current adapter modules.</action>
  <verify>
    <automated>mix test test/parapet/telemetry/async_delivery_test.exs</automated>
  </verify>
  <done>The contract module exposes one stable bounded vocabulary and its helper tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend label policy for contract-aware allowlists</name>
  <files>lib/parapet/internal/label_policy.ex, test/parapet/internal/label_policy_test.exs, test/parapet_test.exs</files>
  <behavior>
    - Test 1: existing safe-label behavior remains intact for current metrics modules.
    - Test 2: contract-aware APIs accept only the allowed keys for each Phase 4 family.
    - Test 3: suspicious keys like `*_id`, `raw_*`, `token`, and `path` are still rejected where applicable.
  </behavior>
  <action>Evolve `Parapet.Internal.LabelPolicy` from regex-only denylist checks into a two-layer guardrail: preserve `assert_safe!/1` for existing metrics, and add a family-aware API that validates the allowed public tag keys for the new async/delivery contract. Keep the implementation simple and explicit: fixed allowlists per event family, no dynamic schema inference. Wire the tests so they protect both backward compatibility and the stricter Phase 4 behavior.</action>
  <verify>
    <automated>mix test test/parapet/internal/label_policy_test.exs test/parapet_test.exs</automated>
  </verify>
  <done>Label policy now proves the Phase 4 contract is bounded by construction without breaking current callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream integration payloads -> Parapet public telemetry | Rich or high-cardinality metadata crosses into a public event contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | Async/delivery contract module | mitigate | Force bounded allowlists and `refs` separation so public tags never expose raw identifiers or provider payload fragments. |
| T-04-01-02 | Tampering | Label policy | mitigate | Encode allowed keys explicitly per family so downstream metrics cannot silently widen the contract. |
</threat_model>

<verification>
- Run the focused contract and label-policy tests.
- Confirm the new contract module remains compile-safe and dependency-agnostic.
</verification>

<success_criteria>
- There is one explicit shared source of truth for Phase 4 event families and safe metadata.
- Label policy can validate the new families directly instead of depending only on heuristic name rejection.
- The contract proves how exact identifiers are handled without turning them into label-safe metadata.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/4/04-01-SUMMARY.md`
</output>
