---
phase: 04
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/parapet/integrations/mailglass.ex
  - lib/parapet/integrations/chimeway.ex
  - test/parapet/integrations/mailglass_test.exs
  - test/parapet/integrations/chimeway_test.exs
autonomous: true
requirements:
  - DELV-01
  - TRIAGE-01
must_haves:
  truths:
    - "Mailglass and Chimeway emit normalized delivery event families instead of thin journey shims."
    - "Provider acceptance, terminal delivery, provider failure, suppression, and webhook drift remain distinguishable where supported."
    - "Public metadata remains bounded and excludes high-cardinality identifiers."
  artifacts:
    - path: "lib/parapet/integrations/mailglass.ex"
      provides: "Mailglass translation onto the Phase 4 delivery contract"
    - path: "lib/parapet/integrations/chimeway.ex"
      provides: "Chimeway translation onto the Phase 4 delivery contract"
  key_links:
    - from: "lib/parapet/integrations/mailglass.ex"
      to: "lib/parapet/telemetry/async_delivery.ex"
      via: "shared contract helpers"
    - from: "lib/parapet/integrations/chimeway.ex"
      to: "lib/parapet/telemetry/async_delivery.ex"
      via: "shared contract helpers"
---

<objective>
Migrate `Mailglass` and `Chimeway` to the normalized Phase 4 delivery contract.

Purpose: ensure downstream reliability features consume one delivery vocabulary instead of provider-specific or journey-only shims.
Output: adapter modules and tests that emit bounded `[:parapet, :delivery, ...]` public events with safe metadata.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-phases/4/4-CONTEXT.md
@.planning/v0.7-phases/4/RESEARCH.md
@.planning/v0.7-phases/4/PATTERNS.md
@.planning/v0.7-phases/4/04-01-PLAN.md
@lib/parapet/integrations/mailglass.ex
@lib/parapet/integrations/chimeway.ex
@lib/parapet/integrations/sigra.ex
@lib/parapet/integrations/accrue.ex
@lib/parapet/internal/safe_handler.ex
@test/parapet/integrations/mailglass_test.exs
@test/parapet/integrations/chimeway_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize the real Chimeway telemetry surface before refactor</name>
  <files>test/parapet/integrations/chimeway_test.exs</files>
  <behavior>
    - Test 1: the upstream Chimeway events and metadata keys relied on by the adapter are pinned explicitly in a characterization test or equivalent proof.
    - Test 2: the proof makes clear which delivery states are actually exposed today versus which states are deferred until the sibling library grows them.
  </behavior>
  <action>Because `RESEARCH.md` leaves broader Chimeway event coverage as the one unresolved uncertainty, start this plan by pinning the actual upstream Chimeway telemetry surface used by Parapet. Use a characterization-style test, fixture, or equivalent proof against the real currently-supported Chimeway event names and metadata keys. This task should reduce uncertainty, not invent hypothetical provider states. Once the supported upstream surface is explicit, the remaining adapter work can normalize only what is actually present and document any capability gaps cleanly.</action>
  <verify>
    <automated>mix test test/parapet/integrations/chimeway_test.exs</automated>
  </verify>
  <done>The plan no longer depends on guessed Chimeway event names or metadata; the current upstream surface is pinned in executable proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rework Mailglass onto normalized delivery families</name>
  <files>lib/parapet/integrations/mailglass.ex, test/parapet/integrations/mailglass_test.exs</files>
  <behavior>
    - Test 1: supported outbound events emit `[:parapet, :delivery, :outbound]` with bounded metadata.
    - Test 2: provider feedback emits `[:parapet, :delivery, :provider_feedback]` with normalized outcomes such as `provider_accepted`, `delivered`, `failed`, `bounced`, `complained`, or `suppressed` where appropriate.
    - Test 3: webhook ingest emits `[:parapet, :delivery, :webhook_ingest]` and keeps webhook-plane delay or failure distinct from provider failure.
    - Test 4: unsafe identifiers like recipient or provider message ids are absent from public tags and only present in `refs` if the contract exposes them.
  </behavior>
  <action>Rewrite `Parapet.Integrations.Mailglass.setup/0` to attach the relevant upstream delivery, provider-feedback, and webhook events using grouped attachment patterns (`attach_many/4` or the repo's safe attachment seam). Use the Phase 4 contract helpers from plan 01 to shape normalized metadata and measurements. Do not keep emitting the old `[:parapet, :journey, :mail_delivery]` event as the primary public contract. Preserve crash safety and explicit host-owned activation.</action>
  <verify>
    <automated>mix test test/parapet/integrations/mailglass_test.exs</automated>
  </verify>
  <done>Mailglass emits the new delivery families and its tests assert normalized outcomes plus metadata safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rework Chimeway onto normalized delivery families</name>
  <files>lib/parapet/integrations/chimeway.ex, test/parapet/integrations/chimeway_test.exs</files>
  <behavior>
    - Test 1: for the states the characterization task proved available upstream, provider-accepted and callback-confirmed delivery remain distinct.
    - Test 2: callback delay or webhook silence maps to a webhook or callback-plane signal, not generic provider failure, when the upstream surface exposes the needed timing or failure cues.
    - Test 3: bounded metadata includes only safe fields such as `integration`, `provider`, `channel`, `outcome`, `failure_class`, `delay_bucket`, and `fault_plane` where applicable.
  </behavior>
  <action>Rewrite `Parapet.Integrations.Chimeway` to mirror the same public delivery families as Mailglass while respecting the actual Chimeway surface proven in Task 1. Use explicit `process_event/3` clauses for each supported upstream state and compute normalized `outcome`, `fault_plane`, and optional `delay_bucket` values through the shared contract module. Follow the stronger `Sigra`/`Accrue` multi-event translation pattern instead of the current single failure-event shim, but do not over-promise unsupported provider states.</action>
  <verify>
    <automated>mix test test/parapet/integrations/chimeway_test.exs</automated>
  </verify>
  <done>Chimeway emits the same normalized delivery vocabulary with safe metadata and preserves acceptance-vs-delivery separation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider and webhook payloads -> public delivery telemetry | Rich provider metadata crosses from sibling integrations into public Parapet telemetry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | Mailglass / Chimeway adapters | mitigate | Allowlist only bounded metadata fields and route exact identifiers into `refs` or drop them. |
| T-04-02-02 | Denial of Service | Telemetry handler path | mitigate | Preserve safe attachment and rescue behavior so malformed provider payloads cannot crash host telemetry emitters. |
</threat_model>

<verification>
- Run focused adapter tests for Mailglass and Chimeway.
- Confirm the new public events are the normalized delivery families and not the old journey shim.
</verification>

<success_criteria>
- Mailglass and Chimeway now speak one public delivery contract.
- Acceptance, terminal delivery, provider failure, suppression, and webhook drift remain distinguishable where the upstream integration supports them.
- Adapter tests prove public metadata is bounded and safe.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/4/04-02-SUMMARY.md`
</output>
