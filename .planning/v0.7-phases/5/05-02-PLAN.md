---
phase: 05
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lib/parapet/slo/mailglass_delivery.ex
  - lib/parapet/slo/chimeway_delivery.ex
  - lib/parapet/slo/rindle_async.ex
  - test/parapet/slo/mailglass_delivery_test.exs
  - test/parapet/slo/chimeway_delivery_test.exs
  - test/parapet/slo/rindle_async_test.exs
  - test/parapet/slo_test.exs
autonomous: true
requirements:
  - DELV-02
  - DELV-03
  - ASYNC-01
  - ASYNC-02
  - ASYNC-03
must_haves:
  truths:
    - "Mailglass, Chimeway, and Rindle each expose one explicit built-in provider module with a small catalog of slices."
    - "Retry noise stays distinct from terminal or user-harming failure in the provider catalogs."
    - "Webhook or callback freshness remains separate from backlog in the shipped async and delivery slice definitions."
  artifacts:
    - path: "lib/parapet/slo/mailglass_delivery.ex"
      provides: "Mailglass provider-backed delivery slice catalog"
    - path: "lib/parapet/slo/chimeway_delivery.ex"
      provides: "Chimeway provider-backed delivery slice catalog"
    - path: "lib/parapet/slo/rindle_async.ex"
      provides: "Rindle provider-backed async slice catalog"
  key_links:
    - from: "lib/parapet/slo/mailglass_delivery.ex"
      to: "lib/parapet/metrics/async_delivery.ex"
      via: "shared metric family selectors"
    - from: "lib/parapet/slo/chimeway_delivery.ex"
      to: "lib/parapet/telemetry/async_delivery.ex"
      via: "delivery outcome and webhook-plane semantics"
    - from: "lib/parapet/slo/rindle_async.ex"
      to: "lib/parapet/telemetry/async_delivery.ex"
      via: "discard vs retryable vs freshness semantics"
---

<objective>
Implement the three explicit provider catalogs for Mailglass, Chimeway, and Rindle.

Purpose: convert the normalized Phase 4 contract into a concrete, opinionated built-in slice catalog without adding hidden activation.
Output: provider modules and tests for the exact Phase 5 slice set.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-phases/5/5-CONTEXT.md
@.planning/v0.7-phases/5/RESEARCH.md
@.planning/v0.7-phases/5/PATTERNS.md
@.planning/v0.7-phases/5/05-01-PLAN.md
@.planning/v0.7-phases/4/04-02-SUMMARY.md
@.planning/v0.7-phases/4/04-03-SUMMARY.md
@lib/parapet/slo/provider.ex
@lib/parapet/slo.ex
@lib/parapet/integrations/mailglass.ex
@lib/parapet/integrations/chimeway.ex
@lib/parapet/integrations/rindle.ex
@test/parapet/integrations/mailglass_test.exs
@test/parapet/integrations/chimeway_test.exs
@test/parapet/integrations/rindle_test.exs
@test/parapet/slo_test.exs

<interfaces>
From lib/parapet/slo/provider.ex:
```elixir
@callback slos() :: [struct()]
```

From lib/parapet/slo.ex:
```elixir
def all do
  legacy_slos = Application.get_env(:parapet, :slos, [])

  provider_slos =
    Application.get_env(:parapet, :providers, [])
    |> Enum.flat_map(fn provider -> provider.slos() end)
    |> Enum.map(&Parapet.SLO.Resolvable.to_slo/1)

  legacy_slos ++ provider_slos
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Mailglass and Chimeway delivery provider catalogs</name>
  <files>lib/parapet/slo/mailglass_delivery.ex, lib/parapet/slo/chimeway_delivery.ex, test/parapet/slo/mailglass_delivery_test.exs, test/parapet/slo/chimeway_delivery_test.exs</files>
  <behavior>
    - Test 1: `Parapet.SLO.MailglassDelivery` exposes exactly the default slices from D-08, with `provider_accepted` distinct from `delivered`.
    - Test 2: `mailglass_suppression_drift` is marked diagnostic or ticket-grade rather than a default paging-budget SLO.
    - Test 3: `Parapet.SLO.ChimewayDelivery` exposes exactly the default slices from D-10 and stays aligned to the proven Chimeway surface from Phase 4.
    - Test 4: callback freshness slices group on bounded operator labels and do not collapse into provider-acceptance semantics.
  </behavior>
  <action>Implement `Parapet.SLO.MailglassDelivery` and `Parapet.SLO.ChimewayDelivery` per D-05 through D-11. Both modules should `@behaviour Parapet.SLO.Provider` and return a small catalog of Phase 5 slice specs from `slos/0`, not hidden registration side effects. Mailglass must ship `mailglass_submit_acceptance`, `mailglass_confirmed_delivery`, `mailglass_webhook_freshness`, and `mailglass_suppression_drift`; Chimeway must ship `chimeway_provider_acceptance`, `chimeway_callback_confirmation`, and `chimeway_callback_freshness`. Build selectors over the shared metric families and the normalized Phase 4 outcomes, keeping callback/webhook delay separate from provider failure and preserving bounded labels per D-20 and D-34.</action>
  <verify>
    <automated>mix test test/parapet/slo/mailglass_delivery_test.exs test/parapet/slo/chimeway_delivery_test.exs</automated>
  </verify>
  <done>The two delivery providers expose the exact locked slice catalogs with explicit, low-cardinality semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the Rindle async provider catalog and explicit registration proof</name>
  <files>lib/parapet/slo/rindle_async.ex, test/parapet/slo/rindle_async_test.exs, test/parapet/slo_test.exs</files>
  <behavior>
    - Test 1: `Parapet.SLO.RindleAsync` exposes exactly the slices from D-12.
    - Test 2: `retryable_failed` is represented as diagnostic or warning noise, while `discarded` is the terminal user-harming state for paging semantics.
    - Test 3: queue freshness and callback freshness are separate slices, proving ASYNC-03 at the provider layer.
    - Test 4: explicit provider registration through `config :parapet, providers: [...]` or `Application.put_env/3` is required; `Parapet.attach/1` does not silently activate SLOs.
  </behavior>
  <action>Implement `Parapet.SLO.RindleAsync` per D-06 and D-12 through D-15. Return a small provider-owned catalog covering `rindle_terminal_success`, `rindle_queue_freshness`, `rindle_callback_freshness`, `rindle_long_running_stage`, and `rindle_funnel_regression`. Encode queue-age or delay, not raw depth alone, as the default freshness signal per D-15. Treat `retryable_failed` as non-paging noise unless paired later by generator logic with sustained freshness burn per D-13, and treat `discarded` as terminal page-worthy harm per D-14. Extend `test/parapet/slo_test.exs` only enough to prove the explicit provider-registration seam from D-03, D-04, D-23, and D-24 stays intact while the provider-first path becomes the blessed one.</action>
  <verify>
    <automated>mix test test/parapet/slo/rindle_async_test.exs test/parapet/slo_test.exs</automated>
  </verify>
  <done>Rindle has a concrete built-in async provider, and the suite proves SLO activation remains explicit and provider-driven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider catalogs -> operator-visible SLO and alert semantics | Built-in defaults can amplify the wrong symptoms if terminality or plane separation is encoded incorrectly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Denial of Service | Delivery and async provider catalogs | mitigate | Keep paging semantics tied to user-harming or terminal states only, with retry noise and low-urgency drift encoded as warning or diagnostic slices. |
| T-05-02-02 | Tampering | Provider registration seam | mitigate | Preserve explicit `:providers` registration and add tests proving `Parapet.attach/1` does not auto-enable built-in SLOs. |
</threat_model>

<verification>
- Run the focused provider catalog tests plus `test/parapet/slo_test.exs`.
- Confirm the provider module names match the locked decision exactly.
- Confirm callback freshness, webhook freshness, and backlog freshness are separate slice families.
</verification>

<success_criteria>
- The repo ships the three exact provider modules named in D-06.
- Each provider exposes a small opinionated slice catalog instead of thin primitives or a provider-console matrix.
- Retryable async failure remains distinct from discarded work and from freshness symptoms.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/5/05-02-SUMMARY.md`
</output>
