---
phase: 05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/metrics/async_delivery.ex
  - lib/parapet/slo/slice_spec.ex
  - lib/parapet/slo/resolvable.ex
  - test/parapet/metrics/async_delivery_test.exs
  - test/parapet/metrics/mailglass_test.exs
  - test/parapet/metrics/chimeway_test.exs
  - test/parapet/metrics/rindle_test.exs
  - test/parapet/slo/resolvable_test.exs
autonomous: true
requirements:
  - DELV-02
  - DELV-03
  - ASYNC-01
must_haves:
  truths:
    - "Phase 5 has one shared low-cardinality metrics surface over the Phase 4 async and delivery contract instead of provider-specific metric families."
    - "Built-in providers can declare bounded slice specs without embedding ad hoc raw PromQL in each provider module."
    - "The provider-first path remains explicit and compatible with existing legacy `%Parapet.SLO{}` callers."
  artifacts:
    - path: "lib/parapet/metrics/async_delivery.ex"
      provides: "Shared Telemetry.Metrics families for normalized async and delivery events"
    - path: "lib/parapet/slo/slice_spec.ex"
      provides: "Bounded provider-owned slice spec for generated Phase 5 SLOs and alerts"
    - path: "lib/parapet/slo/resolvable.ex"
      provides: "Resolution seam from provider structs into legacy SLOs and generator inputs"
  key_links:
    - from: "lib/parapet/metrics/async_delivery.ex"
      to: "lib/parapet/telemetry/async_delivery.ex"
      via: "shared event family names and allowed tag shapes"
    - from: "lib/parapet/slo/slice_spec.ex"
      to: "lib/parapet/slo/resolvable.ex"
      via: "provider slice resolution for generator consumption"
---

<objective>
Create the shared metrics and slice-spec foundation that all Phase 5 built-ins will use.

Purpose: lock one coherent productization seam before the provider catalogs and Prometheus artifacts branch out.
Output: a shared async/delivery metrics module plus a bounded slice-spec resolution model for provider-owned built-ins.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-phases/5/5-CONTEXT.md
@.planning/v0.7-phases/5/RESEARCH.md
@.planning/v0.7-phases/5/PATTERNS.md
@.planning/v0.7-phases/4/04-01-SUMMARY.md
@.planning/v0.7-phases/4/04-02-SUMMARY.md
@.planning/v0.7-phases/4/04-03-SUMMARY.md
@lib/parapet/telemetry/async_delivery.ex
@lib/parapet/internal/label_policy.ex
@lib/parapet/metrics/scoria.ex
@lib/parapet/slo/resolvable.ex
@lib/parapet/slo/scoria_eval.ex
@test/parapet/telemetry/async_delivery_test.exs
@test/parapet/internal/label_policy_test.exs

<interfaces>
From lib/parapet/telemetry/async_delivery.ex:
```elixir
def event_families, do: @event_families
def event_name(family)
def allowed_public_keys(family)
def delay_bucket(delay_ms)
def shape_metadata(family, metadata)
```

From lib/parapet/slo/resolvable.ex:
```elixir
@spec to_slo(t) :: Parapet.SLO.t()
def to_slo(struct)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared async/delivery metric families over the Phase 4 contract</name>
  <files>lib/parapet/metrics/async_delivery.ex, test/parapet/metrics/async_delivery_test.exs, test/parapet/metrics/mailglass_test.exs, test/parapet/metrics/chimeway_test.exs, test/parapet/metrics/rindle_test.exs</files>
  <behavior>
    - Test 1: the module exports one shared metric catalog for the six Phase 4 families rather than per-provider metric namespaces.
    - Test 2: counters end in `_total`, delay or duration metrics use `_seconds`, and tags stay bounded to the family allowlists.
    - Test 3: backlog and callback delay remain separate metric families so ASYNC-03 stays visible at the metrics layer.
    - Test 4: focused metric tests prove Mailglass, Chimeway, and Rindle selectors bind to the shared metric families without widening labels or collapsing freshness and terminality semantics.
  </behavior>
  <action>Create `Parapet.Metrics.AsyncDelivery` as the shared Phase 5 metric catalog per D-26, D-31, D-33, and D-34. Consume only the normalized `Parapet.Telemetry.AsyncDelivery` event families from Phase 4, not raw provider telemetry. Use one coherent family per delivery or async concern, keep tag shapes aligned to the contract allowlists, and make raw depth or retry noise secondary signals rather than the primary operator surface per D-15 and D-19. The implementation should be data-driven enough that provider modules can filter by `integration`, `fault_plane`, `queue`, `channel`, or `pipeline_stage` without inventing new metric names. Add focused provider-facing metric tests for Mailglass, Chimeway, and Rindle so Wave 0 coverage proves the shared families are actually usable for the exact Phase 5 slice catalogs.</action>
  <verify>
    <automated>mix test test/parapet/metrics/async_delivery_test.exs test/parapet/metrics/mailglass_test.exs test/parapet/metrics/chimeway_test.exs test/parapet/metrics/rindle_test.exs</automated>
  </verify>
  <done>The repo has a shared low-cardinality metric surface for Phase 5, and its focused tests prove naming, event bindings, and family separation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introduce a bounded provider slice spec and resolution path</name>
  <files>lib/parapet/slo/slice_spec.ex, lib/parapet/slo/resolvable.ex, test/parapet/slo/resolvable_test.exs</files>
  <behavior>
    - Test 1: a slice spec validates required fields such as slice name, objective or diagnostic mode, metric selectors, alert class, and runbook metadata.
    - Test 2: the existing `%Parapet.SLO{}` and `Parapet.SLO.ScoriaEval` resolution paths still work unchanged.
    - Test 3: provider-owned slice specs resolve into a form the generator can consume without forcing raw arbitrary PromQL into provider modules.
  </behavior>
  <action>Add a bounded internal spec such as `Parapet.SLO.SliceSpec` per D-25, D-29, and D-30, and extend `Parapet.SLO.Resolvable` so provider modules can return either legacy `%Parapet.SLO{}` values or richer Phase 5 slice specs without breaking compatibility per D-40. Keep the spec explicit: include slice identity, integration, fault plane, category (`:paging_slo`, `:freshness`, `:diagnostic` or equivalent), operator grouping labels, and enough matcher data for the generator to build PromQL later. Do not add hidden registration, mutation, or auto-attach behavior; this is a resolution seam only per D-03, D-04, and D-23.</action>
  <verify>
    <automated>mix test test/parapet/slo/resolvable_test.exs</automated>
  </verify>
  <done>Provider modules have a bounded data model for Phase 5 built-ins, and the existing resolvable behavior still passes for legacy callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Normalized telemetry metadata -> Prometheus metric labels | Bounded public metadata becomes long-lived TSDB dimensions |
| Provider slice specs -> generated PromQL artifacts | Rich provider-owned config crosses into generated alert and recording rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | `Parapet.Metrics.AsyncDelivery` | mitigate | Bind tags to Phase 4 family allowlists and refuse exact identifiers or provider payload fragments as label dimensions. |
| T-05-01-02 | Tampering | `Parapet.SLO.SliceSpec` | mitigate | Validate the bounded slice fields explicitly so providers cannot smuggle unsupported alert metadata or raw unsafe selectors into the generator seam. |
</threat_model>

<verification>
- Run the focused metrics and resolvable tests.
- Confirm the foundation consumes Phase 4 normalized events only.
- Confirm no new auto-registration or auto-attach behavior was introduced.
</verification>

<success_criteria>
- Shared Phase 5 metric families exist and preserve low-cardinality discipline.
- Provider modules have one explicit slice-spec seam for generated built-ins.
- Legacy `%Parapet.SLO{}` resolution still works while the blessed path shifts to providers.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/5/05-01-SUMMARY.md`
</output>
