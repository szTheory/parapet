global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/rules/parapet.yml

scrape_configs:
  - job_name: parapet_demo
    metrics_path: /metrics
    static_configs:
      - targets:
          - web:4000
