---
phase: 23-foundations-telemetry-contract-lease-until-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/telemetry/recovery_action.ex
  - test/parapet/telemetry/recovery_action_test.exs
  - docs/telemetry.md
  - docs/stability.md
autonomous: true
requirements:
  - FND-02

must_haves:
  truths:
    - "An adopter reading docs/telemetry.md can see the full [:parapet, :operator, :recovery_action, ...] event family under the Experimental tier — every event name, measurement key, and metadata key is explicit and distinguishable from the v1.0 frozen Stable telemetry."
    - "mix verify.public_api classifies Parapet.Telemetry.RecoveryAction as :experimental via the @moduledoc admonition regex; the gate exits 0."
    - "Parapet.Telemetry.RecoveryAction.event_families/0 returns the exact frozen 8-element list of concrete event-name tuples; mutating that list breaks the contract test."
    - "Parapet.Telemetry.RecoveryAction.normalize_outcome/1 (and the other four vocab guards) raises ArgumentError on unknown atoms — closed-vocab freeze is enforced at the module boundary, not just in docs."
    - "Parapet.Telemetry.RecoveryAction.shape_metadata/2 strips private keys and builds a refs sub-map keyed by :incident_ref | :claim_ref | :step_ref | :preview_ref — Phase 26 emit-sites can call it to produce contract-conformant payloads."
    - "docs/stability.md lists Parapet.Telemetry.RecoveryAction in the Experimental Modules table — adopters scanning the stability surface see what is safe to depend on."
  artifacts:
    - path: "lib/parapet/telemetry/recovery_action.ex"
      provides: "Frozen contract module for the [:parapet, :operator, :recovery_action, ...] telemetry family — mirrors Parapet.Telemetry.AsyncDelivery 1:1 under Experimental tier"
      contains: "@event_families ["
      min_lines: 250
    - path: "test/parapet/telemetry/recovery_action_test.exs"
      provides: "Module-introspection contract test (no emit-site assertions per D-16) — asserts event family list, vocab guards, shape_metadata refs/key-stripping"
      contains: "Parapet.Telemetry.RecoveryAction"
      min_lines: 50
    - path: "docs/telemetry.md"
      provides: "New '## Recovery Action Family (Experimental)' section after Semantic Guarantees enumerating all 8 event names, measurement keys, metadata keys, closed vocabularies"
      contains: "## Recovery Action Family (Experimental)"
    - path: "docs/stability.md"
      provides: "New row in Experimental Modules table for Parapet.Telemetry.RecoveryAction"
      contains: "Parapet.Telemetry.RecoveryAction"
  key_links:
    - from: "lib/parapet/telemetry/recovery_action.ex"
      to: "lib/mix/tasks/verify.public_api.ex"
      via: "@moduledoc ExDoc admonition '> #### Experimental {: .warning}' matching regex ~r/####\\s+Experimental\\s*\\{:\\s*\\.warning\\}/"
      pattern: "#### Experimental \\{: \\.warning\\}"
    - from: "test/parapet/telemetry/recovery_action_test.exs"
      to: "lib/parapet/telemetry/recovery_action.ex"
      via: "alias Parapet.Telemetry.RecoveryAction + RecoveryAction.event_families/0 / normalize_*/1 / shape_metadata/2 introspection"
      pattern: "RecoveryAction\\.(event_families|normalize_|shape_metadata)"
    - from: "docs/telemetry.md"
      to: "lib/parapet/telemetry/recovery_action.ex"
      via: "section enumerates the same 8 events + measurement keys + metadata keys + vocab atoms the module exposes"
      pattern: "\\[:parapet, :operator, :recovery_action"
---

<objective>
Lock the v1.1 telemetry event family `[:parapet, :operator, :recovery_action, ...]` under the **Experimental** stability tier — irreversible-on-publish under the v1.0 freeze. Ship the `Parapet.Telemetry.RecoveryAction` contract module (mirroring `Parapet.Telemetry.AsyncDelivery` 1:1), the module-introspection contract test that guards the freeze, and the documentation sections that name the events for future capability authors and operator-UI emit-sites.

Purpose: a future capability addition (Phase 24/25/26) can wire a new emit-site to a documented telemetry event without inventing a new event name — the contract is machine-readable (`event_families/0`) and human-readable (`docs/telemetry.md`).
Output: new module + test + two doc updates. Pure additive on the v1.0 frozen surface. Zero new dependencies, zero `mix.lock` churn.

Honors FND-02 + D-09 through D-16 from CONTEXT.md. Resolves OQ-1 (return 8 concrete event names from `event_families/0` plus companion `span_families/0`). Incorporates Finding #5 (`duration_ms` + `duration_native` is the project's CONVENTION — converted in handlers from the raw `:telemetry.span/3` `duration` key — the contract module documents the *post-conversion* public surface).

Lands together with `23-02-PLAN.md` (FND-01 migration + ClaimService changes) in a **single coherent PR** per D-17. Both plans are Wave 1 with zero `files_modified` overlap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-foundations-telemetry-contract-lease-until-migration/23-CONTEXT.md
@.planning/phases/23-foundations-telemetry-contract-lease-until-migration/23-RESEARCH.md
@.planning/phases/23-foundations-telemetry-contract-lease-until-migration/23-PATTERNS.md
@.planning/phases/23-foundations-telemetry-contract-lease-until-migration/23-VALIDATION.md

@lib/parapet/telemetry/async_delivery.ex
@test/parapet/telemetry/async_delivery_test.exs
@docs/telemetry.md
@docs/stability.md
@lib/mix/tasks/verify.public_api.ex

<interfaces>
<!-- Key contracts the executor must mirror — extracted from canonical analog files. -->
<!-- The new module mirrors AsyncDelivery 1:1 (D-09); the new test mirrors async_delivery_test.exs (D-15). -->

Public surface to implement on `Parapet.Telemetry.RecoveryAction`
(mirror AsyncDelivery shape; signatures per PATTERNS.md analog):

  - `event_families/0` returns the 8-element list of concrete event name tuples (D-10 + OQ-1 resolution)
  - `span_families/0` returns `[[:parapet, :operator, :recovery_action, :executed]]` — companion to event_families for span-vs-single-shot dispatch in Phase 26
  - `allowed_public_keys/1` accepts either the full event tuple (list) or the last atom of a single-shot event / the span base atom; returns the per-family allowlist (mirrors AsyncDelivery `allowed_public_keys/1`)
  - `shape_metadata/2` strips private keys, normalizes known vocab atoms, and builds the `:refs` sub-map (mirrors AsyncDelivery `shape_metadata/2`)
  - `normalize_outcome/1` — accepts atoms/strings from `@outcomes` vocab; raises ArgumentError otherwise
  - `normalize_short_circuit_reason/1` — accepts atoms/strings from `@short_circuit_reasons` vocab; raises ArgumentError otherwise
  - `normalize_failure_class/1` — accepts atoms/strings from `@failure_classes` vocab; raises ArgumentError otherwise
  - `normalize_actor_kind/1` — accepts atoms/strings from `@actor_kinds` vocab; raises ArgumentError otherwise
  - `normalize_action_kind/1` — accepts atoms/strings from `@action_kinds` vocab; raises ArgumentError otherwise

Closed vocabularies (per D-12 — all atom values are FROZEN; adding new atoms is a minor-version additive change under Experimental tier):

  - @event_families: 8 concrete tuples (5 single-shot + 3 span sub-events; see Task 1 action for the literal list)
  - @outcomes atoms: :previewed, :confirmed, :short_circuited, :conflicted, :succeeded, :failed
  - @short_circuit_reasons atoms: :incident_resolved, :breaker_open, :preview_expired, :target_refs_drift
  - @failure_classes atoms: :precondition_failed, :provider_unavailable, :partial_failure, :internal_error
  - @actor_kinds atoms: :human, :system
  - @action_kinds atoms (string vocab keyed by atom): :operator -> "operator", :automation -> "automation", :escalation -> "escalation"
  - @allowed_ref_keys: :incident_ref, :claim_ref, :step_ref, :preview_ref
  - @known_ref_mappings: incident_id -> :incident_ref, claim_id -> :claim_ref, step_id -> :step_ref, preview_id -> :preview_ref

Per-family allowed public metadata keys (D-12, applied via `allowed_public_keys/1`):

  - For every event family the allowed public keys are the closed-vocab keys that meaningfully appear at that emit-site:
      :previewed       -> [:capability_id, :action_kind, :outcome, :actor_kind]
      :preview_failed  -> [:capability_id, :action_kind, :outcome, :failure_class, :actor_kind]
      :confirmed       -> [:capability_id, :action_kind, :outcome, :actor_kind]
      :short_circuited -> [:capability_id, :action_kind, :outcome, :short_circuit_reason, :actor_kind]
      :conflicted      -> [:capability_id, :action_kind, :outcome, :actor_kind]
      :executed        -> [:capability_id, :action_kind, :outcome, :failure_class, :actor_kind]  (applied to all three span sub-events)

Detection regex `verify.public_api` uses (per `lib/mix/tasks/verify.public_api.ex:110`):

  ~r/####\s+Experimental\s*\{:\s*\.warning\}/

The `@moduledoc` MUST contain exactly `> #### Experimental {: .warning}` (four hashes, the `{: .warning}` directive on the same line). NO `@stability` attribute — admonition is the single source of truth (per Phase 19 D-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship Parapet.Telemetry.RecoveryAction contract module (mirror AsyncDelivery 1:1; Experimental tier)</name>
  <files>lib/parapet/telemetry/recovery_action.ex</files>
  <read_first>
    - lib/parapet/telemetry/async_delivery.ex (the 1:1 template per D-09; ~330 lines; copy structure, substitute family-specific constants)
    - lib/parapet/automation/claim_service.ex lines 1-15 (canonical Experimental admonition shape that verify.public_api regex-detects)
    - lib/mix/tasks/verify.public_api.ex around line 110 (the admonition detection regex `~r/####\s+Experimental\s*\{:\s*\.warning\}/`)
    - .planning/phases/23-foundations-telemetry-contract-lease-until-migration/23-PATTERNS.md (section "lib/parapet/telemetry/recovery_action.ex" — full structural mapping)
    - .planning/phases/23-foundations-telemetry-contract-lease-until-migration/23-CONTEXT.md (D-09 through D-14 — the locked decisions this task implements)
  </read_first>
  <behavior>
    - `RecoveryAction.event_families/0` returns the 8-element list (in declaration order): `[:parapet, :operator, :recovery_action, :previewed]`, `:preview_failed`, `:confirmed`, `:short_circuited`, `:conflicted`, `[:parapet, :operator, :recovery_action, :executed, :start]`, `:executed, :stop]`, `:executed, :exception]`.
    - `RecoveryAction.span_families/0` returns `[[:parapet, :operator, :recovery_action, :executed]]`.
    - `RecoveryAction.allowed_public_keys/1` accepts a full event tuple (list) or a family atom; returns the per-family allowed public metadata key list per the `<interfaces>` map.
    - `RecoveryAction.shape_metadata/2` accepts a family atom + a metadata map; returns a map whose top-level keys are a subset of `allowed_public_keys(family)`, has known refs lifted into a `:refs` sub-map keyed by the four `:incident_ref|:claim_ref|:step_ref|:preview_ref` atoms (when source keys `incident_id|claim_id|step_id|preview_id` are present), and strips every other private key. If no refs present, no `:refs` key is added.
    - `RecoveryAction.normalize_outcome/1` returns the atom on `:previewed | :confirmed | :short_circuited | :conflicted | :succeeded | :failed` and raises `ArgumentError` (message containing `"Unsupported outcome"`) on any other input. Strings are normalized via `String.trim |> String.to_atom`.
    - `RecoveryAction.normalize_short_circuit_reason/1`, `normalize_failure_class/1`, `normalize_actor_kind/1`, `normalize_action_kind/1` behave identically against their respective closed-vocab maps; each raises `ArgumentError` with `"Unsupported <label>"` on unknown input.
    - Compile-time gate: `mix verify.public_api` classifies the module as `:experimental` (the `@moduledoc` admonition regex matches).
  </behavior>
  <action>
Create `lib/parapet/telemetry/recovery_action.ex` mirroring `lib/parapet/telemetry/async_delivery.ex` 1:1 in structure. Per D-09 (mirror AsyncDelivery), D-10 (8 concrete event tuples — OQ-1 resolution), D-11 (measurement keys), D-12 (closed metadata vocabularies).

Module shape:

1. `defmodule Parapet.Telemetry.RecoveryAction do` with `@moduledoc` containing:
   - One-sentence description: "Public contract helpers for Parapet's recovery action telemetry family."
   - The Experimental admonition exactly: `> #### Experimental {: .warning}` on a heading line, followed by a blockquoted paragraph explaining the surface is experimental in v1.x, may change in a minor release with single-version CHANGELOG notice, with a link to `stability.html`. Mirror the wording from `lib/parapet/automation/claim_service.ex:5-10`.
   - A paragraph documenting Finding #5: "`duration_ms` and `duration_native` are the project's CONVENTION for span events — handlers convert the raw `:telemetry.span/3` `duration` (native units) into both `duration_ms` (integer) and `duration_native` (integer) before re-emitting. Phase 26 emit-sites will call `:telemetry.span/3` directly; the project's instrumenter converts before downstream subscribers see the payload. Adopters subscribing to these events should expect `duration_ms` + `duration_native` on `:stop`/`:exception`, and `system_time` (integer) on `:start`."

2. `@event_families` module attribute — the 8-element list literally enumerated in the order: `:previewed`, `:preview_failed`, `:confirmed`, `:short_circuited`, `:conflicted`, then the three `:executed` span sub-events `:start`, `:stop`, `:exception`. (Per OQ-1, full enumeration of concrete tuples — the "seven logical families" framing is documented in the docstring but the list has 8 entries.)

3. Companion `@span_families` module attribute: `[[:parapet, :operator, :recovery_action, :executed]]`.

4. Closed-vocab `@outcomes`, `@short_circuit_reasons`, `@failure_classes`, `@actor_kinds`, `@action_kinds` maps — atom keys mapped to themselves (string keys also accepted via `normalize_key/1`). Atom values per the `<interfaces>` block above.

5. `@allowed_ref_keys [:incident_ref, :claim_ref, :step_ref, :preview_ref]`.

6. `@known_ref_mappings %{incident_id: :incident_ref, claim_id: :claim_ref, step_id: :step_ref, preview_id: :preview_ref}`.

7. Per-family allowed-keys map `@recovery_action_family_keys` — keys are the last atom of single-shot events plus `:executed` (the span base); values are the allowed public metadata key lists from the `<interfaces>` block above. Build `@allowed_public_keys` from this single map (no merge needed — there's only one family scope here, unlike AsyncDelivery which merges delivery + async).

8. Public functions (each with `@doc since: "1.1.0"`):
   - `event_families/0` returning `@event_families`
   - `span_families/0` returning `@span_families`
   - `allowed_public_keys/1` — two clauses: `when is_list(family)` dispatches to the family-atom clause via `family_key/1`; `when is_atom(family)` does `Map.fetch!(@allowed_public_keys, family)`. The `family_key/1` private helper returns the last atom of a single-shot event tuple OR `:executed` for any span sub-event tuple (i.e. `[:parapet, :operator, :recovery_action, :executed, :start]` -> `:executed`).
   - `shape_metadata/2` — mirrors AsyncDelivery: takes the public keys from `allowed_public_keys(family)`, applies `maybe_normalize_known_values/1` (which calls the appropriate `normalize_*` on `:outcome`, `:short_circuit_reason`, `:failure_class`, `:actor_kind`, `:action_kind` when present), extracts known refs into a `:refs` sub-map (via `extract_known_refs/1`), merges any explicit `:refs` in the metadata via `merge_explicit_refs/2` (filtered against `@allowed_ref_keys`), and adds `:refs` to the output only when non-empty.
   - `normalize_outcome/1`, `normalize_short_circuit_reason/1`, `normalize_failure_class/1`, `normalize_actor_kind/1`, `normalize_action_kind/1` — each dispatches to the shared `normalize_enum/3` private helper with the appropriate vocab map and label string (e.g. `"outcome"`, `"short_circuit_reason"`).

9. Private helpers — copy verbatim from `async_delivery.ex`:
   - `normalize_enum/3` and `normalize_key/1` (PATTERNS.md "Shared Patterns" section)
   - `extract_known_refs/1`, `merge_explicit_refs/2`, `normalize_ref_key/1`, `maybe_put/3` (PATTERNS.md "extract_known_refs..." shared pattern)
   - `maybe_normalize_known_values/1` — applies the five `normalize_*` functions selectively when their keys are present in the public-metadata map.
   - `family_key/1` — last-atom-of-tuple-or-:executed-for-span helper.

DO NOT:
- Add any `@stability :experimental` module attribute — the admonition is the single source of truth (Phase 19 D-02, restated in CONTEXT D-09).
- Use `###` or `#####` for the admonition heading — `verify.public_api` regex requires exactly `####`.
- Add measurement keys to the contract module data (`duration_ms` etc. are documented in `@moduledoc` text and in `docs/telemetry.md`, not in a module attribute — match AsyncDelivery which has no measurements attribute).
- Emit any `:telemetry.execute` calls or attach handlers in this module — D-16 forbids emit-site code in Phase 23.
- Touch `lib/parapet/telemetry/async_delivery.ex` — Stable, frozen.

Use `@doc since: "1.1.0"` on every public function (this is the v1.1 ship version; AsyncDelivery uses `"1.0.0"` because it shipped in v1.0).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | grep -i recovery_action | grep -v "^$"; test $? -ne 0 || echo "FAIL: compile warnings on RecoveryAction"; mix verify.public_api 2>&1 | grep -q "Parapet.Telemetry.RecoveryAction" && mix verify.public_api 2>&1 | grep -qE "(experimental|Experimental).*RecoveryAction|RecoveryAction.*(experimental|Experimental)" && echo "OK: classified Experimental"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -E '@moduledoc' lib/parapet/telemetry/recovery_action.ex` exits 0
    - Source assertion: `grep -F "> #### Experimental {: .warning}" lib/parapet/telemetry/recovery_action.ex` exits 0
    - Source assertion: `grep -E '\[:parapet, :operator, :recovery_action, :executed, :start\]' lib/parapet/telemetry/recovery_action.ex` exits 0
    - Source assertion: `grep -E '\[:parapet, :operator, :recovery_action, :executed, :stop\]' lib/parapet/telemetry/recovery_action.ex` exits 0
    - Source assertion: `grep -E '\[:parapet, :operator, :recovery_action, :executed, :exception\]' lib/parapet/telemetry/recovery_action.ex` exits 0
    - Source assertion: `grep -E 'def span_families' lib/parapet/telemetry/recovery_action.ex` exits 0
    - Source assertion: `grep -E 'def shape_metadata' lib/parapet/telemetry/recovery_action.ex` exits 0
    - Source assertion: `grep -F '@doc since: "1.1.0"' lib/parapet/telemetry/recovery_action.ex` exits 0
    - Behavior (via iex/oneshot): `mix run -e 'IO.inspect(length(Parapet.Telemetry.RecoveryAction.event_families()))'` prints `8`
    - Behavior: `mix run -e 'IO.inspect(Parapet.Telemetry.RecoveryAction.normalize_outcome(:succeeded))'` prints `:succeeded`
    - Behavior: `mix run -e 'try do Parapet.Telemetry.RecoveryAction.normalize_outcome(:bogus); rescue ArgumentError -> IO.puts("raised") end'` prints `raised`
    - CLI: `mix verify.public_api` exits 0 AND classifies `Parapet.Telemetry.RecoveryAction` as `:experimental` (admonition regex matches)
    - CLI: `mix compile --warnings-as-errors` exits 0 (no Dialyzer/compile warnings from the new module)
  </acceptance_criteria>
  <done>
    `lib/parapet/telemetry/recovery_action.ex` is committed; `mix verify.public_api` and `mix compile --warnings-as-errors` both exit 0; introspection sanity checks above all pass. Module is ready for Task 2's contract test to assert against.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship the contract test + update docs/telemetry.md + docs/stability.md</name>
  <files>test/parapet/telemetry/recovery_action_test.exs, docs/telemetry.md, docs/stability.md</files>
  <read_first>
    - test/parapet/telemetry/async_delivery_test.exs (the 1:1 template per D-15; ~61 lines)
    - lib/parapet/telemetry/recovery_action.ex (module being asserted against — read after Task 1 lands)
    - docs/telemetry.md (file-level Stable header at lines 1-10 is NOT to be touched; Semantic Guarantees ends ~line 137; new section inserts immediately after)
    - docs/stability.md (Experimental Modules table around line 49; the new row inserts between `Parapet.MCP.PrometheusClient` and `Parapet.Automation.CircuitBreaker` per CONTEXT D-14)
    - .planning/phases/23-foundations-telemetry-contract-lease-until-migration/23-PATTERNS.md (sections for `test/parapet/telemetry/recovery_action_test.exs`, `docs/telemetry.md`, `docs/stability.md`)
    - .planning/phases/23-foundations-telemetry-contract-lease-until-migration/23-CONTEXT.md (D-13, D-14, D-15, D-16 — the locked decisions for the test + docs surfaces)
  </read_first>
  <behavior>
    - `mix test test/parapet/telemetry/recovery_action_test.exs` exits 0.
    - The test file contains exactly four `test "..." do ... end` blocks (mirror async_delivery_test.exs's four-test scaffold) — NO `:telemetry.attach`, NO `Process.sleep`, NO event-capture (D-16: module introspection only).
    - First test asserts `RecoveryAction.event_families()` is `==` (ordered equality, not membership) the literal 8-tuple list in declaration order. Mutating the list in `recovery_action.ex` breaks this test.
    - Second test exercises each of the five `normalize_*` functions — happy-path on one valid atom each, and `assert_raise ArgumentError, ~r/Unsupported <label>/, fn -> ... end` on one invalid atom each.
    - Third test exercises `shape_metadata/2` with a metadata map containing a public key (e.g. `:capability_id`), a mapped ref source key (e.g. `:incident_id`), an explicit `:refs` sub-map carrying one valid ref + one invalid (filtered) ref, and a private key (e.g. `:internal_debug`). Asserts: public key retained; `:refs.incident_ref` populated; invalid ref filtered out; private key absent in output.
    - Fourth test asserts span family membership: `assert [:parapet, :operator, :recovery_action, :executed] in RecoveryAction.span_families()`, and that `allowed_public_keys([:parapet, :operator, :recovery_action, :executed, :stop])` equals the documented `:executed` family allowlist.
    - `grep -F "## Recovery Action Family (Experimental)" docs/telemetry.md` exits 0.
    - `grep -F "Parapet.Telemetry.RecoveryAction" docs/stability.md` exits 0 (new row added).
    - The file-level Stable header at `docs/telemetry.md:3-10` is byte-identical before/after (only INSERTIONS allowed after Semantic Guarantees).
  </behavior>
  <action>
**(a) `test/parapet/telemetry/recovery_action_test.exs`** — create new file mirroring `test/parapet/telemetry/async_delivery_test.exs` 1:1 in shape (per D-15, D-16). Use `use ExUnit.Case, async: true` (pure module introspection, no DB, no process state). Alias `Parapet.Telemetry.RecoveryAction`. Four tests:

1. `test "exposes the eight frozen recovery action event families"` — asserts `RecoveryAction.event_families() == [...8-element literal list in declaration order...]` (ordered equality). The list literal is identical to `@event_families` in `recovery_action.ex`.

2. `test "normalizes bounded vocabulary atoms only"` — one happy-path + one `assert_raise` per `normalize_*` function. Example: `assert RecoveryAction.normalize_outcome(:succeeded) == :succeeded`; `assert_raise ArgumentError, ~r/Unsupported outcome/, fn -> RecoveryAction.normalize_outcome(:bogus_outcome) end`. Repeat for the four other `normalize_*` functions, each with a label-specific regex (e.g. `~r/Unsupported short_circuit_reason/`).

3. `test "shapes metadata into public keys, builds refs sub-map, and strips private keys"` — call `RecoveryAction.shape_metadata(:previewed, %{capability_id: :retry_async_item, action_kind: :operator, outcome: :previewed, incident_id: "inc-123", refs: %{step_ref: "step-9", bogus_ref: "filtered"}, internal_debug: "ignored"})`. Assert: `shaped.capability_id == :retry_async_item`; `shaped.outcome == :previewed`; `shaped.refs == %{incident_ref: "inc-123", step_ref: "step-9"}` (note: `bogus_ref` filtered out by `@allowed_ref_keys`); `refute Map.has_key?(shaped, :internal_debug)`.

4. `test "exposes the executed span family"` — `assert [:parapet, :operator, :recovery_action, :executed] in RecoveryAction.span_families()`; assert `allowed_public_keys([:parapet, :operator, :recovery_action, :executed, :stop]) == allowed_public_keys(:executed)` (sub-event dispatches to the span base family's allowed key list).

NO emit-site assertions. NO `:telemetry.attach`. NO `Process.sleep`. NO `try/catch` around event capture. This is module introspection only, per D-16.

**(b) `docs/telemetry.md`** — append a new section AFTER `## Semantic Guarantees` (around line 137). Do NOT modify lines 1-10 (the file-level Stable header). Section heading exactly: `## Recovery Action Family (Experimental)`. Per D-13.

Section structure (mirror the existing "Async And Delivery Families" section structure):

  1. Opening admonition (callout) — exact shape:
     ```
     > #### Experimental {: .warning}
     >
     > This event family is **experimental** in v1.x. Event names, measurement keys, and
     > metadata keys may change in a minor release with a single CHANGELOG entry. See
     > [Stability & Deprecation Policy](stability.html) for details.
     ```

  2. One subsection per event family (7 subsections, since the executed triplet is one logical family with three sub-events per D-10's "7 event tuples" framing). For each: `#### \`[:parapet, :operator, :recovery_action, :<atom>]\``, a one-line description of when it fires, a `**Measurements:**` bullet list, and a `**Metadata:**` bullet list.

     The seven subsections:
       - `:previewed` — emitted when an operator opens a Preview panel; measurements `count: 1`; metadata `capability_id`, `action_kind`, `outcome: :previewed`, `actor_kind`.
       - `:preview_failed` — emitted when Preview render itself errors; same measurements; metadata adds `failure_class`.
       - `:confirmed` — emitted when the operator clicks Confirm (BEFORE claim acquisition); measurements `count: 1`; metadata same as `:previewed` but `outcome: :confirmed`.
       - `:short_circuited` — emitted when Confirm is rejected by a gate (preview expired, breaker open, target_refs drift, incident resolved); measurements `count: 1`; metadata adds `short_circuit_reason`.
       - `:conflicted` — emitted when ClaimService returns `{:conflicted, claim_id}`; measurements `count: 1`; metadata as `:confirmed` but `outcome: :conflicted`.
       - `[:parapet, :operator, :recovery_action, :executed]` (span family) — emitted as a `:telemetry.span/3` triplet covering capability `execute/2`. List the three sub-event tuples explicitly with their measurement contracts: `:start` carries `system_time` (integer); `:stop` and `:exception` carry `duration_ms` (integer) and `duration_native` (integer). Include Finding #5's documentation prose: "`duration_ms` and `duration_native` are the project's measurement convention — the project instrumenter converts the raw `:telemetry.span/3` `duration` (native units) into both keys before downstream subscribers see the payload."

  3. A final subsection `### Closed Vocabularies` enumerating the atom values for `outcome`, `short_circuit_reason`, `failure_class`, `actor_kind`, `action_kind`, and the four `:refs` keys (`incident_ref`, `claim_ref`, `step_ref`, `preview_ref`). Cite `docs/stability.md` policy: "Closed-vocab atoms are part of the contract — adding new atoms is additive (minor); removing or renaming is breaking (major)."

**(c) `docs/stability.md`** — insert a new row in the Experimental Modules table between `Parapet.MCP.PrometheusClient` and `Parapet.Automation.CircuitBreaker` (per D-14, around line 49). Row format mirrors the existing Stable row for `Parapet.Telemetry.AsyncDelivery` (line ~38):

  `| \`Parapet.Telemetry.RecoveryAction\` | Machine-readable recovery action telemetry contract |`

DO NOT touch the Stable Modules table or the file's header sections. DO NOT add `@stability` attribute references — the gate is the admonition in the module.
  </action>
  <verify>
    <automated>mix test test/parapet/telemetry/recovery_action_test.exs --color 2>&1 | tail -20; grep -F "## Recovery Action Family (Experimental)" docs/telemetry.md && grep -F "Parapet.Telemetry.RecoveryAction" docs/stability.md && echo "OK: doc anchors present"</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `mix test test/parapet/telemetry/recovery_action_test.exs` exits 0 with 4 tests, 0 failures
    - Source assertion: `grep -c '^  test "' test/parapet/telemetry/recovery_action_test.exs` returns `4`
    - Source assertion (anti-emit-site, D-16): `grep -E ':telemetry\.attach|Process\.sleep|:telemetry\.execute' test/parapet/telemetry/recovery_action_test.exs` exits 1 (no matches)
    - Source assertion: `grep -F 'use ExUnit.Case, async: true' test/parapet/telemetry/recovery_action_test.exs` exits 0
    - Doc grep: `grep -F "## Recovery Action Family (Experimental)" docs/telemetry.md` exits 0
    - Doc grep: `grep -F "Parapet.Telemetry.RecoveryAction" docs/stability.md` exits 0
    - Doc grep: `grep -F "duration_ms" docs/telemetry.md | grep -F "Recovery Action"` (post-insertion line near Recovery Action section) — visual confirm via head/tail OR grep -A 100 anchored on the section heading
    - Doc grep (Stable header preserved): `grep -n "Stable Contract" docs/telemetry.md | head -1` returns a match at a line <= 10
    - Doc grep (closed-vocab enumerated): `grep -E ':incident_resolved|:breaker_open|:preview_expired|:target_refs_drift' docs/telemetry.md` exits 0
    - CLI: `mix verify.public_api` exits 0 (the new stability.md row is consistent with the module admonition)
    - CLI: `mix test` (full suite) exits 0 — no regressions
  </acceptance_criteria>
  <done>
    Contract test passes; `docs/telemetry.md` contains the new Experimental section with all 7 logical event families enumerated; `docs/stability.md` lists `Parapet.Telemetry.RecoveryAction` in the Experimental Modules table; full test suite green; `mix verify.public_api` green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Phase 23 Plan 01 is an internal contract module + module-introspection test + two documentation surfaces. No new user-facing inputs, no authentication, no cryptography, no network boundary. The `normalize_*` functions raise `ArgumentError` on unknown atoms — closed-enum guard at the module boundary (V5 input validation per ASVS, applied to *internal* callers in Phase 26+). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-NA | — | Plan 01 | accept | **N/A — internal contract module + module-introspection test + documentation updates; no new attack surface.** No new dependencies (zero packages installed; no slopcheck gate applies). Closed-vocab guards (`normalize_*`) raise ArgumentError on unknown atoms but the callers in Phase 23 are tests and Phase 26+ internal emit-sites — no untrusted-input plane crosses this module. See `.planning/phases/23-foundations-telemetry-contract-lease-until-migration/23-RESEARCH.md` "Security Domain". Package legitimacy gate not applicable — zero installs. |
</threat_model>

<verification>
- `mix test test/parapet/telemetry/recovery_action_test.exs` exits 0 (4 tests, 0 failures)
- `mix test` (full suite) exits 0 — no regressions
- `mix verify.public_api` exits 0 AND classifies `Parapet.Telemetry.RecoveryAction` as `:experimental`
- `mix compile --warnings-as-errors` exits 0
- `mix credo --strict` exits 0 for the new module
- `mix dialyzer` exits 0 with no new warnings introduced by the new module
- `grep -F "## Recovery Action Family (Experimental)" docs/telemetry.md` exits 0
- `grep -F "Parapet.Telemetry.RecoveryAction" docs/stability.md` exits 0
- The file-level Stable header at `docs/telemetry.md:1-10` is byte-identical before/after (no edits)
</verification>

<success_criteria>
- New module `Parapet.Telemetry.RecoveryAction` ships with frozen `@event_families` list of 8 concrete tuples (5 single-shot + 3 span sub-events), companion `span_families/0`, `allowed_public_keys/1`, `shape_metadata/2`, and five `normalize_*` vocab guards — mirror of `AsyncDelivery` 1:1.
- Contract test (`test/parapet/telemetry/recovery_action_test.exs`) is module-introspection only (no emit-site assertions per D-16) and guards the frozen surface against drift.
- `docs/telemetry.md` has the new "## Recovery Action Family (Experimental)" section after Semantic Guarantees, enumerating every event, measurement key, metadata key, and closed-vocab atom set.
- `docs/stability.md` lists `Parapet.Telemetry.RecoveryAction` in the Experimental Modules table.
- `mix verify.public_api` classifies the new module as `:experimental`.
- A future Phase 26 emit-site can call `RecoveryAction.shape_metadata(:previewed, %{...})` to produce a contract-conformant payload without inventing keys or atoms.
- Zero changes to `lib/parapet/telemetry/async_delivery.ex` (Stable, frozen).
- Zero changes to `Parapet.Operator.confirm_runbook_step/4` (D-18 — Phase 25 territory).
- Zero new dependencies; `mix.lock` unchanged.
</success_criteria>

<output>
Create `.planning/phases/23-foundations-telemetry-contract-lease-until-migration/23-01-SUMMARY.md` when done. Summary documents: which 8 concrete event tuples landed (verbatim), which 6 outcome / 4 short_circuit_reason / 4 failure_class atoms ship, the Experimental admonition shape used in `@moduledoc`, and confirmation that `mix verify.public_api` classified the module correctly. Note in the summary that this plan lands together with `23-02-SUMMARY.md` in one PR per D-17.
</output>
