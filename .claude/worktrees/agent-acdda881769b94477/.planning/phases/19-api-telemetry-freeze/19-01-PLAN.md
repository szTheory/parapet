---
phase: 19-api-telemetry-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/verify.public_api.ex
  - test/mix/tasks/verify.public_api_test.exs
  - mix.exs
  - docs/stability.md
  - docs/telemetry.md
autonomous: true
requirements: [STAB-02, STAB-03, STAB-04]
must_haves:
  truths:
    - "mix verify.public_api invokes the real Mix.Tasks.Verify.PublicApi task (not mix docs)"
    - "mix verify.public_api exits non-zero when any public module has no stability-tier callout"
    - "docs/stability.md exists, is registered in extras:, and enumerates every public module's tier"
    - "docs/telemetry.md carries a Stable Contract callout cross-linking stability.md"
  artifacts:
    - path: "lib/mix/tasks/verify.public_api.ex"
      provides: "Tier detection + non-zero exit on :unclassified"
      contains: "detect_tier_from_text"
    - path: "docs/stability.md"
      provides: "Written stability + deprecation policy with full per-module tier enumeration"
      contains: "## Deprecation Cycle"
    - path: "mix.exs"
      provides: "Alias shadow removed; docs/stability.md registered in extras:"
      contains: "docs/stability.md"
    - path: "docs/telemetry.md"
      provides: "Stability-freeze header (frozen names, additive-only, no :event_prefix)"
      contains: "Stable Contract"
  key_links:
    - from: "mix.exs aliases"
      to: "Mix.Tasks.Verify.PublicApi.run/1"
      via: "alias deletion (no shadow)"
      pattern: "verify\\.public_api"
    - from: "docs/stability.md"
      to: "docs/telemetry.md"
      via: "ExDoc relative link"
      pattern: "telemetry\\.html"
---

<objective>
Make the STAB-04 enforcement gate live and ship the written stability policy that every
downstream artifact references. This is the SPINE of Phase 19: the gate must exist and be
wired at the CLI BEFORE the module annotations (Wave 2) can be verified by it.

Three deliverables:
1. Harden `Mix.Tasks.Verify.PublicApi` to detect the tier from the ExDoc callout and
   `System.halt(1)` on any `:unclassified` module (D-02, D-03), and DELETE the `mix.exs`
   alias that shadows the task today (D-04 — without this the gate is dead code at the CLI).
2. Create `docs/stability.md` (exact filename, D-09) with the full per-module tier table,
   semver promise, breaking-vs-additive rules, and the deprecation cycle (D-10); register
   it in `mix.exs` `extras:`.
3. Add the stability-freeze header to `docs/telemetry.md` (D-08).

Purpose: A live gate + a written policy are the foundation the entire freeze rests on.
The policy doc carries the COMPLETE tier enumeration so Wave 2 plans only touch `lib/`
moduledocs (no contention on this file).

Output: A functional `mix verify.public_api` gate, `docs/stability.md`, an updated
`docs/telemetry.md`, and an updated `mix.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-api-telemetry-freeze/19-CONTEXT.md
@.planning/phases/19-api-telemetry-freeze/19-RESEARCH.md
@.planning/phases/19-api-telemetry-freeze/19-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor needs no exploration for these contracts. -->

Current `lib/mix/tasks/verify.public_api.ex` (full file is the analog — extend in place):
- `run/1` calls `Mix.Task.run("compile")`, `Application.load(:parapet)`,
  `:application.get_key(:parapet, :modules)`, then filters with `public_api_module?/1`,
  maps with `check_module_docs/1`, prints a JSON/inspect manifest, and `System.halt(1)`
  if any `not m.has_docs` (the existing halt block is the structural template, ~lines 33-36).
- `public_api_module?/1` already excludes `Parapet.Internal.`, `Parapet.TestSupport.`,
  and `.Resolvable.` and keeps `Parapet.*`/`Parapet`. DO NOT change this filter.
- `check_module_docs/1` matches `Code.fetch_docs/1` results: `:hidden -> false`,
  `:none -> false`, `%{} -> true`, `{:error,_} -> false`.

Current `mix.exs`:
- `defp aliases do [ "verify.public_api": ["docs --warnings-as-errors"] ] end`
  (this is the ONLY alias; deleting it leaves `[]`).
- `defp docs` has `extras:` listing README.md, CHANGELOG.md, docs/HISTORY.md, then
  docs/adopter-flows.md, docs/operator-ui.md, docs/slo-reference.md, docs/telemetry.md,
  docs/getting-started.md, docs/troubleshooting.md, docs/slo-authoring-guide.md, and the
  four docs/integrations/*.md files.
- `files:` whitelist already includes `docs` wholesale — NO whitelist change needed.

Current `docs/telemetry.md` opening: `# Telemetry Event Schema` (line 1), blank line,
then a prose paragraph beginning "Parapet emits telemetry as a public contract."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the verify.public_api gate and delete the shadow alias</name>
  <files>lib/mix/tasks/verify.public_api.ex, test/mix/tasks/verify.public_api_test.exs, mix.exs</files>
  <read_first>
    - lib/mix/tasks/verify.public_api.ex (the file being extended — full current source)
    - test/mix/tasks/verify.public_api_test.exs (existing test to extend)
    - mix.exs (aliases + docs sections)
    - .planning/phases/19-api-telemetry-freeze/19-RESEARCH.md (STAB-04 section, lines 139-277 — exact detect_tier + run/1 patterns)
    - .planning/phases/19-api-telemetry-freeze/19-PATTERNS.md (verify.public_api.ex pattern assignment + System.halt block)
  </read_first>
  <action>
    Extend `Mix.Tasks.Verify.PublicApi` in place (do NOT rewrite):
    (1) Rename `check_module_docs/1` to `check_module/1` and have it return a map with
    `module`, `has_docs`, AND a new `tier` field. Extract the moduledoc text from the same
    `Code.fetch_docs/1` call by adding a `{:docs_v1, _, _, _, %{"en" => text}, _, _}` clause
    that calls a new private `detect_tier_from_text/1`; keep the `:hidden`/`:none`/`{:error,_}`
    clauses returning tier `:internal`/`:unclassified`/`:unclassified` respectively.
    (2) Add private `detect_tier_from_text/1`: returns `:stable` when the text contains BOTH
    `"{: .info}"` AND `"Stable"`; returns `:experimental` when it contains BOTH `"{: .warning}"`
    AND `"Experimental"`; otherwise `:unclassified`. (Both substrings required per Pitfall 2 —
    a `.warning` callout without "Experimental" must NOT match.)
    (3) In `run/1`, after the existing missing-docs `System.halt(1)` block, add a SECOND guard:
    compute `unclassified = Enum.filter(manifest, & &1.tier == :unclassified)` and if non-empty,
    `IO.puts(:stderr, ...)` an error naming the fix ("Add '> #### Stable {: .info}' or
    '> #### Experimental {: .warning}' to each @moduledoc"), list the offending modules, then
    `System.halt(1)`. Mirror the existing stderr+halt block structure exactly.
    Then in `mix.exs`, DELETE the `"verify.public_api": ["docs --warnings-as-errors"]` alias so
    `defp aliases do [] end` — per D-04 this stops `mix verify.public_api` from shadow-running
    `mix docs` and makes it resolve to the real task module.
    Finally extend `test/mix/tasks/verify.public_api_test.exs` (use the existing `capture_io`
    pattern): add a test asserting the printed manifest contains `"tier"`, and add a unit test
    calling `detect_tier_from_text/1` (expose it for test if needed) covering the three cases —
    `"{: .info}"`+"Stable" -> `:stable`, `"{: .warning}"`+"Experimental" -> `:experimental`,
    a `.warning` callout without "Experimental" -> `:unclassified`.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/verify.public_api_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mix/tasks/verify.public_api_test.exs` exits 0
    - `grep -c 'detect_tier_from_text' lib/mix/tasks/verify.public_api.ex` is >= 2 (defn + call)
    - `lib/mix/tasks/verify.public_api.ex` contains a second `System.halt(1)` guarded by an `:unclassified` filter
    - `mix.exs` no longer contains the string `"verify.public_api":`
    - `detect_tier_from_text/1` returns `:unclassified` for a `{: .warning}` callout whose text lacks "Experimental"
  </acceptance_criteria>
  <done>Gate detects tier from the moduledoc callout, exits non-zero on any `:unclassified` module, and `mix verify.public_api` invokes the real task (alias deleted). Note: `mix verify.public_api` will now legitimately FAIL until Wave 2 annotates the modules — that is expected and proves the gate is live.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/stability.md policy with full tier enumeration and register in extras</name>
  <files>docs/stability.md, mix.exs</files>
  <read_first>
    - docs/telemetry.md (structural analog: H1 + intro paragraph + H2 sections in docs/)
    - mix.exs (extras: list in defp docs)
    - .planning/phases/19-api-telemetry-freeze/19-CONTEXT.md (D-09, D-10, D-11, D-12, D-14 — tier classification + deprecation cycle)
    - .planning/phases/19-api-telemetry-freeze/19-RESEARCH.md (STAB-02 section + full module surface audit, lines 335-394, 613-641)
    - .planning/phases/19-api-telemetry-freeze/19-PATTERNS.md (docs/stability.md pattern + cross-link rules)
  </read_first>
  <action>
    Create `docs/stability.md` (this EXACT filename per D-09). Use the `docs/telemetry.md`
    H1+intro+H2 structure as the format analog. Write these seven sections:
    (1) Stability Tiers — table: Tier / Signal (callout) / Semver Guarantee, covering Stable
    (`> #### Stable {: .info}`, full semver protection), Experimental (`> #### Experimental {: .warning}`,
    one-release CHANGELOG notice before a breaking change), Internal (`Parapet.Internal.*` or
    `@moduledoc false`, no guarantees).
    (2) Public API Surface Enumeration — the COMPLETE per-module tier table organized by
    namespace, taken from D-11/D-12 and the RESEARCH module surface audit. Stable: `Parapet`,
    `Parapet.Integration`, `Parapet.SLO.Provider`, `Parapet.SLO.SliceSpec`, `Parapet.Runbook`,
    `Parapet.Escalation.Policy`, `Parapet.Notifier`, `Parapet.Evidence`, `Parapet.Operator`,
    `Parapet.Deploy`, `Parapet.SLO.StarterPack.WebSaaS`, `Parapet.SLO.StarterPack.DeliverySaaS`,
    `Parapet.Telemetry.AsyncDelivery`. Experimental: all `Parapet.MCP.*`, `Parapet.Automation.*`,
    `Parapet.Metrics.*`, `Parapet.Probe*`, `Parapet.Evidence.Archiver`, `Parapet.Evidence.Retrospective`,
    `Parapet.Evidence.ArchiveWorker`, all `Parapet.Integrations.*`, `Parapet.Notifier.Slack/Teams/ObanWorker`,
    `Parapet.Escalation.Worker`, all `Parapet.Plug.*`, `Parapet.Operator.ActionPayload`,
    `Parapet.Operator.WorkbenchContract`, `Parapet.Capabilities`, all 8 `Parapet.Spine.*` modules,
    `Parapet.SLO`, the SLO preset modules (`SLO.HTTP`, `SLO.LoginJourney`, `SLO.Oban`,
    `SLO.ChimewayDelivery`, `SLO.MailglassDelivery`, `SLO.RindleAsync`, `SLO.ScoriaEval`),
    `Parapet.SLO.Generator`. Internal: `Parapet.Internal.*`, `Parapet.TestSupport.*`.
    (3) Semver Promise — Stable = no breaking change without a major bump + a full deprecation cycle.
    (4) What Counts as Breaking vs Additive — breaking: rename/remove a public fn or callback,
    change arity/return shape, remove/rename a telemetry event name; additive: new functions, new
    optional opts, new telemetry measurement/metadata keys.
    (5) Deprecation Cycle (D-10) — heading `## Deprecation Cycle`: soft `@doc deprecated:` (CHANGELOG,
    no compile warning) -> hard `@deprecated` for >= 1 minor (compile warning; replacement must
    already exist) -> removal only at a major. Experimental tier: a single CHANGELOG entry suffices.
    (6) Telemetry Contract — frozen `[:parapet, …]` event names, additive-only measurement/metadata
    evolution, explicit "no configurable `:event_prefix`" rule; link to telemetry.md via
    `[Telemetry Reference](telemetry.html)`.
    (7) Deprecation Register — current entry: `Parapet.SLO.define/2` is hard-deprecated; replacement
    `Parapet.SLO.Provider`; document the deprecation window (per D-14: hard `@deprecated` now,
    removal at next major).
    Then add `"docs/stability.md"` to the `mix.exs` `extras:` list (insert after `"docs/HISTORY.md"`).
    No `files:` whitelist change is needed (it covers `docs` wholesale).
  </action>
  <verify>
    <automated>test -f docs/stability.md && grep -q "## Deprecation Cycle" docs/stability.md && grep -q "docs/stability.md" mix.exs && grep -q "telemetry.html" docs/stability.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/stability.md` exists
    - `docs/stability.md` contains the heading `## Deprecation Cycle`
    - `docs/stability.md` contains a relative link `telemetry.html`
    - `docs/stability.md` names both `Parapet.SLO.define/2` and `Parapet.SLO.Provider` in the deprecation register
    - `mix.exs` `extras:` contains `docs/stability.md`
  </acceptance_criteria>
  <done>`docs/stability.md` enumerates the full frozen surface with per-module tiers, states the semver promise, defines breaking vs additive, specifies the deprecation cycle, documents the telemetry freeze, and is registered in `extras:`.</done>
</task>

<task type="auto">
  <name>Task 3: Add the stability-freeze header to docs/telemetry.md</name>
  <files>docs/telemetry.md</files>
  <read_first>
    - docs/telemetry.md (file being edited — lines 1-8)
    - .planning/phases/19-api-telemetry-freeze/19-RESEARCH.md (STAB-03 section, lines 644-660 — exact header text)
    - .planning/phases/19-api-telemetry-freeze/19-PATTERNS.md (docs/telemetry.md edit-in-place pattern, lines 302-326)
  </read_first>
  <action>
    Insert an ExDoc `.info` admonition callout immediately after the `# Telemetry Event Schema`
    H1 heading (before the existing intro paragraph) in `docs/telemetry.md`. The callout title is
    `> #### Stable Contract {: .info}` and the body states: this reference is stable as of v1.0.0;
    `[:parapet, …]` event names are frozen (rename/remove = semver-major); measurement and documented
    metadata keys may be extended in minor releases but not removed/renamed without a deprecation
    cycle; Parapet will never add a configurable `:event_prefix` (all event names are static); and a
    cross-link `[Stability & Deprecation Policy](stability.html)`. Use the exact admonition syntax
    `> #### Title {: .info}` with a blank `>` line, then `>`-prefixed body lines.
  </action>
  <verify>
    <automated>grep -q "Stable Contract" docs/telemetry.md && grep -q "{: .info}" docs/telemetry.md && grep -q "event_prefix" docs/telemetry.md && grep -q "stability.html" docs/telemetry.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/telemetry.md` contains `> #### Stable Contract {: .info}`
    - `docs/telemetry.md` mentions the "no configurable `:event_prefix`" rule
    - `docs/telemetry.md` cross-links `stability.html`
    - The callout appears after the H1 and before the existing intro paragraph
  </acceptance_criteria>
  <done>`docs/telemetry.md` opens with a Stable Contract callout documenting the frozen telemetry surface and cross-linking the stability policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate -> release | `mix verify.public_api` is the contract enforcement point; if it does not actually run, an unclassified surface ships silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering (of CI gate) | `mix.exs` `verify.public_api` alias shadow | mitigate | Delete the alias (D-04) so `mix verify.public_api` invokes the real task; Task 1 acceptance asserts `mix.exs` no longer contains `"verify.public_api":` |
| T-19-02 | Spoofing (false tier) | `detect_tier_from_text/1` matching `.warning` alone | mitigate | Require BOTH callout class AND tier word ("Stable"/"Experimental") per Pitfall 2; unit test asserts a `.warning`-only callout resolves `:unclassified` |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable); no supply-chain surface introduced |

No HIGH-severity threats: this plan adds no runtime surface, handles no user input, and installs
no packages. Severity is LOW (CI-gate integrity), fully mitigated by D-04 + the detect-tier unit test.
</threat_model>

<verification>
- `mix test test/mix/tasks/verify.public_api_test.exs` exits 0
- `mix.exs` no longer contains `"verify.public_api":`
- `docs/stability.md` exists with `## Deprecation Cycle` and a `telemetry.html` link
- `docs/telemetry.md` contains the Stable Contract `.info` callout and the `event_prefix` rule
- `mix verify.public_api` runs the real task (it will exit non-zero until Wave 2 annotations land — this confirms the gate is LIVE, not a regression)
</verification>

<success_criteria>
- STAB-04: `mix verify.public_api` invokes the real task and `System.halt(1)`s on any `:unclassified` module; the shadow alias is gone.
- STAB-02: `docs/stability.md` exists, enumerates the full surface with tiers, semver promise, breaking-vs-additive, and the deprecation cycle; registered in `extras:`.
- STAB-03: `docs/telemetry.md` carries the stability-freeze header cross-linking stability.md.
</success_criteria>

<output>
Create `.planning/phases/19-api-telemetry-freeze/19-01-SUMMARY.md` when done.
</output>
