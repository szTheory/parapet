---
phase: 19-api-telemetry-freeze
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - lib/parapet.ex
  - lib/parapet/integration.ex
  - lib/parapet/slo/provider.ex
  - lib/parapet/slo/slice_spec.ex
  - lib/parapet/runbook.ex
  - lib/parapet/escalation/policy.ex
  - lib/parapet/notifier.ex
  - lib/parapet/evidence.ex
  - lib/parapet/operator.ex
  - lib/parapet/deploy.ex
  - lib/parapet/slo/starter_pack/web_saas.ex
  - lib/parapet/slo/starter_pack/delivery_saas.ex
  - lib/parapet/telemetry/async_delivery.ex
autonomous: true
requirements: [STAB-01]
must_haves:
  truths:
    - "Every Stable-tier public module displays a > #### Stable {: .info} ExDoc callout cross-linking stability.html"
    - "Every public function in every Stable-tier module carries @doc since: \"1.0.0\""
    - "mix verify.public_api resolves each of these 13 modules to tier :stable (not :unclassified)"
  artifacts:
    - path: "lib/parapet.ex"
      provides: "Stable callout on the top-level API + @doc since: on attach/1 and peers"
      contains: "{: .info}"
    - path: "lib/parapet/slo/provider.ex"
      provides: "Stable callout on the core SLO behaviour"
      contains: "{: .info}"
    - path: "lib/parapet/telemetry/async_delivery.ex"
      provides: "Stable callout on the machine-readable contract module"
      contains: "{: .info}"
  key_links:
    - from: "each Stable @moduledoc"
      to: "docs/stability.md"
      via: "ExDoc relative link stability.html"
      pattern: "stability\\.html"
    - from: "Stable modules"
      to: "mix verify.public_api"
      via: "detect_tier_from_text -> :stable"
      pattern: "#### Stable \\{: \\.info\\}"
---

<objective>
Annotate the 13 Stable-tier public modules with the `> #### Stable {: .info}` ExDoc callout
(D-01, D-11, D-12) and add `@doc since: "1.0.0"` to every public function in those modules (D-13,
documentation-only). After this plan plus Plan 04, the live STAB-04 gate from Plan 01 passes.

This plan depends on Plan 01: the gate must be live (so annotations are verifiable) and
`docs/stability.md` must exist (so the callouts' `stability.html` cross-links resolve under
`mix docs`).

Purpose: Stable modules are the surface adopters can rely on under `~> 1.0` — the callout
signals full semver protection and the `@doc since:` badge shows the 1.0.0 baseline in HexDocs.

Output: 13 modified `lib/` files; no behavior change (moduledoc + @doc metadata only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-api-telemetry-freeze/19-CONTEXT.md
@.planning/phases/19-api-telemetry-freeze/19-PATTERNS.md
@.planning/phases/19-api-telemetry-freeze/19-RESEARCH.md

<interfaces>
<!-- The exact callout to insert (D-01) and where (PATTERNS lines 333-345, 362-363). -->

Stable callout block to insert into each Stable module's @moduledoc, immediately after the
first sentence/paragraph and before any `## Options`/`## Examples` section:

  > #### Stable {: .info}
  >
  > This module is **stable** as of v1.0.0. Its public API will not change without a
  > major-version bump and a full deprecation cycle. See
  > [Stability & Deprecation Policy](stability.html) for details.

`@doc since: "1.0.0"` placement (D-13, Pitfall 6) — MUST accompany an existing `@doc` string,
never stand alone:

  @doc since: "1.0.0"
  @doc """
  ...existing or new doc string...
  """
  def public_fun(...), do: ...

CONFIRMED file facts (no need to re-verify):
- `lib/parapet/slo/slice_spec.ex` has a real `@moduledoc` (Stable, not @moduledoc false).
- `lib/parapet/operator.ex` has a real `@moduledoc` (Stable). Only operator/action_payload.ex
  and operator/workbench_contract.ex carry `@doc false` on individual functions — those two
  modules are Experimental and belong to Plan 04, NOT this plan.
- Only the 3 `Parapet.Internal.*` modules use `@moduledoc false`; none of these 13 do.

The 13 Stable modules and their files are exactly the `files_modified` list in this plan's
frontmatter. Do NOT touch any other file (Plan 04 owns the Experimental set).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stable callouts + @doc since on the core API and SLO/telemetry contracts</name>
  <files>lib/parapet.ex, lib/parapet/integration.ex, lib/parapet/slo/provider.ex, lib/parapet/slo/slice_spec.ex, lib/parapet/slo/starter_pack/web_saas.ex, lib/parapet/slo/starter_pack/delivery_saas.ex, lib/parapet/telemetry/async_delivery.ex</files>
  <read_first>
    - lib/parapet.ex (moduledoc + public functions)
    - lib/parapet/integration.ex (behaviour moduledoc + callbacks/functions)
    - lib/parapet/slo/provider.ex (behaviour moduledoc)
    - lib/parapet/slo/slice_spec.ex (moduledoc + public functions — confirmed real moduledoc)
    - lib/parapet/slo/starter_pack/web_saas.ex and delivery_saas.ex (moduledocs + public functions)
    - lib/parapet/telemetry/async_delivery.ex (moduledoc + public functions: event_families/0, allowed_public_keys/1, etc.)
    - .planning/phases/19-api-telemetry-freeze/19-PATTERNS.md (ExDoc callout syntax + @doc since placement, lines 329-403)
  </read_first>
  <action>
    For each of these 7 modules, insert the Stable callout block (from the interfaces section)
    into the `@moduledoc`, immediately after the first descriptive sentence/paragraph and before
    any `## Options`/`## Examples` heading. Preserve all existing moduledoc content. Then add
    `@doc since: "1.0.0"` to EVERY public (`def`, not `defp`, and not `@doc false`) function in
    each module — paired with the existing `@doc` string where one exists, or with a brief new
    `@doc` string where the function currently has none (per Pitfall 6, `@doc since:` must never
    stand alone). For `Parapet.SLO.StarterPack.DeliverySaaS`, leave the existing `@doc false`
    function untouched (it is intentionally hidden). Do not change any function body or signature.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | grep -v '^$' | tail -5; grep -l "#### Stable {: .info}" lib/parapet.ex lib/parapet/integration.ex lib/parapet/slo/provider.ex lib/parapet/slo/slice_spec.ex lib/parapet/slo/starter_pack/web_saas.ex lib/parapet/slo/starter_pack/delivery_saas.ex lib/parapet/telemetry/async_delivery.ex | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0 (no `@doc since:` standalone warnings)
    - All 7 of these files contain the literal `#### Stable {: .info}` (grep count == 7)
    - Each file contains at least one `@doc since: "1.0.0"`
    - No `def`/signature in any of these files changed (only `@moduledoc`/`@doc` additions)
  </acceptance_criteria>
  <done>The 7 core-API/SLO/telemetry modules carry the Stable callout cross-linking stability.html and every public function shows the `@doc since: "1.0.0"` badge.</done>
</task>

<task type="auto">
  <name>Task 2: Stable callouts + @doc since on the operator/evidence/runbook/notifier/escalation/deploy surface</name>
  <files>lib/parapet/runbook.ex, lib/parapet/escalation/policy.ex, lib/parapet/notifier.ex, lib/parapet/evidence.ex, lib/parapet/operator.ex, lib/parapet/deploy.ex</files>
  <read_first>
    - lib/parapet/runbook.ex (moduledoc + public functions / DSL)
    - lib/parapet/escalation/policy.ex (behaviour moduledoc + callbacks)
    - lib/parapet/notifier.ex (behaviour moduledoc — note: the BEHAVIOUR is Stable; concrete adapters Slack/Teams/ObanWorker are Experimental and belong to Plan 04)
    - lib/parapet/evidence.ex (moduledoc + public functions)
    - lib/parapet/operator.ex (moduledoc — confirmed real, Stable)
    - lib/parapet/deploy.ex (moduledoc + mark/1)
    - .planning/phases/19-api-telemetry-freeze/19-PATTERNS.md (ExDoc callout syntax + @doc since placement)
  </read_first>
  <action>
    For each of these 6 modules, insert the Stable callout block (from the interfaces section)
    into the `@moduledoc` immediately after the first sentence/paragraph, preserving existing
    content. Add `@doc since: "1.0.0"` to every public function (paired with a doc string per
    Pitfall 6). For `Parapet.Notifier`, annotate only the behaviour module itself — do NOT touch
    `Notifier.Slack`, `Notifier.Teams`, or `Notifier.ObanWorker` (Plan 04 owns those). For
    `Parapet.Deploy`, annotate `mark/1`. Do not change any function body or signature.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | grep -v '^$' | tail -5; grep -l "#### Stable {: .info}" lib/parapet/runbook.ex lib/parapet/escalation/policy.ex lib/parapet/notifier.ex lib/parapet/evidence.ex lib/parapet/operator.ex lib/parapet/deploy.ex | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0
    - All 6 of these files contain the literal `#### Stable {: .info}` (grep count == 6)
    - `lib/parapet/notifier.ex` carries the Stable callout but `lib/parapet/notifier/slack.ex` is NOT modified by this plan
    - Each file contains at least one `@doc since: "1.0.0"`
  </acceptance_criteria>
  <done>The operator/evidence/runbook/notifier-behaviour/escalation-policy/deploy modules carry the Stable callout and `@doc since: "1.0.0"` on their public functions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| moduledoc -> verify.public_api gate | The callout text is the single source of truth the gate parses; a missing/malformed callout silently degrades the tier guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering / contract-doc integrity | Stable module @moduledoc callouts | mitigate | The live gate (Plan 01) resolves these modules to `:stable`; a malformed callout would resolve `:unclassified` and fail `mix verify.public_api` in the phase gate |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable) |

No HIGH-severity threats: documentation/metadata edits only, no runtime surface, no user input,
no installs. Severity LOW (contract-doc integrity), mitigated by the live gate from Plan 01.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0 (proves no standalone `@doc since:` and valid moduledoc syntax)
- All 13 Stable files contain `#### Stable {: .info}`
- After Plan 04 also lands, `mix verify.public_api` exits 0 (no module resolves `:unclassified`)
</verification>

<success_criteria>
- STAB-01 (Stable portion): every Stable-tier public module displays the Stable callout and every Stable public function carries `@doc since: "1.0.0"`; the live gate resolves all 13 to `:stable`.
</success_criteria>

<output>
Create `.planning/phases/19-api-telemetry-freeze/19-03-SUMMARY.md` when done.
</output>
