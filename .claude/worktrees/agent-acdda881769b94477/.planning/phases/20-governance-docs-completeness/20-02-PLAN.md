---
phase: 20-governance-docs-completeness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [GOV-04]
must_haves:
  truths:
    - "A README reader learns Parapet commits to semver from 1.0 — the public API will not break without a major bump"
    - "A README reader sees the supported Elixir / OTP / Postgres version matrix"
  artifacts:
    - path: "README.md"
      provides: "1.0 semver commitment paragraph + Elixir/OTP/Postgres compatibility matrix"
      contains: "Semantic Versioning"
  key_links:
    - from: "README.md"
      to: "semver.org"
      via: "Semantic Versioning link"
      pattern: "semver\\.org"
---

<objective>
Add a 1.0 semver commitment statement and a supported version matrix (Elixir / OTP / Postgres) to README.md.

Purpose: Close GOV-04 — adopters must see an explicit stability commitment and version support before trusting `~> 1.0`.
Output: An edited README.md with a "Compatibility" matrix and a "Stability & Versioning" section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-governance-docs-completeness/20-CONTEXT.md
@.planning/phases/20-governance-docs-completeness/20-PATTERNS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add semver commitment + version matrix to README (GOV-04)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current structure — badge block at lines 1–5, intro paragraphs, `## Features` at line 13, `## Installation` at line 21; the new content goes before `## Features` per PATTERNS.md)
    - .planning/phases/20-governance-docs-completeness/20-PATTERNS.md (section "README.md (edit — add semver commitment + version matrix)" — exact table and prose shape)
    - .planning/phases/20-governance-docs-completeness/20-CONTEXT.md (D-02 — version matrix values and parenthetical)
  </read_first>
  <action>
    Edit README.md to add two new H2 sections, placed after the intro/philosophy paragraphs and BEFORE the existing `## Features` heading (line 13), so they are high in the document for discoverability.

    Section "Compatibility" — a Markdown table with rows for the locked D-02 values: Elixir `1.19+`, OTP `26–28`, Postgres `14+`. Immediately under the table, add the exact parenthetical/sentence: "CI validates on Elixir 1.19 / OTP 27 / PG 14." Do NOT claim the broader OTP range is CI-tested (be honest about the gap — see CONTEXT.md "Specific Ideas").

    Section "Stability & Versioning" — a paragraph stating Parapet follows Semantic Versioning (link to https://semver.org/), and that starting at `1.0` the public API (modules, functions, telemetry event names, SLO slice names, and Prometheus metric names documented in hexdocs) will not break without a major-version bump. Note that pre-1.0 minor releases may include breaking changes recorded in CHANGELOG.md (link to CHANGELOG.md). Exact wording is Claude's discretion; the semver commitment and the API-surface enumeration are required.

    Use existing README Markdown conventions (the file already uses `##` headings and fenced code blocks). Do not alter the badge block, intro, Features, or Installation content.
  </action>
  <verify>
    <automated>grep -q "Semantic Versioning" README.md && grep -q "semver.org" README.md && grep -qi "Elixir" README.md && grep -q "1.19" README.md && grep -qi "OTP" README.md && grep -qi "Postgres" README.md && grep -q "CI validates on Elixir 1.19" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a "Compatibility" section with a table listing Elixir `1.19+`, OTP `26–28`, Postgres `14+`
    - README.md contains the exact sentence fragment `CI validates on Elixir 1.19 / OTP 27 / PG 14`
    - README.md contains a "Stability & Versioning" section that links to `semver.org` and states the 1.0 public-API-stability commitment
    - The semver section enumerates the public surface (modules, functions, telemetry event names, SLO slice names, Prometheus metric names)
    - The badge block (lines 1–5), `## Features`, and `## Installation` content are unchanged
  </acceptance_criteria>
  <done>README.md has a Compatibility matrix (D-02 values + CI parenthetical) and a Stability & Versioning section committing to semver from 1.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan edits one static Markdown file. No runtime code, input, or network surface is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Repudiation | README version-support claim | mitigate | State the CI-validated combination explicitly (Elixir 1.19 / OTP 27 / PG 14) so the support claim is not overstated beyond what CI proves (D-02 / CONTEXT.md "Specific Ideas"). |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (documentation-only — RESEARCH.md "Package Legitimacy Audit" confirms zero packages). |
</threat_model>

<verification>
- `grep -q "CI validates on Elixir 1.19" README.md` exits 0
- `mix test` passes (no code changed)
</verification>

<success_criteria>
README.md states the 1.0 semver commitment and shows the Elixir/OTP/Postgres support matrix with the honest CI-validation parenthetical, satisfying GOV-04.
</success_criteria>

<output>
Create `.planning/phases/20-governance-docs-completeness/20-02-SUMMARY.md` when done.
</output>
