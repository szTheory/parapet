---
phase: 20-governance-docs-completeness
plan: 05
type: execute
wave: 2
depends_on: ["20-01", "20-03", "20-04"]
files_modified:
  - mix.exs
autonomous: true
requirements: [GOV-05, DOCS-06]
must_haves:
  truths:
    - "mix hex.build --dry-run includes CONTRIBUTING.md, SECURITY.md, and CODE_OF_CONDUCT.md in the published package"
    - "hexdocs serves four grouped extras sections: Getting Started / Guides / Integration Guides / Reference"
    - "The getting-started guide is the hexdocs landing page"
    - "mix docs --warnings-as-errors builds clean with all 8 integration guides grouped under Integration Guides"
  artifacts:
    - path: "mix.exs"
      provides: "files: whitelist with governance globs; docs main: getting-started; extras with 4 new guides; 4-group groups_for_extras"
      contains: "getting-started"
  key_links:
    - from: "mix.exs package.files"
      to: "CONTRIBUTING.md / SECURITY.md / CODE_OF_CONDUCT.md"
      via: "files: whitelist globs"
      pattern: "CONTRIBUTING\\*"
    - from: "mix.exs docs.main"
      to: "docs/getting-started.md"
      via: "main: getting-started"
      pattern: "main:\\s*\"getting-started\""
---

<objective>
Wire all Phase 20 artifacts into `mix.exs`: ship the governance triad via the Hex `files:` whitelist (GOV-05), add the four new integration guides to `extras:`, switch the hexdocs landing page to getting-started, and restructure `groups_for_extras:` into the four locked groups (DOCS-06).

Purpose: Close GOV-05 and DOCS-06 — the convergence plan that makes the new docs actually ship and appear correctly grouped on hexdocs.
Output: A single edited `mix.exs` (functions `package/0` and `docs/0`).

This plan is Wave 2: it depends on 20-01 (governance files must exist so `mix hex.build --dry-run` ships them and so they are real), 20-03 (the four integration guide files must exist or `mix docs --warnings-as-errors` fails on the `extras:` references), and 20-04 (the slo-reference → slo-authoring anchor must resolve to avoid a docs warning).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-governance-docs-completeness/20-CONTEXT.md
@.planning/phases/20-governance-docs-completeness/20-RESEARCH.md
@.planning/phases/20-governance-docs-completeness/20-PATTERNS.md
@mix.exs

<interfaces>
<!-- Current mix.exs state the executor edits in place. Verified by codebase read. -->

package/0 files: (mix.exs line 42):
  files: ~w(lib priv .formatter.exs mix.exs README* CHANGELOG* LICENSE* docs)

docs/0 main: (mix.exs line 55):
  main: "readme"

docs/0 extras: (mix.exs lines 58-74) — 15 entries, ending with the existing integration guides:
  "docs/integrations/sigra.md", "docs/integrations/accrue.md",
  "docs/integrations/rulestead.md", "docs/integrations/threadline.md"

docs/0 groups_for_extras: (mix.exs lines 76-78):
  groups_for_extras: [ Guides: ~r/docs\// ]

docs/0 also has: skip_undefined_reference_warnings_on: ["CHANGELOG.md"] (line 75) — leave unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add governance docs to the Hex files: whitelist (GOV-05)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (current `package/0` at lines 40-51 — the `files:` sigil list at line 42 is the edit target)
    - .planning/phases/20-governance-docs-completeness/20-CONTEXT.md (D-07 — exact target files: list)
    - .planning/phases/20-governance-docs-completeness/20-RESEARCH.md (Pitfall 2 — root .md files not matching an existing glob are silently excluded; governance globs must be added explicitly)
  </read_first>
  <action>
    Edit the `files:` sigil in `package/0` (mix.exs line 42) to add three governance globs. Target state (D-07), inserting `CONTRIBUTING* SECURITY* CODE_OF_CONDUCT*` after `CHANGELOG*` and before `LICENSE*`:
    `files: ~w(lib priv .formatter.exs mix.exs README* CHANGELOG* CONTRIBUTING* SECURITY* CODE_OF_CONDUCT* LICENSE* docs)`
    Do not add the governance docs to `extras:` (RESEARCH Pitfall 3 — they ship in the package via `files:` only, they do NOT appear in hexdocs navigation). Do not change `licenses:` or `links:`.
  </action>
  <verify>
    <automated>grep -q "CONTRIBUTING\* SECURITY\* CODE_OF_CONDUCT\*" mix.exs && mix hex.build --dry-run 2>&1 | grep -E "CONTRIBUTING|SECURITY|CODE_OF_CONDUCT"</automated>
  </verify>
  <acceptance_criteria>
    - mix.exs `files:` list contains the globs `CONTRIBUTING*`, `SECURITY*`, `CODE_OF_CONDUCT*`
    - `mix hex.build --dry-run` output lists `CONTRIBUTING.md`, `SECURITY.md`, and `CODE_OF_CONDUCT.md` among the packaged files
    - Governance docs are NOT added to `extras:`
    - `licenses:` and `links:` in `package/0` are unchanged
  </acceptance_criteria>
  <done>The Hex package whitelist ships the three governance docs, verified by `mix hex.build --dry-run`.</done>
</task>

<task type="auto">
  <name>Task 2: Switch docs landing page and add integration guides to extras (DOCS-06)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (current `docs/0` at lines 53-79 — `main:` at line 55, `extras:` at 58-74)
    - .planning/phases/20-governance-docs-completeness/20-CONTEXT.md (D-04 — main: getting-started; D-09 — add four guides to extras)
    - .planning/phases/20-governance-docs-completeness/20-RESEARCH.md (Pitfall 1 — `main:` takes the bare stem `"getting-started"`, no path, no extension)
  </read_first>
  <action>
    Edit `docs/0`:
    (1) Change `main: "readme"` (line 55) to `main: "getting-started"` (D-04). Use the bare stem exactly — no path prefix, no `.md` extension (Pitfall 1). `docs/getting-started.md` is already in `extras:`, so this resolves.
    (2) Add the four new integration guide paths to the `extras:` list (D-09), after the existing `"docs/integrations/threadline.md"` entry:
    `"docs/integrations/chimeway.md"`, `"docs/integrations/mailglass.md"`, `"docs/integrations/rindle.md"`, `"docs/integrations/scoria.md"`.
    Leave `skip_undefined_reference_warnings_on:` unchanged. Do not touch `groups_for_extras:` in this task (Task 3 owns it).
  </action>
  <verify>
    <automated>grep -q 'main: "getting-started"' mix.exs && grep -q "docs/integrations/chimeway.md" mix.exs && grep -q "docs/integrations/mailglass.md" mix.exs && grep -q "docs/integrations/rindle.md" mix.exs && grep -q "docs/integrations/scoria.md" mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - mix.exs `docs/0` has `main: "getting-started"` (bare stem, no path/extension)
    - `extras:` lists all four new guide paths: chimeway.md, mailglass.md, rindle.md, scoria.md
    - The pre-existing extras entries (README, CHANGELOG, docs/*, the four original integration guides) are still present
    - `skip_undefined_reference_warnings_on:` is unchanged
  </acceptance_criteria>
  <done>`main:` points to getting-started and all eight integration guides are in `extras:`.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure groups_for_extras into four groups and verify the docs build (DOCS-06)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (current `groups_for_extras:` at lines 76-78 — `[Guides: ~r/docs\//]`, to be replaced entirely)
    - .planning/phases/20-governance-docs-completeness/20-CONTEXT.md (D-03 — the exact four-group structure and member lists)
    - .planning/phases/20-governance-docs-completeness/20-RESEARCH.md (Code Examples "Verified mix.exs groups_for_extras Pattern" — the exact target block; Pitfall 6 — regex overlap, use explicit lists for 3 groups and a regex only for Integration Guides)
    - .planning/phases/20-governance-docs-completeness/20-PATTERNS.md (the target groups_for_extras block + the "place Integration Guides regex correctly" note)
  </read_first>
  <action>
    Replace the entire `groups_for_extras:` block (mix.exs lines 76-78) with the four-group structure (D-03), in this order so the nav reads Getting Started → Guides → Integration Guides → Reference:
    - "Getting Started": explicit list `["README.md", "docs/getting-started.md"]`
    - Guides: explicit list `["docs/adopter-flows.md", "docs/operator-ui.md", "docs/slo-authoring-guide.md", "docs/troubleshooting.md", "docs/HISTORY.md", "CHANGELOG.md"]`
    - "Integration Guides": regex `~r|docs/integrations/|` (captures all 8 guides without enumerating — future-proof)
    - Reference: explicit list `["docs/stability.md", "docs/telemetry.md", "docs/slo-reference.md"]`
    Use explicit file lists for Getting Started, Guides, and Reference (NOT a broad `~r/docs\//`) to avoid the regex-overlap pitfall where a broad docs regex swallows the integration guides (Pitfall 6). Only Integration Guides uses a regex.
    After editing, build the docs to confirm no warnings: `mix docs --warnings-as-errors`. This also confirms the slo-reference anchor from plan 20-04 and the four new extras resolve.
  </action>
  <verify>
    <automated>grep -q '"Integration Guides": ~r|docs/integrations/|' mix.exs && grep -q '"Getting Started":' mix.exs && grep -q 'Reference:' mix.exs && mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - mix.exs `groups_for_extras:` has exactly four groups in order: "Getting Started", Guides, "Integration Guides", Reference
    - "Getting Started" = `["README.md", "docs/getting-started.md"]`; Reference = `["docs/stability.md", "docs/telemetry.md", "docs/slo-reference.md"]`
    - "Integration Guides" uses the regex `~r|docs/integrations/|` (not an explicit file list)
    - Guides uses an explicit list and does NOT use a broad `~r/docs\//` that would swallow integration guides
    - `mix docs --warnings-as-errors` exits 0 (clean build — all extras and cross-link anchors resolve)
  </acceptance_criteria>
  <done>groups_for_extras is the four-group structure; `mix docs --warnings-as-errors` builds clean with all 8 integration guides grouped under Integration Guides and getting-started as the landing page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex package publish surface | `files:` controls what ships to Hex.pm. Over-inclusion could leak unintended files; under-inclusion could omit required governance docs. Verified via `mix hex.build --dry-run`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Information Disclosure | Hex `files:` whitelist | mitigate | Only add the three named governance globs (`CONTRIBUTING*`, `SECURITY*`, `CODE_OF_CONDUCT*`); do not broaden the whitelist. Verify the exact packaged file set with `mix hex.build --dry-run`. |
| T-20-06 | Tampering (build integrity) | groups_for_extras regex overlap | mitigate | Use explicit file lists for 3 groups and a single scoped regex for Integration Guides (Pitfall 6); confirm with `mix docs --warnings-as-errors`. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (documentation-only — RESEARCH.md "Package Legitimacy Audit" confirms zero packages). |
</threat_model>

<verification>
- `mix hex.build --dry-run 2>&1 | grep -E "CONTRIBUTING|SECURITY|CODE_OF_CONDUCT"` shows all three governance docs
- `grep -q 'main: "getting-started"' mix.exs` exits 0
- `mix docs --warnings-as-errors` exits 0
- `mix test --warnings-as-errors` passes (phase gate — full suite green)
</verification>

<success_criteria>
mix.exs ships the governance triad in the Hex package (GOV-05), serves four grouped extras with getting-started as the landing page, and `mix docs --warnings-as-errors` builds clean (DOCS-06).
</success_criteria>

<output>
Create `.planning/phases/20-governance-docs-completeness/20-05-SUMMARY.md` when done.
</output>
