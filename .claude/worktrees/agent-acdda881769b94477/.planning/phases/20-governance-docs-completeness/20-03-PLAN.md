---
phase: 20-governance-docs-completeness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/integrations/chimeway.md
  - docs/integrations/mailglass.md
  - docs/integrations/rindle.md
  - docs/integrations/scoria.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]
must_haves:
  truths:
    - "An adopter can activate Chimeway monitoring by following docs/integrations/chimeway.md"
    - "An adopter can activate Mailglass monitoring by following docs/integrations/mailglass.md"
    - "An adopter can activate Rindle monitoring by following docs/integrations/rindle.md"
    - "An adopter can activate Scoria monitoring by following docs/integrations/scoria.md"
    - "Each guide has the same five-section shape as docs/integrations/sigra.md"
  artifacts:
    - path: "docs/integrations/chimeway.md"
      provides: "Chimeway integration guide (prerequisites → what it unlocks → activation → config keys → troubleshooting)"
      contains: "Parapet.attach(adapters: [:chimeway])"
    - path: "docs/integrations/mailglass.md"
      provides: "Mailglass integration guide"
      contains: "Parapet.attach(adapters: [:mailglass])"
    - path: "docs/integrations/rindle.md"
      provides: "Rindle integration guide (all 7 events listed)"
      contains: "Parapet.attach(adapters: [:rindle])"
    - path: "docs/integrations/scoria.md"
      provides: "Scoria integration guide (metrics + evidence-spine value props, reporter wiring, no pre-built SLO)"
      contains: "Parapet.attach(adapters: [:scoria])"
  key_links:
    - from: "docs/integrations/scoria.md"
      to: "Parapet.Metrics.Scoria.metrics()"
      via: "reporter wiring instruction"
      pattern: "Parapet.Metrics.Scoria.metrics"
---

<objective>
Author the four missing integration guides — Chimeway (DOCS-01), Mailglass (DOCS-02), Rindle (DOCS-03), and Scoria (DOCS-04) — each matching the `docs/integrations/sigra.md` golden template exactly. Content is derived from the integration module source code (already read and summarized in RESEARCH.md).

Purpose: Close DOCS-01 through DOCS-04 — adopters cannot activate these integrations today because no guide exists.
Output: Four new Markdown files under `docs/integrations/`. (Wiring into `mix.exs` `extras:` and `groups_for_extras:` happens in plan 20-05, which owns `mix.exs`.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-governance-docs-completeness/20-CONTEXT.md
@.planning/phases/20-governance-docs-completeness/20-RESEARCH.md
@.planning/phases/20-governance-docs-completeness/20-PATTERNS.md
@docs/integrations/sigra.md
@docs/integrations/rulestead.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Chimeway, Mailglass, and Rindle guides (DOCS-01, DOCS-02, DOCS-03)</name>
  <files>docs/integrations/chimeway.md, docs/integrations/mailglass.md, docs/integrations/rindle.md</files>
  <read_first>
    - docs/integrations/sigra.md (the golden template — copy its exact five-section structure: title + intro, ## Prerequisites, ## What it unlocks, ## Activation, ## Config keys, ## Troubleshooting)
    - .planning/phases/20-governance-docs-completeness/20-RESEARCH.md (sections "Chimeway", "Mailglass", "Rindle" under "Integration Guide Content — Derived from Source" — handler IDs, event lists, metadata shapes, SLO providers, troubleshooting items)
    - .planning/phases/20-governance-docs-completeness/20-PATTERNS.md (the per-guide section specs for chimeway.md, mailglass.md, rindle.md)
    - lib/parapet/integrations/chimeway.ex (verify @handler_id and the events the module attaches before writing)
    - lib/parapet/integrations/mailglass.ex (verify @handler_id and the three event families)
    - lib/parapet/integrations/rindle.ex (verify @events — all seven must be listed in the guide)
  </read_first>
  <action>
    Create three integration guides, each matching docs/integrations/sigra.md structure exactly (D-08): title `# Parapet + [Name]` + two-sentence intro, then `## Prerequisites`, `## What it unlocks`, `## Activation`, `## Config keys`, `## Troubleshooting`. No extra sections, no reordering.

    docs/integrations/chimeway.md (DOCS-01):
    - Prerequisites: `chimeway` optional dep caveat + `mix parapet.install`.
    - What it unlocks: Chimeway delivery events become `[:parapet, :delivery, :provider_feedback]` / `[:parapet, :delivery, :webhook_ingest]` metrics tagged by `outcome`, `failure_class`, `fault_plane`, `delay_bucket`. Reference the pre-built provider `Parapet.SLO.ChimewayDelivery` (slices `chimeway_provider_acceptance`, `chimeway_callback_confirmation`, `chimeway_callback_freshness`) and link to docs/slo-reference.md.
    - Activation: `Parapet.attach(adapters: [:chimeway])`; state it attaches a handler for `[:chimeway, :event, :failed]` (handler id `parapet-chimeway-delivery-events`).
    - Config keys: none at Parapet level.
    - Troubleshooting (three `###` Q&As): metrics not appearing (reporter wiring), duplicate attach conflict (handler `parapet-chimeway-delivery-events`), provider_feedback vs webhook_ingest routing via `callback_delay?/1`.

    docs/integrations/mailglass.md (DOCS-02):
    - Prerequisites: `mailglass` optional dep caveat + `mix parapet.install`.
    - What it unlocks: three families — `[:parapet, :delivery, :outbound]` (send stop), `[:parapet, :delivery, :provider_feedback]` (reconcile stop), `[:parapet, :delivery, :webhook_ingest]` (webhook exception); tags `integration: :mailglass`, `provider`, `channel: :email`, `outcome`, `fault_plane`, optional `failure_class`, `delay_bucket`; refs `message_id`, `delivery_id`, `provider_message_id`. Reference `Parapet.SLO.MailglassDelivery` (slices `mailglass_submit_acceptance`, `mailglass_confirmed_delivery`, `mailglass_webhook_freshness`, `mailglass_suppression_drift`) and link to docs/slo-reference.md.
    - Activation: `Parapet.attach(adapters: [:mailglass])`; state it attaches handlers (via `attach_many`) for `[:mailglass, :outbound, :send, :stop]`, `[:mailglass, :reconcile, :stop]`, `[:mailglass, :webhook, :ingest, :exception]` (handler id `parapet-mailglass-delivery`).
    - Config keys: none at Parapet level.
    - Troubleshooting (`###` Q&As): metrics not appearing (reporter wiring), duplicate attach conflict, `latency_ms` field on webhook events.

    docs/integrations/rindle.md (DOCS-03):
    - Prerequisites: `rindle` optional dep caveat + `mix parapet.install`.
    - What it unlocks: three async families — `[:parapet, :async, :stage]` (started/processed/failed/discarded, tags `pipeline_stage`, `outcome`, `retry_state`, `fault_plane`), `[:parapet, :async, :backlog]` (backlog, tag `queue`), `[:parapet, :async, :callback]` (callback_delayed + reconciliation_delayed, tag `delay_bucket`); common tags `integration: :rindle`, `provider`, `queue`; refs `job_id`, `webhook_id`. Reference `Parapet.SLO.RindleAsync` (slices `rindle_terminal_success`, `rindle_queue_freshness`, `rindle_callback_freshness`, `rindle_long_running_stage`, `rindle_funnel_regression`) and link to docs/slo-reference.md.
    - Activation: `Parapet.attach(adapters: [:rindle])`; the guide MUST list all seven source events (RESEARCH Pitfall 5): `[:rindle, :media, :started]`, `[:rindle, :media, :processed]`, `[:rindle, :media, :failed]`, `[:rindle, :media, :discarded]`, `[:rindle, :media, :backlog]`, `[:rindle, :media, :callback_delayed]`, `[:rindle, :media, :reconciliation_delayed]` (handler id `parapet-rindle-async`).
    - Config keys: none at Parapet level.
    - Troubleshooting (`###` Q&As): duplicate attach conflict, `pipeline_stage` normalization (string → atom), `retry_state` inference from `attempt`/`attempt_number` metadata.

    Use the project cross-link convention from PATTERNS.md: bare relative paths from repo root (e.g. `[Parapet SLO Reference](docs/slo-reference.md)`).
  </action>
  <verify>
    <automated>for f in chimeway mailglass rindle; do test -f docs/integrations/$f.md || exit 1; done; grep -q "Parapet.attach(adapters: \[:chimeway\])" docs/integrations/chimeway.md && grep -q "Parapet.attach(adapters: \[:mailglass\])" docs/integrations/mailglass.md && grep -q "Parapet.attach(adapters: \[:rindle\])" docs/integrations/rindle.md && grep -c "^## " docs/integrations/rindle.md | grep -qx 5 && grep -q "reconciliation_delayed" docs/integrations/rindle.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/integrations/chimeway.md`, `docs/integrations/mailglass.md`, `docs/integrations/rindle.md` all exist
    - Each file has exactly five `## ` H2 sections in sigra.md order: Prerequisites, What it unlocks, Activation, Config keys, Troubleshooting (`grep -c "^## " docs/integrations/<f>.md` returns 5)
    - chimeway.md contains `Parapet.attach(adapters: [:chimeway])` and references `Parapet.SLO.ChimewayDelivery`
    - mailglass.md contains `Parapet.attach(adapters: [:mailglass])`, all three event atoms, and references `Parapet.SLO.MailglassDelivery`
    - rindle.md contains `Parapet.attach(adapters: [:rindle])`, references `Parapet.SLO.RindleAsync`, and lists all seven `[:rindle, :media, …]` events (including `reconciliation_delayed`)
    - No guide invents sections beyond the sigra.md template
  </acceptance_criteria>
  <done>Three guides exist, each matching the sigra.md five-section template, with correct activation lines, event lists, SLO provider references, and troubleshooting Q&As derived from the module source.</done>
</task>

<task type="auto">
  <name>Task 2: Author Scoria guide (DOCS-04)</name>
  <files>docs/integrations/scoria.md</files>
  <read_first>
    - docs/integrations/sigra.md (five-section template structure)
    - docs/integrations/rulestead.md (the "no pre-built SLO provider" + "Reporter wiring required" patterns — Scoria reuses both)
    - .planning/phases/20-governance-docs-completeness/20-RESEARCH.md (section "Scoria" under "Integration Guide Content" — handler IDs, seven events, evidence-spine behavior, Prometheus metrics, low-cardinality labels; plus Pitfall 4 "Scoria guide omitting incident/action-item behavior")
    - .planning/phases/20-governance-docs-completeness/20-PATTERNS.md (section "docs/integrations/scoria.md" — exact section content)
    - lib/parapet/integrations/scoria.ex (verify the handler IDs and the events handled)
    - lib/parapet/metrics/scoria.ex (verify metric names and tags: scoria_evaluation_total, scoria_mcp_errors_total)
  </read_first>
  <action>
    Create docs/integrations/scoria.md matching the sigra.md five-section template (D-08): title `# Parapet + Scoria` + intro, `## Prerequisites`, `## What it unlocks`, `## Activation`, `## Config keys`, `## Troubleshooting`.

    - Prerequisites: `scoria` optional dep caveat + `mix parapet.install`.
    - What it unlocks — cover BOTH value propositions (Pitfall 4):
      (a) Prometheus metrics via `Parapet.Metrics.Scoria`: `scoria_evaluation_total` (tags `guardrail`, `passed`, `model_name`) and `scoria_mcp_errors_total` (tags `reason`, `tool_name`).
      (b) Evidence-spine integration (unique to Scoria): config-deployed events create `Parapet.Evidence` incident records with runbook data; stale workflow events create action items; resumed workflows resolve them.
      Include the "Reporter wiring required" pattern (from rulestead.md) instructing the host app to add `Parapet.Metrics.Scoria.metrics() ++ your_other_metrics()` to their Telemetry.Metrics reporter.
      State there is NO pre-built SLO provider for Scoria and link to docs/slo-authoring-guide.md for building custom slices. Cross-link docs/telemetry.md noting `[:parapet, :scoria, …]` events follow the same additive-only stability rules.
    - Activation: `Parapet.attach(adapters: [:scoria])`; list the seven events handled: `[:scoria, :sre, :telemetry]`, `[:scoria, :config, :deployed]`, `[:scoria, :mcp, :tool, :exception]`, `[:scoria, :workflow, :stale]`, `[:scoria, :workflow, :expired]`, `[:scoria, :workflow, :resumed]`, `[:scoria, :eval, :completed]`; and the five handler IDs: `parapet-scoria-telemetry`, `parapet-scoria-config-telemetry`, `parapet-scoria-mcp-telemetry`, `parapet-scoria-workflow-telemetry`, `parapet-scoria-eval-handler`.
    - Config keys: none at Parapet level (Scoria uses `Code.ensure_loaded?/1` at runtime for `Scoria.Workflow`).
    - Troubleshooting (`###` Q&As): reporter wiring (metrics not scraped), duplicate attach conflict across the five handler IDs, high-cardinality metadata note (the module strips non-safe labels; only `[:model, :provider, :tool_name]` are kept).

    Use bare relative cross-link paths (`docs/slo-authoring-guide.md`, `docs/telemetry.md`).
  </action>
  <verify>
    <automated>test -f docs/integrations/scoria.md && grep -q "Parapet.attach(adapters: \[:scoria\])" docs/integrations/scoria.md && grep -q "Parapet.Metrics.Scoria.metrics" docs/integrations/scoria.md && grep -q "scoria_evaluation_total" docs/integrations/scoria.md && grep -qi "incident" docs/integrations/scoria.md && grep -c "^## " docs/integrations/scoria.md | grep -qx 5</automated>
  </verify>
  <acceptance_criteria>
    - `docs/integrations/scoria.md` exists with exactly five `## ` H2 sections in sigra.md order (`grep -c "^## "` returns 5)
    - Contains `Parapet.attach(adapters: [:scoria])` and lists all seven `[:scoria, …]` events plus the five handler IDs
    - "What it unlocks" covers BOTH the Prometheus metrics (`scoria_evaluation_total`, `scoria_mcp_errors_total`) AND the evidence-spine behavior (incident / action-item creation) — i.e. contains a case-insensitive match for "incident"
    - Contains the reporter-wiring instruction `Parapet.Metrics.Scoria.metrics()`
    - States there is no pre-built SLO provider and links to docs/slo-authoring-guide.md
    - Notes the low-cardinality safe labels `[:model, :provider, :tool_name]`
  </acceptance_criteria>
  <done>scoria.md exists matching the template, covers both metrics and evidence-spine value props, includes reporter wiring, the seven events, five handler IDs, and the no-pre-built-SLO note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan creates four static Markdown files. No runtime code, input, or network surface is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering (doc accuracy) | Integration guide event/handler names | mitigate | Content is derived from and verified against the integration module source (`read_first` requires reading the `.ex` files); guides must not invent event names from memory (RESEARCH "Don't Hand-Roll"). |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (documentation-only — RESEARCH.md "Package Legitimacy Audit" confirms zero packages). |
</threat_model>

<verification>
- All four guides exist: `for f in chimeway mailglass rindle scoria; do test -f docs/integrations/$f.md || exit 1; done`
- Each guide has the five-section sigra.md shape (`grep -c "^## "` returns 5 for each)
- `mix test` passes (no code changed)
- Note: `mix docs --warnings-as-errors` is run in plan 20-05 after these files are wired into `extras:`.
</verification>

<success_criteria>
Four integration guides (Chimeway, Mailglass, Rindle, Scoria) exist under `docs/integrations/`, each matching the sigra.md template with accurate, source-derived activation lines, event lists, SLO references, and troubleshooting — satisfying DOCS-01 through DOCS-04.
</success_criteria>

<output>
Create `.planning/phases/20-governance-docs-completeness/20-03-SUMMARY.md` when done.
</output>
