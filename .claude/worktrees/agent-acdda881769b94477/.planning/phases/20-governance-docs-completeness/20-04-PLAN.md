---
phase: 20-governance-docs-completeness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/slo-authoring-guide.md
  - docs/slo-reference.md
autonomous: true
requirements: [DOCS-05]
must_haves:
  truths:
    - "An adopter reading the SLO authoring guide learns that a Parapet.SLO.Provider returning multiple slices IS the bundle abstraction — no separate macro needed"
    - "The guide names Parapet.SLO.StarterPack.DeliverySaaS as the canonical bundle example"
    - "docs/slo-reference.md cross-links to the new Provider-as-bundle section"
  artifacts:
    - path: "docs/slo-authoring-guide.md"
      provides: "Provider-as-bundle pattern section"
      contains: "Provider-as-bundle"
    - path: "docs/slo-reference.md"
      provides: "Cross-link to the Provider-as-bundle section"
      contains: "provider-as-bundle-pattern"
  key_links:
    - from: "docs/slo-reference.md"
      to: "docs/slo-authoring-guide.md#provider-as-bundle-pattern"
      via: "Markdown anchor cross-link"
      pattern: "slo-authoring-guide.md#provider-as-bundle-pattern"
---

<objective>
Document the Provider-as-bundle pattern in the SLO authoring guide (a `Parapet.SLO.Provider` that returns slices from multiple sub-providers IS the bundle), with `Parapet.SLO.StarterPack.DeliverySaaS` as the canonical example, and cross-link it from the SLO reference.

Purpose: Close DOCS-05 — adopters must understand multi-integration SLO composition without searching for a separate bundle abstraction (the SLO-B1 macro was explicitly dropped in favor of this doc).
Output: An appended section in `docs/slo-authoring-guide.md` and a one-sentence cross-link in `docs/slo-reference.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-governance-docs-completeness/20-CONTEXT.md
@.planning/phases/20-governance-docs-completeness/20-RESEARCH.md
@.planning/phases/20-governance-docs-completeness/20-PATTERNS.md
@docs/slo-authoring-guide.md
@docs/slo-reference.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Provider-as-bundle section to the SLO authoring guide (DOCS-05)</name>
  <files>docs/slo-authoring-guide.md</files>
  <read_first>
    - docs/slo-authoring-guide.md (current structure — existing H2 sections: "How to decide what to slice", "Writing a custom slice" at line 39, "Low-traffic and low-volume services", "What not to do" at line 106; the new section is appended after "Writing a custom slice" or at the end, matching the existing H2 + prose + ```elixir code-block style)
    - .planning/phases/20-governance-docs-completeness/20-PATTERNS.md (section "docs/slo-authoring-guide.md (edit — add Provider-as-bundle section)" — exact section content and code example)
    - .planning/phases/20-governance-docs-completeness/20-RESEARCH.md (section "Provider-as-Bundle Pattern (DOCS-05)" — what to document and the verified code example)
    - .planning/phases/20-governance-docs-completeness/20-CONTEXT.md (D-10 — add section here, not a separate file; reference DeliverySaaS; cross-link from slo-reference.md)
    - lib/parapet/slo/starter_pack/delivery_saas.ex (verify the slos/0 composition pattern and Code.ensure_loaded?/1 guards before documenting)
  </read_first>
  <action>
    Append a new H2 section titled "Provider-as-bundle pattern" to docs/slo-authoring-guide.md (D-10 — in this file, not a separate file). The heading text must produce the anchor `#provider-as-bundle-pattern` (lowercase, hyphenated) so slo-reference.md can link to it. Content:
    - State that a `Parapet.SLO.Provider` whose `slos/0` returns slices from multiple sub-providers IS the bundle abstraction — no separate macro or base module is required; list concatenation (`++`) is the composition primitive.
    - Name `Parapet.SLO.StarterPack.DeliverySaaS` as the canonical example (its `slos/0` composes `WebSaaS.slos()` with conditionally-guarded delivery slices).
    - Include an ```elixir example of a host-defined bundle provider implementing `@behaviour Parapet.SLO.Provider` and concatenating `Parapet.SLO.StarterPack.WebSaaS.slos()` with a `Code.ensure_loaded?/1`-guarded `Parapet.SLO.MailglassDelivery.slos()` plus custom slices.
    - Show registration: `config :parapet, providers: [MyApp.SLO.FullStack]`.
    - Document the conditional-registration rule: use `Code.ensure_loaded?/1` to guard slices for optional host libraries; the bundle module itself stays loadable (passes `mix verify.public_api`) regardless of host-lib presence — point at the `Parapet.SLO.StarterPack.DeliverySaaS` moduledoc for the reference implementation.
    - End with a cross-link to docs/slo-reference.md (e.g. the starter-packs anchor) using a bare relative path.
    Match the existing guide's Markdown conventions (H2, prose, fenced ```elixir blocks). Do not alter the other existing sections.
  </action>
  <verify>
    <automated>grep -q "## Provider-as-bundle pattern" docs/slo-authoring-guide.md && grep -q "Parapet.SLO.StarterPack.DeliverySaaS" docs/slo-authoring-guide.md && grep -q "Code.ensure_loaded?" docs/slo-authoring-guide.md && grep -q "@behaviour Parapet.SLO.Provider" docs/slo-authoring-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/slo-authoring-guide.md contains an H2 heading `## Provider-as-bundle pattern`
    - The section names `Parapet.SLO.StarterPack.DeliverySaaS` as the canonical example
    - The section contains an ```elixir example using `@behaviour Parapet.SLO.Provider` and `Code.ensure_loaded?`
    - The section explains that the bundle module remains loadable / passes `mix verify.public_api` regardless of optional-lib presence
    - The section ends with a cross-link to docs/slo-reference.md
    - Pre-existing sections ("How to decide what to slice", "Writing a custom slice", "Low-traffic and low-volume services", "What not to do") are unchanged
  </acceptance_criteria>
  <done>The SLO authoring guide has a "Provider-as-bundle pattern" H2 section documenting multi-slice provider composition with the DeliverySaaS example and conditional-registration guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-link the Provider-as-bundle section from the SLO reference (DOCS-05)</name>
  <files>docs/slo-reference.md</files>
  <read_first>
    - docs/slo-reference.md (current structure — "Starter Packs" section at line 45; the DeliverySaaS bullet is where the cross-link is appended)
    - .planning/phases/20-governance-docs-completeness/20-PATTERNS.md (section "docs/slo-reference.md (edit — add cross-link)" — exact sentence to append to the DeliverySaaS bullet)
  </read_first>
  <action>
    Edit docs/slo-reference.md: in the "Starter Packs" section, append one sentence to the `Parapet.SLO.StarterPack.DeliverySaaS` entry linking to the new authoring-guide section: "See the [Provider-as-bundle pattern](docs/slo-authoring-guide.md#provider-as-bundle-pattern) in the SLO authoring guide for how to build your own bundle provider." Use the bare relative path convention. The anchor `#provider-as-bundle-pattern` must match the heading slug created in Task 1. Do not alter other entries.
  </action>
  <verify>
    <automated>grep -q "slo-authoring-guide.md#provider-as-bundle-pattern" docs/slo-reference.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/slo-reference.md contains a Markdown link with the anchor `docs/slo-authoring-guide.md#provider-as-bundle-pattern`
    - The link sits in/near the `Parapet.SLO.StarterPack.DeliverySaaS` entry under Starter Packs
    - The anchor target matches the heading created in Task 1 (lowercase-hyphenated `provider-as-bundle-pattern`)
    - No other slo-reference.md entries are modified
  </acceptance_criteria>
  <done>docs/slo-reference.md cross-links the new Provider-as-bundle section via a matching anchor on the DeliverySaaS entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan edits two static Markdown files. No runtime code, input, or network surface is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering (doc accuracy) | Provider-as-bundle code example | mitigate | Example is verified against `lib/parapet/slo/starter_pack/delivery_saas.ex` (required in `read_first`) so the documented pattern matches the canonical implementation. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (documentation-only — RESEARCH.md "Package Legitimacy Audit" confirms zero packages). |
</threat_model>

<verification>
- `grep -q "## Provider-as-bundle pattern" docs/slo-authoring-guide.md` exits 0
- `grep -q "slo-authoring-guide.md#provider-as-bundle-pattern" docs/slo-reference.md` exits 0
- `mix test` passes (no code changed)
- Note: anchor resolution is confirmed by `mix docs --warnings-as-errors` in plan 20-05.
</verification>

<success_criteria>
The SLO authoring guide documents the Provider-as-bundle pattern with the DeliverySaaS example, and the SLO reference cross-links it via a matching anchor — satisfying DOCS-05.
</success_criteria>

<output>
Create `.planning/phases/20-governance-docs-completeness/20-04-SUMMARY.md` when done.
</output>
