---
phase: 22-release-readiness-1-0-cut
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - .planning/phases/22-release-readiness-1-0-cut/22-VERIFICATION.md
  - README.md
  - mix.exs
autonomous: true
requirements: [REL-03]

must_haves:
  truths:
    - "The proportionate 1.0 release gate is written down in one canonical artifact"
    - "The gate lists the exact automated commands and the exact manual cold-start walkthrough"
    - "The gate explicitly excludes out-of-scope hardening work"
    - "Any helper alias added for operator ergonomics mirrors the documented commands instead of hiding them"
  artifacts:
    - path: ".planning/phases/22-release-readiness-1-0-cut/22-VERIFICATION.md"
      provides: "Canonical 1.0 release verification truth surface"
      contains: "mix verify.public_api"
  key_links:
    - from: "22-VERIFICATION.md"
      to: "REL-03"
      via: "documented release gate"
      pattern: "mix dialyzer"
---

<objective>
Create the canonical proportionate 1.0 release verification truth surface: one artifact that
states the exact automated commands, the bounded manual cold-start walkthrough, and the
remaining external checks required before the final cut.

Purpose: REL-03 is not just "run some commands"; it is an explicit credibility artifact that
names the release bar and prevents scope creep into a full hardening program.

Output: a committed `22-VERIFICATION.md` (and only minimal helper wiring if truly useful)
that operators can follow before cutting 1.0.0.
</objective>

<context>
@.planning/phases/22-release-readiness-1-0-cut/22-CONTEXT.md
@.planning/phases/22-release-readiness-1-0-cut/22-RESEARCH.md
@.planning/phases/22-release-readiness-1-0-cut/22-PATTERNS.md
@.planning/phases/21-runnable-demo-app/21-VERIFICATION.md

<interfaces>
- REL-03 requires: `mix verify.public_api`, `mix test`, `mix credo --strict`, `mix dialyzer`,
  `mix compile --no-optional-deps --warnings-as-errors`, and one manual cold-start walkthrough.
- Current truth says `release_gate` branch protection is already active; the artifact should
  reflect that current state instead of repeating the stale Phase 21 gap as if unresolved.
- The artifact must explicitly state what is not part of the gate: security audit, perf
  rerun, multi-version matrix, SHA pinning, or broader maturity work.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical release verification artifact</name>
  <files>.planning/phases/22-release-readiness-1-0-cut/22-VERIFICATION.md, README.md, mix.exs</files>
  <action>
Create `.planning/phases/22-release-readiness-1-0-cut/22-VERIFICATION.md` as the canonical
release truth surface for REL-03. It should:

1. List the exact automated commands required before the cut.
2. List the exact manual cold-start walkthrough and post-publish URL checks.
3. Call out current external truths already satisfied (for example `release_gate` branch
   protection).
4. Explicitly exclude out-of-scope hardening work.

If a small helper alias in `mix.exs` or a short README pointer materially reduces operator
error, add it only if it mirrors the documented commands and does not hide the real proof
surface. Keep file scope narrow.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-release-readiness-1-0-cut/22-VERIFICATION.md && rg -n "mix verify.public_api|mix test|mix credo --strict|mix dialyzer|mix compile --no-optional-deps --warnings-as-errors|cold-start" .planning/phases/22-release-readiness-1-0-cut/22-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `22-VERIFICATION.md` exists
    - The document names every required REL-03 command
    - The document includes the manual cold-start walkthrough
    - The document explicitly excludes out-of-scope hardening items
    - Any helper alias or pointer added stays faithful to the documented commands
  </acceptance_criteria>
  <done>The 1.0 release gate is explicit, bounded, and reviewable from one canonical artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator -> release truth | Whether the final release bar is explicit and bounded |
| Historical verification -> current proof | Whether stale reports are mistaken for current truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Repudiation | vague or drifting release gate | mitigate | Write one canonical verification artifact that names the exact command/manual proof surface. |
| T-22-03B | Tampering | scope creep into unrelated hardening | mitigate | Explicitly list out-of-scope items in the artifact and keep file changes narrow. |
</threat_model>

<verification>
- `22-VERIFICATION.md` exists and contains the full REL-03 gate.
- Any helper alias or pointer added remains faithful to the documented commands.
</verification>

<success_criteria>
- REL-03 has one honest, operator-readable truth surface.
- The release gate is bounded to the chosen v1.0 freeze depth rather than widened into a full audit.
</success_criteria>

<output>
Create `.planning/phases/22-release-readiness-1-0-cut/22-03-SUMMARY.md` when done.
</output>
