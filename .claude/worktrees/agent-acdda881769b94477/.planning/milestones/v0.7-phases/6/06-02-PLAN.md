---
phase: 06
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - lib/parapet/operator/workbench_contract.ex
  - lib/parapet/operator.ex
  - test/parapet/operator/workbench_contract_test.exs
  - test/parapet/operator_test.exs
autonomous: true
requirements:
  - TRIAGE-02
  - TRIAGE-03
must_haves:
  truths:
    - "The operator detail payload exposes a compact triage block derived from durable evidence instead of parsing alert titles or free-form prose."
    - "Chronology remains the authoritative source of sequence, while the top card acts as a current-state index into that evidence."
    - "Operator-facing classification preserves the locked distinctions between provider degradation, backlog, worker failure, webhook delay, and suppression drift."
  artifacts:
    - path: "lib/parapet/operator/workbench_contract.ex"
      provides: "Deterministic triage-card derivation from incident summaries and snapshots"
    - path: "lib/parapet/operator.ex"
      provides: "Workbench-ready incident detail payload with derived triage and chronology"
  key_links:
    - from: "lib/parapet/operator/workbench_contract.ex"
      to: "lib/parapet/spine/incident.ex"
      via: "current-state summary interpretation"
    - from: "lib/parapet/operator/workbench_contract.ex"
      to: "lib/parapet/spine/timeline_entry.ex"
      via: "latest `triage_snapshot` and chronology derivation"
---

<objective>
Turn Phase 6 durable evidence into an operator-facing classification contract.

Purpose: make the workbench answer symptom, likely plane, why we think that, and safe next step from durable data rather than UI-only inference.
Output: expanded workbench derivation, richer incident detail payload, and focused tests for triage rendering inputs.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-phases/6/6-CONTEXT.md
@.planning/v0.7-phases/6/RESEARCH.md
@.planning/v0.7-phases/6/PATTERNS.md
@.planning/v0.7-phases/6/06-VALIDATION.md
@.planning/v0.7-phases/6/06-01-PLAN.md
@docs/operator-ui.md
@lib/parapet/operator/workbench_contract.ex
@lib/parapet/operator.ex
@lib/parapet/spine/incident.ex
@lib/parapet/spine/timeline_entry.ex
@test/parapet/operator/workbench_contract_test.exs
@test/parapet/operator_test.exs

<interfaces>
From lib/parapet/operator/workbench_contract.ex:
```elixir
defstruct [
  :severity,
  :affected_journey,
  :correlated_change,
  :resolved_at,
  :approval_state,
  :recommendation_state
]

def derive(%Incident{} = incident, entries) when is_list(entries)
```

From lib/parapet/operator.ex:
```elixir
def incident_detail(incident_id) do
  incident = Evidence.repo().get!(Incident, incident_id)
  entries = Evidence.repo().all(...)
  derived = WorkbenchContract.derive(incident, entries)

  %{
    incident: incident,
    entries: entries,
    derived: derived,
    external_links: extract_links(entries)
  }
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend `WorkbenchContract` for Phase 6 triage derivation</name>
  <files>lib/parapet/operator/workbench_contract.ex, test/parapet/operator/workbench_contract_test.exs</files>
  <behavior>
    - Test 1: the latest durable summary or `triage_snapshot` determines `symptom`, `fault_plane`, `impact`, `next_safe_action`, `confidence`, and `evidence_facts`.
    - Test 2: chronology ordering remains authoritative for `classification_updated_at` and related timestamps.
    - Test 3: derivation never depends on parsing the incident title or description.
  </behavior>
  <action>Expand `Parapet.Operator.WorkbenchContract` so it derives the compact Phase 6 triage card from durable evidence. Preserve existing approval and recommendation derivations, but add fields for current symptom, likely plane, operator impact, next safe action, confidence, and short ordered evidence facts. Prefer the current incident summary in `runbook_data`, fall back to the latest `triage_snapshot` where appropriate, and keep chronology-aware timestamp derivation explicit. The output should give generated UI layers enough deterministic structure that they can render the top block without becoming a second classification engine.</action>
  <verify>
    <automated>mix test test/parapet/operator/workbench_contract_test.exs</automated>
  </verify>
  <done>The workbench contract can produce a stable Phase 6 triage block directly from durable summary and chronology data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return a richer workbench-ready incident detail payload</name>
  <files>lib/parapet/operator.ex, test/parapet/operator_test.exs</files>
  <behavior>
    - Test 1: `incident_detail/1` returns the incident, ordered entries, derived triage data, and external links coherently.
    - Test 2: chronology remains available as the primary evidence surface immediately under the derived triage data.
    - Test 3: operator detail stays read-oriented and evidence-first rather than adding Phase 7 mutation or replay behavior.
  </behavior>
  <action>Update `Parapet.Operator.incident_detail/1` so the public payload cleanly exposes the Phase 6 triage contract along with ordered chronology and existing external links. Keep the boundary Phoenix-free and evidence-first per the operator docs; this plan should not add broad new operator commands or UI-owned state machines. If the payload needs small additional keys to surface chronology summaries or exact follow-up hints, keep them derived from durable evidence rather than duplicating raw alert payloads.</action>
  <verify>
    <automated>mix test test/parapet/operator_test.exs</automated>
  </verify>
  <done>The operator boundary can now return the Phase 6 triage card and chronology in one stable evidence-first contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Durable incident evidence -> operator-facing contract | Incorrect derivation would misclassify the failing plane or hide chronology behind mutable current-state wording |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | `WorkbenchContract` derivation | mitigate | Derive triage fields from bounded summary data and typed timeline entries only, never from unstructured title parsing. |
| T-06-02-02 | Repudiation | `Operator.incident_detail/1` evidence presentation | mitigate | Keep chronology entries exposed in ordered form so operators can inspect the durable rationale behind the current triage card. |
</threat_model>

<verification>
- Run the focused workbench-contract and operator tests.
- Confirm the top triage block is derived from durable summary or snapshot data only.
- Confirm chronology remains a first-class part of the detail payload.
</verification>

<success_criteria>
- `WorkbenchContract` exposes a deterministic Phase 6 triage contract.
- `Operator.incident_detail/1` returns an evidence-first payload with current-state triage and chronology.
- Operator-facing classification preserves the locked async and delivery fault-plane distinctions.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/6/06-02-SUMMARY.md`
</output>
