---
phase: 05
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - lib/parapet/slo.ex
  - lib/parapet/slo/generator.ex
  - lib/mix/tasks/parapet.gen.prometheus.ex
  - priv/templates/parapet.gen.prometheus/recording_rules.yml.eex
  - priv/templates/parapet.gen.prometheus/alerts.yml.eex
  - priv/templates/parapet.gen.prometheus/rules.yml.eex
  - docs/slo-reference.md
  - test/parapet/slo/generator_test.exs
  - test/mix/tasks/parapet.gen.prometheus_test.exs
autonomous: true
requirements:
  - DELV-02
  - DELV-03
  - ASYNC-01
  - ASYNC-02
  - ASYNC-03
must_haves:
  truths:
    - "`mix parapet.gen.prometheus` renders rules from active providers only."
    - "Generated alerts distinguish retry noise, terminal failure, backlog freshness, and webhook or callback freshness as separate operator symptoms."
    - "Generated Prometheus artifacts stay host-owned and inspectable, with separate recording and alert outputs plus a compatibility path if retained."
  artifacts:
    - path: "lib/mix/tasks/parapet.gen.prometheus.ex"
      provides: "Provider-first generator task over active built-ins"
    - path: "priv/templates/parapet.gen.prometheus/recording_rules.yml.eex"
      provides: "Recording-rule artifact template for Phase 5 slices"
    - path: "priv/templates/parapet.gen.prometheus/alerts.yml.eex"
      provides: "Symptom-first alert artifact template for Phase 5 slices"
  key_links:
    - from: "lib/mix/tasks/parapet.gen.prometheus.ex"
      to: "lib/parapet/slo.ex"
      via: "active provider aggregation only"
    - from: "lib/parapet/slo/generator.ex"
      to: "priv/templates/parapet.gen.prometheus/recording_rules.yml.eex"
      via: "provider slice-spec rendering"
    - from: "lib/parapet/slo/generator.ex"
      to: "priv/templates/parapet.gen.prometheus/alerts.yml.eex"
      via: "terminality-aware alert rendering"
---

<objective>
Turn the provider catalogs into generated Prometheus artifacts and blessed adopter docs.

Purpose: Phase 5 is not complete until active providers produce inspectable recording rules and alert rules with the right symptom-first semantics.
Output: provider-first generator plumbing, split Prometheus templates, updated docs, and executable generator tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.7-phases/5/5-CONTEXT.md
@.planning/v0.7-phases/5/RESEARCH.md
@.planning/v0.7-phases/5/PATTERNS.md
@.planning/v0.7-phases/5/05-01-PLAN.md
@.planning/v0.7-phases/5/05-02-PLAN.md
@lib/parapet/slo.ex
@lib/parapet/slo/generator.ex
@lib/mix/tasks/parapet.gen.prometheus.ex
@priv/templates/parapet.gen.prometheus/rules.yml.eex
@docs/slo-reference.md
@test/mix/tasks/parapet.gen.prometheus_test.exs

<interfaces>
From lib/parapet/slo.ex:
```elixir
def all do
  legacy_slos = Application.get_env(:parapet, :slos, [])

  provider_slos =
    Application.get_env(:parapet, :providers, [])
    |> Enum.flat_map(fn provider -> provider.slos() end)
    |> Enum.map(&Parapet.SLO.Resolvable.to_slo/1)

  legacy_slos ++ provider_slos
end
```

From lib/mix/tasks/parapet.gen.prometheus.ex:
```elixir
def igniter(igniter) do
  slos = SLO.all()
  yaml_content = EEx.eval_file(template_path, slos: slos, windows: windows)
  Igniter.create_new_file(igniter, "priv/parapet/prometheus/rules.yml", yaml_content)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rework generator plumbing around active providers and bounded slice specs</name>
  <files>lib/parapet/slo.ex, lib/parapet/slo/generator.ex, lib/mix/tasks/parapet.gen.prometheus.ex, test/parapet/slo/generator_test.exs</files>
  <behavior>
    - Test 1: generator inputs can distinguish legacy `%Parapet.SLO{}` objects from richer provider slice specs without breaking current behavior.
    - Test 2: `mix parapet.gen.prometheus` renders from active providers only, with no hidden built-in registration or `attach/1` side effects.
    - Test 3: slice metadata drives page, ticket, and warning or diagnostic classes so retry noise and terminal failure render differently.
  </behavior>
  <action>Refactor the generator pipeline per D-24 through D-30 so the blessed Phase 5 path is provider-first and active-provider-only per D-27. Add whatever aggregation helpers are needed in `Parapet.SLO` or `Parapet.SLO.Generator` to collect the slice-spec catalog from configured providers, preserve legacy `%Parapet.SLO{}` support only as a compatibility path per D-40, and keep the mix task thin and host-owned. The generator must separate retry noise from terminal or user-harming failure, preserve callback/webhook freshness versus backlog as different fault planes, and carry bounded grouping labels like `integration`, `fault_plane`, `queue`, `channel`, or `pipeline_stage` per D-20.</action>
  <verify>
    <automated>mix test test/parapet/slo/generator_test.exs</automated>
  </verify>
  <done>The generator can render Phase 5 provider slices coherently, and the active-provider-only contract is proven in focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate split Prometheus artifacts and document the blessed Phase 5 path</name>
  <files>priv/templates/parapet.gen.prometheus/recording_rules.yml.eex, priv/templates/parapet.gen.prometheus/alerts.yml.eex, priv/templates/parapet.gen.prometheus/rules.yml.eex, docs/slo-reference.md, test/mix/tasks/parapet.gen.prometheus_test.exs</files>
  <behavior>
    - Test 1: the mix task creates `recording_rules.yml` and `alerts.yml` from active providers, and any retained `rules.yml` compatibility file is assembled from the same source of truth.
    - Test 2: generated alerts include `for` durations, minimum-volume or freshness guards, and different severities for page versus ticket or warning semantics.
    - Test 3: generated output keeps callback/webhook freshness separate from backlog and treats suppression drift or long-running stages as non-paging diagnostics.
  </behavior>
  <action>Replace the one-template generic burn-rate output with split host-owned templates per D-28 and D-41. `recording_rules.yml.eex` should render the slice recordings and helper series; `alerts.yml.eex` should render symptom-first alerts with terminality-aware taxonomy per D-16 through D-22; `rules.yml.eex` may remain only as a compatibility aggregate if execution needs it to reduce migration risk, but the docs must present the split files as the blessed path per D-40. Update `docs/slo-reference.md` so adopters learn the provider registration seam, the exact built-in modules, and the fact that `mix parapet.gen.prometheus` reflects active providers only rather than hidden defaults.</action>
  <verify>
    <automated>mix test test/mix/tasks/parapet.gen.prometheus_test.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <done>The repo generates inspectable split Prometheus artifacts from active providers only, and the reference docs describe the Phase 5 activation and artifact model accurately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated rules -> Prometheus and Alertmanager | Incorrect or noisy rule generation pages humans or hides user-harming failure |
| SLO docs -> adopters | Wrong activation guidance would cause hidden defaults or missing provider coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Denial of Service | Prometheus alert generation | mitigate | Encode `for` durations, minimum-volume guards, and terminality-aware severity so transient retries and low-traffic blips do not page by default. |
| T-05-03-02 | Tampering | `mix parapet.gen.prometheus` activation semantics | mitigate | Read from configured providers only and keep any legacy compatibility output derived from the same explicit source catalog rather than hidden built-ins. |
</threat_model>

<verification>
- Run the focused generator tests and the mix-task test.
- Confirm the generated artifacts are sourced from configured providers only.
- Validate that callback/webhook freshness and backlog render as separate alert paths.
</verification>

<success_criteria>
- `mix parapet.gen.prometheus` becomes a provider-first, active-provider-only generator.
- Generated artifacts page on terminal or user-harming symptoms and downgrade retry noise or diagnostics appropriately.
- The blessed host-owned artifact layout is explicit in both generated files and docs.
</success_criteria>

<output>
After completion, create `.planning/v0.7-phases/5/05-03-SUMMARY.md`
</output>
