---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/operator.ex
  - lib/parapet/escalation/worker.ex
  - test/parapet/operator_test.exs
  - test/parapet/escalation/worker_test.exs
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "Manual escalation trigger and suppression are durable operator commands, not UI-only toggles or direct Oban job surgery."
    - "The escalation worker remains the authoritative truth gate and honors bounded suppression state."
    - "Every risky escalation control writes timeline and audit evidence atomically through the operator seam."
  artifacts:
    - path: lib/parapet/operator.ex
      provides: Audited manual escalation and suppression command APIs
    - path: lib/parapet/escalation/worker.ex
      provides: Worker-visible suppression and typed escalation chronology
  key_links:
    - from: lib/parapet/operator.ex
      to: lib/parapet/escalation/worker.ex
      via: durable suppression and manual-trigger state consumed at execution time
    - from: lib/parapet/operator.ex
      to: test/parapet/operator_test.exs
      via: bounded command behavior and audit semantics
---

<objective>
Create the durable escalation control seam for Phase 4.

Purpose: make manual trigger and suppression real operator commands with durable evidence and worker-visible semantics before any UI renders them.
Output: new operator APIs, worker gating logic, and focused tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v0.8-phases/4/4-CONTEXT.md
@.planning/milestones/v0.8-phases/4/RESEARCH.md
@.planning/milestones/v0.8-phases/4/PATTERNS.md
@.planning/milestones/v0.8-phases/4/VALIDATION.md
@lib/parapet/operator.ex
@lib/parapet/escalation/worker.ex
@lib/parapet/evidence.ex
@test/parapet/operator_test.exs
@test/parapet/escalation/worker_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add audited manual escalation and suppression commands</name>
  <files>lib/parapet/operator.ex, test/parapet/operator_test.exs</files>
  <behavior>
    - Test 1: `trigger_next_escalation/2` writes a typed timeline entry and audit row through the operator seam.
    - Test 2: `suppress_pending_escalation/3` records bounded expiring suppression state distinct from acknowledge or resolve.
    - Test 3: invalid payloads or invalid suppression windows fail cleanly without mutating incident state.
  </behavior>
  <action>Add Phase 4 operator APIs for bounded manual escalation control. Keep them Phoenix-free and route all writes through `Parapet.Evidence.run_operator_command/1`. `trigger_next_escalation/2` should express explicit operator intent and, when appropriate, enqueue or mark a worker-consumable trigger without becoming direct job surgery. `suppress_pending_escalation/3` should persist only narrow current-state data such as reason, actor, and expiry, while appending explicit chronology that keeps suppression separate from acknowledge or resolve semantics.</action>
  <verify>
    <automated>mix test test/parapet/operator_test.exs</automated>
  </verify>
  <done>Phase 4 has a durable operator-owned command seam for escalation trigger and suppression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teach the escalation worker to honor durable suppression and emit typed evidence</name>
  <files>lib/parapet/escalation/worker.ex, test/parapet/escalation/worker_test.exs</files>
  <behavior>
    - Test 1: scheduled escalation short-circuits when a valid suppression window is active.
    - Test 2: short-circuit chronology explains whether the skip came from state, suppression, or other bounded reasons.
    - Test 3: normal execution still appends durable escalation evidence and does not trust UI-only hints.
  </behavior>
  <action>Extend `Parapet.Escalation.Worker` so it reads the bounded escalation command state from the incident and remains the truth gate for execution. It should short-circuit on active suppression, append typed timeline evidence for that branch, and preserve the existing state-based short-circuit behavior for investigating or resolved incidents. If a manual trigger path is introduced, keep worker invocation and evidence semantics consistent with scheduled execution so the UI does not create a separate escalation narrative.</action>
  <verify>
    <automated>mix test test/parapet/escalation/worker_test.exs</automated>
  </verify>
  <done>Worker execution is now suppression-aware and writes typed escalation chronology that later plans can project safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator intent -> durable escalation state | Risky manual controls must survive restarts and remain auditable |
| Durable state -> worker execution | Worker must not trust UI-only state or hidden scheduler mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `Parapet.Operator` escalation commands | mitigate | Persist only bounded escalation control fields through the existing atomic operator seam. |
| T-04-01-02 | Repudiation | Manual trigger and suppression | mitigate | Every control writes typed timeline and audit evidence with explicit actor and reason. |
| T-04-01-03 | Denial of Service | Suppression or trigger handling | mitigate | Keep `Parapet.Escalation.Worker` as the final truth gate and reject unbounded or invalid suppression windows. |
</threat_model>

<verification>
- Run the operator and escalation worker tests.
- Confirm manual escalation controls remain distinct from acknowledge or resolve semantics.
- Confirm suppression is durable and worker-visible rather than hidden scheduler mutation.
</verification>

<success_criteria>
- Operators can durably trigger the next escalation and temporarily suppress pending escalation through audited commands.
- Worker execution honors suppression state and explains short-circuits through typed chronology.
- The backend seam is ready for a derived UI projection without inventing alternate truth.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.8-phases/4/04-01-SUMMARY.md`
</output>
