---
phase: 03-operator-ui-performance
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - lib/parapet/operator.ex
  - bench/operator_ui_perf.exs
  - docs/operator-ui.md
  - test/parapet/operator/queue_pagination_test.exs
autonomous: true
requirements:
  - SCALE-01
must_haves:
  truths:
    - Phase 3 leaves behind a reproducible proof surface instead of anecdotal “it feels faster” claims.
    - Queue/list telemetry remains low-cardinality and safe for public observability contracts.
    - The 50k+ benchmark lane is advisory and reproducible without becoming a default merge blocker.
  artifacts:
    - path: lib/parapet/operator.ex
      provides: Low-cardinality instrumentation around bounded queue fetch behavior
    - path: bench/operator_ui_perf.exs
      provides: Reproducible 50k+ queue fetch and first-render benchmark harness
    - path: docs/operator-ui.md
      provides: Operator UI performance contract and guidance for adopters
  key_links:
    - from: lib/parapet/operator.ex
      to: bench/operator_ui_perf.exs
      via: benchmarked bounded queue API and perf telemetry surface
    - from: docs/operator-ui.md
      to: bench/operator_ui_perf.exs
      via: documented proof workflow and advisory lane instructions
---

<objective>
Add the proof surfaces for Phase 3.

Purpose: make queue performance auditable through low-cardinality telemetry, deterministic tests, and a reproducible 50k+ benchmark lane rather than informal inspection.
Output: telemetry instrumentation, advisory benchmark harness, and operator-ui docs updates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/v0.9-phases/3/3-CONTEXT.md
@.planning/v0.9-phases/3/RESEARCH.md
@.planning/v0.9-phases/3/VALIDATION.md
@lib/parapet/operator.ex
@docs/operator-ui.md
@test/parapet/operator/queue_pagination_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instrument bounded queue behavior with low-cardinality proof events</name>
  <files>lib/parapet/operator.ex, test/parapet/operator/queue_pagination_test.exs</files>
  <read_first>
    - lib/parapet/operator.ex
    - .planning/v0.9-phases/3/RESEARCH.md
    - .planning/PROJECT.md
    - test/parapet/operator/queue_pagination_test.exs
  </read_first>
  <behavior>
    - Queue fetch or page-load instrumentation emits only bounded labels such as scope, direction, page-size bucket, and result bucket.
    - Telemetry never emits incident ids, raw cursor payloads, or other high-cardinality values.
    - Tests prove the instrumentation hooks exist at the bounded queue seam.
  </behavior>
  <action>
    Add queue performance proof instrumentation to the bounded queue API in `lib/parapet/operator.ex` using the project’s existing telemetry discipline. Emit exact low-cardinality metadata categories only: queue scope, direction, page size bucket, result-size bucket, and timing measurements. Do not emit incident ids, raw cursor values, or full titles. Extend `test/parapet/operator/queue_pagination_test.exs` or companion assertions so the instrumentation seam is covered alongside the queue contract.
  </action>
  <acceptance_criteria>
    - `lib/parapet/operator.ex` contains telemetry execution for queue/page behavior
    - `lib/parapet/operator.ex` does not contain telemetry metadata keys for raw cursor values or incident ids in the new instrumentation
    - `mix test test/parapet/operator/queue_pagination_test.exs -x` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/parapet/operator/queue_pagination_test.exs -x</automated>
  </verify>
  <done>The bounded queue seam exposes safe public proof signals for performance behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add a reproducible 50k+ benchmark lane and document how to run it</name>
  <files>bench/operator_ui_perf.exs, docs/operator-ui.md</files>
  <read_first>
    - docs/operator-ui.md
    - .planning/v0.9-phases/3/RESEARCH.md
    - .planning/v0.9-phases/3/VALIDATION.md
  </read_first>
  <behavior>
    - A benchmark script or equivalent dedicated task deterministically seeds exactly a 50k+ incident dataset and measures queue fetch plus actual generated UI first-render behavior for one bounded page.
    - The benchmark lane is explicitly advisory and not part of the default `mix test` merge gate.
    - The benchmark harness uses only repo/runtime tooling already present in the project and does not add new Hex dependencies.
    - Documentation tells adopters exactly how to run the benchmark and what success signal to look for.
  </behavior>
  <action>
    Create `bench/operator_ui_perf.exs` as the advisory benchmark lane described in `RESEARCH.md`, targeting the bounded queue API and the actual generated Operator LiveView first render for one bounded page at a 50k+ incident dataset. The harness must deterministically seed exactly the benchmark dataset itself, using a reproducible bootstrap path inside `bench/operator_ui_perf.exs` or an exact prerequisite command documented in the plan, and then render the generated UI path used by adopters. Capture both queue-fetch cost and first-render timing/shape for the bounded page. The harness must be dependency-free: use only existing Mix, Ecto, Repo, timer, and telemetry/runtime facilities already in the project rather than adding Benchee or other new packages. Keep the lane opt-in and outside the default merge gate. Update `docs/operator-ui.md` with exact instructions for running `mix run bench/operator_ui_perf.exs`, what deterministic seeding/bootstrap it performs, what it proves, and how the queue refresh behavior is intentionally operator-paced rather than silently live-reordered.
  </action>
  <acceptance_criteria>
    - `bench/operator_ui_perf.exs` exists
    - `bench/operator_ui_perf.exs` contains a deterministic dataset bootstrap path for 50k+ incidents
    - `docs/operator-ui.md` contains a section describing the benchmark or performance proof lane
    - `docs/operator-ui.md` documents that the benchmark is advisory rather than a default merge gate
    - `mix run bench/operator_ui_perf.exs` is documented as the benchmark entrypoint
    - `bench/operator_ui_perf.exs` explicitly exercises generated UI first render for one bounded page at a 50k+ incident dataset
    - `mix run bench/operator_ui_perf.exs` completes as the automated proof command for the advisory lane
  </acceptance_criteria>
  <verify>
    <automated>mix run bench/operator_ui_perf.exs</automated>
  </verify>
  <done>Phase 3 has a repeatable 50k+ proof lane and docs that explain how to use it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Queue instrumentation -> observability backend | Performance proof signals must stay low-cardinality and privacy-safe |
| Advisory benchmark lane -> developer workflow | Benchmark tooling must not become an accidental default merge blocker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | Queue telemetry metadata | mitigate | Emit only bounded buckets and queue scope metadata, never raw cursor or incident identifiers. |
| T-03-08 | Denial of Service | Benchmark lane defaults | mitigate | Keep the benchmark opt-in and outside the default `mix test`/CI merge gate. |
| T-03-09 | Repudiation | Performance proof claims | mitigate | Document the benchmark command and capture reproducible queue-proof behavior rather than relying on informal observations. |
</threat_model>

<verification>
- Run `mix test test/parapet/operator/queue_pagination_test.exs -x`.
- Inspect telemetry metadata to confirm it is low-cardinality and excludes incident ids/cursor payloads.
- Run or at minimum document `mix run bench/operator_ui_perf.exs` as the advisory 50k+ queue-fetch plus generated-UI first-render lane.
</verification>

<success_criteria>
- Phase 3 ships low-cardinality queue performance proof signals.
- A reproducible advisory benchmark exists for 50k+ incidents and measures actual generated UI first render for one bounded page.
- The benchmark lane is dependency-free and runnable with the repo's existing toolchain.
- Operator UI docs explain the bounded queue model and its performance proof workflow.
</success_criteria>

<output>
After completion, create `.planning/v0.9-phases/3/03-03-SUMMARY.md`
</output>
