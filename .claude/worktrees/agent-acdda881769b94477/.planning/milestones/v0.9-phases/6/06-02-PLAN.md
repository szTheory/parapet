---
phase: 06-verify-cardinality-protection
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .planning/v0.9-phases/1/VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/01-cardinality-protection/01-01-SUMMARY.md
  - .planning/phases/01-cardinality-protection/01-UAT.md
autonomous: true
requirements:
  - PERF-01.a
  - PERF-01.b
must_haves:
  truths:
    - The required reconciliation surfaces now agree that Phase 1 is verified for `PERF-01.a` and `PERF-01.b`.
    - Phase 1 validation wording points at the new verification artifact and preserves the honest `skip` boundary for the live doctor command.
    - Any legacy Phase 1 summary or UAT wording that would mislead maintainers about doctor exit codes or current workspace behavior is either corrected or explicitly left unchanged because no contradiction remains.
  artifacts:
    - path: .planning/v0.9-phases/1/VALIDATION.md
      provides: Nyquist validation wording reconciled to the new Phase 1 verification artifact.
    - path: .planning/REQUIREMENTS.md
      provides: Direct requirement-state closure for `PERF-01.a` and `PERF-01.b`.
    - path: .planning/phases/01-cardinality-protection/01-01-SUMMARY.md
      provides: Required honesty fix removing the stale claim that a live doctor run validated the current workspace successfully.
    - path: .planning/phases/01-cardinality-protection/01-UAT.md
      provides: Required honesty fix aligning unsafe-finding exit-code wording to current doctor semantics.
  key_links:
    - from: .planning/v0.9-phases/1/VALIDATION.md
      to: .planning/v0.9-phases/1/VERIFICATION.md
      via: explicit verification-method references for `PERF-01.a` and `PERF-01.b`
      pattern: VERIFICATION.md
    - from: .planning/REQUIREMENTS.md
      to: .planning/v0.9-phases/1/VERIFICATION.md
      via: checked requirement bullets and traceability statuses updated only after verification exists
      pattern: PERF-01
    - from: .planning/phases/01-cardinality-protection/01-UAT.md
      to: lib/mix/tasks/parapet.doctor.ex
      via: corrected exit-code wording for `error` versus execution failure
      pattern: Exit codes
---

<objective>
Reconcile only the artifacts directly affected by the new Phase 1 verification evidence, without drifting into milestone-wide cleanup.

Purpose: turn the new proof into accurate local traceability for `PERF-01.a` and `PERF-01.b`, and remove only the stale Phase 1 wording that would otherwise remain misleading per D-08, D-09, and D-10.
Output: updated `.planning/v0.9-phases/1/VALIDATION.md`, updated `.planning/REQUIREMENTS.md`, and required honesty fixes to the stale Phase 1 summary/UAT files.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v0.9-MILESTONE-AUDIT.md
@.planning/v0.9-phases/6/06-CONTEXT.md
@.planning/v0.9-phases/6/RESEARCH.md
@.planning/v0.9-phases/6/06-PATTERNS.md
@.planning/v0.9-phases/1/VERIFICATION.md
@.planning/v0.9-phases/1/VALIDATION.md
@.planning/phases/01-cardinality-protection/01-01-SUMMARY.md
@.planning/phases/01-cardinality-protection/01-UAT.md
@lib/mix/tasks/parapet.doctor.ex

<interfaces>
From .planning/REQUIREMENTS.md:
```markdown
### PERF-01: TSDB Cardinality Protection
- [ ] System provides a `mix parapet.doctor cardinality` sub-command ...
- [ ] System strictly limits the number of labels per metric at compile-time ...
```

From lib/mix/tasks/parapet.doctor.ex:
```elixir
# Exit codes:
# - 0: no findings at or above the active threshold
# - 1: at least one finding at or above the active threshold
# - 2: doctor execution failed or a runtime probe could not run
```
</interfaces>
</context>

<tasks>

  <task type="auto">
  <name>Task 1: Reconcile the required proof-tracking artifacts only per D-08, D-09, and D-10</name>
  <files>.planning/v0.9-phases/1/VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <action>
    Update `.planning/v0.9-phases/1/VALIDATION.md` so it no longer uses the single compressed `[PERF-01]` row. Split the coverage into explicit `PERF-01.a` and `PERF-01.b` rows, point each row at the new `.planning/v0.9-phases/1/VERIFICATION.md` plus the exact proof methods behind it, and make the gap analysis say that the earlier gap is closed by the new verification artifact rather than by summary-only implementation claims. Then update `.planning/REQUIREMENTS.md` narrowly: flip only the two `PERF-01` checklist bullets to checked, and change only the two `PERF-01.a` and `PERF-01.b` traceability statuses from `Pending` to `Verified`. Do not edit `ROADMAP.md`, `STATE.md`, or any unrelated requirement rows; broad milestone synchronization remains Phase 9 per D-10.
  </action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
validation = Path(".planning/v0.9-phases/1/VALIDATION.md").read_text()
requirements = Path(".planning/REQUIREMENTS.md").read_text()

assert "| PERF-01.a |" in validation
assert "| PERF-01.b |" in validation
assert ".planning/v0.9-phases/1/VERIFICATION.md" in validation
assert "- [x] System provides a `mix parapet.doctor cardinality` sub-command to statically analyze metrics configurations and flag unsafe label patterns." in requirements
assert "- [x] System strictly limits the number of labels per metric at compile-time to prevent accidental TSDB explosion." in requirements
assert "| PERF-01.a | Phase 6 | Verified |" in requirements
assert "| PERF-01.b | Phase 6 | Verified |" in requirements
assert "| SCALE-01.a | Phase 2 | Verified |" in requirements
assert "| SCALE-01.b | Phase 2 | Verified |" in requirements
assert "| SCALE-01.c | Phase 7 | Pending |" in requirements
assert requirements.count("| PERF-01.a | Phase 6 | Verified |") == 1
assert requirements.count("| PERF-01.b | Phase 6 | Verified |") == 1
print("Phase 1 validation and PERF-01 traceability rows reconciled exactly.")
PY</automated>
  </verify>
  <done>The required Phase 1 validation and requirement-traceability surfaces now point at the new verification artifact and close only the directly covered `PERF-01` gap.</done>
</task>

<task type="auto">
  <name>Task 2: Apply required honesty fixes to the stale Phase 1 summary/UAT wording per D-09</name>
  <files>.planning/phases/01-cardinality-protection/01-01-SUMMARY.md, .planning/phases/01-cardinality-protection/01-UAT.md</files>
  <action>
    Apply the concrete Phase 1 honesty corrections required by D-09. In `.planning/phases/01-cardinality-protection/01-01-SUMMARY.md`, replace the stale claim that `mix parapet.doctor cardinality` "validates the current system configuration successfully" with wording that reflects the bounded live-workspace truth: the command exists and currently may return `skip` when no SLOs are configured, so closure proof comes from the targeted doctor tests rather than that live invocation alone. In `.planning/phases/01-cardinality-protection/01-UAT.md`, replace the stale expectation that unsafe doctor findings exit with code `2`; the corrected wording must say unsafe findings are ordinary `error` results that exit with code `1`, while code `2` is reserved for doctor execution failure or runtime probe failure. These legacy edits are required because the contradictions already exist today; do not leave the files unchanged.
  </action>
  <verify>
    <automated>! rg -n "validates the current system configuration successfully|unsafe.*exit with code 2|fatal.*exit with code 2" .planning/phases/01-cardinality-protection/01-01-SUMMARY.md .planning/phases/01-cardinality-protection/01-UAT.md</automated>
  </verify>
  <done>No stale Phase 1 artifact still contradicts the new verification report about live doctor `skip` behavior or current exit-code semantics, and both known contradictions have been corrected directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification artifact -> requirements/validation state | Requirement status should change only after the new proof exists. |
| Legacy Phase 1 docs -> future maintainer interpretation | Old wording can mislead later audits if not reconciled to current doctor semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Repudiation | `.planning/REQUIREMENTS.md` | mitigate | Flip only the directly verified `PERF-01` statuses after Plan 1 creates the proof artifact. |
| T-06-05 | Tampering | legacy Phase 1 summary/UAT wording | mitigate | Correct only the stale claims about live `skip` behavior and exit-code semantics; leave unrelated history intact. |
| T-06-06 | Denial of Service | milestone closure workflow | accept | This plan intentionally does not absorb broader `ROADMAP.md` and `STATE.md` cleanup; that remaining work is explicitly deferred to Phase 9. |
</threat_model>

<verification>
- Confirm `.planning/v0.9-phases/1/VALIDATION.md` references `.planning/v0.9-phases/1/VERIFICATION.md` and splits `PERF-01.a` from `PERF-01.b`.
- Confirm `.planning/REQUIREMENTS.md` marks only the two `PERF-01` bullets and traceability rows as verified.
- Confirm any changed legacy Phase 1 summary/UAT wording no longer contradicts `skip` semantics or doctor exit codes.
</verification>

<success_criteria>
- The required reconciliation files are updated and no longer leave `PERF-01.a` or `PERF-01.b` orphaned.
- Legacy summary/UAT wording is corrected directly where it currently misleads maintainers about current proof or exit semantics.
- Phase 6 stays narrow: no `ROADMAP.md` or `STATE.md` cleanup is pulled into this closure phase.
</success_criteria>

<output>
After completion, create `.planning/v0.9-phases/6/06-02-SUMMARY.md`
</output>
