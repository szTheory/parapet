---
phase: 17-recovery-depth-runbook-templates
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - priv/templates/parapet.gen.runbooks/retry_storm.ex.eex
  - priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex
  - priv/templates/parapet.gen.runbooks/partial_backlog_drain.ex.eex
  - lib/mix/tasks/parapet.gen.runbooks.ex
  - test/mix/tasks/parapet.gen.runbooks_test.exs
autonomous: true
requirements: [RCV-02]
must_haves:
  truths:
    - "retry_storm exists at full depth with a guidance-only mitigation (NOT :retry_async_item — retrying worsens a storm, RESEARCH correction)"
    - "suppression_drift exists at full depth, guidance-only throughout (no allowlisted capability fits escalation suppression)"
    - "partial_backlog_drain exists at full depth with a :retry_async_item / target_kind: :async_item / requires_preview: true mitigation"
    - "the generator copies all three new templates with on_exists: :skip (host-ownership / no-clobber contract preserved)"
    - "the generator-content regression test asserts the three new files generate, each contains warning:, and each deepened existing template contains warning:"
  artifacts:
    - path: "priv/templates/parapet.gen.runbooks/retry_storm.ex.eex"
      provides: "guidance-only retry_storm template at full depth"
      contains: "warning:"
    - path: "priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex"
      provides: "guidance-only suppression_drift template at full depth"
      contains: "warning:"
    - path: "priv/templates/parapet.gen.runbooks/partial_backlog_drain.ex.eex"
      provides: "partial_backlog_drain template wiring :retry_async_item preview-first"
      contains: "capability: :retry_async_item"
    - path: "lib/mix/tasks/parapet.gen.runbooks.ex"
      provides: "three new copy_template calls, each on_exists: :skip"
      contains: "on_exists: :skip"
    - path: "test/mix/tasks/parapet.gen.runbooks_test.exs"
      provides: "generator-content regression layer (D-11 #3)"
  key_links:
    - from: "lib/mix/tasks/parapet.gen.runbooks.ex copy_template calls"
      to: "priv/templates/parapet.gen.runbooks/*.ex.eex (3 new)"
      via: "Igniter.copy_template(... on_exists: :skip)"
      pattern: "on_exists: :skip"
    - from: "partial_backlog_drain mitigation"
      to: "Parapet.Capabilities allowlist (:retry_async_item)"
      via: "capability: :retry_async_item, target_kind: :async_item, requires_preview: true"
      pattern: "capability: :retry_async_item"
---

<objective>
Author the three new prebuilt runbook templates at the same RCV-02 depth as the deepened four, wire them into the generator (explicit `copy_template` per file, each with `on_exists: :skip`), and extend the generator-content regression test (D-11 layer 3) to assert the new files generate, carry `warning:`, and that deepened existing templates carry `warning:` too.

Purpose: An operator needs three additional trustworthy templates — `retry_storm`, `suppression_drift`, `partial_backlog_drain`. Per the RESEARCH.md D-07 correction, `retry_storm` is GUIDANCE-ONLY (executing `:retry_async_item` on storming items worsens the storm — overrides CONTEXT.md D-07/Specific Ideas). `suppression_drift` is also guidance-only (no allowlisted capability fits escalation suppression). Only `partial_backlog_drain` legitimately wires `:retry_async_item` with `target_kind: :async_item` (stuck-subset retry is an exact semantic fit).

Output: Three new `.ex.eex` templates, three new generator copy_template calls (on_exists: :skip preserved), and the generator-content regression layer green.

DEPENDS ON 17-01: These templates use `warning:`. The 17-01 DSL surface MUST render warning end-to-end before these templates reference it, or Elixir silently swallows the keyword (D-01, sequencing constraint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-recovery-depth-runbook-templates/17-RESEARCH.md
@.planning/phases/17-recovery-depth-runbook-templates/17-PATTERNS.md
@.planning/phases/17-recovery-depth-runbook-templates/17-VALIDATION.md

<interfaces>
<!-- Allowlisted capabilities (lib/parapet/capabilities.ex:8-12) — ONLY these three (D-05). NO new capability ids. -->
valid capabilities: :retry_async_item, :requeue_dead_letter, :request_manual_provider_check

<!-- Per-template mitigation routing (RESEARCH.md D-07, corrected): -->
retry_storm           -> GUIDANCE-ONLY (type: :mitigation, kind: :guidance, preview_only: true) — :retry_async_item is WRONG here
suppression_drift     -> GUIDANCE-ONLY (no fitting capability)
partial_backlog_drain -> :retry_async_item, target_kind: :async_item, requires_preview: true (exact fit)

<!-- Generator pattern (lib/mix/tasks/parapet.gen.runbooks.ex:32-56): four existing Igniter.copy_template calls, each priv-dir source -> lib_dir dest, assigns, on_exists: :skip. assigns (line 26-30) already has module_prefix/app_name/base_name. Add three more calls AFTER line 56, BEFORE the |> Igniter.add_notice. -->

<!-- Generator-content test (test/mix/tasks/parapet.gen.runbooks_test.exs:8-48): test_project(app_name: :test); generated module names are Test.Parapet.Runbooks.<Name>; file paths are lib/test/parapet/runbooks/<name>.ex; assertions use `source =~ "..."`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author retry_storm and suppression_drift (guidance-only)</name>
  <files>priv/templates/parapet.gen.runbooks/retry_storm.ex.eex, priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex</files>
  <read_first>
    - priv/templates/parapet.gen.runbooks/callback_delay.ex.eex (the guidance-only, no-capability analog to mirror)
    - .planning/phases/17-recovery-depth-runbook-templates/17-PATTERNS.md (sections "retry_storm.ex.eex — NEW template (guidance-only)" and "suppression_drift.ex.eex — NEW template (guidance-only)" — full step skeletons)
    - .planning/phases/17-recovery-depth-runbook-templates/17-RESEARCH.md (D-07 per-template analysis, lines 236-256; Pitfall 6 retry_storm misassignment, lines 495-497)
    - lib/parapet/capabilities.ex (lines 8-12 — confirm there is NO capability that fits a storm or suppression-drift scenario)
  </read_first>
  <action>
    Create priv/templates/parapet.gen.runbooks/retry_storm.ex.eex with module `<%= inspect(@module_prefix) %>.RetryStorm`, `use Parapet.Runbook`, title/description, and three steps: (1) `:assess_storm` precondition (type: :manual, kind: :guidance, preview_only: true) with a warning that retry-accelerating mitigations worsen the storm; (2) `:reduce_retry_pressure` GUIDANCE-ONLY mitigation (type: :mitigation, kind: :guidance, preview_only: true — NO capability) instructing the operator to increase backoff / reduce concurrency / pause the queue via host tooling, with a warning about delaying legitimate work; (3) `:verify_storm_cleared` verification (type: :manual, kind: :guidance, preview_only: true). DO NOT use capability: :retry_async_item — retrying items during a storm worsens it (RESEARCH D-07 correction overrides CONTEXT.md).

    Create priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex with module `<%= inspect(@module_prefix) %>.SuppressionDrift`, three steps all guidance: (1) `:identify_drifted_suppressions` precondition with a warning that stale suppressions silently block escalations; (2) `:clear_stale_suppressions` GUIDANCE-ONLY mitigation (no capability — none of the three fits escalation suppression) with a warning that clearing may immediately trigger escalation; (3) `:verify_escalation_restored` verification. NO capability: id anywhere in this file.

    Both files: precondition and verification are DISTINCT type: :manual, kind: :guidance steps (D-10). Mirror callback_delay.ex.eex structure and the existing templates' voice. Author label/description/guidance/warning text per the PATTERNS.md skeletons (Claude's discretion on exact wording).
  </action>
  <verify>
    <automated>grep -L 'capability:' priv/templates/parapet.gen.runbooks/retry_storm.ex.eex priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex</automated>
  </verify>
  <acceptance_criteria>
    - retry_storm.ex.eex and suppression_drift.ex.eex both exist
    - `grep -c 'capability:' priv/templates/parapet.gen.runbooks/retry_storm.ex.eex` == 0
    - `grep -c 'capability:' priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex` == 0
    - `grep -c 'warning:' priv/templates/parapet.gen.runbooks/retry_storm.ex.eex` >= 1
    - `grep -c 'warning:' priv/templates/parapet.gen.runbooks/suppression_drift.ex.eex` >= 1
    - each file contains a `type: :mitigation` step with `kind: :guidance` and a verification step with id :verify_storm_cleared (retry_storm) / :verify_escalation_restored (suppression_drift)
    - each file's module header is `defmodule <%= inspect(@module_prefix) %>.RetryStorm do` / `...SuppressionDrift do`
  </acceptance_criteria>
  <done>retry_storm and suppression_drift exist at full depth, both guidance-only with no capability dispatch, each with precondition + mitigation + >=1 warning: + distinct verification.</done>
</task>

<task type="auto">
  <name>Task 2: Author partial_backlog_drain, wire all three into the generator</name>
  <files>priv/templates/parapet.gen.runbooks/partial_backlog_drain.ex.eex, lib/mix/tasks/parapet.gen.runbooks.ex</files>
  <read_first>
    - priv/templates/parapet.gen.runbooks/stalled_executor.ex.eex (the exact analog — same :retry_async_item / :async_item / requires_preview pattern)
    - lib/mix/tasks/parapet.gen.runbooks.ex (the file being modified — the four existing copy_template calls lines 32-56, assigns lines 26-30, the |> Igniter.add_notice that follows line 56)
    - .planning/phases/17-recovery-depth-runbook-templates/17-PATTERNS.md (sections "partial_backlog_drain.ex.eex — NEW template" and "lib/mix/tasks/parapet.gen.runbooks.ex — add three copy_template calls")
    - lib/parapet/capabilities.ex (lines 8-12 — confirm :retry_async_item is allowlisted)
  </read_first>
  <action>
    Create priv/templates/parapet.gen.runbooks/partial_backlog_drain.ex.eex with module `<%= inspect(@module_prefix) %>.PartialBacklogDrain`, `use Parapet.Runbook`, title/description, and three steps: (1) `:identify_stuck_items` precondition (type: :manual, kind: :guidance, preview_only: true) with a warning that retrying healthy items causes duplicate side effects; (2) `:retry_stuck_items` mitigation (type: :mitigation, kind: :capability, capability: :retry_async_item, target_kind: :async_item, requires_preview: true) with a warning to review the preview count before confirming; (3) `:verify_drain` verification (type: :manual, kind: :guidance, preview_only: true). Mirror stalled_executor.ex.eex exactly for the capability mitigation shape.

    In lib/mix/tasks/parapet.gen.runbooks.ex, add three new `|> Igniter.copy_template(...)` calls AFTER the last existing call (after line 56) and BEFORE `|> Igniter.add_notice(...)`. Each call mirrors the existing structure exactly: source `Path.join([:code.priv_dir(:parapet), "templates", "parapet.gen.runbooks", "<name>.ex.eex"])`, dest `Path.join([lib_dir, "<name>.ex"])`, `assigns`, `on_exists: :skip`. The three names: retry_storm, suppression_drift, partial_backlog_drain. EVERY new call MUST pass `on_exists: :skip` (host-ownership / no-clobber contract — criterion 3, D-08). Reuse the existing `assigns` variable unchanged (it already has module_prefix/app_name/base_name).
  </action>
  <verify>
    <automated>grep -c 'on_exists: :skip' lib/mix/tasks/parapet.gen.runbooks.ex</automated>
  </verify>
  <acceptance_criteria>
    - partial_backlog_drain.ex.eex exists, module header `defmodule <%= inspect(@module_prefix) %>.PartialBacklogDrain do`
    - partial_backlog_drain.ex.eex contains `capability: :retry_async_item`, `target_kind: :async_item`, `requires_preview: true`, >=1 `warning:`, and a verification step id :verify_drain
    - `grep -c 'on_exists: :skip' lib/mix/tasks/parapet.gen.runbooks.ex` == 7 (4 existing + 3 new)
    - lib/mix/tasks/parapet.gen.runbooks.ex contains copy_template calls referencing "retry_storm.ex.eex", "suppression_drift.ex.eex", "partial_backlog_drain.ex.eex"
    - the three new copy_template calls precede the `Igniter.add_notice` call
  </acceptance_criteria>
  <done>partial_backlog_drain exists wiring :retry_async_item preview-first; all three new templates are wired into the generator with on_exists: :skip (4+3 = 7 total skip flags).</done>
</task>

<task type="auto">
  <name>Task 3: Extend the generator-content regression test (D-11 layer 3)</name>
  <files>test/mix/tasks/parapet.gen.runbooks_test.exs</files>
  <read_first>
    - test/mix/tasks/parapet.gen.runbooks_test.exs (the file being modified — full current test lines 1-50: file-path assertions ~15-18, content-assertion blocks ~20-48)
    - .planning/phases/17-recovery-depth-runbook-templates/17-PATTERNS.md (section "test/mix/tasks/parapet.gen.runbooks_test.exs — extend file-path + content assertions")
    - .planning/phases/17-recovery-depth-runbook-templates/17-VALIDATION.md (Per-Task Verification Map — generator-content layer rows)
  </read_first>
  <action>
    In test/mix/tasks/parapet.gen.runbooks_test.exs, extend the existing test. (1) After the four existing file-path assertions (~line 18), add three new ones for `lib/test/parapet/runbooks/retry_storm.ex`, `.../suppression_drift.ex`, `.../partial_backlog_drain.ex`. (2) Add three new content-assertion blocks (mirroring the existing `Rewrite.source!(...) |> Rewrite.Source.get(:content)` pattern): retry_storm_source asserts `=~ "defmodule Test.Parapet.Runbooks.RetryStorm do"` and `=~ "warning:"`; suppression_drift_source asserts `=~ "defmodule Test.Parapet.Runbooks.SuppressionDrift do"` and `=~ "warning:"`; partial_backlog_drain_source asserts `=~ "defmodule Test.Parapet.Runbooks.PartialBacklogDrain do"` and `=~ "capability: :retry_async_item"` and `=~ "warning:"`. (3) Extend the FOUR existing template content blocks (stalled_executor/dead_letter/provider_outage/callback_delay) to also assert `=~ "warning:"` — this proves the plan 17-02 deepening landed at the generated-content layer. This is the third regression layer (D-11 #3) that, with 17-01's schema + projection layers, prevents the silent-swallow failure mode from ever returning.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/parapet.gen.runbooks_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - test asserts file paths for retry_storm.ex, suppression_drift.ex, partial_backlog_drain.ex under lib/test/parapet/runbooks/
    - test asserts `=~ "warning:"` for all seven templates (4 deepened + 3 new)
    - test asserts partial_backlog_drain_source `=~ "capability: :retry_async_item"`
    - `mix test test/mix/tasks/parapet.gen.runbooks_test.exs` exits 0
  </acceptance_criteria>
  <done>The generator-content regression layer asserts all three new files generate, all seven templates carry warning:, and partial_backlog_drain wires :retry_async_item — completing the three-layer test contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator copy_template -> adopter host filesystem | the generator writes runbook files into the host's lib/; must never overwrite adopter edits |
| template `capability:` -> Parapet.Capabilities allowlist | a wired mitigation must name one of three real capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Tampering (data-loss) | a new copy_template call dropping `on_exists: :skip`, overwriting an adopter's edited runbook | mitigate | Every new copy_template call passes `on_exists: :skip`; Task 2 acceptance asserts exactly 7 skip flags (4 existing + 3 new). Criterion 3 / D-08 |
| T-17-09 | Elevation/Correctness | a new template naming an unwired capability -> broken Preview/Execute ({:error, :capability_unwired}) | mitigate | retry_storm + suppression_drift are guidance-only (zero capability refs, asserted by grep); partial_backlog_drain reuses only :retry_async_item; generator-content test asserts the exact capability string |
| T-17-10 | Tampering/Safety | retry_storm wired to :retry_async_item -> retrying storming items worsens the storm (operator-safety) | mitigate | retry_storm is GUIDANCE-ONLY per RESEARCH D-07 correction; Task 1 acceptance asserts `capability:` count == 0 in retry_storm.ex.eex |
| T-17-11 | Tampering/Safety | a destructive new mitigation auto-executing without a scoped preview | mitigate | partial_backlog_drain's wired mitigation is `requires_preview: true` (preview-first, bounded); guidance-only mitigations dispatch no capability and are inherently safe |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase — pure Elixir source + EEx templates; no dependency changes |
</threat_model>

<verification>
- `mix test test/mix/tasks/parapet.gen.runbooks_test.exs` — generator-content regression layer green (all 7 templates contain warning:, 3 new files generate, partial_backlog_drain wires :retry_async_item).
- `grep -c 'on_exists: :skip' lib/mix/tasks/parapet.gen.runbooks.ex` == 7 — no-clobber contract preserved.
- `mix test` — full suite green after both Wave 2 plans merge.
- Manual smoke (AC-03, phase gate): open the operator UI against a new template (e.g. partial_backlog_drain) and confirm precondition + scoped preview + amber warning + bounded mitigation + verification render.
</verification>

<success_criteria>
- Three new templates exist at full RCV-02 depth: precondition, scope check, warning, bounded preview-first mitigation, verification.
- retry_storm and suppression_drift are guidance-only (no capability dispatch); partial_backlog_drain wires :retry_async_item preview-first.
- No new capability: id introduced anywhere.
- All three new templates copied with on_exists: :skip (host-ownership contract preserved).
- The three-layer regression test contract is complete: schema (17-01) + projection (17-01) + generator content (this plan).
</success_criteria>

<output>
Create `.planning/phases/17-recovery-depth-runbook-templates/17-03-SUMMARY.md` when done
</output>
