---
phase: 18-adoption-authoring-docs
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - docs/integrations/sigra.md
  - docs/integrations/accrue.md
  - docs/integrations/rulestead.md
  - docs/integrations/threadline.md
autonomous: true
requirements: [ADOPT-05]

must_haves:
  truths:
    - "An adopter can activate each integration without reading source, using the uniform Parapet.attach(adapters: [...]) line"
    - "All four guides follow one consistent structure: Prerequisites, what it unlocks, activation, config keys, troubleshooting"
    - "The Accrue, Rulestead, and Threadline guides do NOT claim they unlock SLO slices"
    - "The Rulestead guide tells adopters to wire Parapet.Metrics.Rulestead.metrics() into their Telemetry.Metrics reporter (OQ-3)"
    - "No guide shows the old broken Parapet.attach(adapters: [:rulestead]) framing as a crash"
  artifacts:
    - path: "docs/integrations/sigra.md"
      provides: "Sigra integration guide; backs the WebSaaS login slice"
      min_lines: 30
    - path: "docs/integrations/accrue.md"
      provides: "Accrue integration guide; billing journey metrics, no SLO slice"
      min_lines: 30
    - path: "docs/integrations/rulestead.md"
      provides: "Rulestead integration guide; flag-change counter, metrics wiring note"
      min_lines: 30
    - path: "docs/integrations/threadline.md"
      provides: "Threadline integration guide; audit-evidence interoperability, no metrics, no SLO slice"
      min_lines: 30
  key_links:
    - from: "docs/integrations/sigra.md"
      to: "Parapet.attach(adapters: [:sigra])"
      via: "uniform activation line"
      pattern: "attach\\(adapters: \\[:sigra\\]\\)"
    - from: "docs/integrations/rulestead.md"
      to: "Parapet.Metrics.Rulestead.metrics()"
      via: "Telemetry.Metrics reporter wiring note"
      pattern: "Parapet.Metrics.Rulestead.metrics"
---

<objective>
Author the four per-integration guides under `docs/integrations/` — `sigra.md`, `accrue.md`, `rulestead.md`, `threadline.md` (ADOPT-05 / AC-04) — each following one consistent structure so an adopter can discover what the integration unlocks and activate it without reading source.

Purpose: AC-04 requires that an adopter using a built-in integration can discover from its guide what it unlocks and enable it without reading source. This plan depends on 18-01 (the code change), which made activation uniform: all four guides now show the SAME `Parapet.attach(adapters: [...])` line (D-16 SUPERSEDES D-07 — do NOT special-case Rulestead's activation line).

Scope is EXACTLY these four integrations (D-10): Chimeway, Mailglass, Rindle, and Scoria do NOT get new guides — their SLO surfaces are already covered in docs/slo-reference.md. Do not author or register guides for them.

Output: A new `docs/integrations/` directory and four Markdown guides, honestly scoped per D-08/D-09 (only Sigra backs a pre-built SLO slice). (mix.exs registration and the full mix docs gate happen in plan 18-05.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-adoption-authoring-docs/18-CONTEXT.md
@.planning/phases/18-adoption-authoring-docs/18-RESEARCH.md
@.planning/phases/18-adoption-authoring-docs/18-PATTERNS.md
@.planning/phases/18-adoption-authoring-docs/18-01-PLAN.md

<voice_and_facts>
Voice/analog (D-03, D-11): mirror docs/operator-ui.md — every guide uses the SAME shape (Prerequisites -> what it unlocks -> activation -> config keys -> 2-3 troubleshooting answers). Sentence-case headings, second-person prose, elixir/bash fences, no emojis. ### sub-headings phrased as symptoms in the troubleshooting section.

Scope (D-10): author guides for ONLY Sigra, Accrue, Rulestead, Threadline. Do NOT author Chimeway/Mailglass/Rindle/Scoria guides (already in docs/slo-reference.md).

DEPENDS ON 18-01: the code change added setup/0 to Rulestead and a Parapet.Integration behaviour, so Parapet.attach(adapters: [:rulestead]) now WORKS. All four guides show the uniform line (D-16 supersedes D-07). Do NOT write Parapet.Integrations.Rulestead.attach() as the activation line, and do NOT frame Parapet.attach(adapters: [:rulestead]) as a crash.

VERIFIED per-integration truth (cite by symbol, not line number):
- Sigra (integrations/sigra.ex setup/0): wires Sigra auth telemetry -> Parapet journey metrics parapet_journey_login_count and parapet_journey_signup_count. Backs the WebSaaS web_saas_login_journey slice — without Sigra (or another emitter) that slice has no data. Activation: Parapet.attach(adapters: [:sigra])
- Accrue (integrations/accrue.ex setup/0): wires billing telemetry -> metrics parapet.journey.billing.checkout.count and parapet.journey.billing.webhook.duration. NO SLO slice — these metrics are the foundation for a custom slice (link to slo-authoring-guide.md). Activation: Parapet.attach(adapters: [:accrue])
- Rulestead (integrations/rulestead.ex; now has setup/0 delegating to attach/0 after 18-01): wires [:rulestead,:admin,:ruleset,:published] -> re-emits [:parapet,:rulestead,:flag_change]. The metrics module Parapet.Metrics.Rulestead DEFINES parapet_rulestead_flag_change_total but the integration does NOT wire it to a reporter (OQ-3): the adopter MUST include Parapet.Metrics.Rulestead.metrics() in their Telemetry.Metrics reporter for the counter to reach Prometheus. NO SLO slice. Activation: Parapet.attach(adapters: [:rulestead])
- Threadline (integrations/threadline.ex setup/0): audit-evidence interoperability ONLY — NO metrics module, NO SLO slice. Inbound [:threadline,:audit,:event] -> Parapet.Evidence.log_tool_audit (always fires). Outbound [:parapet,:audit,:created] -> Threadline.log_audit/1 guarded by Code.ensure_loaded?(Threadline) (only if host has the Threadline lib). Activation: Parapet.attach(adapters: [:threadline])

DRIFT GUARDS (RESEARCH anti-patterns + VALIDATION suite):
- Accrue/Rulestead/Threadline guides must NOT contain the phrase "SLO slice" (anti-drift check 6 fails on it). They unlock metrics or audit evidence, not slices.
- No doc may contain "attach(adapters: [:rulestead])" framed as crashing (anti-drift check 2: the form must appear ONLY as the valid uniform line, never as a documented crash).
- Optional integrations are detected by Code.ensure_loaded? (host-supplied lib), not by a Parapet dep.

Cross-link (D-02): sigra.md -> docs/getting-started.md and docs/slo-reference.md; accrue.md -> docs/slo-authoring-guide.md (build your own slice); all -> docs/getting-started.md for the base install.
</voice_and_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/integrations/ and author sigra.md and accrue.md</name>
  <files>docs/integrations/sigra.md, docs/integrations/accrue.md</files>
  <read_first>
    - docs/operator-ui.md (the structural analog: Prerequisites -> Installation/Activation -> config -> troubleshooting)
    - lib/parapet/integrations/sigra.ex (setup/0 wiring; emits parapet_journey_login_count / signup)
    - lib/parapet/metrics/sigra.ex (the journey metric names)
    - lib/parapet/slo/starter_pack/web_saas.ex (web_saas_login_journey slice depends on Sigra's login metric)
    - lib/parapet/integrations/accrue.ex (setup/0 wiring)
    - lib/parapet/metrics/accrue.ex (parapet.journey.billing.checkout.count, parapet.journey.billing.webhook.duration)
  </read_first>
  <action>
    Create the docs/integrations/ directory (it does not exist yet) and author the first two guides following the D-11 shared template exactly (Prerequisites -> what it unlocks -> activation -> config keys -> troubleshooting), mirroring operator-ui.md.

    sigra.md (the one integration that DOES back an SLO slice):
    - What it unlocks: Sigra login/signup events become Parapet journey metrics (parapet_journey_login_count, parapet_journey_signup_count); the WebSaaS web_saas_login_journey slice relies on parapet_journey_login_count — without Sigra (or another emitter) that slice has no data. This is the ONLY guide that may legitimately reference the WebSaaS login slice.
    - Activation (elixir fence): Parapet.attach(adapters: [:sigra])
    - Cross-link to docs/getting-started.md and docs/slo-reference.md.

    accrue.md (metrics, NOT slices — D-09):
    - What it unlocks: Accrue billing events become Parapet journey metrics (parapet.journey.billing.checkout.count, parapet.journey.billing.webhook.duration). There is NO pre-built SLO slice — these metrics are the foundation for a custom slice. Do NOT use the phrase "SLO slice" anywhere in this file. Link to docs/slo-authoring-guide.md for building a custom slice.
    - Activation (elixir fence): Parapet.attach(adapters: [:accrue])

    Each guide: 2-3 ### troubleshooting symptom answers grounded in real behavior (e.g., "metrics not appearing" -> confirm the integration is attached and the host emits the source telemetry). Sentence-case headings, no emojis, backtick inline code for module/metric/config names.
  </action>
  <verify>
    <automated>test -f docs/integrations/sigra.md && test -f docs/integrations/accrue.md && grep -q "adapters: \[:sigra\]" docs/integrations/sigra.md && grep -q "parapet_journey_login_count" docs/integrations/sigra.md && ! grep -q "SLO slice" docs/integrations/accrue.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/integrations/ directory exists with sigra.md and accrue.md
    - sigra.md shows the uniform Parapet.attach(adapters: [:sigra]) line and names parapet_journey_login_count + the login slice dependency
    - accrue.md shows Parapet.attach(adapters: [:accrue]) and names the two billing metrics
    - grep "SLO slice" docs/integrations/accrue.md returns 0 results (anti-drift check 6)
    - Both follow the D-11 structure (Prerequisites, what it unlocks, activation, config keys, troubleshooting)
    - No emojis present
  </acceptance_criteria>
  <done>sigra.md and accrue.md exist under docs/integrations/ in the uniform D-11 shape; Sigra references the login slice, Accrue surfaces metrics only (no "SLO slice").</done>
</task>

<task type="auto">
  <name>Task 2: Author rulestead.md and threadline.md</name>
  <files>docs/integrations/rulestead.md, docs/integrations/threadline.md</files>
  <read_first>
    - docs/operator-ui.md (structural analog)
    - docs/integrations/sigra.md (the sibling shape just authored in Task 1 — match it)
    - lib/parapet/integrations/rulestead.ex (now has setup/0 after 18-01; wires flag_change re-emit)
    - lib/parapet/metrics/rulestead.ex (defines parapet_rulestead_flag_change_total via metrics/0 — NOT auto-wired; OQ-3)
    - lib/parapet/integrations/threadline.ex (setup/0; inbound log_tool_audit always; outbound Threadline.log_audit guarded by Code.ensure_loaded?)
  </read_first>
  <action>
    Author the last two guides in the same D-11 shape.

    rulestead.md (metrics + the OQ-3 wiring gap — D-09):
    - What it unlocks: Rulestead ruleset-published events become the parapet_rulestead_flag_change_total counter. CRITICAL (OQ-3): the integration wires the event handler but does NOT register the metric with a reporter — the adopter MUST include Parapet.Metrics.Rulestead.metrics() in their host app's Telemetry.Metrics reporter config for the counter to reach Prometheus. State this explicitly.
    - Do NOT use the phrase "SLO slice".
    - Activation (elixir fence): Parapet.attach(adapters: [:rulestead]) — this is now the VALID uniform line (18-01 added setup/0). Do NOT write Parapet.Integrations.Rulestead.attach() and do NOT frame the uniform line as a crash.

    threadline.md (audit-evidence interoperability ONLY — D-08):
    - What it unlocks: audit-evidence interoperability, NOT SLO slices and NOT Prometheus metrics. Inbound [:threadline,:audit,:event] events are logged as Parapet.Evidence audit records (always active). Outbound [:parapet,:audit,:created] events are forwarded to Threadline.log_audit/1 only if the host app has the Threadline library loaded (Code.ensure_loaded?(Threadline) guard).
    - This guide MUST NOT contain an "unlocks SLO slices" or "unlocks metrics" section, and MUST NOT contain the phrase "SLO slice".
    - Activation (elixir fence): Parapet.attach(adapters: [:threadline])

    Each guide: 2-3 ### troubleshooting symptom answers. Sentence-case headings, no emojis, backtick inline code.
  </action>
  <verify>
    <automated>test -f docs/integrations/rulestead.md && test -f docs/integrations/threadline.md && grep -q "adapters: \[:rulestead\]" docs/integrations/rulestead.md && grep -q "Parapet.Metrics.Rulestead.metrics" docs/integrations/rulestead.md && ! grep -q "SLO slice" docs/integrations/rulestead.md && ! grep -q "SLO slice" docs/integrations/threadline.md && grep -qi "Code.ensure_loaded" docs/integrations/threadline.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/integrations/rulestead.md and threadline.md exist
    - rulestead.md shows the uniform Parapet.attach(adapters: [:rulestead]) line (NOT the old attach() form, NOT framed as a crash)
    - rulestead.md names Parapet.Metrics.Rulestead.metrics() and the Telemetry.Metrics reporter wiring requirement (OQ-3)
    - threadline.md describes inbound/outbound audit interoperability and the Code.ensure_loaded?(Threadline) guard
    - grep "SLO slice" returns 0 results in BOTH files (anti-drift check 6)
    - No emojis present
  </acceptance_criteria>
  <done>rulestead.md and threadline.md exist in the uniform D-11 shape; Rulestead documents the OQ-3 reporter wiring and the uniform activation line; Threadline is scoped to audit-evidence interoperability with no slice/metric claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter to docs | Read-only Markdown; no input handling, no execution surface |
| documented activation line to host runtime | Docs instruct the adopter to call Parapet.attach/1; correctness matters (a crashing line would break the 30-min path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Tampering (drift) | Rulestead activation line | mitigate | Show only the uniform Parapet.attach(adapters: [:rulestead]) line, valid after 18-01; never the old attach() form or a crash framing. Verified against the 18-01 setup/0 delegate. Anti-drift check 2 enforces this. |
| T-18-04-02 | Tampering (drift) | "unlocks SLO slices" claims | mitigate | Accrue/Rulestead/Threadline guides must not claim SLO slices (none have slice modules); enforced by anti-drift check 6 (grep "SLO slice"). |
| T-18-04-03 | Repudiation (silent failure) | Rulestead metrics not reaching Prometheus | mitigate | Document the OQ-3 reporter-wiring requirement so the flag-change counter is not silently dropped. |
| T-18-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — documentation only. |

Attack surface is essentially nil: four Markdown files with no input parsing, auth, cryptography, or I/O. The only residual concern is activation/claim accuracy (covered above).
</threat_model>

<verification>
- All four integration guides exist under docs/integrations/ in the uniform D-11 shape
- Anti-drift check 2 passes: attach(adapters: [:rulestead]) appears only as the valid uniform line, never as a crash
- Anti-drift check 6 passes: no "SLO slice" claim in accrue.md, rulestead.md, threadline.md
- rulestead.md documents the OQ-3 Telemetry.Metrics reporter wiring
- No emojis in any file
- Full mix docs --warnings-as-errors validation is deferred to plan 18-05
</verification>

<success_criteria>
- An adopter can activate each of the four integrations from its guide alone, using the uniform Parapet.attach(adapters: [...]) line (ADOPT-05 / AC-04)
- Only Sigra references a pre-built SLO slice; Accrue and Rulestead surface metrics, Threadline surfaces audit evidence
- The Rulestead guide closes the OQ-3 gap with the reporter-wiring note
</success_criteria>

<output>
Create `.planning/phases/18-adoption-authoring-docs/18-04-SUMMARY.md` when done
</output>
