---
phase: 18-adoption-authoring-docs
plan: 05
type: execute
wave: 3
depends_on: ["18-02", "18-03", "18-04"]
files_modified:
  - mix.exs
autonomous: true
requirements: [ADOPT-03, ADOPT-04, ADOPT-05, SLO-03, SLO-04]

must_haves:
  truths:
    - "All seven new docs are registered in the mix.exs extras list and render under the Guides group on hexdocs"
    - "mix docs --warnings-as-errors exits 0 — every cross-link between the new docs resolves"
    - "The full anti-drift grep suite passes"
    - "mix test and mix verify.public_api are green"
  artifacts:
    - path: "mix.exs"
      provides: "extras: list extended with all seven new docs"
      contains: "docs/getting-started.md"
  key_links:
    - from: "mix.exs"
      to: "docs/integrations sigra md"
      via: "groups_for_extras Guides regex"
      pattern: "docs/integrations/sigra.md"
---

<objective>
Register all seven new documentation files in the `mix.exs` `extras:` list (D-01) and run the phase verification gate: the full anti-drift grep suite, `mix docs --warnings-as-errors`, `mix test`, and `mix verify.public_api`.

Purpose: ExDoc does not glob — unlisted files ship in the tarball but never render on hexdocs.pm. This is the only place the cross-links between the seven new docs can be validated end-to-end, because `mix docs --warnings-as-errors` only resolves links once every target is a registered extra. This plan is the phase's final integration and validation step.

Output: An updated `mix.exs` and a green docs build with all anti-drift checks passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-adoption-authoring-docs/18-CONTEXT.md
@.planning/phases/18-adoption-authoring-docs/18-RESEARCH.md
@.planning/phases/18-adoption-authoring-docs/18-PATTERNS.md
@.planning/phases/18-adoption-authoring-docs/18-VALIDATION.md

<interfaces>
Current mix.exs extras block (inside defp docs):
      extras: [
        "README.md",
        "CHANGELOG.md",
        "docs/HISTORY.md",
        "docs/adopter-flows.md",
        "docs/operator-ui.md",
        "docs/slo-reference.md",
        "docs/telemetry.md"
      ],

groups_for_extras already matches docs/integrations via the ~r/docs/ regex, so NO groups_for_extras change is needed (D-01).
The package files: whitelist already ships the whole docs dir, so NO files: change needed (D-01).
mix.exs also has skip_undefined_reference_warnings_on: ["CHANGELOG.md"] — do NOT add the new docs to that list; their cross-links MUST resolve under --warnings-as-errors.

Seven entries to append after "docs/telemetry.md" (four-space indent, matching existing entries):
  "docs/getting-started.md",
  "docs/troubleshooting.md",
  "docs/slo-authoring-guide.md",
  "docs/integrations/sigra.md",
  "docs/integrations/accrue.md",
  "docs/integrations/rulestead.md",
  "docs/integrations/threadline.md"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the seven new docs in mix.exs extras (D-01)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (the defp docs extras block, groups_for_extras, package files whitelist, skip_undefined_reference_warnings_on)
  </read_first>
  <action>
    Append the seven new file paths to the existing extras: list, after "docs/telemetry.md" and before the closing bracket, in reading order: docs/getting-started.md, docs/troubleshooting.md, docs/slo-authoring-guide.md, docs/integrations/sigra.md, docs/integrations/accrue.md, docs/integrations/rulestead.md, docs/integrations/threadline.md. Match the existing indentation and trailing-comma style.

    Do NOT change groups_for_extras (the ~r/docs/ regex already matches docs/integrations files). Do NOT change the package files: whitelist. Do NOT add the new docs to skip_undefined_reference_warnings_on — their cross-links must resolve.
  </action>
  <verify>
    <automated>grep -c "docs/getting-started.md\|docs/troubleshooting\|docs/slo-authoring-guide\|docs/integrations" mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c over the new doc paths in mix.exs returns 7 (anti-drift check 1: all seven registered)
    - mix compile exits 0 (mix.exs parses)
    - groups_for_extras, package files whitelist, and skip_undefined_reference_warnings_on are unchanged
  </acceptance_criteria>
  <done>All seven new docs are appended to the mix.exs extras list with no other docs-config changes.</done>
</task>

<task type="auto">
  <name>Task 2: Run the docs build and full anti-drift validation gate</name>
  <files>mix.exs</files>
  <read_first>
    - .planning/phases/18-adoption-authoring-docs/18-VALIDATION.md (the eight-check anti-drift suite)
    - docs/getting-started.md, docs/troubleshooting.md, docs/slo-authoring-guide.md (cross-link targets must resolve)
    - docs/integrations/sigra.md, accrue.md, rulestead.md, threadline.md
  </read_first>
  <action>
    Run the phase verification gate. This task makes no further edits unless a check fails — if a check fails, the fix belongs in the owning plan's files (getting-started/troubleshooting in 18-02, slo-authoring in 18-03, integration guides in 18-04, behaviour/CHANGELOG in 18-01); apply the minimal correction and re-run.

    Execute, in order, the eight anti-drift checks from 18-VALIDATION.md plus the phase gate:
    1. grep -c of the new doc paths in mix.exs returns 7
    2. grep -rn for the rulestead adapters attach form across docs/ returns it only as the valid uniform line, never framed as a crash (the VALIDATION crash-framing check returns 0)
    3. grep -rn "config :parapet, :slos" docs/getting-started.md returns 0
    4. grep "min_total_rate" docs/slo-authoring-guide.md matches with 0.01
    5. grep -E for the six windows in docs/slo-authoring-guide.md returns 1
    6. grep "SLO slice" across docs/integrations/accrue.md, rulestead.md, threadline.md returns 0
    7. mix docs --warnings-as-errors exits 0
    8. ls doc/ shows all seven rendered .html files (including doc/integrations/*.html)

    Then run the phase gate commands: mix test, then mix verify.public_api, then mix docs --warnings-as-errors. All must exit 0.
  </action>
  <verify>
    <automated>mix test && mix verify.public_api && mix docs --warnings-as-errors && [ "$(grep -c 'docs/getting-started.md\|docs/troubleshooting\|docs/slo-authoring-guide\|docs/integrations' mix.exs)" = "7" ] && ! grep -rq 'Parapet.Integrations.Rulestead.attach()' docs/ && ! grep -rq 'config :parapet, :slos' docs/getting-started.md && ! grep -lq 'SLO slice' docs/integrations/accrue.md docs/integrations/rulestead.md docs/integrations/threadline.md</automated>
  </verify>
  <acceptance_criteria>
    - mix test, mix verify.public_api, and mix docs --warnings-as-errors all exit 0 (full suite + clean docs build; all cross-links between the seven new docs resolve; ExDoc renders every extra)
    - mix test exits 0 (full suite including the 18-01 behaviour and Rulestead regression tests)
    - mix verify.public_api exits 0
    - All eight anti-drift checks from 18-VALIDATION.md pass (registration count 7, no crashing rulestead form, no legacy :slos, min_total_rate 0.01 named, six windows present, no "SLO slice" in the three guides, clean docs build, seven .html files rendered)
    - ls doc/integrations/ shows sigra.html, accrue.html, rulestead.html, threadline.html
  </acceptance_criteria>
  <done>mix docs --warnings-as-errors, mix test, and mix verify.public_api are all green, and the full anti-drift grep suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mix.exs extras config to ExDoc | Dev-only build configuration; no runtime, no untrusted input |
| docs build to published hexdocs | Whatever is registered renders publicly; risk is shipping a doc that steers adopters to insecure config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | Information Disclosure | published docs set | mitigate | The validation gate (anti-drift suite plus mix docs --warnings-as-errors) is the final check that no doc ships a broken cross-link, the crashing Rulestead line, the legacy :slos path, or a false SLO-slice claim before publication. |
| T-18-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — a mix.exs extras edit plus build/test commands only; zero new dependencies. |

Attack surface is essentially nil: a dev-only ExDoc config edit plus read-only validation commands. No auth, input parsing, cryptography, or runtime I/O is introduced.
</threat_model>

<verification>
- mix.exs lists all seven new docs in extras (grep -c returns 7)
- mix docs --warnings-as-errors exits 0; all seven .html files render under doc/
- mix test and mix verify.public_api are green
- The full eight-check anti-drift suite from 18-VALIDATION.md passes
</verification>

<success_criteria>
- All seven new docs render under the Guides group on hexdocs (ADOPT-03/04/05, SLO-03/04 all become discoverable)
- mix docs --warnings-as-errors, mix test, and mix verify.public_api are all green
- No documentation drift remains: anti-drift suite clean
</success_criteria>

<output>
Create `.planning/phases/18-adoption-authoring-docs/18-05-SUMMARY.md` when done
</output>
