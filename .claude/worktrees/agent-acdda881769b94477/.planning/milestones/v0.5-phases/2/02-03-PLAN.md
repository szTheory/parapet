---
phase: 2
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - priv/templates/parapet.gen.ui/operator_live.ex.eex
autonomous: true
requirements:
  - JTBD-03
must_haves:
  truths:
    - Operator UI has a specific section for Critical Journeys.
    - Operator UI renders the Critical Journeys component.
  artifacts:
    - path: priv/templates/parapet.gen.ui/operator_components.ex.eex
      provides: Critical Journeys UI summary component
      exports: ["critical_journeys/1"]
    - path: priv/templates/parapet.gen.ui/operator_live.ex.eex
      provides: Dashboard rendering logic
  key_links:
    - from: priv/templates/parapet.gen.ui/operator_components.ex.eex
      to: operator_live
      via: render
      pattern: critical_journeys
---

<objective>
Surface the "Critical Journeys" (Login, Signup, Checkout, Webhooks) explicitly in the Parapet Operator UI.
Purpose: Allow operators to immediately map user harm (SLO burn) to specific business flows.
Output: Modified Operator components and live view templates containing the Critical Journeys widget.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@priv/templates/parapet.gen.ui/operator_components.ex.eex
@priv/templates/parapet.gen.ui/operator_live.ex.eex
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Critical Journeys Component</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex</files>
  <action>
    Add a new component `critical_journeys(assigns)` to `operator_components.ex.eex`.
    The component should accept `@journeys` (a list of maps, e.g., `%{name: "Login", status: :healthy}`).
    Render a visually distinct dashboard section showing these business flows.
    Following `PATTERNS.md` recommendations, it should be a focused list of Journeys and their current binary SLO state.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Component `critical_journeys/1` exists and is exportable to applications</done>
</task>

<task type="auto">
  <name>Task 2: Wire Component to Operator LiveView</name>
  <files>priv/templates/parapet.gen.ui/operator_live.ex.eex</files>
  <action>
    Modify `operator_live.ex.eex` to actually render the new `<.critical_journeys journeys={@journeys} />` component.
    Provide static mock data or static links for the `@journeys` assign in the `mount/3` function representing Login, Signup, Checkout, and Webhooks for now, as decided in `RESEARCH.md`.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Operator UI actively renders the Critical Journeys component.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend | Rendered UI for Operators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Spoofing | operator UI | accept | Internal operator UI, standard authentication protects access. |
</threat_model>

<verification>
`mix compile` must succeed for the generator template format.
</verification>

<success_criteria>
Operator UI templates provide a way to render Critical Journeys status.
</success_criteria>

<output>
After completion, create `.planning/phases/2/02-03-SUMMARY.md`
</output>
