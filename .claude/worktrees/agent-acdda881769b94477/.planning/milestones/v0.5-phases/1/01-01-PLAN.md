---
phase: 1
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/probe.ex
  - test/parapet/probe_test.exs
  - lib/parapet/metrics/probe.ex
  - test/parapet/metrics/probe_test.exs
autonomous: true
requirements:
  - PROBE-01
must_haves:
  truths:
    - Developer can define a module using `Parapet.Probe`
    - Probe execution reliably emits standard telemetry metrics
  artifacts:
    - path: lib/parapet/probe.ex
      provides: Probe behavior and macro
    - path: lib/parapet/metrics/probe.ex
      provides: Telemetry metrics translation
  key_links:
    - from: lib/parapet/probe.ex
      to: :telemetry
      via: span execution
      pattern: ":telemetry.span"
    - from: lib/parapet/metrics/probe.ex
      to: :telemetry
      via: handler attachment
      pattern: "Parapet.attach"
---

<objective>
Implement the core `Parapet.Probe` behavior and its telemetry metrics handler.

Purpose: Allow operators to define active checks that maintain SLO signal quality. The macro ensures standardized metric emission via `:telemetry.span/3`.
Output: Working probe macro and metrics handler.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1/PATTERNS.md
@.planning/phases/1/RESEARCH.md

<interfaces>
<!-- Contracts to implement downstream -->
From lib/parapet/probe.ex (to be created):
```elixir
defmodule Parapet.Probe do
  @callback run() :: :ok | {:error, term()}
  # Macro injects execute/0 which wraps run/0 in :telemetry.span/3
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Parapet.Probe macro</name>
  <files>lib/parapet/probe.ex, test/parapet/probe_test.exs</files>
  <behavior>
    - Defines a behaviour with `run/0` callback returning `:ok | {:error, term()}`.
    - Provides a `__using__` macro that injects an `execute/0` function.
    - `execute/0` wraps the user's `run/0` in `:telemetry.span/3` with event `[:parapet, :probe, :run]`.
    - Span metadata includes `%{probe: inspect(__MODULE__)}`.
    - Span result metadata includes `status: "success"` for `:ok` and `status: "error"` for `{:error, reason}`.
  </behavior>
  <action>
    Create the `Parapet.Probe` module using the pattern outlined in RESEARCH.md Pattern 1. Ensure `run/0` is an overridable callback. The injected `execute/0` should execute `:telemetry.span`. Use standard behavior/macro patterns as seen in `Parapet.Runbook`.
  </action>
  <verify>
    <automated>mix test test/parapet/probe_test.exs</automated>
  </verify>
  <done>Tests pass demonstrating the telemetry span is executed and run/0 is invoked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Parapet.Metrics.Probe</name>
  <files>lib/parapet/metrics/probe.ex, test/parapet/metrics/probe_test.exs</files>
  <behavior>
    - Exposes a `setup/0` function that attaches to `[:parapet, :probe, :run, :stop]` and `[:parapet, :probe, :run, :exception]` using `Parapet.attach/1`.
    - Exposes a `metrics/0` function that defines Prometheus metrics (e.g., `counter`, `distribution`) for the probe run.
    - Uses `Parapet.Internal.LabelPolicy.assert_safe!([:probe, :status])` to enforce label safety.
    - `handle_event/4` translates span metadata into proper telemetry events with `duration_ms`.
  </behavior>
  <action>
    Create `Parapet.Metrics.Probe` using the `Parapet.Metrics.Oban` pattern. Register handlers via `Parapet.attach` in `setup/0`. Emit Prometheus distributions and counters in `metrics/0`, ensuring high-cardinality label pitfalls are avoided by exclusively using safe labels (probe name and status).
  </action>
  <verify>
    <automated>mix test test/parapet/metrics/probe_test.exs</automated>
  </verify>
  <done>Telemetry handlers are successfully attached and emit metric distributions safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Probe execution | Synthetic probes execute internal business logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Denial of Service | `Parapet.Metrics.Probe` | mitigate | Restrict metric labels to `[:probe, :status]` via `LabelPolicy.assert_safe!` to prevent high cardinality memory exhaustion in Prometheus. |
</threat_model>

<verification>
- [ ] `mix test test/parapet/probe_test.exs` passes
- [ ] `mix test test/parapet/metrics/probe_test.exs` passes
- [ ] Label safety is explicitly enforced via `LabelPolicy`
</verification>

<success_criteria>
1. `Parapet.Probe` successfully wraps execution in telemetry spans.
2. `Parapet.Metrics.Probe` correctly intercepts telemetry and defines Prometheus metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/1/01-01-SUMMARY.md`
</output>
