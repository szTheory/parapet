---
phase: 1
plan: 2
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - lib/parapet/probe/native_scheduler.ex
  - test/parapet/probe/native_scheduler_test.exs
  - lib/parapet/probe/oban_scheduler.ex
  - test/parapet/probe/oban_scheduler_test.exs
autonomous: true
requirements:
  - PROBE-02
must_haves:
  truths:
    - Native timer can schedule probes in standalone setups
    - Oban cron can schedule probes in clustered setups without retrying failures
  artifacts:
    - path: lib/parapet/probe/native_scheduler.ex
      provides: In-memory timer for probes
    - path: lib/parapet/probe/oban_scheduler.ex
      provides: Oban cron worker for probes
  key_links:
    - from: lib/parapet/probe/native_scheduler.ex
      to: lib/parapet/probe.ex
      via: execute invocation
      pattern: "apply(probe_module, :execute, [])"
    - from: lib/parapet/probe/oban_scheduler.ex
      to: lib/parapet/probe.ex
      via: execute invocation
      pattern: "apply(probe_module, :execute, [])"
---

<objective>
Implement pluggable schedulers for executing probes.

Purpose: Provide flexibility for running synthetic checks—a simple memory-based timer (`NativeScheduler`) for standalone setups and an Oban-backed scheduler (`ObanScheduler`) for distributed environments.
Output: Working Native and Oban scheduler implementations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1/PATTERNS.md
@.planning/phases/1/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Parapet.Probe.NativeScheduler</name>
  <files>lib/parapet/probe/native_scheduler.ex, test/parapet/probe/native_scheduler_test.exs</files>
  <behavior>
    - Starts as a `GenServer` accepting a list of probes and intervals in its init state.
    - Sets up timers using `:timer.send_interval` or `Process.send_after` for each probe.
    - On receiving the timer message, calls `execute/0` on the specified probe module.
  </behavior>
  <action>
    Create `Parapet.Probe.NativeScheduler` as a standard `GenServer`. It should handle a simple configuration (e.g., `[{MyProbe, 60_000}]`) and dispatch calls to each module's `execute/0` function continuously.
  </action>
  <verify>
    <automated>mix test test/parapet/probe/native_scheduler_test.exs</automated>
  </verify>
  <done>GenServer correctly dispatches to probe modules on the defined interval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Parapet.Probe.ObanScheduler</name>
  <files>lib/parapet/probe/oban_scheduler.ex, test/parapet/probe/oban_scheduler_test.exs</files>
  <behavior>
    - Defines an Oban Worker via `use Oban.Worker, max_attempts: 1`.
    - The `perform/1` callback receives the probe module name in the job arguments.
    - Dynamically invokes `execute/0` on the probe module.
    - Retries are explicitly prevented to avoid muddying SLO metrics.
  </behavior>
  <action>
    Create `Parapet.Probe.ObanScheduler` mapping closely to `Parapet.Notifier.ObanWorker`'s patterns but ensuring `max_attempts: 1`. The job args should specify which probe to run. It delegates execution to the probe. Make sure to conditionally wrap the module definition using `if Code.ensure_loaded?(Oban) do ... end`.
  </action>
  <verify>
    <automated>mix test test/parapet/probe/oban_scheduler_test.exs</automated>
  </verify>
  <done>Oban worker triggers the probe execution without retries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler configuration | Config-driven periodic execution limits which probes can run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Tampering | `Parapet.Probe.ObanScheduler` | mitigate | Ensure the worker validates the provided probe module actually implements `Parapet.Probe` before dynamically invoking it, if passed as arbitrary strings. |
</threat_model>

<verification>
- [ ] `mix test test/parapet/probe/native_scheduler_test.exs` passes
- [ ] `mix test test/parapet/probe/oban_scheduler_test.exs` passes
- [ ] ObanScheduler explicitly hardcodes `max_attempts: 1`
</verification>

<success_criteria>
1. Probes can run on native memory timers.
2. Probes can run via durable Oban cron jobs with no retries.
</success_criteria>

<output>
After completion, create `.planning/phases/1/01-02-SUMMARY.md`
</output>
