---
phase: 1
plan: 3
type: execute
wave: 3
depends_on:
  - 1
  - 2
files_modified:
  - README.md
  - docs/telemetry.md
  - lib/mix/tasks/parapet.install.ex
autonomous: true
requirements:
  - PROBE-03
must_haves:
  truths:
    - Probe metrics handler is initialized in application startup
    - Documentation details how to configure the pluggable scheduler
  artifacts:
    - path: lib/mix/tasks/parapet.install.ex
      provides: Telemetry setup injection
    - path: README.md
      provides: Scheduler configuration guide
  key_links:
    - from: lib/mix/tasks/parapet.install.ex
      to: lib/parapet/metrics/probe.ex
      via: injected setup
      pattern: "Parapet.Metrics.Probe.setup()"
---

<objective>
Wire up probe metrics initialization and document the new synthetic probe system.

Purpose: Ensure probe metrics handlers are registered at startup and operators know how to configure either the Native or Oban scheduler for their environments.
Output: Integrated metrics setup and updated documentation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire up Metrics.Probe in installer</name>
  <files>lib/mix/tasks/parapet.install.ex</files>
  <action>
    Update `lib/mix/tasks/parapet.install.ex` (or wherever metrics setup is generally handled) to inject `Parapet.Metrics.Probe.setup()` during install or explicitly call it in the internal application boot flow. Ensure it follows the existing telemetry initialization conventions found in the codebase.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>The probe metrics setup is integrated in the startup or installation code generation.</done>
</task>

<task type="auto">
  <name>Task 2: Update Configuration and Documentation</name>
  <files>README.md, docs/telemetry.md</files>
  <action>
    Document how to implement a synthetic probe using `use Parapet.Probe`. Add instructions for configuring the pluggable scheduler in application configuration, contrasting `Parapet.Probe.NativeScheduler` vs `Parapet.Probe.ObanScheduler`. Update `docs/telemetry.md` to list the new probe telemetry metrics (e.g., `parapet.probe.run`).
  </action>
  <verify>
    <automated>grep -q "Parapet.Probe" README.md</automated>
  </verify>
  <done>Clear examples exist for defining probes and configuring their schedulers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | Configuration guides provide standard security practices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Information Disclosure | `docs/telemetry.md` | mitigate | Explicitly document that custom probe labels shouldn't include sensitive user IDs or tokens. |
</threat_model>

<verification>
- [ ] Application install task includes the probe metrics handler.
- [ ] README contains configuration example for the scheduler.
- [ ] Telemetry docs reflect new events.
</verification>

<success_criteria>
1. Application fully bootstraps probe telemetry handlers automatically.
2. Developers have actionable docs to use synthetic probes immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/1/01-03-SUMMARY.md`
</output>
