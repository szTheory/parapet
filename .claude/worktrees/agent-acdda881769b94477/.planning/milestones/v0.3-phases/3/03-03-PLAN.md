---
phase: 03-notifications
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: ["lib/parapet/notifier/teams.ex", "test/parapet/notifier/teams_test.exs"]
autonomous: true
requirements: ["NOTIFY-03"]
must_haves:
  truths:
    - "Teams adapter formats and sends rich Adaptive Cards"
  artifacts:
    - path: "lib/parapet/notifier/teams.ex"
      provides: "MS Teams HTTP adapter using Req"
  key_links:
    - from: "lib/parapet/notifier/teams.ex"
      to: "lib/parapet/notifier.ex"
      via: "Teams adapter implements Parapet.Notifier"
---

<objective>
Implement the MS Teams adapter with Adaptive Cards support.

Purpose: Provide an out-of-the-box Microsoft Teams integration that formats incident updates as rich Adaptive Cards.
Output: A complete MS Teams adapter implementing `Parapet.Notifier`.
</objective>

<context>
@.planning/milestones/v0.3-phases/3/RESEARCH.md
@.planning/milestones/v0.3-phases/3/PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement MS Teams Adapter</name>
  <files>lib/parapet/notifier/teams.ex, test/parapet/notifier/teams_test.exs</files>
  <action>
    Create `Parapet.Notifier.Teams` module and declare it as an implementation of `@behaviour Parapet.Notifier`.
    Implement the `deliver(incident, opts)` callback. Build a rich Adaptive Card JSON payload using the data from the `incident` struct. Ensure you include a direct link to the Parapet Operator UI (similar to Slack) using `opts[:operator_url]` if possible.
    Extract the MS Teams Incoming Webhook URL from `opts[:webhook_url]`.
    Use `Req.post(url, json: payload)` to send the payload to MS Teams.
    Handle the HTTP response from `Req`. Return `{:ok, response_body}` on a 2xx status code, and `{:error, reason}` on HTTP errors or non-2xx status codes.
    Write tests to verify the generation of the Adaptive Card payload and API interaction behavior.
  </action>
  <verify>
    <automated>mix test test/parapet/notifier/teams_test.exs</automated>
  </verify>
  <done>Teams adapter correctly formats incident into Adaptive Cards, includes deep links, and sends it via Req.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System to Teams API | Egress traffic sending incident data to third-party |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Spoofing | lib/parapet/notifier/teams.ex | accept | Trusting the user-provided webhook URL in opts is acceptable as configuration is restricted to operators. |
</threat_model>