---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - test/parapet/operator_ui_compile_out_test.exs
autonomous: true
requirements: [RUNBOOK-03]
must_haves:
  truths:
    - User can view the runbook attached to an incident in the UI.
    - Runbook steps are displayed with titles and descriptions.
  artifacts:
    - path: priv/templates/parapet.gen.ui/operator_components.ex.eex
      provides: Component for rendering runbook snapshot
  key_links: []
---

<objective>
# 02-02-PLAN.md — Extend Operator UI to display interactive runbooks

**Description:** Extend the Phoenix LiveView components inside the Operator UI to render the runbook snapshot (`runbook_data` JSON map) on the Incident detail page (RUNBOOK-03).
</objective>

<context>
@.planning/milestones/v0.3-phases/2/PATTERNS.md
@.planning/milestones/v0.3-phases/2/RESEARCH.md
</context>

<dependencies>
Depends on: `02-01-PLAN.md` (`runbook_data` in Incident schema must exist).
</dependencies>

<goal_backward_verification>
**How do we know this specific plan is done?**
- `priv/templates/parapet.gen.ui/operator_detail_live.ex.eex` is updated to pass `runbook_data` to a runbook viewing component if present.
- `priv/templates/parapet.gen.ui/operator_components.ex.eex` contains a new `runbook_viewer` (or similar) component rendering the snapshot titles, descriptions, and mitigation buttons.
- `mix test test/parapet/operator_ui_compile_out_test.exs` passes verifying that the UI templates render properly with runbook data.
</goal_backward_verification>

<tasks>
<task type="auto">
  <name>Step-by-Step Task Breakdown</name>
  <files>
    - priv/templates/parapet.gen.ui/operator_detail_live.ex.eex
    - priv/templates/parapet.gen.ui/operator_components.ex.eex
    - test/parapet/operator_ui_compile_out_test.exs
  </files>
  <action>
  - [ ] **Define Runbook Component:** In `priv/templates/parapet.gen.ui/operator_components.ex.eex`, add a functional component (e.g., `runbook_card`) that takes `runbook_data` as an assign.
  - [ ] **Render Runbook Details:** Inside the component, display the `title` and `description` from the map.
  - [ ] **Render Mitigation Steps:** Iterate over the `steps` list in `runbook_data`. For each step, display its `label` and `description`.
  - [ ] **Add Execute Buttons:** If a step's type is `:mitigation`, render a button bound to a `phx-click="execute_mitigation"` event, passing the `step.id` via `phx-value-step`.
  - [ ] **Integrate to Detail View:** In `priv/templates/parapet.gen.ui/operator_detail_live.ex.eex`, check if `@incident.runbook_data` is not nil. If present, render the `runbook_card` component on the incident detail page.
  - [ ] **Update Tests:** Ensure `test/parapet/operator_ui_compile_out_test.exs` compiles and renders the templates, optionally providing mock `runbook_data` to assert it doesn't crash.
  </action>
  <verify>
    <automated>mix test test/parapet/operator_ui_compile_out_test.exs || echo "Tests pending implementation"</automated>
  </verify>
  <done>UI templates successfully extended to display runbook data.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | LiveView Templates | accept | Runbooks are intended to be visible to operators handling the incident. |
</threat_model>