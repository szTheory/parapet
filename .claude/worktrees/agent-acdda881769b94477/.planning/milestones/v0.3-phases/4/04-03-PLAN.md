# Plan: 04-03-PLAN.md

## Goal
Resolve deferred technical debt and ensure the test suite is 100% green and launch-ready for the v0.3 release.

## Context
During Phase 1, a test failure (`Sigra.setup/0 is undefined or private`) was deferred to maintain velocity. As we close out the v0.3 milestone, all deferred testing issues must be resolved to uphold the project's reliability and DNA constraints.

## Tasks

1. **Investigate `SigraTest` Failure:**
   - Run `mix test test/parapet/integrations/sigra_test.exs`.
   - Diagnose why `Sigra.setup/0` is undefined. It's likely that `Sigra` (the optional sibling library) changed its API or the mock/support file (`test/support/sigra.ex`) is missing the `setup/0` definition expected by the test or integration adapter.

2. **Fix the Support Mock or Integration:**
   - Update `test/support/sigra.ex` or `lib/parapet/integrations/sigra.ex` to correctly match the expected `Sigra` API.
   - Ensure the integration still cleanly compiles out when `Sigra` is absent.

3. **System Polish & Final Checks:**
   - Run the full suite (`mix test`, `mix format --check-formatted`, `mix credo`).
   - Run `mix dialyzer` (if configured) to catch any type spec issues introduced in v0.3.
   - Update `v0.3-ROADMAP.md` and `STATE.md` to reflect full completion of the milestone.

## Verification
- `mix test` passes perfectly.
- All code quality checks pass.
- No deferred items remain in `STATE.md`.