---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - priv/repo/migrations/*_add_correlation_key_to_incidents.exs
  - lib/parapet/spine/incident.ex
  - lib/parapet/spine/alert_processor.ex
  - test/parapet/spine/alert_processor_test.exs
autonomous: true
requirements:
  - ROUTING-02
  - ROUTING-04
must_haves:
  truths:
    - Firing alerts are converted into Incidents
    - Alerts with identical keys are correlated
    - Database prevents duplicate correlation keys for open incidents
  artifacts:
    - path: lib/parapet/spine/incident.ex
      provides: correlation_key field and schema changes
    - path: lib/parapet/spine/alert_processor.ex
      provides: Upsert logic for firing alerts
  key_links:
    - from: lib/parapet/spine/alert_processor.ex
      to: parapet_incidents table
      via: Ecto upsert with unique constraint on correlation_key
---

<objective>
Implement incident creation and correlation logic for firing alerts.

Purpose: To process Alertmanager batches, automatically creating or updating open incidents based on deterministic correlation keys.
Output: Database migration for `correlation_key`, schema update, and the domain logic to handle "firing" alerts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v0.3-phases/1/PATTERNS.md
@.planning/milestones/v0.3-phases/1/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add correlation_key to Incident model</name>
  <files>priv/repo/migrations/*_add_correlation_key_to_incidents.exs, lib/parapet/spine/incident.ex</files>
  <action>
    Generate a new Ecto migration to add a `:string` column named `correlation_key` to the `parapet_incidents` table.
    Add a unique index on `correlation_key` ONLY WHERE `state = 'open'` (e.g. `create unique_index(:parapet_incidents, [:correlation_key], where: "state = 'open'")`).
    Update `lib/parapet/spine/incident.ex` to include `field :correlation_key, :string` in the schema.
    *Note: Reference analog pattern from `lib/parapet/spine/timeline_entry.ex` (via PATTERNS.md) for Ecto schema structure:*
    ```elixir
      schema "parapet_incidents" do
        field :correlation_key, :string
        # ... existing fields
      end
    ```
    Add `:correlation_key` to the permitted fields in the changeset.
    *Note: Reference analog pattern from `timeline_entry.ex` for changeset validation:*
    ```elixir
      def changeset(incident, attrs) do
        incident
        |> cast(attrs, [:correlation_key, ...])
        # ... validations
      end
    ```
  </action>
  <verify>
    <automated>mix ecto.migrate</automated>
  </verify>
  <done>Migration runs successfully and the schema is updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement creation and correlation logic</name>
  <files>lib/parapet/spine/alert_processor.ex, test/parapet/spine/alert_processor_test.exs</files>
  <behavior>
    - Test 1: A "firing" alert creates a new Incident with state 'open' and the correct correlation_key.
    - Test 2: An identical "firing" alert correlates to the existing open Incident without creating duplicates.
    - Test 3: An invalid payload gracefully rejects.
  </behavior>
  <action>
    Implement `Parapet.Spine.AlertProcessor.process_batch(payload)`.
    
    *Note: Reference analog pattern from `lib/parapet/operator.ex` (via PATTERNS.md) for the Service boundary structure:*
    ```elixir
    alias Parapet.Spine.Incident
    alias Parapet.Evidence
    ```
    
    *Note: Incorporate the Shared Error Handling pattern from `lib/parapet/operator.ex` to gracefully reject invalid payloads:*
    ```elixir
    def process_batch(payload) do
      if valid_payload?(payload) do
        # ... process alerts
      else
        {:error, :invalid_payload}
      end
    end
    ```
    
    Inside the valid payload branch: iterate over `payload["alerts"]`. Filter for those with `status == "firing"`.
    For each, derive a `correlation_key`. Use the alert's `fingerprint` (if present) or fallback to hashing the `labels` deterministically (e.g., via `:crypto.hash(:sha256, Jason.encode!(labels)) |> Base.encode16()`).
    Upsert the Incident using `Parapet.Evidence.repo().insert` with `on_conflict: :nothing` or `on_conflict: :replace_all` relying on the database unique constraint to prevent duplicates.
    Map the Prometheus alert's `annotations.summary` or `labels.alertname` to the Incident's `:title` and `:description`.
  </action>
  <verify>
    <automated>mix test test/parapet/spine/alert_processor_test.exs</automated>
  </verify>
  <done>Firing alerts successfully translate to unique open incidents in the database, invalid payloads are rejected properly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → DB | AlertProcessor inserts external data into the database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Incident Schema | mitigate | The partial unique index on `correlation_key` where `state = 'open'` guarantees that webhook spam or race conditions do not generate duplicate open incidents. |
| T-01-04 | Information Disclosure | AlertProcessor | accept | Alert payloads may contain sensitive labels (e.g., customer IDs), but Parapet is an internal tool and relies on host DB encryption at rest. |
</threat_model>

<verification>
- `mix test test/parapet/spine/alert_processor_test.exs` passes.
- Database unique constraint tested with duplicate inserts.
</verification>

<success_criteria>
"Firing" alerts are durably stored as Incidents, with duplicate alerts properly correlated.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3-phases/1/01-02-SUMMARY.md`
</output>
