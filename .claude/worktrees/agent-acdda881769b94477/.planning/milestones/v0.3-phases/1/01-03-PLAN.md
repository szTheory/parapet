---
phase: "01"
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - lib/parapet/spine/alert_processor.ex
  - test/parapet/spine/alert_processor_test.exs
autonomous: true
requirements:
  - ROUTING-03
must_haves:
  truths:
    - Resolved alerts change incident state to "resolved"
    - Resolved alerts insert a TimelineEntry audit record
    - Both state changes happen transactionally
  artifacts:
    - path: lib/parapet/spine/alert_processor.ex
      provides: Auto-resolution and timeline entry logic
  key_links:
    - from: lib/parapet/spine/alert_processor.ex
      to: lib/parapet/spine/timeline_entry.ex
      via: Ecto.Multi.insert
---

<objective>
Implement incident resolution logic for resolved alerts.

Purpose: To automatically close incidents and generate a durable audit trail when Alertmanager sends a "resolved" webhook.
Output: Extended domain logic using `Ecto.Multi` to ensure atomic state transitions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v0.3-phases/1/PATTERNS.md
@.planning/milestones/v0.3-phases/1/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement auto-resolution logic</name>
  <files>lib/parapet/spine/alert_processor.ex, test/parapet/spine/alert_processor_test.exs</files>
  <behavior>
    - Test 1: A "resolved" alert updates the corresponding open Incident's state to "resolved".
    - Test 2: A "resolved" alert inserts a TimelineEntry of type "auto_resolved" containing the alert payload.
    - Test 3: If the open Incident is not found (e.g., already closed or never created), the resolved alert is ignored safely.
  </behavior>
  <action>
    Extend `Parapet.Spine.AlertProcessor.process_batch(payload)` to also handle alerts where `status == "resolved"`.
    For each resolved alert, compute its `correlation_key` (using the same algorithm as firing alerts).
    Query the database for an Incident with that `correlation_key` where `state == "open"`.
    If found, initiate an `Ecto.Multi` transaction applying the 'Transactional Audit/Timeline Boundary' shared pattern (from PATTERNS.md / `lib/parapet/evidence.ex`):
    ```elixir
      multi =
        Ecto.Multi.new()
        |> Ecto.Multi.update(:incident, incident_changeset)
        |> Ecto.Multi.insert(:timeline_entry, fn %{incident: incident} ->
           # return TimelineEntry changeset
        end)
    ```
    1. `Ecto.Multi.update` the incident state to `"resolved"`.
    2. `Ecto.Multi.insert` a `%Parapet.Spine.TimelineEntry{}` (type: `"auto_resolved"`, payload: the alert payload, incident_id: incident.id).
    Execute the transaction via `Parapet.Evidence.repo().transaction/1`.
  </action>
  <verify>
    <automated>mix test test/parapet/spine/alert_processor_test.exs</automated>
  </verify>
  <done>Incidents are automatically and atomically resolved when matching resolved alerts are received.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → DB | Resolving incidents updates database state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Spoofing | AlertProcessor | mitigate | Resolutions are correlated deterministically via the hash of labels/fingerprint. An attacker cannot close arbitrary incidents without knowing the exact labels. |
| T-01-06 | Denial of Service | DB Transaction | accept | Ecto.Multi transactions are lightweight and DB connection pools will handle concurrency appropriately. |
</threat_model>

<verification>
- `mix test test/parapet/spine/alert_processor_test.exs` passes.
</verification>

<success_criteria>
"Resolved" alerts automatically close the correlated Incident and write a TimelineEntry audit record atomically.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.3-phases/1/01-03-SUMMARY.md`
</output>
