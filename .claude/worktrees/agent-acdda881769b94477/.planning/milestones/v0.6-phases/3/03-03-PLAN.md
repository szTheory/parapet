---
phase: "03-threadline-compliance"
plan: "03"
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - test/parapet/integrations/threadline_test.exs
  - test/parapet/evidence_test.exs
autonomous: true
requirements: ["THR-01"]
must_haves:
  truths:
    - "Test suite verifies compile-out isolation of Threadline dependency"
    - "Test suite verifies `:dual_write` and `:threadline_deferred` modes"
  artifacts:
    - path: "test/parapet/integrations/threadline_test.exs"
      provides: "Telemetry event handler and compile-out tests"
    - path: "test/parapet/evidence_test.exs"
      provides: "Audit mode transaction and telemetry dispatch tests"
  key_links: []
---

<objective>
Write comprehensive tests to verify that the Threadline optional dependency compiles out cleanly when missing, and that Parapet correctly honors the `audit_mode` configuration in `Parapet.Evidence`, emitting proper telemetry events in all cases.

Purpose: Guarantee 100% reliability for dual-write compliance functionality and ensure host applications without Threadline are never broken by this integration.
Output: Thorough test coverage for both modules.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v0.6-ROADMAP.md
@.planning/v0.6-REQUIREMENTS.md
@.planning/v0.6-phases/3/RESEARCH.md
@lib/parapet/integrations/threadline.ex
@lib/parapet/evidence.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test Threadline telemetry integration</name>
  <files>test/parapet/integrations/threadline_test.exs</files>
  <behavior>
    - Test 1: `handle_event/4` correctly formats payload and calls Threadline when `Code.ensure_loaded?(Threadline)` is mocked to true.
    - Test 2: `handle_event/4` safely returns `:ok` without crashing when `Code.ensure_loaded?(Threadline)` is mocked to false.
  </behavior>
  <action>
    Implement tests for `Parapet.Integrations.Threadline.handle_event/4`.
    Since Threadline might not be an actual dependency in the test environment, you may need to use `Code.ensure_loaded?/1` mocking via a library like Mock or simply verify the behavior when it's not loaded (which should be the default). 
    Ensure you test the structural mapping of the payload based on telemetry metadata.
  </action>
  <verify>
    <automated>mix test test/parapet/integrations/threadline_test.exs</automated>
  </verify>
  <done>Threadline adapter is proven to be safe as an optional dependency processing telemetry events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test Evidence `audit_mode` conditionals and telemetry dispatch</name>
  <files>test/parapet/evidence_test.exs</files>
  <behavior>
    - Test 1: `run_operator_command/1` in `:dual_write` mode inserts `ToolAudit` to the database and dispatches telemetry event.
    - Test 2: `run_operator_command/1` in `:threadline_deferred` mode does NOT insert `ToolAudit` but still succeeds and dispatches telemetry event.
    - Test 3: `log_tool_audit/1` follows the same conditional behavior.
  </behavior>
  <action>
    Add tests to `Parapet.EvidenceTest` (or similar suite) that mock the application environment:
    `Application.put_env(:parapet, :audit_mode, :dual_write)`
    and
    `Application.put_env(:parapet, :audit_mode, :threadline_deferred)`.
    
    Attach a test telemetry handler to `[:parapet, :audit, :created]` to verify that events are emitted correctly with the expected payload.
    Verify database state (count of `ToolAudit`) after executing the functions to prove that records are bypassed or saved as expected based on mode.
  </action>
  <verify>
    <automated>mix test test/parapet/evidence_test.exs</automated>
  </verify>
  <done>Evidence module is proven to respect configuration accurately and trigger decoupled events.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing | No specific boundary; validating internal behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | EvidenceTest | accept | Testing ensures that deferred mode prevents logging to DB, mitigating disclosure risks in compliance environments. |
</threat_model>

<verification>
- Full test suite passes.
</verification>

<success_criteria>
- 100% test coverage for the new functions and code paths.
- Execution proves optional dependencies are compiled out dynamically.
</success_criteria>

<output>
After completion, create `.planning/v0.6-phases/3/03-03-SUMMARY.md`
</output>
