---
phase: 02-rulestead-flag-correlation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/spine/system_event.ex
  - lib/mix/tasks/parapet.gen.spine.ex
  - priv/repo/migrations/20260517000000_add_parapet_system_events.exs
  - lib/parapet/spine/system_event_pruner.ex
  - lib/parapet/integrations/rulestead.ex
  - test/parapet/integrations/rulestead_test.exs
autonomous: true
requirements:
  - RUL-01
must_haves:
  truths:
    - System can natively buffer generic system events without hard-coupling to external system schemas
    - Extraneous system events are aggressively pruned to prevent storage bloat
    - Rulestead flag changes are buffered when the telemetry event fires
  artifacts:
    - path: lib/parapet/spine/system_event.ex
      provides: Bounded buffer schema
    - path: lib/parapet/spine/system_event_pruner.ex
      provides: GenServer for GC
    - path: lib/parapet/integrations/rulestead.ex
      provides: Telemetry adapter
  key_links:
    - from: lib/parapet/integrations/rulestead.ex
      to: lib/parapet/spine/system_event.ex
      via: Ecto insert on telemetry handle
---

<objective>
Implement `Parapet.SystemEvent` schema, the built-in pruner to prevent bloat, and the Rulestead integration adapter to buffer feature flag mutations natively inside Parapet's spine without deep Ecto coupling.

Purpose: Foundation for correlating incidents with pre-incident administrative actions (like deploys or flag changes) via Telemetry.
Output: Core `SystemEvent` subsystem and `Rulestead` adapter.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/v0.6-phases/2/RESEARCH.md
@.planning/v0.6-phases/2/PATTERNS.md
@.planning/ROADMAP.md

<interfaces>
From `lib/parapet/integrations/scoria.ex`:
```elixir
defmodule Parapet.Integrations.Scoria do
  def attach do
    :telemetry.attach(
      "parapet-scoria-telemetry",
      [:scoria, :sre, :telemetry],
      &__MODULE__.handle_event/4,
      nil
    )
  end
  def handle_event(event, measurements, metadata, _config) do
    # process
  rescue
    e ->
      Logger.error(...)
      :ok
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SystemEvent schema and migrations</name>
  <files>
    - lib/parapet/spine/system_event.ex
    - lib/mix/tasks/parapet.gen.spine.ex
    - priv/repo/migrations/20260517000000_add_parapet_system_events.exs
  </files>
  <action>
    Create the `Parapet.Spine.SystemEvent` Ecto schema. Follow the `TimelineEntry` pattern. Fields: `type` (string), `payload` (map), with standard timestamps.
    Update the Igniter migration body in `lib/mix/tasks/parapet.gen.spine.ex` to create the `parapet_system_events` table (binary_id primary key, index on `inserted_at` for fast pruning).
    Also create a local manual migration `priv/repo/migrations/20260517000000_add_parapet_system_events.exs` so the Parapet test suite has the table available.
  </action>
  <verify>
    <automated>mix ecto.migrate</automated>
  </verify>
  <done>Schema compiles and migrations run without error</done>
</task>

<task type="auto">
  <name>Task 2: Implement built-in GC Pruner</name>
  <files>
    - lib/parapet/spine/system_event_pruner.ex
  </files>
  <action>
    Create a `GenServer` that schedules a `:prune` message on `init` (e.g. every 6 hours). In `handle_info(:prune, state)`, delete all `Parapet.Spine.SystemEvent` records older than 7 days using `Parapet.Evidence.repo().delete_all()`.
    Ensure you `require Logger` and log the pruning actions securely. No Oban dependencies - pure OTP.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>GenServer compiles and correctly implements periodic cleanup</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Rulestead Integration</name>
  <files>
    - lib/parapet/integrations/rulestead.ex
    - test/parapet/integrations/rulestead_test.exs
  </files>
  <behavior>
    - Test 1: `attach/0` registers the telemetry handler.
    - Test 2: Firing `[:rulestead, :admin, :ruleset, :published]` telemetry inserts a `SystemEvent` with the right payload.
    - Test 3: Exceptions in the handler are rescued and do not crash the caller.
  </behavior>
  <action>
    Implement `Parapet.Integrations.Rulestead` following the analog pattern found in `lib/parapet/integrations/scoria.ex`. Define `attach/0` binding to `[:rulestead, :admin, :ruleset, :published]`. 
    Implement `handle_event/4`. Wrap logic in `try..rescue` to prevent exceptions bubbling to the caller.
    On event, insert a `SystemEvent` with `type: "rulestead_flag_change"` and `payload` containing the flag name and new ruleset metadata.
  </action>
  <verify>
    <automated>mix test test/parapet/integrations/rulestead_test.exs</automated>
  </verify>
  <done>Telemetry correctly inserts SystemEvent rows defensively</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Ext -> Parapet | External telemetry payloads (metadata) are untrusted and must be safely serialized |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | `SystemEventPruner` | mitigate | Ensure schema limits blob size, Pruner executes reliably without memory leaks. |
| T-02-02 | Information Disclosure | `Rulestead` adapter | mitigate | Sanitize telemetry metadata to exclude PII/secrets before storing in `SystemEvent.payload`. |
</threat_model>

</threat_model>
