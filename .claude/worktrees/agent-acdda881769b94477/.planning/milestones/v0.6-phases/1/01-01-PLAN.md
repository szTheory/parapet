---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - lib/parapet/plug/metrics.ex
  - lib/parapet/metrics/oban.ex
  - test/parapet/plug/metrics_test.exs
autonomous: true
requirements: [OTL-01.1]
must_haves:
  truths:
    - The :opentelemetry_api is an optional dependency
    - Trace ID is safely extracted if a span exists
    - Trace ID is passed in telemetry metadata, not as a label
  artifacts:
    - path: mix.exs
      provides: optional :opentelemetry_api dependency
    - path: lib/parapet/plug/metrics.ex
      provides: trace extraction logic
  key_links:
    - from: lib/parapet/plug/metrics.ex
      to: ":telemetry"
      via: execute/3 with trace_id in metadata
---

<objective>
Implement OTel trace_id extraction from telemetry events and process dictionary.

Purpose: We need to pull trace context from OpenTelemetry (if it exists) to attach it to our telemetry events, allowing downstream correlation.
Output: Safely extracted `trace_id` in telemetry event metadata without hard-coupling to OTel.
</objective>

<context>
@.planning/v0.6-phases/1/RESEARCH.md
@.planning/v0.6-phases/1/PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add opentelemetry_api dependency</name>
  <files>mix.exs</files>
  <action>Add `{:opentelemetry_api, "~> 1.3", optional: true}` to the deps list in mix.exs.</action>
  <verify>
    <automated>mix deps.get</automated>
  </verify>
  <done>mix.exs updated and dependency resolves</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract trace_id in metrics plug</name>
  <files>lib/parapet/plug/metrics.ex, test/parapet/plug/metrics_test.exs</files>
  <behavior>
    - If OpenTelemetry span exists and is not `:undefined`, extract hex trace ID and add it to the event metadata under the `:trace_id` key.
    - If OpenTelemetry is not loaded, or span is `:undefined`, emit telemetry event normally without crashing.
  </behavior>
  <action>Update `Parapet.Plug.Metrics` to extract trace ID safely using pattern `OpenTelemetry.Tracer.current_span_ctx()`. Ensure `span_ctx != :undefined` before calling `OpenTelemetry.Span.hex_trace_id/1`. Handle `rescue` for cases where OTel is not loaded. Ensure `trace_id` is only appended to the `metadata` payload sent to `:telemetry.execute/3`, and NEVER in `tags` metadata structure to prevent cardinality explosion (per `LabelPolicy`).</action>
  <verify>
    <automated>mix test test/parapet/plug/metrics_test.exs</automated>
  </verify>
  <done>Telemetry event metadata contains `trace_id` when tracing is active</done>
</task>

<task type="auto">
  <name>Task 3: Extract trace_id in Oban metrics</name>
  <files>lib/parapet/metrics/oban.ex</files>
  <action>Apply the same safe OTel trace_id extraction pattern in `Parapet.Metrics.Oban` telemetry handler, appending `trace_id` to the metadata payload sent to `:telemetry.execute/3` without putting it in labels/tags.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Oban metrics event metadata includes `trace_id` when tracing is active</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal state | Tracing data from Erlang process dictionary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | `Parapet.Plug.Metrics` | mitigate | Ensure `trace_id` is passed in metadata, not tags, preventing cardinality explosion in Prometheus endpoints. |
</threat_model>

<verification>
- `mix compile` passes.
- `mix test test/parapet/plug/metrics_test.exs` passes.
</verification>

<success_criteria>
- Telemetry events carry `trace_id` in metadata.
- App does not crash when OTel is missing or unconfigured.
</success_criteria>

<output>
After completion, create `.planning/phases/01-open-telemetry-trace-exemplars/01-01-SUMMARY.md`
</output>
