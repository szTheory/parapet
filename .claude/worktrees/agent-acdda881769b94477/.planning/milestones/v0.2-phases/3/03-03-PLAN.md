---
phase: 3
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/parapet/integrations/mailglass.ex
  - lib/parapet/integrations/chimeway.ex
  - test/parapet/integrations/mailglass_test.exs
  - test/parapet/integrations/chimeway_test.exs
autonomous: true
requirements:
  - ECO-02
  - ECO-04
must_haves:
  truths:
    - "Mailglass adapter compiles out without Mailglass"
    - "Chimeway adapter compiles out without Chimeway"
    - "Mailglass delivery failure events emit SLI telemetry"
  artifacts:
    - path: "lib/parapet/integrations/mailglass.ex"
      provides: "Mailglass telemetry translation"
      contains: "if Code.ensure_loaded?(Mailglass) do"
    - path: "lib/parapet/integrations/chimeway.ex"
      provides: "Chimeway telemetry translation"
      contains: "if Code.ensure_loaded?(Chimeway) do"
  key_links:
    - from: "lib/parapet/integrations/mailglass.ex"
      to: ":telemetry"
      via: "SLI payload emission"
      pattern: ":telemetry.execute\\(\\[:parapet, :journey"
---

<objective>
Implement Mailglass and Chimeway adapters to monitor deliverability SLIs.

Purpose: Achieve ECO-02, translating specific subsystem failures into universal Parapet journeys.
Output: Two tested adapter modules for email integrations.
</objective>

<context>
@.planning/phases/3/PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Mailglass Adapter</name>
  <files>lib/parapet/integrations/mailglass.ex, test/parapet/integrations/mailglass_test.exs</files>
  <behavior>
    - Test: Setup attaches to Mailglass events.
    - Test: Handle event translates Mailglass failure into `[:parapet, :journey, :mail_delivery]` execute.
  </behavior>
  <action>
    Create `Parapet.Integrations.Mailglass` wrapped in `if Code.ensure_loaded?(Mailglass) do`.
    Implement `setup/0` to attach to `[:mailglass, :delivery, :failure]`.
    Implement `handle_event/4` (with rescue block) that extracts duration/metadata and emits Parapet standard SLI telemetry `[:parapet, :journey, :mail_delivery]` with `outcome: :failure`.
  </action>
  <verify>
    <automated>mix test test/parapet/integrations/mailglass_test.exs</automated>
  </verify>
  <done>Mailglass handles failures safely and delegates to Parapet journey metrics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Chimeway Adapter</name>
  <files>lib/parapet/integrations/chimeway.ex, test/parapet/integrations/chimeway_test.exs</files>
  <behavior>
    - Test: Setup attaches to Chimeway events.
    - Test: Translates chimeway delivery failures into SLI metrics safely.
  </behavior>
  <action>
    Create `Parapet.Integrations.Chimeway` wrapped in `if Code.ensure_loaded?(Chimeway) do`.
    Implement `setup/0` and `handle_event/4` (with rescue block) identical in structure to Mailglass, but matching `[:chimeway, :event, :failed]` or similar typical chimney event, emitting Parapet SLI telemetry.
    The new adapter module must be modeled strictly after the closest analog pattern: `lib/parapet/integrations/sigra.ex` (using `Code.ensure_loaded?/1` guards and telemetry safety `rescue` blocks).
  </action>
  <verify>
    <automated>mix test test/parapet/integrations/chimeway_test.exs</automated>
  </verify>
  <done>Chimeway handler functions correctly without crashing.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Telemetry Handler | Incoming metrics from email providers |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | DoS | Event Handlers | mitigate | Wrap all telemetry handle_event blocks in rescue to prevent Host crashes. |
</threat_model>

<success_criteria>
- Mailglass and Chimeway modules map sibling telemetry to Parapet standard SLIs.
- `Code.ensure_loaded?/1` completely masks modules if dependencies are absent.
</success_criteria>

<output>
After completion, create `.planning/phases/3/03-03-SUMMARY.md`
</output>