---
phase: 01
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/parapet/evidence.ex
  - test/parapet/evidence_test.exs
autonomous: true
requirements: [SPINE-04]
must_haves:
  truths:
    - "Application can insert incidents via Evidence context."
    - "High-volume telemetry events cannot bypass the explicit API boundary."
    - "Evidence calls dynamically lookup the host application's Ecto Repo."
  artifacts:
    - path: "lib/parapet/evidence.ex"
      provides: "Public API boundary for Spine schemas"
  key_links:
    - from: "lib/parapet/evidence.ex"
      to: "config :parapet, :repo"
      via: "Application.fetch_env!"
---

<objective>
Create the `Parapet.Evidence` context module.

Purpose: Enforce a clear API boundary that prevents high-volume telemetry from writing to Ecto. All database mutations for Evidence Spine models must go through this context. It acts as the gateway to the host application's Repo.
Output: A module that provides CRUD and transition functions for incidents, timeline entries, and tool audits.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/1/RESEARCH.md

<interfaces>
From lib/parapet/spine/incident.ex (created in 01-01):
```elixir
defmodule Parapet.Spine.Incident do
  use Ecto.Schema
  import Ecto.Changeset
  # schema "parapet_incidents"
  # changeset/2
end
```

From lib/parapet/spine/timeline_entry.ex (created in 01-01):
```elixir
defmodule Parapet.Spine.TimelineEntry do
  use Ecto.Schema
  import Ecto.Changeset
  # schema "parapet_timeline_entries"
  # changeset/2
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Evidence context boundary</name>
  <files>lib/parapet/evidence.ex, test/parapet/evidence_test.exs</files>
  <behavior>
    - `repo/0` fetches `Application.fetch_env!(:parapet, :repo)`.
    - `create_incident(attrs)` -> `{:ok, incident} | {:error, changeset}`
    - `append_timeline(incident_id, attrs)` -> `{:ok, entry} | {:error, changeset}`
    - `log_tool_audit(attrs)` -> `{:ok, audit} | {:error, changeset}`
  </behavior>
  <action>
    Create the `Parapet.Evidence` module. Since the module depends on the host app's Repo, and we do not have a real test database, the tests should use mock objects, or testing the Repo retrieval logic and function structures without full end-to-end DB insertion if no DB is available. (If Ecto sandbox is set up, insert normally, otherwise mock `repo/0` in test or define a dummy repo in `test_helper.exs`).
    Ensure the `repo/0` function cleanly handles missing configuration with a clear error message guiding users to configure `:parapet, :repo`.
  </action>
  <verify>
    <automated>mix test test/parapet/evidence_test.exs</automated>
  </verify>
  <done>Context module exposes explicit boundary functions. Tests confirm structure and repo fetching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telemetry -> Evidence | Explicit API prevents `telemetry.attach` from auto-triggering DB writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service | Evidence Context | mitigate | Context has no implicit event handlers. Developers must explicitly call `create_incident` etc, preventing db connection pool exhaustion from high-volume telemetry. |
</threat_model>

<verification>
mix test test/parapet/evidence_test.exs
</verification>

<success_criteria>
`Parapet.Evidence` boundary is defined and tested. High-volume telemetry is cleanly separated.
</success_criteria>

<output>
After completion, create `.planning/phases/01-spine/01-02-PLAN-SUMMARY.md`
</output>