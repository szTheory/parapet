---
phase: "02-in-app-operator-ui"
plan: "04"
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - mix.exs
  - test/parapet/operator_ui_compile_out_test.exs
  - test/parapet/operator_ui_integration_test.exs
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - Parapet's new operator UI surface has an explicit package/dependency posture and does not accidentally hard-couple every adopter to LiveView.
    - The phase has an end-to-end proof that the operator boundary, generator output, and doctor checks fit together from a host-app perspective.
    - The repo has a compile-safety regression test covering the optional Phoenix/Phoenix LiveView risk called out in research.
  artifacts:
    - path: mix.exs
      provides: explicit dependency/package posture for the operator UI additions
    - path: test/parapet/operator_ui_compile_out_test.exs
      provides: regression proof for optional Phoenix/LiveView compile strategy
    - path: test/parapet/operator_ui_integration_test.exs
      provides: host-facing proof that generator, operator boundary, and doctor assumptions line up
  key_links:
    - from: mix.exs
      to: lib/mix/tasks/parapet.gen.ui.ex
      via: dependency declarations and package file inclusion
    - from: test/parapet/operator_ui_integration_test.exs
      to: lib/parapet/operator.ex
      via: generated UI assumptions matching the public operator API
    - from: test/parapet/operator_ui_integration_test.exs
      to: lib/mix/tasks/parapet.doctor.ex
      via: secure mount expectations validated end to end
---

<objective>
Close the phase with dependency-safety and host-flow verification so the new LiveView surface is shippable without surprising non-Phoenix adopters or leaving the generator/doctor contract unproven.

Purpose: Address the optional Phoenix/Phoenix LiveView risk explicitly and add end-to-end evidence that Plans 01-03 compose correctly.
Output: Package/dependency adjustments plus compile-safety and host-facing integration tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v0.2-ROADMAP.md
@.planning/milestones/v0.2-REQUIREMENTS.md
@.planning/milestones/v0.2-phases/2-in-app-operator-ui/2-CONTEXT.md
@.planning/milestones/v0.2-phases/2-in-app-operator-ui/RESEARCH.md
@mix.exs

<interfaces>
From `mix.exs` today:

```elixir
def project do
  [
    package: [files: ~w(lib priv .formatter.exs mix.exs README* docs)],
    deps: deps()
  ]
end

defp deps do
  [
    {:igniter, "~> 0.7.9"},
    {:telemetry, "~> 1.2"},
    {:telemetry_metrics, "~> 1.0"},
    {:oban, ">= 0.0.0", optional: true},
    {:sigra, ">= 0.0.0", optional: true}
  ]
end
```

Risk to resolve:
- `mix.exs` does not currently declare Phoenix or Phoenix LiveView as direct Parapet dependencies.
- `mix.lock` already contains transitive `phoenix` and `phoenix_live_view` packages via optional dependencies such as `sigra -> flop_phoenix`.
- The phase must therefore distinguish lockfile presence from direct compile-time coupling and define compile-out expectations against the real baseline: transitive packages may exist in the lock, but core Parapet modules still must not require Phoenix unless the UI path is explicitly opted into.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compile-safety and host-flow tests first</name>
  <files>test/parapet/operator_ui_compile_out_test.exs, test/parapet/operator_ui_integration_test.exs</files>
  <behavior>
    - Test 1: The repo's chosen Phoenix/LiveView strategy compiles cleanly when Phoenix packages are present only transitively in `mix.lock` but not declared as direct UI-runtime deps for Parapet's core modules, or fails with an intentional actionable error if the generator path requires opt-in deps.
    - Test 2: A host-flow integration test proves the generated UI assumptions line up with `Parapet.Operator` data/actions and with doctor's authenticated-mount expectations.
    - Test 3: The integration proof confirms the UI stays generator-first and host-owned rather than mounting a packaged admin app.
  </behavior>
  <action>Write the regression tests that force an explicit answer to the dependency risk identified in research against the actual repo baseline. The tests must distinguish direct dependency posture from transitive lockfile presence: prove that core Parapet code remains Phoenix-free when `phoenix` and `phoenix_live_view` appear only through optional/transitive packages, and prove that any UI-specific modules or templates stay behind explicit generator or compile guards. Cover both compile posture and host-flow composition, using D-13 through D-16 as non-negotiable constraints and confirming the workbench still depends on the Phase 01 operator boundary rather than raw schemas.</action>
  <verify>
    <automated>mix test test/parapet/operator_ui_compile_out_test.exs test/parapet/operator_ui_integration_test.exs</automated>
  </verify>
  <done>The repo has failing tests that define the required dependency and host-flow guarantees before `mix.exs` or related implementation is adjusted.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize dependency posture and package wiring</name>
  <files>mix.exs, test/parapet/operator_ui_compile_out_test.exs, test/parapet/operator_ui_integration_test.exs</files>
  <action>Update `mix.exs` to codify the safest Phase 2 dependency posture described in research against the real baseline: preserve generator-first adoption, keep direct runtime Phoenix coupling out of core Parapet unless the UI surface explicitly opts in, and document or encode any compile guards needed because Phoenix packages already exist transitively in `mix.lock`. Adjust package metadata, optional deps, or test setup only as needed to make the strategy real. Re-run the new integration proofs so the final phase validates UI-01 through UI-04 together, including external-link posture per D-05 and audited action semantics per D-17 through D-19.</action>
  <verify>
    <automated>mix test test/parapet/operator_ui_compile_out_test.exs test/parapet/operator_ui_integration_test.exs test/parapet/operator_test.exs test/mix/tasks/parapet.gen.ui_test.exs test/mix/tasks/parapet.doctor_test.exs</automated>
  </verify>
  <done>The package has an explicit safe dependency posture, and the end-to-end test slice proves the operator boundary, generator, and doctor checks work together without violating host ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package dependency graph -> adopter application | New Phoenix-related deps can accidentally force UI concerns onto non-UI adopters. |
| Integration proof -> release confidence | Missing host-flow validation can hide coupling or insecure mount assumptions until after release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Denial of Service | `mix.exs` dependency posture | mitigate | Keep direct Phoenix/LiveView coupling optional or otherwise isolated behind a compile boundary so non-UI adopters do not lose buildability even when Phoenix packages are already present transitively in the lockfile. |
| T-02-14 | Elevation of Privilege | packaged UI posture | mitigate | Preserve generator-first host ownership and prove via integration tests that Parapet does not mount or own auth/router behavior. |
| T-02-15 | Repudiation | end-to-end operator flow | mitigate | Include operator-boundary plus doctor expectations in one integration proof so audit semantics and secure mount assumptions are exercised together. |
</threat_model>

<verification>
mix test test/parapet/operator_ui_compile_out_test.exs test/parapet/operator_ui_integration_test.exs test/parapet/operator_test.exs test/mix/tasks/parapet.gen.ui_test.exs test/mix/tasks/parapet.doctor_test.exs
mix format --check-formatted
</verification>

<success_criteria>
The repo has a documented and tested Phoenix/LiveView dependency posture.
The final test slice exercises the operator boundary, generator assumptions, and doctor security checks together.
Phase 2 can ship without hidden hard coupling, hidden auth ownership, or missing end-to-end proof.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.2-phases/2-in-app-operator-ui/02-04-SUMMARY.md`
</output>
