---
phase: 04
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - priv/templates/parapet.gen.ui/operator_live.ex.eex
  - priv/templates/parapet.gen.ui/operator_components.ex.eex
  - lib/parapet/operator.ex
autonomous: true
requirements:
  - AI-HITL-03
must_haves:
  truths:
    - "Operator UI lists action items needing attention."
    - "Operator UI renders a deep link to the external application using configured MFA."
  artifacts:
    - path: "priv/templates/parapet.gen.ui/operator_components.ex.eex"
      provides: "ActionItem deep linking UI"
      contains: "ui_url_resolver"
  key_links:
    - from: "priv/templates/parapet.gen.ui/operator_components.ex.eex"
      to: "Application config"
      via: "MFA application"
      pattern: "apply(mod, fun, args"
---

<objective>
Update Operator UI templates to render ActionItems and handle MFA-based deep linking.

Purpose: AI-HITL-03 requires operators to review stale workflow items directly in Scoria without building a separate UI.
Output: UI listing ActionItems and a component rendering an external deep link.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/4/RESEARCH.md
@priv/templates/parapet.gen.ui/operator_live.ex.eex
@priv/templates/parapet.gen.ui/operator_components.ex.eex
</context>

<interfaces>
From lib/parapet/operator.ex:
```elixir
def queue_query()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add ActionItems Query</name>
  <files>lib/parapet/operator.ex, test/parapet/operator_test.exs</files>
  <action>Add `action_items_query()` to `Parapet.Operator` that returns a query for `Parapet.Spine.ActionItem` where `state == "open"`, ordered by inserted_at ascending.</action>
  <verify>
    <automated>mix test test/parapet/operator_test.exs</automated>
  </verify>
  <done>Query function exposed and tested.</done>
</task>

<task type="auto">
  <name>Task 2: ActionItem UI Component</name>
  <files>priv/templates/parapet.gen.ui/operator_components.ex.eex, test/parapet/operator_ui_compile_out_test.exs</files>
  <action>Add `action_item_list(assigns)` component iterating over `@items`. Add `action_item_card(assigns)` component. In the card, read the `ui_url_resolver` configuration from `Application.get_env(:parapet, :scoria)[:ui_url_resolver]`. If present, unpack `{mod, fun, args}`, and compute the URL via `apply(mod, fun, args ++ [@item.external_id])`. Render an `<a>` tag with this URL linking to the host UI. If not present, render text "No resolver configured". Use the Deep Link UI and Action Rail patterns from `priv/templates/parapet.gen.ui/operator_components.ex.eex` in PATTERNS.md.</action>
  <verify>
    <automated>mix test test/parapet/operator_ui_compile_out_test.exs</automated>
  </verify>
  <done>Deep linking component implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate ActionItems into LiveView</name>
  <files>priv/templates/parapet.gen.ui/operator_live.ex.eex, test/parapet/operator_ui_integration_test.exs</files>
  <action>In `mount/3`, fetch `action_items = <%= inspect(@repo_module) %>.all(Parapet.Operator.action_items_query())` and assign to socket. In the `render` view, render the `<.action_item_list items={@action_items} />` within the UI, alongside or within the Incident Queue.</action>
  <verify>
    <automated>mix test test/parapet/operator_ui_integration_test.exs</automated>
  </verify>
  <done>LiveView loads and displays ActionItems, verified via integration test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App -> UI | Data shown in UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Elevation of Privilege | MFA Config | accept | `ui_url_resolver` is configured via `sys.config` by developers, not users. Assumed safe. |
</threat_model>
