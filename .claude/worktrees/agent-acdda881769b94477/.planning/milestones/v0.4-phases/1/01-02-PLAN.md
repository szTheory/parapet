---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/mix/tasks/parapet.gen.scoria.ex
  - priv/templates/parapet.gen.scoria/scoria_dashboard.json.eex
  - priv/templates/parapet.gen.scoria/rules.yml.eex
  - lib/mix/tasks/parapet.install.ex
autonomous: true
requirements:
  - AI-TELEMETRY-02
must_haves:
  truths:
    - Running parapet.gen.scoria scaffolds a standalone dashboard and rules
    - The parapet installer can conditionally compose the scoria generator
  artifacts:
    - path: lib/mix/tasks/parapet.gen.scoria.ex
      provides: Scoria generator Mix task
      contains: Igniter.Mix.Task
    - path: priv/templates/parapet.gen.scoria/scoria_dashboard.json.eex
      provides: Dashboard template
    - path: lib/mix/tasks/parapet.install.ex
      provides: Parapet installer
      contains: with_scoria
  key_links:
    - from: lib/mix/tasks/parapet.install.ex
      to: lib/mix/tasks/parapet.gen.scoria.ex
      via: Igniter.compose_task
---

<objective>
Create the `mix parapet.gen.scoria` generator to scaffold Grafana dashboards and Prometheus rules, and wire it into the main Parapet installer.

Purpose: Provides operators with out-of-the-box visibility into AI token usage, costs, and latencies without manual dashboard configuration.
Output: A new Igniter Mix task, EEx templates for Scoria dashboards/rules, and updates to `mix parapet.install`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1/1-PATTERNS.md
@.planning/phases/1/RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build Tooling | Code generator modifying the host application's codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Mix.Tasks.Parapet.Install | mitigate | Use Igniter AST manipulation instead of regex or naive string replacement to ensure safe injection of the Scoria setup code into the host's instrumenter. |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create Scoria Generator Task and Templates</name>
  <files>
    lib/mix/tasks/parapet.gen.scoria.ex,
    test/mix/tasks/parapet.gen.scoria_test.exs,
    priv/templates/parapet.gen.scoria/scoria_dashboard.json.eex,
    priv/templates/parapet.gen.scoria/rules.yml.eex
  </files>
  <behavior>
    - Test 1: Info schema has no required args
    - Test 2: Task creates `priv/parapet/grafana/dashboards/scoria_dashboard.json` and `priv/parapet/prometheus/rules.yml`
  </behavior>
  <action>
    Implement `Mix.Tasks.Parapet.Gen.Scoria` using `Igniter.Mix.Task`.
    Create dummy EEx templates (`scoria_dashboard.json.eex` containing an empty `{}` JSON dashboard for now, and `rules.yml.eex` containing basic prometheus rule stubs for `scoria_llm_token_count_total` and others).
    In the task `igniter/1` function, copy the contents of these EEx files to the host app at `priv/parapet/grafana/dashboards/scoria_dashboard.json` and `priv/parapet/prometheus/rules.yml` using `Igniter.create_new_file`.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/parapet.gen.scoria_test.exs || mix compile</automated>
  </verify>
  <done>Generator successfully places template files into the target project.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Scoria Generator into Parapet Installer</name>
  <files>lib/mix/tasks/parapet.install.ex</files>
  <action>
    Update `Mix.Tasks.Parapet.Install`.
    - Modify `info/2` schema to include `with_scoria: :boolean` (default `false`).
    - In `igniter/1`, check if `--with-scoria` is set. If so, call `Igniter.compose_task(igniter, "parapet.gen.scoria", [])`.
    - Update `setup_code` generation: dynamically build the `setup/0` body to include the conditional `Code.ensure_loaded?(Parapet.Integrations.Scoria)` check block if `--with-scoria` is true. Ensure that if `--with-sigra` is also true, both setup blocks are included in the generated string.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Installer supports --with-scoria flag and outputs proper setup code.</done>
</task>
</tasks>
