---
phase: 03-ai-deploy-correlation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/integrations/scoria.ex
  - test/parapet/integrations/scoria_test.exs
  - lib/parapet/metrics/scoria.ex
  - test/parapet/metrics/scoria_test.exs
autonomous: true
requirements:
  - AI-DEPLOY-01
  - AI-DEPLOY-03
must_haves:
  truths:
    - AI Config Deployment events map to durably saved Incidents
    - Unbounded Scoria MCP errors are filtered to bounded failure modes safely
    - MCP error metric is declared for Prometheus exporting
  artifacts:
    - path: lib/parapet/integrations/scoria.ex
      provides: Telemetry handlers mapping AI config to Ecto and mapping MCP errors to SLIs
    - path: lib/parapet/metrics/scoria.ex
      provides: Declaration of scoria_mcp_errors_total metric
  key_links:
    - from: lib/parapet/integrations/scoria.ex
      to: Parapet.Evidence.create_incident/1
      via: Calling evidence module for AI config events
---

<objective>
Intercept AI Config changes and explicit MCP failure modes from Scoria SRE telemetry, creating durable markers and mapping bounded metrics safely.

Purpose: AI-DEPLOY-01 and AI-DEPLOY-03 - We need to track configuration drift inside our Ecto database to align exact truths for observability, while protecting our Prometheus backend from cardinality explosions generated by raw Scoria errors.
Output: Telemetry translators for config deployments and MCP exceptions, plus updated test assertions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/3/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Handle AI Config Deployed Event</name>
  <files>lib/parapet/integrations/scoria.ex, test/parapet/integrations/scoria_test.exs</files>
  <behavior>
    - Test 1: Given a `[:scoria, :config, :deployed]` event, it delegates to `Parapet.Evidence.create_incident/1` with type `config_change`.
    - Test 2: The Ecto `runbook_data` map saves `scorer_version`, `baseline_version`, and `model` correctly.
  </behavior>
  <action>
  Attach a new `:telemetry.attach/4` in `setup/0` (or update existing) to listen for `[:scoria, :config, :deployed]`. 
  Implement `process_event([:scoria, :config, :deployed], _measurements, metadata)` that calls `Parapet.Evidence.create_incident/1`.
  Set `title` to "AI Config Deployed", `state` to "open", and `runbook_data` to `%{"type" => "config_change", "scorer_version" => metadata[:scorer_version], "baseline_version" => metadata[:baseline_version], "model" => metadata[:model]}`.
  Follow SRE Event Processing Pattern from PATTERNS.md for telemetry mapping.
  </action>
  <verify>
    <automated>mix test test/parapet/integrations/scoria_test.exs</automated>
  </verify>
  <done>AI config changes map directly to exact Ecto markers without duplicate entries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handle MCP Tool Exception Event</name>
  <files>lib/parapet/integrations/scoria.ex, test/parapet/integrations/scoria_test.exs</files>
  <behavior>
    - Test 1: Given a `[:scoria, :mcp, :tool, :exception]`, maps reason `:timeout` to `"timeout"`.
    - Test 2: Unknown reasons fall back to `"execution_failed"`.
    - Test 3: Emits translated event `[:parapet, :scoria, :mcp, :error]`.
  </behavior>
  <action>
  Attach a telemetry listener for `[:scoria, :mcp, :tool, :exception]` in `setup/0`.
  Create `process_event([:scoria, :mcp, :tool, :exception], measurements, metadata)` which extracts `metadata.error`.
  Define a private `map_mcp_failure/1` matching atoms (`%{reason: :timeout} -> "timeout"`, `%{reason: :breaker_open} -> "breaker_open"`, `%{reason: :access_denied} -> "access_denied"`) and a catch-all returning `"execution_failed"`.
  Call `:telemetry.execute([:parapet, :scoria, :mcp, :error], measurements, %{reason: mapped_reason, tool_name: metadata[:tool_name]})`.
  </action>
  <verify>
    <automated>mix test test/parapet/integrations/scoria_test.exs</automated>
  </verify>
  <done>Exceptions are bounded to safe strings protecting against Prometheus cardinality limits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Declare MCP Errors Metric</name>
  <files>lib/parapet/metrics/scoria.ex, test/parapet/metrics/scoria_test.exs</files>
  <behavior>
    - Test 1: The metric list includes `scoria_mcp_errors_total` with tags `:reason` and `:tool_name`.
  </behavior>
  <action>
  Update `metrics/0` in `Parapet.Metrics.Scoria` to append a new `counter/2`:
  `counter("scoria_mcp_errors_total", event_name: [:parapet, :scoria, :mcp, :error], tags: [:reason, :tool_name], description: "Total number of Scoria MCP tool failures")`.
  Update corresponding tests to assert this metric exists in the return list.
  Follow Metrics Declaration Pattern from PATTERNS.md.
  </action>
  <verify>
    <automated>mix test test/parapet/metrics/scoria_test.exs</automated>
  </verify>
  <done>Prometheus scraper exposes scoria_mcp_errors_total metrics safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scoria -> Parapet Telemetry | Receiving unbounded raw error maps from an external system execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | lib/parapet/integrations/scoria.ex | mitigate | Using `map_mcp_failure` fallback strategy ensures unbounded exception messages do not bloat cardinality in metrics registry. |
</threat_model>

<verification>
mix test test/parapet/integrations/scoria_test.exs test/parapet/metrics/scoria_test.exs
</verification>

<success_criteria>
Tests pass verifying the successful bounded telemetry mapping, and the exact database writes.
</success_criteria>

<output>
After completion, create `.planning/phases/3/03-01-SUMMARY.md`
</output>