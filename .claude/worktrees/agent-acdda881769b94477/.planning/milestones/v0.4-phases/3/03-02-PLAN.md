---
phase: 03-ai-deploy-correlation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/parapet.gen.grafana/main_dashboard.json.eex
  - test/mix/tasks/parapet.gen.grafana_test.exs
autonomous: true
requirements:
  - AI-DEPLOY-02
must_haves:
  truths:
    - User can correlate AI Config Deployments on the Grafana dashboard via Ecto annotations.
  artifacts:
    - path: priv/templates/parapet.gen.grafana/main_dashboard.json.eex
      provides: Annotation JSON mapping and Postgres datasource definition
  key_links:
    - from: priv/templates/parapet.gen.grafana/main_dashboard.json.eex
      to: Ecto parapet_incidents
      via: JSON annotation SQL query mapping
---

<objective>
Update the Grafana generation templates to overlay AI Configuration Deployments using Postgres querying.

Purpose: AI-DEPLOY-02 - Operators need a simple timeline overlay connecting when an AI config drift happens natively, mapped to the same visual grid as their standard SLO error ratios.
Output: An augmented `main_dashboard.json.eex` containing explicit Postgres annotation targets and variables.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/3/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate Postgres Datasource and Annotations into Dashboard Template</name>
  <files>priv/templates/parapet.gen.grafana/main_dashboard.json.eex, test/mix/tasks/parapet.gen.grafana_test.exs</files>
  <behavior>
    - Test 1: The generated dashboard includes a `DS_POSTGRES` datasource variable in its `templating` array.
    - Test 2: The generated dashboard has an annotation named `AI Config Changes` targeting `DS_POSTGRES`.
  </behavior>
  <action>
  Modify `priv/templates/parapet.gen.grafana/main_dashboard.json.eex`:
  1. Under `"templating" -> "list"`, append a new variable configuration block for `"DS_POSTGRES"` (similar to `DS_PROMETHEUS` but with `type: "postgres"` and `query: "postgres"`).
  2. Under `"annotations" -> "list"`, append a new annotation named `"AI Config Changes"`. Map its `datasource.uid` to `"${DS_POSTGRES}"`. Set `"rawQuery"` to: `"SELECT inserted_at AS \"time\", title AS \"text\", runbook_data->>'model' AS \"tags\" FROM parapet_incidents WHERE runbook_data->>'type' = 'config_change'"`. Set `iconColor` to `"rgb(255, 0, 255)"`.
  Modify `test/mix/tasks/parapet.gen.grafana_test.exs` to assert that executing `mix parapet.gen.grafana` generates a dashboard that contains `DS_POSTGRES` and `AI Config Changes`.
  Reference Annotation Tracking Deploy/Config Patterns from PATTERNS.md for the overall structure.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/parapet.gen.grafana_test.exs</automated>
  </verify>
  <done>Grafana dashboards expose exactly correlated deployment lines without Prom tailing delays.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grafana -> Postgres | Execution of raw queries against Ecto database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | priv/templates/parapet.gen.grafana/main_dashboard.json.eex | accept | Operators control Grafana Postgres variables. Readonly SELECT bounds risk appropriately. |
</threat_model>

<verification>
mix test test/mix/tasks/parapet.gen.grafana_test.exs
</verification>

<success_criteria>
Generating the dashboard correctly templates Postgres references that can interact with `parapet_incidents`.
</success_criteria>

<output>
After completion, create `.planning/phases/3/03-02-SUMMARY.md`
</output>