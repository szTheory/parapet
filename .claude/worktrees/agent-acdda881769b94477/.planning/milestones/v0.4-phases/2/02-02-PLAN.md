---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/parapet/slo/scoria_eval.ex
  - lib/parapet/metrics/scoria.ex
  - lib/parapet/integrations/scoria.ex
autonomous: true
requirements:
  - AI-SLO-02
must_haves:
  truths:
    - Developers can instantiate `Parapet.SLO.ScoriaEval.new/1` and have it correctly translated to PromQL.
    - Scoria telemetry `[:scoria, :eval, :completed]` is safely converted to the `scoria_evaluation_total` Prometheus counter.
    - The metric strictly enforces low cardinality limits (stripping trace IDs or prompt hashes).
  artifacts:
    - path: lib/parapet/slo/scoria_eval.ex
      provides: Scoria-specific SLO definition struct
      contains: Parapet.SLO.Resolvable implementation
    - path: lib/parapet/metrics/scoria.ex
      provides: Telemetry handler and metrics definitions for Scoria
    - path: lib/parapet/integrations/scoria.ex
      provides: Global integration setup
      contains: Parapet.Metrics.Scoria.setup()
  key_links:
    - from: lib/parapet/slo/scoria_eval.ex
      to: lib/parapet/metrics/scoria.ex
      via: "PromQL references scoria_evaluation_total"
    - from: lib/parapet/integrations/scoria.ex
      to: lib/parapet/metrics/scoria.ex
      via: setup/0
---

<objective>
Implement the Scoria-specific SLO definition struct (`Parapet.SLO.ScoriaEval`) and the telemetry-to-metrics pipeline (`Parapet.Metrics.Scoria`) to safely emit and track AI evaluation pass rates.

Purpose: Operators can define, monitor, and alert on system objectives derived from AI evaluation pass rates, without risking Prometheus TSDB bloat from high-cardinality metadata.
Output: The `Parapet.SLO.ScoriaEval` struct, the `Parapet.Metrics.Scoria` metric adapter, and an updated Scoria integration setup.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/2/RESEARCH.md
@.planning/phases/2/DECISIONS.md
@.planning/phases/2/PATTERNS.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telemetry to Metrics | Converting untrusted Scoria telemetry metadata into Prometheus labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Denial of Service | Parapet.Metrics.Scoria | mitigate | Strictly drop high-cardinality metadata (e.g., `trace_id`, `prompt_hash`) from the metrics event to prevent Prometheus TSDB OOMs. Only allow `guardrail`, `passed`, and `model_name`. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Scoria Metrics Pipeline</name>
  <files>
    lib/parapet/metrics/scoria.ex,
    test/parapet/metrics/scoria_test.exs
  </files>
  <behavior>
    - Test 1: Telemetry handler extracts only safe labels (`guardrail`, `passed`, `model_name`) and drops others like `trace_id`.
  </behavior>
  <action>
    Create `Parapet.Metrics.Scoria`. Implement `setup/0` which attaches to `[:scoria, :eval, :completed]` using `:telemetry.attach/4`. 
    The handler function must strictly enforce cardinality by dropping everything except `guardrail`, `passed`, and `model_name` from the telemetry metadata. Then emit a downstream event `[:parapet, :scoria, :eval, :completed]` for the actual metrics definitions.
    Implement `metrics/0` (following `Parapet.Metrics.HTTP` pattern) which returns a `Telemetry.Metrics.counter` for `scoria_evaluation_total` based on the sanitized downstream event.
  </action>
  <verify>
    <automated>mix test test/parapet/metrics/scoria_test.exs</automated>
  </verify>
  <done>Scoria telemetry is correctly processed and converted to safe metric definitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ScoriaEval SLO Struct</name>
  <files>
    lib/parapet/slo/scoria_eval.ex,
    test/parapet/slo/scoria_eval_test.exs
  </files>
  <behavior>
    - Test 1: Instantiating `new/1` with invalid properties raises an ArgumentError.
    - Test 2: `Parapet.SLO.Resolvable.to_slo/1` correctly constructs the `scoria_evaluation_total` PromQL strings.
  </behavior>
  <action>
    Create `Parapet.SLO.ScoriaEval`. Define struct with `[:name, :objective, :guardrail, :labels, :runbook]`. Reference analog `lib/parapet/slo/http.ex` from `PATTERNS.md` and use the pattern excerpts for struct definition. Implement `new/1` to build and validate the struct, explicitly referencing the shared pattern 'Struct Validation' from `lib/parapet/slo.ex` in `PATTERNS.md`.
    Implement the `Parapet.SLO.Resolvable` protocol for `%Parapet.SLO.ScoriaEval{}`. In `to_slo/1`, map the struct to `%Parapet.SLO{}`. 
    Construct `good_events` as `"sum(rate(scoria_evaluation_total{guardrail=\"#\{eval.guardrail}\", passed=\"true\"#\{labels_str}\}[window]))"` and `total_events` without `passed=\"true\"`. Use a helper function `format_labels` to process `eval.labels`.
  </action>
  <verify>
    <automated>mix test test/parapet/slo/scoria_eval_test.exs</automated>
  </verify>
  <done>ScoriaEval structs can be resolved to accurate Parapet.SLO rules.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Scoria Metrics in Integration Setup</name>
  <files>
    lib/parapet/integrations/scoria.ex
  </files>
  <action>
    Update `Parapet.Integrations.Scoria.setup/0`. Add a call to `Parapet.Metrics.Scoria.setup()` ensuring that when the integration is initialized, both the SRE error telemetry (from Phase 1) and the new evaluation metrics are configured.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Scoria Metrics initialization is wired into the main integration flow.</done>
</task>

</tasks>

<success_criteria>
- The telemetry pipeline securely protects downstream TSDBs from high-cardinality metadata.
- `Parapet.SLO.ScoriaEval` generates correct PromQL to track evaluation error budgets.
</success_criteria>

<output>
After completion, create `.planning/phases/2/02-02-SUMMARY.md`
</output>
