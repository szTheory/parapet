---
phase: "01"
plan: "05"
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified: ["test/test_helper.exs", "test/parapet/internal/label_policy_test.exs", "test/parapet/internal/safe_handler_test.exs", "test/mix/tasks/parapet.install_test.exs", "test/mix/tasks/verify.public_api_test.exs"]
autonomous: true
requirements: ["TELE-03", "TELE-04", "ERR-01", "PKG-04", "INST-01", "INST-02", "INST-03", "INST-04", "INST-05"]
must_haves:
  truths:
    - "Test suite completely exercises telemetry safety, label policy, and mix tasks"
  artifacts:
    - path: "test/test_helper.exs"
      provides: "Test framework setup"
    - path: "test/parapet/internal/label_policy_test.exs"
      provides: "Label policy unit tests"
    - path: "test/parapet/internal/safe_handler_test.exs"
      provides: "Telemetry safe handler and error logging tests"
    - path: "test/mix/tasks/parapet.install_test.exs"
      provides: "Installer idempotent and dry-run tests"
    - path: "test/mix/tasks/verify.public_api_test.exs"
      provides: "Public API manifest validation tests"
  key_links: []
---

<objective>
Implement the validation strategy defined in 1-VALIDATION.md for Phase 1.

Purpose: Provide rigorous automated testing for error boundaries, module documentation checks, label policies, and the Igniter code generator to satisfy Nyquist compliance.
Output: A complete test suite running under ExUnit.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1-foundation/1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Test Helper & Label Policy Tests</name>
  <files>test/test_helper.exs, test/parapet/internal/label_policy_test.exs</files>
  <action>
1. If `test/test_helper.exs` does not exist, create it with `ExUnit.start()`.
2. Create `test/parapet/internal/label_policy_test.exs`. Add unit tests to verify that `Parapet.Internal.LabelPolicy.assert_safe!/1` successfully permits allowed labels, and correctly raises an `ArgumentError` when high-cardinality labels (like `user_id`, `raw_data`, `token`, `path`) are provided.
  </action>
  <acceptance_criteria>
    - `test/test_helper.exs` calls `ExUnit.start()`
    - `label_policy_test.exs` asserts both valid labels and raised exceptions for invalid labels.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/parapet/internal/label_policy_test.exs</automated>
  </verify>
  <done>Label policy is thoroughly tested against cardinality rules.</done>
</task>

<task type="auto">
  <name>Task 2: Safe Handler & Error Logging Tests</name>
  <files>test/parapet/internal/safe_handler_test.exs</files>
  <action>
Create `test/parapet/internal/safe_handler_test.exs`.
Write tests to ensure that `Parapet.Internal.SafeHandler.attach/5` properly hooks into `:telemetry`. Execute a telemetry event that triggers a crashing handler. Verify that the exception is rescued (the process does not crash) and that the exception along with its stacktrace is logged using `ExUnit.CaptureLog`.
  </action>
  <acceptance_criteria>
    - Captures logs for the crashing handler and asserts on `Exception.message` and `__STACKTRACE__`.
    - Host process remains alive.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/parapet/internal/safe_handler_test.exs</automated>
  </verify>
  <done>Safe handler properly traps exceptions and logs stacktraces.</done>
</task>

<task type="auto">
  <name>Task 3: Mix Tasks Tests (Install & Verify Public API)</name>
  <files>test/mix/tasks/parapet.install_test.exs, test/mix/tasks/verify.public_api_test.exs</files>
  <action>
1. Create `test/mix/tasks/parapet.install_test.exs`. Implement an integration test using Igniter's testing utilities (`Igniter.Test`) to assert that `mix parapet.install` correctly injects `Parapet.Plug.Metrics` into an `Endpoint`, sets up `config.exs`, handles idempotency, and respects `--dry-run`.
2. Create `test/mix/tasks/verify.public_api_test.exs`. Write tests asserting that `Mix.Tasks.Verify.PublicApi` generates the structured machine-readable manifest of public modules, and correctly exits or returns when documentation is missing on a public boundary versus hidden on `Parapet.Internal`.
  </action>
  <acceptance_criteria>
    - Installer tests verify idempotency and configuration injection.
    - Public API verify tests assert manifest generation and doc rules.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/parapet.install_test.exs test/mix/tasks/verify.public_api_test.exs</automated>
  </verify>
  <done>Mix tasks are tested end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing | Test boundaries enforce the library constraints locally before CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Repudiation | Test Suite | mitigate | Use ExUnit.CaptureLog to verify exception logs actually exist and include the stacktrace. |
</threat_model>

<verification>
mix test
</verification>

<success_criteria>
All Phase 1 requirements have their automated tests passing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>