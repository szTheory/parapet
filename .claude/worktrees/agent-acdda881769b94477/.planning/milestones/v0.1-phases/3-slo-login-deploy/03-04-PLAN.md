---
phase: "03-slo-login-deploy"
plan: "04"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - "lib/mix/tasks/parapet.doctor.ex"
  - "test/mix/tasks/parapet.doctor_test.exs"
  - "docs/slo-reference.md"
autonomous: true
requirements:
  - DOCS-03
must_haves:
  truths:
    - "mix parapet.doctor successfully validates runbook presence in SLO definitions"
    - "The CLI task exits with code 2 if any runbook is missing"
  artifacts:
    - path: "lib/mix/tasks/parapet.doctor.ex"
      provides: "Mix task for environment and configuration safety checks"
    - path: "test/mix/tasks/parapet.doctor_test.exs"
      provides: "Tests for the doctor CLI output and exit codes"
  key_links:
    - from: "lib/mix/tasks/parapet.doctor.ex"
      to: "lib/parapet/slo.ex"
      via: "Iterates through available SLOs"
---

<objective>
Implement the `mix parapet.doctor` Mix task to statically analyze the application's Parapet configuration, specifically enforcing that every defined SLO contains an actionable runbook link, and write SLO documentation.

Purpose: Prevent the anti-pattern of firing alerts without remediation steps. Ensure operators have the necessary context when paged.
Output: A Mix task `parapet.doctor`, its corresponding integration test, and `docs/slo-reference.md`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/3-slo-login-deploy/RESEARCH.md
@.planning/phases/3-slo-login-deploy/PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Parapet Doctor Task</name>
  <files>lib/mix/tasks/parapet.doctor.ex, test/mix/tasks/parapet.doctor_test.exs</files>
  <behavior>
    - Defines `Mix.Tasks.Parapet.Doctor`.
    - Iterates over application-defined SLOs (mocked or loaded depending on context).
    - Validates that `runbook` property is present and is a non-empty string on all defined SLOs.
    - If a runbook is missing, prints an error message and calls `System.halt(2)`.
    - Tests intercept `System.halt` (e.g. via mock or asserting on exit) and assert on CLI output.
  </behavior>
  <action>
    Implement `lib/mix/tasks/parapet.doctor.ex` based on the `Mix.Tasks.Verify.PublicApi` pattern shown in PATTERNS.md. Ensure it forces an application compilation/load first so the SLOs are detectable. Assert that `System.halt(2)` is invoked strictly when runbooks are missing.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/parapet.doctor_test.exs</automated>
  </verify>
  <done>Doctor task correctly checks runbooks and halts the system with the expected exit code on failure.</done>
</task>

<task type="auto">
  <name>Task 2: Create SLO Reference Documentation</name>
  <files>docs/slo-reference.md</files>
  <action>
    Create or update `docs/slo-reference.md` with a complete SLO definition example (DOCS-03). The documentation should demonstrate how to define a custom SLO (like the login journey) and explain the required fields, focusing on the runbook URL and multi-window burn-rate mechanics.
  </action>
  <verify>
    <automated>test -f docs/slo-reference.md || echo "Created docs/slo-reference.md"</automated>
  </verify>
  <done>slo-reference.md exists and contains the complete SLO definition example.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI/CD Pipeline | CLI task executed in trusted pipeline environments |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | `lib/mix/tasks/parapet.doctor.ex` | accept | CLI task is executed by the developer or CI system locally; no external input vectors. |
</threat_model>

<verification>
1. `mix test test/mix/tasks/parapet.doctor_test.exs` passes.
2. Running `mix parapet.doctor` on a bad configuration exits with `2`.
</verification>

<success_criteria>
- Missing runbooks in SLO definitions fail the safety gate reliably.
- The task conforms to the Mix Task convention.
- SLO documentation reference is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-slo-login-deploy/03-04-SUMMARY.md`
</output>
