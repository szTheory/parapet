# Plan: 04-04 - Day-1 Guide and Launch Readiness

## Goal
Update the `README.md` and complete documentation to provide a seamless "Zero to First Alert" experience. (Addresses DOCS-01).

## Approach
1. **README Overhaul:** Rewrite the README to focus entirely on the operator loop:
    - Installation (`mix parapet.install`)
    - Defining the first SLO.
    - Generating Artifacts (`mix parapet.gen.prometheus` & `grafana`).
    - Validating with the Doctor (`mix parapet.doctor`).
    - Viewing the dashboard.
2. **Verify Phase 1-3 Requirements:** Do a final sweep of the `REQUIREMENTS.md` to ensure all integer phase requirements (PKG-01..04, OSS-01..04, etc.) are truly met and documented.
3. **Public API Audit:** Run `mix verify.public_api` (or similar `mix docs` strict mode) to ensure the public API is tightly locked and documented.
4. **Release Prep:** Ensure `mix.exs` has the correct `files:` whitelist so we don't publish the `.planning` folder.

## Success Criteria
- The README reads like a product manual for a solo SaaS founder.
- No gaps requiring source code reading to get Prometheus/Grafana working.
- `mix hex.build` produces a clean package without internal planning docs.