---
phase: 02-http-ecto-oban
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/parapet/plug/metrics.ex
  - lib/parapet/metrics/http.ex
  - test/parapet/plug/metrics_test.exs
  - test/parapet/metrics/http_test.exs
autonomous: true
requirements: [HTTP-01, HTTP-02, HTTP-03, HTTP-04, HTTP-05, HTTP-06, ERR-02]
must_haves:
  truths:
    - HTTP requests generate parapet telemetry and metric counts using route patterns, not raw paths.
    - Route fallback uses _unknown for unhandled paths.
    - Startup metrics registration catches duplicate ArgumentError and logs rather than crashing.
  artifacts:
    - path: lib/parapet/plug/metrics.ex
      provides: HTTP route metrics using register_before_send
    - path: lib/parapet/metrics/http.ex
      provides: HTTP Prometheus counters and histograms
  key_links:
    - from: lib/parapet/plug/metrics.ex
      to: conn.private[:phoenix_route]
      via: register_before_send
---

<objective>
Implement the HTTP metrics collection surface for Parapet, enabling the telemetry pipeline to monitor application web traffic health without interfering with core application flows.

Purpose: Provide out-of-the-box visibility into web requests while ensuring stability and bounding label cardinality.
Output: Core metrics modules for HTTP, a Phoenix Plug, and corresponding test suites.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/2-http-ecto-oban/RESEARCH.md
@.planning/phases/2-http-ecto-oban/2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Scaffold HTTP Test Files</name>
  <files>test/parapet/plug/metrics_test.exs, test/parapet/metrics/http_test.exs</files>
  <action>Create the ExUnit test scaffolds for the HTTP plug and the HTTP metrics components to support TDD-style verification for subsequent tasks.</action>
  <verify>
    <automated>mix test test/parapet/plug test/parapet/metrics/http_test.exs</automated>
  </verify>
  <done>Both test files exist and run (even if 0 tests or failing).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Build HTTP Plug and Metrics</name>
  <files>lib/parapet/plug/metrics.ex, lib/parapet/metrics/http.ex, test/parapet/plug/metrics_test.exs, test/parapet/metrics/http_test.exs</files>
  <behavior>
    - Test 1: Plug emits [:parapet, :http, :request] telemetry with route from conn.private[:phoenix_route] and div(conn.status, 100).
    - Test 2: Plug defaults route to "_unknown" if no route matched.
    - Test 3: HTTP Metrics module registers counters and distributions via Telemetry.Metrics, wrapping in try/rescue ArgumentError to avoid crashes on reload.
  </behavior>
  <action>Implement `Parapet.Plug.Metrics` adopting the `Plug` behaviour. Use `Plug.Conn.register_before_send/2` to capture status and route without blocking. Implement `Parapet.Metrics.HTTP` to register Prometheus distributions and counters for HTTP latency and counts. When attaching telemetry, explicitly reference analog file paths from Phase 1, such as using `Parapet.attach/1` in `lib/parapet.ex` (which leverages `Parapet.Internal.SafeHandler` in `lib/parapet/internal/safe_handler.ex`) for safely attaching telemetry handlers. Additionally, ensure metric labels adhere to the strict cardinality policy by validating them via `Parapet.Internal.LabelPolicy.assert_safe!/1` before they are emitted or registered.</action>
  <verify>
    <automated>mix test test/parapet/plug/metrics_test.exs test/parapet/metrics/http_test.exs</automated>
  </verify>
  <done>Plug correctly extracts `phoenix_route`, maps status classes, metrics register safely using the label policy, and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | HTTP metrics process request payloads and headers, exposing vector for cardinality exhaustion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | Parapet.Plug.Metrics | mitigate | Use `conn.private[:phoenix_route]` directly and strictly avoid `conn.request_path` to prevent label cardinality explosion from raw paths. |
| T-02-02 | Denial of Service | Parapet.Metrics.HTTP | mitigate | Wrap `Telemetry.Metrics` registration in `try/rescue e in [ArgumentError]` to avoid initialization node crashes. Apply `LabelPolicy.assert_safe!/1`. |
</threat_model>

<verification>
mix test test/parapet/plug/metrics_test.exs test/parapet/metrics/http_test.exs
mix format --check-formatted
mix credo --strict
</verification>

<success_criteria>
All ExUnit tests for HTTP modules are passing.
Plug successfully extracts routes using `register_before_send/2`.
Application restarts or duplicate config do not trigger a boot crash (`ArgumentError` mitigated).
Label policy assertions guard the metric inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/2-http-ecto-oban/02-01-SUMMARY.md`
</output>